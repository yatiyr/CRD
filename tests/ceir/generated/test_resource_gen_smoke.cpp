// GENERATED by ceir_opgen.py from engine/ceir/ops/resource.ceirop.toml - DO NOT EDIT (schema v1). Regenerate with
// `python tools/ceir_opgen/ceir_opgen.py`; the crd-ceir-opgen-drift ctest fails on drift.
//
// CEIR-2d smoke gate for the `resource` dialect: every op self-registers, reflects a coherent schema, and
// its generated builder yields a verifier-accepted op - proven WITHOUT a hand-written line per op (a new
// op in the TOML grows this test on the next regen). Rich semantic tests live beside it (test_resource_gen.cpp).

#include <crd/ceir/ceir.hpp>
#include <crd/ceir/gen/resource_ops.hpp>

#include <crd/memory/allocators/malloc_allocator.hpp>

#include <catch2/catch_test_macros.hpp>

namespace
{
using namespace crd::ceir;

TEST_CASE("ceir resource gen smoke: the dialect self-registers and reflects a coherent schema",
          "[ceir][gen][smoke][resource]")
{
    crd::memory::MallocAllocator root;
    Context                      ctx(&root);
    Dialect* const               dlt = resource::register_resource_ops(ctx);
    REQUIRE(dlt != nullptr);
    CHECK(dlt->name() == crd::containers::StringView("resource"));

    const crd::containers::ConstSpan<OpSchema> schemas = resource::resource_op_schemas();
    REQUIRE(schemas.size() == 4U);

    crd::containers::StringView prev;
    for (const OpSchema& s : schemas)
    {
        if (!prev.empty()) { CHECK(prev < s.name); } // reflection is emitted sorted by name
        prev = s.name;
        const OpId reflected = ctx.intern_op(s.dialect, s.name);
        CHECK(ctx.intern_op(s.dialect, s.name) == reflected); // interning is idempotent
        CHECK(ctx.dialect_of(reflected) == dlt); // a reflected op is a registered kind of this dialect
        CHECK(s.qualified.size() == s.dialect.size() + 1U + s.name.size());
    }
}

TEST_CASE("ceir resource gen smoke: every op builds through its generated builder and the verifier accepts it",
          "[ceir][gen][smoke][resource]")
{
    crd::memory::MallocAllocator root;
    Context                      ctx(&root);
    (void)resource::register_resource_ops(ctx);

    // resource.declare
    {
        Operation* const op = resource::build_declare(ctx, ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // resource.export
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 1U, ctx.type_i32());
        Operation* const op = resource::build_export(ctx, src->result(0U), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // resource.import
    {
        Operation* const op = resource::build_import(ctx, ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // resource.view
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 2U, ctx.type_i32());
        Operation* const op = resource::build_view(ctx, src->result(0U), src->result(1U), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }
}

TEST_CASE("ceir resource gen smoke: the generated verifier rejects a malformed construction",
          "[ceir][gen][smoke][resource]")
{
    crd::memory::MallocAllocator root;
    Context                      ctx(&root);
    (void)resource::register_resource_ops(ctx);

    // resource.export
    {
        Operation* const bad = ctx.create_operation(resource::export_kind(ctx), {}, 0U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // resource.view
    {
        Operation* const bad = ctx.create_operation(resource::view_kind(ctx), {}, 1U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }
}
} // namespace
