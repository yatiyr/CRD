// GENERATED by ceir_opgen.py from engine/ceir/ops/frame.ceirop.toml - DO NOT EDIT (schema v1). Regenerate with
// `python tools/ceir_opgen/ceir_opgen.py`; the crd-ceir-opgen-drift ctest fails on drift.
//
// CEIR-2d smoke gate for the `frame` dialect: every op self-registers, reflects a coherent schema, and
// its generated builder yields a verifier-accepted op - proven WITHOUT a hand-written line per op (a new
// op in the TOML grows this test on the next regen). Rich semantic tests live beside it (test_frame_gen.cpp).

#include <crd/ceir/ceir.hpp>
#include <crd/ceir/gen/frame_ops.hpp>

#include <crd/memory/allocators/growable_tlsf_allocator.hpp>

#include <catch2/catch_test_macros.hpp>

namespace
{
using namespace crd::ceir;

TEST_CASE("ceir frame gen smoke: the dialect self-registers and reflects a coherent schema",
          "[ceir][gen][smoke][frame]")
{
    crd::memory::GrowableTlsfAllocator root;
    Context                      ctx(&root);
    Dialect* const               dlt = frame::register_frame_ops(ctx);
    REQUIRE(dlt != nullptr);
    CHECK(dlt->name() == crd::containers::StringView("frame"));

    const crd::containers::ConstSpan<OpSchema> schemas = frame::frame_op_schemas();
    REQUIRE(schemas.size() == 4U);

    crd::containers::StringView prev;
    for (const OpSchema& s : schemas)
    {
        if (!prev.empty()) { CHECK(prev < s.name); } // reflection is emitted sorted by name
        prev = s.name;
        const OpId reflected = ctx.intern_op(s.dialect, s.name);
        CHECK(ctx.intern_op(s.dialect, s.name) == reflected); // interning is idempotent
        CHECK(ctx.dialect_of(reflected) == dlt); // a reflected op is a registered kind of this dialect
        CHECK(s.qualified.size() == s.dialect.size() + 1U + s.name.size());
    }
}

TEST_CASE("ceir frame gen smoke: every op builds through its generated builder and the verifier accepts it",
          "[ceir][gen][smoke][frame]")
{
    crd::memory::GrowableTlsfAllocator root;
    Context                      ctx(&root);
    (void)frame::register_frame_ops(ctx);

    // frame.draw_list
    {
        Operation* const op = frame::build_draw_list(ctx, ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // frame.graph
    {
        Operation* const op = frame::build_graph(ctx, ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // frame.history
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 1U, ctx.type_i32());
        Operation* const op = frame::build_history(ctx, src->result(0U), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // frame.pass
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 1U, ctx.type_i32());
        Operation* const op = frame::build_pass(ctx, src->result(0U), ctx.attr_symbol("x"), ctx.attr_string("x"), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }
}

TEST_CASE("ceir frame gen smoke: the generated verifier rejects a malformed construction",
          "[ceir][gen][smoke][frame]")
{
    crd::memory::GrowableTlsfAllocator root;
    Context                      ctx(&root);
    (void)frame::register_frame_ops(ctx);

    // frame.graph
    {
        Operation* const bad = ctx.create_operation(frame::graph_kind(ctx), {}, 0U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // frame.history
    {
        Operation* const bad = ctx.create_operation(frame::history_kind(ctx), {}, 1U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // frame.pass
    {
        Operation* const bad = ctx.create_operation(frame::pass_kind(ctx), {}, 0U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }
}
} // namespace
