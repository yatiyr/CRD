// GENERATED by ceir_opgen.py from engine/ceir/ops/rt.ceirop.toml - DO NOT EDIT (schema v1). Regenerate with
// `python tools/ceir_opgen/ceir_opgen.py`; the crd-ceir-opgen-drift ctest fails on drift.
//
// CEIR-2d smoke gate for the `rt` dialect: every op self-registers, reflects a coherent schema, and
// its generated builder yields a verifier-accepted op - proven WITHOUT a hand-written line per op (a new
// op in the TOML grows this test on the next regen). Rich semantic tests live beside it (test_rt_gen.cpp).

#include <crd/ceir/ceir.hpp>
#include <crd/ceir/gen/rt_ops.hpp>

#include <crd/memory/allocators/growable_tlsf_allocator.hpp>

#include <catch2/catch_test_macros.hpp>

namespace
{
using namespace crd::ceir;

TEST_CASE("ceir rt gen smoke: the dialect self-registers and reflects a coherent schema",
          "[ceir][gen][smoke][rt]")
{
    crd::memory::GrowableTlsfAllocator root;
    Context                      ctx(&root);
    Dialect* const               dlt = rt::register_rt_ops(ctx);
    REQUIRE(dlt != nullptr);
    CHECK(dlt->name() == crd::containers::StringView("rt"));

    const crd::containers::ConstSpan<OpSchema> schemas = rt::rt_op_schemas();
    REQUIRE(schemas.size() == 6U);

    crd::containers::StringView prev;
    for (const OpSchema& s : schemas)
    {
        if (!prev.empty()) { CHECK(prev < s.name); } // reflection is emitted sorted by name
        prev = s.name;
        const OpId reflected = ctx.intern_op(s.dialect, s.name);
        CHECK(ctx.intern_op(s.dialect, s.name) == reflected); // interning is idempotent
        CHECK(ctx.dialect_of(reflected) == dlt); // a reflected op is a registered kind of this dialect
        CHECK(s.qualified.size() == s.dialect.size() + 1U + s.name.size());
    }
}

TEST_CASE("ceir rt gen smoke: every op builds through its generated builder and the verifier accepts it",
          "[ceir][gen][smoke][rt]")
{
    crd::memory::GrowableTlsfAllocator root;
    Context                      ctx(&root);
    (void)rt::register_rt_ops(ctx);

    // rt.blas_build
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 1U, ctx.type_i32());
        Operation* const op = rt::build_blas_build(ctx, src->result(0U), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // rt.instance_populate
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 2U, ctx.type_i32());
        Operation* const op = rt::build_instance_populate(ctx, src->result(0U), src->result(1U), ctx.attr_int(0), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // rt.ray_query
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 5U, ctx.type_i32());
        Operation* const op = rt::build_ray_query(ctx, src->result(0U), src->result(1U), src->result(2U), src->result(3U), src->result(4U), ctx.attr_symbol("x"), ctx.attr_string("x"), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // rt.sbt_build
    {
        Operation* const op = rt::build_sbt_build(ctx, ctx.attr_symbol("x"), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // rt.tlas_build
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 1U, ctx.type_i32());
        Operation* const op = rt::build_tlas_build(ctx, src->result(0U), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // rt.trace
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 6U, ctx.type_i32());
        Operation* const op = rt::build_trace(ctx, src->result(0U), src->result(1U), src->result(2U), src->result(3U), src->result(4U), src->result(5U), ctx.attr_string("x"), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }
}

TEST_CASE("ceir rt gen smoke: the generated verifier rejects a malformed construction",
          "[ceir][gen][smoke][rt]")
{
    crd::memory::GrowableTlsfAllocator root;
    Context                      ctx(&root);
    (void)rt::register_rt_ops(ctx);

    // rt.blas_build
    {
        Operation* const bad = ctx.create_operation(rt::blas_build_kind(ctx), {}, 1U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // rt.instance_populate
    {
        Operation* const bad = ctx.create_operation(rt::instance_populate_kind(ctx), {}, 1U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // rt.ray_query
    {
        Operation* const bad = ctx.create_operation(rt::ray_query_kind(ctx), {}, 0U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // rt.sbt_build
    {
        Operation* const bad = ctx.create_operation(rt::sbt_build_kind(ctx), {}, 1U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // rt.tlas_build
    {
        Operation* const bad = ctx.create_operation(rt::tlas_build_kind(ctx), {}, 1U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // rt.trace
    {
        Operation* const bad = ctx.create_operation(rt::trace_kind(ctx), {}, 0U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }
}
} // namespace
