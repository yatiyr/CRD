// GENERATED by ceir_opgen.py from engine/ceir/ops/render.ceirop.toml - DO NOT EDIT (schema v1). Regenerate with
// `python tools/ceir_opgen/ceir_opgen.py`; the crd-ceir-opgen-drift ctest fails on drift.
//
// CEIR-2d smoke gate for the `render` dialect: every op self-registers, reflects a coherent schema, and
// its generated builder yields a verifier-accepted op - proven WITHOUT a hand-written line per op (a new
// op in the TOML grows this test on the next regen). Rich semantic tests live beside it (test_render_gen.cpp).

#include <crd/ceir/ceir.hpp>
#include <crd/ceir/gen/render_ops.hpp>

#include <crd/memory/allocators/growable_tlsf_allocator.hpp>

#include <catch2/catch_test_macros.hpp>

namespace
{
using namespace crd::ceir;

TEST_CASE("ceir render gen smoke: the dialect self-registers and reflects a coherent schema",
          "[ceir][gen][smoke][render]")
{
    crd::memory::GrowableTlsfAllocator root;
    Context                      ctx(&root);
    Dialect* const               dlt = render::register_render_ops(ctx);
    REQUIRE(dlt != nullptr);
    CHECK(dlt->name() == crd::containers::StringView("render"));

    const crd::containers::ConstSpan<OpSchema> schemas = render::render_op_schemas();
    REQUIRE(schemas.size() == 11U);

    crd::containers::StringView prev;
    for (const OpSchema& s : schemas)
    {
        if (!prev.empty()) { CHECK(prev < s.name); } // reflection is emitted sorted by name
        prev = s.name;
        const OpId reflected = ctx.intern_op(s.dialect, s.name);
        CHECK(ctx.intern_op(s.dialect, s.name) == reflected); // interning is idempotent
        CHECK(ctx.dialect_of(reflected) == dlt); // a reflected op is a registered kind of this dialect
        CHECK(s.qualified.size() == s.dialect.size() + 1U + s.name.size());
    }
}

TEST_CASE("ceir render gen smoke: every op builds through its generated builder and the verifier accepts it",
          "[ceir][gen][smoke][render]")
{
    crd::memory::GrowableTlsfAllocator root;
    Context                      ctx(&root);
    (void)render::register_render_ops(ctx);

    // render.color_attachment
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 1U, ctx.type_i32());
        Operation* const op = render::build_color_attachment(ctx, src->result(0U), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // render.depth_attachment
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 1U, ctx.type_i32());
        Operation* const op = render::build_depth_attachment(ctx, src->result(0U), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // render.draw
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 3U, ctx.type_i32());
        Operation* const op = render::build_draw(ctx, src->result(0U), src->result(1U), src->result(2U), ctx.attr_symbol("x"), ctx.attr_string("x"), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // render.draw_indexed
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 4U, ctx.type_i32());
        Operation* const op = render::build_draw_indexed(ctx, src->result(0U), src->result(1U), src->result(2U), src->result(3U), ctx.attr_symbol("x"), ctx.attr_string("x"), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // render.draw_indirect
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 2U, ctx.type_i32());
        Operation* const op = render::build_draw_indirect(ctx, src->result(0U), src->result(1U), ctx.attr_symbol("x"), ctx.attr_string("x"), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // render.draw_indirect_count
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 3U, ctx.type_i32());
        Operation* const op = render::build_draw_indirect_count(ctx, src->result(0U), src->result(1U), src->result(2U), ctx.attr_symbol("x"), ctx.attr_string("x"), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // render.mesh_dispatch
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 4U, ctx.type_i32());
        Operation* const op = render::build_mesh_dispatch(ctx, src->result(0U), src->result(1U), src->result(2U), src->result(3U), ctx.attr_symbol("x"), ctx.attr_string("x"), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // render.mesh_dispatch_indirect
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 2U, ctx.type_i32());
        Operation* const op = render::build_mesh_dispatch_indirect(ctx, src->result(0U), src->result(1U), ctx.attr_symbol("x"), ctx.attr_string("x"), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // render.mesh_dispatch_list
    {
        Operation* const op = render::build_mesh_dispatch_list(ctx, ctx.attr_symbol("x"), ctx.attr_string("x"), ctx.attr_string("x"), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // render.scene_draw_list
    {
        Operation* const op = render::build_scene_draw_list(ctx, ctx.attr_symbol("x"), ctx.attr_string("x"), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // render.scope
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 1U, ctx.type_i32());
        Operation* const op = render::build_scope(ctx, src->result(0U), ctx.attr_int(0), ctx.attr_int(0), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }
}

TEST_CASE("ceir render gen smoke: the generated verifier rejects a malformed construction",
          "[ceir][gen][smoke][render]")
{
    crd::memory::GrowableTlsfAllocator root;
    Context                      ctx(&root);
    (void)render::register_render_ops(ctx);

    // render.color_attachment
    {
        Operation* const bad = ctx.create_operation(render::color_attachment_kind(ctx), {}, 1U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // render.depth_attachment
    {
        Operation* const bad = ctx.create_operation(render::depth_attachment_kind(ctx), {}, 1U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // render.draw
    {
        Operation* const bad = ctx.create_operation(render::draw_kind(ctx), {}, 0U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // render.draw_indexed
    {
        Operation* const bad = ctx.create_operation(render::draw_indexed_kind(ctx), {}, 0U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // render.draw_indirect
    {
        Operation* const bad = ctx.create_operation(render::draw_indirect_kind(ctx), {}, 0U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // render.draw_indirect_count
    {
        Operation* const bad = ctx.create_operation(render::draw_indirect_count_kind(ctx), {}, 0U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // render.mesh_dispatch
    {
        Operation* const bad = ctx.create_operation(render::mesh_dispatch_kind(ctx), {}, 0U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // render.mesh_dispatch_indirect
    {
        Operation* const bad = ctx.create_operation(render::mesh_dispatch_indirect_kind(ctx), {}, 0U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // render.mesh_dispatch_list
    {
        Operation* const bad = ctx.create_operation(render::mesh_dispatch_list_kind(ctx), {}, 0U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // render.scene_draw_list
    {
        Operation* const bad = ctx.create_operation(render::scene_draw_list_kind(ctx), {}, 0U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // render.scope
    {
        Operation* const bad = ctx.create_operation(render::scope_kind(ctx), {}, 0U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }
}
} // namespace
