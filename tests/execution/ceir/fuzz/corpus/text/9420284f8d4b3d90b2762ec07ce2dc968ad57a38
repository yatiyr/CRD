module {
  ^bb0(%0 : !i32):
    %1 = test.add(%0, %0) {big = 1e+20, flag = true, frac = 4.5, i = -42, s = "he said \"hi\\\n", sym = @target, ty = !struct<Point,x:!i32,y:!f32>, ty2 = !fn<(!param<T,!trait<Ord>>)->(!option<!i32>)>, ty3 = !view<!image<d2,!vec<4x!f32>>,mip,layer>, ty4 = !tensor<!f32,!shape<!dim<4>,!dim<N>,!dim<dyn>>>, ty5 = !qty<!vec<3x!f32>,L1>, ty6 = !qual<borrow,!buffer<plain,!f32>>, whole = 4.0} : !vec<4x!f32>
    %2, %3 = test.split(%1)
    scf.region(%1) {
      ^bb0(%4 : !i32):
        test.use(%1, %4) {note = "has a { brace"}
    }
    test.empty() {
    }
    cf.multiblock() {
      ^bb5:
        cf.br()
      ^bb1(%5 : !f32, %6 : !f32):
        test.sink(%5)
    }
    plugin.wIdget()
    test.consumer(%7)
    %7 = test.producer() : !i32
    func.func() {sym_name = "callee_fn"} {
      ^bb0(%8 : !i32):
        func.rejurn(%8)
    }
    %9 = func-call(%0) {callee = @callee_fn} : !i32
}
