// GENERATED by ceir_opgen.py from engine/execution/ceir/ops/audio.ceirop.toml - DO NOT EDIT (schema v1). Regenerate with
// `python tools/ceir_opgen/ceir_opgen.py`; the crd-ceir-opgen-drift ctest fails on drift.
//
// CEIR-2d smoke gate for the `audio` dialect: every op self-registers, reflects a coherent schema, and
// its generated builder yields a verifier-accepted op - proven WITHOUT a hand-written line per op (a new
// op in the TOML grows this test on the next regen). Rich semantic tests live beside it (test_audio_gen.cpp).

#include <crd/ceir/ceir.hpp>
#include <crd/ceir/gen/audio_ops.hpp>

#include <crd/memory/allocators/growable_tlsf_allocator.hpp>

#include <catch2/catch_test_macros.hpp>

namespace
{
using namespace crd::ceir;

TEST_CASE("ceir audio gen smoke: the dialect self-registers and reflects a coherent schema",
          "[ceir][gen][smoke][audio]")
{
    crd::memory::GrowableTlsfAllocator root;
    Context                      ctx(&root);
    Dialect* const               dlt = audio::register_audio_ops(ctx);
    REQUIRE(dlt != nullptr);
    CHECK(dlt->name() == crd::containers::StringView("audio"));

    const crd::containers::ConstSpan<OpSchema> schemas = audio::audio_op_schemas();
    REQUIRE(schemas.size() == 7U);

    crd::containers::StringView prev;
    for (const OpSchema& s : schemas)
    {
        if (!prev.empty()) { CHECK(prev < s.name); } // reflection is emitted sorted by name
        prev = s.name;
        const OpId reflected = ctx.intern_op(s.dialect, s.name);
        CHECK(ctx.intern_op(s.dialect, s.name) == reflected); // interning is idempotent
        CHECK(ctx.dialect_of(reflected) == dlt); // a reflected op is a registered kind of this dialect
        CHECK(s.qualified.size() == s.dialect.size() + 1U + s.name.size());
    }
}

TEST_CASE("ceir audio gen smoke: every op builds through its generated builder and the verifier accepts it",
          "[ceir][gen][smoke][audio]")
{
    crd::memory::GrowableTlsfAllocator root;
    Context                      ctx(&root);
    (void)audio::register_audio_ops(ctx);

    // audio.biquad
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 1U, ctx.type_i32());
        Operation* const op = audio::build_biquad(ctx, src->result(0U), ctx.attr_int(0), ctx.attr_float(0.0), ctx.attr_float(0.0), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // audio.compressor
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 1U, ctx.type_i32());
        Operation* const op = audio::build_compressor(ctx, src->result(0U), ctx.attr_float(0.0), ctx.attr_float(0.0), ctx.attr_float(0.0), ctx.attr_float(0.0), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // audio.delay
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 1U, ctx.type_i32());
        Operation* const op = audio::build_delay(ctx, src->result(0U), ctx.attr_int(0), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // audio.gain
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 1U, ctx.type_i32());
        Operation* const op = audio::build_gain(ctx, src->result(0U), ctx.attr_float(0.0), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // audio.mix
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 1U, ctx.type_i32());
        Operation* const op = audio::build_mix(ctx, src->result(0U), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // audio.send
    {
        Operation* const src = ctx.create_operation(ctx.intern_op("smoke", "src"), {}, 1U, ctx.type_i32());
        Operation* const op = audio::build_send(ctx, src->result(0U), ctx.attr_float(0.0), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }

    // audio.source
    {
        Operation* const op = audio::build_source(ctx, ctx.attr_symbol("x"), ctx.attr_int(0), ctx.attr_int(0), ctx.type_i32());
        CHECK(op != nullptr);
        CHECK(ctx.verify(*op));
    }
}

TEST_CASE("ceir audio gen smoke: the generated verifier rejects a malformed construction",
          "[ceir][gen][smoke][audio]")
{
    crd::memory::GrowableTlsfAllocator root;
    Context                      ctx(&root);
    (void)audio::register_audio_ops(ctx);

    // audio.biquad
    {
        Operation* const bad = ctx.create_operation(audio::biquad_kind(ctx), {}, 1U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // audio.compressor
    {
        Operation* const bad = ctx.create_operation(audio::compressor_kind(ctx), {}, 1U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // audio.delay
    {
        Operation* const bad = ctx.create_operation(audio::delay_kind(ctx), {}, 1U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // audio.gain
    {
        Operation* const bad = ctx.create_operation(audio::gain_kind(ctx), {}, 1U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // audio.send
    {
        Operation* const bad = ctx.create_operation(audio::send_kind(ctx), {}, 1U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }

    // audio.source
    {
        Operation* const bad = ctx.create_operation(audio::source_kind(ctx), {}, 1U, ctx.type_i32());
        CHECK_FALSE(ctx.verify(*bad));
    }
}
} // namespace
