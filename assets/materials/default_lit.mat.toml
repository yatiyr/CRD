[material]
name      = "default_lit"
domain    = "Surface"
alpha_mode = "Opaque"

[shaders]
vertex   = "assets/shaders/surface.vert"
fragment = "assets/shaders/surface.frag"

[defaults]
# No explicit parameters — all SurfaceData fields use hardcoded defaults
# in crd_evaluate_surface() (base_color=1, roughness=1, metallic=0).
