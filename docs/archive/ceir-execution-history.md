# CEIR execution history

<!-- doc-role: archive -->
> Historical reference; old status/schedules/grants are not current instructions. Current work: [ROADMAP](../ROADMAP.md); current rules: [AGENTS](../../AGENTS.md).

> CEIR-1…35 closed on their recorded contracts. This is historical evidence, not an active loop or tracker.
> All new work and current status: [ROADMAP](../ROADMAP.md).

# D-007 · CEIR — THE MASTER TICK TRACKER

> **CEIR execution-spine history (bands 1–35 closed, 2026-09-11).** Future renderer/UI work and RAH status
> live in the [renderer/UI master plan](../ROADMAP.md#master-table). The CEIR-era ordering and
> next-step prose below are historical. One row per sub-slice; first column is the tick:
> `✅` done · `◧` in progress · `⬜` todo · `◀ NEXT` marks the front. **A row ticks ✅ ONLY at its GATE** — never
> because code or a "head" exists (the D-007 house rule).
>
> ### The three documents, and which one wins
> | Document | Role | When you read it |
> |---|---|---|
> | **Mission constitution** — [`../research/2026-08-07-ceir-universal-programming-master-roadmap.md`](../research/2026-08-07-ceir-universal-programming-master-roadmap.md) (§0–§185) | **THE LAW.** The full architecture, every contract, every rule. | Read the §§ a row cites **before implementing that row.** |
> | **This tracker** | THE INDEX + tick state. Summarizes each slice and cites its §§; never restates the law. | To see what's next and what's done. |
> | **Per-band design spec** — `docs/design/ceir-<band>-<slug>.md` (written at band OPEN) | The reuse audit (what already exists, file:line) + sequenced increments + per-increment gate. The house convention (see `docs/design/README.md`). | When you were told "read the slice and implement it." |
> | **Landed history** — [`D-007-gpu-program-system.md`](D-007-gpu-program-system.md) | The CKIR/RAF/RET ledger + the pre-CEIR post-RAF plan. Its live-tracking role **MOVED here at CEIR-0f (2026-08-08)** — that doc's master table is now historical. | For "how did we get here." |
>
> **Citation convention:** `§NN` = section NN of the mission constitution. Its headings are `# NN. TITLE`, so
> searching `# 39.` jumps to §39. A row's `→ §§` list is the *complete* set of sections that define its contract.

---

# Architecture in one screen — READ THIS BEFORE THE ROWS

**The one sentence (§1 north star).** Every reusable algorithm expressible from capabilities Cerid *already*
understands must be a **versioned, inspectable, serializable, hot-reloadable program asset** — whether it does
rendering, GPU compute, ray tracing, ML/AI, scientific computing, media, geometry, UI, physics, animation, audio,
or application logic. New native C++ is required **only** to introduce a genuinely new *host capability / OS
integration / device provider / hardware primitive* — never merely because someone invented a new *algorithm* from
existing capabilities.

**The mantra (§1).**
```
ALGORITHMS ARE PROGRAM ASSETS.   CAPABILITIES ARE NATIVE PRIMITIVES.
COMPILERS CHOOSE LOWERINGS.      BACKENDS EXECUTE.
```

**The stack (§3) — five layers, one-way lowering.**
```
AUTHORING   text · CR-D007 visual · TOML/domain frontends · C++ builder · importers      §9 §11 §121 §124
      │     (all projections emit the SAME canonical IR — there is no privileged path)     §121 §166
      ▼
CHIR        Cerid High-level IR — future language layer (modules, generics, ADTs,          §4  (design-only now;
      │     closures, traits, ownership, async/state/events). Lowers/erases into CEIR.          implemented CEIR-32)
      ▼
CEIR        Cerid Execution IR — typed SSA + graph/CFG/structured regions; resources,       §2 §12 §13
      │     effects, capabilities, evaluation domains; ~30 open-world dialects (§33–§68).
      ├───────────────────────────┐
      ▼                           ▼
CKIR (per-invocation kernel math,  EXECUTION PROVIDERS (CPU/GPU/NPU/media/remote —          §69 §85
 the shipped shader/kernel IR)      native graph/program APIs; advertise ops/costs)
      └───────────────┬───────────┘
                      ▼
COMPILED EXECUTION PLAN  →  Vulkan · D3D12 · CUDA · HIP · Metal · WebGPU · CPU/SIMD/jobs     §144 §153
                            · codecs · NPU · remote nodes
```

**The three IRs, crisply.**
- **CKIR** *(exists, 100% of the engine, bit-exact)* — **what one GPU invocation computes.** Per-thread shader/
  kernel math. Unchanged by this detour; CEIR references CKIR programs by asset identity and may *generate* CKIR
  during lowering (§85 §86).
- **CEIR** *(this detour)* — **what work exists, what data/resources flow, what controls execution, where work can
  run, how work generates more work, how resources synchronize, and which lower program implements each op** (§2).
- **CHIR** *(design-only now, §4)* — **source-language semantics** (generics, ADTs, closures, ownership, reflection)
  that must NOT be baked into an execution IR. Lowers/erases into CEIR. Implemented at CEIR-32, after the CEIR
  corpus exists to teach the language its ergonomics.

**Why it is NOT a giant enum (§6 §7).** Completeness comes from **open-world extensibility**, not a finite opcode
list. CEIR is MLIR-shaped: `Operation` + typed operands/results + attributes + **regions** + **dialects**
(registered op sets) + **interfaces/traits** — analyses dispatch on interfaces, never `switch(op.kind)`. A new
algorithm is a new *asset*; a new abstraction is a CEIR function or a custom op that *lowers* to existing ops
(§101 Level A/B); only a genuinely new hardware/host primitive is native C++ (§100, §101 Level C). ⛔ A giant
`enum CeirOp { Draw, Dispatch, ForwardPlus, Deferred, Nanite, Lumen … }` is the FORBIDDEN destination — it repeats
the exact `FramePassKind` combinatorial mistake RAF-12 just deleted.

**What stays native FOREVER (§177):** backend API calls, OS/window, filesystem/network impl, device enumeration,
swapchain acquire/present, vendor compiler invocation, hardware codec/NPU providers, native sensors, the job-system
impl, the compiler/runtime itself. **What must NOT stay native-only (§178):** renderer architectures, culling,
lighting, post-processing, GI, RT workflows, compute chains, numerical workflows, tensor/ML graphs, media chains,
UI effect graphs, geometry pipelines.

**Visual = a projection, not a runtime (§9 §10 §166).** Visual and textual authoring are two lenses on ONE typed
semantic program. Graph coordinates/edges are UI; canonical semantics are operations/values/regions/symbols. A
user function with a typed interface automatically becomes a node (§11). Lenses: dataflow · structured control-flow
· frame/workflow · state-machine · reactive/event · timeline (§9).

### Where the architecture is defined — the jump table
| To understand… | Research §§ |
|---|---|
| North star + mantra · CEIR vs CKIR vs CHIR · the stack | §1 · §2 §4 §85 · §3 |
| Open-world / no-enum / dialects · table-driven op defs (ODS) | §6 §7 §101 · §8 |
| Visual = source projection · lenses · text/graph round-trip · user-fn→node | §9 · §9 · §10 §166 · §11 |
| Typed SSA · regions (graph/CFG/structured) · complete control flow | §12 · §13 · §14 |
| Evaluation domains + partial evaluation | §15 §73 |
| Types · units · semantic-domain types · ownership · explicit state · shapes · layout | §16 · §17 · §18 · §19 · §20 · §21 · §22 |
| Resource types · memory domains · residency/streaming | §23 · §24 · §25 |
| Effects · determinism · numerical semantics · error model · concurrency | §26 · §27 · §28 · §29 · §30 |
| Jobs/fiber integration · real-time domains | §31 · §32 |
| The ~30 dialects (`ceir.*`) | §33–§68 |
| Providers · native graph backends · provider partitioning · sharding | §69 · §70 · §102 · §103 |
| Optimizer (partial-eval/DCE/CSE/fusion/split/memory) · transform/rewrite · autotune/PGO/cost | §73–§78 · §71 §72 §146 · §80 §81 §82 |
| CPU/tiered host execution · CKIR integration · CKIR frontier features | §83 §84 · §85 · §86 |
| Expressiveness corpora (renderer/shadow/GI/RT/vgeo/compute/hesap/ML/media/UI/audio) | §87–§97 |
| Capability security · native intrinsics · custom dialect/plugin levels | §99 · §100 · §101 |
| Asset model · deps · interface hash · hot reload · state migration · incremental | §105 · §106 · §107 · §108 · §109 · §110 |
| Provenance · debugger · profiler · capture/replay | §111 · §112 · §113 · §114 |
| Validation layers · race detection · certification hooks | §115 · §116 · §117 |
| Reference executor · fuzz/differential · transformation correctness | §118 · §119 · §120 |
| Programmatic API · CLI/RPC/MCP · source control/diff/merge · import/export | §121 · §122 · §123 · §124 |
| Material/technique relationship · command-model & framegraph relationship | §125 · §158 §159 |
| Migrations: framegraph · executor | §126 · §127 |
| The PROOFS (scene.raster→script) | §128–§143 |
| Compiler pipeline · multi-level IR snapshots · frontier transform schedules | §144 · §145 · §146 |
| Capability system · required/preferred/optional · quality tiers | §147 · §148 · §149 |
| Hot-path rules · editor-mode richness · specialization | §153 · §154 · §155 |
| Resource binding model · global resource tables | §156 · §157 |
| Editor views · node UX · large-graph scalability | §163 · §164 · §165 |
| Test matrices (IR/compiler/render/compute/tensor-ML/hot-reload) | §167–§172 |
| Maturity model (L0–L8) · machine-readable manifest | §173 · §174 |
| The implementation bands (36 after the 2026-08-09 re-baseline: 0–35) · pause policy · native-forever vs must-not | §175 · §176 · §177 §178 |
| Definitions of Done (universality/visual/scripting/GPU) | §179 · §180 · §181 · §182 |
| Reference reading (MLIR/IREE/XLA/Triton/DX/Vulkan/Slang) | §183 |

**House rules on EVERY row** (not repeated per row): crd containers only (`IAllocator` ctor arg, no std) ·
serialization is FIELD-BY-FIELD, never raw-POD memcpy (the padding/content-hash scar) · ASCII test names · run
`ctest`, never the bare binary · tidy per touched file · GPU gates = both backends + `crd::gpu::ValidationCapture` ·
parallel results carry the `{1..16}` determinism moat · Allman / 4-space / 120-col.

**Naming note:** the CEIR-1 C++ type names are **FINALIZED by ADR-0109 §5** (`crd::ceir::{Context, Module,
Operation, Value, Block, Region, SymbolTable, Type, Dialect, OpId, SourceLoc, KernelRef, IExecutionProvider,
ModuleBuilder}`) — no longer a working proposal. Later-band op/dialect names remain proposals until their band opens.

**Row-refinement rule:** CEIR-0…16 carry full sub-slice rows now; CEIR-17…35 carry one gated row each and split
into sub-slices at band OPEN, sized by CEIR-0z's estimates (far-band detail is not invented before the inventory).

**Pause policy (§176):** until CEIR-16z, allowed work = bug fixes · unsafe half-migration completion · CKIR fixes ·
tests · docs · RAH-1/2 · CEIR slices. Parked work is tabled at the bottom so nothing is silently dropped.

---

## Parallel track — RAH

**Future RAH status and implementation contracts moved to the
[renderer/UI master plan §5](../ROADMAP.md#master-table)
on 2026-09-11.** Do not maintain a second live RAH table here.

Historical CEIR-era state: RAH-0 audit was pending review; RAH-1a.1 closed the visbuffer fold
(`ColorAttachmentDesc` float/uint clear; `RenderingDesc.visbuffer/clear_id` deleted), with recorded
REN-38-F6 97 assertions on both backends plus the Vulkan visbuffer gate. RAH-1a.2 (legacy G-buffer deletion),
RAH-1a-close and RAH-2 were open. The new plan preserves those IDs and explicitly accounts for the
remaining full RAH-1 parent contract, which this narrow sub-row list did not enumerate.

## CEIR-0 — Repository inventory + architecture ADRs — ✅ **CLOSED** (0a–0z all ACCEPTED by 2026-08-08; header was stale ⬜ — fixed at the 2026-08-09 re-baseline)  → §175 band 0

**Band contract (§175 CEIR-0).** No code. Produce the ground-truth map of every execution-program representation in
the tree (from CODE, §127), the three ADR drafts + the CHIR design note, and restructure D-007 so exactly one live
tracker exists. Close = the mission doc's **§184 fifteen-item report**, answered from evidence, plus honest KLOC/
duration sizing for CEIR-1…13 *(pre-re-baseline numbering; old 1…13 = new 1…7 + 11…16 — see the 2026-08-09 mapping
table below)*. **Gate: no ambiguous ownership remains.**

| ✔ | Slice | Deliverable | Gate | → §§ |
|---|---|---|---|---|
| ✅ | CEIR-0a | **Execution-path inventory FROM CODE** — [`docs/design/ceir-0a-execution-path-inventory.md`](../design/ceir-0a-execution-path-inventory.md). Every execution-program representation inventoried + classified with file:line evidence (the 14 executors + their `record_*` lowerings, the ONE frame-graph runtime [verified from code], `scene_renderer.cpp`'s 9-block `render()` composite catalog + program hand-list, `IComputeContext` consumers [CKIR backend + bvh-gpu 24 sites + 258 test occ.], the five cookers, `.crdr`, draw, media). **Headline: RAF already did the atomic-vs-composite split** — CEIR is a promotion, not a rewrite (executors = atomic verbs but mechanically pre-§6/§8; frame graph = composite sequencer → `ceir.frame`; scene_renderer's C++ orchestration = the real CEIR-16 target; `visbuffer.raster` = a residual special-case CEIR-14a dissolves). CEIR-16 sizing now concrete: 9 `render()` blocks + hand-list, not a 6610-line file. | ✅ table w/ file:line evidence; zero "per the docs"; §9 empty | §127 §128 |
| ✅ | CEIR-0b | **Scripting-direction ADR** — [`docs/decisions/0108-…md`](../decisions/0108-ceir-owned-language-stack-supersedes-cpp-only-scripting.md) (ADR-0108) — **ACCEPTED 2026-08-07 (user-approved).** **Surgically** supersedes ADR-0081 §9 ONLY (C++-is-ONLY clause); §1-§8 (agent-native CLI/RPC/MCP) reaffirmed. C++ stays first-class native + hot-reload; Cerid gains an OWNED textual/visual language stack; no Lua/Python/JS runtime; CLI/RPC/MCP stays source-of-truth; program assets agent-authorable. Non-negotiables pinned (§98/§99): no mandatory GC in hot paths · deterministic time/RNG · capability security · C++ FFI. ✅ **EXECUTED 2026-08-10 at CEIR-13z (the first CEIR vertical slice, §5 gate 2):** the cornerstone flip (PRINCIPLES/AGENTS/README/ROADMAP C++-only → Cerid-owned CEIR/CHIR + CR-D007-visual) + the ADR-0081 §9 in-file strike (struck-in-place + `Superseded-by: ADR-0108` header line + the decisions-README rows) + forward-pointers on the secondary live-rule carriers (phase-3.1.6, phase-4.0, ADR-0065 D17, the ROADMAP historical banner). Awaiting the user's ONE ratification commit (ADR-0108 §7 — the commit IS the ratification gate). | ✅ ADR accepted + flip executed | §5 §98 §99 |
| ✅ | CEIR-0c | **CEIR/CHIR/CKIR ownership ADR** — [`docs/decisions/0109-…md`](../decisions/0109-ceir-chir-ckir-ownership-and-module-placement.md) (ADR-0109) — ✅ **ACCEPTED.** Layer contract (CEIR=orchestration; CKIR=kernels, unchanged, referenced by `KernelRef` identity; CHIR=reserved → CEIR-32); one-way lowering; **`engine/ceir` (crd-ceir) host-only, deps core/log/memory/containers/units ONLY** — acyclic by construction; GPU/jobs via **dependency inversion**: abstract `IExecutionProvider` in crd-ceir, impls in bridge modules **`crd-ceir-host`** (→jobs) + **`crd-ceir-gpu`** (→gpu-context/render-graph/kir, where CEIR-0a `record_*`+CKIR-compile STAY). Invariants I3/I4/I5 (extend ADR-0103 I1/I2). Finalized CEIR-1 names + §10 semantic-identity model. **Gates CEIR-1.** | ✅ **ACCEPTED 2026-08-07** (gates CEIR-1; 0d/0e can draft in parallel) | §2 §4 §85 §10 §69 |
| ✅ | CEIR-0d | **Native-intrinsic ADR** — [`docs/decisions/0110-…md`](../decisions/0110-native-intrinsic-schema-and-plugin-levels.md) (ADR-0110) — ✅ **ACCEPTED.** An intrinsic = an ordinary CEIR-2 op + §100 native-binding metadata + a bridge handler (`IntrinsicRegistry` in crd-ceir, ADR-0109). Legitimacy IFF test (capability=intrinsic · algorithm=program · **composable-but-slow=provider, never Level C**); plugin levels A/B/C. Classifies the CEIR-0a atomic set (`present`/codecs/resolvers ✅; `submit_overlay`→`ceir.render` @ CEIR-14). Registry lands after CEIR-4; first shipped `present` @ CEIR-15. | ✅ **ACCEPTED 2026-08-07** (gates CEIR-2a field set + CEIR-16c; NOT critical path) | §100 §101 |
| ✅ | CEIR-0e | **CHIR-0 language design NOTE** (not an ADR; design-only) — [`docs/design/ceir-0e-chir-0-language-design-note.md`](../design/ceir-0e-chir-0-language-design-note.md) — ✅ **ACCEPTED; ZERO implementation.** §98 feature set (erased-vs-lowered-vs-runtime); ownership options weighed → **leaning: values + generational handles + arenas + state stores + a LIGHT borrow (NOT a full checker)**, tiebreak = visual+agent authorability; syntax sketches; text/visual projection (shares ADR-0109 §6 identity model); all binding decisions deferred to the CEIR-32 ADR against the corpus. | ✅ **ACCEPTED 2026-08-07** (design note; decides nothing; NOT critical path) | §4 §98 §19 §10 |
| ✅ | CEIR-0f | **D-007 restructure DONE (2026-08-08):** CEIR-spine section added to `D-007-gpu-program-system.md`; the old master table re-hung under the CEIR bands (banner: RPL→CEIR-18 · MLR→CEIR-24 · frame/executor→CEIR-15/16 · I2D→CEIR-31 · hesap-GPU→CEIR-22 · D7E→CEIR-33, marked "do NOT tick live"); **THIS file wired as the one live tracker** (context.md + ROADMAP pointers moved here); the **ADR-0106 supersession PLAN written** (strike executes @ CEIR-15f); **§PR-3's maturity ladder struck-in-place** → §173 + the registry header. All additive (D-007 +41 lines, zero content deleted). | ✅ exactly one live tracker; no row tracked twice; no third live maturity ladder | §175 §159 |
| ✅ | CEIR-0g | **Maturity merge + §174 manifest** — [`docs/design/ceir-0g-maturity-and-manifest.md`](../design/ceir-0g-maturity-and-manifest.md) — ✅ **ACCEPTED.** Finding: the two ladders measure DIFFERENT axes (RAF-asset maturity vs CEIR-program maturity) — merge ≠ renumber. ONE forward model = CEIR L0–L8; a **two-axis transition** (`raf_level` today's reality + `ceir_level` forward track, both honest; per-class §PR-4: A/A+R/A+E converge, B keeps both, T = n/a). §174 manifest adds `providers` + `determinism_tier`; registry-migration plan (schema 1→2 now, →3 at 13z). §PR-3 supersession is a CEIR-0f action. | ✅ **ACCEPTED 2026-08-08** | §173 §174 |
| ✅ | CEIR-0h | **Migration + DELETION ledger** — [`docs/design/ceir-0h-migration-and-deletion-tables.md`](../design/ceir-0h-migration-and-deletion-tables.md) — ✅ **ACCEPTED.** Built FROM CEIR-0a (not re-derived): what PROMOTES (14 verbs, runtime, cookers, CKIR, IComputeContext) vs DELETES (composite C++ orchestration + duplicate/privileged paths). Every deletion names its parity gate FIRST: frame-path F1–3 @ CEIR-15f · orchestration E1–5 @ CEIR-16z (E1 = the program-variant ladder = the §128 proof) · residual R1–2 (visbuffer/overlay) @ CEIR-14 · §PR-3 supersession @ 0f. CEIR-34 executes it verbatim. | ✅ **ACCEPTED 2026-08-08** | §126 §127 §178 |
| ✅ | CEIR-0z | **CEIR-0 close report + sizing** — [`docs/design/ceir-0z-close-report-and-sizing.md`](../design/ceir-0z-close-report-and-sizing.md) — ✅ **ACCEPTED.** The §184 fifteen-item report (each answered from 0a–0h evidence). Honest DERIVED sizing: CEIR-1…13 ≈ **34–55 KLOC** (in-tree anchors + per-band confidence; the four Low bands 10–13 dominate) *(pre-re-baseline numbering: old 10–13 = new 13–16)* · **~4–8 mo dark period** (very-low confidence, banded on RAH-parallelism + no-band-2/4-redesign). 5 unresolved design Qs docketed to their bands. ⚠ item 4 finalizes when 0f executes. | ✅ **ACCEPTED 2026-08-08** (15 answered) | §184 |

## CEIR-1 — Core IR substrate — ✅ CLOSED 2026-08-08 (1a–1z) → §175 band 1 · core: §12 §7

> **BAND 1 CLOSED.** The full host-only IR substrate: identity + the in-arena def-use graph (1a) · SymbolTable +
> `ceir.func` (1b) · interned typed attributes + source map (1c) · open-world dialect registry + traits/interfaces +
> verifier (1d) · deterministic textual printer + parser (1e) · FourCC-chunked binary serialization (1f) ·
> `ModuleBuilder` fluent API (1g) · the round-trip fuzz + malformed corpus + stable-hash harness (1h) · the
> hello-world band gate (1z). `tests/ceir` **49/49** (from 7 at 1a) + a 7/7 `crd::memory::GrowableLinearAllocator`
> gate. Every slice gated on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan + LLVM-20 tidy** +
> `crd-ceir-invariants` (I3/I5/I6). Headline finding: the **1h fuzz caught two real OOM crashes** in loaders that had
> already passed four slices of unit gates. **NEXT: user commits the batch → GitHub CI green → CEIR-2.**

**Band contract.** New module `engine/ceir` — the in-memory IR + its two serial forms. Working shape (finalized in
0c): `crd::ceir::Context` (owns dialect registry + interned types/attrs/strings), `Module`, `Op` (interned
`(dialect,name)` id + `ValueId` operand/result spans + attr map + region list, **arena-allocated** per §153),
`Value` (typed, def-use chain), `Block`, `Region{Graph|SsaCfg}` (§13), `SymbolTable`. Textual form is MLIR-shaped
(`%v = dialect.op(%a,%b) {attr=…} : type`), printed DETERMINISTICALLY. Binary form is FourCC-chunked like CRDR,
versioned from byte one (§104), field-by-field (⛔ padding scar). **Reuse:** `crd::containers` + TLSF/arena; FNV-1a
interning; the CRDR chunk pattern. **Gate: typed hello-world round-trips text/binary/builder (§167).**

| ✔ | Slice | Deliverable | → §§ |
|---|---|---|---|
| ✅ | CEIR-1a | ✅ **CLOSED (4-config sweep PASS, 2026-08-08):** new module `engine/ceir` (`crd-ceir`, host-only, deps core/log/memory/containers/units per ADR-0109) — `Context`/`Module`/`Operation`/`Value`/`Block`/`Region` + **intrusive in-arena def-use** (MLIR-shape `Use`; RAUW = splice, zero-alloc) + O(1) intrusive op/block edit + `SourceLoc` field from day one. The growable bump **arena is `crd::memory::GrowableLinearAllocator`** — it lives in **crd-memory** (its proper home; the growable sibling of `LinearAllocator`), NOT a crd-ceir-private `detail/Arena`, with its own **7/7 boundary-adversary gate** in `tests/memory` (chunk growth · oversized-own-chunk · exact fill-to-tail · reset-reuse · no-per-alloc-malloc-within-a-chunk · dealloc-noop · reallocate-copies · destructor-frees-every-chunk). `tests/ceir` **7/7** incl. the ⭐ **no-per-op-malloc gate** (32 ops, 0 parent allocs) + def-use/RAUW/set_operand/erase/insert_before/boundaries. **I3/I5 grep-gates** — `crd-ceir-invariants` ctest (`scripts/check_ceir_invariants.{ps1,sh}`, both OS blocks): I5 link-edge whitelist, I3 no forbidden-module include NOR shading-lang name; **proven to bite** (negative fixture → exit 1). **Gate: `per-slice-check.ps1` PASS all four — win-debug + win-asan + win-shipping(LTCG) + win-tidy, zero failures.** En route the sweep peeled **7 pre-existing cross-band blockers** (RAF/REN/CKIR bands had never passed shipping-LTCG/asan-complete/tidy) — all fixed gold-standard, none quarantined: frame_asset bare-scalar; RAF-10 `catch_discover_tests` ENVIRONMENT split; **DX12 + Vulkan RT pipeline-cache keyed by pointer/handle → content-hash** (real engine bug, DX12 flake 200/200 after); AS-4 CUDA autotuner timing guarded under ASan; C4743 LTCG ODR = stale-obj (build/win-shipping wiped); 37 clang-tidy errors across 12 files. See session log 2026-08-08. | §12 §13 |
| ✅ | CEIR-1b | ✅ **CLOSED 2026-08-08.** `SymbolTable` (per-Module, arena-backed `crd::containers::HashMap<StringView,SymbolEntry>` with an explicit `SvHash`; `define` rejects duplicates, no silent overwrite) + `Visibility{Public,Private,Nested}` (a real typed field — `symbol_table.hpp`). The **`ceir.func`** dialect (`func.hpp`/`func.cpp`, `crd::ceir::func`) built ENTIRELY on the generic Context factories (open-world; core never switches on func): `create_func` (func.func + body Region + entry-Block params, registered in the module table), `create_return` (terminator over the params), `create_call` (callee recorded via a Context symbol-ref side-table — the CEIR-1c-replaced interim for a `SymbolRefAttr`), `resolve_call` (lazy, **cross-module by name** — resolve a call against any module's table). `tests/ceir` **12/12** (7 core + 5 func: define/lookup/visibility, duplicate-reject + empty-name, body+params+return with def-use, call resolves, cross-module resolve/unresolved) + `crd-ceir-invariants` green (I3/I5 held). Tidy-clean (7 files). **Gate:** crd-ceir-tests PASS **all four configs** (debug/asan/shipping-LTCG/tidy) — a COMPLETE gate since crd-ceir has **zero downstream consumers** (grep-proven), so a full-tree sweep adds no signal for a leaf-module change (re-earns its keep at the band close / when the crd-ceir-host/gpu bridges land). ⚠ scoped-gate rationale flagged for user. | §34 |
| ✅ | CEIR-1c | ✅ **CLOSED 2026-08-08.** **Interned typed attribute values** (`attr.hpp`: `AttrValue` = tagged union over Int/Float(bit-exact)/Bool/String/SymbolRef/Type + `AttrId`; the Context dedups identical values → one `AttrId`, so equality is a u32 compare). **Per-op AttrDict** — `Operation` gains an arena `NamedAttr[]` (interned name→AttrId), grow-by-rebuild (operand-grow policy); read via `op->attr(name)`/`has_attr`/`num_attrs`, set via `Context::set_attr` (overwrite-in-place). **Source map / provenance (§111)** — `Context::register_file`→stable dedup'd `file_id`, `file_path`; every op carries `SourceLoc{file_id,line,col}` from day one (the field reserved in 1a is now *fed* — no retrofit). **Dissolved the CEIR-1b interim**: `func.call`'s callee is now a real **`SymbolRef` attribute** (`Context::m_symbol_refs` deleted; `resolve_call` reads `op->attr("callee")`). `tests/ceir` **17/17** (+guard = 18: attr dedup/kind-round-trip, dict set/overwrite/lookup, SymbolRef-callee resolves, source-map dedup + SourceLoc round-trip). Tidy-clean (7 files). **Gate:** crd-ceir-tests PASS all four configs (debug/asan/shipping-LTCG/tidy) + crd-ceir-invariants — scoped-complete (crd-ceir still has zero downstream). | §111 |
| ✅ | CEIR-1d | ✅ **CLOSED 2026-08-08.** **Dialect registry** (`dialect.hpp`/`dialect.cpp`): `Context::register_dialect(name)` → `Dialect`; `Dialect::register_op(op, traits, verifier)` interns "dialect.op" + records an **`OpInfo`** (ODS-lite: kind/name/dialect/traits/verify + interface list; printer/parser hooks reserved for 1e). **Traits** (`OpTrait` flags — Terminator/Symbol/SymbolTable/Pure/IsolatedFromAbove) via `has_trait`; **verifier** dispatch via `Context::verify` (opaque/unknown ⇒ valid); **interfaces** via `intern_interface`/`register_interface`/`get_interface` (an analysis dispatches through a registered function-table, NEVER a switch). **Open-world proven:** an UNREGISTERED-dialect op is a first-class Operation (dialect_of=null, has_trait=false, verify=valid). The `func` dialect self-registers (`func::register_dialect`: func.func=Symbol, func.return=Terminator+verifier). **⛔ I6 grep-gate** added to `crd-ceir-invariants` (both OS): forbids `switch (op.kind())` — **proven to bite** on a negative fixture. Shared `detail::StringViewHash` (DRY'd symbol_table). `tests/ceir` **22/22** (+5 dialect: registry/traits, opaque-unknown, verifier, interface-dispatch). Tidy-clean (10 files). **Gate:** crd-ceir-tests PASS all 4 configs + crd-ceir-invariants (I3/I5/I6) — scoped-complete (zero downstream). | §6 §7 §101 |
| ✅ | CEIR-1e | ✅ **CLOSED 2026-08-08.** **Deterministic textual PRINTER** (`print.hpp`/`print.cpp`): IR → canonical MLIR-flavored text (`module { ^bb0(%0 : !t1): %1 = dialect.op(%0, %0) {attr = val, …} : !t1 <nested {region}> }`); SSA values numbered by a fixed **pre-order walk** (block-args → op-results → recurse op-regions), attributes emitted **sorted by name** → the same semantic graph prints **byte-identical**; floats via `std::to_chars` (shortest round-trippable) with an enforced `.`/`e` marker so `4.0` never re-reads as an int; unknown-dialect ops print opaquely by their interned `dialect.op`; **NO layout emitted** — the text is semantics only (§10; coords/edges are UI, regenerated). **Recursive-descent PARSER** (`parse.hpp`/`parse.cpp`, `parse(ctx,text)→ParseResult{module,ok,error_offset,error}`): whitespace-skipping cursor, grammar mirrors the printer; **id→Value fixup pass** so a Graph-region **use-before-def** (legal under free block-insertion order) resolves; **strings unescaped before interning** (else `\`/`"` grow each round); `count_trailing_regions` scans **balanced braces skipping string literals** (a nested op's string attr may hold `{`) so `create_operation` gets `num_regions` upfront; `{`-disambiguation (attrs `{name =` vs region `{^`/`{}`); **malformed input REJECTED** with a byte offset (trailing garbage · duplicate SSA id · undefined operand · non-`dialect.op` name · truncation) — the signal CEIR-1h's corpus reuses. **MLIR-faithful symbol identity** (advisor call): the func's name/visibility now ride **ON the op** as `sym_name`/`sym_visibility` string attrs (`func.cpp` — the SymbolTable is an INDEX over `sym_name`, not the source of truth; no `@name` grammar, no per-dialect hooks), so identity prints + round-trips through the generic attr machinery; the parser **rebuilds the module SymbolTable** from those attrs (duplicate name → parse error) and a `func.call` resolves against the rebuilt table post-parse. `tests/ceir` **31/31** (+5 roundtrip: rich-graph byte-exact print⇄parse [every attr kind · negative int · `4.0`/exponent floats · escaped string w/ quote+backslash+brace · nested+empty+multi-block regions · use-before-def fixup · func/call/return], double-parse determinism, opaque unknown-dialect, func symbol-identity survives, malformed-rejection). Tidy-clean (5 files). **Gate (ratified per-slice contract):** crd-ceir-tests PASS **2 Windows (win-debug + win-asan) + 2 Linux (linux-gcc-debug + linux-gcc-asan, WSL) + LLVM-20 tidy**, + `crd-ceir-invariants` (I3/I5/I6) green both OSes — scoped-complete (crd-ceir zero downstream). **⚠ D-007-divergence (→CEIR-1f):** the TEXT form is semantics-only and does **not** encode `Region::kind` (Graph vs SsaCfg) — an SsaCfg body round-trips as Graph textually (invisible to the byte-exact text DoD; both sides print without a kind). The BINARY form (1f) MUST carry the region-kind field. | §10 §166 |
| ✅ | CEIR-1f | ✅ **CLOSED 2026-08-08.** **Binary serial form** (`binary.hpp`/`binary.cpp`: `serialize(ctx,module,alloc)→Array<u8>` + `deserialize(ctx,bytes)→ParseResult`) — the compact sibling of the text form, in the CRDR mould (ADR-0038): magic `'CEIR'` + a version word + FourCC/length-prefixed chunks a reader iterates and **SKIPS by length** when the FourCC is unknown (forward-compat, tested by splicing a synthetic `'XXXX'` chunk). Chunks (v1): **`STRP`** string pool · **`SRCM`** source-file map · **`ATTR`** attribute-value pool · **`BODY`** the region graph. **⛔⛔ FIELD-BY-FIELD little-endian** — never a struct blast (the padding-in-content-hash scar); the CKIR-serialize `put_u*` writers + bounds-checked `.ok`-latching `Cursor` reader, self-contained (crd-ceir cannot link crd-kir; the CRDR container likewise). **⭐ Content-pure:** the pools are built from the MODULE WALK (first-use order, only what the module references) and BODY holds pool INDICES — never a Context id — so **the blob is a pure function of module content** (dirty-context byte-equality PROVEN: the same graph built in a clean vs a pre-polluted Context serializes byte-equal). This closes the **1e region-kind divergence**: the binary form carries `Region::kind` (Graph/SsaCfg), restored on load via the new `Context::set_region_kind` (verified structurally at BOTH the module body and an op region). `SourceLoc` survives BY PATH (re-`register_file`'d), a Graph-region use-before-def resolves via the same fixup pass as the parser, and symbol identity rebuilds through the **shared `detail::register_symbol`** helper (extracted from the parser so the two loaders never drift). `serialize∘deserialize∘serialize` is **byte-exact**; `print∘deserialize∘serialize == print` (the two forms agree). Malformed input REJECTED with a byte offset: bad magic (0) · unsupported version (4) · truncation · trailing bytes after the last chunk · out-of-range pool index. `tests/ceir` **37/37** (+6 binary; `build_rich` extracted to a shared `rich_graph.hpp` reused by the text + binary gates). Tidy-clean (11 files). **Gate:** crd-ceir-tests PASS **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan (WSL) + LLVM-20 tidy** + `crd-ceir-invariants` (I3/I5/I6) both OSes. **⚠ scar:** the I6 raw-line grep bites PROSE that spells `switch`+`kind()` — comments must describe the rule without writing the pattern. **→ CEIR-3** adds a `TYPE` chunk (TypeId is an inline opaque u32 here) and bumps the version. | §104 §123 |
| ✅ | CEIR-1g | ✅ **CLOSED 2026-08-08.** **`ModuleBuilder` fluent C++ API** (`builder.hpp`/`builder.cpp`) — an ergonomic host-side builder that emits ORDINARY canonical IR. **⛔⛔ NO privileged bypass:** every op is created through the SAME `Context::create_operation`, placed with the SAME intrusive block edits, attributed via `set_attr`, and (if symbol-defining) registered through the SAME shared `detail::register_symbol` — a builder-made module is byte-identical to the hand-built one (PROVEN: `print(builder) == print(hand)`). Surface: **`ModuleBuilder`** (owns the module; `add_block(args,type,into)` append+select, `set_insertion`/`insertion`, `op(dialect,name)`, `func`/`ret`/`call` convenience reusing `crd::ceir::func`, and **`verify(&failing)`** that walks every op through the REAL per-kind `Context::verify` — no stub); **`OpBuilder`** (fluent `.operand/.operands/.result/.results(n,t)/.attr/.regions/.loc` → terminal `build()`/`build_result(i)`); **`InsertionGuard`** (RAII save/restore for nested-region building). ⛔ **`build()` returns nullptr on a duplicate `sym_name`** — the op is `erase()`d (no silent overwrite, mirroring `create_func`); `build_result` asserts success. Insertion point asserted (in `func()`, before `create_func` so a bail can't leave a registered-but-unplaced func). Verifier-routing proven by a rejection test (a custom-dialect verifier requiring ≥1 operand; a 0-operand build → `verify()` false + `failing` pinpoints it) and the builder module binary-round-trips. `tests/ceir` **41/41** (+4 builder). Tidy-clean (4 files). **Gate:** crd-ceir-tests PASS **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan (WSL) + LLVM-20 tidy** + `crd-ceir-invariants` (I3/I5/I6). **⚠ note (→ later canonicalization):** the 1f binary BODY stores an op's attrs in dict INSERTION order, where the text printer SORTS them by name — deterministic content either way (not a purity bug), but a builder-vs-hand *blob* equality would require matching `.attr()` call order; a canonical-order pass is a later-band consideration. | §121 |
| ✅ | CEIR-1h | ✅ **CLOSED 2026-08-08.** **The permanent test harness, seeded now** (§119/§167). **Round-trip fuzz** (`test_fuzz.cpp`): random-but-VALID modules generated THROUGH `ModuleBuilder` (dogfoods 1g — operands drawn only from already-defined SSA values, 0/1/**2**-region ops, 0..N-op blocks incl. empty, attrs of every kind, nested regions) must round-trip **byte-exact** through BOTH forms — text `print→parse→print` AND binary `serialize→deserialize→serialize` — across a fixed seed array (⛔ inline xorshift64, nonzero state, NO `<random>`, never time-seeded — the A/B-deterministic scar) plus the dense `build_rich` fixture. **Stable content hash** — `stable_hash(ctx,module,scratch)` = FNV-1a over the content-pure 1f blob (NEW surface): deterministic, Context-history-independent (clean-vs-dirty hash EQUAL), discriminating, and stable under a BINARY round-trip (never across the text path — region kind / NaN are text-invisible). **Malformed corpus** (`test_malformed.cpp`): a table of bad text + bad binary — each REJECTED via `ParseResult{ok=false,error_offset}`, never a crash — plus a **single-byte-corruption SWEEP** (XOR every position of a valid text and a valid blob → assert no crash; ASan/UBSan is the memory-safety proof). ⛔⛔ **Two loader HARDENINGS this slice — real OOM crashes the harness caught in code that had already passed FOUR slices of gates** (the fuzz doing its job on day one): (a) the **text parser** OOM'd on a huge def-id (`%4000000000`) — now bounded by text length in `register_value`; (b) the **binary decoder** OOM'd on a corrupt count — now every count is bounded (operands/attrs/regions/blocks by the chunk length; num_args/num_results by `kMaxDecodeCount`, a **documented v1 format constraint** in binary.hpp). `tests/ceir` **46/46** (+5). Tidy-clean (5 files). **Gate:** crd-ceir-tests PASS **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan (WSL) + LLVM-20 tidy** + `crd-ceir-invariants` (I3/I5/I6). **§167 map:** SEEDED now — parser/printer round-trip · binary round-trip · stable hash · malformed IR · builder/text equivalence · source-map preservation · unknown-dialect; DEFERRED with their band — dominance + region legality → CEIR-5b ✅ (`test_structure.cpp`), op-schema generation → CEIR-2, dialect versioning → later. **⚠ v1 limitation:** a `SymbolRef` attr's text is identifier-only (the printer emits `@name` raw, no quoting — MLIR quotes; ours doesn't yet); a later band adds quoting + bumps the format. | §119 §167 |
| ✅ | CEIR-1z | ✅ **CLOSED 2026-08-08 — BAND 1 GATE.** A **typed hello-world** (`func.func @add1` with a param → `arith.const {value=1}` → `math.add` → `func.return`, plus a top-level `arith.const {value=41}` + `func.call @add1`) built TWO ways (hand factories + `ModuleBuilder`), asserting the full §167 acceptance: `print(hand) == print(builder)` (builder/text equivalence), `print(parse(print(m))) == print(m)` (text round-trip), `serialize→deserialize→serialize` byte-exact + `print(deserialize(serialize(m))) == print(m)` (binary ⇄ text agreement), and `func.call` **resolves** `func.func` after the builder-native form AND a text parse AND a binary load (symbol survives all three forms). "const" is a GENERIC op with a `value` attr — no dedicated dialect. `tests/ceir/test_hello.cpp` 3 cases; the **fuzz corpus is green in ctest** (test_fuzz/test_malformed) and the **guard greps are registered** (the `crd-ceir-invariants` ctest — I3/I5/I6, both OSes). `tests/ceir` **49/49**. Tidy-clean. **Gate:** crd-ceir-tests PASS **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan (WSL) + LLVM-20 tidy**. | §167 |

## CEIR-2 — Schema/ODS-like op-definition generator — ✅ CLOSED (2a–2z) 2026-08-08 → §8

**Band contract (§8).** Ops are DEFINED in TOML (authoring-text house rule), generated to C++ — never hand-kept in
parallel. `*.ceirop.toml` per dialect under `engine/ceir/ops/`; generator `tools/ceir_opgen/ceir_opgen.py` (checked
in, output committed — the `gen_fft_batched.py` precedent) emits: typed op wrapper + builder + verifier scaffold +
reflection record + CR-D007 node metadata + CLI/MCP schema + doc stub + test skeleton. Schema fields per §8: name ·
dialect · version · operands(+variadic) · results · attrs · regions · traits · effects · domain legality ·
capability requirements · type/shape inference · fold · serialization schema · editor hints · docs · deprecation.
**Gate: a new pure op is added with no central enum/switch edits (proves §7).**

| ✔ | Slice | Deliverable | → §§ |
|---|---|---|---|
| ✅ | CEIR-2a | ✅ **CLOSED 2026-08-08 (with 2b, one gate).** The **`*.ceirop.toml` schema** (`engine/ceir/ops/README.md`, one file/dialect) with ALL §8 fields (name·dialect·version·operands+variadic·results·attrs·regions·traits·effects·domain·capability·type/shape-inference·fold·serialization·editor-hints·docs·deprecation) + the **ADR-0110 §2.1 native-binding fields** (`[op.native]`: provider·determinism·thread-safety·hot-reload·lifetime·cost·capabilities) so intrinsic ops define through the SAME generator (no parallel schema). The **validator** (`tools/ceir_opgen/ceir_opgen.py`, stdlib `tomllib`, no deps) checks **STRUCTURE + VOCABULARY of every field** — not just admits the key: unknown field, bad trait / attr-kind / region-kind, missing required key, duplicate op, duplicate member name, variadic-not-last, `schema_version` mismatch, non-array `effects`, non-string `domain`/`docs`/inference/fold, malformed `deprecation{since,replaced_by,note}`, `[op.native]` missing provider/determinism or bad **ADR-0098 determinism tier** — each a `file:line: error: message` cook-time diagnostic (the declared-contract rule). **`tools/ceir_opgen/test_opgen.py`** = 20 validator unit tests → the `crd-ceir-opgen-validator` ctest. **⚠ documented split (codegen ONLY, not the schema):** the semantic-heavy fields (effects/domain/type-shape-inference/fold) are schema-validated now; their CODEGEN scaffolds until CEIR-3/4/6 give the machinery — the schema contract is whole. | §8 |
| ✅ | CEIR-2b | ✅ **CLOSED 2026-08-08 (with 2a).** **Codegen** (`ceir_opgen.py` → committed `engine/ceir/generated/crd/ceir/gen/<dialect>_ops.{hpp,cpp}`, auto-globbed into `crd-ceir`, on its PUBLIC include path): per op a **kind interner**, a **typed wrapper** (a view over `Operation*` with named operand/result/attr accessors — no stringly-typed call sites), a **builder** (through the ordinary Context factories, no privileged construction), and a **verifier** (arity + region-count + required-attr/kind), plus a **`register_<dialect>_ops(Context&)`** that self-registers the dialect + every op (traits + verifier) via the CEIR-1d `Dialect::register_op` path — mirroring `func::register_dialect`. **DETERMINISTIC** output (ops sorted by name, `\n` newlines, no timestamp, schema-version header) so a regen is byte-identical — the **`crd-ceir-opgen-drift`** ctest (`ceir_opgen.py --check`, CRLF-normalized) guards it; ⛔ NEVER hand-edit generated output, fix the templates. Example dialect **`arith`** (`const`/`addi`/`muli`/`cmpi`) — chosen over `func` (already hand-written at 1b; would collide). `tests/ceir/test_arith_gen.cpp` **3/3** (self-register+traits, builder/wrapper/verifier round-trip through `Context::verify`, verifier rejects malformed) — **a real op dialect added with ZERO central-enum/switch edits (§7 proof)**. **Gate:** crd-ceir-tests **54/54** on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** (Python3 found both OSes) + LLVM-20 tidy CLEAN on the GENERATED C++ (templates emit clean, NOLINT budget 0) + the opgen drift+validator ctests green both OSes. New surface: `ceir_opgen.py --check`, `crd-ceir-opgen-drift`/`-validator`, `crd::ceir::arith::*`. | §8 |
| ✅ | CEIR-2c | ✅ **CLOSED 2026-08-08.** **Reflection record** — a hand-written shared `crd/ceir/op_schema.hpp` (`OpSchema`/`OperandInfo`/`ResultInfo`/`AttrInfo`); the generator emits per-dialect `constexpr` static arrays + `<dialect>_op_schemas() → ConstSpan<OpSchema>` (constexpr because `StringView` = `std::string_view`) — the op schema as queryable C++ data, the **SAME truth** the wrapper/builder/verifier are generated from (no parallel truth). The semantic-heavy fields (`effects`/`domain`) + the `[op.native]` binding are **reflected NOW as raw strings** (schema-known; their typed C++ vocabulary lands at CEIR-4). **Editor-node metadata (§161) satisfied by DERIVATION** — input pins ≡ operands, output pins ≡ results, widgets ≡ attributes (no separate artifact). **CLI/MCP schema (§122)** — a committed, deterministic `engine/ceir/generated/crd/ceir/gen/<dialect>.ops.json` **DERIVED from the same model** the C++ emitters consume (so it can never drift from the codegen), `ensure_ascii` + fixed key order + `schema_version`; it rides the SAME `--check` drift ctest (which now covers hpp+cpp+json — 3 files/dialect) — the agent-discovery artifact (readable without linking the engine). Tests: `test_arith_gen.cpp` gains a **reflection↔codegen coherence** case (schema sorted-by-name + size == op count; a built op's operand/result/attr counts == its reflected schema); `test_opgen.py` gains a **JSON-parses-and-matches-model** case (21 validator cases total). **Gate:** crd-ceir-tests **55/55** on win-debug + win-asan + linux-gcc-debug + linux-gcc-asan + LLVM-20 tidy CLEAN (op_schema.hpp + generated C++) + drift(hpp/cpp/json)+validator ctests green both OSes. New surface: `crd::ceir::OpSchema`, `arith_op_schemas()`, `<dialect>.ops.json`. | §8 §122 §161 |
| ✅ | CEIR-2d | ✅ **CLOSED 2026-08-08.** **Doc + test-skeleton generation**, both DERIVED from the same model (no parallel truth) and drift-guarded — `--check` now covers **5 files/dialect**. **Doc (§123):** a committed per-dialect `engine/ceir/generated/crd/ceir/gen/<dialect>.ops.md` op-reference (dialect summary + per op: qualified name, summary, docs prose, version, traits, regions, and — when declared — domain/effects/type-shape-inference/fold markers, a **DEPRECATED** badge + since/replaced_by/note, native binding; operands/results/attributes as pipe-escaped markdown tables). To feed it, `validate()` now RETAINS the four fields it previously dropped (`deprecation`/`type_inference`/`shape_inference`/`fold`) — no validated field is silently lost — and `emit_json` emits those **conditionally (key iff declared)** so arith (which declares none) stays **byte-identical** to 2c. **Test-skeleton (§8):** a committed, GATED, generated Catch2 smoke test `tests/ceir/generated/test_<dialect>_gen_smoke.cpp` that (a) registers the dialect + walks `<dialect>_op_schemas()` asserting sorted-by-name, size==op-count, each reflected op interns idempotently + `dialect_of`==the dialect, (b) builds EVERY op through its generated builder (dummy operands from an unregistered source op's results; required attrs by kind) and asserts `Context::verify` **accepts** it, and (c) constructs a MALFORMED version (no operands/attrs/regions) and asserts the verifier **rejects** it — **both directions**, so a verifier that regressed to a vacuous `return true` can't pass silently (emitted only for ops an empty construct is actually malformed for; the whole reject-case is omitted when none qualify, since an assertion-free `TEST_CASE` passes vacuously — the very failure mode it guards). A test that **GROWS from a TOML edit alone** (the artifact 2z cites). Compiled into crd-ceir-tests via the house `crd_collect_sources` CONFIGURE_DEPENDS glob of `tests/ceir/generated/` — **a new dialect's smoke test builds with ZERO CMake edit**. `main()` now makedirs per-output (GEN_DIR + the new test dir). `test_opgen.py` +3 (24 total): every-op-heading/deterministic/pipe-escape md case; a **declared-optional-fields render in BOTH md+json** case (deprecation/effects/domain/inference/native — the branches arith can't reach); and a **negative-case-emitted-iff-empty-is-malformed** case (a pure-variadic-operand op emits no `CHECK_FALSE`; a required-attr op does). **Gate:** crd-ceir-tests **58/58** (55 + 3 smoke) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy CLEAN on the generated smoke `.cpp` + drift(hpp/cpp/json/md/smoke = 5 files) + validator(24) green both OSes. New surface: `<dialect>.ops.md`, `test_<dialect>_gen_smoke.cpp`. | §8 §123 |
| ✅ | CEIR-2z | ✅ **CLOSED 2026-08-08 — BAND 2 GATE.** A new **full-surface reference dialect `test`** (`engine/ceir/ops/test.ceirop.toml`: `test.dummy` = fixed + variadic operands, required int/string + an OPTIONAL bool attr, a region, **multi-trait** `Symbol|IsolatedFromAbove`, `effects=[read,write]`, `domain`, the declared-marker `type_inference`/`shape_inference`/`fold` (rendered in the committed `.md`/`.json`; machinery lands CEIR-3/6), a full `[op.native]` binding, `deprecation` — **every field the schema can express**; `test.kinds` = a required attr of every remaining kind float/bool/symbol/type) was added by **writing ONLY the TOML** — `git status` shows the new `.toml` + its generated artifacts and **nothing else** (no central enum/switch, no generator, no CMakeLists). One regen produced **all seven** artifacts — `test_ops.{hpp,cpp}` (kind interners, typed wrappers, builders incl `build_kinds(…, AttrId a_float, AttrId a_bool, AttrId a_sym, AttrId a_type, …)`, verifiers, `register_test_ops`, reflection), `test.ops.json`, `test.ops.md`, and `tests/ceir/generated/test_test_gen_smoke.cpp` — and the smoke test **built + passed with ZERO CMake edit** (house CONFIGURE_DEPENDS glob). This first-ever-compiled the effects `constexpr StringView[]` path, the multi-trait `OpTrait::A|OpTrait::B` register/reflection path, the intrinsic-reflection path, and the float/bool/symbol/type builder+`_ATTR_FACTORY` paths — **the §7/§8 open-world property, proven end-to-end (add an op ⇒ a TOML edit, zero central edits).** **Pre-2z generator errata** (latent defects the narrow arith surface hid, fixed with arith byte-identical — the "phase-0 errata / phase-1 TOML-alone" split): a lone-variadic verifier emitted a tautological `< 0U` (guarded); `effects` elements were shape-unvalidated (now string-checked + a negative test); the md rendered Python `True`/`False` for native bools (now lowercase). `_traits_expr`'s multi-trait branch was investigated and found CORRECT (OpTrait is a bitmask enum with `operator|` overloads — no fix, verified by `test.dummy` compiling). `test_opgen.py` 24→**26**. **Gate:** crd-ceir-tests **61/61** (58 + 3 `test` smoke) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy CLEAN on the 3 new generated C++ files + drift(**10 files** = 5×2 dialects) + validator(26) green both OSes. **→ BAND 2 CLOSED; CEIR-3 next.** | §7 §8 |

## CEIR-3 — Type/shape/unit/lifetime foundation — ✅ CLOSED (3a–3z) → §16–§22

**Band contract.** The interned type system CEIR values carry. Scalars/aggregates (§16, incl. tagged-union/`Option`/
`Result`); resource types (§23); symbolic shapes (§21/§35); **quantity metadata = ADR-0078 dimension tags on
high-level types, erased at the kernel/raw boundary** (§17 — reuse `crd::units`, do NOT reinvent); ownership/view
qualifiers (§19). **CHIR headroom NOW:** generic params + constraint records + interface/trait types + callable
types exist in the IR before any frontend emits them (append-at-END, type-system edition). **Gate: invalid unit/
shape/view combinations are rejected.**

| ✔ | Slice | Deliverable | → §§ |
|---|---|---|---|
| ✅ | CEIR-3a | ✅ **CLOSED 2026-08-08.** `TypeId` becomes a **real interned STRUCTURAL type** (was an opaque u32). New `type.hpp`: `TypeKind` (Bool/Int/Float/Index · Vector/Matrix/Complex/Quaternion · Array/Tuple/Struct/Enum/Variant/Option/Result — Option/Result first-class, not Variant sugar) + `FloatKind` (F16/BF16/F32/F64/**F8E4M3/F8E5M2**); a `Type` record (**`kind` is a FIELD not a method** — I6-safe jump table) with structural equality (the struct/enum NAME participates). `Context` gains a type intern table (the AttrValue pattern; `intern_type` deep-stabilizes borrowed spans/names into the arena) + factories (`type_bool/index/int(w,signed)/float(fk)` + i32/i64/f32/f64 + `type_vector/matrix/complex/quaternion/array/tuple/struct/enum/variant/option/result`) + `type_of` (**ASSERTS out-of-range — no silent fallback**). **Canonical `!`-sigil grammar** (zero-space, nested types keep the sigil): `!i32 !u16 !f32 !bf16 !f8e4m3 !bool !index`, `!vec<4x!f32>`, `!mat<4x4x!f32>`, `!complex<!f32>`, `!quat<!f32>`, `!array<8x!i32>`, `!tuple<!i32,!f32>`, `!struct<Point,x:!i32,y:!f32>`, `!enum<Color,red,green>`, `!variant<!i32,!f32>`, `!option<!i32>`, `!result<!i32,!u32>` — printer + recursive **depth-guarded** parser round-trip byte-exact. **Binary v2**: a **child-first `TYPE` pool** chunk (version 1→2, chunk-count 4→5; every record references strictly-lower indices, decoder rejects forward/self refs by construction); the ATTR `of_type` payloads + BODY per-result/per-arg refs **remap through the TYPE pool** (1-based; keeps the 1f **content-purity** — proven by a dirty-context test that now pollutes the type table). Format is **per-result/per-arg-capable** (uniform C++ API today) so distinct multi-result typing needs no v3 bump (1f one-bump promise). **Full migration, no fallback** — every `TypeId{n}` magic literal (tests + generated smoke) → real factory calls; `ceir_opgen.py` evolved + dialects regenerated. **⭐ scar** ([[feedback_generator_needs_full_surface_reference_input]]): 3 stray `TypeId{1U}` literals aliased to incidental intern state — valid-but-wrong, caught ONLY by the clean-vs-dirty content-purity blob compare (the `type_of` assert catches out-of-range, not aliasing). Fuzz gains a **bounded-depth random-type generator** (every kind, end-to-end round-trip); malformed corpus gains bad-type-syntax + 200-deep-nesting rejection. **Gate:** crd-ceir-tests **65/65** on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy (all changed TUs) + invariants (I3/I5/I6) + opgen drift/validator green both OSes. | §16 |
| ✅ | CEIR-3b | ✅ **CLOSED 2026-08-08.** **Generics** — three `TypeKind`s APPENDED (after `Result`, ordinal-stable): **`TypeParam`** (name + `members`=constraint traits), **`Trait`** (name + `members`=supertraits — named `Trait` NOT Interface, to avoid clashing with CEIR-1d's op `InterfaceId`), **`Callable`** (`count`=num params, `members`=params++results). All reuse the v2 `Type` record ⇒ **NO binary version bump** (decoder kind range widened to `Callable`). A type param is **structurally identified by (name, constraints)** (two `T:Ord` generics share one interned param; substitution binds every occurrence; alpha-scoping is CHIR's). `Context`: `type_param/type_trait/type_callable` + `type_has_params` (is-ground query). **Trait conformance** = live Context state (`register_conformance`/`satisfies`, transitive over supertraits) — NOT serialized (verifier-checked *substitution* is the deliverable; conformance *declarations* rebuild from ops when an `impl` op exists — the symbol precedent). **`substitute` → a diagnostic-bearing `SubstResult{ok,type,failed_param,failed_trait}`** (the 3z "constraint violation" is a POINTING diag, not a bool): binds params, checks each concrete `satisfies` the param's constraints, **unbound params REMAIN** (stays generic), ground subtrees are identity, the walk is **memoized** (shared DAGs). `has_params`/conformance walks are iterative worklists+seen-set (no exponential re-visit). **Grammar**: `!param<T[,!trait<..>]*>`, `!trait<Name[,!trait<..>]*>`, `!fn<(P,..)->(R,..)>` (mandatory parens; `!fn<()->()>` legal; `->` the only new token) — byte-exact round-trip. **⛔ Trap (advisor, latent since 3a): the decoder admitted structurally-invalid types** (a `Vector` with 0 children, a `Callable` whose `count` indexes past `members` = OOB) — added `type_is_well_formed(const Type&)` (memory-safety arity/parity ONLY, not value semantics) called from `decode_type` (reject w/ byte offset) + a **patched-blob** rejection test (walks chunk headers to the `TYPE` fourcc, inflates a record's child-count). Tests: generics intern/dedup (param structural identity)/round-trip, transitive conformance, substitution accept/violation/unbound/nested/both-callable-sides, well-formedness units; `rich_graph` gains a generic-callable attr (round-trip+purity); fuzz + malformed cover the new kinds. **Gate:** crd-ceir-tests **70/70** on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy + GCC `-Werror=switch` (both `emit_type`+`type_is_well_formed` cover all 18 kinds) + invariants + opgen ctests green both OSes. | §16 §98 |
| ✅ | CEIR-3c | ✅ **CLOSED 2026-08-08.** **Resource + view types (§23).** Nine `TypeKind`s APPENDED (after `Callable`, ordinal-stable) → 27 total, **NO binary version bump** (all reuse the v2 record): `Buffer` (`count`=BufferMode raw/plain/structured/typed), `Image` (`count`=ImageDim, `members[0]`=format element — a TypeId, backend-agnostic; the engine `FgImageFormat` stays out per §23), `Sampler` (`is_signed`=comparison), `ResourceTable`, `AccelStruct`/`VideoFrame`/`AudioBuffer`/`ExternalResource` (niladic), `View` (`members[0]`=underlying resource, `count`=a **ViewRange presence MASK**). ⭐ **Fork (advisor): Interpretation B** — a view's TYPE says WHICH range dims (byte/element/mip/layer/aspect) it constrains, the range VALUES are RUNTIME (no producer until view-creation ops @ CEIR-4+; the SPIR-V/MLIR split; fits v2). Scope = all §23 EXCEPT `tensor`/`sparse_tensor` (Shape-dependent → 3d); §24 memory domains = value semantics, not types. Grammar (byte-exact): `!buffer<plain,!f32>`, `!image<d2,!vec<4x!f32>>` (dim keywords **ident-safe `d1`/`d2`/`d3`/`cube`** — a digit-leading `2d` can't parse back, caught first round-trip), `!sampler<cmp>`, `!restable<!f32>`, `!accel`/`!video`/`!audio`/`!external`, `!view<RES,mip,layer>`. **⛔ tri-split `view_combination_valid`** (buffers→byte/element, images→mip/layer/aspect, only buffers/images viewable): parser `fail()`s (the 3z "invalid view combination" diag, early), decoder rejects (byte offset), factory asserts. **⛔ corruption sweep caught advisor Trap 1** — on an already-failed parse the `if(m_ok && !valid)` guard short-circuited into the asserting `type_view` (corrupt-input ABORT); fixed by bailing on prior error first. **Trap 2**: `decode_type` bounds keyword-table scalars (buffer mode/image dim/view mask) — else a bad code prints as nothing (lossy text); free numerics (int width) stay unbounded. Tests: resource/view intern/dedup/round-trip (incl nested view-of-image), combination accept+reject (predicate + parser diag), well-formed units, keyword-bounds patched-blob; `rich_graph` view composite; fuzz valid resource/view; malformed grammar+combination. **Gate:** crd-ceir-tests **73/73** on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy + GCC `-Werror=switch` (both switches cover all 27 kinds) + invariants + opgen ctests green both OSes. | §23 |
| ✅ | CEIR-3d | ✅ **CLOSED 2026-08-08.** **Shape + tensor TYPES + compat predicates (§21/§35).** Four `TypeKind`s APPENDED (after `View`, ordinal-stable) → 31 total, **NO binary version bump** (reuse v2 record): `Dim` (`cols`=DimKind Static/Symbolic/Dynamic; Static uses `count`=extent, Symbolic uses `name`=symbol, structural-by-name), `Shape` (`members`=dims, rank=size, `!shape<>`=rank-0), `Tensor`/`SparseTensor` (`members`=[element, shape]; ~~sparse ENCODING is CEIR-21~~ → **CEIR-23** [STRUCK 2026-08-17 @ the CEIR-21 band-open, advisor + §22: sparse encoding/format ships WITH `ceir.sparse`/`ceir.quant` in CEIR-23 that consume it — no orphan encoding in the dense tensor IR]). ⭐ **Boundary (advisor):** 3d = TYPES + PREDICATES only; the `ceir.shape`/`ceir.tensor` value-op DIALECTS + LAYOUT (§22) are CEIR-21 (tensor/sparse RETURN here from the 3c Shape boundary; §35/§36 are dialect sections like §23's `ceir.resource`). Grammar (byte-exact): `!dim<4>` (digit→uint) / `!dim<dyn>` / `!dim<N>`, `!shape<!dim<4>,!dim<N>>`, `!tensor<!f32,!shape<..>>`, `!stensor<..>`. **Predicates tri-state `ShapeCompat{Compatible,Incompatible,Unknown}`** (Unknown = principled deferral to a CEIR-21 runtime check): `shapes_broadcast` (numpy align-right, reports the RIGHT-ALIGNED position of the first bad pair — the 3z "rank-mismatched broadcast" diag), `shapes_reshape` (static product equality, ⛔ overflow-checked→Unknown, the u32-wrap scar family). Rejected a broadcast-result constructor (no producer until 18). **⛔ Trap1 `dyn` reserved:** a symbolic dim named "dyn" would print `!dim<dyn>`→reparse Dynamic; tri-split (factory asserts, parser maps dyn→Dynamic, canonical rejects) + the Dim parser branch got the `!m_ok` bail. **⛔ Trap2 (latent since 3a, Dim makes acute): `type_is_canonical`** — every ignored field at its `Type::scalar` default (per-kind + per-DimKind; ⛔ default fkind=F32=2) — a `name` on an Int / extent on a Dynamic dim is a DISTINCT type that prints identically, breaking the 1f "two forms agree" contract; decoder rejects, `intern_type` asserts (subsumes well-formed). **Trap4**: `decode_type` bounds DimKind (`cols≤Dynamic`). Tests: dim/shape/tensor intern/dedup/round-trip, broadcast+reshape tri-state (one-is-1/rank-pad/0-extent/symbolic/dynamic/mismatch-with-index/overflow), a **3b×3d substitute seam** (param through a tensor shape), canonical patched-blob; `rich_graph` tensor composite w/ symbolic dim; fuzz dims→shape→tensor; malformed grammar+composition. **Gate:** crd-ceir-tests **77/77** on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy + GCC `-Werror=switch` (all switches cover 31 kinds + DimKind sub-switches) + invariants + opgen ctests green both OSes. | §21 §35 |
| ✅ | CEIR-3e | ✅ **CLOSED 2026-08-08.** **Physical quantities via crd-units dims + the units.erase dialect (§17/§18).** One `TypeKind` APPENDED (`Quantity`) → 32 total, **NO binary version bump** (reuse v2 record). ⭐ **Fork (advisor): bit-pack.** `crd::units::Dim<L,M,T,I,Th,N,J,A>` is COMPILE-TIME-only (8 i32 template exponents), so CEIR carries a RUNTIME mirror — the 8 signed base exponents (ADR-0078/mp-units-P1935; angle a distinct 8th base) **pack into count+cols** (8×i8=64 bits, NO new fields — the params-span principle from 3c/3d) via ONE `pack_dim`/`unpack_dim` pair (⛔ no inline bit math — sign-extension trap). "Reuse crd-units" is **mechanically verified**: `#include <crd/units/dim.hpp>` (link-legal ADR-0109) + a `static_assert` pinning base order to `crd::units::Dim`. `Quantity`: `members[0]`=underlying NUMERIC type, count+cols=packed dim; `quantity_composition_valid` (tri-split) accepts Int/Float/Index/Vector/Matrix/Complex/Quaternion + **TypeParam** (generic quantities), rejects qty-of-qty/dim/shape/aggregate (§18 "don't over-tag"). Grammar (ident-safe): `!qty<!f32,L1>`, `!qty<!f32,L1T-2>`, `!qty<!vec<3x!f32>,L1>`, `!qty<!param<T,..>,M1>`, dimensionless `!qty<!f32,1>` (⛔ empty `,>` rejected — parsed char-wise, base-letter+mandatory-signed-exp, canonical order, no dup). Relations (§17 seeds for CEIR-4): `quantity_dimensions_equal`→`DimMismatch{equal,first_differing_base}` (the 3z `Length+Time` diag), `quantity_dim_mul/div` (⛔ i8 overflow→FAILURE not wrap). ⭐ **The erase op is the FIRST NON-REFERENCE dialect through the CEIR-2 generator**: `engine/ceir/ops/units.ceirop.toml` (dialect `units`, op `erase`, Pure) added by a TOML edit + regen, ZERO central edits — all 5 artifacts appeared, drift now **3 dialects × 5 files**, the auto-generated smoke passes with no hand-written test (the generator earning its keep). Untagged-numeric enforcement is predicate-now; a per-op public-domain-API schema flag arrives when domain dialects land (CEIR-4+; no producer yet — the 3b-conformance precedent). Trap: sign-extension (negative-exp pack/unpack tests at every base); dimensionless Quantity is LEGAL + DISTINCT from raw `!f32`. Tests: quantity pack/unpack/intern/dedup/round-trip (generic+vector+dimensionless), dimension relations + composition; `rich_graph` `ty5`=`!qty<!vec<3x!f32>,L1>`; fuzz quantity; malformed corpus. **Gate:** crd-ceir-tests **83/83** on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy + GCC `-Werror=switch` (32 kinds) + invariants + opgen drift(3 dialects)/validator green both OSes. | §17 §18 |
| ✅ | CEIR-3f | ✅ **CLOSED 2026-08-08.** **Ownership/lifetime qualifiers + the escape predicate (§19).** One `TypeKind` APPENDED (`Qualified`) → 33 total, **NO binary version bump** (reuse v2 record). ⭐ **Fork (advisor): ONE `Qualified` wrapper** — `members[0]`=the qualified type, `count`=a mutually-exclusive **`OwnershipKind`** enum (9 §19 categories: `imm`/`mut`/`borrow`/`own`/`shared`/`weak`/`state`/`ext`/`transient`) — NOT nine separate kinds (a value carries exactly one ownership mode; a wrapper composes over resources+values uniformly). `type_qualified` + tri-split `qualified_composition_valid` (accepts numerics/aggregates/generics/**resources** — a borrowed buffer is the whole point; rejects qualifier-of-qualifier / dim / shape / trait = meta-types that aren't runtime values). Grammar (byte-exact): `!qual<borrow,!buffer<plain,!f32>>`, `!qual<own,!tuple<!f32,!i32>>` — 9 keywords in an **unguarded printer/parser if-chain⇔switch** (no `-Werror=switch` on the parser side ⇒ the round-trip test covers **ALL NINE**). Decoder: kind range widened to `Qualified`, `count≤TransientArena` keyword-bound, composition re-check. **The escape rule splits 3f/3z (advisor):** 3f delivers the region-visible predicate **`value_escapes_region(const Value*, const Region*)`** (walks the def-use list; a use whose region isn't contained by the defining region = escape; direction-correct via `region_contains` upward walk); 3z composes it over `Qualified<BorrowedView>` in a module walk. **⛔ IR-CORE SCAR (advisor-flagged, latent since 1a): `create_operation` never wired `Region::m_parent`** (the region→op back-link) — invisible until 3f's first UPWARD walk; a null chain makes `region_contains` bail early → **false-positive "escape"**. Fixed at region creation (both parser+decoder route through `create_operation`, so parsed/decoded modules inherit it — 3z depends on this). **⛔ SUBST COMPOSITION HOLE (advisor, retroactively closes 3e/3b): `substitute` is a FOURTH construction path** bypassing the tri-split — binding `T→qual`/`T→qty` could intern a composition-invalid `qual<_,qual<..>>` / `qty<qty<..>>` that prints-but-won't-reparse; `subst_rec` now re-checks Quantity/Qualified/Tensor/SparseTensor composition before intern and fails through `SubstResult{...,failed_compose}` (a pointing diag). Escape contract = **direct-use only, type-directed** (every value TYPED `Qualified<BorrowedView>` checked vs its defining region; leaks through untyped intermediates aren't chased — the contract 3z's walk implements). Unqualified `!f32` ≠ `!qual<imm,!f32>` (distinct, intentional). Tests: qualifier intern/dedup/round-trip (all 9 kinds + nested resource/aggregate), composition accept+reject, `value_escapes_region` (no-use/inside/escape + a **3-level nest**), a **3b×3f substitute seam** (param through a qualifier, ownership preserved) + a composition-violation reject (qual+qty), keyword-bound patched-blob; `rich_graph` `ty6`=`!qual<borrow,!buffer<plain,!f32>>`; fuzz+malformed inherit it. **Gate:** crd-ceir-tests **87/87** (773 assertions) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy + GCC `-Werror=switch` (33 kinds + `emit_ownership` 9-way) + invariants + opgen ctests green both OSes. | §19 |
| ✅ | CEIR-3z | ✅ **CLOSED 2026-08-08. BAND-3 GATE.** The four band-3 error checks fire as **POINTING diagnostics** (each names the exact offender, and the pointing field **discriminates** — a second case moves it, a control case does not fire): **(1) `Length+Time`** — `quantity_dimensions_equal`→`DimMismatch.first_differing_base` (L-vs-T → base 0; L-vs-LT → base 2, ruling out a hardcoded 0); **(2) rank-mismatched broadcast** — `shapes_broadcast`→`BroadcastResult.position` (`[4,3]`vs`[5,3]` → right-aligned position 1, + a Compatible control); **(3) borrowed-view region escape** — a NEW public module walk `find_borrowed_escape(Module&)`→`BorrowEscape{value, escaping_use}` composing `value_escapes_region` over every value typed `!qual<borrow,_>` (both block args AND op results; the non-escaping borrow and the escaping *owned* value are both correctly ignored — the type-directed contract); **(4) generic-constraint violation** — `substitute`→`SubstResult.failed_param`/`failed_trait` (exact `(T, Ord)`, + an accept control after `register_conformance`). ⭐ **Scope boundary (advisor, honest):** 3z ships pointing **predicates** + the one `find_borrowed_escape` module walk (the borrowed-view escape is a program property whose inputs — typed values, regions, uses — exist NOW); it is **NOT** a module-level type verifier — op-level wiring of (1)/(2)/(4) onto typed operands composes at **CEIR-4** (no typed-operand producer op exists yet — the 3e "predicate-now" precedent). **Pointing upgrade to the 3f surface:** `value_escapes_region` (bool) gained the companion `first_escaping_use(Value*,Region*)→const Operation*` (a bool can't point; a SourceLoc-bearing op can); `value_escapes_region` now delegates to it (no API break). `find_borrowed_escape` is **first-offender** (deterministic pre-order = the printer's SSA order), **structural not dominance-aware** (walks uses; the CFG/dominance verifier is CEIR-5b), **direct-use + type-directed** (a leak through an untyped intermediate is caught only if the intermediate itself carries the borrowed type). Dedicated artifact `tests/ceir/test_band3_gate.cpp` (4 TEST_CASEs, `[gate3]`). **Gate:** crd-ceir-tests **91/91 (791 assertions; +20 gate)** on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy + GCC `-Werror=switch` + invariants + opgen ctests green both OSes. **→ BAND 3 CLOSED.** | §16 §17 §19 §21 |

## CEIR-4 — Effect + determinism model — ✅ CLOSED (4a–4z) → §26 §27 §15

**Band contract.** Every effectful op declares semantic effects (§26 families) optionally carrying resource/range
identity — the frame graph's proven read/write/lifetime discipline (WAR-needs-lifetime, RMW-not-RWM scars) promoted
to first-class IR. Determinism classes (§27) **align 1:1 with the ADR-0098 T1/T2/T3 tiers CKIR already certifies** —
ONE vocabulary. Evaluation domains (§15) + real-time classes (§32) as metadata with legality checking. Numerical
semantics (§28) attach here. **Gate: compiler distinguishes reorderable vs ordered ops correctly.**

| ✔ | Slice | Deliverable | → §§ |
|---|---|---|---|
| ✅ | CEIR-4a | ✅ **CLOSED 2026-08-09.** **The §26 effect vocabulary + typed effect records, declared via the 2a schema.** `effect.hpp`: **`EffectFamily`** (all **27** §26 families verbatim, `u8`, ⛔ APPEND-AT-END — a `static_assert` pins `Debug==26` in lockstep with the generator's `EFFECT_FAMILIES`; NO subsetting per NO-FOLLOW, `MemoryReadWrite` stays a family) + **`EffectRecord`** POD `{EffectFamily family; EffectTarget target(None/Operand/Result); u32 index; u32 range_mask}` (a trivially-copyable POD, no StringView, so generated `constexpr` arrays + arena copies are cheap; `range_mask` reuses the **3c `ViewRange`** bitmask, 0=whole resource). Declaration is **KIND-LEVEL** (an effect names an operand/result POSITION; per-instance SSA resolution is CEIR-4d; instance-*dependent* effects — a call's callee-derived set — get an `EffectsFn` hook at CEIR-5, NOT built now [✅ BUILT at CEIR-5c: `OpInfo::effects_fn` + `Context::effective_effects`]). ⭐ **Attach point = B1 (advisor):** effects live on **`OpInfo`** (beside traits+verify), passed to `register_op(op, traits, verify, effects={})` which **arena-COPIES** them (the caller's span need not outlive the call — the alloc-outlives-borrowers rule), queried via **`Context::op_effects(OpId)`** — NOT a CEIR-1d `InterfaceId` and NOT reflection-only; the "effect *interface*" IS `op_effects` over `OpInfo` (the row's word is not a `1d` interface). ⛔ **EMPTY≠UNKNOWN contract** (header + tested): `op_effects` empty means "registered + provably effect-free" ONLY when `op_info(kind)!=nullptr`; an UNREGISTERED kind (§6.11 unknown dialect) is MAXIMALLY effectful, never reorderable. **⛔ func.call landmine (advisor):** a registered op that forgets to declare reads as *provably* effect-free — `func.call` defaulted so → now declares a conservative `ExternalCall` barrier (memory `feedback_registered_default_empty_reads_as_provably_none`). **Pure ⇒ zero effects** (§26) at BOTH live arms — the generator (pointing cook error) + a `register_op` assert (+ a hand-path `family≤Debug && target≤Result` bound). **2a schema:** TOML `effects` accepts a bare family string OR an inline table `{family, operand|result, range=[...]}` — the generator validates the family vocabulary + operand/result index bounds + range names (pointing errors), emits typed `EffectRecord` arrays (before `register_%s_ops` so both it and the `OpSchema` table reuse them), and `OpSchema.effects` upgrades `StringView[]→EffectRecord[]` (the "strings until CEIR-4" scaffold retired; `.ops.json` effects went `string[]→object[]` under schema_version 1 deliberately — a scaffold field, no external consumers). Effects are **registration-time data, NOT serialized** — no binary surface touched, no version bump. `test.ceirop.toml` exercises BOTH forms (full-surface reference). Tests: `test_effect.cpp` (round-trip both forms, empty≠unknown, arena-copy under ASan, func.call barrier) + `test_opgen.py` +11 (family/index/range/Pure rejections + lockstep count). **Gate:** crd-ceir-tests **98/98 ctest** (820 assertions) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** (Windows via `ctest`, not binary-direct) + LLVM-20 tidy + GCC `-Werror=switch` + `crd-ceir-opgen-{drift,validator}` (36 py tests) + invariants green both OSes. | §26 |
| ✅ | CEIR-4b | ✅ **CLOSED 2026-08-09.** **§27 determinism classes + compiler modes + §28 numerical semantics.** `semantics.hpp`: **`DeterminismClass`** (the 5 ADR-0098/§27 tiers + **`Unspecified=0` = the default/no-claim**, `u8`, static_assert-pinned to the generator's ordered `DETERMINISM_TIERS`) carried on **`OpInfo`** (the 4a attach-point mirror — `register_op(…, det=Unspecified)`, arena-free scalar), declared via a top-level 2a-schema `determinism` field, queried by **`Context::op_determinism`**. ⛔ **EMPTY≠UNKNOWN** as for effects — an unregistered kind returns Unspecified but is UNKNOWN (`op_info` first); Unspecified fails any strict mode ("you cannot certify what was never classified"). **Naming reconciled** to §27-verbatim `ExternalNondeterminism` (was the short `External`; no TOML used it; ADR-0110 §2.1 struck in-place). **Two levels, both real (§27 "operation/provider"):** the op-level class (semantic contract) + `[op.native].determinism` (provider claim) stay distinct; a **cook-time consistency** check requires native to be **at-least-as-strong** as the op (documented rank BitExact>WithinTarget>WithinBackend>Nondeterministic≥ExternalNondeterminism — the last pair a judgment call: External is a narrower source). `OpSchema.native_determinism` typed `StringView→DeterminismClass` (scaffold retired). **`CompilerMode`** (`Normal`(default)/`Fast`/`Deterministic`/`CertifiedDeterministic`) is **session state, never serialized** (proven: setting it doesn't change a module blob). Legality: **`determinism_satisfies_mode`** (documented map: Certified→BitExact only; Deterministic→the 3 deterministic tiers; Normal/Fast→all) — tested as the **full 6×4 matrix**. ⭐ **§28 `NumericalSemantics`** — all **12** §28 knobs verbatim (IEEE/fast-math/FMA/FTZ/denorm/rounding/overflow/int-wrap/NaN/precision/mixed/stochastic), each 0=Inherit, **per-INSTANCE** (the same add can be strict here, fast there) so it rides ONE packed **`numerics` int ATTRIBUTE** (12 nibbles → i64; `pack`/`unpack` one pair; ⛔ unpack VALIDATES bounds — a corrupt nibble is rejected, the "decoder arm"; `numerics` is now RESERVED attr vocabulary). **`numerics_satisfies_mode`** honors the **fmad scar** (Fast ADMITS FMA/fast-math; Certified/Deterministic forbid fast-math + Relaxed-IEEE but ALLOW FMA — deterministic when consistent). ⛔ **The enforcement primitive `find_mode_violation(Module&)`** walks pre-order and returns the FIRST op violating the active mode — its determinism class (§27) **OR** its per-instance numerics (§28) **OR a corrupt `numerics` attr (a violation in EVERY mode, Normal included)**. **Boundaries (honest):** pass-wiring is deferred (no pass manager until **CEIR-6**); §27 replay-recording + deterministic-alternatives are runtime/executor (**CEIR-5+**); the kind's class vs an instance's numerics are NOT cross-checked here (a `BitExact` kind with an instance `fast_math=On` is effectively degraded — an op-verifier concern, CEIR-6-ish). `arith` int ops declare `BitExact` (true, cheap — gives the walk material); `test.dummy` carries an op-level class weaker than its native claim (the valid full-surface case); func ops stay Unspecified. Tests: `test_semantics.cpp` (op_determinism round-trip + empty≠unknown, the 6×4 matrix, `find_mode_violation` for §27 AND §28 + corrupt-attr, mode-not-serialized, numerics pack/unpack + out-of-range reject + text/binary attr survival, fmad-scar legality) + `test_opgen.py` +4 (unknown determinism, native-weaker-than-op reject, native-stronger OK, tier lockstep). **Gate:** crd-ceir-tests **106/106 ctest** (both OSes via `ctest`) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy + GCC `-Werror=switch` (rank + satisfies switches complete) + `crd-ceir-opgen-{drift,validator}` (40 py) + invariants green. **No binary version bump** (numerics rides the existing int-attr; determinism is registration data). | §27 §28 |
| ✅ | CEIR-4c | ✅ **CLOSED 2026-08-09.** **§15 eval domains + §32 realtime classes + the domain-legality verifier.** ⭐ **register_op consolidated into an `OpSpec` descriptor (advisor, decided at 4c open):** `register_op(op, spec={})` with `OpSpec{traits, verify, effects, determinism, domain}` — ONE signature (C++20 designated initializers: `{.effects=…, .determinism=…}`), MIGRATED at every site this slice (generated regen + func.cpp×3 + test_effect/test_semantics/test_builder); `spec.effects` is still arena-COPIED (the spec is a temporary). **`EvalDomain`** (Unspecified=0 + the 10 §15 domains verbatim, static_assert-pinned to the generator's `EVAL_DOMAINS`) on `OpInfo` via the spec, declared through the 2a `domain` field (the `"host"` scaffold → real §15 values; `OpSchema.domain` StringView→EvalDomain — 3rd scaffold retired), queried by `Context::op_domain`, same EMPTY≠UNKNOWN discipline. ⛔ **The 4c walk does NOT consume kind-domain** — kind-domain × region-domain placement checking is an op-verifier concern (CEIR-6-ish); the §15 partial-evaluation use is later still (predicate-declared-now). **`RealtimeClass`** (Unspecified + the 7 §32 classes verbatim, static_assert-pinned) — deliberately **NOT a 2a-schema field** (a REGION property, not per-op-kind), so NO generator vocabulary; `Offline` (a deadline class) and `OfflineTime` (an eval domain) are DISTINCT axes. ⭐ **A region's (domain+realtime) rides ONE packed `region_exec` int ATTRIBUTE on the region-OWNING op** (regions have no attr dict; rides the existing attr machinery — module CONTENT, no new serialized surface, no version bump; `region_exec` is now RESERVED attr vocabulary; pack/unpack validated). Symmetric with 4b: the compiler MODE is session state (never serialized); the region tag IS content (survives print/parse AND serialize/deserialize — the verifier re-fires post-round-trip, tested both forms). ⛔ **`find_domain_violation(Module&)→DomainViolation{op, region_owner, effect, unknown_kind}`** — pre-order, INNERMOST-tag-wins (an owner's tag replaces its parent's for its subtree; module body untagged/unconstrained; PRESENT-empty `{}` is an explicit unconstrain OVERRIDE, distinct from an ABSENT/inheriting attr; whether a nested region may RELAX is execution semantics → CEIR-5+). Seeded §32 rule: a `FileIO`/`NetworkIO` effect is illegal where `realtime==AudioRealTime` OR `domain∈{DeviceTime,HostAudioTime}` (the table function is the extensible point; ~~`ExternalCall` stays legal-for-now — CEIR-5's EffectsFn refines calls~~ **[✅ CEIR-5c: `ExternalCall` FLIPPED to forbidden in audio-RT — the EffectsFn hook now resolves a `func.call` to its callee's PRECISE families, so a remaining `ExternalCall` means genuinely-unmodeled code; a call to a pure func passes, a FileIO/unresolved call flags]**). ⛔ **The vacuous-pass trap:** an UNREGISTERED op in an audio region is MAXIMALLY effectful ⇒ flagged (`unknown_kind`; ~~FileIO stands proxy for "tag is constraining"~~ **[CEIR-5c: now reports `ExternalCall`, the barrier the unregistered op's effective mask carries — not a FileIO proxy]**), while a registered effect-free op (arith.addi) is legal — empty≠unknown doing real work. A corrupt `region_exec` = a violation at the owner (`op==region_owner` the only discriminator). Separate from `find_mode_violation` (mode=session vs tag=content); CEIR-4z composes them. arith ops declare `EitherHostOrDevice`; test.dummy `HostFrameTime`; func ops Unspecified. (ADR-0110's IntrinsicDesc effect/domain/determinism bundling is future — no edit.) Tests: `test_domain.cpp` (9 cases: op_domain round-trip + empty≠unknown, region_exec pack/reject, the legality matrix, tag survival + verifier RE-FIRE through text AND binary, forbidden-effect-flagged vs effect-free-legal, unregistered-flagged vs registered-legal, nested inherit-through-untagged + innermost-override + present-empty-unconstrain, corrupt-tag-at-owner) + `test_opgen.py` +3 (unknown domain, 10-tuple lockstep, the `"gpu"`→DeviceTime scaffold fix). **Gate:** crd-ceir-tests **115/115 ctest** (both OSes via `ctest`) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy + GCC `-Werror=switch` + `crd-ceir-opgen-{drift,validator}` (43 py) + invariants green. **No binary version bump.** | §15 §32 |
| ✅ | CEIR-4d | ✅ **CLOSED 2026-08-09.** **Effect-derived ordering hazards (§26/§116) — the frame-graph read/write/lifetime discipline promoted to IR.** A **SCHEMA-QUIET** slice (hazards are DERIVED from 4a effects — no TOML/generator/regen/py, first of the band). `hazard.hpp`: **`HazardKind{None,War,Raw,Waw}`** + **`ResourceClass`** (14 disjoint classes: Memory/HostState/Scene/Ecs/Physics/Audio/Gpu/Io/Time/Random/Log/Debug + `Universe` (overlaps all) + `None` (inert)) + **`effect_access(EffectFamily)→{reads,writes,ResourceClass}`** — a TOTAL switch over all 27 families, ⛔ **NO default case** so a 28th unclassified family is a `-Werror=switch` COMPILE ERROR (the append-at-end guard, free). The conservative-correct judgment calls: Memory-class covers **Alloc/Dealloc/ResourceResidency (write)** so `Deallocate(R)`-then-`read(R)` use-after-free is VISIBLE; GPUCommand→Gpu(w); the three I/O families→ONE `Io` class(rw) (cross-channel reorder conservatively forbidden); **ExternalCall + Synchronization→`Universe`(rw) = a full barrier**; ⛔ **`RandomRead` WRITES `Random`** (a PRNG draw ADVANCES the stream — the naive "read means read" silently makes RNG draws reorderable, destroying replay determinism); **`TimeRead` a pure inert read** (no writer of Time; replay-ordering of clock reads is the recorder's concern, CEIR-5+); `Nondeterministic` inert (4b's DeterminismClass is the real signal); Logging/Debug write their own class. **`Context::ops_hazard(before, after)→HazardKind`** — pure, DIRECTIONAL (caller asserts before precedes after); the STRONGEST hazard (**WAW>RAW>WAR** documented convention) over every effect-pair sharing a resource + overlapping range with ≥1 write. **Resource identity** = `(class, Value*|null)`: operand/result `EffectTarget` → the SSA `Value*`; ambient (`target None`) → `null`=whole class; overlap = Universe-either ∨ (same class ∧ (same `Value` ∨ either null)); ranges overlap iff `m1==0 ∨ m2==0 ∨ (m1&m2)!=0`. ⛔ **DISTINCT SSA Values assumed NON-ALIASING** (vacuously safe — no view-creation op exists yet; a CEIR-6+ alias model refines it). **Reports EVERYTHING** — an effect-derived RAW that coincides with an SSA def-use edge is NOT filtered (a scheduler union-ing free SSA edges dedups trivially). ⛔ EMPTY≠UNKNOWN: an UNREGISTERED op = Universe rw (hazards anything); a registered EFFECT-FREE (Pure) op touches nothing ⇒ `None` vs anything. **`Context::collect_block_hazards(Block&, Array<Hazard>&)`** — the O(n²) all-pairs CORRECTNESS reference over ONE block's ops in LIST (authored-linearization) order; NO transitive reduction (the scheduler's job); CEIR-15d builds the indexed version on the same primitive. Cross-block / loop-carried need the CFG (CEIR-5b). Enabling change: `Operation::result(u32)` loosened to `const` (mirrors `operand`, non-breaking). Tests: `test_hazard.cpp` (7 cases, 46 assertions: classifier incl RandomRead-writes/TimeRead-inert, the W→R/R→W/W→W/R→R quartet on a shared operand, range disjoint/overlap + distinct-Value non-aliasing, ambient-vs-specific + RandomRead/Logging/TimeRead families, func.call barrier + unknown-vs-effectful + unknown-vs-Pure=None + WAW>RAW>WAR precedence, **Result-targeted RAW + the Deallocate WAR/UAF lifetime scar**, the collect_block_hazards exact edge-list). **Gate:** crd-ceir-tests **122/122 ctest** (both OSes via `ctest`) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy + GCC `-Werror=switch` (classifier total switch) + `crd-ceir-opgen-{drift,validator}` (43 py, drift green with NO regen — correct for a schema-quiet slice) + invariants green. **No binary version bump.** | §26 §116 |
| ✅ | CEIR-4z | ✅ **CLOSED 2026-08-09. BAND-4 GATE.** The band contract's exit criterion — **"the compiler distinguishes reorderable vs ordered ops correctly"** — met verbatim over ONE curated module composing all three band-4 axes. ⭐ **TEST-ONLY gate** (like 3z): reuses `find_mode_violation` (4b) + `find_domain_violation` (4c) + `ops_hazard`/`collect_block_hazards` (4d); a unified `verify` entry stays UNBUILT until a consumer exists (CEIR-6 passes / 12d scheduler) — the three predicates have different lifetimes (mode = session, tag = content, hazards = pairwise-derived), and one entry point would blur them (the 4c header comment corrected in-place). **Centerpiece — the WAR-needs-lifetime scar** (`feedback_frame_graph_war_needs_resource_lifetime`): `read(R)` then `write(R)` over one buffer is **WAR purely from effects + SSA resource identity** — NO declaration-order metadata anywhere — with the adjacent contrast (the same two op kinds on a DIFFERENT buffer → `None`), so the test reads as the scar's proof, not just an assertion. **Curated module (staged):** a baseline block that is Certified-clean + domain-clean but hazard-rich (`buf`/`write`/`read`/`write2`/`rng`/`rng2`/`log`) — `collect_block_hazards` asserts the EXACT 4-edge forward matrix (W→R RAW, W→W WAW, R→W WAR, rng→rng2 WAW; every other pair `None`, pinned by absence + `REQUIRE`d size) + a reverse-direction sweep (RAW↔WAR flip, WAW symmetric) = the row's "every pair classified correctly" honestly. ⛔ **The BitExact-baseline trap** (empty≠unknown's 5th appearance): a gate dialect registered with DEFAULT (Unspecified) determinism would make Certified flag the first op and the mode axis test nothing — so EVERY gate op is `BitExact` (a seeded PRNG is bit-exact too), and `func.call`/unknown ops are kept OUT of the main block. **Orthogonality (the composed claim):** each axis perturbed in isolation moves ONLY itself, asserted by FULL edge-list IDENTITY (endpoints + kind, not just count): a per-instance `fast_math` numerics knob trips Certified (`find_mode_violation` → that op) — hazards identical; an appended Unspecified op trips the DETERMINISM half — hazards identical; a side region tagged audio-RT with a `FileIO` op fires `find_domain_violation` — the main block's hazards identical. Unknown-op-is-a-barrier vs Pure-op-is-reorderable proven in a SIDE block (Universe edges would spray the exact list). ⭐ **The round-trip Value-identity claim** (nothing had fired it): serialize→deserialize the curated module, re-register the dialect, re-`collect_block_hazards` → same edge kinds — proving the shared-operand `Value` identity reconstitutes through the parser's operand FIXUP pass (effect re-binding was 4c; Value-identity-through-fixup is new). Dedicated `test_band4_gate.cpp` (5 cases, `[gate4]`, 38 assertions). **Gate:** crd-ceir-tests **127/127 ctest** (both OSes via `ctest`) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy + GCC `-Werror=switch` + `crd-ceir-opgen-{drift,validator}` (43 py) + invariants green. **→ BAND 4 CLOSED (4a–4z).** | §26 §168 |

## CEIR-5 — Structured control flow + functions — ✅ **CLOSED 2026-08-09** (5a ✅ 5b ✅ 5c ✅ 5d ✅ 5z ✅) → §13 §14 §20

**Accumulated landings (placed at 4-band close, advisor):** the `EffectsFn` hook (func.call's callee-derived effects) → **CEIR-5c** ✅ **DONE** (`OpInfo::effects_fn` + `Context::effective_effects`, consumed by the hazard `table` overloads + `find_domain_violation`, closing the 4c "ExternalCall stays legal-for-now" gap); cross-block hazards → **5b at earliest, honestly re-deferred to CEIR-15d** (5b builds dominance/terminator *verification*, not CFG-reachability hazard analysis); nested-region-relax (may a child region relax its parent's realtime class) → ~~**5d/5z** (needs execution semantics — an executor that RUNS regions gives relax its meaning)~~ **[✅ RE-ROUTED at 5z close (struck in place): the CEIR-5z reference executor is a SEMANTIC executor — it never consults `region_exec` (a region tag is a scheduler/domain-legality concern, not a value-semantics one), so "an executor that runs regions" INFORMS but does NOT decide relax. Nested-region-relax is a SCHEDULER concern → CEIR-15 (the frame/schedule band), where region execution constraints actually bind.]**


**Band contract.** `ceir.core` structured ops — `if`/`switch`/`for`/`foreach`/`while`/`match`/`scope` as
region-carrying ops (§13 §14); SSACFG blocks+branches beneath them; calls + bounded-recursion attrs; explicit
state: `state<T>`/`history<T>`/`delay<T>` (§20) — graph cycles ONLY through state/delay (verifier rule). Plus the
first **reference executor** skeleton (host, slow, correct — §118) running core+func+state. **Gate: a nontrivial
program executes in the reference host runtime.**

| ✔ | Slice | Deliverable | → §§ |
|---|---|---|---|
| ✅ | CEIR-5a | ✅ **CLOSED 2026-08-09** (landed across a checkpoint + a continuation; ONE commit supersedes the checkpoint proposal). ⭐ **DONE:** (1) **Generator region-SIGNATURE schema evolution** — a `regions` list entry may declare `{kind, args=[{name, type_hint}], variadic}`; `type_hint` is a DOC string (NO TOML type grammar — the 3a lesson; real arg types are construction-time), the arg COUNT is the structural contract, `variadic` only on the last entry (validation + verifier emission + 4 py tests). (2) The **`ceir.core` dialect** via 2a schemas: **`scope`** (1 region), **`if`** (cond + 2 regions), **`for`**/**`foreach`** (1 region with a typed entry-block arg — induction var / element), **`yield`** (variadic-operand Terminator). ⛔ Every core op is **BitExact** (a control-flow CONSTRUCT is deterministic) and declares **ZERO effects** (a structured op is not itself effectful — its effects are its CONTENTS', which the CEIR-4d per-block hazard walk already sees). (3) **`Context::fold_constant_if`** — the constant-condition `if` fold (partial-eval seed): a constant `arith.const` cond splices the TAKEN region's single block into the parent before the `if` and erases the `if`. ⛔ **BAILS (no mutation)** on: not-a-`core.if`; non-constant cond; multi-block region; missing `core.yield` terminator; a `region_exec`-tagged `if` (CEIR-4c — inlining would delete the domain constraint); AND ⛔ **any `region_exec`-tagged op INSIDE the taken block** (the 2nd band-4-metadata-breaks-a-band-5-rewrite case — a rewrite must audit the attrs it MOVES, not only the op it deletes). ⭐ **DONE (the continuation):** (4) **Variadic RESULTS** — the generator's 3rd variadic axis (validation + variadic-aware `num_results` verifier); `scope`/`if` gain variadic results (`results = [{variadic}]`), value-producing. (5) **`while`** (2 regions: cond + body; structural now — the cond-yields-exactly-1-bool contract is CEIR-5b). (6) **`switch`/`match`** — VARIADIC case regions (a selector/scrutinee operand + N regions; STRUCTURAL only, PATTERN semantics are CHIR's). (7) **`fold_constant_if` RAUW value-forwarding** — folds the taken region (THEN *and* ELSE contents, both tested) and RAUWs the `if`'s results with the taken `yield`'s operands; a new ⛔ bail on a yield-operand-count ≠ if-result-count MISMATCH (else the RAUW would index OOB — a UAF-shaped bug). **The yield↔owner count contract is enforced at its SOLE consumer (the fold); the general region-walking verifier lands at CEIR-5b** (where the 3f `parent_op` back-link earns its keep). **FOUR design decisions a resumer inherits:** SKELETON-VERIFIES (entry-block arg count checked only WHEN a block exists — existence is 5b); STATEMENTS-first→VALUES-landed; **HOMOGENEOUS result types** (all results share one placeholder type — per-result typing joins at CEIR-6 inference, and the fold REPLACES result values with the yields regardless, so NO `create_operation` overload was needed); **MIN-ARITY builders** (a generated builder produces the minimum arity on every variadic axis — operands/results/regions; full-arity construction is `create_operation`; noted in the generated hpp banner). Loop-carried loop values (iter_args) → **CEIR-5d** (§20 feedback-only-through-state) **[✅ RESOLVED at 5d, but NOT via iter_args: struck in place — §20's spelling is `core.state`/`delay`/`history` (a state op INSIDE the loop body carries the accumulator); iter_args-as-structural-sugar is consciously NOT built, being an MLIR-ism absent from §13/§20]**. ⚠ **Process note:** this continuation DISPROVED two blockers the checkpoint row asserted — the "region-count builder param owed" (the MIN-ARITY convention made it unnecessary) and the implied `create_operation` overload (HOMOGENEOUS placeholders made it unnecessary): a checkpoint's blocker-claims are GUESSES unless the code was read — mark them unverified. **Gate:** crd-ceir-tests **138/138 ctest** on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy + GCC `-Werror=switch` + `crd-ceir-opgen-{drift,validator}` (49 py) + invariants green. | §13 §14 |
| ✅ | CEIR-5b | ✅ **CLOSED 2026-08-09.** **The §115 structure verifier.** `Context::find_structure_error(Module&) -> StructureError{op, value, kind}` — ONE top-down region walk returning the FIRST structural defect (deterministic pre-order = the printer's SSA order, the 3z first-offender convention) or `None`. `StructureErrorKind` is a **no-default `-Werror=switch`** enum (6): `UseBeforeDef`, `CaptureThroughIsolation`, `MissingTerminator`, `TerminatorNotLast`, `YieldCountMismatch`. **[⚠ CEIR-5d extended this enum +2 (`FeedbackWithoutState`, `StateDepthInvalid`) and RE-CLASSIFIED same-block forward/self references from `UseBeforeDef` to `FeedbackWithoutState` — the 5b use-before-def tests' expectations were updated in place; a genuine `UseBeforeDef` is now a LATER-block / never-defined value.]** **Three §115 checks for the current (no-branch-op) world:** **(1) dominance = per-block def-before-use + capture visibility** — a `DomScope` chain (each block frame owns its `defs`: block args, then op results appended AS the walk passes each op) models the visible SSA environment; ⛔ **isolation-aware** — the walk STOPS at an `IsolatedFromAbove` frame, so a capture that WOULD be visible without isolation but is blocked by it is `CaptureThroughIsolation`, a distinct pointing diagnostic **discriminated by re-probing visibility with isolation OFF** (vs `UseBeforeDef`). **(2) terminators by RegionKind** — an `SsaCfg` block MUST end with a `Terminator`-trait op (an EMPTY block fails too); a `Graph` region imposes none (order is data/effects). ⭐ **The 3f `parent_op` back-link EARNS ITS KEEP here:** an empty SsaCfg block has no op to blame → `MissingTerminator` points at the region's OWNER via `Region::parent_op()` (a diagnostic must point at *something* — 3z; the outermost module body is Graph so this is always non-null). `TerminatorNotLast` catches a non-last Terminator. **(3) the yield↔owner COUNT contract** — generalized from its SOLE 5a consumer (the fold) to every `core.*` region owner: the terminating `core.yield`'s operand count == the owner's result count, with the table override that `core.while`'s cond region (region 0) yields exactly **1** (the loop test). **⚠ Re-scope carried from the 5a `while` line (struck in place):** 5b enforces the cond **COUNT**; **BOOL-ness → CEIR-6** typing (core-op results are HOMOGENEOUS placeholders — a cond fed by a structured result has no checkable element type today). **FOUR conscious relaxations (advisor pre-close) a resumer must not mis-infer:** (a) the yield contract binds `core.*` owners ONLY — a foreign-dialect region owner is unconstrained (open-world / I6-consistent); (b) a **0-result** core region with **no yield** passes (implicit-fall-through statement region); (c) a value-producing op with **EMPTY regions FAILS** — deliberately **STRICTER than the op verifier's SKELETON-VERIFIES**: `find_structure_error` is the whole-program VALIDITY layer (the syntactic-vs-structural split that makes the 1h fuzz corpus — terminator-less SsaCfg blocks, self-capturing nested regions — inappropriate to assert `==None` over; exercised instead by the hand "rich nested valid module" over-strictness test); (d) dominance is **per-block** (sibling blocks invisible — conservative-correct with no branch ops; refined when branches + real CFG dominance land, cross-block reachability hazards stay re-deferred to 12d). **Schema-quiet** — ZERO generator/TOML/regen (opgen drift green with no regen, correct). Enabling surface all pre-existing (no API break): `Region::parent_op()` (3f), `Operation::result(u32) const` (4d). **§167 map ticked:** the CEIR-1h "dominance + region legality → CEIR-5b" DEFERRED item is now covered; the 3z "CFG/dominance verifier is CEIR-5b" pointer satisfied (`find_borrowed_escape` stays structural — a different use-walking layer). Tests: `test_structure.cpp` (7 `[structure]` cases, 19 assertions: well-formed; a rich nested valid module = the over-strictness guard; use-before-def forward+self; capture non-isolated=None vs through `iso.box`=CaptureThroughIsolation; capture-after-owner=UseBeforeDef; terminator rules incl. **empty-block-points-at-owner** + Graph=None + TerminatorNotLast; the yield count contract for `if` + `while` cond). **Gate:** crd-ceir-tests **145/145 ctest** (both OSes via `ctest`) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy (3 files) + GCC `-Werror=switch` + `crd-ceir-opgen-{drift,validator}` (49 py, drift green NO regen) + invariants. **No binary version bump.** | §13 §115 |
| ✅ | CEIR-5c | ✅ **CLOSED 2026-08-09.** **Calls: the callee-derived-effects hook + the recursion-policy verifier (§14/§34).** TWO features, both HAND-side (⛔ **schema-quiet** — zero TOML/generator/regen; opgen drift green with NO regen). **(1) The EffectsFn hook (the CEIR-4a landing).** A NEW `OpInfo::effects_fn`/`OpSpec::effects_fn` fn-ptr (the `verify` precedent — ⛔ the core NEVER name-checks a dialect, I6 intact; the **func dialect registers** `call_effects_fn` for `func.call`). `Context::effective_effects(op, table, out)` = an op's INSTANCE-level effects: for `func.call`, its CALLEE's — resolved via the SymbolTable, unioned TRANSITIVELY across the callee body (nested regions + further calls) as a **`u32` family bitmask** (`collect_effective_mask` = one op's own contribution via hook-or-static; `collect_region_effective_mask` = the recursive callee-body walker the hook re-enters), with a **visited-set RECURSION cycle guard** (a self-/mutually-recursive call graph terminates; the union is complete — a skipped cycle member's effects are already collected up-stack). ⛔ Lifted callee effects become **AMBIENT** `{family, None, 0, 0}` (a callee's operand/result target is meaningless at the call site — the honest whole-class lift; per-arg-passthrough precision deferred with reason). ⛔ **DEGRADES to the full `ExternalCall` barrier** if ANY transitively-reached callee is unresolved OR any reached op is UNREGISTERED (the 4a "registered-empty reads as provably-none" landmine, interprocedural — tested); a resolvable nested call's static `ExternalCall` is **SKIPPED** (the hook early-returns, replaced by the callee walk — else every transitive walk would degrade). **TWO real consumers** (no unused surface): **(a)** `ops_hazard`/`collect_block_hazards` gain `table`-taking OVERLOADS (a call to a pure func is reorderable; the no-table paths stay the conservative `ExternalCall`-barrier baseline — the **A/B pair**; a NON-call op keeps its PRECISE per-Value static access on the table path, tested with distinct non-aliasing operands); **(b)** `find_domain_violation` now resolves calls internally against `m.symbols()` — ⭐ **the 4c "ExternalCall stays legal-for-now" gap CLOSED**: `effect_legal_in_region` FLIPS `ExternalCall`→forbidden in audio-RT, so a call to a pure func passes an audio region while a FileIO/unresolved call flags (⛔ the table is now NON-optional — an empty-set fallback would silently no-op the whole §32 verifier; asserted non-null). **(2) The §34 recursion-policy verifier** (in the FUNC namespace — §34 dialect semantics; Context stays generic). `RecursionPolicy{Unspecified,None,Bounded,Unbounded}` (a closed int-enum attr `recursion` + `recursion_max_depth`, the `region_exec` precedent; `set_recursion_policy` ALWAYS overwrites the depth so a re-declaration can't keep a stale bound — the declared-words scar at the setter). `find_recursion_violation(ctx, m, table)` walks the module body PRE-ORDER (deterministic — funcs are linked into the body, not the symbol-table index) building the resolved call graph, returns the FIRST offender: **`DeclaredNoneRecurses`** (a `None` func on any cycle incl. self-, via a DFS reachability over `collect_callees` which DESCENDS nested regions), **`BoundedMissingDepth`** (declared Bounded but no depth≥1), **`InvalidPolicyAttr`** (corrupt attr, the 4c corrupt-tag precedent). ⛔ **Verifies only where DECLARED** (the constitution's "recursion where legal / bounded recursion attributes"): an UNDECLARED func on a cycle is NOT a violation — unacknowledged recursion is the executor's concern at 5d/5z (a documented RELAXATION, not a silent invalidation of every unannotated recursive module). `None` is proven acyclic over the **resolved single-module** graph only (an unresolvable callee = an unverifiable edge); runtime-depth ENFORCEMENT lands with the executor — the attr is presence+positivity today. Minor behavior note: multi-effect domain violations now report the lowest-ORDINAL illegal family (not declaration order) — deterministic either way. §34-text checked (`docs/research/…master-roadmap.md` §34: "recursion metadata"; §13 menu: "recursion where legal / bounded recursion attributes") — no override. Tests: `test_calls.cpp` (8 `[calls]` cases: static-passthrough + resolved-call-not-opaque, transitive A→B→C + nested-region, self-/mutual-recursion TERMINATE, degrade-on-unresolved/unregistered, hazard A/B + non-call-precision-preserved, domain pure-passes/FileIO-flags/unresolved-flags, recursion None-acyclic/None-self/None-mutual-points-at-declared/Bounded-with-depth/Bounded-without/nested-region-recursion/corrupt-attr). **Cross-refs ticked:** the 4a "EffectsFn hook … NOT built now" and the 4c "ExternalCall stays legal-for-now" promises are FULFILLED (struck in place above). **Gate:** crd-ceir-tests **152/152 ctest** (145 + 7 = the calls cases; both OSes via `ctest`) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy (9 files) + GCC `-Werror=switch` (the two new no-default switches `recursion_policy_name`/`recursion_violation_kind_name`) + `crd-ceir-opgen-{drift,validator}` (49 py, drift green NO regen) + invariants. **No binary version bump.** | §14 §34 |
| ✅ | CEIR-5d | ✅ **CLOSED 2026-08-09.** **§20 STATE IS EXPLICIT: the explicit-state ops + the "graph cycles must pass through explicit state/delay semantics" rule.** §20-text checked (`…master-roadmap.md` §20: `state<T>`/`history<T>`/`delay<T>`/`persistent_resource<T>` + "Graph cycles must pass through explicit state/delay semantics"). **The model (advisor, Model A):** a §20 state op carries a NEW **`OpTrait::StateEdge`** (`1<<5`) whose CONVENTION is that its **LAST operand is the feedback (`next`)** — the ONE operand exempt from CEIR-5b def-before-use, so a feedback edge can forward-reference the value the cycle produces. **Three `ceir.core` ops** (via the 2a generator — NOT schema-quiet): `state`/`delay`/`history`, all shaped **`(init, next) → current`**, StateEdge, BitExact, ⛔ **NOT Pure** (each instance is a DISTINCT cell — a Pure trait would let CSE merge two cells, the CSE trap, written in the TOML) with ZERO effects; `history` gets an optional int `depth` attr (RESERVED vocabulary; absent=1). **Executor semantics pinned in the TOML `docs`** (advisor: the cell is PER STATIC OP INSTANCE and **PERSISTS across evaluations** — it does NOT reset on region re-entry, which TAA/GI/audio-delay require and which makes the `fold_constant_if` state-splice sound; `current` = `init` on the FIRST-EVER evaluation, thereafter the previous `next`) so CEIR-5z implements written words. **The verifier folds into CEIR-5b's `find_structure_error`** (advisor — NO separate SCC pass): per block an **order-free `all_defs` set** (block args + every op result); when the running def-before-use check fails, discriminate — a value in same-block `all_defs` is a BACK-EDGE, legal ONLY as a StateEdge op's last operand (⇒ **`FeedbackWithoutState`** otherwise), else the existing `CaptureThroughIsolation`/`UseBeforeDef`. ⭐ **The soundness one-liner:** every def-use cycle has ≥1 same-block back-edge; a back-edge is legal ONLY as a state feedback ⇒ every surviving cycle passes through a StateEdge op (§20), enforced by the existing walk — no cycle detector needed. `depth` is validated trait-generically (any StateEdge op carrying `depth` → Int≥1 else **`StateDepthInvalid`**) — the declared-words scar at the verifier, NO I6 name-check. **`StructureErrorKind` +2** (`FeedbackWithoutState`, `StateDepthInvalid`; the no-default `-Werror=switch` forces both cases). **Open-world:** the exemption keys on the TRAIT — a hand-registered non-core StateEdge op gets it (tested). **FOUR conscious decisions a resumer must not mis-infer:** (a) the state op must **HEAD its cycle in list order** (its feedback operand is the back-edge) — an equivalently-cyclic program listed otherwise must be re-ordered (a canonicalization concern, not a semantics loss); (b) **cross-block/cross-region feedback stays `UseBeforeDef`** (same-block membership required — conservative); (c) a normally-visible feedback operand (a degenerate pass-through register) is legal (no back-edge at all); (d) the exemption applies **uniformly across region kinds** (simpler than Graph-only). **Composition:** orthogonal to 5c (recursion is the §34 call-graph over `resolve_call`; feedback is the def-use graph — neither walk traverses the other's edges); `fold_constant_if` splicing a state op is sound (same op = same cell, same eval count — no new bail). **⚠ RE-CLASSIFICATION (advisor Catch #1):** the 5b same-block forward-ref + self-ref tests now report `FeedbackWithoutState` (strictly more informative) — updated in place with a comment + a note struck into the 5b row above. **⚠ Struck the 5a "iter_args → 5d" promise:** §20's loop-carried accumulator is a `core.state` INSIDE the loop body (what 5z executes), NOT MLIR iter_args (an -ism absent from §13/§20). **`persistent_resource<T>` routed** to ceir.resource/§36 (a resource-lifetime concern), NAMED not silently subset (NO-FOLLOW); the §66/§67 `ceir.state` dialect is state MACHINES, a different surface. **⭐ ROUND-TRIP proven (advisor pre-close):** a legal feedback cycle FORWARD-references the value it feeds back; the 1e parser AND the 1f binary decoder BOTH resolve that forward reference (the advisor's parser-rejection hypothesis was DISPROVEN — 5z's text-parsed gate inherits an OPEN path, tested). ⛔ Subtle contract surfaced + documented: the `StateEdge` trait is REGISTRY state (registration), NOT serialized module content (open-world) — so a consumer VERIFYING a round-tripped module must register its dialects (an unregistered state op has no trait ⇒ its feedback conservatively reads as `FeedbackWithoutState`); 5z's executor registers dialects, so no surprise. Tests: `test_state.cpp` (9 `[state]` cases: legal cycle through state/delay/history, self-feedback, pass-through-register, combinational-cycle→FeedbackWithoutState, init-forward-ref→FeedbackWithoutState (pins last-operand), cross-block-feedback→UseBeforeDef, depth absent/=3/=0/neg/non-int, open-world trait, **text+binary round-trip**) + the 2 re-classified 5b cases + `test_opgen.py` +1 (StateEdge accepted) + the regenerated `core` smoke. **Gate:** crd-ceir-tests **161/161 ctest** (152 + 9; both OSes via `ctest`) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy (5 files) + GCC `-Werror=switch` (the 8-case `StructureErrorKind` switch) + `crd-ceir-opgen-{drift,validator}` (50 py, drift green AFTER the committed regen) + invariants. **No binary version bump.** | §20 |
| ✅ | CEIR-5z | ✅ **CLOSED 2026-08-09. BAND-5 GATE — the FIRST reference executor (§118).** `engine/ceir/exec.{hpp,cpp}` (`crd::ceir::exec`): a SLOW, CORRECT semantic interpreter for the core subset — arith (const/addi/muli/cmpi) + ceir.core control flow (scope/if/for/while/switch/match/yield + §20 state/delay/history) + ceir.func calls. ⛔ **Dispatch is an Interpreter-OWNED `OpId→EvalFn` table, NOT an OpInfo hook (advisor):** `register_op` is first-wins idempotent, so a hook attached AFTER the generated registration would silently no-op (the registered-default-empty scar); semantics are INSTALLED per dialect (`install_{arith,core,func}_semantics` — open-world, a caller may add/override), and an op with NO installed EvalFn is a typed error, never skipped (EMPTY≠UNKNOWN at the executor). Values are **wrapping `i64`** (⛔ via `u64` casts — signed overflow is UB; real per-width typing joins CEIR-6). **§20 state cells = a depth-N RING keyed by the STATIC op instance, persisting across evaluations INCL. across function calls** — ⭐ `state` ≡ `delay` ≡ `history(depth=1)` under ONE register mechanism (paying the two 5d TOML IOUs — "depth taps land at 5z" + "the distinction is the executor's update timing"): `current` = the value from `depth` evals ago (init-filled until warm); READ at eval, LATCH `cell ← env[next]` at BLOCK-EVAL END (read-all-then-latch → cross-feeding cells latch simultaneously, hardware-register timing). **Frames:** a fresh env per `func.call` (recursion-safe); nested regions (if/for/switch bodies) SHARE the function frame (non-isolated capture). **Output = the entry function's `func.return` values** (the call-based `@acc` accumulator dissolves the loop-doesn't-yield problem — a state cell in `@acc` persists across the loop's calls; `@acc(0)` after the loop reads the total unchanged); byte-pin = 8-byte LE per i64 (`pin_values`). **Fuel** (`max_steps`, default 2^24) → `FuelExhausted` typed error at the loop op (⛔ timeout≠hang-proof). Typed `ExecResult{values, error, op}` over 11 §118 failure modes (a no-default `-Werror=switch`): NoSemantics/UnresolvedCall/UndefinedValue/BadForStep/SelectorOutOfRange/UnknownPredicate/CondArity/FuelExhausted/NoEntry/BadArity. ⛔ **`foreach` is a NAMED NoSemantics deferral** (no collection value domain until types land, CEIR-6); `match` ≡ `switch` (selector=index) reference behavior, documented. `cell_value(op)` inspection (§118 deterministic debugging) — BUILDER-form only (cells are pointer-keyed; pointers don't survive a round-trip). An Interpreter is ONE SESSION (fuel + cells carry across `invoke`s — a stateful func keeps its cells between top-level calls; construct fresh for an independent run). **⛔ The CALLER registers the module's dialects** (the 5d trait-is-registry-state finding — the executor reads StateEdge/Terminator traits + resolve_call). **THE GATE (`test_band5_gate.cpp`, `[gate5]`):** a pinned program — `func.func @acc(%delta)` (a state<T> running total across CALLS) + `@main(%n)` (a bounded `core.for` whose body does `cmpi` → **`core.match`** selecting `acc(iv)` vs `acc(2*iv)`, plus a second in-loop feedback cell; after the loop `acc(0)` then a VALUE-PRODUCING **`core.if`** bonus) — is **BUILT TWICE (builder + print→parse)**, executes to `@main(6)=118` in BOTH, **byte-IDENTICAL** (`pin_values`), AND survives a binary round-trip; the in-loop cell reads 15. One assertion proving the executor + the round-trip + state/control-flow + calls — the whole band. **Obligation B (advisor):** nested-region-relax RE-ROUTED to CEIR-15 (the semantic executor never consults `region_exec` — struck in the band header). **NOT schema-quiet from 5d's regen, but SCHEMA-QUIET here** (exec is hand C++; the lib globs `src/` so exec.cpp needs no CMakeLists edit). Tests: `test_exec.cpp` (9 `[exec]` cases: arith incl the wrap, if branch+yield-forward, a for+state sum, the ring across depths, cross-call state persistence, switch+out-of-range, the 7 error modes, open-world install) + `test_band5_gate.cpp` (the gate). **Gate:** crd-ceir-tests **171/171 ctest** (160 + 9 exec + 1 gate + 1 fuzz-adjacent; both OSes via `ctest`) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy (4 files) + GCC `-Werror=switch` (the 11-case ExecError + 8-case StructureErrorKind switches) + `crd-ceir-opgen-{drift,validator}` (50 py) + invariants. **No binary version bump.** **→ BAND 5 CLOSED.** | §118 |

## CEIR-6 — Async/task/runtime domains — ✅ **CLOSED 2026-08-09** (6a ✅ 6b ✅ 6c ✅ 6z ✅) → §30 §31 §37 §38

**Band contract.** `ceir.async` (tokens/async values/join/race/cancel, §37) + `ceir.task` (§38) with the **host job
provider lowering onto crd-jobs** — `task.parallel_for`→`crd::jobs::parallel_for` (6b) + `task.map_reduce` fixed-order reduction (6z); ~~`task.spawn`/waits→jobs
counters/fiber waits, main-thread pinning, deadline/priority attrs~~ **[6z: band 6 ships the async+parallel+map-reduce CORE; priority landed 6c (`priority_for` in the bridge), deadline → CEIR-24/29 (no crd-jobs primitive), spawn/continuation/main-thread/worker/fiber-wait/task-group + jobs-backed launch/await → CEIR-11a (§84 async-host subset) where a compiled-plan consumer needs them]**. ⛔ No second scheduler (§31): CEIR is an
authorable front-end to the existing fibers. This is where the **first execution provider** (host/jobs) is born in
minimal form (§69). **Scars:** worker_index aliasing · frame-arena exhaustion under parallel_for · workers reset on
shutdown. **Gate: a job-system parallel program is authored as CEIR.**

| ✔ | Slice | Deliverable | → §§ |
|---|---|---|---|
| ✅ | CEIR-6a | ✅ **CLOSED 2026-08-09. BAND 6 OPENS.** **`ceir.async` ops + the §116 use-once token-misuse verifier.** A NEW `ceir.async` dialect (via the 2a generator): **`launch { region } -> %token`** (structured fork — §30 structured concurrency, NOT detached spawn), **`await(%token) -> (%v…)`** (consume, get the body's yields), **`join(%t…) -> %token`** (all-of), **`race(%t…) -> %index`** (any-of; index-only — value-carrying race needs async-value `!async<T>` types → routed within §37 to CEIR-6b/6c), **`cancel(%token)`**. ⛔ **Tokens are TRAIT-typed, not TYPE-typed (advisor):** two NEW OpTrait bits — **`TokenProducer`** (`1<<6`, convention: EVERY result is a token) + **`TokenConsumer`** (`1<<7`, convention: EVERY operand is a consumed token — revisitable per the StateEdge-last-operand precedent when a mixed-operand op arrives); **`join` carries BOTH**, `race` is consumer-only (its index result is NOT a token). A producer trait can't be forgotten, whereas a `!token` TYPE would be invisible to a mistyped op (the 3z construction hole) + would drag print/parse/canonical/binary/fuzz — the `!async<T>` async-VALUE type is NAMED-deferred to the slice that needs typed results (NO-FOLLOW). **The verifier `Context::find_token_misuse(Module&) -> {value, op, kind}`** — a USE-ONCE (linear) def-use walk (§116 is the taxonomy; the MECHANISM is linear use-counting — 4d's read/write hazards cannot express "exactly once"): every token consumed **EXACTLY once** by a `TokenConsumer` operand SLOT. Kinds (no-default `-Werror=switch`): **`Unconsumed`** (0 — a leaked async op, no await XOR cancel), **`MultiplyConsumed`** (>1 SLOTS — double-await / `join(t,t)` counts as 2), **`ConsumedByNonConsumer`** (a token operand of a non-consumer op). Pre-order first-offender (use-misuses caught at the use; Unconsumed post-walk at the producer); trait-keyed = open-world (the core never name-checks async). **FOUR conscious edges, each tested:** a token consumed in BOTH `if`-branches = 2 static uses = flagged (no path-sensitivity substrate — named); a token yielded out of a region / passed to `func.call` / fed to arith = `ConsumedByNonConsumer` ⇒ **tokens are region- AND function-CONFINED, so §30's structured discipline falls out FREE**; produce-outside / consume-inside a LOOP = 1 static use = passes (dynamic multiplicity needs execution — a documented gap). ⛔ **Register-to-verify (opposite direction from 5d):** an UNregistered async module has no `TokenProducer` traits ⇒ zero tokens tracked ⇒ a leak passes VACUOUSLY (a false NEGATIVE — the perf-flag-measures-empty-frame shape); can't fix structurally (traits are registry state; name-sniffing breaks I6) — the consumer MUST register the dialect (late binding works — OpId is a content hash; tested print→parse-without-async→None→register→Unconsumed). **Effects/determinism (Catch #2, advisor):** all five ops declare an ambient **`Synchronization`** effect (NOT structured-zero-effects like ceir.core — a launch's region runs at an UNSPECIFIED later time, so the 4d block walk seeing its contents at the launch point would be a lie; Synchronization = a Universe barrier, 4d reorders nothing across it); `race` is `Nondeterministic`, others Unspecified (⚠ **finding:** Unspecified ALSO fails Deterministic compiler mode — so the mode-composition test asserts `find_mode_violation == l0` (first async op) + a direct `op_determinism(race)==Nondeterministic`, not a forced race-first). ⭐ **The §32 Synchronization FLIP (Catch #1):** `effect_legal_in_region` now forbids `Synchronization` in an audio-real-time region (a blocking `await` in an audio callback IS the priority-inversion §32 forbids; band 6 introduces the first blocking waits, so the flip lands WITH them — NOT a new "legal-for-now"; blast radius checked — no existing test broke; both sides tested + the 5c×6a path: a CALL to an async helper flags Synchronization in audio via `effective_effects`). Executor: async ops are a NAMED `NoSemantics` deferral (~~reference semantics land with the crd-jobs provider at **CEIR-6b**~~ **[✅ SPLIT at 6b (struck in place): the SEQUENTIAL-reference half landed at CEIR-6b — `exec::install_async_semantics` (launch runs the body at launch, token value = a session yield-store handle, await returns them, race → index 0, cancel = consume/no-op); JOBS-BACKED parallel async → ~~CEIR-6c~~ **CEIR-6z (re-struck at 6c: 6c delivered scopes+cancellation, NOT jobs-backed async — a real fiber-pool async run belongs in the BAND GATE, whose authored map-reduce wraps in `async.scope`, paying the scope×provider composition seam)** **[re-struck AGAIN at 6z: the SEAM was PAID at 6z — `async.scope`'s body drives crd-jobs via `task.map_reduce`, its FIRST run through HostProvider; jobs-backed launch/await-ON-POOL itself → CEIR-11a (§84 async-host subset)]**]**; the TOML `docs` pin the executor contract — launch runs at an unspecified time / sequential-reference at launch, race → index 0 — so 6b/6c implement written words). **DEFERRALS pinned to real rows:** ceir.task + crd-jobs provider + async EXECUTOR semantics → **6b**; structured-concurrency `group`/scopes + cancellation plumbing → **6c** (so 6a builds NO group op); `!async<T>` async-value types → within §37 to the slice needing typed results. Tests: `test_async.cpp` (13 `[async]` cases: well-formed launch→await/cancel/join/race, Unconsumed, double-await + `join(t,t)`, the three non-consumer confinements, both-branches, open-world hand producer/consumer, the Synchronization barrier, the audio flip both sides + the 5c call path, the loop-edge gap, register-to-verify, race-nondeterminism/mode, NoSemantics) + `test_opgen.py` +1 (both token traits) + the generated async smoke. **Gate:** crd-ceir-tests **187/187 ctest** (171 + the 13 `[async]` cases + 3 generated async-smoke; both OSes via `ctest`) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy (5 files) + GCC `-Werror=switch` (the 4-case `TokenMisuseKind` switch) + `crd-ceir-opgen-{drift,validator}` (51 py, drift green post-regen) + invariants. **No binary version bump.** | §37 §30 |
| ✅ | CEIR-6b | ✅ **CLOSED 2026-08-09** (landed across a checkpoint + a continuation; ONE commit supersedes the checkpoint proposal). **The FIRST execution provider — CEIR leaves crd-ceir onto the fiber pool.** ⭐ **The `crd-ceir-host` BRIDGE module** (new `engine/ceir-host/` → crd-ceir + crd-jobs; the FIRST component that leaves the host-only core; ⛔ crd-ceir gains NO jobs edge — the `crd-ceir-invariants` I3/I5 gate PROVES it green: `crd/jobs/` a forbidden include + not in the CMake `allowed` set; `provider.hpp` names no jobs type — I4). **The abstract `IExecutionProvider` seam** in crd-ceir (`provider.hpp`: `name`/`advertises(Context&,OpId)`/`execute(Context&,Module&,entry,args)→exec::ExecResult`; ⛔ append pure-virtuals at END, D135/ADR-0109 §4.2; the full §69 advertise+compile→plan + partitioner → **CEIR-24/29**, named). **`HostProvider`** (in the bridge) EXECUTES `task.parallel_for` by lowering to `crd::jobs::parallel_for` — ⭐ **per-ITEM** (not per-range — a deliberate STRENGTHENING: strictly stronger isolation + independent fuel, cost = count sem-table copies) FRESH interpreters from the prototype, each over its OWN `MallocAllocator` (race-free: reads of the const Context are the only shared access — win-asan + linux-gcc-asan clean with 4 threads, the milestone this slice existed to reach); the per-index yield written to a submit-thread-pre-sized output Array at DISJOINT slots (no push_back/locks/atomics); `map_output(pf_op)→ConstSpan` inspection (instance-keyed — there is no array VALUE domain, the foreach-deferral reason). **A parallel-purity PRE-FLIGHT** (submit thread): the body + its RESOLVED CALLEES must be StateEdge-free (a cell would make results depend on the range split, killing the 6z {1..16} bit-identity — and state ops declare zero effects, so the effect machinery can't see them) → `ExecError::ParallelBodyStateful`; unresolved call → `UnresolvedCall`; body yield≠1 → `ParallelYieldArity`. ⚠ **MODULE-WIDE pre-flight (deliberately conservative):** it rejects the whole module if ANY parallel_for is impure, even one the entry never calls (over-rejection, safe; entry-reachable scoping is a later refinement). **Sequential-reference ASYNC** (the 6a IOU's SEQUENTIAL half, paid in crd-ceir): `exec::install_async_semantics` — launch runs the body at launch, the token's runtime value = a **SESSION-scoped handle** into an Interpreter yield-store (⛔ a forged/cross-session token → `ExecError::BadToken`, NOT a silent empty — the vacuous-pass shape closed at the executor), await returns the stashed yields, join concatenates, race → index 0 deterministically, cancel = consume/no-op ("real cancellation is 6c"); JOBS-BACKED parallel async → **6c** (struck in the 6a row). NEW crd-ceir surface (each with a real consumer): `Interpreter` prototype ctor + `allocator()`/scratch (the per-item clones); `invoke_region` (run a body region with block-args — the per-index runner; ⛔ the body must be SELF-CONTAINED — outer captures are `UndefinedValue`, tested + pinned in the TOML docs); `set_user`/`user()` (the provider threads its parallel context); `store_yields`/`valid_yield_handle`/`stored_yields` (the async installer). §38's other seven ops (spawn/continuation/main-thread/worker/fiber-wait/deadline-priority/task-group) ~~NAMED-deferred to **6c**~~ **[re-struck at 6c: 6c built NONE of them — of the seven, PRIORITY landed (`priority_for` map in the bridge) and DEADLINE is declined-with-reason → **CEIR-24/29** (§69 provider planning, where a deadline-aware scheduler is decided); the remaining SIX (spawn/continuation/main-thread/worker/fiber-wait/task-group) EXCEED band-6's remaining capacity — 6z is a GATE not a feature slice, and 21/26 are ML/graph-provider rows — so they are SURFACED for the user's 6z prompt to place (candidate: a 6d extension slice)]** **[RESOLVED at 6z → CEIR-11a (§84 async-host subset), where a compiled-plan consumer pulls the full §38 task vocabulary — NOT a 6d; 6z closes the band]**. ⛔ TESTS own the `crd::jobs` pool (a Catch listener init/shutdown; the provider NEVER does — the workers-reset scar). Tests: `test_host_provider.cpp` (8 `[host]` cases: parallel_for maps iv*iv on the pool, **`num_jobs`∈{1,8} IDENTICAL** (the 6z determinism seed) + num_jobs>count clamp, calls-in-body, empty-range/step≤0, no-provider→NoSemantics, the 4 pre-flight cases, FuelExhausted-points-at-pf, captured-body→UndefinedValue, advertises, the audio-flip composition) + `test_exec.cpp` clone-independence + `test_async.cpp` sequential-exec + BadToken + the renamed separate-installer test. **Gate:** crd-ceir-tests **201/201 ctest** (194 crd-ceir + 7 `[host]`; both OSes via `ctest -R ceir` — the "ceir host:" naming) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy + GCC `-Werror=switch` (the `ExecError` enum grew BadToken/ParallelBodyStateful/ParallelYieldArity) + `crd-ceir-opgen-{drift,validator}` (51 py) + **`crd-ceir-invariants` (crd-ceir STILL jobs-free)** + invariants. **No binary version bump.** | §38 §31 §69 |
| ✅ | CEIR-6c | ✅ **CLOSED 2026-08-09. STRUCTURED-CONCURRENCY SCOPES + COOPERATIVE CANCELLATION.** A NEW `async.scope { region } -> (%results…)` op (via the 2a generator): a structured-concurrency boundary (`Synchronization` effect, graph region, variadic results — the join-at-exit surface). ⭐ **Scope-confinement is LAYERED verification, NOT new verifier code (advisor):** a token launched inside a scope and consumed outside is already `UseBeforeDef` (5b def-before-use) and a never-consumed scope token is already `Unconsumed` (6a linear token-misuse) — so §30's structured discipline for scopes falls out of the 5b+6a substrate FREE (mirrors how 6a's region/function confinement fell out of `ConsumedByNonConsumer`); PROVEN by tests, not asserted (scope-launched token used after the scope → `UseBeforeDef`; scope-launched never-awaited → `Unconsumed`; a `Synchronization` scope in an audio-real-time region → `find_domain_violation` flags it via the 6a §32 flip). **Cooperative CANCELLATION** (§30): a new `ExecError::Cancelled` + a `const std::atomic<bool>*` cancel flag on the Interpreter (`set_cancel_flag`/`cancelled()`, NOT proto-copied — each clone gets its own), checked at the top of `eval_block`/`eval_for`/`eval_while` (op- and loop-iteration granularity) BEFORE fuel spend — ⛔ **crd-jobs has NO cancel primitive, so cancellation is COOPERATIVE by construction** (a running op finishes; the loop observes the flag next iteration); `HostProvider::request_cancel()` sets a provider-owned flag threaded into the prototype AND every per-item sub-interpreter (`RangeJob.cancel`), so a mid-flight parallel map stops at op granularity (sticky/single-use — a fresh provider per run). Test uses a PRE-SET flag (a mid-flight cancel is inherently racy — the deterministic assertion is Cancelled-beats-Fuel with a huge `sub_fuel` = `1<<40`). ⭐ **`priority_for(RealtimeClass) -> crd::jobs::Priority` map lives IN THE BRIDGE (I4):** a region's §32 RealtimeClass tag drives the `parallel_for` dispatch Priority (AudioRealTime/FrameCritical→High · Background/Offline→Low · rest→Normal) — a TOTAL no-default switch (a new §32 class = a `-Werror=switch` compile error). ⛔ It maps a crd-ceir vocabulary to a **jobs TYPE**, so it CANNOT live in crd-ceir without breaking I4 (crd-ceir names no jobs type — `crd-ceir-invariants` stays green). ⚠ **The map arm is NOT dead code:** the provider runs NO verifier before `execute`, so the `AudioRealTime→High` arm is reachable on an UNVERIFIED module even though `find_domain_violation` would flag a Synchronization-in-audio region — the provider does not gate on it (verification is the caller's contract). **DEFERRALS — pinned to real rows (advisor ledger, no vague "forward"):** the row title's **DEADLINE** attr is DECLINED-WITH-REASON — crd-jobs has NO deadline primitive, so an attr no scheduler reads is the cook-only-gate anti-pattern → routed to **CEIR-24/29** (§69 provider planning, where a deadline-aware scheduler is decided); **jobs-backed parallel ASYNC** (`launch`/`await` on the pool, vs. today's sequential-reference) → **CEIR-6z** (a real fiber-pool async run belongs in the band GATE, whose map-reduce wraps in `async.scope` — the scope×provider composition seam, never yet run through HostProvider: scope is tested in-core only); §38's remaining **six task ops** (spawn/continuation/main-thread/worker/fiber-wait/task-group) EXCEED band-6's remaining capacity (6z is a GATE, 21/26 are ML/graph rows) → SURFACED for the user's 6z prompt ~~(candidate: a 6d extension)~~ **[RESOLVED at 6z: the scope×provider SEAM was PAID at 6z (`async.scope`'s body drove crd-jobs via `task.map_reduce` — its first run through HostProvider); jobs-backed launch/await-on-pool + all six task ops → CEIR-11a (§84 async-host subset), NOT a 6d — 6z closes the band]**. ⛔ **NO new op verifier + NO binary version bump** (scope-confinement layered on 5b+6a; the ops/effects grew but the printed/parsed IR did not). Tests: `test_async.cpp` +4 `[async]` (scope sequential-exec → 7; the confinement PAIR — scope-token-outside → UseBeforeDef + never-consumed → Unconsumed; the scope-Synchronization-in-audio flag; Cancelled-vs-Fuel via a pre-set `std::atomic<bool>`) → **`[async]` 22 cases / 91 assertions**; `test_host_provider.cpp` +2 `[host]` (`priority_for` map direct — AudioRealTime/FrameCritical→High, Background/Offline→Low, Unspecified/Throughput→Normal; `request_cancel`→Cancelled with a huge sub_fuel) → **`[host]` 10 cases / 50 assertions**. **Gate:** crd-ceir-tests **207/207 ctest** (201 + 4 `[async]` + 2 `[host]`; both OSes via `ctest -R ceir`) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** (the ASan-clean threaded run on linux-gcc-asan is the money config) + LLVM-20 tidy (6 files) + GCC `-Werror=switch` (the `priority_for` RealtimeClass switch + `ExecError` grew `Cancelled`) + `crd-ceir-opgen-{drift,validator}` (drift green post-regen) + **`crd-ceir-invariants` (crd-ceir STILL jobs-free — the priority map is in the BRIDGE)** + invariants. **No binary version bump.** | §30 §32 |
| ✅ | CEIR-6z | ✅ **CLOSED 2026-08-09. BAND-6 GATE MET — a parallel map-reduce AUTHORED as CEIR runs on the fiber pool, bit-identical across `{1..16}`.** The in-IR reduction the `task.parallel_for` docs pinned to 6z: a NEW **`task.map_reduce(%lo, %hi, %step, %init)` `map { ^m(%iv): … } combine { ^c(%acc, %elem): … } -> %result`** op (via the 2a generator) — operand order **(lower, upper, step, init)** so the shared `run_parallel_map` helper reads operands 0..2 identically for both task ops (advisor). ⭐ **The provider (`HostProvider`, crd-ceir-host) runs the MAP in PARALLEL** (`crd::jobs::parallel_for`, disjoint index-order slots — the exact 6b machinery, refactored into `run_parallel_map` shared with `parallel_for`), **then FOLDS the results SEQUENTIALLY in INDEX ORDER** (`acc = init; acc = combine(acc, out[i])` for i=0..count) — so the result is num_jobs-INDEPENDENT and **bit-identical across ALL {1..16}** even for a NON-associative combine: the map output is index-order-disjoint (split-independent) and the fold order is FIXED. ⛔ **The fold runs on ONE fresh sub-interpreter cloned from the prototype — NOT a re-entrant `invoke_region` on the mid-invoke `in`** (its frame is live — advisor landmine); a plain i64 accumulator, `set_cancel_flag` threaded so `request_cancel` also stops a long reduce; a fold-step error is attributed to the map_reduce op (index-order first). `map_reduce` has an **SSA result** (unlike statement `parallel_for`) so it COMPOSES inside `async.scope` — the gate wraps the map-reduce in `async.scope { %r = map_reduce…; yield %r }`, ⭐ **`async.scope`'s FIRST run through HostProvider** (the scope×provider composition seam 6c could only test in-core). **Pre-flight** (submit thread) unified into `check_parallel_region`, extended to BOTH regions: the map yields 1 + 1 block-arg (%iv); the combine yields 1 + **2 block-args (%acc, %elem) — arg count checked so `invoke_region`'s bind never trips `BadArity` mid-fold** (this also hardens the latent parallel_for map-arity gap); both StateEdge-free transitively (a cell would make the fold depend on the split). ⭐ **THE GATE TEST (`[host]`):** `@main() { %s = async.scope { %r = map_reduce(0, 37, 1, init=0) map{iv: iv*iv} combine{acc,elem: acc*31 + elem}; yield %r }; return %s }` — run through HostProvider for **EVERY num_jobs ∈ {1..16}** (N=**37, a PRIME ≥16** so every split is ragged — no even divide), each result **byte-pinned (`pin_values`) and asserted byte-identical to num_jobs=1 AND to an INDEPENDENT wrapping-u64 reference fold** (the combine is NON-associative `acc*31+elem` — bit-identity alone would pass 16 identical WRONGS, the bit-exact-blind scar; the reference wraps in u64 to mirror the executor's i64, else the test itself is signed-overflow UB). + a `map_reduce` pre-flight TEST_CASE (stateful combine → `ParallelBodyStateful`; wrong-arity combine → `BadArity`; combine yields≠1 → `ParallelYieldArity`) + the **empty-range identity** (`map_reduce(0,0,1,init=42) → 42`, run **BARE outside any scope** — the happy path off the scope seam; advisor) + `advertises(task.map_reduce)`. NO new `ExecError` (arg-count reuses `BadArity` — the same error `invoke_region` raises), NO exec.hpp/cpp change (the reduction lives entirely in the bridge). **DEFERRALS ROUTED (advisor ledger):** jobs-backed launch/await-ON-POOL + §38's six residual task ops (spawn/continuation/main-thread/worker/fiber-wait/task-group) → **CEIR-11a** (§84 async-host subset — inbound noted at 8a); the scope×provider seam is PAID here. Tests: `test_host_provider.cpp` +2 TEST_CASEs (the gate + map_reduce pre-flight) + a map_reduce empty-range SECTION → **`[host]` 12 cases**. **Gate:** crd-ceir-tests **209/209 ctest** (207 + 2 `[host]`; both OSes via `ctest -R ceir`) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** (⭐ **the {1..16} bit-identity proven ASan-clean under 4 threads on linux-gcc-asan — the money config this gate existed to reach**) + LLVM-20 tidy (2 files) + GCC `-Werror=switch` (no new switch — arg-count reuses BadArity) + `crd-ceir-opgen-{drift,validator}` (drift green post-regen: the new op) + **`crd-ceir-invariants` (crd-ceir STILL jobs-free — the whole reduction is in the BRIDGE)** + invariants. **No binary version bump.** ⭐ **BAND 6 CLOSED.** | §30 §31 |

## CEIR-7 — Asset/cook/runtime lifecycle — ◧ **PAUSED at 7b (2026-08-09 re-baseline)** (7a ✅ 7b ✅; the remainder re-homed → CEIR-10) → §105–§110

**Band contract.** CEIR programs become ordinary Cerid assets. **Reuse hard:** CRDR container (new `'CEIR'` chunk),
the ADR-0104 content-hash cook cache + variant dedup, the RAF-11 hot-reload machinery (⛔ its reentrant-
`init_programs` guard+reserve+drain scar applies verbatim), D5 generation-retire. New: the **interface hash** (§107,
params/results/caller-visible effects/capabilities/state schema) split from the content hash so implementation-only
edits hot-swap without invalidating callers; asset classes (§105); `engine://`+`app://`+`runtime://` namespaces
(RAF-9's `engine://` registry). **Gate: a live CEIR program hot-swaps safely.**

| ✔ | Slice | Deliverable | → §§ |
|---|---|---|---|
| ✅ | CEIR-7a | ✅ **CLOSED 2026-08-09. BAND 7 OPENS.** **The `'CEIR'` cook: source → VERIFIED → binary chunk, carrying the CONTENT hash + the §107 INTERFACE hash + the §106 dependency record.** ⭐ **crd-ceir CORE (no new edge — I5 green as-is):** `interface_hash(§107)` — FNV-1a over a CANONICAL projection: each EXPORTED (Public) func **SORTED BY NAME** (⛔ never body order — a function reorder is an impl edit), its {param types · result types (encoded STRUCTURALLY — recursing `Type::members`, never a Context-local TypeId int, so cross-Context stable like the 1f blob) · the 5c **TRANSITIVE effective effect set** (a body edit that adds a caller-visible effect IS an interface change) · the §20 **MODULE-WIDE state schema** (every StateEdge cell in module pre-order — a PRIVATE callee's cells are live state a 7c hot-swap must migrate; the safety asymmetry: under-inclusion risks a wrong "compatible" verdict, over-inclusion only a spurious recook)}. ⛔ Impl-only bodies / private funcs / constants EXCLUDED ⇒ an implementation-only edit leaves it UNCHANGED (the §107 hot-swap property). Mirrors the `stable_hash` (content hash) precedent. `collect_dependencies(§106)` — **external** calls (an unresolved callee is an IMPORT, not an error) + intrinsics + providers, **schema/registry-driven** (op_info.intrinsic/native_provider), ⛔ NEVER dialect-name-sniffing (I6). `find_unregistered_op` — the STRICT cook check (EMPTY≠UNKNOWN: an unregistered kind's verifiers pass VACUOUSLY). ⭐ **The OpInfo/OpSpec PROMOTION (a crd-ceir core change):** `intrinsic` + `native_provider` promoted from the generated OpSchema to the RUNTIME `OpInfo` (the determinism/domain precedent — the dep collector reads them without a schema index); the 2a generator emits them ONLY for `[op.native]` ops (drift-safe: every prior file byte-identical — only the `test` dialect churned) + a validator test pins the emission; ⛔ `native_determinism` NOT promoted (no runtime consumer — the deadline-attr anti-pattern) → CEIR-16c. ⭐ **The `crd-ceir-cook` BRIDGE** (new `engine/ceir-cook/` → crd-ceir + crd-resources + crd-render-asset-core; ADR-0109 §4.2 — crd-ceir gains NO asset edge): `cook_program` VERIFIES (unregistered → structure → domain → token → borrow → §34 recursion; ⛔ mode-violation SKIPPED — session state, not module validity) THEN serialize + hash + deps + package into a **CRDR container (type `'CEIR'`): a `'META'` render-asset `CookedHeader` (the §107 iface + content hash + type/schema a loader validates) · a `'CEIR'` program blob · a `'CDEP'` §106 dep chunk**. `cook_program_text` (§105 source — parse then cook; ⛔ text ≡ builder hashes, the no-privileged-path property §121). `read_program` round-trips + validates; ⛔ a MISSING/unparseable `'CDEP'` is `BadDeps`, never a silent "no deps" (the default-empty scar). **REUSES** the RAF cooked-envelope framework (`CookedHeader`/`interface_hash_of`/`content_hash_of`/`RuntimeSlot` — CEIR JOINS the family, like shader RAF-4) + `AssetType::Program` (**CLAIMED** for the §105 CookedProgramAsset — no cooker previously produced it). ⭐ **Cross-Context PURITY (the headline test):** cook in Context A → read into a FRESH Context B → recompute BOTH hashes on B's deserialized module → equal the header fields (no intern-state leak — the 3a dirty-Context precedent + the declared-header-words-validated scar). **Invariants:** I5 GREEN AS-IS (crd-ceir gained no link edge — the cook is in the bridge); I3 **HARDENED** (+`crd/resources` — crd-ceir may never include the CRDR layer either). **DEFERRALS — pinned to real rows:** `ckir_refs` (KernelRef asset deps) → **CEIR-13** (no CKIR op yet — the schema slot exists); `native_determinism` → **CEIR-16c**; RuntimeSlot/handle model → **CEIR-7b** (its row); the persistent CookDb / `register_cook_handler` file integration → **CEIR-7c** *(re-homed → CEIR-10a at the re-baseline)*; §105 asset classes beyond CookedProgramAsset + the `engine://`/`app://`/`runtime://` namespaces remain **band-7 scope (7b/7c)**; ~~`ResourceId{0, asset_id}` is PROVISIONAL — **7b** decides the real ResourceId↔AssetId mapping.~~ **[RESOLVED at 7b: the container id = `ResourceId::from_content(program blob)` (the shader-cook precedent, ADR-0104 content-addressed); the AssetId stays the LOGICAL identity (the CookedHeader.id + the RuntimeHandle key); the two schemes are PERMANENTLY DISTINCT layers — physical container id vs logical program id, no mapping.]** ~~⚠ **The §107 CAPABILITY CONTRACT field has NO owning tracker row** — FLAGGED for the user (a decision, not a route); the interface hash folds it in when a home exists (a recook, honest).~~ **[RESOLVED at the 2026-08-09 re-baseline: the user placed it — CEIR-8f owns capability contracts; the interface-hash fold lands there (a recook).]** Tests: `test_program_asset.cpp` (7 `[program-asset]` — the four-edit §107 matrix + cross-Context + deps + unregistered) + `test_program_cook.cpp` (10 `[cook]` — cross-Context round-trip, §107-through-the-blob, deps survival, text≡builder, unregistered/structure/recursion reject, BadDeps, WrongType, BadContainer) + `test_opgen.py` +1 (native emission). **Gate:** crd-ceir-tests + crd-ceir-host-tests + crd-ceir-cook-tests **226/226 ctest** (209 + 7 `[program-asset]` + 10 `[cook]`; both OSes via `ctest -R ceir`) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy + GCC `-Werror=switch` (the `CookError`/`ReadError` switches) + `crd-ceir-opgen-{drift,validator}` (**52** py) + **`crd-ceir-invariants` (I5 as-is, I3 +crd/resources)** + a full **win-debug all-targets build** (no crdr consumer broke) + invariants. **No binary version bump.** | §105 §106 §107 |
| ✅ | CEIR-7b | ✅ **CLOSED 2026-08-09. The CEIR RUNTIME PROGRAM + generation-safe handle (§105).** A **`RuntimeProgram`** (in the crd-ceir-cook bridge, `runtime_program.hpp`) — the LOADED, immutable runtime form of a cooked `'CEIR'` asset = `{Module* · content_hash · interface_hash}` (the executable form — CompiledExecutionPlan / interpreter binding — is **CEIR-11**, NOT here). ⭐ **CEIR is the FIRST consumer of crd-render-asset-core's `RuntimeSlot<T>`/`RuntimeHandle<T>` framework** (RAF-3 built the generation-tagged handle for the render families to adopt — CEIR adopted it first): `using ProgramSlot = RuntimeSlot<RuntimeProgram>` reused VERBATIM (install bumps the generation; a handle minted before a same-id re-install is `is_current()==false` — the RAF-11 hot-reload safety property, proven for RuntimeProgram). ⛔ **`load_program` VALIDATES the header's declared hashes against the payload** (the declared-header-words-validated scar — closed AT LOAD; 7a closed it at cook, `read_program` does NOT recompute): registration check FIRST (`find_unregistered_op` → a typed `UnregisteredOp`, because `interface_hash`'s effect walk is registration-sensitive — the register-to-verify contract), THEN recompute both hashes and match the header. **Two mismatch discriminators, both tested:** a CONTENT mismatch = a CORRUPT/SPLICED blob; an INTERFACE mismatch WITH content matching = **REGISTRY DRIFT** (a dialect's declared effects changed between cook and load) — the discriminator a 7c swap-compatibility comparison relies on. ⭐ A content-hash match **carries the cook-time verification FORWARD** — no verifier re-run at load. **Identity resolved (7a's provisional struck in place):** the CRDR container id = `ResourceId::from_content(program blob)` (the shader-cook precedent, ADR-0104 content-addressed); the **AssetId is the LOGICAL identity** (the `CookedHeader.id` + the `RuntimeHandle` key); ResourceId (128-bit physical container) and AssetId (64-bit logical, path-hashed) are PERMANENTLY DISTINCT layers, no mapping. ⛔ **Context: BORROW, not own** (advisor) — no ownership / deferred-destroy machinery (that is 7c's row); but the **per-generation-Context pattern is named as 7c's INBOUND** (each generation deserialized into its OWN Context, so a hot-swap frees an old generation WHOLESALE — arenas free per-Context, never per-module) + PROVEN by the two-Context two-generation test (two generations coexist independently, both modules alive). **Module edges:** crd-render-asset-core moved **PRIVATE→PUBLIC** in the bridge CMake (usage requirements — `runtime_program.hpp` names the RAF types); crd-resources stays PRIVATE (impl-only); ⛔ all-targets build NOT triggered (only the bridge's OWN files changed — no non-ceir engine header touched). crd-ceir gains NO edge (I5 green). Tests: `test_runtime_program.cpp` (6 `[runtime]`: hash-validated load → current handle; same-id re-install → old handle stale + wrong-id handle not current; two-Context two-generation coexistence; the SPLICE forge (P1 header + P2 program) → `ContentHashMismatch`; unregistered-at-load → `UnregisteredOp`; a corrupted iface-word (META offset 12, content intact) → `InterfaceHashMismatch` — also PINS the header-offset layout). **Gate:** crd-ceir-tests + crd-ceir-host-tests + crd-ceir-cook-tests **232/232 ctest** (226 + 6 `[runtime]`) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** (full `-R ceir` on every config) + LLVM-20 tidy + GCC `-Werror=switch` (the `LoadError` switch) + `crd-ceir-opgen-{drift,validator}` (52 py) + **`crd-ceir-invariants` (crd-ceir asset-free + jobs-free)** + invariants. ~~⚠ The §107 capability-contract field STILL has no owning row (flagged for the user since 7a).~~ **[RESOLVED at the re-baseline → CEIR-8f.]** **No binary version bump.** | §105 |
| ➡ | ~~CEIR-7c/7d/7z~~ | **RE-HOMED → CEIR-10a/10b/10z at the 2026-08-09 universality re-baseline.** Why the pause: 7c's state migration CONSUMES stable state identity (a CEIR-8d foundation output — the 7a close itself recorded "cells are instance-keyed, no stable id"), and 7d's plan cache CONSUMES the CEIR-8h incremental-evaluation architecture. Building them first guaranteed rework; they resume as CEIR-10 AFTER foundation closure + universality validation, verbatim contracts preserved. | §108 §109 §110 §172 |

---

## ⭐ THE 2026-08-09 UNIVERSALITY RE-BASELINE (user-directed; full renumber per the quest's §113)

**Why.** The universality quest (`docs/research/2026-08-09-ceir-universality-review.md` — the standing review doc;
its section numbers cited below as **U-§n**) audited the shipped substrate (bands 1–7b, 232 tests, 4 configs)
against the standard *"one coherent programmable substrate for every computational domain Cerid may target —
engine, MATLAB-class, DAW, DCC, CAD/CAM, EDA/PCB, simulation, ML, media, agents"*. Verdict: the op layer is
genuinely open-world (interned OpId, no central switch, unknown-dialect preservation — I6-gated), but **types,
attributes, effect families, effect locations, and region kinds are closed vocabularies; semantic identity is
pointer-based; there is no analysis/pass/rewrite framework, no incremental-evaluation model, no transaction model,
and capability contracts have no home.** Several of these are RECORDED debts: ADR-0109 §6 promised stable semantic
identity at CEIR-1c (only SourceLoc landed); `attr.hpp:6` promised extended attribute kinds at "CEIR-2/3" (never
landed); the 7a close flagged capability contracts ownerless and state cells instance-keyed. **CEIR-8 pays these
debts; CEIR-9 proves the payment; CEIR-10 resumes band 7 on the closed foundation.** Nothing shipped is rewritten —
every foundation slice EXTENDS the live substrate with all prior tests green (serialization/hash changes are
versioned recooks, named per slice).

**The numbering map (old → new).** Closed bands 0–7 keep their numbers (frozen ledger). Everything ⬜ renumbered:

| Old | New | | Old | New | | Old | New |
|---|---|---|---|---|---|---|---|
| — | **8 Foundation Closure (NEW)** | | 10 Compute+transfer | 13 | | 22 AD | 25 |
| — | **9 Universality Validation (NEW)** | | 11 Render | 14 | | 23 Optimizer-1 | 26 |
| 7c/7d/7z | **10a/10b/10z** | | 12 FrameGraph | 15 | | 24 Transform IR | 27 |
| 8 Ref executor | 11 | | 13 Executor migration | 16 | | 25 Autotune | 28 |
| 9 Resource/memory | 12 | | 14–21 | 17–24 (+3) | | 26–32 | 29–35 (+3) |

**Cadence (user verdict 2026-08-09):** foundation ADRs are advisor-approved, fully autonomous (the gold-standard
mandate — "EVERYTHING MUST BE GOLD STANDARD AND UNIVERSAL"); each CEIR-8 slice = ADR-quality design (0c rigor) →
advisor gate → implementation → tests → 4-config gate, in one slice.

---

## CEIR-8 — FOUNDATION CLOSURE — ✅ **BAND 8 CLOSED** (8a ✅, 8b ✅, 8c ✅, 8d ✅, 8e ✅, 8f ✅, 8g ✅, 8h ✅, 8i ✅, 8z ✅) (the universality quest, part 1) → U-§112 §121 · mission §10 §99 §101

**Band contract.** Close every U-§112 foundation item BEFORE any further feature band. Universality is achieved by
OPEN-WORLD SEMANTICS (U-§5): a new domain must normally require new dialects/ops/types/attrs/interfaces/providers —
never a second execution architecture (U-§1) — and where a vocabulary stays deliberately closed, the reason is
documented in the ADR. ⛔ NOT a rewrite: the shipped 232-test substrate stays green through every slice; format
changes are versioned recooks. ⛔ The anti-goals (U-§125) bind every design: no universal node enum, no universal
Variant, no Context god-object, no stringly-typed extension surface. **Gate: the U-§121 twenty-item foundation DoD
answered item-by-item from evidence.**

| ✔ | Slice | Deliverable | → §§ |
|---|---|---|---|
| ✅ | CEIR-8a | ✅ **CLOSED 2026-08-09 (ADR-0111 ACCEPTED).** **The open-world TYPE model — dialect-defined type-classes BESIDE the built-in `TypeKind` fast path, the op model applied to types.** ⭐ **`TypeKind::Extern` is the ONE-TIME enum edit that opens the door — `testcad.toposhape` AND `testeda.net` landed with ZERO further `TypeKind` edits (and every custom type after them, forever).** A `TypeClassId` (interned FNV "dialect.class", mirroring `OpId`) + `Dialect::register_type_class(TypeClassSpec{verify hook, version})` → a `TypeClassInfo` on the Context; an `Extern` `Type` carries `type_class` + `type_class_version` and its PARAMETERS ride the existing slots (`members`/`count`/`cols`/`is_signed`/`fkind`/`name`/`labels`) — so custom types inherit interning, structural equality/hash, arena spans, and serialization FREE (the 3a machinery). ⛔ **The load-bearing invariant:** `operator==` + the intern hasher + `type_is_canonical` all include `type_class`/`type_class_version` (two different classes with identical params = DIFFERENT types; the junk-fields-are-Extern-only canonicality guard closes the prints-identically-but-differs hazard). **String-keyed serialization** (the class STRING via STRP, mirroring op-name encoding) + a **GENERIC canonical text form** (`!extern<CLASS,VER,COUNT,COLS,SIGNED,FKIND,"NAME",NMEM,m..,NLAB,"l"..>` — ⛔ NO pretty print/parse hooks in 8a: they'd emit text an UNREGISTERED Context cannot re-parse, breaking the U-§56 round-trip; named-forward to the language/editor bands). **Binary:** an Extern record's trailing class-string + version, ONLY on `kind==Extern` → **NO version bump** (pre-8a decoders reject the out-of-range kind; pre-8a blobs decode byte-identically — tested). **The verify TRIPLE** (ADR §2.5): the factory `type_extern` ASSERTS, the binary decoder + text parser REJECT (registered-invalid), an UNREGISTERED class PRESERVES opaquely at all three (U-§56). **Version range-check on decode:** a NEWER-schema record than the loader = reject; an older record = accept (migration named-forward — ⚠ **version-sensitivity consequence:** an old-version record is a DISTINCT type from the current-schema type, honest). ⭐ **The U-§56 headline PROVEN both text AND binary:** serialize/print registered → decode/parse into an UNREGISTERED Context → re-serialize/re-print BYTE-EXACT → late-register → the preserved type UNIFIES (same TypeId) with a factory build of the class. Generics: `substitute`/`type_has_params` walk `members` generically — a generic custom type substitutes through, class preserved (tested). Interface hash: the class string emitted CONDITIONALLY (only Extern) → custom types discriminate by class, ZERO churn for existing content. Shape/broadcast queries reach an Extern only by caller error → degrade to Unknown/false via their existing defaults. **Scope fence (named-forward in ADR):** TOML `[[type]]` generator · pretty hooks · attr/data payloads (8b) · migration transforms. **Tests:** `test_extern_type.cpp` (10 `[extern-type]`: dedup + different-class discriminator; the canonicality guard; text+binary round-trip; the U-§56 headline both forms + late-register unify; the verify triple [factory/decoder/parser]; interface-hash class discrimination; version range [newer-reject + older-accept]; substitute-through-Extern; pre-8a no-churn; a single-byte-corruption fuzz sweep over the Extern decode path, ASan-proven). **Gate:** crd-ceir-tests + host + cook **242/242 ctest** (232 + 10) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** (all 3 targets rebuilt fresh against the new `Type` layout — the stale-.obj scar) + LLVM-20 tidy (11 files) + GCC `-Werror=switch` (every `TypeKind` switch carries the Extern arm) + opgen drift/validator (no TOML change) + `crd-ceir-invariants` + invariants. **No binary version bump.** | U-§6 |
| ✅ | CEIR-8b | ✅ **CLOSED 2026-08-09 (ADR-0112 ACCEPTED).** **The open-world ATTRIBUTE model — TWO wrappers (`TypedConst`, `Extern`) over ONE aggregate vocabulary (`Array`, `Dict`), the 8a type-model surface applied to attributes; the `attr.hpp` CEIR-2/3 IOU (arrays/dicts/typed constants) PAID.** ⭐ **`AttrKind::{Array,Dict,TypedConst,Extern}` is the ONE-TIME enum edit that opens the door — `testunit.dimension` custom attributes AND arbitrary nested aggregates landed with ZERO further `AttrKind` edits (and every dialect attribute-class after, forever).** An `AttrClassId` (interned FNV "dialect.attr", mirroring `TypeClassId`/`OpId`) + `Dialect::register_attr_class(AttrClassSpec{verify hook, version})` → an `AttrClassInfo` on the Context; an `Extern`/`TypedConst` `AttrValue` carries a single `payload` `AttrId`, `Array`/`Dict` carry an `elems` span (+ parallel SORTED `keys` for `Dict`) — so custom + aggregate attributes inherit interning, structural equality/hash, arena spans, and serialization FREE (the 1c machinery). ⛔ **The load-bearing invariants:** `operator==` + `attr_is_canonical` include payloads/spans/`attr_class`/`wrapped_type` (a different class or wrapped-type with identical payload = a DIFFERENT attr — the landmine); the junk-fields-are-kind-scoped canonicality guard (`i==0` reads the whole scalar union) closes the prints-identically-but-differs hazard; **Dict keys byte-order SORTED + UNIQUE** (dedup + content-hash stability — `attr_dict` canonicalizes; a hand-built unsorted/duplicate dict is REJECTED, never asserted); ⛔ **the WRAPPER-COMPOSITION rule** — a wrapper's payload must not itself be a wrapper (the `qty<qty>` precedent), gated by `verify_attr_extern` for BOTH wrappers; a **child-first ATTR pool** (aggregates/wrappers pool their child `AttrId`s before their own index, mirroring `intern_type_pool` — the decoder rejects a forward ref by construction). **String-keyed serialization** (the class STRING via STRP) + a **GENERIC canonical text form** (`[v0,v1]` · `{"k":v}` · `#typed<!TYPE,payload>` · `#extern<dialect.attr,VER,payload>` — ⛔ NO pretty hooks: they'd break the U-§56 round-trip). **Binary + text:** the new fields written ONLY on the new kinds → the six scalar encodings are BYTE-IDENTICAL to pre-8b → **NO version bump, ZERO recook** (existing content's `stable_hash` unchanged — tested). ⛔ **Attributes feed the CONTENT hash but NOT the interface hash** (a func's body-op attr changes `stable_hash`, not `interface_hash` — tested). **The verify TRIPLE** (ADR §5): the factory `attr_typed`/`attr_extern` ASSERT, the binary decoder + text parser REJECT (registered-invalid + non-canonical + wrapper-on-wrapper), an UNREGISTERED class PRESERVES opaquely (U-§56). **Version range-check SYMMETRIC across text + binary** — a NEWER-schema record is rejected by an older loader in BOTH forms (⭐ the 8a text-parser leg, which was binary-only, RETROFITTED here so `parse_extern` types + `parse_wrapper_attr` attrs agree). **Depth guard** on the (now-recursive) text parser (the binary decoder is iterative — no guard needed). ⭐ **The U-§56 headline PROVEN both text AND binary:** serialize/print registered → decode/parse into an UNREGISTERED Context → re-serialize/re-print BYTE-EXACT → late-register → the preserved attr UNIFIES (same AttrId) with a factory build. **Scope fence (named-forward in ADR):** TOML `[[attr]]` generator · pretty hooks · migration transforms. **Tests:** `test_extern_attr.cpp` (13 `[extern-attr]`: dedup + wrapper/class discriminator; the canonicality guard [junk-on-scalar, unsorted/duplicate dict, classless Extern]; nested text+binary round-trip [the child-first pool]; empty aggregates; the U-§56 headline both forms + late-register unify; the verify triple; the wrapper-composition rule; content≠interface hash; version range both forms; a hostile unsorted/duplicate-dict TEXT graceful-reject; the depth guard; pre-8b no-churn; a single-byte-corruption fuzz sweep, ASan-proven). **Gate:** crd-ceir-tests + host + cook **255/255 ctest** (242 + 13) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** (all 3 targets rebuilt fresh against the new `AttrValue` layout — the stale-.obj scar) + LLVM-20 tidy (10 files) + GCC `-Werror=switch` (every `AttrKind` switch carries the four new arms) + opgen drift/validator + `crd-ceir-invariants`. **No binary version bump.** | U-§7 |
| ✅ | CEIR-8c | ✅ **CLOSED 2026-08-09 (ADR-0113 ACCEPTED).** **Effect FAMILY widening u32→u64 (closed vocabulary, documented-why-closed) + the OPEN effect-LOCATION model (dialect-extensible).** ⭐ **The family bitmask went u32→u64 to admit the 8 U-§19 domain families** (`DocumentRead/Write`, `ConstraintRead/Write`, `TransactionBoundary`, `UIRead/Write`, `AgentAction` — ordinals 27..34, crossing bit 31) — each CLASSIFIED in `hazard.hpp::effect_access` (the `-Werror=switch` total-switch forces it; +4 `ResourceClass`es Document/Constraint/Ui/Agent, Transaction→Universe). ⛔ **Families stay CLOSED — documented-why (U-§5):** the mask is the O(1) union type on the hot `collect_region_effective_mask` path (an open set can't be a fixed-width bitset); the total-switch classifier IS the append-at-end safety; families are access-pattern AXES, domain identity lives in the LOCATION. ⛔ **The SECOND family consumer** (`effect_legal_in_region`, §32 audio-RT legality — a DENYLIST predicate, NOT switch-guarded) was audited by hand: `TransactionBoundary` (can block, like Synchronization) + `AgentAction` (unbounded, like ExternalCall) join the forbidden set; the 6 bounded domain read/writes stay legal (like SceneWrite). ⭐ **The OPEN LOCATION model:** `EffectTarget` extended IN PLACE (append `BufferRange`·`ImageSubresource`·`TensorSlice`·`EcsComponent`·`DocObject`·`StateSlot`·`File`·`Net` + one `Extern` door after `Result` — ordinals 0..2 unchanged so every generated `EffectRecord` array compiles untouched; `using LocationKind = EffectTarget`). A `LocationClassId` (interned FNV "dialect.location", mirroring TypeClassId/AttrClassId) + `Dialect::register_location_class(LocationClassSpec{verify, resource_class, version})` → a Context registry; `EffectRecord` gains a `LocationClassId location_class` (a `u64` → the POD stays trivially-copyable; the junk-field guard: set IFF `target==Extern`, asserted at register_op — the factory leg of the verify triple). ⛔ **EMPTY≠UNKNOWN, extended:** the hazard analysis reads an Extern location's registered `resource_class` — an **UNREGISTERED** Extern location is `ResourceClass::Universe` (conflicts with EVERYTHING, never inert). **Serialization:** effects are REGISTRATION metadata (re-derived on load — `binary.cpp` encodes zero effect bytes), so the location model has NO per-instance serialization surface (the 8a/8b string-keyed/text/decode-version-range-check legs are **N/A-by-design**, per-instance location IDENTITY named-forward to 8d — NOT a parallel identity graph). ⛔ **The ONE serialized surface — a NAMED, universal RECOOK (unlike 8a/8b):** the §107 interface-hash projection pushes the u64 mask (`push_u32`→`push_u64`) → EVERY effectful program's KernelRef changed (even effect-free ones — the mask is pushed unconditionally); `kCeirCookSchema` bumped **1→2** so stale cooked blobs reject CLEANLY at the existing `read_cooked_header` SchemaMismatch check. ⛔ **NO `kBinaryVersion` bump** — the module blob is byte-unchanged (a bump would wrongly reject valid pre-8c blobs). **Tests:** `test_effect_open.cpp` (8 `[effect-open]`: the ≥bit-32 interface-hash truncation catcher [AgentAction bit-34 aliasing MemoryReadWrite bit-2]; the u64 mask through `effective_effects`; a PRIVATE callee's bit-34 family lifting transitively into the caller's iface hash [the `call_effects_fn` u64 path]; Extern location → registered class / UNREGISTERED → Universe; the hazard matrix [same class conflicts, distinct classes don't, unregistered conflicts with all]; the location verify hook rejecting a registered-invalid record; the audio-RT legality classification [Agent/Txn forbidden, DocumentWrite legal]; `kBinaryVersion==2` + a byte-exact round-trip). **Gate:** crd-ceir-tests + host + cook **263/263 ctest** (255 + 8) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** (all 3 targets fresh against the new `EffectRecord`/family-width layout — the stale-.obj scar) + LLVM-20 tidy (12 files) + GCC `-Werror=switch` (`effect_access` carries the 8 new arms) + opgen drift/validator (the EFFECT_FAMILIES lockstep 27→35 + its test) + `crd-ceir-invariants`. **Interface-hash recook (kCeirCookSchema 1→2); NO binary-format bump.** | U-§19 U-§20 |
| ✅ | CEIR-8d | ✅ **CLOSED 2026-08-09 (ADR-0114 ACCEPTED).** **Content-independent STABLE SEMANTIC IDENTITY — the ADR-0109 §6 IOU (only SourceLoc landed at 1c).** ⭐ **A `StableId` (u64, 0=invalid) rides `Operation`; ONE id space** — a function IS a `func.func` op, a state slot IS a `StateEdge` op, a visual node's identity IS its op's stable id (no per-kind counters). **Assignment (`Context::assign_stable_ids`) is module-scoped, pre-order, one-time, idempotent** (max+1 for id-0 ops; already-assigned ops never re-derived → survives edits/reorders/renames). ⛔ **NOT a Context counter** (that would break the blob-is-a-pure-function-of-content gate); module-scoped pre-order keeps ids a pure function of content. ⛔ **A monotone WATERMARK** (max id EVER assigned, per module, serialized + restored) — an `erase()`d op is tombstoned/invisible to a live-max scan, so without it a later append would REUSE the dead id and the delete/re-add discriminator would silently pass (state corruption); assignment draws from `max(scan_max, watermark)+1`, so a freed id is never reused (in-memory or across load). **Serialization:** an additive, forward-skippable `STID` chunk (`count + watermark + per-op ids`, body pre-order) → pre-8d decoders skip it, a pre-8d blob decodes fine (ids re-seed pre-order) → ⛔ **NO `kBinaryVersion` bump.** Decode is build-raw-graceful-reject (a 0 id / duplicate / count-mismatch / id>watermark ⇒ reject, never assert; fuzz-swept ASan). ⛔ **The content hash stays id-INDEPENDENT:** `stable_hash` builds the blob WITHOUT the STID chunk → byte-identical to pre-8d → **ZERO content-hash churn, cook-cache hits survive.** The principle: content projections are id-free (`stable_hash` AND the TEXT form — text stays the id-free content projection, documented-why: a per-op id token would churn every print golden for no persistence gain; a test pins that a fresh module's text round-trip reproduces its ids); identity/persistence surfaces (binary STID + the in-memory Module) carry ids. ⭐ **The §20 state schema re-keyed by stable id (sorted, id VALUE in the hash):** a StateEdge-cell REORDER is now invariant (the 7a/8c false-incompatible FIXED), while delete-id-1 + add-id-2 is correctly INCOMPATIBLE (an order-only hash would silently lose state at 10a migration). One shared `assign_stable_ids` used by serialize + interface_hash (no drift). Runtime `m_cells` stays `Operation*`-keyed (transient within-run; cross-version cell matching is 10a). **Interface-hash recook (kCeirCookSchema 2→3); NO binary-format bump.** ⛔ **Unblocks CEIR-10a state migration, CEIR-8i transactions, semantic diff, visual identity; anchors 8c's built-in location kinds' per-instance identity.** **Tests:** `test_stable_id.cpp` (12 `[stable-id]`: pre-order 1..N + idempotent; ids ride the op through serialize; hostile-STID graceful reject; content-hash id-independence; blob purity; the reorder-invariant + delete/re-add-sensitive state schema; the erase-never-reuses-an-id watermark [in-memory + across load]; a genuine pre-8d blob decode→re-serialize fixpoint; `kBinaryVersion==2`; a fresh-module text round-trip reproduces ids; a single-byte fuzz sweep). **Gate:** crd-ceir-tests + host + cook **275/275 ctest** (263 + 12) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** (all 3 targets fresh against the new `Operation`/`Module` layout — the stale-.obj scar) + LLVM-20 tidy (8 files) + GCC + opgen drift/validator + `crd-ceir-invariants`. | U-§101 U-§12 · §10 |
| ✅ | CEIR-8e | ✅ **CLOSED 2026-08-09 (ADR-0115 ACCEPTED).** **Trait/interface split + region-kind reservation — the closed-vs-open BEHAVIOR boundary.** ⛔ **OpTrait stays CLOSED, documented-why-closed (U-§5) + a FACTORY GUARD:** traits are the fixed reasoning AXES the core's verifiers switch on (5b Terminator, 5d StateEdge, 6a Token*); dialects **SET** core-minted bits, never **MINT** — `register_op` asserts `(spec.traits & ~kKnownTraitsMask) == 0` (the `kLastEffectFamily` parallel; the dialect.hpp "a dialect ORs its own bits" comment struck in place); `OP_TRAITS` count-pinned in the opgen validator. ⭐ **The `OpInterface` registry PROMOTED to the TYPED open-world surface** (`interface.hpp`): PLUGIN BEHAVIOR lives in a typed function-table interface an op-kind implements — dispatched with ZERO `switch(op.kind)` and ZERO new trait bits. `InterfaceId` became a **u64 FNV** of the interface name (like TypeClassId/AttrClassId/LocationClassId) → `T::kId` is a **COMPILE-TIME constant** (a `constexpr interface_hash_ct` byte-identical to `fnv1a_64`) so a query mints it with zero registry work (the per-query linear string scan is GONE); `intern_interface` keeps the runtime path + a reverse-lookup name table. **Type-safe accessors** `register_op_interface<T>`/`get_op_interface<T>` do the id+cast internally (can't mismatch). ⛔ **"Op first" is a LOUD contract:** `register_interface` asserts the kind is registered (an impl lives on its OpInfo) rather than silently dropping — a plugin footgun on the extension surface; re-register overwrites in place. **Catalog: ONE live proof (`crd.iface.cost` — the only family with no existing home) + 7 reserved names MAPPED to their homes** (MemoryEffect→EffectRecord/EffectsFn · Shape→opgen type/shape_inference · Lowering→IExecutionProvider · Timeline→region_exec+8f · Incremental→8h · Constraint→8c family · Serialization→8a/8b/8c+8d) — so a future slice EXTENDS the existing surface, never forks it (the third-graph anti-pattern). ⛔ **RegionKind stays `{Graph, SsaCfg}`** (the structural graph-vs-CFG tag); the reserved region semantics (Reactive/StateMachine/Timeline/Constraint/Device) are **interface/attr-gated via a documented POLICY** (the CEIR-4c `region_exec` attr precedent) — NO enum widening, NO serialized-format change; the policy names the one future path (append per 8a if a *structural* need proves out). ⛔ **The band's FIRST ZERO-MOTION slice:** traits/interfaces are registration metadata, RegionKind unchanged → NO kBinaryVersion bump, NO kCeirCookSchema bump, NO interface/content-hash change, NO recook of any kind (the full gate still ran — `InterfaceId` u32→u64 grew the OpInterface/OpInfo layout, the stale-.obj scar). **Tests:** `test_interface_typed.cpp` (4 `[interface]`: typed dispatch knowing nothing about the kind + overwrite + missing/unregistered→nullptr; compile-time kId == runtime intern (the FNV pin) + distinct names→distinct ids + reverse lookup; the 7 reserved names all distinct; kKnownTraitsMask is exactly the 8 contiguous bits, no gap/no stray). **Gate:** crd-ceir-tests + host + cook **279/279 ctest** (275 + 4) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** (all 3 targets fresh against the new `InterfaceId`/`OpInterface` layout) + LLVM-20 tidy (7 files incl. the new `interface.hpp`) + GCC (the constexpr FNV path) + opgen drift/validator (the OP_TRAITS count-pin) + `crd-ceir-invariants`. **ZERO recook.** | U-§8 U-§10 |
| ✅ | CEIR-8f | ✅ **CLOSED 2026-08-09 (ADR-0116 ACCEPTED).** **Capability contracts + domain/safety split + typed time domains — three U-§ gaps.** ⭐ **The U-§57 CAPABILITY model OWNS the §107 field ownerless since 7a.** A `CapabilityId` = FNV of a capability NAME (`gpu.compute`/`file.write`/`external.process`/… — open-world, **intern-only**: a capability is a name, no verify/version). `OpSpec.capabilities` (opgen-emitted from `[op.native] capabilities`) → `register_op` interns them (⛔ rejects an EMPTY name — the declared-words-validated guard) into `OpInfo.required_capabilities` (arena-copied, the effects precedent). The PROGRAM's required set = the **module-wide** (pre-order — funcs ARE ops in the body, so no "transitive" machinery) **sorted-UNIQUE union**; ⛔ an UNREGISTERED op contributes `external.process` (EMPTY≠UNKNOWN — an unknown op must not read as requiring-nothing). It JOINS the §107 interface hash (a `"caps:"` section, **count pushed UNCONDITIONALLY even at 0** — the 8c no-conditional-width lesson; then the sorted-unique id VALUES) → a NAMED interface-hash recook, **kCeirCookSchema 3→4**; `stable_hash`+`kBinaryVersion` UNTOUCHED (registration metadata — a cap-bearing module round-trips byte-exact). `program_capabilities`/`capabilities_satisfied` are the host-grant queries; sandbox enforcement + a cooked-META surfacing of the set are named-forward. **The domain/safety split (U-§23):** `EvalDomain` stays WHERE; the safety axes (may-allocate/may-block/may-IO + derived `realtime_safe`) are a **PROJECTION of the effect families** — `effect_safety` a per-family **TOTAL SWITCH** (the `effect_access` shape, `-Werror=switch`-guarded — NOT a predicate that dodges the guard, the 8c hole), folded by `op_safety` (unregistered ⇒ maximally unsafe). ⛔ **The two RT oracles reconciled as a documented SUBSET, not drift:** the `effect_legal_in_region` HARD gate permits `Allocate` in an audio-RT region (a soft cost, not a priority-inversion deadlock) while `realtime_safe` excludes it → `realtime_safe ⟹ legal`, never converse (pinned side by side). **Typed time domains (U-§17):** ⛔ **a time domain IS an 8a TYPE-CLASS** (NO `TimeDomainId` — that would fork `TypeClassId`); a hand-registered `time` dialect registers the six built-ins (wall/sim/frame/audio_sample/sequencer/logical) with a verify hook (one Int/Float/Quantity/TypeParam member — the units-Time seed + generics); `time.wall<T> != time.sim<T>` as distinct TypeIds (the ADR-0111 landmine) — the type distinction a future operand-type checker rejects mixing on (**enforcement named-forward** — no such checker exists yet). A plugin `game.turn` domain works with ZERO central edits; U-§56 round-trip inherited from 8a. **Tests:** `test_capability.cpp` (7 `[capability]`), `test_safety.cpp` (2 `[safety]`), `test_time_domain.cpp` (3 `[time]`): the required-set round-trip + external.process EMPTY≠UNKNOWN + func-body-reach + module-wide sorted-unique union + host-grant check + the interface-hash reorder-invariant/membership-sensitive/unregistered-moves-it + cap-bearing byte-exact round-trip; effect_safety+op_safety fold + the two-oracles subset; wall≠sim + plugin domain + verify-rejects-0/2-members + U-§56. **Gate:** crd-ceir-tests + host + cook **291/291 ctest** (279 + 12) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** (all 3 targets fresh against the new `OpInfo`/`OpSpec` layout) + LLVM-20 tidy (14 files incl. the new `time.hpp`/`time.cpp`) + GCC `-Werror=switch` (the 35-arm `effect_safety`) + opgen drift/validator (the capabilities→OpSpec emission + the empty-name arm; regen clean) + `crd-ceir-invariants`. **Interface-hash recook (kCeirCookSchema 3→4); NO binary-format bump; ZERO content-hash churn.** | U-§57 U-§23 U-§17 · §99 §147 |
| ✅ | CEIR-8g | ✅ **CLOSED 2026-08-09 (ADR-0117 ACCEPTED).** **The compiler-infrastructure SKELETON — four thin frameworks, ⛔ FRAMEWORKS ONLY (no optimization passes / no real lowering — CEIR-26/27; the no-consumer rule).** ⛔ **Context stays lean (U-§72):** every manager is a SEPARATE class over a Context/Module, ZERO new Context members. ⭐ **ONE shared `fnv1a_ct` (id.hpp)** — `make_interface_id`/`make_analysis_id`/`make_diagnostic_code` all call it (a copy-per-id would drift from `hash_string`; the 8e `interface_hash_ct` was hoisted here); `AnalysisId`/`DiagnosticCode` are open-world FNVs, `kId` compile-time. **AnalysisManager (U-§73):** a registered analysis (`kId` + a compute fn → an arena `const T*`); `get<T>` computes-or-caches; ⛔ **never-stale INVALIDATION** — a pass declares a PRESERVED set, the manager EVICTS the rest (+ `invalidate_all`); a pass reporting **unchanged preserves ALL** regardless of its set. ⛔ **NO inter-analysis dependency tracking** — that would be a new dependency graph ONE SLICE before 8h unifies the ≥2 the tree holds; inter-analysis deps are a documented pass-author contract, **named-forward to 8h**. (Eviction leaks into the arena — accepted, grow-by-rebuild.) **PassManager (U-§65):** a `Pass` = name + run fn (`bool(Context&, Module&, DiagnosticEngine&)`) + preserved set; `run` sequences + drives invalidation + ⛔ **STOPS on a Fatal** (`has_fatal()` — the skeleton's one real semantic hook, wired not just claimed). **Rewrite/Conversion (U-§67/U-§74):** `RewritePattern{match, rewrite}` + ⛔ **caller-driven per-op `try_apply`** (NOT a block walk — the iterator-invalidation/worklist driver IS CEIR-26, reserved); `ConversionTarget` per-kind legality (Legal/Illegal/Dynamic) with ⛔ an **UNLISTED kind ⇒ Illegal** (EMPTY≠UNKNOWN). **DiagnosticEngine (U-§102):** one structured surface — `DiagnosticCode` + `Severity` (closed axis) + `SourceLoc` + message + notes + fix-its; `render` prints code-name + `file:line:col` + message; ⛔ **ALL text COPIED into the engine arena** (alloc-outlives-borrowers — a diagnostic outlives the dying buffer it came from, ASan-proven on the money config). ⛔ **ZERO-MOTION recook** (nothing serializes — no kBinaryVersion/kCeirCookSchema/hash change; the ceir.hpp umbrella edit is why all 3 targets rebuild + the full gate runs). **Tests:** `test_pass_manager.cpp` (6 `[analysis][pass]`: compute-once/cache + never-stale invalidation + FRESH-data-after-invalidation + unchanged-preserves-all + PassManager-drives-invalidation + Fatal-short-circuits-the-pipeline + shared-FNV pin), `test_rewrite.cpp` (2 `[rewrite]`: try_apply match/rewrite/decline + ConversionTarget legality + unlisted⇒Illegal + last-set-wins), `test_diagnostic.cpp` (4 `[diagnostic]`: emit/collect/has_errors + render + Fatal + the text-COPY ASan probe + shared-FNV pin). **Gate:** crd-ceir-tests + host + cook **303/303 ctest** (291 + 12) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy (9 files incl. the 3 NEW headers pass_manager/rewrite/diagnostic.hpp) + GCC + opgen drift/validator + `crd-ceir-invariants`. **ZERO recook.** | U-§65 U-§72–74 U-§102 |
| ✅ | CEIR-8h | ✅ **CLOSED 2026-08-09 (ADR-0118 ACCEPTED).** **The incremental-evaluation ENGINE-FIRST UNIFICATION — ONE dependency/dirty engine the tree's ≥2 models converge on.** ⛔ **The layering wall decided the home:** `crd-ceir` is asset-free (I4/I5) and `render-asset-core` does NOT link crd-ceir — independent SIBLING modules, so a crd-ceir engine is a graph render-asset-core can NEVER adopt (a permanent third graph by construction); the ONLY home both link with zero new edges is a LOW shared module → **`crd::containers::IncrementalDag`** (a DAG is a generic data structure). ⭐ **A DAG keyed by `NodeId = u64`** with a DETERMINISTIC topo (deps-first, min-id tie-break — the RAF-11 reproducibility contract) + `affected_by` (transitive dependents), ABSORBED byte-identical from `DependencyGraph`; PLUS per-node **two revision hashes** (content/interface) driving the ⭐ **§107 rule GENERALIZED**: `recompute_after_change` recomputes the node on a content-OR-interface change, and its transitive dependents ONLY on an INTERFACE change (a content-only change HOT-SWAPS — dependents stay valid). ⛔ Holds STRUCTURE+REVISIONS+dirty propagation, NOT the consumer's cached RESULTS (keyed by the content hash). ⭐ **THE ABSORB IS REAL THIS SLICE:** `render-asset-core::DependencyGraph` keeps its 4-method API BYTE-STABLE and swaps its internals to an owned `IncrementalDag` (AssetId↔NodeId lossless + order-preserving) — the existing RAF/render-asset/scene-render/ceir-cook suites are the regression net (a canonical model with zero real consumers is how third graphs are born — the 7a capability field sat ownerless six slices). `validate_against` (needs AssetRegistry) stays wrapper-side. ⛔ **Named-forward (deliberately NOT wired):** the 8g AnalysisManager (a DIFFERENT paradigm — preserved-sets vs dependency-propagation; forcing it invents the inter-analysis edges 8g refused — CEIR-26+), the CookDb (already PRODUCES the content/interface pair the engine consumes — semantic unification, plan-cache wiring at 10b). `hesap-sched::DependencyGraph` is a scheduling precedence graph (no revision model) — a different problem, out of scope. **Advisor pre-close caught 5, all fixed:** the `interface` member/param renamed `interface_rev` (the Windows COM `#define interface struct` landmine in a foundational header); the blast radius enumerated BY SYMBOL (render-asset + scene-render + ceir-cook — which static-links render-asset-core, so it was RELINKED + re-run, not just the stale-object sanity); a reverse-insertion determinism test added; interface-change-implies-self-recompute (over-recompute, never stale); the hesap-sched note. ⛔ **ZERO-MOTION recook** (the engine is runtime state; DependencyGraph's §106 CDEP records unchanged in shape — the API is byte-stable). **Tests:** `test_incremental_dag.cpp` (6 `[containers][incremental]`: topo determinism + tie-break; affected_by transitive dependents; insertion-order independence; cycle⇒false; ⛔ the §107 rule [interface propagates, content-only doesn't, interface-only recomputes self]); the render-asset-core suite (exact-order + tie-break + cycle) is the byte-identical regression pin. **Gate:** crd-containers-tests + crd-render-asset-core-tests + crd-scene-render-tests + crd-ceir-cook-tests (relinked) + crd-ceir **on win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** (the money config verifies the wrapper + engine memory-safe) + LLVM-20 tidy (4 files incl. the new `incremental_dag.hpp`) + GCC + opgen drift/validator. **ZERO recook; the ≥2 graphs are now ONE.** | U-§15 U-§71 · §110 |
| ✅ | CEIR-8i | ✅ **CLOSED 2026-08-09 (ADR-0119 ACCEPTED).** **The TRANSACTION model — atomic authored-mutation with commit/rollback, the LAST foundation framework before 8z.** ⭐ **Eager mutation + an inverse-op UNDO JOURNAL, reverse-order replay** (rejected: a COW module snapshot fights the arena's stable-pointer identity model — handles ARE identity; a deferred-apply journal can't let the tx body see its own edits). ⛔ **Reverse-order replay IS the correctness proof:** each primitive's inverse telescopes back last-to-first (a new op wired to another new op un-wires before its producer is un-inserted; an erase preceded by a RAUW un-erases first, then the RAUW undoes onto the restored results). ⛔ **Transaction RECORDS, Context APPLIES — never a second mutation implementation** (the frame-graph-recording-mode discipline): six thin transaction-only Context primitives (`reinsert_erased_op`/`detach_and_point_use`/`rauw_recording`/`restore_attr_dict`/`resync_symbols`/`find_by_stable_id`) ride Context's EXISTING friendships → **ZERO `ir.hpp` edit**. **REUSES 8d/8h/8g:** references a pre-existing op by its 8d `StableId` (survives the edit); reports the touched/removed id-sets to the 8h `IncrementalDag` (the tx says WHAT changed, the consumer computes revisions — the 8h division of labour); a rejected edit / failed commit reports through the 8g `DiagnosticEngine`. ⛔ **The id-under-rollback subtlety (advisor):** `begin()` calls `assign_stable_ids` up front (settling pre-existing ids = the canonical serialized form, so the ONLY tx-window assignments are to tx-created ops) + records the watermark; `rollback()` restores it → byte-identity is UNCONDITIONAL; commit keeps the advance; a replacement never reuses a freed id (the 8d discriminator holds across a committed tx). ⛔ **Graceful-reject + POISON** (agent-facing, deserialize=build-raw-graceful-reject): every mutator pre-validates (belongs, not-erased, index-in-range, erase-target use-free) → a violation emits a Diagnostic + poisons; a poisoned commit auto-rolls-back. ⛔ **commit VERIFIES before the point of no return** (per-op verifier + a symbol re-sync that mutates only on success) → a failed commit never advances the watermark. **Advisor pre-close caught the region-bearing-erase subtree-boundary hole** (a nested IN-edge or OUT-edge would leak an SSA edge across the tombstone — `Operation::erase` doesn't recurse): the erase mutator walks the subtree and rejects both directions; a CLOSED subtree commits whole + rolls back whole. ⛔ **ZERO FORMAT MOTION** — a transaction serializes NOTHING (runtime edit-session state, like the 8g AnalysisManager / 4b compiler mode); a committed tx changes CONTENT → the ORDINARY content-hash cache miss, NOT a schema recook. `kBinaryVersion`=2, `kCeirCookSchema`=4 UNTOUCHED; no new serialized record ⇒ no new fuzz corpus. **Scope (advisor-blessed):** authored edits only; compiler-internal rewrites (5a fold, 8g rewrite driver) adopt transactional journaling at CEIR-26 — two entry disciplines over one Context path, no second implementation. Block-append to a PRE-EXISTING region named-forward (1a has no block-unlink inverse). **Tests:** `test_transaction.cpp` (12 `[ceir][transaction]`: commit applies + touched/removed; **the A/B money proof** [a poisoned edit → byte-identical rollback]; a duplicate-symbol commit-verify failure → byte-identical; reverse-replay exact block order [both interleavings]; a replacement never reuses a freed id; watermark-restore under rollback with a mid-tx serialize; erase-with-live-uses rejected not asserted; the attr-undo lifetime proof [rollback, DESTROY the tx, then read — the linux-asan Context-arena UAF guard]; the 8h seam [touched drives `recompute_after_change`, §107 rule]; region-bearing erase IN-edge reject / OUT-edge reject / closed-subtree accept+rollback). **Gate:** crd-ceir-tests + host + cook **315/315 ctest** (303 + 12) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** (all 3 targets fresh against the new `context.hpp` surface) + LLVM-20 tidy (6 files incl. the new `transaction.hpp`) + GCC `-Werror=switch` (the 5-arm rollback switch) + opgen drift/validator + `crd-ceir-invariants`. **ZERO recook.** | U-§14 U-§52 |
| ✅ | CEIR-8z | ✅ **CLOSED 2026-08-09 (no ADR — a gate COMPOSES ADR-0111…0119, the 3z/4z/5z precedent).** **BAND-8 GATE — the foundation is universal by open-world semantics, not a second architecture.** ⭐ **The U-§121 foundation DoD answered ITEM-BY-ITEM from evidence** (review doc: the §B foundation matrix — 23 rows, framed as the operational form of the quest's twenty-item list — each row → its closing slice + the SPECIFIC test that is its evidence): **20/23 CLOSED**, the 3 remainders (Provenance transform-preservation → 26, unknown-plugin EXECUTION-denial → 9h, agent QUERY introspection → 9g) are ◧ CONSUMER-band-deferred, NOT foundation gaps (each needs a real consumer to test against; policy/seam set). ⛔ **Advisor-caught: the §B matrix contradicted itself** — 3 stale rows fixed during the pass (Time domains read ❌ though 8f closed it; Unknown-plugin "no policy doc" though 8a/8e landed it; **Provenance "policy at 8g" was UNVERIFIED — ADR-0117 states no transform-preservation policy** → corrected to named-forward-26, not inherited as fact). ⭐ **`test_band8_gate.cpp` (5 `[gate8]`) — the COMPOSING proof, not a slice re-run:** ONE curated program (an Extern TYPE [8a] in an exported func signature + an Extern/aggregate ATTR [8b] on a cap-bearing op [8f] with EXPLICIT determinism/domain axes [the 4z trap] + settled stable ids [8d]) composed through: byte-identical text⇔binary; **decode into an UNREGISTERED host preserves the plugin content + re-serializes byte-exact** (U-§56 × STID × caps × extern in ONE blob — no slice test did this); re-register UNIFIES the preserved plugin type + the interface hash is **cross-Context pure** (caps+extern composed); **a TRANSACTION edits the preserved plugin content in the unregistered host** — commit succeeds (an unknown op verifies true) + touched-set correct, a second edit rolls back BYTE-IDENTICALLY (the U-§52 agent-edits-plugin story end-to-end); a single-byte corruption sweep over the **composed** blob (cross-chunk: ATTR pool × STID × extern class strings). ⛔ **Gate discipline confirmed:** recook migrations (kCeirCookSchema 1→2→3→4, kBinaryVersion held at 2) executed + clean (test_program_cook SchemaMismatch rejects stale blobs; the cook suite green at schema 4); the **fuzz corpus** covers every open-world SERIALIZED record (extern types 8a / extern+aggregate attrs 8b / STID chunk 8d / the composed blob 8z; effects/locations/safety/time/caps are REGISTRATION metadata, NOT serialized → N/A-by-design); **NO new invariant earns its keep** (documented-why: I6 gates op-kind dispatch, GCC `-Werror=switch` gates Extern-arm completeness [stronger than grep], the single-mutation-path is an ADR-0119 discipline construction paths legitimately use raw primitives for). **Gate:** crd-ceir-tests + host + cook **320/320 ctest** (315 + 5) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** (the money config proving the composed-blob fuzz + plugin-content transaction memory-safe) + LLVM-20 tidy (test_band8_gate.cpp) + GCC `-Werror=switch` + opgen drift/validator + `crd-ceir-invariants`. **ZERO recook (a gate serializes nothing new). → BAND 8 CLOSED (8a..8z).** | U-§121 |

## CEIR-9 — UNIVERSALITY VALIDATION — ✅ **CLOSED 2026-08-10** (the universality quest, part 2: proof, not features — 9a–9h proofs + 9z gate; U-§97 = NO ×8, zero engine-line changes; U-§126 affirmative at proof scale) → U-§114–§120 §122

**Band contract.** Thin MOCK-dialect probes (U-§115 — `test.cad.extrude`-class, kept as architectural regression
fixtures) + the MANDATORY external-plugin proof (U-§116). Each proof applies the U-§97 failure criterion: *did this
domain require a second generic scheduler/compiler/runtime?* If yes — extend CEIR (a missing universal concept) or
formalize a provider (legitimate specialization) — DOCUMENT WHICH; never hide duplication. **Gate: the U-§122
seventeen-domain proof matrix + the U-§126 final-review answer, from evidence.**

| ✔ | Slice | Deliverable | → §§ |
|---|---|---|---|
| ✅ | CEIR-9a | ✅ **CLOSED 2026-08-09 (no ADR — a proof COMPOSES; no new serialized record → no fuzz).** **U1 Notebook/incremental — a notebook/reactive workload runs on the foundation with ZERO new machinery.** ⭐ Cells are REAL CEIR arith ops (const/addi/muli), deps = SSA operands, 8d stable ids = cell identity; each cell's 8h **content** revision = a FORMULA hash (kind + operand DEP stable-ids + attrs — reorder/id-INDEPENDENT), **interface** revision = a computed-VALUE hash → the §107 rule becomes the notebook **early-cutoff** (a formula edit that preserves the value hot-swaps its dependents). Every edit rides an **8i transaction** (touched-set seeds the eval; a no-op edit's unchanged content hash ⇒ zero recomputes — the content-addressed memo, the first real consumer of the 8i→8h seam). ⛔ **U-§97 verdict: NO second engine, DEMONSTRATED (3 parts):** (1) single-level cutoff IS the engine (`recompute_after_change` with unchanged interface returns no dependents); (2) the CHAINED driver is necessarily consumer-side because chaining needs re-evaluation + the engine holds NO results by the 8h division → it structurally cannot chain; (3) the direct-dependents map mirrors the engine's own dep edges. The generic machinery (topo/deps/revisions/§107) is ALL IncrementalDag; the consumer is ~100 lines of evaluate/compare/propagate glue. ⭐ The content=formula/interface=value mapping GENERALIZES 8h's cook reading (interface-as-content-projection) — the engine compares u64s, agnostic to the reading; that generality is itself U-§97 evidence. Near-misses (observations, not gaps): no per-op content-hash primitive (consumer computes from the public op surface); no direct-dependents accessor (derived). Multi-cell-per-transaction edits named-forward to 9d/9g. **Advisor-approved close (U-§97 honest).** **Tests:** `test_notebook.cpp` (7 `[ceir][notebook]`): initial-once; edit-input → exact value-dependents + the A*0 chained cutoff; engine-conservative-superset ⊇ evaluator-precise with F the named cut node; edit-different-input → precise partial set; value-preserving swap → single-level §107 cutoff; no-op tx edit → zero recomputes; identical-formula position-independence. Exact counts, not "≥". **Gate:** crd-ceir-tests + host + cook **327/327 ctest** (320 + 7) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** (the money config proving the notebook+engine memory-safe) + LLVM-20 tidy (test_notebook.cpp) + GCC `-Werror=switch` + opgen drift/validator + `crd-ceir-invariants`. **NO recook (a proof serializes nothing).** | U-§82 U-§119 |
| ✅ | CEIR-9b | ✅ **CLOSED 2026-08-09 (no ADR — a proof COMPOSES; no new serialized record → no fuzz).** **U2 DAW/timeline — a DAW/audio graph runs on the foundation with ZERO new machinery.** ⭐ The mock `audio` dialect is INLINE-registered (zero central edits): source/gain/mix (RT-safe), `delay` (carries `OpTrait::StateEdge`), `load_sample` (FileIO effect), `scratch_alloc` (Allocate effect), `graph` (region-owner) — each with EXPLICIT determinism+domain axes. Four foundation surfaces compose: **8f time** (`time.audio_sample<T>` distinct TypeId from wall + a plugin `tempo.beat` clock), **5d state** (feedback = the plugin delay's last operand; `find_structure_error` accepts it, rejects a combinational gain loop as FeedbackWithoutState with the offender pointed), **4c legality** (`find_domain_violation` flags the disk-load in an audio-RT region), **8e interfaces** (latency = a typed `LatencyInterface` query). ⛔ **U-§97 verdict: NO second engine (3 parts):** (1) the feedback discipline keys on the **TRAIT, never a kind name** — a plugin's delay is exactly as legal as core.delay (proven via the plugin-ONLY path); (2) offline-vs-realtime is a region-TAG property of ONE module object — flag→retag-offline→clean→retag-realtime→flag on the SAME pointer is the no-second-graph proof (U-§38); (3) latency is a typed interface with EMPTY≠UNKNOWN (missing ⇒ chain latency UNKNOWN, never silently 0). No new time/state/scheduler/legality model. The two-oracles subset pinned by THREE points (`realtime_safe ⟹ legal`, the allocating op the converse-witness). ⭐ **Multi-rate assessed (not waved):** distinct rates = distinct time type-classes (mixing is a type error — the wall≠sim property); a resample op is ordinary (operand/result in different domains); rate-match enforcement rides the same named-forward operand checker — NO new time model. Honest boundaries (not gaps): latency proven at op-KIND level (a graph-walking accumulator over first_op/next_in_block is trivial glue, not exercised); the delay fixture's init is a source result (a legality probe, not a numeric evaluator). **Advisor-approved close (U-§97 honest).** **Tests:** `test_daw.cpp` (5 `[ceir][daw]`): sample-time≠wall+plugin-beat; plugin-delay feedback legal vs combinational rejected (offender pointed); FileIO-in-audio-RT flagged + offline tag-flip legal + flip-back flagged; the two-oracles subset (3 points); latency interface summed + missing⇒unknown. **Gate:** crd-ceir-tests + host + cook **332/332 ctest** (327 + 5) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy (test_daw.cpp) + GCC `-Werror=switch` + opgen drift/validator + `crd-ceir-invariants`. **NO recook (a proof serializes nothing).** | U-§83 U-§38 U-§39 |
| ✅ | CEIR-9c | ✅ **CLOSED 2026-08-09 (no ADR — a proof COMPOSES; no new serialized record → no fuzz).** **U3 DCC modifier graph — a Blender-class modifier stack runs on the foundation with ZERO new machinery; the 8h `affected_by` IS the depsgraph.** ⭐ **The differentiator from 9a — TWO DRIVERS, ONE ENGINE, NO CODE MOTION:** 9a PUSHED forward from a seed (Salsa); 9c PULLS — `affected_by(M)` computes the invalidation set UPFRONT (Blender's TAG pass), then ONE topo walk evaluates only tagged nodes, early-outing any whose inputs all turned out unchanged. The engine changed by ZERO lines between the two proofs (a 9z synthesis input). Mock `dcc` dialect INLINE-registered (base_mesh/subdivide/transform/boolean/deform/output, explicit axes); a mesh = a mock poly count; a DAG with a JOIN over TWO SEPARATE-source branches. Edits ride 8i transactions. ⛔ **U-§97 verdict: NO second depsgraph (3 parts):** (1) the TAG pass IS the engine (`affected_by(edited)` = the invalidation set, asserted EXACT both ways on the subdivide+deform edits, excluding the sibling branch on the cutter edit — precise upstream isolation); (2) the eval walk is consumer POLICY because early-out needs re-evaluation + the engine holds no results (per 9a); (3) `deps[]` mirrors the engine's own edges. The §107 early-out proven by a topology-preserving deform edit (formula changes, output mesh does not → its tagged dependent `output` is the NAMED cut). ⭐ **Topology-vs-geometry facets assessed (not a gap):** a facet = an ordinary additional output node (the 9b resample shape); dependency granularity = which node you edge to; enforcement rides the named-forward operand checker. Modifier REORDER named-forward (the 8i structural-edit tx path). **Advisor-approved close (U-§97 honest).** **Tests:** `test_dcc.cpp` (5 `[ceir][dcc]`): initial-once; subdivide edit (tag==eval, exact both ways); cutter-branch edit (subdivide branch cache-hit — isolation); topology-preserving deform edit (tag⊃eval, `output` the named cut); no-op tx edit → zero recomputes. **Gate:** crd-ceir-tests + host + cook **337/337 ctest** (332 + 5) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy (test_dcc.cpp) + GCC `-Werror=switch` + opgen drift/validator + `crd-ceir-invariants`. **NO recook.** | U-§84 U-§37 |
| ✅ | CEIR-9d | ✅ **CLOSED 2026-08-09 (no ADR — a proof COMPOSES; no new serialized record → no fuzz).** **U4 CAD parametric — the 8i TRANSACTION is the headline; a parametric feature graph runs on the foundation with ZERO new machinery.** Mock `cad` dialect INLINE-registered (sketch/extrude/assembly/fillet/body); a two-feature assembly; a mock scalar geometry. ⛔ **The shared incremental helpers HOISTED to `tests/ceir/incremental_helpers.hpp` at this third consumer** (9a/9c refactored onto it, proven neutral at 337/337 before a line of CAD — the 9c forward-note DISCHARGED). ⛔ **U-§97 verdict: NO second transaction-manager / depsgraph / constraint-solver (3 parts):** (1) **multi-op atomicity IS 8i, unmodified** — one tx editing several dimensions returns a multi-id `touched()`; the eval seeds from the UNION of the affected subtrees (both branches re-eval where a single edit leaves the sibling cached) → **the 9a multi-cell-per-transaction named-forward is now CLOSED** (+ a no-op edit inside a multi-op tx recomputes only the real edit's subtree — multi-op UNION × content memo). (2) **a CAD constraint is TWO existing surfaces, zero new mechanism** — the 8c `ConstraintRead` effect is CLASSIFICATION, the sketch's `VerifyFn` (the tx's existing commit-verify) is ENFORCEMENT; a violating commit is REJECTED (diag `ceir.transaction.verify_failed` → rollback → byte-identical, dims restored), a satisfying one succeeds. (3) a bidirectional constraint SOLVER is an ordinary fixpoint NODE (the graph stays one-way; enforcement granularity rides the named-forward checker). ⭐ **The CAD argument for transactions:** the satisfying edit (4,4→6,6) transits a VIOLATING intermediate state (6,4 after the first set_attr) and commits anyway because verification is ATOMIC at commit — a constrained model can't be edited dimension-by-dimension without inconsistent states, so enforcement MUST be transactional; 8i gives that for free. **Advisor-approved close (U-§97 honest).** **Tests:** `test_cad.cpp` (5 `[ceir][cad]`): single-dim edit (partial re-eval, cite 9c); MULTI-OP (both branches re-eval; a fresh-fixture control shows the sibling cached); no-op-inside-multi-op (only the real subtree); byte-identical rollback (serialize A/B + dims read back); constraint reject (verify-fail → rollback + the diag) then satisfying commit. **Gate:** crd-ceir-tests + host + cook **342/342 ctest** (337 + 5) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy (incremental_helpers.hpp + test_cad.cpp + the refactored 9a/9c) + GCC `-Werror=switch` + opgen drift/validator + `crd-ceir-invariants`. **NO recook.** | U-§85 U-§31 U-§120 |
| ✅ | CEIR-9e | ✅ **CLOSED 2026-08-09 (no ADR — a proof COMPOSES; no new serialized record → no fuzz).** **U5 PCB/EDA + external provider — a board pipeline runs on the foundation with ZERO new machinery; external tools are ordinary capability-bearing ops.** Mock `eda` dialect INLINE-registered (board_document/drc/route/gerber_export). ⛔ **U-§97 verdict: NO second provider/capability system (3 parts):** (1) **an external tool IS an ordinary capability-bearing op** — 8f caps + 4a effects + 4b determinism + a content-addressed `tool` attr = the complete TYPED CONTRACT, queried generically; `capabilities_satisfied` is the sandbox boundary (a GPU-only host PROVABLY can't run the pipeline — it grants gpu.compute, the pipeline needs external.process+file.write); the runtime DISPATCH is the IExecutionProvider seam, named-forward CEIR-24/29 (contract proven, execution not claimed); the tool identity is content-addressed (two boards differing only in the router `tool` attr hash differently). (2) ⛔ **the op-LOCAL commit-verify boundary (the finding this slice earns):** 8i's commit-verify runs on TOUCHED ops only, so a rule fires iff its carrier op is edited — a rule ON the board catches board edits (the DRC), but a whole-board DRC SWEEP needs a `find_*_violation` walk / a pass over the committed module (consumer-band, named-forward to 9g's post-commit verifier sweep + CEIR-26). Op-local is the CORRECT commit-time granularity; module-wide COMPOSES on top — a boundary, not a gap. (3) ⭐ **U-§34 DEMONSTRATED:** a program-level walk over every op finds Document/Constraint/ExternalCall/FileIO families but NO GPUCommand effect, NO DeviceTime domain, NO gpu.compute cap — **the vocabulary was domain-neutral before this domain arrived** (a 9z synthesis input, paired with 9c's "two drivers, one engine"). Gaps assessed (all expressible): typed error = an 8g Diagnostic from the dispatch; streaming = an op/token; bidirectional = Document r/w. **Advisor-approved close (U-§97 honest).** **Tests:** `test_eda.cpp` (4 `[ceir][eda]`): host-grant contract (GPU-only host can't run it + EMPTY≠UNKNOWN); the queryable typed contract + content-addressed provenance; a DRC violation rejected by the board verifier at commit; the U-§34 program-level domain-neutral walk. **Gate:** crd-ceir-tests + host + cook **346/346 ctest** (342 + 4) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy (test_eda.cpp) + GCC `-Werror=switch` + opgen drift/validator + `crd-ceir-invariants`. **NO recook. ⭐ FIVE domains in (9a–9e), the U-§97 answer has been NO every time — zero foundation edits, zero engine-line changes.** | U-§87 U-§35 U-§58 |
| ✅ | CEIR-9f | ✅ **CLOSED 2026-08-09 (no ADR — a proof COMPOSES; no new serialized record → no fuzz).** **U6 Game/ECS effects — ECS system parallelism runs on the foundation with ZERO new machinery; the 4d hazard analysis over 4a EcsRead/EcsWrite effects IS the parallelism oracle.** Mock `ecs` dialect INLINE-registered (movement/render/physics/input/spawn/coarse). ⛔ **U-§97 verdict: NO second scheduler/parallelism-analysis (3 parts):** (1) **ECS parallelism inference IS the 4d hazard analysis** (`ops_hazard`/`collect_block_hazards` UNMODIFIED; no scheduler built — the scheduler consumes these edges at CEIR-12d, named-forward): disjoint components → parallel, shared+write → ordered (exact RAW/WAR/WAW); the compiler derives the FULL 6-edge set from declared effects alone, the 4 disjoint pairs edgeless. (2) **per-component identity IS the SSA Value** (a distinct block-arg per component, effect targets an OPERAND — the canonical 4d resource model); the 8c `EcsComponent` location KIND was exercised ONLY as the conservative whole-class fallback (a `coarse` system conflicts with EVERY Ecs access, even mutually-parallel ones — safe-but-pessimal, EMPTY≠UNKNOWN for location identity; precision is opt-in via the operand). (3) a structural mutation needs NO special mechanism — a whole-store write (null resource) is the barrier by the existing nullptr conflict rule. ⛔ **The one foundation change any proof has forced: a STALE forward-claim corrected** — the 4d source comment said the whole-class fallback was "named-forward to 8d", but 8d shipped per-OP identity, NOT per-LOCATION identity (the 8z scar); fixed in place to the honest unbound form (a comment-only crd-ceir edit, re-gated across 4 configs). **Advisor-approved close (the WAR-direction fixture error was caught by the exact-kind discipline — the failure was the discipline working).** **Tests:** `test_ecs.cpp` (4 `[ceir][ecs]`): disjoint=parallel + shared-write=ordered (exact kinds); the whole-store structural barrier; the full 6-edge `collect_block_hazards` set + 4 disjoint pairs absent; the `EcsComponent` conservative whole-class fallback pinned. **Gate:** crd-ceir-tests + host + cook **350/350 ctest** (346 + 4) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** (all 3 targets rebuilt for the context.cpp comment fix) + LLVM-20 tidy (test_ecs.cpp + context.cpp) + GCC `-Werror=switch` + opgen drift/validator + `crd-ceir-invariants`. **NO recook. ⭐ SIX domains in (9a–9f), U-§97 = NO every time — zero engine-line changes; the ONLY foundation change any proof forced is correcting one stale comment.** | U-§88 U-§41 |
| ✅ | CEIR-9g | ✅ **CLOSED 2026-08-09 (no ADR — a proof COMPOSES; no new serialized record → no fuzz).** **U7 Agent transaction — the CULMINATION of band 9: the FULL agent loop runs on the foundation with ZERO new machinery.** A mock agent DISCOVERS ops via the machine-readable `OpSchema` registry (the header's own "CLI/MCP/agent discovery" surface; `.ops.json` is its out-of-process serialization, and NO no-std JSON reader exists → the in-process `<dialect>_op_schemas()` tables ARE the registry), ⛔ **ZERO hard-coded op knowledge** (SELECTS by property [arity+result count], interns via the discovered `schema.dialect`/`name`; `op_name(kind)==schema.qualified` closes the loop). ⛔ **U-§97 verdict: NO second authoring/verify/diagnostic/discovery system (3 parts):** (1) the agent loop is a THIN composition over four existing surfaces — OpSchema discovery + 8i authoring (unmodified) + verification at TWO granularities + 8g diagnostics (read by CODE). (2) the three-legged authoring contract, LAYERED HONESTLY: schema (arity+required attrs+kinds) + a chosen type + STRUCTURAL verify DEMONSTRATED (test 3: a lazy agent authoring a source without its required attr is caught by the op-local commit-verify backstop); SEMANTIC type validity is a further BOUND named-forward (the generated verifiers are structural, as their source stamps "Semantic verification lands at CEIR-3/4"; the type-constraint language is CHIR) — ⛔ faking a type verifier to fit the design-call assumption would have been the failure; the proof adapted on primary evidence. (3) ⭐ **the op-local/module-wide composition DEMONSTRATED, closing the 9e named-forward:** the agent runs a MODULE-WIDE `find_structure_error` SWEEP MID-TX (8i's eager application makes the authored state visible pre-commit) → catches a defect NO op-local commit-verify can see (a same-block forward reference → `FeedbackWithoutState`, exact kind pinned) → emits its OWN 8g diagnostic (`agent.sweep.structure_defect`), reads it by code, rolls back byte-identically; the op-local commit-verify is the BACKSTOP behind the sweep (belt-and-suspenders). **Advisor-approved close (the structural-vs-semantic layering surfaced honestly; the exact StructureError kind pinned).** **Tests:** `test_agent.cpp` (3 `[ceir][agent]`): discover-by-property + author + modify + mid-tx sweep clean + commit (op_name==discovered); the module-wide-sweep defect → agent diagnostic → byte-identical rollback; the op-local commit-verify backstop catches a lazy agent (verify_failed → rollback). **Gate:** crd-ceir-tests + host + cook **353/353 ctest** (350 + 3) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy (test_agent.cpp) + GCC `-Werror=switch` + opgen drift/validator + `crd-ceir-invariants`. **NO recook. ⭐ SEVEN domains in (9a–9g), U-§97 = NO every time — zero engine-line changes; the ONLY foundation change any proof forced is one stale-comment correction (9f).** | U-§94 U-§50–53 U-§117 |
| ✅ | CEIR-9h | ✅ **CLOSED 2026-08-09 (no ADR — a proof COMPOSES; a foreign-plugin blob DOES serialize → fuzz-swept).** **U8 the MANDATORY external-plugin proof — the ACID TEST of the open-world thesis: a whole `plugin` dialect authored in a test-side namespace (`plugin_ext`, public headers only, ZERO engine source edits) registers its FULL surface through PUBLIC open-world APIs.** ⛔ **U-§97/U-§116 verdict: ZERO core edits, NO private wall (3 parts):** (1) **the acid test passed + the claim is MECHANICAL** — NINE surfaces through public doors (custom TYPE class 8a + ATTR class 8b + effect-LOCATION class 8c + op INTERFACE 8e + OPS with verifiers + a REWRITE + a lowering ConversionTarget + an IExecutionProvider); the "zero central-enum edits" is a STANDING GATE in `crd-ceir-invariants` (⛔ **U-§116: TypeKind/AttrKind end at `Extern` via a new script pin; EffectFamily via its existing kLastEffectFamily compile-time static_assert — two mechanisms, one gate**, passing both platforms); the in-test assertions confirm the Extern doors. A future slice widening a central enum for a plugin trips it FOREVER. (2) **the 8g rewrite framework carried its FIRST real pattern** — `A(B(x))→C(x)` fired via `try_apply` (a genuine IR mutation: create-C-wired-to-x → RAUW A's result → erase A then B-if-dead), authored ENTIRELY plugin-side, with the worklist driver still cleanly RESERVED (evidence 8g's skeleton boundary was drawn right); a non-match returns false. (3) **two honest ASYMMETRIES named (neither a wall):** a plugin HAND-AUTHORS its OpSchema table (a first-party dialect gets it generated — a tooling asymmetry, unbound named-forward); execution is a CONTRACT stub (advertises real, execute=NoSemantics — dispatch → 24/29). The plugin's custom type/attr round-trip + preserve through an unregistered host + unify (U-§56 for a foreign plugin) + a single-byte fuzz sweep ASan-clean; DISCOVERABLE by the 9g agent path over its hand-built schema table (U-§117). **Advisor-approved close (the acid test passed on the real question).** **Tests:** `test_plugin.cpp` (5 `[ceir][plugin]`): the full surface via public doors + Extern assertions; foreign U-§56 round-trip/preserve/unify + fuzz; the rewrite fires + non-match; the agent discovers+authors a plugin op; the provider advertises plugin ops + declines foreign. **Gate:** crd-ceir-tests + host + cook **358/358 ctest** (353 + 5) on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + LLVM-20 tidy (test_plugin.cpp) + GCC `-Werror=switch` + opgen drift/validator + `crd-ceir-invariants` (EXTENDED with the U-§116 enum-pin) + the plugin-blob fuzz sweep. ⛔ **ZERO engine source edited** (only tests/ + the invariants scripts). **NO recook. ⭐ EIGHT domains in (9a–9h), U-§97 = NO every time — zero engine-line changes; the only foundation changes any proof forced are one stale-comment correction (9f) + one standing gate ADDED (the U-§116 pin).** | U-§116 U-§117 U-§118 |
| ✅ | CEIR-9z | ✅ **CLOSED 2026-08-10 — BAND 9 CLOSED. DOC-SYNTHESIS gate (no ADR, no gate test, no recook — DECISIONS, not omissions: unlike the 3z/4z/5z/8z composition gates whose guarantees lived in separate tests that never met, band-9's eight slices are each ALREADY a composition, so the 358-suite × 4 configs IS the gate's executable half — a mega-test would be the band's first redundant artifact).** ⭐ **The U-§122 seventeen-domain matrix filled from evidence** (`docs/research/…-universality-review.md` §I): **7 PROVEN rows** (9a–9g, a test consumer drives each) + **2 ◧ foundation-validated** (Build pipeline, Reactive UI — every mechanism proven, no domain consumer driven) + **4 ◑ analogous-proven** (MATLAB, CAM, Physics/CAE, Media — the characteristic shape proven by an analogous 9x slice) + **4 ○ future** (Renderer-15, Compute-13, ML-24, Distributed-30 — space validated, domain unproven) = 17; a **9th proof** (9h foreign plugin) validates the open-world DOORS, not a matrix row → **8 proof slices in all**. ⛔ **RE-VERIFIED every mid-band §I row against the code (the 8z discipline)** — caught + fixed two stale "Agent introspection" rows (§B + DoD-23) still citing 9g-as-forward after 9g shipped. ⭐ **The U-§97 tally recorded in one place** (the review doc): 8 slices, U-§97 = **NO ×8**, **zero engine-line changes** across the band (the only deltas: one stale-comment fix at 9f + one standing gate ADDED, the U-§116 enum-pin — gate infra, not engine). ⭐ **The U-§126 final answer, both halves affirmative, bounded honestly to PROOF scale:** CEIR is a universal execution substrate — validated (not merely designed) across 7 mock-domains + 1 foreign plugin, each a COMPOSITION of shipped foundation slices; production-scale validation accrues as consumer bands 10→35 build the real domains, each re-asking U-§97 against reality. **Advisor-approved close (caught + fixed the legend/U-§126 count errors: 17 rows = 7+2+4+4, not 8+2+4+5).** **Gate (doc-only ⇒ no rebuild, binaries current from 9h):** **358/358 ctest** on **win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** + opgen drift/validator clean + `crd-ceir-invariants` (incl. the U-§116 pin) + GCC `-Werror=switch`. NO tidy (docs-only, zero source touched), NO fuzz (no new serialized record). | U-§122 U-§126 |

## CEIR-10 — Asset lifecycle completion (band-7 remainder, re-homed) — ✅ **BAND 10 CLOSED 2026-08-10** (10a ✅ / ADR-0120 · 10b ✅ / ADR-0121 · 10z ✅ gate) → §108 §109 §110 §172

**Band contract.** The paused 7c/7d/7z contracts VERBATIM, now consuming their foundation inputs: state migration
keys on **8d stable state identity** (never instance order); the plan cache is an **8h incremental-cache client**;
~~reload transactions ride **8i**~~ → **CORRECTED at 10a (ADR-0120 §2.2):** install atomicity is the 7b `ProgramSlot`
(SET-level all-or-nothing), NOT 8i; 8i is the UPSTREAM authoring seam (an agent edit → 8i commit → content-hash change →
the reload's detect trigger). Reuse pinned at 7b: `ProgramSlot` generations + the interface-hash swap-compat
discriminator (registry drift IS the incompatible case) + the per-generation-Context wholesale-free pattern + the
RAF-11 reentrant-guard scar verbatim.

⭐ **10a DESIGN LOCKED (2026-08-10, 2 advisor consults) → `docs/design/ceir-10a-hot-reload-and-state-migration.md`**
(the ADR-0120 substrate). Key resolutions: migration is a structural DECISION + a live-value MOVE (the 5z
`exec::Interpreter` cells, snapshot/restore-by-8d-stable-id); atomic install is the 7b **SLOT** (SET-level
all-or-nothing), NOT 8i (8i is the upstream authoring seam — a documented in-place correction of "rides 8i");
"compile-affected" = the 8h affected-SET only (compilation named-forward to 11). ⛔ **Cooked format UNTOUCHED — no
schema bump / recook / fuzz** (the host-subset gate compares live-vs-new interface hashes in memory; the expected-callee
`KernelRef` record has no consumer this slice → named-forward to 10b/CKIR-13). Detect front-end **reuses**
`ResourceManager` hot-reload (mtime/debounce/callback), NOT a new file-watch. New: a `contract_hash` (§107 minus the
state schema) so Reject (signature change) is distinguished from Migrate (state-schema-only change).

**10a implementation staged (one row, one DoD — no fragmentation): (1) core exposure · (2) supervisor + set-install ·
(3) migration wiring · (4) detect front-end.** ✅ **Stage 1 DONE + gated (2026-08-10):** the crd-ceir CORE surfaces —
`collect_state_schema` (module-wide `(stable_id, type, depth)` cells) + `contract_hash` (the §107 projection MINUS the
state schema — the Reject-vs-Migrate split) refactored out of `interface_hash` **byte-identically** (the §107 matrix is
the regression net — no recook) + the Interpreter `snapshot_state_by_id`/`restore_state_by_id` cell-value move by 8d
stable id (absent id / depth mismatch → skip → init-fill). 3 stale in-place comment fixes shipped (`program_asset.hpp`
BODY-order→8d + caps-folded-in; `program_asset.hpp` KernelRef CEIR-10→**13**; `runtime_program.hpp` exec-form
CEIR-8→**11**). `test_reload_migration.cpp` (6 `[ceir][reload]`: schema extraction; the 3-way decision table
body/signature/state-only; value migration; absent+depth-mismatch skip). Gate: **364/364 × 4 configs** (win-debug ·
win-asan · linux-gcc-debug · linux-gcc-asan) + LLVM-20 tidy (5 files) + GCC `-Werror=switch` + opgen drift/validator +
`crd-ceir-invariants` (U-§116 pin holds — zero central-enum edits). **NO recook, NO fuzz** (cooked format untouched).
✅ **Stage 2 API-LOCKED (2026-08-10, 3rd advisor consult) → design note §8** (`ReloadSet` in `ceir-cook/hot_reload.{hpp,cpp}`):
heap-`Generation{Context*, RuntimeProgram}` units (the push-back-UAF scar → RuntimeProgram never by-value in a growable
array) + per-Entry ProgramSlot; **the 8h dag keyed `set_revision(asset, content_hash, contract_hash)` IS the validate-set**
(⭐ the advisor's correction: dependent-safety keys on `contract_hash` NOT `interface_hash` — a state-schema-only edit must
NOT reject dependents; `affected_by` = "recompiles-affected" unmodified); a 3-way `ReloadDecision`
{NoChange/HotSwap/NeedsMigration/ContractChange}; **Reject-always on a contract change** (EMPTY≠UNKNOWN — the host is an
unseen caller; leaf-allow named-forward to CEIR-11); reject destroys the candidate Generation (last-good = not installing,
no stored blob); dep graph rebuilt WHOLESALE per membership change (arena-string lifetime); dup-symbol + AssetId-0 loud
reject. ✅ **Stage 2 DONE + gated (2026-08-10):** `ReloadSet` in `ceir-cook/hot_reload.{hpp,cpp}` — heap-`Generation`
units (placement-new Context, reject-destroys-candidate, one-deep zombie drain — ASan-clean); `add`/`reload`/`remove`/
`drain` + `handle`/`is_current`/`program`/`generation`/`affected`; the 8h dag `set_revision(content, contract)` IS the
validate-set (`contract_hash`-keyed dependent-safety); the 3-way `ReloadDecision` (HotSwap installs, the rest keep
last-good); a `Registrar` callback for per-generation dialect registration; dup-symbol + AssetId-0 loud reject.
`test_hot_reload.cpp` (6 `[ceir][reload]`). Gate: **370/370 × 4 configs** + LLVM-20 tidy (3 files) + GCC `-Werror=switch`
(new enums) + opgen + `crd-ceir-invariants` (U-§116 holds — bridge-only, crd-ceir core untouched). **NO recook, NO fuzz**.
✅ **Stage 3 DONE + gated (2026-08-10, advisor-confirmed fork B):** state migration is CALLER-DRIVEN — the ReloadSet
does NOT own interpreters (live §20 cells are populated by EXECUTION, which it never does). A `MigrationFn` (fn-ptr
mutating a `StateSnapshot` array) registered per-asset (`register_migration`, last-wins); its PRESENCE flips a
NeedsMigration reload from Reject → install (the fn runs caller-side, never fake-invoked on an empty snapshot); a free
`migrate_state(old_interp, new_interp, new_module, fn, user)` = snapshot → fn → restore (fn==nullptr = verbatim HotSwap
path). Refusal governs ONLY the cell move (init-fill, no install-rollback). +3 `[ceir][reload]` tests: **the DoD — a real
value survives a depth-1→depth-2 reshape via a mock runtime** (old interp warm=7 → reload installs → migrate_state
expands [7]→[7,7] → new interp continues from 7, control re-inits to 0); refusal→init-fill; HotSwap-verbatim. Gate:
**373/373 × 4 configs** + tidy (3 files) + `-Werror=switch` + opgen + `crd-ceir-invariants` (U-§116 holds — bridge-only).
NO recook, NO fuzz. ✅ **Stage 4 DONE + gated (2026-08-10) — the SOURCE-in detect seam + the RAF-11 reentrant guard
(advisor-corrected scope: not a wrapper watcher, but the honest cook step + bad-edit path stages 1–3 never exercised).**
`add_source`/`reload_source` = a transient cook Context (`cook_program_text`, per-call, §121 text≡builder) → delegate to
`add`/`reload`; a cook failure is a DISTINCT typed outcome (`CookFailed`/`cook_error`) that installs+destroys NOTHING →
**last-good keeps running** (the most common real hot-reload event, finally tested). ⛔ **The RAF-11 reentrant guard**
(`m_reloading` + RAII `GuardScope`): a mutation from inside a registrar/migration fn is a loud typed reject
(`AddError::Reentrant` / `ReloadResult::reentrant`) — proven by a registrar that smuggles the set and re-enters `reload`.
This closes the twice-deferred **7a CookDb/register_cook_handler routing** in its host-subset form; the mtime/filesystem
SIGNAL is production I/O (named-forward — CEIR owns the swap, never the ResourceManager: two-swap-authority). +4
`[ceir][reload]` tests (source hot-swap; **bad source → CookFailed → last-good**; source signature → ContractChange;
reentrant reject). Gate: **377/377 × 4 configs** + tidy (3) + `-Werror=switch` (widened `AddError`) + opgen +
`crd-ceir-invariants` (U-§116 holds — bridge-only). NO recook, NO fuzz. ⭐ **ALL 4 STAGES BUILT — the full 10a lifecycle
works.** ◀ **NEXT = 10a CLOSE** (ADR-0120 describing the settled 4-stage architecture + fork B + the named-forwards; §7
gate-plan reconcile; fresh pre-close advisor; tracker row → ✅).

| ✔ | Slice | Deliverable | → §§ |
|---|---|---|---|
| ✅ | CEIR-10a | ✅ **CLOSED 2026-08-10 (ADR-0120).** (was 7c) The full hot-reload lifecycle on a `ReloadSet` (crd-ceir-cook bridge; crd-ceir core touched ONLY in stage 1): source-in → verify+cook → compile-affected (8h) → validate-set → atomic install-or-reject → generation bump → deferred destroy; last-good on EVERY failure; ⛔ no mixed-generation call graph; state migration + the 7a CookDb/`register_cook_handler` routing (host-subset form) — CLOSED. **Shipped as 4 gated increments (364→370→373→377):** (1) crd-ceir CORE surfaces — `collect_state_schema` + `contract_hash` (the §107 projection MINUS the state schema — the Reject-vs-Migrate split) refactored out of `interface_hash` **byte-identically** (no recook; the §107 matrix is the net) + Interpreter `snapshot_state_by_id`/`restore_state_by_id`; (2) the supervisor — heap-`Generation{Context*,RuntimeProgram}` units (push-back-UAF scar), ⭐ **the 8h dag `set_revision(content, contract_hash)` IS the validate-set** (dependent-safety keys on `contract_hash` NOT `interface_hash` — a state-schema-only edit must not reject dependents; `affected_by` = recompiles-affected unmodified), the 3-way `ReloadDecision`, Reject-always on a contract change (EMPTY≠UNKNOWN — the host is an unseen caller); (3) ⭐ **CALLER-DRIVEN migration (fork B)** — the ReloadSet does NOT own interpreters (live §20 cells are populated by execution); a `MigrationFn` registered per-asset, its PRESENCE flips NeedsMigration → install, a free `migrate_state(old_in, new_in, new_module, fn)` = snapshot→fn→restore (nullptr = verbatim HotSwap); (4) the SOURCE-in seam (`add_source`/`reload_source`, transient cook Context, `CookFailed` typed outcome → **last-good keeps running** on a bad edit) + the ⛔ RAF-11 reentrant guard. **NAMED-FORWARDS (validated non-blocking):** mtime/filesystem detect signal → production (CEIR owns the swap, never the ResourceManager — two-swap-authority); `CookedHeader.id` validation → future (read path doesn't surface it); leaf-allow on a contract change → CEIR-11; expected-callee `KernelRef` record → 10b/CKIR-13; semantic call-site compat → 3/4. **Advisor-reviewed close (caught 2 ADR misstatements: the CEIR-8 cadence-clause overreach + a test miscount — fixed).** Tests: `test_reload_migration.cpp` (6 `[ceir][reload]`, stage 1) + `test_hot_reload.cpp` (13 `[ceir][reload]`, stages 2–4) = **19 `ceir 10a` cases**. **Gate: 377/377 ctest × 4 configs** (win-debug + win-asan + linux-gcc-debug + linux-gcc-asan) + LLVM-20 tidy + GCC `-Werror=switch` + opgen drift/validator + `crd-ceir-invariants` (U-§116 holds). **NO recook, NO fuzz** (cooked format untouched). | §108 §109 |
| ✅ | CEIR-10b | ✅ **CLOSED 2026-08-10 (ADR-0121).** (was 7d) Execution-plan cache — a `PlanCache` in `ceir-cook/plan_cache.{hpp,cpp}` (bridge-only). ⭐ **A STORE + VALIDATE-ON-HIT client of live truth (advisor fork), NOT a compile pipeline:** the caller compiles an opaque artifact + `put()`s it; `get()` returns hit/miss/stale-deps but ONLY after validating each recorded dep's CURRENT `interface_hash` via a **resolver** fn-ptr → the cache never trusts itself ("caches never truth" made mechanical). ⛔ No compile callback (10b never names the compile→plan interface — deferred to 21/26); deps key on **`interface_hash`** NOT contract (cache-validity wants conservatism — a stale plan is corruption; a spurious recompile is cheap); no second dag (a client of the ReloadSet's 8h dag via the resolver; 0 ⇒ STALE, EMPTY≠UNKNOWN); **in-memory only** (persistence → 11/13 — refines the 10a KernelRef named-forward); bridge-local `PlanDep{AssetId, u64 interface_hash}` (≡ ADR-0109 §85 KernelRef, NOT a crd-ceir mint — I5). Key = {content_hash, target=FNV(provider name), compiler_version}; hit/miss counters first-class (10z asserts them). Tests: `test_plan_cache.cpp` (3 `[ceir][plancache]`: put/get/miss + version + two-targets; §107-extended [callee body-edit→HIT, interface change→StaleDeps, removed→StaleDeps]; clear/evict never-truth) + `test_hot_reload.cpp` +1 (⭐ **the 10z REHEARSAL** — a callee interface change stales exactly `affected(B) ∪ {B}`, C untouched). Gate: **381/381 × 4 configs** + LLVM-20 tidy + opgen + `crd-ceir-invariants` (U-§116 holds). NO recook, NO fuzz (in-memory). ⛔ **The 10a "KernelRef → 10b PERSISTED plan cache" named-forward is REFINED (struck in place, ADR-0120 §4 + 10a note):** the IN-MEMORY record (`PlanDep`) landed at 10b; the SERIALIZED/persistent form → CEIR-11+/13 (the real plan payload doesn't exist, so the format is not designed blind). No `plan_status_name` (deliberate — a programmatic result, not a diagnostic surface). **Advisor-reviewed close (ADR-0121 clean; caught 4 design-note doc-vs-reality drifts — reconciled).** ◀ **NEXT = CEIR-10z** (the §172 band-close matrix — a COMPOSING test with hit-count-delta assertions across the 3 edit classes; inherits the pairing rehearsal), which CLOSES BAND 10. | §110 §104 |
| ✅ | CEIR-10z | ✅ **CLOSED 2026-08-10 → ⭐⭐ BAND 10 CLOSED (10a..10z).** (was 7z) The BAND-10 GATE — the §172 matrix, host subset — a **COMPOSING test** (not doc-synthesis like 9z: band 10's lifecycle + cache guarantees only MEET here, checked in lockstep against a resolver backed by the LIVE ReloadSet). `test_band10_gate.cpp` (1 `[ceir][gate10]`) walks ONE program graph (A imports B; C independent) through the three §172 edit classes: **(1) body-edit B → HotSwap installs live AND caller A's plan stays a HIT** (§107 at the plan layer, proven LIVE — B's own plan self-misses by new content); **(2) signature-edit B → ContractChange REJECT + last-good AND A's plan still HIT** (a reject invalidates nothing); **(3) dep-edit (cold-reload B, new signature) → B's interface changes → A's plan StaleDeps, B self-misses = the recompile set `affected(B) ∪ {B}`, C untouched**. ⛔ **EXACT hit/miss counter deltas asserted at every step** (the 9a not-≥ discipline: 3/0 → 6/1 → 8/1 → 9/3) — the §172 "cache-hit counts". ⛔ Re-verified the 10a/10b public surfaces against code (the 8z discipline); the gate IS the live re-verification. **Advisor pre-close caught 2 (the unasserted ∪{B} half + weak `>`/`≥` counters → exact) — fixed + 4-config re-run.** **Gate: 382/382 ctest × 4 configs** (win-debug + win-asan + linux-gcc-debug + linux-gcc-asan) + LLVM-20 tidy + GCC `-Werror=switch` + opgen + `crd-ceir-invariants` (U-§116 holds). **NO recook, NO fuzz, NO ADR** (a gate composes — the 3z/4z/5z/8z/9z precedent). | §172 |

## CEIR-11 — Reference executor + compiled host plan — ✅ **CLOSED 2026-08-10** (11a ✅ / ADR-0122 · 11b ✅ / ADR-0123 · 11c ✅ crd-perf seam+adapter · 11z ✅ §153 audit) — the §84 two-tier execution model COMPLETE: the §118 reference oracle + the compiled tier differential-verified byte-for-byte against it. ⭐ the FIRST fully-closed band of the 2026-08-09 re-baselined spine. → §83 §84 §118 §153

**Band contract.** Two execution tiers (§84). Reference tier: the CEIR-5 executor grown to the full host subset,
maximal diagnostics (the §118 correctness oracle). Compiled tier: `CompiledExecutionPlan` = dense arrays of
pre-resolved op thunks + slot-indexed values — **§153 audited: zero strings, zero per-op heap, zero map lookups,
zero source parsing in the loop** (CountingAllocator-gated, the v14-m allocation-free-infer precedent). Profiler:
every plan op wrapped in `CRD_PERF_SCOPE`-compatible regions (crd-perf, no new profiler). **Gate: no source
parsing / string lookup in the shipping execution loop.**

⭐ **11a DESIGN LOCKED (2026-08-10, advisor consult) → `docs/design/ceir-11a-reference-executor.md`** (the ADR-0122
substrate). Audit (coverage matrix): the executor covers arith(4)/core(10)/func(3)/async-sequential(6); 11a adds the six
§38 residual ops (spawn/continuation/main-thread/worker/fiber-wait/task-group — DON'T EXIST, need TOML+opgen+semantics),
jobs-backed launch/await ON-POOL (today sequential), §112 step hooks (fn-ptr seam, null-default). ⭐ **Value-model risk
CLOSED:** arith is integer-only → the i64 Interpreter covers the full subset (no reshaping fork). Key resolutions:
sequential reference for EVERYTHING lives in **crd-ceir core** (the §118 oracle), jobs-backed in **crd-ceir-host** (rebind
via last-install-wins). Ops-first is MECHANICAL (name-interned → NO format motion/fuzz/recook) — but the six ops'
SIGNATURES are a genuine design sub-fork (§38 is a vocabulary list) → **advisor consult at the stage-2 design point before
authoring TOML**. Internal stages: (1) audit+note ✅ · (2a) the six ops ✅ · (2b-i) §112 hooks + stale
fixes ✅ · (2b-ii-A) pre-flight move to core ✅ · (2b-ii-B) parallel_for/map_reduce sequential reference ✅ · (3)
jobs-backed ON-POOL async ✅ · (4) the 11a DoD gate.

✅ **Stage 3 DONE + gated (2026-08-10) — jobs-backed launch/await ON-POOL via CONDITIONAL POOLING.** ⛔ The crux
(advisor): `async.launch` bodies run IN-FRAME in the reference (captures + state legally work, UNLIKE parallel_for), so
stage 3 must never reject what stage 2 ran. A bridge-local **pool-eligibility classifier** (`exec::region_state_free`
[the exported StateEdge+calls walk] + a NEW no-outer-captures walk) → **pool** (a heap-owned `PooledToken{own scratch,
result, counter}` per launch; the worker runs a fresh sub, fills the result BEFORE the counter decrements = the
happens-before edge; the growable table is heap-owned so it never moves an entry the JobDecl captured by pointer — the
push-back-UAF scar) or **sequential in-frame fallback** (`launch_seq_inframe`); byte-parity by CONSTRUCTION in both
branches. ⛔ **Pooled handles live in a DISJOINT space (`kPoolBase=1<<40`)** so a pooled token never truncates in the
`u32(tok)` cast + aliases a sequential handle; **ALL FIVE consumers** (await/join/race/cancel/fiber_wait) route through
one `resolve_yields`/`token_valid`. spawn/worker → pooled (any thread); main_thread → `pin_thread=0`; group/scope → no
override. `execute()` **drains** the pooled table at exit (leak containment). ⭐ **`pooled_count()` witness (CUMULATIVE —
survives the drain):** asserted ==N pooled / ==0 fallback. Verifies: intern_op read-only on hit; wait pumps thread 0.
`test_host_provider.cpp` +6 (`[ceir][host]` — **T1** N pure launches byte-match the sequential reference + witness==2;
**T2** capturing → fallback, witness==0; **T3** stateful → fallback, witness==0; **T4** main_thread-pinned pooled + awaited
from main completes [the pump path, no deadlock]; **T5** join/race/cancel resolve pooled tokens; **T6** a leaked pooled
token drained at exit [ASan-clean]). Gate: **396/396 × 4 configs** (win-asan + linux-gcc-asan the money configs — the
pooled workers under real threads + the drain, ASan-clean) + tidy (5 files) + GCC `-Werror=switch` + `crd-ceir-invariants`
(crd-ceir core STILL jobs-free — `region_state_free` names no jobs type; the pool is all bridge-side). ⛔ **NO format
motion / fuzz / recook.** ◀ **NEXT = stage 4 (the 11a DoD gate) + ADR-0122** (verify the number then).

✅ **Stage 2b-ii-B DONE + gated (2026-08-10) — the SEQUENTIAL reference for task.parallel_for / task.map_reduce (the
§118 oracle); the "full host subset" reference is COMPLETE.** New file-local `eval_parallel_for_seq`/`eval_map_reduce_seq`
(installed by `install_task_semantics`; the jobs-backed provider OVERRIDES via last-install-wins). ⛔ **The 6z fold
pattern (advisor landmine):** the body runs on ONE fresh SUB-interpreter cloned from `in` (per-index `invoke_region` on
the sub) — NOT `run_region` in `in`'s frame (captures would wrongly resolve) NOR `invoke_region` on the live `in`; runs
the map SEQUENTIALLY in index order → the SAME output as the provider's parallel run. New core surface: an
Interpreter-owned **`map_output` store** (`store_map_output`/`map_output(op)` — the m_yield_store/m_cells family, named
verbatim to mirror `HostProvider::map_output`), `invoke()`/`invoke_region()` capture **`const Module*`** + a `module()`
accessor, a `fuel()` accessor. The provider proto gains `install_task_semantics` (a task.spawn through HostProvider is no
longer NoSemantics). ⛔ **NO execution sharing** — the provider + reference share only the pre-flight ANALYSIS, compute
results INDEPENDENTLY (the bit-exact-blind scar). ⭐ **The first oracle-vs-provider AGREEMENT (the 11b differential
harness in miniature):** `test_host_provider.cpp` +3 (`[ceir][host]`): a non-associative map_reduce (map iv*iv, combine
acc*31+elem) — core-sequential vs HostProvider across **{1..16}** byte-identical (`pin_values`) + `map_output` span-equal;
a stateful body → `ParallelBodyStateful` from the sequential reference (shared pre-flight); an outer-capture body →
`UndefinedValue` in both (the 6z fold pattern's self-contained sub). Gate: **390/390 × 4 configs** (linux-gcc-asan the
money config — the {1..16} threaded run ASan-clean vs the reference) + tidy + GCC `-Werror=switch` + `crd-ceir-invariants`
(crd-ceir STILL jobs-free — the sequential reference names no jobs type). ⛔ **NO format motion / fuzz / recook.** ◀
**NEXT = stage 3 (DESIGN LOCKED 2026-08-10, advisor consult → design note §8; implement next tick):** jobs-backed
launch/await ON-POOL via **CONDITIONAL POOLING** — ⛔ the crux: `async.launch` bodies run IN-FRAME in the reference
(captures + state legally work, UNLIKE parallel_for), so "require pure" would be a capability regression; stage 3 must
never reject what stage 2 ran. A bridge-local **pool-eligibility classifier** (StateEdge-free + calls-resolved + NO outer
captures) → pool (fresh sub, counter = happens-before) or sequential-fallback (bridge-side via run_region/store_yields/
set_value); byte-parity by CONSTRUCTION in both branches. ⛔ Pooled handle > u32-max would TRUNCATE + alias a sequential
handle → ALL FIVE consumers (await/join/race/cancel/fiber_wait) route through one `resolve_token(i64)`. `execute()` DRAINS
the pooled table at exit (leak containment). spawn/worker → pooled; main_thread → pin_thread=0; group/scope → no override.
⭐ **`pooled_count()` witness** (asserted ==N pooled / ==0 fallback — else a never-pools impl passes every parity test).
Verifies DONE: intern_op read-only on hit (no worker-read race); wait pumps thread 0 (execute() must run on main). T1–T6.
Then stage 4 (11a DoD gate) + ADR-0122.

✅ **Stage 2b-ii-A DONE + gated (2026-08-10) — the parallel-purity pre-flight MOVED to crd-ceir core** (the 9d
hoist-at-second-consumer; zero behavior change). `PfResult`/`preflight_region`/`check_parallel_region`/`preflight` (the
StateEdge-free + arity + terminator analysis, PURE IR — no jobs types) lifted from the crd-ceir-host bridge's anon
namespace to public `exec::` surface (`exec::PreflightResult` · `exec::check_parallel_region` · `exec::preflight_parallel`);
the provider's `execute()` now DELEGATES. ⭐ **So the provider's PARALLEL run + the (2b-ii-B) reference's SEQUENTIAL run
share ONE legality analysis — agree by construction.** Swapped the bridge-era `"func.call"`/`"task.*"` STRING compares for
interned OpIds (idempotent intern → non-const `Context&`). The conservative module-wide over-rejection is documented as an
oracle/provider divergence (not a bug). Gate: **387/387 × 4 configs** (core + bridge relink) + tidy + GCC `-Werror=switch`
+ `crd-ceir-invariants` (crd-ceir STILL jobs-free — I4 holds, the pre-flight names no jobs type). ⛔ **NO format motion / no
new tests** (a behavior-preserving move — the existing host-provider parallel tests exercise it via delegation). ◀ **NEXT
= stage 2b-ii-B** (the sequential reference EvalFns via the 6z fold pattern [fresh sub-interpreter, per-index invoke_region;
NOT run_region-in-frame — captures wrongly resolve — NOR invoke_region-on-`in` — live frame] + an Interpreter-owned
`map_output` store [the m_yield_store/m_cells family] + `invoke()` captures `const Module*` + a `fuel()` accessor + the
first oracle-vs-provider agreement tests; ⛔ NO execution sharing — the bit-exact-blind scar).

✅ **Stage 2b-i DONE + gated (2026-08-10) — §112 step hooks + 3 stale-comment fixes** (split from 2b: core-only, no
bridge relink — distinct blast radius from 2b-ii's pre-flight move). The §112 debugger SEAM (hooks only): a pre-op + a
post-op `StepHook = void(*)(const Operation&, void*)` + `set_step_hooks(pre, post, user)` on the Interpreter — pre fires
BEFORE each dispatched op, post ONLY after a SUCCESSFUL dispatch; ⛔ null-default (zero work when unset — one null check
in `eval_op`); NOT proto-copied (per-session, like set_user/cancel). 3 stale-comment fixes: `exec.hpp` "per-width typing
joins CEIR-6" → integer-only-through-11a + "Jobs-backed parallel async CEIR-6c" → CEIR-11a; `task.ceirop.toml` header
"route to CEIR-8" → LANDED at CEIR-11a. `test_task.cpp` +1 (`[ceir][task]`: exact pre/post counts [2/2 — const+return, the
not-≥ discipline] + null-default zero-cost). Gate: **387/387 × 4 configs** + tidy + GCC `-Werror=switch` + opgen
drift-clean (TOML comment) + `crd-ceir-invariants` (U-§116). ⛔ **NO format motion / fuzz / recook.** ◀ **NEXT = stage
2b-ii** (the parallel_for/map_reduce sequential reference — move `check_parallel_region` from the bridge to crd-ceir core,
the 9d hoist-at-2nd-consumer precedent so oracle+provider agree by construction; relinks the bridge).

✅ **Stage 2a DONE + gated (2026-08-10) — the six §38 residual ops (advisor-designed signatures).** `task.spawn`/
`main_thread`/`worker` `{body}→%token` (TokenProducer, share `eval_launch` — placement value-unobservable in the §118
oracle) · `task.continuation` `(%tok){body(%vals)}→%next` (Consumer+Producer, NEW `eval_continuation`: antecedent yields
→ body block-args → new token) · `task.fiber_wait` `(%tok)→(%vals)` (mirrors `eval_await`, host wait yields the fiber) ·
`task.group` `{body}→(%results)` (NO token traits — bounds not forks, shares `eval_scope`, confinement layered on 5b+6a).
Layering invariant: async = concurrency STRUCTURE (no execution commitment) · task = host/job PLACEMENT (the shared-EvalFn
model is the state/delay/history precedent). ⛔ **Blocking pre-TOML verify PASSED** — crd-jobs offers every lowering
(run/wait/pin_thread=0/parallel_for) → no cook-only-gate. New `install_task_semantics`. ⭐ **The open-world payoff
ASSERTED:** the 6a `find_token_misuse` verifier + the generated smoke tests cover all six ops with **ZERO verifier edits**;
the §32 audio-RT flip fires on a task op. `test_task.cpp` (4 `[ceir][task]`: spawn→fiber_wait roundtrip · continuation
chain (10+5=15) · Unconsumed + double-consume · audio flip) + 3 auto-grown generated smoke. Gate: **386/386 × 4 configs**
+ tidy + GCC `-Werror=switch` + opgen drift-clean (regen) + `crd-ceir-invariants` (U-§116). ⛔ **NO format motion / fuzz /
recook** (name-interned — the CEIR-2 doctrine held). ◀ **NEXT = stage 2b.**

| ✔ | Slice | Deliverable | → §§ |
|---|---|---|---|
| ✅ | CEIR-11a | ✅ **CLOSED 2026-08-10 (ADR-0122). BAND 11 OPENS.** Reference executor grown to the FULL host subset (the §118 oracle for the 11b compiled tier) + step hooks + the inbound band-6 async work — shipped as 4 gated stages (design note `ceir-11a-reference-executor.md`; 382→386→387→390→396→397): **(2a)** the six §38 ops (spawn/continuation/main_thread/worker/fiber_wait/group — TOML+opgen; the layering invariant: async=STRUCTURE, task=PLACEMENT, so spawn/main_thread/worker share `eval_launch`, group shares `eval_scope`, only `continuation` is new; ⭐ the 6a token verifier covers them with ZERO edits); **(2b-i)** §112 step hooks (pre/post fn-ptr, null-default, not proto-copied) + 3 stale-comment fixes; **(2b-ii-A)** the parallel-purity pre-flight MOVED to core (`exec::preflight_parallel`/`region_state_free` — 9d hoist, agree-by-construction); **(2b-ii-B)** the parallel_for/map_reduce SEQUENTIAL reference (⛔ the 6z FOLD PATTERN — fresh sub, per-index invoke_region; the first oracle-vs-provider byte-agreement, non-associative, {1..16}); **(3)** jobs-backed launch/await ON-POOL via ⭐ **CONDITIONAL POOLING** (the crux: launch bodies run in-frame, so never reject what stage 2 ran — classify pure+self-contained → pool [heap `PooledToken`, counter=happens-before], else sequential fallback; byte-parity by construction; disjoint handle space `1<<40` → all 5 consumers via `resolve_yields`; `execute()` drains; cumulative `pooled_count()` witness). **(4/DoD gate)** ⭐ the full-subset **composing** program: arith+core(for/if/state)+func(call)+async(launch/await)+task(spawn/fiber_wait) in ONE reference session — exact result (21), exact §112 dispatch count (29), state-cell latching, cross-dialect `find_token_misuse==None`. **NAMED-FORWARDS:** the compiled tier + the 3 converging 10x forwards → CEIR-11b; continuation semantic yield-check → 3/4; `!async<T>` → §37; real race/deadline → 24/29; alloc-free/string-free audit → 11z. **Advisor-reviewed close (mandated the composing gate — the guarantees never met in one program).** ⛔ **crd-ceir core STILL jobs-free** (the pool is all bridge-side; `region_state_free` names no jobs type). Tests: `test_task.cpp` (6 `[ceir][task]`) + `test_host_provider.cpp` (agreement + 6 ON-POOL `[ceir][host]`). **Gate: 397/397 × 4 configs** (win-asan+linux-gcc-asan the threaded money configs) + tidy + GCC `-Werror=switch` + opgen + `crd-ceir-invariants`. **NO recook, NO fuzz, NO binary version bump.** | §118 §84 §112 §38 |
| ✅ | CEIR-11b | ✅ **CLOSED 2026-08-10 (ADR-0123) — the COMPILED tier of §84, differential-verified byte-for-byte against the §118 reference oracle across a program corpus.** DESIGN LOCKED 2026-08-10 (advisor consult → `docs/design/ceir-11b-compiled-execution-plan.md`, the ADR-0123 substrate). Plan compiler + `CompiledExecutionPlan` + the differential harness (reference vs compiled, `pin_values` byte-compare over the corpus). ⭐ **The representation (advisor-corrected): STRUCTURED CONTROL FLOW COMPILED TO DENSE INSTRUCTION ARRAYS** — the Module is compile-time INPUT ONLY; the plan owns a tree of dense `Instr{thunk, operand-slot span, result-slot span, immediate, child-seq indices}`; ⛔ the hot loop NEVER touches an `Operation*` (a structural Region walk = "authoring-graph traversal", still a §153 violation; flat jump-streams rejected — §153 wants DENSITY not flatness). Slots: every SSA Value → dense u32 slot, flat i64 store per frame (kills the env map); thunks pre-resolved (kills the dispatch map). ⛔ **The three HIDDEN §153 violations (the real 11z risks, VERIFIED):** per-eval attr reads (`eval_const` "value", `eval_cmpi` "predicate" STRING, state "depth") → compile-time immediates/enums; the state-cell `HashMap<Operation*,Cell>` → dense cell indices + pre-baked per-block LATCH LISTS; `resolve_call` per call → compiled-function index + arena frame windows. ⛔ **Compiled thunks are INDEPENDENT impls** (never delegate to the reference EvalFns — the bit-exact-blind scar; share the SPEC not code). Compile-time rejection = a typed `CompileError` (the differential covers programs that compile). Home = crd-ceir core `plan.{hpp,cpp}`; compiles the SEQUENTIAL semantics; ⛔ does NOT name the §69 compile→plan interface (→ 21/26). **PlanCache: the plan is an IN-MEMORY object** (raw thunk ptrs non-serializable) — NOT wedged into 10b's byte-artifact; the serializable blueprint (thunk-TABLE indices) is named-forward. Corpus (⛔ "7z" is pre-renumber → band-7's §121 cooked-vs-builder-twin byte-identity AT THE PLAN LAYER): the 5z gate (loop/switch/calls/state) + the 6z map-reduce + exec/async/task, via a SHARED BUILDER header (rich_graph.hpp/1f precedent). Stages: (1) note ✅ · (2) straight-line arith ✅ · (3a) control flow ✅ · (3b) §20 state · (4) calls + async/task · (5) full-corpus differential + ADR-0123 + close. Advisor on each stage's design. ✅ **Stage 2 DONE + gated (2026-08-10):** `plan.{hpp,cpp}` (crd-ceir core) — `compile(ctx, module, entry)` → a dense `CompiledPlan{instrs, operand/result pools, num_slots, param/result slots}` for straight-line arith (const/addi/muli/cmpi + func.return); the compile-time slot allocator (a `Value*→u32` map — COMPILE-time only) + the §153 immediates (const value, the cmpi "predicate" STRING → a dense `CmpPred` enum ONCE); `run(plan, args)` — a flat i64 slot array, a **dense switch** dispatch (§153: a jump table, NOT a map/string/heap touch, NO `Operation*`). ⛔ INDEPENDENT thunks (never call the reference EvalFns). Typed `CompileError` for not-yet-supported ops (a legit tier difference — the differential covers programs that compile). ⭐ **The DIFFERENTIAL harness skeleton:** `test_plan.cpp` (3 `[ceir][plan]`) — a `differential()` helper runs @entry through BOTH the reference interpreter AND the compiled plan, `pin_values` byte-compares (straight-line arith 5*5+3=28; cmpi sgt both ways; UnsupportedOp/NoEntry rejects). Gate: **400/400 × 4 configs** + tidy + GCC `-Werror=switch` + `crd-ceir-invariants` (crd-ceir jobs-free — the plan is pure). NO recook/fuzz/version-bump. ✅ **Stage 3a DONE + gated (2026-08-10) — CONTROL FLOW compiled to CHILD instruction arrays** (split from stage 3: control flow now, §20 state → 3b). The plan is now a TREE of dense `Seq{instrs, yield_slots}` (each Seq owns its instrs — nested but movable, no interleaving); a control-flow `Instr` records CHILD SEQ indices (into `child_pool`) + the recursive compiler (`compile_seq`) compiles each region's block → a child seq; ⛔ the executor `run_seq` recurses into child seqs BY INDEX (never an `Operation*`/Region). `If`/`Scope` forward the child's yield → results; `For` binds the induction slot (imm) + loops [lo,hi) step (⛔ `RunError::BadForStep` on step≤0); `While` = cond-yields-1 loop (`CondArity`); `Switch`/`Match` = run `children[sel]` (`SelectorOutOfRange`). ⭐ **New `RunError`/`RunResult`** — the differential compares the ERROR too (a runtime BadForStep/SelectorOutOfRange must agree). A fuel budget (1<<24) is the hang-guard. `test_plan.cpp` +3 (`[ceir][plan]`: if branch-forwarding both ways; a bounded for [no-op statement, no state yet]; BadForStep + SelectorOutOfRange agreement). Gate: **403/403 × 4 configs** + tidy + GCC `-Werror=switch` + `crd-ceir-invariants`. NO recook/fuzz/version-bump. ✅ **Stage 3b DONE + gated (2026-08-10) — §20 STATE (dense cells + plan-owned rings + pre-baked latch lists).** `core.state`/`delay`/`history` → `Op::State`: a depth-N ring keyed by a dense CELL index (the Instr imm), ⛔ replacing the reference `HashMap<Operation*,Cell>`; per-run storage = `Array<Ring>` (ring pre-sized from the compile-time `cell_depths`). cell_read reproduced INDEPENDENTLY (first eval init-fills from op0, result=`ring[pos]`); the latch is a pre-baked `Latch{cell,next_slot}` list per `Seq` (block-end `ring[pos]=slots[next]; pos=(pos+1)%depth` — read-all-then-latch, ⛔ no runtime trait scan, the §153 win). ⭐ **The FEEDBACK-EDGE fix (found by the for+state test):** a cell's `next` names a value defined LATER in the block (`state(0, acc+iv)`) → eager slotting rejected the forward ref, so a state op pushes ONLY op0 (init) and the `next` slot is DEFERRED, resolved into the latch AFTER the block compiles (the latch fires at block END). ⭐ **`RunResult.cells`** — a for+state accumulator's value lives in the CELL not the return, so the differential grew cell inspection (`cells[i]`=ring[pos] after the run, byte-compared to the reference `cell_value`, the 11a `map_output` parity §118). `test_plan.cpp` +4 (`state(7,0)` cell_read→7; a `for(0,3,1)`+`state(0,acc+iv)` accumulator latches 0+1+2=3 in BOTH engines; ⭐ a **depth-2** delay-line witness — `for(0,4,1)`+`state[depth=2](../detours/0,iv)` reads 0,0,0,1→`ring[pos]==2`, discriminating a depth-1 cell's 3, so the value proves the ring wraparound + whole-ring init-fill; `delay(3)+history(4)=7` routing/multi-cell — the advisor-mandated depth≥2 + delay/history coverage, else compiled-accepts-what-reference-errors surfaces only at the stage-5 corpus, the inverse of the never-reject scar). Gate: **407/407 × 4 configs** + tidy + GCC `-Werror=switch` + opgen + `crd-ceir-invariants` (crd-ceir STILL jobs-free — the rings are arenas). NO recook/fuzz/version-bump. ✅ **Stage 4a DONE + gated (2026-08-10) — func.call (compiled-function indices + arena FRAME WINDOWS).** The plan is now a table of `CompiledFn{entry_seq, num_slots, param_slots}` with FRAME-RELATIVE slots; `compile` is a WORK-LIST over the call graph (@entry=fn0; each `func.call` resolves its callee ONCE at compile → a compiled-fn INDEX in the Instr imm, index assigned BEFORE its body compiles so recursion/mutual-recursion resolves + enqueued). ⛔ Each function gets a FRESH slot map (frame-relative); seqs/pools/cells/fn-table stay PLAN-global. The executor `run_seq` threads a `frame_base` — every slot is `stack[base+slot]` on a growing slot STACK; control-flow child seqs share the base, only `Op::Call` pushes a WINDOW (grow by callee num_slots zero-filled, bind args caller→callee, run the callee entry seq, min-copy yields→results BEFORE popping, shrink — capacity retained, amortized arena; recursion native + fuel-bounded). ⛔ The realloc discipline (the push_back-UAF scar in new clothes): NEVER hold a slot ptr/ref across the callee run — index through the Array (ASan-clean proves it). ⭐ **Cells stay GLOBAL per-op** (`m_cells` keys by op pointer → a callee's cell shared across invocations; the dense index already matches). Error tiers: unresolved-callee / call-param arity (both structural) → COMPILE reject (`UnresolvedCall`/`CallArity`), reference fails at RUNTIME (§4); result copy COUNT matches the reference but the residue differs (reference leaves extras UNSET→`UndefinedValue`-if-read, compiled zero-fills → 0 — the defined/undefined gap is a stage-5 watch, NOT an arity error since the reference runs such a program when the extra is never read). `test_plan.cpp` +5 (basic `call add1(41)==42`; nested `add2→inc→inc==42`; ⭐ shallow **recursion** `sum(5)==15`; ⭐ a **state-cell-in-callee-called-twice** witness — `tick(){s=state(0,s+1)}` ×2 → `0+1==1` & `cells[0]==2`, a per-frame cell would give `0`, vs reference `cell_value`; unresolved + call-arity compile-rejects). Gate: **412/412 × 4 configs** + tidy + GCC `-Werror=switch` + opgen + `crd-ceir-invariants` (crd-ceir STILL jobs-free/asset-free — the slot stack is a plain arena). NO recook/fuzz/version-bump. ✅ **Stage 4b DONE + gated (2026-08-10) — async/task (COMPLETES STAGE 4).** A run-global **TOKEN STORE** (`Array<Array<i64>>` in RS, grows per launch — ⛔ NOT per-frame; `store_token`=push+handle, mirroring the reference `store_yields`) + six INDEPENDENT thunks, all in the CURRENT frame (only `func.call` pushes): `Op::Launch` (async.launch / task.spawn/main_thread/worker — run the body NOW, store its yields, result=handle); `Op::Await` (async.await / task.fiber_wait — bad handle → `RunError::BadToken`; min-copy stored yields → results); `Op::Continuation` (⛔ COPY the antecedent yields BEFORE running the body — the store reallocs under a span; bind them as body args via the new `Seq::arg_slots`; run; store → new handle; body-arity ≠ antecedent-yield-count → `RunError::ContinuationArity`, reference `BadArity`); `Op::Join` (concat every token's yields → ONE store); `Op::Race` (winner index 0, ⛔ validates NO handle — the reference does `value_of` only); `Op::Cancel` (no-op, ⛔ validates NO handle); async.scope/task.group → `Op::Scope`. ⭐ **NAMED FORWARD → STAGE 4c: task.parallel_for / map_reduce** run an isolated sub-interpreter with captures pinned `UndefinedValue` (the 6z fold); they compile-reject as `UnsupportedOp` for now. The advisor (pre-close) corrected the deferral rationale — NOT a provider-tier punt but a **small 4c**: compile the body as a mini-function (a FRESH slot map, so a capture fails `slot_of` at COMPILE — the §4 pattern) + a per-index frame-window run + the independent index-order fold + the state-free preflight as a compile check. ⛔ It is also a LOCKED stage-5 corpus entry ("the 6z map-reduce"), so it must be built (4c), not dropped. `test_plan.cpp` +9 (launch→await 42; ⭐ a **token launched in a CALLEE awaited in the CALLER**=7 — the run-global-store witness, a per-frame store would `BadToken`; continuation chain 15; join concat 7; race→0; cancel no-op 8; task.spawn→fiber_wait routing 9; ⭐ `BadToken` agreement=EXACT `ExecError::BadToken`, not merely !ok; ⭐ `ContinuationArity` agreement=1-yield antecedent into a 2-arg body → reference `BadArity`). The token guard u32-truncates the handle EXACTLY like the reference `valid_yield_handle` (a 2³²+ handle wraps identically in both tiers) at all 3 sites. The differential helper now installs async+task semantics (SEPARATE from `install_builtin_semantics`). Gate: **421/421 × 4 configs** + tidy + GCC `-Werror=switch` + opgen + `crd-ceir-invariants`. NO recook/fuzz/version-bump. ✅ **Stage 4c DONE + gated (2026-08-10) — data-parallel (COMPLETES STAGE 4).** `task.parallel_for`/`map_reduce`, the 6z-fold reference, compiled. The map/combine body compiles as an ISOLATED mini-function (`compile_fn_body`, a FRESH slot map → an outer CAPTURE fails `slot_of` at COMPILE = `CapturedValue`, the §4 pattern; runs per-index in a 4a frame WINDOW; ⛔ never holds a funcs/seqs ref across the inner compile_seq). ⭐ **SHARED pre-flight (the design fork — advisor: REUSE):** at compile, `exec::check_parallel_region` (moved to core in 11a for exactly this — legality ANALYSIS is the shared layer, execution stays independent) validates the map region (1 arg, yields 1, transitively state-free) + combine (2 args, yields 1); its `ExecError` maps to a `CompileError` (`ParallelArity`/`ParallelYield`/`ParallelStateful`/`UnresolvedCall`). Sharing it makes accept/reject agreement true BY CONSTRUCTION (a drifted reimpl could compile-accept a stateful body the reference rejects — the dangerous direction). Thunks (INDEPENDENT execution): `Op::ParallelFor` maps the range → `map_output[imm]` (a statement, no SSA result); `Op::MapReduce` maps then FOLDS the combine fn (args=acc,elem) in INDEX order → the reduced SSA result. `count=(hi>lo)?(hi-lo+step-1)/step:0` (verbatim); step≤0→`RunError::BadForStep`. ⛔ Fuel is snapshot/restored around the body loops (the reference gives the body a SEPARATE budget — the parent is uncharged). ⛔ `child_pool` holds compiled-FUNCTION indices for dp ops (documented at the Instr field + both thunks). ⭐ **FULL body isolation (advisor pre-close):** the reference sub isolates env + cells + **`m_yield_store`** — a RAII `TokenScope` gives each parallel body phase a FRESH per-phase token store (swap the run-global `rs.tokens` out, discard the body's after; the fold gets its own — two subs). Without it a body `await(parent-handle)` would WRONGLY resolve (reference: `BadToken` — compiled-accepts-what-the-reference-rejects, the dangerous direction) + a body-launched handle would leak the parent's numbering. `RunResult.map_outputs` (dense map index) = the §118 inspection parity vs the reference `map_output(op)` (default-empty agrees with find-fail-or-stored-empty — no watch item; **watch (d):** a nested dp op's map_output is sub-local/discarded in the reference → stage-5's generic compare SKIPS nested dp ops). `test_plan.cpp` +8 (⭐ a **parallel_for map inspection** iv²=[0,1,4,9] vs `in.map_output`; ⭐ a **NON-ASSOCIATIVE** map_reduce acc*2+elem=11, the index-order witness; a **capture** reject=`CapturedValue`; a **stateful** body reject=reference RUNTIME `ParallelBodyStateful` vs compiled COMPILE `ParallelStateful`, paired exact errors; **BadForStep** runtime agreement; an **empty range**; ⭐ a **fresh-store await** witness=parent-handle-0 + body `await(0)`→BadToken both; ⭐ a **fresh-store numbering** witness=body launches from 0→map [0,1] both, not parent-global [1,2]). The unsupported-op reject test migrated `task.parallel_for`→`core.foreach` (parallel_for now compiles). Gate: **429/429 × 4 configs** (ASan validates the map frame-window realloc + the TokenScope move/discard) + tidy + GCC `-Werror=switch` (ParallelFor/MapReduce arms + 4 CompileError arms) + opgen + `crd-ceir-invariants` (crd-ceir STILL jobs-free/asset-free — `check_parallel_region` is core + jobs-free). NO recook/fuzz/version-bump. ⭐ **STAGE 4 COMPLETE — full op-class coverage (arith, control flow, state, calls, async/task, data-parallel), no named-forward remaining.** ✅ **Stage 5 corpus + a real bug fix DONE + gated (2026-08-10); ADR-0123 + row flip REMAIN.** The SHARED corpus builder (`tests/ceir/corpus.hpp`, the rich_graph/1f precedent — build each program ONCE, run through BOTH engines): 5 programs (**5z** band-5 gate loop+match+if+calls+state→118; **6z** non-assoc map_reduce→33930; **async** launch/await/join/continuation→29; **the six task ops**→46; **composing** all-dialects→118), each returning module + state/dp op handles in COMPILE ORDER. The corpus differential (`test_plan.cpp` +6): each through the reference AND the compiled plan — VALUES (`pin_values`) + CELLS (handle-based, watch (a): read→equal, unread→0) + MAP_OUTPUTS (top-level, watch (d)); the §4 contract + watch items (a)–(d) stated ONCE in the harness header. ⭐ **§121 plan-layer twin:** a cooked→loaded module (`serialize`→`deserialize`, dialects re-registered) compiles to BYTE-IDENTICAL results — compile both twins, compare the dense arrays (NO handles; compile-order indices align). ⛔ `build_5z` REUSED EXACTLY (`test_band5_gate.cpp` refactored to consume it — the build-once extraction). ⭐⭐ **THE CORPUS DID ITS JOB — a real stage-3a bug caught + FIXED:** `compile_seq` captured a control-flow op's `children_off` BEFORE compiling its region children, so a for-body containing a `match` (NESTED cf) aliased the match's arm0 → `Op::For` ran arm0 (`acc(iv)`) every iteration → 15 not 18. Every simpler test missed it (no NESTED control flow; the composing for-body is a dead iv²). Fix: compile children FIRST into a local buffer, THEN reserve a contiguous `child_pool` block + set `children_off`. Pinned by a minimal **nested-cf regression** (a `for` containing an `if` → 22) + the 5z corpus. Gate: **436/436 × 4 configs** (429 + 6 corpus + 1 regression) + tidy + GCC `-Werror=switch` + opgen + `crd-ceir-invariants`. NO recook/fuzz/version-bump. ✅ **CLOSED (ADR-0123 ACCEPTED; the §4 error-tier table is the differential contract; ADR-0121 §2.1 "opaque bytes" struck-in-place — the plan is an in-memory object). Stage progression 397→400→403→407→412→421→429→436. The §153 hot-loop AUDIT (CountingAllocator + no-string-table-touch) is CEIR-11z; per-op crd-perf regions are CEIR-11c.** | §84 §119 |
| ✅ | CEIR-11c | ✅ **CLOSED 2026-08-10 (no ADR — an integration slice).** crd-perf integration — as SHIPPED: a §153-clean **SEAM** (null-default pre/post op hooks around the dense `switch(Op)`, passing the op id) + a bridge **ADAPTER** doing per-Op-class dispatch COUNTS + parent-pause SELF-TIME + 2 crd-perf regions (`ceir.plan.run` / `ceir.plan.compile`) + the plan-compile stats as COUNTERS. ⭐ NOT a region-per-op: that would overflow the 4096-slot ring at fuel 1<<24 (a lying profiler — no-silent-caps); aggregation is ring-safe, and a region-driving consumer stays possible through the same seam. **CORE SEAM (part 1) gated (2026-08-10):** ⛔ crd-ceir core CANNOT link crd-perf (I5 + jobs-free), so 11c is a SEAM (`plan.hpp` `RunHooks{pre,post,user}` null-default fn-ptrs around the dense `switch(Op)`, passing the op id — NOT an Operation*, §153; observation-only; unbalanced pre>post on error) + `CompileResult.stats` (PlanStats). A perf-linking BRIDGE consumer (part 2, crd-ceir-host + crd-perf) owns the crd-perf calls (the §112/jobs_adapter pattern). ⭐ Volume-tension ruling: region-per-instr is a lying profiler (4096-ring overflows at fuel 1<<24) → per-op-class counts + parent-pause self-time + 1 region/run + 1/compile + stats-as-counters. `test_plan.cpp` +2 (seam pre==post==35 on composing + byte-identical with/without; erroring run pre>post). **PART 2 — the BRIDGE adapter (2026-08-10):** `engine/ceir-host/plan_perf.{hpp,cpp}` (crd-perf + crd-time added PRIVATE) — `PlanProfile` (per-Op counts + parent-pause self-time + a `depth_overflow` witness), `profiled_run` (the `ceir.plan.run` region + the parent-pause hooks), `profiled_compile` (the `ceir.plan.compile` region + the stat counters). Self-time via `MonotonicClock` (the profile fills even perf-OFF; only the crd-perf calls compile out). `test_plan_perf.cpp` +3 (straight-line exact counts + shape stats + perf-ON counter readback; a LOOP for(0,1000){muli} → nesting max_depth≥2 + granularity-proof total_self>0; an erroring await→BadToken run → the adapter survives the unbalanced pre>post). ⛔ self-time STRUCTURAL only (non-negative + measurable; NEVER quantitative — the flake scar); a `static_assert` guards the per-Op arrays vs a `plan::Op` widening. Advisor pre-close review (added the loop + error tests + the static_assert). ⛔ crd-ceir CORE STILL jobs-free/asset-free — the crd-perf/jobs link is BRIDGE-only. The real consumer (a live profiling front-end / provider composition) → CEIR-21/26 (the 10b PlanCache precedent). Gate: **441/441 × 4 configs** (438 core + 3 host) + tidy + GCC `-Werror=switch` + opgen + `crd-ceir-invariants`. NO recook/fuzz/version-bump. | §113 |
| ✅ | CEIR-11z | ✅ **CLOSED 2026-08-10 (no ADR — a §153 proof slice).** The hot-loop AUDIT (`test_plan.cpp` +3): (1) ALLOCATION-FREE arith loop — a `for{muli}` run at M=10 vs M=1000, alloc COUNT independent of the trip count + ≤4 (fixed setup) → zero heap per iteration, via a `CountingAllocator` decorator; (2) CELLS + CALL FRAMES AMORTIZED-arena — a `for{ call inc + state cell }` → `count(10)==count(1000)` (the ring init-fill + first frame growth are one-time, steady-state ZERO — the ADR-0123 §2.2 claim; dp token arrays are amortized-arena BY DESIGN, out of strict-zero scope); (3) NO-STRING-TABLE-TOUCH — a plan compiled in an inner scope runs correctly AFTER the Context (op/attr/string tables) is DESTROYED (⛔ `run()` is Context-free; the plan is dense — no OpId/AttrId). All on the **hooks-NULL shipping path**. The differential corpus (5z/6z/async/tasks/composing + the §121 twin) is already green in-gate. Gate: **444/444 × 4 configs** (the alloc count is IAllocator-level → stable under ASan) + tidy + `-Werror=switch` + opgen + `crd-ceir-invariants`. NO recook/fuzz/version-bump. | §153 |

## CEIR-12 — Resource/memory subsystem — ✅ **CLOSED 2026-08-10** (12a ✅ ceir.resource dialect + find_resource_misuse · 12b ✅ §20/§24/§25 intent attrs + find_resource_intent_misuse · 12c ✅ alias/lifetime analysis compute_block_lifetimes + resources_interfere/may_alias · 12d ✅ / ADR-0124 memory planner plan_block_memory + inspectable MemoryPlan · 12z ✅ composing band gate) — the resource→memory pipeline COMPLETE: dialect → planning-intent attrs → effect-derived live-range analysis → interval-coloring planner, aliasing provably saves memory (REN-1 IR edition). The SECOND fully-closed band of the 2026-08-09 re-baselined spine. Named-fwd: §15d frame-compile consumes the planner; CFG for cross-block ranges; providers (§150/13+) lower memory_domain + realize history rings + bytes. → §36 §24 §78

**Band contract.** `ceir.resource` (§36): views/ranges/subresources over the 3c types; transient/persistent/history
lifetimes; alias groups; memory-domain INTENT (§24 — ⛔ never Vulkan memory types / D3D heap flags as semantics);
residency/streaming hooks (§25); the memory-planner interface (§78 profiles: latency/balanced/memory/deterministic).
**Reuse:** the render-graph's greedy interval-coloring transient aliaser + lifetime analysis = the planner's first
implementation. **Scars, all here:** WAR-needs-resource-lifetime (both backends) · aliaser-must-check-slot-SIZE ·
RMW-not-RWM · transient-borrowed-bundle format. **Co-design with RAH-2.** **Gate: resource-graph lifetime +
aliasing tests.**

| ✔ | Slice | Deliverable | → §§ |
|---|---|---|---|
| ✅ | CEIR-12a | ✅ **CLOSED 2026-08-10 (no ADR — op-vocabulary slice).** Resource decl/view/range ops + import/export. PART 2 = the **`find_resource_misuse`** module-walk verifier (`context.cpp`; the type-system enforcement — identifies the ops by `op_name` string [const-safe, I6-clean], reads the CEIR-3c resource TYPE): 8 `ResourceMisuseKind`s — view (result-is-View · operand-VIEWABLE[Buffer/Image only — the advisor caught view-of-view being legal-by-accident; §36 views are flat] · underlying==operand · mask-valid · arity=1+2·popcount) · export operand-resource · declare/import result-resource; check order CONTRACTUAL. `test_resource.cpp` +2 (a well-formed declare+view+import+export module + a ⭐ BINARY+TEXT round-trip [View/ExternalResource kinds in op results survive the serializers] + all 8 malformed constructions rejected with the exact kind incl. view-of-view + a raw-`intern_type` invalid-mask). Advisor pre-close review (tightened the view operand to viewable + the contractual-order comment). Gate: **449/449 × 4 configs** + tidy + opgen drift/validator + `-Werror=switch` + invariants. NO recook/fuzz/version-bump. Named-fwd: SEMANTICS→12b+ (typed NoSemantics today); PROVIDER binding→13+/§150; alias/lifetime ANALYSIS→12c. **DIALECT (part 1) gated 2026-08-10:** `engine/ceir/ops/resource.ceirop.toml` (4 ops) — `declare` (GRAPH-owned resource; `name` + `alias_group` symbol attrs; Pure), `view` (ONE op spans view/range/subresource — the 3c `View` type unifies them, the mask lives in `type_view(underlying,mask)`, offsets/sizes are a variadic runtime tail; Pure), `import` (EXTERNALLY-owned; provider-bound handle = §150 named-forward; distinct OP from declare — planner plans declare's NEVER import's; Pure), `export` (external-publish boundary; conservative `MemoryReadWrite{operand=0}` so the 12c hazard walk orders it). ⛔ Resource-ness rides the CEIR-3c TYPES (Buffer/Image/View/ExternalResource) NOT a new trait (kKnownTraitsMask pinned); semantics = a typed NoSemantics named-forward (async precedent). opgen regenerated the 5 artifacts (no central edit; glob-collected). Smoke **3/3** on win-debug + win-asan + linux-gcc + opgen drift/validator; full ceir **447/447**. ◀ **part 2 = the `find_resource_misuse` module-walk verifier (view operand/underlying/mask/arity + export operand — the type-system enforcement layer) + the rich `test_resource.cpp` (round-trip + negatives) + the 12a close (no ADR).** | §36 §23 |
| ✅ | CEIR-12b | ✅ **CLOSED 2026-08-10 (no ADR -- attr-vocabulary slice).** The resource planning-INTENT attributes on `ceir.resource`: `resource.declare` carries `lifetime` {transient\|persistent\|history} (§20 -- `history<T>` binds here) + `history_length` (the history RING depth, >=1, valid only under lifetime=history) + `memory_domain` (§24 INTENT: host\|pinned_host\|device_local\|host_visible_device\|unified\|upload\|readback\|sparse\|external\|peer_visible\|distributed -- providers lower, NEVER Vulkan/D3D flags as semantics; §24's transient/persistent ride the `lifetime` axis) + `residency` (§25: resident\|streamable\|evictable) as CLOSED vocabularies, plus `streaming_priority`/`budget_class` as OPEN tags (alias_group-style, 12d consumes); `resource.export` gains `direction` {read\|readwrite} (ADVISORY -- the effect stays conservative MemoryReadWrite; closes 12a's stale export named-forward). Enforcement = a SEPARATE module walk **`Context::find_resource_intent_misuse`** + `ResourceIntentMisuseKind` (8 kinds), kept distinct from 12a's `find_resource_misuse` so its contractual order + 8 pinned negatives stay UNTOUCHED (widen-enum-audit scar); wrong VALUE and wrong KIND fold into one kind per attr (the state-depth precedent); a planning-intent attr on `resource.import` (never planned) is `IntentAttrOnImport`. `test_resource.cpp` +2 (all-six-attrs + export direction, BINARY+TEXT round-trip with VALUE read-back on BOTH twins covering string/int/symbol; 2 positives + 9 negatives incl. the wrong-kind fold + both HistoryLengthWithoutHistory paths). Advisor fork + pre-close (minted export `direction` now, pre-minted §25 open tags, §24 word accounting, IntentAttrOnImport, text-twin read-back). Gate: **451/451 × 4 configs** + tidy + opgen drift/validator + `-Werror=switch` + invariants. NO recook/fuzz/version-bump. Named-fwd: alias/lifetime ANALYSIS→12c; planner→12d; provider domain-lowering→13+/§150. | §24 §25 |
| ✅ | CEIR-12c | ✅ **CLOSED 2026-08-10 (no ADR — analysis slice, mirrors CEIR-4d's no-ADR hazard pass).** The resource ALIAS/LIFETIME analysis: **`Context::compute_block_lifetimes`** — per graph-owned resource (`resource.declare` result; `import` EXCLUDED, never planned) a live range `[first, last]` over the block's op order, where `last` follows `resource.view` chains to the root, extends into NESTED-region uses (attributed to the containing "pass" op), extends under any ambient Memory/Universe effect at-or-after the declare (the "over 4d effects" rule — an unregistered op counts), and pins to block-END when the resource is `export`ed. One recursive walker does uses+export+ambient at EVERY nesting (⛔ wrapping in a region never weakens the analysis — the pre-close review caught nested export/ambient being invisible). Predicates **`resources_interfere`** (interval overlap) + **`resources_may_alias`** (BOTH explicit Transient — Unspecified/Persistent/History never pool — ∧ neither exported ∧ same TypeKind ∧ same NON-ZERO `size_class` bucket ∧ disjoint; the frame graph's greedy interval model, the 12d planner colors it). Mints the OPEN `size_class` int attr on `resource.declare` (the frame graph's slot bucket; joined `kIntentAttrNames` → `IntentAttrOnImport` on import). `test_lifetime.cpp` +4 (WAR-lifetime scar + aliasable positive control; lifetime-class gate; slot-SIZE + kind + exported + ambient + unregistered + unspecified-size; nested-region/export/ambient + view-chain + import-excluded). Doc-truth: the 4d hazard comment's "views don't exist yet" clause STRUCK in place — `write(%buf)` vs `read(view(%buf))` was a real `ops_hazard` false-negative; **the hazard walk consulting the 12c view→root map ✅ LANDED in CEIR-13d part 3** (`op_access_at` + the bridge `gather` normalize through `Context::resource_root`); a view laundered through a region-yield/call-result still escapes both analyses. Gate: **455/455 × 4 configs** + tidy + opgen drift/validator + `-Werror=switch` + invariants. NO recook/fuzz/version-bump. | §78 §26 |
| ✅ | CEIR-12d | ✅ **CLOSED 2026-08-10 (ADR-0124 — a plan-producing tier with a downstream-consumed contract, the 11b/ADR-0123 precedent; §15d consumes "the CEIR-12 planner").** The memory PLANNER **`Context::plan_block_memory(block, PlanProfile, MemoryPlan&)`** — colors 12c's live ranges into physical slots: poolable-eligible resources (the `resources_may_alias` gates) processed in `(first asc, decl-idx)` order, first-fit into a same-`(size_class, kind)`-bucket non-dedicated slot whose end `< first`, else a fresh shareable slot; everything else dedicated (the frame graph's greedy aliaser, ported). Inspectable **`MemoryPlan`** (§162): `slots[]` (kind·size_class·dedicated·span·occupant_count·**history_length** — a `history<T>` ring's depth = its memory multiple, default 1 per 12b), `assignments[]` (declaration order; `SlotAssignment{resource, slot, **SlotReason**, prior}` — 6 reasons EXPLAIN the negative decisions too: Pooled·NewPoolSlot·DedicatedLifetime·DedicatedExported·DedicatedUnsized·DedicatedProfile), and `transient_logical`/`transient_physical` (poolable-only; `physical < logical` iff pooling — the REN-1 proof, IR edition). §78 **PlanProfile**: Memory (alias) vs Latency (dedicate — preserve parallelism) REAL; Balanced/Deterministic ride Memory (named-forward). Consistency invariant: every co-slotted pair satisfies `resources_may_alias` (by construction). Optimality is by-construction (12c input start-sorted; the sort is a guard, NOT a scoreboard win over the reference). `test_planner.cpp` +5 (physical<logical proof; minimal-coloring + consistency; Latency==baseline; every dedicated reason + history depths 1&2; bucket isolation + determinism + Balanced/Deterministic ride). Gate: **460/460 × 4 configs** + tidy + opgen drift/validator + `-Werror=switch` + invariants. Named-fwd: bytes→provider/§150; CFG cross-block ranges; history-ring realization. | §78 §162 |
| ✅ | CEIR-12z | ✅ **CLOSED 2026-08-10 (no ADR — a gate composes, it does not decide).** The BAND-12 COMPOSING gate (`test_band12_gate.cpp`, the band8 pattern): ONE frame-graph-shaped module carries all four 12a ops (declare/view/import/export) + the §20/§24/§25 intent attrs + mixed lifetimes, and the band composes IN SEQUENCE — (12a) `find_resource_misuse` + (12b) `find_resource_intent_misuse` + structure all `None`; (12c) the WAR resource `%war` extends to its LAST use (pos 19) **through a `resource.view` chain** + a direct read, NOT declaration order; (12d) `plan_block_memory(Memory)` pools the three disjoint same-bucket transients → `transient_logical == 5`, `transient_physical == 3` (⭐ the REN-1 "aliasing saves memory" proof, IR edition — EXACT counts, so a halved-pooling regression FAILS the gate), while `%war` (spans them), `%big` (slot-SIZE scar, different bucket), and the `history`(depth 2)/`persistent`/exported resources each keep own slots; the `import` gets NO assignment (`assignments.size()==8` — the planner never plans imports). Latency cross-check (`physical == logical == 5`); the co-slotted consistency invariant (every pair `resources_may_alias`); a serialize→deserialize twin re-plans to the SAME plan SHAPE per declaration-index (slot+reason+prior-null-ness — reproducible cross-context; the intent attrs survived). Gate: **461/461 × 4 configs** + tidy + opgen drift/validator + invariants. Test-only (a gate that forced an engine change = a 12a–d defect; none did). | §36 §78 |

## CEIR-13 — Compute + transfer (first GPU contact) — ✅ **CLOSED 2026-08-10** (13z ✅ the §129 execution proof + the ADR-0108 cornerstone flip executed; **BAND-13 DONE — NEXT = CEIR-14**) (13a ✅ + 13b ✅ + 13c ✅ + 13d ✅ CLOSED 2026-08-10 — the ceir.compute dispatch dialect [13a], the ceir.transfer movement dialect [13b], CKIR-by-identity: the [op.kernel_ref] machinery + DependencyRecord.ckir_refs + the cook's KernelResolveFn interface-hash contract check [13c, CDEP v5]; the LOWERING pass in the NEW `crd-ceir-gpu` BRIDGE [13d: 1a scaffold + ADR-0125 · 1b dispatch lowering · 2 transfer + ambient-narrowing (precise per-binding barriers) · 3 the 12c view→hazard retrofit + FLIP — all DONE, 481/481×4], §158/§79; NEXT = 13z the §129 EXECUTION PROOF [add/reduce/scan/FFT as CEIR assets run through the lowering, Vulkan+DX12 — ADR-0108 cornerstone flip] and any 13e) → §42 §50 §85 · PROOF §129

**Band contract.** `ceir.compute` (§42) + `ceir.transfer` (§50), lowered onto the EXISTING canonical command model
(`gpu-context` `command_model.hpp` / `detail/command_lowering.hpp`) and `IComputeContext` **via the `crd-ceir-gpu`
bridge module** (ADR-0109 §4.2 — the render/compute provider; `crd-ceir` core never links gpu-context) — CEIR
orchestrates the proven layer, it does not invent GPU plumbing (§158). CKIR by identity (§85): `compute.dispatch @kernel` references
CKIR programs by content-hash; compiler-EMITTED CKIR deferred to CEIR-26. **The proof reuses already-bit-exact
kernels** (`build_reduce`, `build_scan`, radix sort, FFT) — the new thing under test is ORCHESTRATION, not math.
**Scars:** upload→first-read barrier (grid-size time bomb) · dispatch_1wg · timing asserts same-pass-only.
**Proof (§129):** add/reduction/scan/FFT program asset on both backends.

| ✔ | Slice | Deliverable | → §§ |
|---|---|---|---|
| ✅ | CEIR-13a | ✅ **CLOSED 2026-08-10 (no ADR — op-vocabulary + verifier slice, 12a precedent).** The `ceir.compute` dialect (`compute.ceirop.toml`, CORE-only — the `crd-ceir-gpu` bridge lowers at 13d, core never links gpu-context): **`compute.dispatch`** (direct — grid = 3 dynamic Index operands + variadic `bindings`) + **`compute.dispatch_indirect`** (args buffer + variadic bindings), both RESULTLESS. Kernel referenced by a `kernel` SYMBOL (@kernel identity; KernelRef {asset_id, interface_hash} resolution = 13c); per-binding access = an `access` STRING `{r|w|rw}` comma-separated (one/binding, empty=0); binding **KIND derived from the operand's CEIR-3c type** (Buffer→storage/Image→texture/View→underlying), **SLOT = positional** — NO kind attr (12a one-source-of-truth). ⛔ effects = a CONSERVATIVE BASELINE **GPUCommand + ambient MemoryReadWrite** (indirect + static MemoryRead{args}): a dispatch may touch any bound memory so it MUST hazard exports/transfers (the advisor's Q4 — GPUCommand-only left a dispatch + the export of its own output freely reorderable, the WAR-scar IR edition); 13d NARROWS the ambient to per-binding from `access`. **`Context::find_dispatch_misuse`** (the find_resource_misuse house pattern) — 6 kinds: grid-is-Index · access(⛔ wrong-KIND/absent FOLDS into token-invalid per 12b, robust to a raw deserialized module · tokens · arity=n-3/n-1) · binding-is-resource · args-is-Buffer/View. **[amended CEIR-13c: a 7th kind `KernelNotSymbol` (absent/non-symbol @kernel, checked FIRST — identity before contract) once the kernel identity became load-bearing; the access-fold consistency.]** `test_compute.cpp` +3 (well-formed direct+indirect + BINARY/TEXT round-trip w/ kernel+access read-back; 6 negatives incl. the wrong-kind fold; ⭐ the conservative-baseline PINS: ops_hazard(dispatch,export)≠None + a dispatch's ambient extends a prior transient's 12c range). NO-FOLLOW (TOML): §42 persistent-kernels/cooperative-launches/dispatch-groups→13+/26; uniform-buffer bindings (no BufferMode::Uniform)→13d; BindingFrequency→13d. Gate: **467/467 × 4 configs** + tidy + opgen drift/validator + `-Werror=switch` + invariants. | §42 |
| ✅ | CEIR-13b | ✅ **CLOSED 2026-08-10 (no ADR — op-vocabulary + verifier slice).** The `ceir.transfer` dialect (`transfer.ceirop.toml`, CORE-only; bridge lowers at 13d) — 5 ops with **STATIC per-kind** 4a effects: **`copy`** `(dst, src)` ⛔ memcpy/D3D12 order (Write{0}+Read{1}; unified buffer/image/buffer↔image + SUBRESOURCE-via-views SHIPPED) · **`upload`** Write{dst}+open `source` · **`readback`** Read{src}+open `dest` (both resultless — host materialization provider-side) · **`clear`** Write{dst}+`value` (the buffer FILL WORD; typed image clear→13d) · **`mip_gen`** ReadWrite{image}. ⛔ STATIC effects (NOT 13a's ambient baseline — no per-instance access to defer): a transfer hazards PRECISELY, so `upload(%b)`→`readback(%b)` is exactly **RAW** (the upload→first-read barrier scar, IR edition) and — unlike a dispatch — does NOT extend an unrelated resource's 12c range (precise operand, never whole-Memory). **`Context::find_transfer_misuse`** (6 kinds, advisor Q4 closed 3 legal-by-accident shapes): OperandNotTransferable (Buffer\|Image\|View-of-those; ⛔ ExternalResource OUT) · CopySrcIsDst (same-SSA self-copy; distinct views of one root PASS) · MipGenNotImage · ClearValueInvalid (present∧non-Int, 13a fold) · ClearValueOnImage. `test_transfer.cpp` +3 (well-formed incl. buffer→image + view-subresource + BINARY/TEXT round-trip w/ source/dest/value read-back; 5 negatives; ⭐ the RAW pin + the precise-no-bleed 12c contrast + a cross-dialect ambient-composition seam). ⛔ KNOWN HOLE (12c strike): transfer-through-view escapes ops_hazard (retrofit→13d); scars pinned on DIRECT resources. NO-FOLLOW (TOML): §50 tensor/blit/resolve/peer/sparse/compression→13+/26; typed image clear→13d. Gate: **473/473 × 4 configs** + tidy + opgen drift/validator + `-Werror=switch` + invariants. | §50 |
| ✅ | CEIR-13c | ✅ **CLOSED 2026-08-10 (no ADR — extends the cook decision surface; two-tick split, both parts gated).** CKIR-by-identity — Reading B (the dispatch PINS an expected §107 hash, cook compares pin==resolved; NO cross-layer hash recompute, ADR-0109). **PART 1:** the opgen **`[op.kernel_ref]`** table `{symbol, interface}` (the `[op.native]` precedent — validates the named attrs exist with the right kinds, emits `.kernel_ref_symbol`/`.kernel_ref_interface` into OpSpec conditionally; `test_opgen.py` 53→59); `compute.dispatch`/`dispatch_indirect` gain an OPTIONAL `kernel_interface` int (the expected hash pin, u64 as i64 bits; absent=unpinned — additive, no version bump); `OpSpec`/`OpInfo` gain the marker (register_op promotes it); **`DependencyRecord.ckir_refs`** (`{name, interface_hash, pinned}` — ⛔ `pinned` a distinct bool, not hash==0) extracted by `collect_dependencies` SCHEMA-DRIVEN from `op_info.kernel_ref_symbol` (⛔ I6, never op-name-sniffing), deduped+sorted by name. **Two named-forwards STRUCK:** §64 (ckir_refs LANDED) + program_asset.hpp:28 (KernelRef deps DELIBERATELY excluded from interface_hash — which kernel is IMPLEMENTATION not caller-contract; folding breaks §107 hot-swap; rides the §106 deps + cook check instead). `test_compute.cpp` +1 (pin/unpinned/dedup/sorted + the I6 negative + a high-bit hash BINARY+TEXT round-trip). ⛔ IDENTITY became LOAD-BEARING → `find_dispatch_misuse` gained a 7th kind **`KernelNotSymbol`** (absent/non-symbol @kernel, checked FIRST — the access-fold consistency, advisor). **PART 2 (cook BRIDGE):** **CDEP schema v4→v5** — the FIRST cooked-format change of the detour (append ckir_refs {name·u64·u8 pinned}; stale v4 blobs clean-reject at read_cooked_header; content hash + kBinaryVersion UNCHANGED) + **`cook_program(…, KernelResolveFn resolve = nullptr, void* user = nullptr)`** (fn-ptr+user, RunHooks precedent; nullptr=deferred, CDEP persists) + `scan_kernel_contracts` re-walk via the SAME OpInfo field (CookResult.op → the offending DISPATCH): existence ALWAYS checked (`KernelUnresolved`, incl. unreadable identity), hash IFF pinned (`KernelInterfaceMismatch`). `test_program_cook.cpp` +1 (mock resolver over a 2nd cooked program's interface_hash: pin-match / mismatch@dispatch / unresolved / unpinned-OK / no-resolver→CDEP round-trips two refs [pinned+unpinned, sorted]). Gate: **475/475 × 4 configs** + tidy + opgen drift/validator (59) + `-Werror=switch` + invariants. | §85 §107 |
| ✅ | CEIR-13d | ✅ **CLOSED 2026-08-10 — PARTS 1a (scaffold + ADR-0125) + 1b (dispatch lowering + barriers) + 2 (transfer + ambient-narrowing) + 3 (12c view→hazard retrofit + FLIP) all DONE + gated (481/481 × 4).** The lowering pass: CEIR region → an INSPECTABLE command list with 4d-derived barriers (incl. the upload barrier BY CONSTRUCTION). ⛔ Advisor decomposition: the lowering is a PURE VALUE-LIST fn **`lower_region(ctx, block) → Array<LoweredCommand>`** (compile ≠ run, §158 — NEVER drives IComputeContext; testable with no GPU); handles stay UNRESOLVED (the 13c KernelRef + Value*s) → bind to a gpu-context DispatchDesc at EXECUTE (13z / CKIR-by-identity seam). **PART 1a:** **ADR-0125** (the bridge birth + the value-list contract [the plan-producing-tier shape] + conservative hazard-derived barriers + dep-narrowing) · the NEW **`crd-ceir-gpu`** bridge module (`engine/ceir-gpu`, the crd-ceir-host template — links crd-ceir + crd-gpu-context ONLY; render-graph/kir edges added WHEN CONSUMED per the acyclic gate, §4.2 amendment-by-narrowing) · `crd/ceir/gpu/lower.hpp` (the `LoweredCommand` value type {Dispatch·Barrier} + `lower_region` sig) + a stub · `tests/ceir-gpu` smoke (module links + contract) · **gate6b.sh target list updated** (advisor: it hardcodes targets, else the Linux gate silently skips crd-ceir-gpu-tests). ⭐ invariants confirm crd-ceir CORE stays host-only (I5) despite the new gpu-context bridge. **PART 1b (DONE):** `lower_region` emits a Dispatch per compute.dispatch/_indirect (grid resolved ALL-OR-NOTHING from arith.const operands via `defining_op()`, else `dynamic_grid`; Indirect never resolves a grid; zero group lowered as-is — ZeroDraw is validate_dispatch's EXECUTE rejection) + ONE Barrier before each carrying the strongest incoming `ops_hazard` (reverse scan → NEAREST strongest source, deterministic). ⛔ LoweredCommand is CEIR-NATIVE + validate-at-EXECUTE (primary-source divergence from the advisor's checklist: validate_dispatch needs a non-null kernel the lowering lacks — surfaced + confirmed; ADR-0125 §2.3 already had it right). `test_lower.cpp` +2 (structure/one-barrier-per-dispatch/nearest tie-break/dispatch-free; grid const/zero/dynamic/indirect; determinism). **PART 2 (DONE):** `ceir.transfer` lowered (LoweredKind::Transfer + our own LoweredTransferKind — the 5 shipped 13b ops, NOT the subsetting crd::gpu::TransferKind; Clear fill word resolved for §162) + the 13a ambient NARROWED in the bridge: a `gather(op)→precise accesses` (dispatch bindings+`access` · every other op's static per-operand effects via op_effects/effect_access) + `precise_hazard` (mirrors accesses_conflict). So the **upload→first-read barrier is DISCRIMINATING** (present iff the dispatch binds the uploaded buffer; disjoint → no barrier). ⛔ TWO hazard notions BY DESIGN (core ops_hazard stays conservative; only the bridge narrows — don't unify). ⛔ coupling reason = FALSE-GREEN avoidance NOT test-churn (decoupled, the scar test passes via the ambient regardless). 4 pins tested (EMPTY≠UNKNOWN unregistered→ambient · malformed access→ambient · GPUCommand ordering-inert · the 12c VIEW HOLE pinned as documented behavior). `test_lower.cpp` rewritten (the part-1b binding-less "→barrier" asserts correctly FLIPPED to no-barrier). **PART 3 (DONE):** the 12c view→hazard retrofit CLOSED across CORE + bridge in ONE move — new **`Context::resource_root`** (follows `resource.view` chains to the root via `defining_op`, one hop per 12a, loop-guarded; IDENTITY only, not byte-range) normalizes every captured resource in `op_access_at` (core) AND `gather` (bridge, all 3 capture sites), so `write(%buf)` vs `read(view(%buf))` now HAZARDS. The context.hpp/`accesses_conflict`/ADR-0124/ADR-0125/12c-row named-forwards STRUCK-in-place LANDED. `test_hazard.cpp` +1 (view→root RAW/WAR + different-buffer-view→None + two-views-of-one-buffer→hazard); the part-2 bridge view-hole pin FLIPPED (no-barrier → Barrier(Raw)) + retitled. ⛔ a view laundered through a region-yield/call-result STILL escapes (a deeper inter-op alias hole, named-forward). Advisor pre-close CONFIRMED. Gate: **481/481 × 4** + tidy (5 files) + opgen + `-Werror=switch` + invariants (crd-ceir CORE stays host-only — a pure hazard-walk refinement). | §158 §79 |
| ✅ | CEIR-13z | ✅ **CLOSED 2026-08-10 — the §129 execution proof + the ADR-0108 flip (was DESIGN-LOCKED; advisor-consulted; design note `docs/design/ceir-13z-execution-proof.md`; impl starts 13z-1 next tick — the 11a/11b stage-1 precedent).** **Proof (§129):** `add`·`reduce`·`scan`·FFT as CEIR program ASSETS — authored text AND builder, dispatched Vulkan AND DX12, ValidationCapture-silent, bit-exact vs the existing CPU oracles, hot-reload live-swaps, reference executor validates host-visible semantics. **Forks (advisor):** A → standalone `execute_lowered(ctx,commands,IComputeContext&,resolver,bindings)` in crd-ceir-gpu (NOT IExecutionProvider — provider-wrap = 21/26 forward); B+C COLLAPSE → NO crd-kir edge, a caller-supplied kernel-resolver fn-ptr → `IGpuProgram*` (test links kir; the 13c cook precedent; null→validate_dispatch NullProgram); D → backend-agnostic executor (`ckir_kernel_dispatch.hpp` abstracts both), FFT LAST (its multi-dispatch chain STRESSES 13d's inter-pass barriers); F → the honest claim = CEIR-path output BYTE-IDENTICAL to direct-CKIR-path (all 4), math keeps convention (int `==`, FFT f32-tol vs DFT + bit-exact impulse/DC — source=scoreboard, the row's "bit-exact" honored where true); G → the reference leg is a BRIDGE-side CPU oracle execution (core Interpreter STAYS NoSemantics for dispatch, §150 forward + I5 intact). **13z-1a DONE + gated 2026-08-10 (ADR-0126):** the execution SEAM — `execute_lowered`/`validate_lowered` in crd-ceir-gpu bind a 13d-lowered list to the ADR-0100 IComputeContext surface (a caller-supplied `KernelResolveFn`→`ComputePipeline*` + a `ResolvedBinding` Value*→`ComputeBuffer*` table; bindings `resource_root`-normalized). ⭐ ADR-0126 pins the **two-surface reality** (ADR-0100 IComputeContext = the compute-KERNEL execution surface where the CKIR proof kernels + oracles live vs RAF-2 command_model DispatchDesc = the frame-graph surface, NOT dead code) — the DispatchDesc/`validate_dispatch` language in ADR-0125 §2.3 + lower.hpp STRUCK-in-place, the semantics re-homed into typed `ExecuteError{UnresolvedKernel·ZeroDispatch·BindingArity·UnmappedBinding·UnsupportedCommand}`. Dispatch-only (Transfer/dynamic-grid→UnsupportedCommand, named-forward); the Barrier→ComputeAccess map is 13z-3 (FFT, the fft2d TransferDst scar). 4 DEVICE-FREE always-runs tests (fake resolver/buffers/recorder — guards the all-skip false-green): validate+record a good dispatch, view→root normalization, the 5 error paths, execute refuses+records-nothing. **485/485 × 4 configs** + tidy(4) + `-Werror=switch` + opgen + invariants (crd-ceir CORE untouched — bridge/test only). Checkpoints: 13z-1a seam+ADR-0126 ✅ · **13z-1b `add` on device — ✅ CLOSED 2026-08-10, BOTH BACKENDS (Vulkan 487/487×4 + DX12 489/489 Windows)** [new `tests/ceir-gpu-vulkan` target (links crd-kir-vulkan); shared `tests/gpu-shared/ceir_execute_1wg.hpp` — `build_add_kernel` (KGraph c=a+b) + `build_add_ceir_asset` + `dispatch_ceir_1wg` (clones `dispatch_kernel_1wg`, swaps ONLY `rec.dispatch`→`execute_lowered`); the `add` CEIR asset lowers → runs on a REAL Vulkan device (Windows) AND llvmpipe (Linux), **byte-identical to the direct CKIR dispatch** + `==` the CPU oracle + **validation-SILENT** (`ValidationCapture.error_count()==0`); device-skip-guarded + a device-free lower-shape always-runs case (all-skip false-green guard). ⛔ tidy-gate fix: `scripts/tidy-files.ps1` now globs `engine/*/generated` (a header including a generated engine header was parsing UNGATED). DX12 mirror LANDED (`tests/ceir-gpu-dx12`, guarded `if(NOT TARGET crd-kir-dx12)`, links crd-gpu-context-dx12; `emit_compute_kernel_hlsl`→`create_pipeline_from_hlsl`; real D3D12 device byte-identical + oracle; ⛔ ValidationCapture-silent stays Vulkan-only — gpu-context-dx12 has no info-queue capture, a named-forward). 1b CLOSED both backends] · **13z-2 ✅ CLOSED 2026-08-10 reduce/scan (text+builder), BOTH BACKENDS (win 496 / linux 492); 2b ADDED scan + the full §121 text-authoring** — scan (`build_scan_block` write_blocksum=TRUE, 3 buffers r,w,w) on Vulkan+DX12 byte-identical to direct CKIR + inclusive-prefix oracle + `bsum[0]==total` (landmine 2); the §121 text-authoring proven end-to-end — an EMBEDDED hand-authored text literal parses to the SAME canonical module as the builder (`print(parse(text))==print(builder)`) + lowers identically (device-free, all 4 configs), AND a **parsed-from-text reduce asset executes on a real Vulkan GPU == oracle** (the text-authoring device leg; text-on-both-backends is ceremony — byte-equal canonical modules + deterministic 13d lowering ⇒ the builder's DX12 run carries the text asset). ⛔ ASCII-only test-name scar re-hit: a `§` in a TEST_CASE name broke ctest's Windows name-matching → renamed. 2a was** [advisor-decomposed: single-workgroup variants ONLY — `execute_lowered` no-ops Barrier until 13z-3, so a multi-dispatch asset would run WITHOUT inter-pass barriers = pass-by-luck; the D-PROBE answered fork D (a compute.dispatch asset with a DANGLING `@kernel` symbol round-trips through CEIR text byte-exact + the parsed module lowers identically — the §121 text path works); harness generalized to a module-wrapped `build_ceir_dispatch_asset(kernel,access,nbufs)` (+ back-compat add wrapper); **reduce on BOTH backends** (`build_reduce_block`, 2 buffers r,w) byte-identical to direct CKIR + `==` sum oracle (⛔ INTEGER-valued f32 inputs, sum<2²⁴ → every association order exact, so the naive `==` oracle is legit — landmine 1) + validation-silent (Vulkan); NEXT 2b: scan (`build_scan_block` write_blocksum=TRUE so every buffer is live — landmine 2; 3 buffers r,w,w) both backends + the full §121 text-authoring (parsed module executes on device)] · **13z-3 ✅ CLOSED 2026-08-10 FFT (barrier stress), BOTH BACKENDS (win 501 / linux 496) — part 1 the 13d BARRIER COMPLETION** [advisor Fork A option (a): `LoweredCommand::Barrier` gains a `resource` field; `lower_region` emits ONE barrier PER conflicting root resource (in `after`'s binding-operand order), not one-per-dispatch — ⛔ a 13d CORRECTNESS FIX: the part-2 "one barrier, nearest-strongest" DROPPED conflicts (a dispatch reading N buffers from N prior passes recorded only 1; the FFT is where the 2nd is a real data race); `precise_hazard` retired, per-pair primitives stay; ADR-0126 §3.3 resolved; +1 discriminating multi-resource test (2 buffers/2 writers → 2 RAW barriers). **part 2 DONE + gated 2026-08-10 (win 499 / linux 495)**: `execute_lowered` REPLAYS each Barrier as `rec.barrier(root_buffer, from, to)` via `hazard_access` (RAW→ShaderWrite→ShaderRead · WAW→WW · WAR→RW; nullptr resource → all bound buffers). ⛔ WAR wrinkle VERIFIED both backends: Vulkan issues a real COMPUTE→COMPUTE execution dep, DX12 IGNORES `from` and issues a `to`=ShaderWrite→UNORDERED_ACCESS UAV barrier (direction-agnostic) — no conservatism needed. FakeRec extended to RECORD barrier calls; +2 device-free tests (a write-then-read chain → 1 RAW barrier on the shared root with the right accesses; 2-buffers-from-2-writers → 2 per-resource barriers). ⛔ dispatch→dispatch only (the harness owns the upload TransferDst→ShaderRead). **part 3 ✅ CLOSED 2026-08-10, BOTH BACKENDS (win 501 / linux 496) — 13z-3 DONE**: the 6-dispatch 2D FFT (`build_fft2d_c2c`, 14 buffers, 4 unique kernels) as a MULTI-DISPATCH CEIR asset. New harness: `build_ceir_multi_asset` (N declares + M dispatches, per-pass grid/access/binds — honest access FFT=`r,r,r,r,w,w`/transpose=`r,w`) + `MultiResolve`/`resolve_multi` (dispatch op→pipeline BY IDENTITY, no registry) + `dispatch_ceir_multi` (clone of `dispatch_fft2d`, execute_lowered REPLACING the dispatch+barrier loop; harness owns the upload barrier). The CEIR lowering derives the inter-pass per-resource barriers (⭐ pass 3 gets TWO — RAW on b_tr_re from pass 1 AND b_tr_im from pass 2, the part-1 completion LOAD-BEARING); execute_lowered replays them; **BIT-EXACT vs the CPU oracle** (`run_fft2d_cpu`) on real Vulkan (0.99s) + llvmpipe + validation-silent. DX12 NEXT: the mechanical mirror (`emit_compute_kernel_hlsl`→`create_pipeline_from_hlsl`) closes 13z-3] · **13z-4 ✅ reference leg + hot-reload — BOTH LEGS DONE + gated 2026-08-10 (win-debug/asan full ceir 510/510 · linux-gcc-debug/asan 505/505).** LEG 1 (reference executor) [advisor-decomposed. 1a: `execute_lowered_cpu` (test-side, links kir — the I5 wall holds, "bridge-side"=test-side NOT the crd-ceir-gpu module) runs the LOWERED FFT list on the CPU via `eval_cpu_kernel`, BYTE-EQUAL to the plan-driven `run_fft2d_cpu` — proves the lowering's dispatch sequence + binding mapping + grid (`cmd.groups_x`) against an independent driver, DEVICE-FREE all 4 configs. 1b: the CORE Interpreter REFUSES a `compute.dispatch` with a TYPED `NoSemantics` error (exec.cpp:238) — I5: the core stays device-blind, §150 forward]. **LEG 2 ✅ hot-reload via the 10a ReloadSet text seam (`add_source`/`reload_source`):** (1) THE PROBE — a func-less top-level `compute.dispatch` over `resource.declare` COOKS (`cook_program_text` — a Registrar installs arith/func/resource/compute into the transient cook Context so the verifiers aren't vacuous) + LOADS into a fresh generation Context + re-lowers to 1 dispatch — the untested cooked-blob round-trip of compute+resource (prior 10a coverage was func/arith/state only); (2) THE HOTSWAP (device-free) — `reload_source` @radd→@rmax moves `content_hash` but HOLDS `interface_hash` (a func-less module ⇒ empty exported-func projection ⇒ symbol feeds stable_hash NOT interface_hash — recalled claim CONFIRMED, decision == HotSwap + installed) + the NoChange seal (identical text = no-op → cook determinism); (3) THE DEVICE HOT-SWAP — a SYMBOL-KEYED resolver (reads the dispatch's `kernel` symbol) so the RELOADED TEXT ALONE selects add-reduce(2016)→max-reduce(63) on a real Vulkan device, killing the tautology (both pipelines + resolver FIXED across runs; ⛔ rebind `sctx.ctx = gen->ctx` + re-collect binds from the RELOADED module — reload mints a fresh Context; ASan-clean). Harness refactor: `dispatch_ceir_1wg_resolved` core (takes `KernelResolveFn`) + `dispatch_ceir_1wg` delegate (all 13z-1/2/3 callers unchanged). ⭐ **CUDA as a SECOND platform (user-directed, mission: all backends all platforms):** the CUDA CEIR tests (add/reduce/scan/6-dispatch FFT) now BUILD + RUN on the real Linux **RTX 4070 Ti SUPER** (WSL GPU passthrough, linux-gcc-debug 5/5) — genuine CUDA-on-Linux coverage, added to gate6b. ⛔ **scar — the PRE_TEST `_NOT_BUILT` sentinel fires in BOTH discovery modes (built-vs-configured is the axis, NOT PRE_TEST vs POST_BUILD)** — it correctly flagged unbuilt CUDA on a CUDA-capable box (how the RTX coverage was found). ⛔ **Linux-ASan CUDA:** `cuInit` in the WSL `libcuda.so` shim FAILS under ASan → the tests SKIP, but the driver's cuInit allocs (~50 KB, some in `<unknown module>` JIT frames a `leak:libcuda` suppression can't name) trip LSan at exit; fix = `ASAN_OPTIONS=detect_leaks=0` on the CUDA test binary, `if(UNIX)`-guarded (ASan's use-after-free/overflow checks stay ON; the tests never reach our code under ASan; Windows debug+asan cover CUDA with the native driver, no suppression). LEG 2 CLOSED. **13z-z ✅ CLOSED 2026-08-10 — BAND-13 CLOSES.** (a) THE BAND-13 GATE: the §129 execution proof COMPOSES — add/reduce/scan/6-dispatch FFT as CEIR program ASSETS, text + builder authored, cooked, hot-reloaded, executed BIT-EXACT across Vulkan + DX12 + CUDA (win-debug/asan full ceir 510/510, linux-gcc-debug/asan 505/505, CUDA on two physical platforms — Windows + real Linux RTX 4070 Ti). (b) ⚠ **THE ADR-0108 CORNERSTONE FLIP EXECUTED** (this IS the first CEIR vertical slice, §5 gate 2): PRINCIPLES/AGENTS/README/ROADMAP C++-only → Cerid-owned CEIR/CHIR + CR-D007-visual; ADR-0081 §9 struck-in-place + `Superseded-by: ADR-0108` header + the decisions-README rows; forward-pointers on the secondary carriers (phase-3.1.6/4.0, ADR-0065 D17, ROADMAP historical banner). Awaiting the user's ONE ratification commit (ADR-0108 §7). **NEXT: CEIR-14.** | §129 §170 |

## CEIR-14 — Render dialect — ✅ **CLOSED 2026-08-11 — BAND-14 COMPLETE** (14a–14d device-free dialect + 14z the render DEVICE proof band, all DONE + gated). The §169 shape list — triangle (14z-3) · MRT with per-target typed clears (14z-4: float/uint/heterogeneous) · depth-only (14z-5) · indexed-indirect(-count) (14z-6) · mesh dispatch (14z-7) — is proven device-side on BOTH backends, pixel-asserted; **validation-silent on VULKAN (real GPU + llvmpipe); the DX12 info-queue capture stays the 13z named-forward.** ⭐ ALL shapes ride ONE machine — `execute_render_frame` + one materializer + one lowering — and each cross-cutting path was FORCED by a later shape (the 2-scope closure by 4c, the per-op program-map by 14z-5, the operand-keyed buffer-map by 14z-6): §169 proves a SINGLE executor, not five. ⭐ **the frame-recording pull-forward (the user's no-name-forward verdict, overruling the original Option-A-defer recommendation) EMPIRICALLY VINDICATED: the device proofs found + fixed FIVE two-backend engine bugs** — DX12 uint-clear value-convert (14z-4c c2), DX12 per-attachment RTV formats (c3), record_plain depth-LOAD ignored (14z-5), free_transients transient-buffer wrapper leak (14z-5), the mesh y/z silent narrow → loud refusal (14z-7). ⛔ **band-14 does NOT claim CEIR render is "done": the proofs drive TEST-AUTHORED CEIR through identity/map sentinel resolvers — cook integration + authored-graph unification + resolver production are exactly CEIR-15 (FrameGraph unification) / CEIR-16 (Executor migration).** Was ◧ DESIGN-LOCKED 2026-08-10 (design note `docs/design/ceir-14-render-dialect.md`; advisor-consulted + 3 discriminating checks: opgen supports region ops ✅ · `lower_region` is flat, region-recursion = 14b/z machinery · `AttrKind::Float` exists ✅. Locked: `render.scope` REGION op (resultless, `kind="graph"`) + attachment-DECLARE ops (`render.color_attachment`/`render.depth_attachment`) producing a new attachment TYPE CLASS consumed by the scope's variadic tail — targets are OPERANDS not attrs [barrier/`resource_root` see operands only]; role-in-TYPE [12a]; typed clears = Float attrs. 14a = ops+verifier+structural+§121-text, DEVICE-FREE; draws→14b, lowering→14b/z, pixel-proof→14z. Effects = compute precedent [GPUCommand+ambient MemoryReadWrite; declares effect-free]. `find_render_misuse` mirrors `find_dispatch_misuse` + typed-clear-vs-format [RAH-1a.1 lifted to IR]. NEXT: author `render.ceirop.toml`.) → §40 §41 · PROOF §169

**Band contract.** `ceir.render` (§40) on RAH's hardened model: attachments are the RAH-1 typed
`ColorAttachmentDesc` span — G-buffer/visbuffer/velocity/object-ID are program-defined output contracts, NOT ops
(§41 — the RAH-1a.1 lesson generalized); draw family (draw/indexed/indirect/indirect-count/mesh-dispatch/patch,
§40); resource tables per RAH-2 (§157). Lowers to the same canonical command verbs the frame graph records today.
**Scars:** indirect draws must push the DrawIndex row · depth-only borrowing a color program dies when FS gains
discard · NDC±Y mirror on RTT-sampled passes. **Proof: triangles/MRT/depth/indirect/mesh/tess on both backends.**

| ✔ | Slice | Deliverable | → §§ |
|---|---|---|---|
| ✅ | CEIR-14a | Render scope op with typed N-attachment span (float/int/uint + depth/stencil + typed clears + resolve + MSAA + RO-depth). **✅ CLOSED 2026-08-10 — win-debug/asan full ceir 507/507 + ceir-14a 4/4; linux-gcc-debug/asan 512/512; opgen validator(59)+drift(exit 0); GCC -Werror=switch; crd-ceir-invariants(I3/I4/I5); LLVM-20 tidy(4 files). ⛔ ASCII-name scar RE-HIT (a `§121` in a TEST_CASE name → ctest "no tests ran"→false Failed under ctest while the direct exe PASSED; renamed to "sec 121"). Was:** `engine/ceir/ops/render.ceirop.toml` authored (3 ops — `render.scope` REGION op [resultless, `kind="graph"`, variadic attachment tail, width/height/sample_count attrs, GPUCommand+MemoryReadWrite] + `render.color_attachment`/`render.depth_attachment` DECLARE ops [Pure metadata; load/store/typed-clear/blend/compare/read-only attrs; produce attachment-typed values]); opgen REGENERATED (`render_ops.{hpp,cpp,json,md}`) + `--check` clean (no drift) + **compiles into crd-ceir**. **The attachment TYPE CLASS LANDED (`engine/ceir/{include/crd/ceir/render.hpp,src/render.cpp}`, the `time.cpp` precedent):** `register_dialect` = the generated `register_render_ops` + `register_type_class("color_attachment"/"depth_attachment", {verify_attachment,1})`; `verify_attachment` = one underlying Image / View-of-Image / TypeParam member (0/>1/non-image reject; color vs depth share the hook — the ROLE is the distinct CLASS, per ADR-0111); `type_color_attachment`/`type_depth_attachment` factories via `type_extern`. **Compiles into crd-ceir.** **`find_render_misuse` LANDED** (`context.{hpp,cpp}`, mirror `find_dispatch_misuse`/`scan_dispatch`): `RenderMisuse`/`RenderMisuseKind` (12 kinds) + `scan_render` pre-order walk + `render_misuse_kind_name`. Checks: attachment operand is Image/View-of-Image (`AttachmentNotImage`); closed-vocab per-attachment attrs (`load`/`store`/`clear_kind`/`blend`/`compare` — reuses the file-local `ceir_sv_in`, the resource-intent precedent); ⭐ the RAH-1a.1 typed-clear-vs-format scar lifted to the IR (`ClearKindFormatMismatch` — a `uint` clear only on an unsigned-Int-format attachment, via unwrap View→Image→`members[0]` + `!is_signed`); every `render.scope` operand is a color/depth attachment Extern type (`ScopeOperandNotAttachment`, via `type_class_name`); at-most-one-depth (`MultipleDepthAttachments` — a TYPE property, ADR-0111); width/height ≥1 (`RenderAreaInvalid`) + sample_count pow2-in-[1,64] (`SampleCountInvalid`). **Compiles into crd-ceir.** REMAINING (next tick): structural + §121-text tests (`tests/ceir/test_render.cpp` — the misuse matrix + a scope round-trips text≡builder + the type-class distinctness); `test_opgen.py` render entry; the 4-config gate → 14a CLOSE. | §41 |
| ✅ | CEIR-14b | Draw ops + dynamic state (viewport/scissor/shading-rate) with effects/hazards declared. **✅ CLOSED 2026-08-10 — 4-config gate GREEN: win-debug/asan full ceir 513/513 (+14b lowering 3/3); linux-gcc-debug/asan 518/518 (+CUDA 2/2); opgen `--check`; -Werror=switch; crd-ceir-invariants(I3/I4/I5); LLVM-20 tidy. Dynamic state NAMED-FORWARD (no command-model verb). Was DESIGN-LOCKED 2026-08-10** (advisor + 4 checks; design note §"CEIR-14b design-lock"): `render.draw` + `render.draw_indexed` (`kernel_ref={symbol=program}`, counts=index operands const-folded [dynamic→UnsupportedCommand], `first_vertex`/`first_index` int attrs, draw_indexed order counts→index_buffer→variadic bindings+access; RESULTLESS GPUCommand+ambient MemoryReadWrite). Lowering: `BeginRender`/`Draw`/`EndRender` widened at END, `lower_region` RECURSES render.scope → begin/body/end, barriers at SCOPE granularity; `execute_lowered` rejects render kinds TYPED (the Transfer `UnsupportedCommand` mirror — 14z raster executor consumes). Verifier +`DrawOutsideScope`/`NestedRenderScope`/`ComputeInRenderScope`/`ProgramNotSymbol`/`DrawCountNotIndex`/access-arity (`scan_render` gains `in_scope`). ⭐ **LOAD-BEARING 14b test:** the region-interior binding hazard hole (dispatch-writes-B → scope-draw-binds-B → assert a barrier before the scope; the flat 13d walk sees only scope operands — the scope's ambient MemoryReadWrite must cover it, NEVER exercised by a region op). ⛔ **CHECK 2 narrowed the slice:** the command model has NO viewport/scissor verbs → dynamic state (viewport/scissor/shading-rate) is NAMED-FORWARD (the cook-only-gates-ship scar), NOT 14b. 3-tick build-up: (i) TOML+verifier+tests · (ii) lowering+full-consumer-audit+hazard-test · (iii) gate. **part (i) ✅ DONE + gated 2026-08-10:** `render.draw`/`render.draw_indexed` + `find_render_misuse` extended (9 new kinds; `scan_render`→`scan_render_region(…,in_scope)`; `ceir_check_draw` reuses scan_dispatch helpers; `ceir_op_is_gpu_command` via `op_effects`) + `test_render.cpp` +3 (positive draw+indexed · scope-with-draw round-trip · 8-negative matrix). Gate GREEN: win-debug/asan ceir 510/510 + 14 7/7; linux-gcc-debug/asan 515/515; opgen; -Werror=switch; tidy. **part (ii) ✅ DONE 2026-08-10 (win-debug green):** `LoweredKind` widened (BeginRender/Draw/EndRender); `lower_region` handles `render.scope` (BeginRender + `lower_scope_body` + EndRender; barrier gather runs for the scope); `execute_lowered`/`validate_lowered`/`execute_lowered_cpu` reject render kinds TYPED. ⭐ **the LOAD-BEARING hazard-hole test PASSES** — the scope's ambient MemoryReadWrite hazards vs a dispatch's write (barrier before the scope) despite the flat walk seeing only the scope's attachment operand → the ambient covers the region-interior binding hole, NO fix needed. +3 device-free tests. win-debug ceir 513/513 + tidy. **NEXT: part (iii) the full 4-config gate (win-asan + Linux + rebuild vulkan/dx12/cuda for the harness change) → 14b CLOSE.** | §40 |
| ✅ | CEIR-14c | Indirect + indirect-count + mesh/task dispatch (the DrawIndex scar as a lowering-level ctest). **✅ CLOSED 2026-08-10 — 4-config gate GREEN: win-debug/asan full ceir 517/517 (+14c 4/4); linux-gcc-debug/asan 522/522 (+CUDA 2/2); opgen `--check`; -Werror=switch; crd-ceir-invariants; tidy(4). part (ii): the lowering-shape test (draw+indirect+mesh→BeginRender+3 Draws+EndRender, proving the EFFECT predicate auto-handles the new ops) + the DrawIndex-PRESERVATION ctest (max_draws=37 + args survive lowering; the push named-forward to 14z). part (i) was:** 4 ops authored (`render.draw_indirect` [args, max_draws=DrawIndex-range], `render.draw_indirect_count` [args+count], `render.mesh_dispatch` [3-grid], `render.mesh_dispatch_indirect` [args]; `kernel_ref={program}`, static MemoryRead on args/count; opgen `--check` clean). Verifier: `ceir_check_draw` REFACTORED to a `DrawShape` descriptor + `draw_shape_of` (THE authoritative draw-op name list) + 3 new kinds (IndirectArgsNotBuffer/IndirectCountNotBuffer/MaxDrawsInvalid). ⭐ **the 14c two-list fragility RESOLVED:** `lower_scope_body` switched to an EFFECT predicate (`op_emits_draw` = any GPUCommand op in a scope is a draw — the verifier's ComputeInRenderScope guarantees it), so the lowering needs NO draw-op name list; adding a draw op is a ONE-place edit. `test_render.cpp` +2 (indirect+mesh positives · IndirectArgs/IndirectCount/MaxDraws negatives). **⛔ DrawIndex scar = a PRESERVATION test at the IR layer** (max_draws + args identity survive lowering); the executor-pushes-the-row assertion + the forced-value probe are NAMED-FORWARD to 14z (the scar warns cheap checks can't see the failure). **NEXT: part (ii) — the lowering-shape test (indirect/mesh → Draw) + the DrawIndex-preservation ctest + the 4-config gate → 14c CLOSE.** | §40 |
| ✅ | CEIR-14d | Resource-table binding semantics over RAH-2's resident tables. **✅ CLOSED 2026-08-11 — a THIN verifier-proof slice (advisor-scoped: no vocabulary without a customer).** `TypeKind::ResourceTable` + `type_resource_table` already exist (CEIR-3c) and `ceir_is_resource_kind` already covers it (CEIR-12a) — so a §157 resident table already DECLARES + BINDS + HAZARDS through the existing machinery. Proven by 2 device-free tests (`test_execute.cpp`): (a) a table binding participates in the PRECISE per-resource hazard (write-T → read-T = a RAW barrier with `resource == resource_root(T)`, not the ambient); (b) a table declare + a dispatch binding it verify (`find_dispatch_misuse` None) + the ResourceTable type round-trips text==builder. ⛔ NAMED-FORWARD (12a — no op without a consumer): in-KERNEL table indexing (`table[id]`) = CKIR descriptor/nonuniform indexing (not a host CEIR op); host-side element extraction → the row where a consumer exists; the `BindingKind::ResourceTable` enumerator + the type→kind derivation → CEIR-14z/16 (widen-enum-audit pre-flagged; ⛔ NOT the deprecated BindlessTextureArray, §156). Gate: win-debug/asan 519/519; linux-gcc-debug/asan 524/524; tidy. | §156 §157 |
| ✅ | CEIR-14z | **Proof:** triangle · MRT (typed clears asserted per-target) · depth-only · indexed-indirect(-count) · mesh dispatch — pixel-asserted both backends, ValidationCapture-silent. ◧ **FORK RESOLVED — Option A (test-surface encoder bridge) RATIFIED by the user 2026-08-11; unify at CEIR-15/16. 14z-0 decision packet: `docs/design/ceir-14z-render-execution-proof.md`. NEXT = 14z-1 (the device-free materializers).** 14a–14d are device-FREE; 14z is the FIRST render DEVICE execution → a RASTER EXECUTOR consuming BeginRender/Draw/EndRender via `ICommandEncoder` (materialize `RenderingDesc` from attachment ops + `RasterDrawPacket` from draw ops; `IRasterProgram` [DISTINCT from IGpuProgram] via `create_raster_program(vs,fs)`; both backends, pixel-asserted). ⛔ **THE FORK (user's call, ADR-0108-flip class):** a CEIR raster executor is a SECOND PRODUCER into the ⛔⛔⛔ only-authored-frame-graphs machinery — how a CEIR render program relates to authored graphs (become a graph? drive an encoder beside it? replace executor registration?) is the CEIR-15 (FrameGraph unification) + CEIR-16 (Executor migration) direction decision the user has NOT made. Options in the packet: A test-surface encoder bridge now + unify at 15/16 (⭐ recommended, reversible, matches 13z-compute); B lower-into-authored-graph now (bigger, commits direction early); C drive-beside (rejected — violates single-path). ⛔ do NOT resolve autonomously. Conditional technical decomposition (if A): 14z-1 materializers (device-free) → 14z-2 execute_render_lowered+FakeEncoder → 14z-3..7 per-shape device proofs (triangle→MRT→depth-only[⛔≠forward scar]→indexed-indirect[+DrawIndex push]→mesh). **14z-1 ✅ DONE + gated 2026-08-11 (win 521/521, linux 526/526):** `render_materialize.{hpp,cpp}` in crd-ceir-gpu — PURE `materialize_rendering_desc(scope op, RasterTargetResolveFn)→RenderingDesc` (attachment ops→ColorAttachmentDesc/DepthStencilAttachmentDesc; LoadOp/StoreOp/BlendMode/DepthCompare/ClearKind mapped from the CEIR string attrs; ⭐ RAH-1a.1 thread PROVEN: `clear_kind=uint`→`ClearKind::Uint`+`clear_uint`; depth attachment goes to `.depth` not `.color[]`) + `materialize_draw_packet(draw op, RasterProgramResolveFn→IRasterProgram*)→RasterDrawPacket` (RasterCommandKind+GeometryKind per op name; counts const-folded; first_vertex/index/max_draws from attrs — max_draws survives). 2 device-free tests (`test_execute.cpp`, sentinel resolvers). ⛔ bindings + index/args-buffer pointers → 14z-2 (a binding resolver). **14z-2 ✅ DONE + gated 2026-08-11 (win 522/522, linux 527/527):** `execute_render_lowered(ctx, cmds, ICommandEncoder&, target/program resolvers)` (render_materialize.cpp) walks the lowered list — BeginRender→`materialize_rendering_desc`→`encoder.begin_rendering`; Draw→`materialize_draw_packet`→`encoder.draw`; EndRender→`encoder.end_rendering`. ⛔ Barrier INERT (encoder-surface barriers are the frame graph's — 15/16); Dispatch/Transfer→`UnsupportedCommand` (render-only executor; mixed=15/16); null program/target→`UnresolvedProgram` (new ExecuteError). Device-free `FakeEncoder` test (begin×1/draw×2/end×1 + captured descs + the null-program error path). **The whole device-FREE render execution path now works: lower_region→execute_render_lowered→ICommandEncoder. 14z-3 ✅ DONE + gated 2026-08-11 — ⭐ THE FIRST RENDER PIXELS FROM CEIR.** A shared, backend-neutral CEIR program (`tests/gpu-shared/ceir_render_triangle.hpp`: `func main { render.scope(color_attachment{load=clear, clear=(0,0,1,1) blue}) { render.draw(3,1) @tri } }` + the `run_ceir_render` lower→execute wrapper + identity-sentinel resolvers, Catch2-FREE like ckir_raster_triangle.hpp) is lowered by `lower_region` and driven through `execute_render_lowered` onto a REAL raster `ICommandEncoder`, drawing the shared CKIR triangle (`build_triangle_vs/fs`): centre=RED (inside), corner=BLUE (clear). `tests/ceir-gpu-vulkan/test_ceir_render_vulkan.cpp` (the device proof — `create_vulkan_raster_context(*vk)`, ValidationCapture-SILENT error+warning=0 — PLUS the device-FREE lower-shape guard [find_render_misuse clean + [BeginRender,Draw,EndRender]] = the 4-config always-runs leg) + `tests/ceir-gpu-dx12/test_ceir_render_dx12.cpp` (the standalone-rig DX12 mirror). ⛔ mirrors the 13z harness split: shared Catch2-free builder+wrapper; per-backend rig+WARN-skips+pixel asserts. ⭐ the device draw RAN (not skipped) on Windows-Vulkan (real GPU) + Windows-DX12 (real GPU) + Linux-Vulkan (llvmpipe). Gate GREEN (full ceir family re-verified via ctest, not per-slice): win-debug 535/535 + win-asan 535/535 (incl. the 3 new: device-free guard + Vulkan device + DX12 device); linux-gcc-debug 529/529 + linux-gcc-asan 529/529 (incl. the 2 new Vulkan; CUDA family green on the real WSL GPU); LLVM-20 tidy clean (3 files). **⚠ 14z-4/14z-6 MECHANISM DISCOVERY 2026-08-11 (design note §"14z-4/14z-6 MECHANISM DISCOVERY"): MRT + indexed-indirect are FRAME-RECORDING verbs → a proof-list fork.** `execute_render_lowered` drives a STANDALONE synchronous `ICommandEncoder` (Option A), but `draw_storage_mrt` (per-target `color[]` clears) is `if (!frame_recording()) return;` on BOTH backends — no-ops synchronously (the corrected RAF-7 scar: MRT/indexed-indirect/shadowed/bindless are frame-recording verbs, "the graph is the only consumer"). The only synchronous MRT path is `draw_gbuffer` (bundled IGBufferTarget) — and it applies ONE shared clear to all attachments, so "typed clears asserted PER-TARGET" is NOT expressible on the encoder surface. ~~DECISION: name-forward MRT+indirect to 15/16~~ **⛔ OVERRULED by the user's verdict 2026-08-11 (gold-standard, no shortcuts; change the engine / improve former decisions / solve bugs immediately). RATIFIED: the CEIR render executor DRIVES FRAME-RECORDING MODE** (both backends have `create_frame_graph` — Vulkan REN-1 + DX12 `Dx12FrameGraph`; the "DX12 until port" comment was stale), so EVERY render shape proves device-side via its REAL body — MRT with GENUINE per-target clears. This makes the Option-A synchronous-encoder decision BETTER (user-authorized). PLAN (design note §RATIFIED PLAN): **14z-4a** `execute_render_frame(ctx,cmds,IRasterContext&,resolvers)` in crd-ceir-gpu — owns fg-create+imports, ONE PASS per render.scope (writes=targets, reads=storage; fg derives barriers), record fn reuses the existing `execute_render_lowered` walk UNCHANGED; prove the TRIANGLE through it first, both backends (mirror `crd::rendergraph::execute_frame`). **14z-4b** FIX the per-attachment typed-clear engine gap (`command_lowering.hpp:139/365` collapses all clears to `color[0].clear` + one `ClearColor` into `draw_storage_mrt`; thread the full kind+float+uint per attachment like the `blends[]` idiom → both backends; ⛔ virtual-sig change ⇒ widen-audit EVERY override+caller incl. `enc_draw_storage_mrt` + the stub in `tests/scene-render/test_scene_render.cpp`). **14z-4c** the MRT device proof (MRT arm `command_lowering.hpp:355` = color.size≥2 + StoragePull/`*buf` ⇒ the CEIR draw needs a binding ⇒ the 14z-1 binding resolver enters scope; distinct per-target clears + `build_gbuffer_two_output_fs` RED@0/GREEN@1; +uint-mixed-attachment feasibility). Then 14z-5 depth-only[⛔≠forward] → 14z-6 indexed-indirect[+DrawIndex push] → 14z-7 mesh — all through the frame-recording drive → band-14 closes, NO name-forward. ⛔ lowered Barriers stay INERT; CEIR-barrier↔fg-barrier reconciliation = CEIR-15/16. **14z-4a ✅ DONE + gated 2026-08-11:** `execute_render_frame(ctx,cmds,IRasterContext&,IAllocator&,resolvers)` in crd-ceir-gpu (render_materialize.cpp) — fg-create + per-`render.scope` import+ONE-PASS (writes=targets; fg derives barriers+readback), record fn reuses `execute_render_lowered` UNCHANGED on the scope slice through the frame-recording encoder; per-scope closures in a RESERVED Array (stable ptrs). `ExecuteError` +NoFrameGraph +FrameBuildFailed (widen-audit clean; gcc -Werror=switch green). Shared `run_ceir_render_frame` twin. The TRIANGLE renders RED through the frame graph on Vulkan + DX12 (+ llvmpipe), ONE submission, ValidationCapture-silent; the synchronous 14z-3 proof stays (additive). Gate: win-debug/asan 537/537, linux-gcc-debug/asan 530/530, LLVM-20 tidy(7). **14z-4b ✅ DONE + gated 2026-08-11 (the per-attachment typed-clear ENGINE FIX):** moved `ClearKind` → raster_context.hpp + added `struct AttachmentClear{kind,color,uint_value,blend}`; both backends' `draw_storage_mrt` take `const AttachmentClear*` (per-attachment typed clear+blend, WBOIT multiplicative-identity OVERRIDES caller); `command_lowering.hpp:355` builds the array from RenderingDesc.color[i]. ⛔ non-virtual duck-typed verb ⇒ audit = 2 backends + 1 template call site (render-graph mentions in comment only; StubRaster returns null encoder). PROOF: strengthened the RAF-7 MRT gate (`test_frame_graph_gpu.cpp::run_mrt_gpu`, BOTH backends) — c0 clears BLUE / c1 clears RED ⇒ corners read blue@0/red@1 DISTINCT (a shared-clear broadcast would match them) + centres RED@0/GREEN@1 (behaviour preserved). Gate GREEN: win-debug raf7 both + Vulkan [frame-graph] 52/884 + raf2 command-encoder 8/8 (the `command_lowering` mapping consumer); win-asan raf7 both; linux-gcc-debug/asan raf7 Vulkan + ceir render 13/13 (gcc -Werror compiled the move+sig); tidy(6). ⭐ the WBOIT multiplicative-identity clear OVERRIDE is exercised through the new signature by `run_mrt_blend_gpu` (RevealageMultiply reveal attachment cleared-to-1), called from BOTH raf7 cases — passed both backends. ⛔ UINT arm implemented (Vulkan VkClearValue.uint32; DX12 draw_visbuffer bit-reinterpret) but proven at 14z-4c; heterogeneous uint@0+float@1 MRT (pass_pso single rt_fmt) = 14z-4c. **14z-4c(2-scope + c1) ✅ DONE + gated 2026-08-11.** (a) The 2-SCOPE test (execute_render_frame N-scope closure path, 11b MIRROR shape, ASan-clean). (b) ⭐ **THE FIRST CEIR PROGRAM TO DRIVE MRT** — `render_draw_binding_start` EXPORTED from crd-ceir (over the internal `draw_shape_of`, one source); `RasterBindingResolveFn` + optional binding params threaded through materialize_draw_packet (binding tail → ResourceBinding{Object,StorageBuffer,ordinal,buffer}; null→typed-fail; slot=ordinal) + execute_render_lowered + execute_render_frame; storage-buffer contract documented (NOT fg-tracked; hazard=15/16). A CEIR `render.scope{color0{BLUE},color1{RED}}{render.draw(3,1,%vbuf)}` drives `draw_storage_mrt` via `execute_render_frame` — color0 RED centre/BLUE corner, color1 GREEN centre/RED corner (distinct corners⇒per-attachment clears[4b]; distinct centres⇒MRT ordering; %vbuf⇒binding resolver⇒StoragePull arm), both backends+llvmpipe, ValidationCapture-silent. Composes 14z-4a executor + 14z-4b clears + 14z-4c bindings. Gate: win-debug/asan 541/541, linux-gcc-debug/asan 532/532, tidy(7). **14z-4c(c2) ✅ DONE + gated 2026-08-11 — uint-HOMOGENEOUS MRT + a real DX12 ENGINE BUG FIX.** 2× R32_UINT attachments (create_visbuffer_target) + build_visbuffer_two_output_fs (ids 7/9) + build_ceir_mrt_uint_render (uint images type_int(32,false), clear_uint 100/200; ceir_append_mrt_body DRY-extracted) drive draw_storage_mrt's uint arm — color0 centre7/corner100, color1 centre9/corner200 (distinct corners⇒per-attachment TYPED clears; distinct centres⇒MRT ordering). ⭐ the FIRST NON-ZERO DX12 uint clear CAUGHT the 14z-4b bug: the DX12 uint arm was BIT-REINTERPRET (memcpy→float≈0→value-converts to 0); FIXED to VALUE-CONVERT (static_cast<float>(v), exact <2^24). Vulkan (VkClearValue.uint32) already correct. Gate: win-debug/asan ceir 543/543 + raf7 7/7 (float MRT/WBOIT intact), linux-gcc-debug/asan ceir+raf7 539/539 (uint MRT on llvmpipe), tidy(5). **14z-4c(c3) ✅ DONE + gated 2026-08-11 — HETEROGENEOUS uint@0+float@1 MRT + a real DX12 per-attachment-format ENGINE FIX. 14z-4 (MRT) COMPLETE both backends.** pre-checks: `scan_render_region` has NO format-agreement check (mixed scope accepted) + the HLSL emitter types each output independently (`uint o0:SV_Target0`+`float4 o1:SV_Target1`). `build_gbuffer_uint_float_fs` + `build_ceir_mrt_mixed_render` (color0 R32_UINT+uint clear, color1 RGBA8+float clear). ⭐ **VULKAN passed as the CONTROL with ZERO engine change** (formats derived from per-attachment views ⇒ c3 was DX12-ONLY): color0(uint) 7/100, color1(float) GREEN/BLUE. ⭐ **DX12 ENGINE FIX (per-attachment RTV formats):** `pso_for`/`build_graphics_pso` broadcast ONE `rt_fmt` to all RTVFormats → a mixed scope got the wrong PSO. Fix: both gain `const DXGI_FORMAT* rt_fmts=nullptr` (`RTVFormats[i]=rt_fmts?rt_fmts[i]:rt_fmt` — nullptr broadcasts, byte-identical homogeneous path); ⛔ the PSO CACHE identity done the `state` way — `pso_for` materializes the full `DXGI_FORMAT fmts[8]`+`all_default` (fast-path/u32-key use `all_default`, key stays a prefilter — content-hash scar), the cache compare adds `std::memcmp(rt_fmts, fmts, nf*...)`, store `std::memcpy`, `PsoCacheEntry` gains `DXGI_FORMAT rt_fmts[8]{}`, `pass_pso` threads it, `draw_storage_mrt` builds the array from each target's `color_format()`. DX12 mixed proof PASSES (15 assertions, uint 7/100 + float GREEN/BLUE). ⚠ `build_mesh_pso`/line ~241 has a 2nd RTFormats fill site hardcoding kColorFormat — NAMED-FORWARD (no mesh-MRT consumer; 14z-7). Gate (4 configs): win-debug ceir 545/545 + raf7 7/7 + dx12-raster 100 cases (regression-free), win-asan ceir 545/545 + raf7 (pso_for ASan-covered via mixed+raf7+c1/c2), linux-gcc-debug 540/540, linux-gcc-asan 540/540 (Vulkan mixed on llvmpipe), tidy(5). **14z-5 ✅ DONE + gated 2026-08-11 — DEPTH-ONLY by DEPTH-TEST OCCLUSION (attachment-only, no sampling — advisor verdict); caught+fixed TWO real 2-backend engine bugs.** Proof (2 scopes, 1 color+depth target T): scope 0 = a genuine DEPTH-ONLY pass (`draw_storage_depth_only`, an n_out=0 `build_depth_only_const_fs(0.5)` — the ⛔ depth-only≠forward scar honored by construction) writes depth 0.5 under the shared triangle, 1.0 elsewhere; scope 1 fullscreen-tests frag_depth 0.75 (LessEqual) against the LOADED depth ⇒ centre BLUE (0.75>0.5 fails), corner RED (0.75≤1.0 passes). ⭐ **BUG 1: `record_plain` (the frame-recording path for draw/draw_depth) HARDCODED the depth loadOp to CLEAR** (both backends), ignoring `m_next_load_depth` — so a fullscreen depth-tested draw always re-cleared depth; the occlusion proof fired its designed-for failure FIRST run. Fixed: read+consume the flag (mirror the 8 scene-verb sites). ⭐ **BUG 2 (pre-existing leak the broader linux-asan run surfaced): `free_transients` deleted transient IMAGE wrappers but NOT the transient BUFFER wrapper** (`new VulkanTransientBuffer`/`new Dx12TransientBuffer`) — 24-byte leak per graph rebuild, both backends (LeakSanitizer named it via REN-38 + SceneRenderer::render). Fixed: `delete n.buffer` gated on n.transient (imported nodes borrow). Harness (test-only): `build_color_depth_fs` (n_out=1+frag_depth), `build_ceir_depth_occlusion_render` + per-op `CeirProgramMap`/`ceir_mapped_program_resolver` + `run_ceir_render_frame_depth`. Gate (4 configs): win-debug/asan ceir-14z 18/18 + raf7 7/7 + REN-3 178/178 + REN-38 117/117 + command-encoder 4/4; linux-gcc-debug/asan 688/688 (1 VRS skip) — LeakSanitizer CLEAN; LLVM-20 tidy(6). **14z-6 ✅ DONE + gated 2026-08-11 — INDEXED-INDIRECT(-count) with the REN-40 DrawIndex-PUSH proof, both backends (c1+c2).** ADVISOR: prove by per-sub-draw DrawIndex POSITIONAL split (no first_draw_index attr — runtime scene state, 15/16 fwd; g.first_draw_index stays 0). c1: added an `index_offset` attr to render.draw_indirect/_count (opgen regen, drift-clean); the materializer resolves op0→g.args_buffer + op1→g.count_buffer + reads index_offset→g.index_offset (the FIRST operand-keyed multi-buffer resolver CeirBufferMap — the 4c identity sentinel would now fail). Proof: render.draw_indirect(%args,%vbuf){max_draws=2} + build_vertex_pull_drawindex_vs (X-shift by the pushed SV_DrawIndex) + a 2-identical-command args buffer ⇒ draw 0 LEFT / draw 1 RIGHT (both RED, corner BLUE) — right-RED proves SV_DrawIndex=1 was PUSHED. ⛔ the advisor's multi_indexed_batch_count() assertion DROPPED (verified the REN-40 indirect verb ticks NO such counter — it's the CPU-multi verbs'; the positional split discriminates every no-op mode instead). c2: render.draw_indirect_count(%args,%count,%vbuf) run twice — count=2 both RED / count=1 right BLUE ⇒ the device count GATES execution (indirect_count_supported() guard). ⛔ args buffer is BACKEND-specific (indirect_command_stride/arg_offset; DX12's leading u32 = DrawIndex). Gate (4 configs): win-debug/asan ceir 551/551 + 14z-6 4/4 (opgen drift clean), linux-gcc-debug/asan 543/543 LeakSanitizer CLEAN, tidy(6). **14z-7 ✅ DONE + gated 2026-08-11 — MESH DISPATCH, the LAST band-14 shape.** A CEIR render.mesh_dispatch(1,1,1) drives draw_mesh (a PROCEDURAL mesh shader, build_triangle_mesh — one meshlet) through the frame graph: centre RED, corner BLUE, both backends. ⛔ the mesh program = create_mesh_program (distinct factory; draw_mesh no-ops on a non-mesh program via is_mesh()). Mostly pure-harness (materializer→Meshlet→draw_mesh + record_mesh pre-existed). ⭐ ONE engine addition (advisor-caught): the mesh_dispatch op is 3D but every Meshlet verb consumes group_count_x ONLY — a y/z>1 grid would SILENTLY draw (gx); materialize_draw_packet now REFUSES a y/z≠1 grid LOUDLY (→UnsupportedCommand, the dynamic-grid precedent) + a device-free negative pins it. ⚠ API-STEP-DOWN NAMED-FORWARD: draw_mesh leveled the 3D device APIs (vkCmdDrawMeshTasksEXT/DispatchMesh both 3D) to 1D — a verb widen-audit with no y/z consumer, the user's call. ⛔ render.mesh_dispatch_indirect (MeshletIndirect) device consumer = named-forward (§169 "mesh dispatch" = the DIRECT op, proven; the indirect mechanism is device-proven at 14z-6). ⭐ mesh RAN on llvmpipe (both linux configs, ~0.4s — NOT skipped). Gate (4 configs): win-debug/asan ceir 554/554 + 14z-7 3/3, linux-gcc-debug/asan 545/545 (mesh on llvmpipe) LeakSanitizer CLEAN, tidy(5). **⭐⭐⭐ BAND-14 COMPLETE — see the CEIR-14 header. NEXT = CEIR-15 (FrameGraph unification), an architecture band: opens with a §126 decision packet, not code.** | §169 |

## CEIR-15 — FrameGraph unification — ✅ **CLOSED 2026-08-11** (all 15a–15z ✅ 4-config; the FrameGraph is a CEIR dialect gated by validate_ceir_frame, old path deleted §121, ADR-0106 amended) (decision packet `docs/design/ceir-15-framegraph-unification.md`; advisor-consulted; sources read: master roadmap §39 [ceir.frame owns the full topology model] + §126 [8 migration steps = 15a–15z 1:1], ADR-0106 [superseded @ 15f — frontends survive], ADR-0109 §4.2 [crd-ceir host-only ⇒ crd-frame-cook→crd-ceir is acyclic]). ⭐ **The frame asset is ALREADY CEIR-shaped** (the RAF-12.3 §7 fold: a pass = executor-id + reads/writes + typed param bag = identity + operands + attrs) — CEIR-15 is a PROMOTION, not a rewrite. **Dialect LOCKED (FOUR ops, advisor-refined 2026-08-11): `frame.graph` region + `frame.pass` + `frame.draw_list` + `frame.history`; RESOURCES REUSE `resource.declare`/`import`** (NOT a new frame.resource op — the 12b/c/d planner is built over declare's attrs; the 11 FrameResourceKinds decompose onto lifetime={transient|persistent|history} + import + frame-scoped OPEN attrs; PingPong = lifetime=history,history_length=1). `frame.pass`: executor = a SYMBOL attr (ExecutorTypeId derived by hash — never one-op-per-executor); ⛔ reads/writes = ONE variadic `resources` tail + an `access` {r|w|rw} token string (opgen: only the last operand may be variadic — the compute.dispatch/render.draw pattern); resources-as-operands ⇒ CEIR's hazard/lifetime machinery DERIVES the barriers (the §159 realization). `frame.history(R)→underlying-type` = the Fork B read op (B1, RESOLVED): resource_root chases only resource.view by op kind, so a distinct op's result is its own root ⇒ the false-intra-frame-RAW fix is STRUCTURAL. **Checks ran:** AttrKind has NO Vec4 (decompose, 14a precedent); CEIR attrs are OPEN (opgen verifies only required) ⇒ the param bag maps to attrs directly, app params validated at cook against the EXECUTOR schema. References = OPTIONAL SYMBOL attrs on frame.pass (⛔ NOT kernel_ref — executor is a MECHANIC name, no interface hash; kernel_ref is one-per-op but a pass refs shader+kernel+technique+6 RT programs ⇒ the multi-ref CDEP extraction is NAMED to 15c, not the 15a toml). Migration = a CONVERTER PAIR FrameGraphDesc↔ceir.frame with a ROUND-TRIP-IDENTITY gate (backward converter ⇒ 15e incremental). **Fork B (history/ping-pong hazard) ✅ RESOLVED = B1** (frame.history read op, structural via resource_root by-op-kind). Births **ADR-0127** (ceir.frame + converter placement F1 = in crd-frame-cook). **15a-1 ✅ dialect COMPILES + 15a-2 ✅ DONE + gated 2026-08-11** — `frame.ceirop.toml` (4 ops: frame.graph/pass/draw_list/history), opgen (drift-clean, validator 59/59), `frame.hpp`/`frame.cpp` (register_dialect + `draw_list` marker type-class); **`Context::find_frame_misuse`** (the find_render_misuse mirror, `scan_frame_region` — 13 kinds: PassOutsideGraph/ForeignCommandInGraph/ExecutorNotSymbol/PassAccess{Invalid,Arity}/PassOperandNotResource/ForEach/Queue/DrawListCull/Sort/Limit/HistoryOperandNotHistory/HistoryFramesBackInvalid; reuses ceir_render_string_ok/ceir_parse_access/ceir_is_resource_kind/ceir_op_is_gpu_command; the frame.history operand's lifetime=history-declare identity via defining_op). `test_frame.cpp` (positive + §121-text round-trip + 11-section negative matrix). ⛔ CAUGHT: the tests/ceir CMakeLists lists sources EXPLICITLY (not glob) — test_frame.cpp was silently NOT compiled (only the opgen-generated frame SMOKE tests rode in via ${CEIR_GENERATED_TESTS}); the first "gate" ran only smoke tests. Fixed (added test_frame.cpp) — the exe now lists the 3 real tests (ctest #458-460). Gate (4 configs, REAL tests): win-debug/asan full ceir 432/432 (3272 assertions) + [frame] 6/6 (45 assertions); linux-gcc-debug/asan 432/432 (gcc -Werror=switch clean, LSan-clean); tidy(5). **15a-3a ✅ DONE + gated 2026-08-11 — the FORWARD converter FrameGraphDesc→ceir.frame + ADR-0127.** ⭐ **ADR-0127 ACCEPTED** (the ceir.frame dialect + the converter placement): the converter lives IN crd-frame-cook (a new `crd-frame-cook → crd-ceir` link edge — acyclic by construction, crd-ceir is host-only per ADR-0109 §4.2; NO bridge needed for a host-to-host cook converter). `frame_ceir.{hpp,cpp}` `to_ceir_frame(desc, ctx)→Module`: resources→resource.declare/import (kind→lifetime {transient|persistent|history}/import + a `name` attr for asset-id/round-trip; PingPong→history+history_length=1), draw_lists→frame.draw_list (all/any/none comma-joined + cull/sort/limit), passes→frame.pass (executor NAME as a symbol — a 14-builtin id→name table + the custom p.executor; reads/writes resolved by a NAME→Value map → the `resources` operand tail + `access` w/r tokens; the kDrawList param → a draw_list operand; for_each/queue + shader/kernel/technique ref params → symbol attrs). `test_frame_ceir.cpp` (a FrameGraphBuilder scene → to_ceir_frame → find_frame_misuse CLEAN + the ceir text round-trips print==parse-print). ⛔ re-hit last tick's lesson: tests/frame-cook CMakeLists is EXPLICIT — added test_frame_ceir.cpp (verified --list-tests shows the 2 cases). Gate (4 configs): win-debug/asan [ceir][frame] 2/2 (7 assertions) + full crd-frame-cook 27/27 (681); linux-gcc-debug/asan same (gcc = the new edge acyclic, LSan-clean); tidy(3). **15a-3b-1 ◧ forward-complete 2026-08-11 (win-debug verified):** the forward converter now stores the FULL round-trip field set so the backward can recover a byte-identical desc — added `name` attrs to frame.pass + frame.draw_list (toml + opgen regen, drift-clean) for pass/draw-list name recovery; the resource.declare/import now carries `frame_kind` (the EXACT FrameResourceKind as its enum int — lifetime alone is lossy) + `format` (the FgImageFormat enum int — no format-string mapping to expose; emit reads the reconstructed enum) + width/height/scale/layers/samples/mips/sampled/storage/depth_buffer/no_alias/resizable/stride/count/size_bytes (non-defaults only). **15a-3b ✅ DONE + gated 2026-08-11 — the converter PAIR is LOSSLESS (the round-trip-identity gate is GREEN).** `from_ceir_frame(ctx, m, alloc, out)` reconstructs the FrameGraphDesc from the ceir.frame module: name/schema from the frame.graph attrs; resources from the resource.declare/import attrs (name/frame_kind/format/sizing/flags); draw_lists from name/all-any-none-split/cull/sort/limit; passes from name/executor (→executor_id via crd::renderpass::executor_type_id, +p.executor iff !is_builtin) + reads/writes recovered from the `resources` operands' `access` tokens + the operands' `name` attrs (a frame.draw_list operand → the kDrawList param) + for_each/queue/shader/kernel/technique. ⭐ **THE ROUND-TRIP-IDENTITY GATE**: `emit_frame_toml(desc) == emit_frame_toml(to→from(desc))` — a FrameGraphBuilder scene survives desc→ceir→desc′ BYTE-IDENTICALLY (FrameGraphDesc has no operator==; the emitted TOML is its canonical form, the §121 precedent). ⛔ **CAUGHT+FIXED a generalizable CEIR-reader bug**: `attr_value(an INVALID AttrId)` returns a default AttrValue (kind=Int, i=0), so a naive `kind==Int?i:def` reads an ABSENT attr as 0, not the caller's default — layers/mips/samples round-tripped 1→0. Every attr reader now checks `op->attr(name).valid()` first (banked as a scar; a round-trip gate is what catches this class — a forward-only test never exercises "absent⇒default"). Gate (4 configs): win-debug/asan [ceir][frame] 3/3 (10 assertions) + full crd-frame-cook 28/28 (684) + ceir [frame] 6/6 (45); linux-gcc-debug/asan same (gcc, LSan-clean); tidy(3). ⭐ **15a-3 (converter PAIR) + 15a + 15b all COMPLETE — both §126 frontends (.frame.toml @ 15a-3a, FrameGraphBuilder @ 15b) END at byte-identical ceir.frame. NEXT = 15c (the cook VALIDATIONS → CEIR verifiers: the 38-D4-class varying-contract/header-word/shape checks + the executor-schema param validation become CEIR verifiers, each a POINTING diagnostic — §126 step 3), DEVICE-FREE.** → §39 §126 §159

**Band contract.** The §126 eight steps, seam by seam. `frame-cook`'s `FrameGraphDesc` parse emits `ceir.frame`
(§39) instead of the private blob; `FrameGraphBuilder` emits the same; the 38-D4-class cook validations become CEIR
verifiers; `FrameGraphTemplate`/`CompiledFrameGraph` (ADR-0106) become the lowering/plan layer; `execute_frame`
runs CEIR plans. **Invariant preserved:** `frame-cook ⊥ crd-scene` via the `IFrameGraphHost` seam (host resolves
ECS→pre-resolved `DrawItem`; CEIR never sees a scene type). **Per-asset parity gate:** every shipped built-in frame
asset (`forward_csm`, deferred, the sandbox frames) renders PIXEL-IDENTICAL through CEIR before its old path dies —
with a deterministic clock (the A/B-needs-deterministic-clock scar). **ADR-0106 struck in place @ 15f.** Asset IDs
preserved. FrameGraph is not deprecated syntax — its semantics (topology/lifetime/history) become a CEIR dialect
(§159).

| ✔ | Slice | Deliverable | → §§ |
|---|---|---|---|
| ✅ | CEIR-15a | **✅ DONE + gated 2026-08-11.** The `ceir.frame` dialect (`frame.graph`/`frame.pass`/`frame.draw_list`/`frame.history`; resources REUSE `resource.declare`/`import`) + the `FrameGraphDesc ↔ ceir.frame` converter PAIR + the round-trip-identity gate (ADR-0127). 15a-1 dialect (toml+opgen+type-class+register) → 15a-2 `find_frame_misuse` (13 `FrameMisuseKind`s) + structural/§121-text tests → 15a-3 the converter pair (`to_ceir_frame`/`from_ceir_frame`) + `emit_frame_toml(desc)==emit_frame_toml(to→from(desc))` gate. DEVICE-FREE, acyclic `crd-frame-cook → crd-ceir`. | §39 §126 |
| ✅ | CEIR-15b | **✅ DONE + gated 2026-08-11.** `FrameGraphBuilder` → same canonical `ceir.frame` as the `.frame.toml` frontend (§126 step 2). Since `to_ceir_frame` is DETERMINISTIC from a FrameGraphDesc and emit→parse is a lossless desc round-trip (the REN-36 gate), a builder-built graph and the SAME graph parsed from its emitted TOML lower to BYTE-IDENTICAL ceir.frame — the "one runtime/compiler, no privileged frontend" contract (§39). `test_frame_ceir.cpp` "ceir 15b" (build via FrameGraphBuilder → path1 = to_ceir_frame(desc), path2 = to_ceir_frame(parse(emit(desc))); assert print(m1)==print(m2)). ⭐ needed a VALID graph (parse_frame_toml VALIDATES, unlike the emit-only round-trip) → build_scene now writes `@output` (PresentSourceInternal caught the old present-reads-a-transient); ADDED `@output` IMPLICIT-EXTERNAL handling to the converter (to_ceir_frame mints a resource.import — NO frame_kind attr — for any pass reference not declared; from_ceir_frame SKIPS an import without frame_kind; the §39 output endpoint). ⛔ the @output import name is a STRING attr not a symbol — a CEIR symbol can't round-trip '@' through text. Gate (4 configs): win-debug/asan [ceir][frame] 4/4 (14 assertions) + full crd-frame-cook 29/29 (688); linux-gcc-debug/asan same + [frame] 6/6; tidy(2). | §126 §121 |
| ✅ | CEIR-15c | **✅ 15c CLOSED + row RE-VERIFIED 2026-08-11 (GATE-re-verifies-rows doctrine: crd-frame-cook 92/92 assertions 2661, 4-config this session — validate_ceir_frame is the full semantic verifier; converter lossless over its flattened-desc input domain).** Cook validations → CEIR verifiers (varying-contract, header-word, shape — each a pointing diagnostic). **15c-0 ✅ DONE + gated 2026-08-11: the `frame.history` ROUTING fix (Fork-B B1) + the ping-pong round-trip LOCK.** `to_ceir_frame` now routes a pass READ of a `lifetime=history` (PingPongImage) resource through a `frame.history` op (a distinct `resource_root` — no false intra-frame RAW vs the same-frame write); `from_ceir_frame` recovers the read name from the wrapped declare. A TAA-history fixture (`build_taa`, the builder makes only transients so the PingPong is pushed directly) locks it: `find_frame_misuse`-clean, a `frame.history` op is present, and the desc round-trips `emit_frame_toml`-identical. `test_frame_ceir.cpp` "ceir 15c" (#3950; advisor-hardened — the fixture is now `fb.validate()==Ok`, absolute-sized PingPong dodging `PersistentNeedsSize`, a draw-list dodging `MissingDrawList`, and a `frame.history` TEXT parse→print fixpoint — the op had never been text-round-tripped). Gate: win-debug/asan [ceir 15] 8/8 + linux-gcc-debug [ceir 15] 5/5 + clang-tidy(win-tidy) `frame_ceir.cpp` clean. **The §115 classification table (every `FrameCookError` → PARSER \| STRUCTURAL-IMPOSSIBLE \| CEIR-ANALYSIS(15d) \| FRAME-VERIFIER \| PROGRAM-CONTRACT + the 6 NEW-IN-CEIR guards) is in `docs/design/ceir-15-framegraph-unification.md` — the 15c-1 driver.** **15c-1a ✅ DONE + gated 2026-08-11: the 4 NEW-IN-CEIR structural guards** (all executor-agnostic ⇒ crd-ceir CORE `find_frame_misuse` kinds, appended at enum end): `OperandOutsideGraph` (a pass/history operand defined outside this frame.graph region — a block-arg or foreign region; `def->parent_block()->parent_region()==r`), `MultipleGraphs` (a module-level pre-order `second_frame_graph` walk, nested graphs included — a FrameGraphDesc is exactly one), `HistoryReadNotThroughFrameHistory` (a pass READS a lifetime=history declare directly — the Fork-B B1 STRUCTURAL teeth), `HistoryWriteThroughHistory` (advisor-caught inverse: a pass WRITES a frame.history result). Per-operand guards ride a lockstep access-token walk (first char `r`⟺read, last `w`⟺write — NOT the backward converter's last-char-wins scanner) placed AFTER PassAccessInvalid/Arity so no existing negative-matrix fixture shifts. `test_frame.cpp` matrix +4 sections (#460/#264); the well-formed test is the guard-3 polarity canary (frame.history-routed read stays None). Gate: crd-ceir 6/6 (win-debug/asan + linux, gcc -Werror=switch) + clang-tidy `context.cpp` clean; ⛔ CONSUMER-relink (crd-frame-cook-tests is a SEPARATE binary vs the crd-ceir static lib — advisor caught the stale-exe trap): rebuilt + `ceir 15` 8/8 win-debug/asan/linux INCLUDING the guard-3 canary #3950/#523 (frame.history-routed read stays None) + win-debug `frame` 129/129 blast-radius sweep. **15c-1b ✅ DONE + gated 2026-08-11: the `rw`-token backward-converter fix (NEW-IN-CEIR §4).** `from_ceir_frame` decoded access tokens last-char-wins, collapsing `rw`→`w` and silently DROPPING the read half; now decodes STRUCTURALLY (first char `r`⟺read, last `w`⟺write) and maps an `rw` operand to BOTH `reads[]` and `writes[]` (the desc expresses RMW as reads∋X + writes∋X; the forward re-canonicalizes to two operands, w+r). The forward never EMITS `rw`, so the test injects it: forward `build_scene` → `set_attr` the `forward` pass' scene token `r`→`rw` → assert find_frame_misuse None, `scene` in both reads+writes, `validate_frame_graph==Ok` (in-place RMW of a transient is cycle-free — the sort skips a==b), and the desc→ceir→desc canonicalization fixpoint (ceir→desc→ceir is NOT byte-identical for rw — it splits to two operands — but stable after one round). `test_frame_ceir.cpp` "ceir 15c: rw" (#3951). Gate: win-debug/asan `ceir 15` 9/9 + linux 9/9 + clang-tidy `frame_ceir.cpp` clean (crd-frame-cook-only ⇒ no consumer trap). Design note NEW-IN-CEIR §9 (access-token sensibility on draw_list operands) parked → 15c-1c. **15c-1c-1 ✅ DONE + gated 2026-08-11: `validate_ceir_frame` — the graph-semantic verifier layer (§115, frame-cook home for cross-op/@output-convention/kind/registry checks find_frame_misuse CORE cannot own).** CEIR-native POINTING diag `FrameSemanticDiag{op, FrameSemanticKind}` (NOT FrameCookError, NOT extending core FrameMisuseKind — a layer violation). 15c-1c-1 rows: `NoOutputPass` (no frame.pass write-token operand defines a resource.import named "@output"; assumes flattened graph) + `DuplicateName` (per-category {declare+import}/draw_list/pass name sweep, points at the SECOND collider, excludes the implicit @output import). Test: differential ORACLE — `validate_ceir_frame(m).kind == X` AND the desc-side cook pipeline `cook_verdict = emit→parse→validate` agrees (`==FrameCookError::X`); positive canaries build_scene + build_taa → None. `test_frame_ceir.cpp` "validate_ceir_frame" (#3952/3/4). Gate: win-debug/asan 12/12 + linux + tidy `frame_ceir.cpp` clean (crd-frame-cook-only). ⛔ Fork RESOLVED (advisor discriminator): core `find_frame_misuse` iff decidable from lifetime/geometric-ints/vocabs w/ NO frame-cook enum or gpu constant, else validate_ceir_frame → BadResourceSize/PersistentNeedsSize/BadMipCount core; StructuredNeedsStride/AccelIsExternal/ExternalTextureIsReadOnly/LayersOutOfRange frame-cook (table SUPERSEDE at 15c-1c-2). ⛔ 15c-1c-2 BLOCKER (task #21): converter is LOSSY for dimension/depth (hardcodes Dim2D) + maybe requires_caps/fallback/memory_budget — audit+carry+re-lock BEFORE any dimension verifier. **15c-1c-2 AUDIT ✅ + 2a ✅ DONE + gated 2026-08-11 (task #21).** ⛔ AUDIT: to_ceir_frame was LOSSY (emit_frame_toml serializes each; the converter carried NONE; the round-trip-identity gate false-greened because build_scene/build_taa set none — the fixture-coverage scar). Confirmed losses: dimension(kind_2d)+depth [REN-38-B2]; FrameResourceRef.indexed [`[$index]`]; the FULL params bag [RAF-12.3 §7 typed payload — only shader/kernel/technique carried]; top-level requires_caps/fallback/memory_budget_bytes. **2a ✅: carried kind_2d(dimension)+depth as int attrs; a 3-D froxel-volume fixture re-locks the round-trip-identity gate + a direct kind_2d==Tex3D/depth==64 check (#3955).** Gate: win-debug/asan 13/13 + linux + tidy clean. REMAINING (task #21): 2b indexed (per-operand encoding), 2c params-bag (BIG, blocks 15d runtime fidelity), 2d top-level caps (simple graph attrs). **15c-1c-2 def-use ✅ DONE + gated 2026-08-11: ResourceNeverWritten + PingPongNeedsBothWays in validate_ceir_frame.** A single graph walk builds per-declare read/wrote flags (chasing frame.history operands back to the wrapped declare — a ceir.frame ping-pong READ is via frame.history, the 15c-0 routing; @output write detected inline), then checks in validate_frame_graph's EXACT order: PingPongNeedsBothWays (a lifetime=history declare not both read+written) → NoOutputPass → ResourceNeverWritten (a lifetime=transient declare no pass writes; persistent/history/import EXEMPT). Both differential-oracle tests AGREE with the desc-side cook pipeline (#3956/7); build_taa (ping-pong both-ways) stays None. Gate: win-debug/asan 15/15 + linux + tidy clean. **15c-1c-2 shape ✅ DONE + gated 2026-08-11: the REN-38-B2 TransientImage shape block in validate_ceir_frame — CubeNeedsSquare + VolumeNeedsDepth + BadMipCount** (declare_shape_diag mirrors frame_asset's block, order Cube→Volume→BadMip, gated on frame_kind==TransientImage; folded into the post-walk resource loop before NoOutputPass, mirroring the desc's single resource-loop order). ⛔ SPLIT REFINED: these are ALL frame-cook (each gated on frame_kind==TransientImage ⇒ needs the enum), so BadMipCount is NOT pure-core as the discriminator's first take suggested. 3 differential-oracle tests AGREE with the desc cook pipeline (#3958/9/60), minimal lone-mis-shaped-resource fixtures (shape fires before NoOutputPass on both sides). Gate: win-debug/asan 18/18 + linux + tidy clean. **15c-1c-2 resource-"may say" ✅ DONE + gated 2026-08-11: LayersOutOfRange + StructuredNeedsStride + StrideNotAligned in validate_ceir_frame** (declare_shape_diag → declare_resource_diag, dispatched by frame_kind: TransientImage→Layers then shape; Structured/Counter→stride==0 then stride%4; desc-order). 3 differential-oracle tests AGREE (#3961/2/3), lone-mis-shaped-resource fixtures. Gate: win-debug/asan 21/21 + linux + tidy clean. validate_ceir_frame now has 10 rows. **15c-1c-2 external-resource ✅ DONE + gated 2026-08-11: AccelIsExternal + ExternalTextureIsReadOnly.** AccelIsExternal = a per-import check (an import with frame_kind==AccelerationStructure carrying size_bytes/width/height/scale). ExternalTextureIsReadOnly = a pass writing an ExternalTexture import — detected DEFERRED during the walk (ext_write_op), returned post-walk in the desc order (shape → Accel → ExternalTexture → NoOutputPass). 2 oracle tests AGREE (#3964/5; the pass-side fixture is raster.geometry WITH a draw list, dodging the MissingDrawList that precedes ExternalTextureIsReadOnly in the desc pass loop). Gate: win-debug/asan 23/23 + linux + tidy clean. **validate_ceir_frame now = 12 rows; ALL oracle-backed resource/pass rows DONE.** **15c-1c-2 NEW-IN-CEIR consistency ✅ DONE + gated 2026-08-11: KindLifetimeMismatch (§6) + UnknownDimension in validate_ceir_frame.** No desc oracle (ceir.frame is a strict superset), so checked at the HEAD of the post-walk resource loop (post-walk keeps DuplicateName winning; per-resource before shape/PingPong which assume a sane kind/dimension). §6 = kind_lifetime(frame_kind) != lifetime (⛔ catches an ABSENT lifetime too — attr_sv reads "" which kind_lifetime never returns; hand-built fixture since the converter always sets it). UnknownDimension = a `dimension` attr whose int is no FgImageKind (explicit membership, ANY image kind). 3 injection tests with the third leg (op pointer + frame_semantic_kind_name round-trip, locking the enum→name switch that had zero direct coverage). Helper generalized find_frame_pass→find_graph_op. Gate: win-debug/asan 26/26 + linux + tidy clean. **validate_ceir_frame now = 14 rows.** **15c-1c-2 §8/§9 ✅ DONE + gated 2026-08-11: HistoryOutsideGraph + DrawListOutsideGraph + DrawListNotWritable in find_frame_misuse CORE** (crd-ceir change). §8 = a `!in_graph` guard on the frame.draw_list + frame.history branches (only frame.pass had PassOutsideGraph). §9 = a draw-list operand with a write token in the frame.pass operand loop (a draw list is queried, never written). `test_frame.cpp` matrix +3 sections. ⛔ CONSUMER-relink observed: crd-frame-cook-tests rebuilt + `ceir 15` 26/26 canary win-debug/asan/linux + win-debug `frame` 129 sweep. Gate: crd-ceir 4-config (win-debug/asan + linux gcc -Werror=switch) + clang-tidy `context.cpp` clean. **All 3 CORE structural frame verifiers = 7 FrameMisuseKinds; validate_ceir_frame = 14 rows. Every §115 cook validation is now a CEIR verifier EXCEPT the executor-schema (program-contract) layer.** **15c-1c-2 size rules ✅ DONE + gated 2026-08-11: BadResourceSize + PersistentNeedsSize in validate_ceir_frame** (loop head, before the shape dispatch). ⛔ PLACEMENT REFINED (supersedes the advisor's "core PersistentNeedsSize"): BadResourceSize is PARSE-time + kind-specific (TransientImage no-size / TransientBuffer no-size_bytes ⇒ needs frame_kind ⇒ frame-cook, oracle via the parse leg); PersistentNeedsSize (persistent/history scale-only unless resizable) is validate-time and lifetime-decidable so it COULD be core, but it is the cohesive sibling of the other resource-size checks + find_frame_misuse has no resource.declare branch ⇒ frame-cook. 2 oracle tests AGREE (#3969/70). Gate: win-debug/asan 28/28 + linux + tidy clean (crd-frame-cook only). **validate_ceir_frame = 16 rows. EVERY §115 cook validation is now a CEIR verifier EXCEPT the executor-schema (program-contract) layer.** **⛔ SEQUENCING REVISED (recon 2026-08-11): 15c-1d is BLOCKED by the params-bag converter loss (task #21 2c)** — the program-contract verifiers read composite/load/blend/count/material_pass/RT-programs, which the converter drops (only shader/kernel/technique/draw_list carried; the whole RAF-12.3 typed bag lost). 15c-1d's only param-independent row = AsyncQueueNeedsCompute. So **2c lands FIRST**. 2c DESIGN-LOCKED as **Fork C-2** (advisor, design note): per-param attrs `p:<name>`(value, f64-bit-pattern for the v[0] family, `p:<name>:0..3` for Vec4)+`pt:<name>`(the type byte, for blob-fidelity — accessors are type-blind but cook_frame_graph stores the byte); forward iterates p.params INSTEAD OF the shader/kernel/technique special-case (keeps the 3 top-level symbol attrs, skips those names); backward enumerates op attrs (num_attrs API confirmed) iterate-what-exists; full-surface fixture. **2c ✅ DONE + gated 2026-08-11: the FULL RAF-12.3 param bag round-trips (Fork C-2).** to_ceir_frame now iterates p.params → `p:<name>` (value: string/bool/f64-bit-pattern; `p:<name>:0..3` for Vec4) + `pt:<name>` (the FrameParamType byte), SKIPPING the 4 carried specially (shader/kernel/technique top-level symbol attrs + draw_list operand); from_ceir_frame enumerates op attrs (num_attrs/attr_name), rebuilds each param DIRECTLY (iterate-what-exists, no set_pass_* conditionality). attr_f64 added (never narrow a param's double v[] to float). Full-surface fixture (String/Bool/Float/U32/Enum/Vec4 + folded + authored) re-locks the emit_frame_toml round-trip-identity gate (#3971). Gate: win-debug/asan 29/29 + linux + tidy clean (crd-frame-cook only). **2c UNBLOCKS 15c-1d.** **15c-1d ARCHITECTURE DESIGN-LOCKED 2026-08-11 (advisor, design note "15c-1d"):** the ~18 per-executor contract checks live in validate_frame_graph's pass loop (frame_asset.cpp ~1163-1487, ~324 lines, churn-heavy). RESOLVED = **Option B: extract ONE shared `pass_contract_diag(p, ConstSpan<FrameResourceDesc>, where)→FrameCookError`** in frame_asset.cpp, called by BOTH validate_frame_graph AND validate_ceir_frame (A duplicates→desync; C non-pointing+drags parse; D blocks the 15f delete of validate_frame_graph). ⛔ refactor-then-extend, never both in one diff: **15c-1d-0 = PURE REFACTOR** (extract the helper; validate_frame_graph loop calls it + keeps the wrote_output accumulation inline; ZERO behavior change, gate the full desc suite test_frame_asset.cpp; land ALONE) → **15c-1d-1 = CEIR wiring** (extract from_ceir_frame's per-pass rebuild into `rebuild_pass(ctx,op,alloc,FramePassDesc&)` used by both; ⛔ change signature `validate_ceir_frame(ctx,m,alloc)` — needs an allocator to materialize FramePassDescs; wire family-1 MissingDrawList/MissingShader/LoadNeedsGeometry + a FrameCookError→FrameSemanticKind table = the oracle inverted; oracle tests) → families 2-5 (~one tick each; AsyncQueue/Composite/UnknownMaterialPass free with families) → **15c-1d-6** (param closed-vocabs UnknownSamplerFilter/etc = NEW-IN-CEIR, no oracle — validate_frame_graph doesn't range-check enum params, garbage normalizes; injection tests). **15c-1d-0 ✅ DONE + gated 2026-08-11: the PURE REFACTOR — extracted `pass_contract_diag(const FramePassDesc&, ConstSpan<FrameResourceDesc>, String*)→FrameCookError`** from validate_frame_graph's pass loop (frame_asset.cpp). ~324 lines moved VERBATIM (via a Python script — asserts + git-diff verified — since a byte-exact 316-line Edit of a churn-heavy validator is error-prone); the only transform was `desc.resources`→`resources` (the shared_depth check) — behavior-preserving. find_resource/is_sentinel redefined inside the helper; validate_frame_graph's loop now calls it + keeps the wrote_output @output accumulation inline (is_output stays). ZERO behavior change: the FULL crd-frame-cook desc suite is GREEN — 808 assertions/51 cases on win-debug + win-asan (+ linux + tidy pending). Landed ALONE (no CEIR wiring). NEXT = **15c-1d-1 = CEIR wiring**: extract from_ceir_frame's per-pass rebuild into `rebuild_pass` (used by both); ⛔ change signature `validate_ceir_frame(ctx,m,alloc)`; for each frame.pass materialize a FramePassDesc+resource list, call pass_contract_diag, map FrameCookError→FrameSemanticKind (+ header-declare pass_contract_diag); wire family-1 (MissingDrawList/MissingShader/LoadNeedsGeometry) with oracle tests. Then families 2-5 + 15c-1d-6 → CLOSES 15c-1. Converter losses 2b/2d task #21; 15d/e/f/z. **15c-1d-1 ✅ DONE + gated 2026-08-11: the CEIR wiring — the program-contract layer is LIVE in validate_ceir_frame.** ⛔ DIVERGED from the plan text above (advisor §5 simplification, BETTER): NO `rebuild_pass` extraction — validate_ceir_frame calls `from_ceir_frame(ctx,m,alloc)` ONCE (module→full desc), then loops `pass_contract_diag(desc.passes[i], ConstSpan(desc.resources))` pairing each desc pass with its frame.pass op BY INDEX (both graph-order; the op pointers are collected into `pass_ops[kMaxRes]` during the structural walk). Signature changed to `(ctx,m,alloc)`. The SPECIFIC FrameCookError rides `FrameSemanticDiag::contract` (Option 2 — carry the verdict, don't mirror 18 enum values whose mirror IS the drift surface); ExternalTextureIsReadOnly maps back to its OWN kind and the native `ext_write_op` check was DELETED (test #3965 is the migration canary — verdict-identical through the new path). One new kind `ProgramContract` (every other contract). Family-1 oracle tests (MissingDrawList/MissingShader/LoadNeedsGeometry) assert `d.contract == cook_verdict(desc)` — the oracle COLLAPSES to the SAME FrameCookError (no FrameCookError→FrameSemanticKind mapping table to desync, unlike the plan's "oracle inverted"). ⛔ SILENT-SKIP fixed (advisor block): a `from_ceir_frame==false` after a passing structural walk = broken converter invariant → `CRD_ASSERT_MSG` (loud in debug/asan — an unrunnable verifier layer must NEVER read green, the tidy-gate UNGATED doctrine); the pass loop honours the verifier-wide `kMaxRes=128` cap via `min(npass,desc.passes.size())` instead of skipping the WHOLE layer on a >128-pass graph. Gated 32/32 win-debug + win-asan + linux-gcc + tidy-clean. ⭐ side-fix (same tick, solve-immediately): `scripts/tidy-files.ps1` clobbered its `$missing` file COUNTER with the unresolved-include `Select-String` result → a MISSING file could exit 0 behind a later clean one; renamed the local to `$unresolved`. NEXT = families 2-5 (ONE batched tick — accel/indirect/present/queue param-bag coverage, each ~15-line test from the family-1 template) then **15c-1d-6** (param closed-vocabs UnknownSamplerFilter/… = NEW-IN-CEIR checking logic, no oracle) → CLOSES 15c-1. **15c-1d families 2-4 ✅ + gated 2026-08-11 (37/37 win-debug/asan + linux + tidy):** 5 more ProgramContract oracle tests proving `d.contract == cook_verdict` across THREE more executor categories — RT (RtPipelineNeedsThree via raygen-only; RayTraceNeedsAccel via all-3-programs-present-no-AS, which is the STRONG param test: a lost raygen/miss/closest_hit surfaces as RtPipelineNeedsThree≠expected, a loud fail not a silent skip), Indirect (IndirectNeedsArgs + IndirectArgsNotArgs — the pair; both strongly test kernel + the `indirect` bool flag round-trip, else MissingShader fires), Transfer (TransferNeedsOneRead via copy-with-zero-reads). ⭐ TRANSITIVELY PROVES the 2c param bag carries the RT string programs AND the compute `indirect` flag AND that `kernel` (a symbol attr) round-trips like `shader`. Fixtures rely on program-contract firing BEFORE NoOutputPass/ResourceNeverWritten (verified: the block sits at frame_ceir.cpp ~L934, before L960/L967). REMAINDER (families 4b/5, next tick): PresentNeedsOneRead/ClearReadsNothing/AmplifyNeedsCount/VisbufferNeedsUintTarget are param-free/low-risk; ⚠️ AsyncQueueNeedsCompute (needs FramePassDesc::`queue` field round-trip) + CompositeNeedsBlend (needs blend_count/blend_slot params) must first VERIFY their carrier survives to_ceir_frame — possible converter losses (siblings of 2b/2d) to fix before their oracle tests can pass. **15c-1d families 5 ✅ + gated 2026-08-11 (43/43 win-debug/asan + linux + tidy) → FAMILIES 2-5 COMPLETE:** 6 more ProgramContract oracle tests — ClearReadsNothing, PresentNeedsOneRead, AmplifyNeedsCount (raster.tess, no draw-list/count), VisbufferNeedsUintTarget (RGBA8 target ≠ R32Uint), AsyncQueueNeedsCompute, CompositeNeedsBlend. ⭐ RESOLVED the ⚠️ carrier concern above: the converter DOES carry FramePassDesc::`queue` (to_ceir_frame emits the `queue="async"` attr at frame_ceir.cpp L328, from_ceir_frame reads it at L606) — AsyncQueueNeedsCompute passes, so NO converter loss there; CompositeNeedsBlend passing self-verifies the `composite` flag round-trips (else it'd reduce to a valid fullscreen pass, no diag). The program-contract layer now has an oracle test for EVERY executor category (raster/fullscreen/RT-dispatch+pipeline/indirect/tess+mesh amplify/visbuffer/transfer/clear/present/async/composite) — 11 family oracle tests total, all asserting `d.contract == cook_verdict`. REMAINING before 15c-1 closes: **15c-1d-6** ONLY (the param closed-vocab range-checks — UnknownSamplerFilter/Address/Blend/Sort/Cull/… = NEW checking logic in validate_ceir_frame + injection tests, NO oracle since parse_frame_toml rejects bad enums so the desc never carries one; garbage normalizes on the desc side). **15c-1d-6a ✅ + gated 2026-08-11 (46/46 win-debug/asan + linux + tidy):** the closed-vocab RANGE-CHECKS for the INT-carried enum pass params — the NEW checking logic in validate_ceir_frame. 11 total-switch validators (blend/compare/material_pass/filter/address/face_cull/front_face/stencil_op/shading_rate/rate_combiner/conservative) covering ~18 param names (blend0-3 share; the 3 sampler filters + `filter` share; the 3 stencil ops share; depth+stencil compare share), dispatched by a `{name,validator,FrameCookError}` table + one loop that runs BEFORE pass_contract_diag per pass (advisor: else a garbage blend0 truncates to a non-Opaque value that SATISFIES CompositeNeedsBlend — the motivating hole, now a test). ⛔ each validator has NO `default` (widening an enum breaks linux `-Werror=switch` until it learns the value — the positive use of that scar) AND a mandatory `v > 0xFF` guard (u8 enums: static_cast<E>(256) WRAPS to 0 = a valid value; a dedicated 256 test proves the guard). ONE new FrameSemanticKind::UnknownEnumParam; the specific FrameCookError rides `.contract` (NOT ProgramContract — no oracle, like KindLifetimeMismatch). Injection is DESC-LEVEL (set_pass_enum authors a non-default garbage int that to_ceir_frame carries with its `pt:` — simpler than the ceir-attr overwrite the advisor sketched, and transitively re-proves the 2c bag carries these enum params). ⭐ + MAX-VALID ACCEPT SWEEP (advisor gap-catch): every reject test uses 42 (invalid under ALL validators) so the error LABEL comes from the table row, not the validator — a cross-mapped row (e.g. sampler_address→filter_valid) would pass unnoticed and the accept path never ran. Added a sectioned test authoring each vocab's HIGHEST valid value (blend 6/compare 7/material 4/filter 1/address 3/face_cull 2/front_face 1/stencil 7/rate 6/combiner 4/conservative 2) asserting kind==None — catches cross-map + off-by-one. REMAINING = **15c-1d-6b** (the STRING-carried vocabs — draw-list `sort`/`cull`, the `queue` field, resource `format`): from_ceir_frame NORMALIZES a bad string to a default before a desc-based check sees it, so 6b checks them on the ceir OP ATTRS directly; ⚠️ first verify each is carried (for_each carry unverified — if absent it's a 2b/2d-sibling loss). Then 15c-1d-6b CLOSES 15c-1. **15c-1d-6b ✅ + gated 2026-08-11 (52/52 win-debug/asan + linux + tidy) → 15c-1d-6 DONE → ⛔⛔ 15c-1 CLOSED:** the STRING-carried + format vocab checks, on the OP ATTRS directly (from_ceir_frame NORMALIZES a bad string to a default — sort_from→None, cull_from→Frustum — so a desc-based check can't see garbage; VERIFIED all 3 backward readers default). sort/cull on the frame.draw_list op, for_each/queue on the frame.pass op, `format` (INT-carried, 17-case total switch + u8-wrap guard) on the resource op. ⭐ the STRING validity trick reuses the existing forward+backward pair: a string is valid iff `X_str(X_from(s)) == s` (garbage maps to the default enum whose canonical string ≠ garbage; absent=default, unchecked) — no new vocab list. queue is present-only "async" (Graphics absent). for_each is FULLY carried (for_each_str emits the canonical "light.cascades" + for_each_arg rides a separate int attr, L518/L796 — NOT a loss). Injection OP-ATTR-LEVEL (set_attr garbage on the converted op, KindLifetimeMismatch pattern) since the forward only emits valid strings; NO oracle. Accept coverage: sort/cull/format(=0) ride the build_scene positive canary + explicit for_each("cube.faces")/queue(async-on-compute)/format-MAX(D32FloatS8=16, catches a missing last switch case) accepts. ⭐ advisor gap-catch (2 untested surfaces closed): (a) the format check is wired into BOTH resource branches but the declare test only drove one → added an unreferenced ExternalTexture IMPORT with format=99 → UnknownFormat on the import op (else the import branch reads green + a future refactor deletes it silently — the unexecuted-verifier-branch scar); (b) the string accept only covered front_to_back/frustum → added cull=`frustum_occlusion` + sort=`material` accepts (a X_str/X_from spelling asymmetry on a specific value would false-reject a legit graph). **15c-1 = validate_ceir_frame is now the FULL semantic verifier**: structural (find_frame_misuse 7 kinds) + NoOutputPass/DuplicateName + resource-shape + def-use + NEW-IN-CEIR consistency + program-contract (ALL executor families, ProgramContract+contract) + closed-vocab (ALL int+string+format, UnknownEnumParam+contract). NEXT = 15c-1d-6 wrap-up docs, then converter losses 2b (indexed `[$index]`) + 2d (top-level requires_caps/fallback/memory_budget_bytes) = task #21, then 15d (hazard walk)/15e/15f/15z. **converter losses 2b + 2d ✅ + gated 2026-08-11 (56/56 win-debug/asan + linux + tidy):** 2b = the per-operand `[$index]` subscript (FrameResourceRef::indexed) — carried as an `indexed` char-string attr on the frame.pass op, PARALLEL to `access` ('1'/'0' per operand, draw-list operand always '0'; emitted only if any ref is indexed; backward sets ref.indexed by operand index). 2d = the graph-level fields on the frame.graph op: `requires_caps` (comma-joined via join_components / split_components), `fallback` (string), `memory_budget_bytes` (i64, exact < 2^63). Both proven by the round-trip-identity gate (emit_frame_toml equality) + direct field checks (incl. the @output write stays UNindexed — the parallel alignment doesn't leak). ⚠️ NEW FINDING 2e (task #22): the converter carries NEITHER includes/anchors/injects (REN-37.6 composition), yet emit_frame_toml DOES emit them (frame_emit.cpp L193/L215) → a composition graph would FAIL the round-trip gate (latent: no test uses composition). Design-ambiguous — is the ceir.frame the AUTHORED (pre-flatten, carries composition) or FLATTENED form? Resolve before claiming the converter fully lossless. **2b/2d WRAP-UP ✅ + gated 2026-08-11 (59/59 win-debug/asan + linux + tidy) — advisor close batch:** (1) 2b made TWO pass-contract rows REACHABLE that were dead before (from_ceir_frame always produced indexed=false): SubscriptOnNonLayered (indexed ref on layers==1) + IndexWithoutForEach (indexed ref, no for_each) — added both as ORACLE tests (d.contract == cook_verdict; emit writes `[$index]`, parse_ref reads it). (2) 2e ADJUDICATED: the ceir.frame is the AUTHORED form (it carries `for_each`, an EQUALLY pre-flatten construct per the desc comment; and the toml frontend ENDS at ceir.frame, so 15f needs composition representable) → real loss, fix before 15f. **⛔ CORRECTED 2026-08-11 (advisor concurred — primary sources beat the analogy): NOT a loss; the reject is the PERMANENT contract.** `flatten_frame_graph` (REN-37.6) resolves composition into plain passes BEFORE conversion, the §115 table routes composition to PARSER ("the converter sees a FLATTENED graph"), and the frame.graph op-def names subgraphs/includes/injection as §39 NAMED-FORWARD (15c-1a MultipleGraphs = one graph per module this slice). Tie-breaker (structural): `for_each` round-trips because its expansion needs RUNTIME counts (ShadowCastingLights, ADR-0106 FrameRecorder); composition is fully COOK-time resolvable (FrameGraphResolveFn), and carrying it into ceir.frame would force composition special-cases into every 15d analysis — exactly what REN-37.6 prevents. 15f deletes the duplicate RUNTIME path, not the toml frontend where flatten lives. PIPELINE: `parse → flatten_frame_graph → to_ceir_frame`. Task #22 RESOLVED (no carry). The reject test is now the PERMANENT-contract test. "Converter lossless" = lossless over its INPUT DOMAIN (flattened descs); a composition desc is outside the domain (flatten first). Historical note follows (the superseded INTERIM framing). INTERIM: to_ceir_frame now REJECTS (nullptr) a desc carrying includes/anchors/injects rather than silently dropping (the silent-skip scar) — header contract + test. (3) `fallback` → attr_SYMBOL not attr_string (a cross-asset graph reference by name; a future CDEP dependency pass surfaces symbols). (4) SKIPPED (conscious): a structural arity guard on the `indexed` attr (length==operand count) — the backward already bounds `i < idxs.size()` so a short string safely defaults the tail un-indexed (memory-safe, no OOB); hand-authored `indexed` is exotic and the forward always emits correct length. Converter now LOSSLESS for every emit_frame_toml field EXCEPT composition (task #22, loud-rejected). | §115 §126 |
| ✅ | CEIR-15d | **✅ CLOSED 2026-08-11.** Frame compile → CEIR scheduling analyses + the CEIR-12 planner. 15d-1 hazards (per-operand effect narrowing), 15d-2 DependencyCycle (extract-and-share), 15d-3a lifetimes (for-free via the narrowing), 15d-4 for_each (runtime-entangled — rides 15d-5's host `for_each_count`), 15d-5 runtime-fidelity gate (byte-equivalent `FrameGraphTemplate`, Gate-7 one-submission preserved). **15d-3b ✅ (memory-pooling parity, decided WITH the advisor — the "user-decision memo" is resolved, no longer user-blocked):** `compute_block_lifetimes` now makes `ResourceLifetime` the MEMORY-LIVE range `[first-use, last-use]` (was `[declare-pos, last-use]`, which — since the frame converter emits every declare up-front — tied every transient's range to ~0 and blocked ALL pooling). This is parity with the render-graph aliaser's `first_use` model (frame_graph.cpp L1200-1225, same `(kind,size_class)` keys). ⛔ correctness: a SYMMETRIC ambient extension (keyed on a local `declare_pos`, not a struct-layout change) makes any resource allocated at-or-before an ambient op SPAN it in both directions — so a not-yet-first-used resource can never pool into a slot an ambient op clobbers under a live tenant; converted frames have ZERO ambient ops (the 15d-1 narrowing) so full first-use precision holds there. Gated 4-config: crd-ceir 433/433 (win-debug/asan + linux-gcc) + crd-frame-cook 92/92 consumer-relink + tidy `context.cpp` clean; band-12/lifetime/planner/compute deltas each individually justified as the intended more-pooling gain (co-slot-aliasable invariant preserved); new `test_planner.cpp` "ceir 15d-3b" (declared-early/used-late transient pools; unused-degenerate) + `test_lifetime.cpp` (3) pins the ambient clobber-guard. | §78 §79 §126 |
| ✅ | CEIR-15e | Runtime executes CEIR plans; A/B harness runs old + new per asset with the deterministic clock. **✅ 2026-08-11:** 15e-a device-free per-asset byte-equal template (all 15 shipped assets); 15e-b ON-DEVICE A/B parity — LIVE renderer routes every frame through CEIR behind `CRD_FRAME_VIA_CEIR`, `crd-scene-render-tests` 71/71 flag-ON==flag-OFF on REAL Vulkan+DX12 (RAF-10 app-composed + custom executor + RAF-11 reload + REN-36 pixel asserts). FIX: `flatten_frame_graph` consumes anchors. 4-config gated | §126 |
| ✅ | CEIR-15f | **✅ DONE + gated 2026-08-11 (reading A-refined, advisor).** The duplicate BYPASS deleted (NOT the desc — reading B rejected: `build_frame_graph_template` is 796 lines of executor mapping a direct `ceir.frame→template` lowering would duplicate). **15f-1** flipped the CEIR route to DEFAULT-ON. **15f-2** wired `validate_ceir_frame` (the full §115 verifier — ALL of 15c, was TEST-ONLY) in as the LIVE cook gate + a negative test (a no-`@output` frame is rejected VIA the CEIR verifier). **15f-3** DELETED the `=0` bypass + the `CRD_FRAME_VIA_CEIR` flag — the CEIR route is UNCONDITIONAL (§121: no path executes without going through canonical CEIR; `validate_frame_graph` stays as flatten's composition check + the differential-oracle reference). **15f-4** amended **ADR-0106 Decision #2 in-place** (canonical description = `ceir.frame`; `FrameGraphDesc` = blessed Desc-vs-Runtime STAGING, NOT eliminated; runtime + `build_frame_graph_template` load bridge PRESERVED); blob-compat verified (`.crdr` cooker serializes the AUTHORED desc, no flatten → anchor-clear doesn't touch it, stays v8). 4-config each cut: scene-render 72/72 (both backends) + win-asan + linux-gcc + tidy + sandbox smoke. | §126 §159 |
| ✅ | CEIR-15z | **✅ BAND-15 CLOSED 2026-08-11.** **Gate:** every shipped frame asset runs through CEIR pixel-identically both backends; the old path is gone (the-deletion-is-the-proof); `crd-sandbox --smoke-test` green, validation on. RE-VERIFIED fresh (GATE-re-verifies doctrine): crd-ceir 433/433 + crd-frame-cook 92 + crd-scene-render 72 (RAF-10 Vulkan+DX12, REN-36 CSM, REN-41 cook, the 15f-2 negative gate) — win-debug/asan + linux-gcc all exit 0; `crd-sandbox --smoke-test 3` PASS (208 frames, unconditional CEIR, exit 0); the `CRD_FRAME_VIA_CEIR` flag + `=0` bypass DELETED (§121); `validate_ceir_frame` is the live cook gate. clang-tidy per-slice on every changed source. **The FrameGraph is a CEIR dialect: authored `ceir.frame` → validate_ceir_frame (live §115 gate) → CEIR analyses (15d hazards/cycle/lifetimes+first-use plan) → desc (blessed staging) → runtime.** | §126 |

## CEIR-16 — Executor migration — ✅ DONE 2026-08-14 (16a–16d ✅ · 16z visbuffer dissolution ✅ · every composite raster executor is now a CEIR-authored program; the imperative recorders are DELETED)  → §127 · PROOF §128

**Band contract.** Execute the 0a/0h classification. Every **composite** executor (scene raster orchestration,
fullscreen setup, compute setup, RT orchestration, transfer workflows — final list from 0a, not from docs) becomes
a CEIR program asset in the `engine://` built-in pack; every **atomic** capability registers as a CEIR-0d intrinsic
(scene resolvers land as host intrinsics: `scene.resolve_material/technique/program/geometry` — §45's replaceable
convenience tier). The decisive proof: **`scene.raster` as ordinary CEIR (§128)** — begin-render → foreach draw →
resolve chain → build bindings → draw — pixel-identical to the C++ executor on the live sandbox scenes.

> **⛔ TRACKER RECONCILED 2026-08-12.** The rows below were re-sliced to the design-doc §6 decomposition — the
> original 16b/16c/16d rows predated the advisor-refined sub-slicing AND were never ticked as slices closed, so the
> table under-reported reality (only 16a showed). Full live detail → `context.md` Current focus +
> `docs/design/ceir-16-executor-migration.md` STATUS blocks §16b/§16c-d. **✅✅ FINAL 2026-08-14: the WHOLE CEIR-16 band is
> DONE + gated (16a–16d + 16z all ✅). Every composite raster executor records through the generic `record_ceir_render`;
> `record_scene_raster` + `record_visbuffer_raster` + the visbuffer.raster executor are DELETED (14→13 built-ins).** The
> 16d-live-4 (mrt≥2 + THE DELETION) + 16z (visbuffer dissolution) + the CEIR-17a/b scene-dialect work is UNCOMMITTED on top
> of `c44adbf` (user commits — a fresh session `git status` first). The two ◧ checkboxes on 16c/16d were STALE (both slices
> were finished; 16c's §45 resolver/query/cull remainder is RECLASSIFIED to the CEIR-17 scene bridge, not dropped) — ticked
> ✅ 2026-08-14 after re-verifying each row (the GATE-reverifies-rows discipline).

| ✔ | Slice | Deliverable | → §§ |
|---|---|---|---|
| ✅ | CEIR-16a | **✅ DESIGN-LOCKED 2026-08-12** (decision packet `docs/design/ceir-16-executor-migration.md`, advisor-led). Migration order from 0a — MEASURED `record_*` sizes (transfer.copy 4 → … → scene.raster 314 LAST); the CLASSIFICATION CRITERION (atomic iff record_* maps 1:1 onto one ceir op's lowering; composite iff it sequences decisions); the named parity gate = ADR-0106 #4 (command-parity via the FOUND `MockEncoder` in `test_frame_graph.cpp` — `ea.ops==eb.ops` — + pixel-readback + sandbox smoke both backends). Substrate VERIFIED GREEN both backends (ceir-gpu 230 + vk 419 + dx12 310 — the execute-a-ceir.render-draw on-ramp exists). 3 forks DECIDED (lower-once-at-cook plan+replay; visbuffer §41 dissolves AFTER 16d; scene.resolve_* declared-in-registry/host-impl); E3a shadow fit+cull seam scoped IN 16c. | §127 |
| ✅ | CEIR-16b | **RENDER-FAMILY composite executors MIGRATED + 4-config gated + device-verified BOTH backends (deletion-is-the-proof).** `fullscreen.raster` · `mesh.indirect` · `mesh.raster` · `tess.raster` all record through ONE generic `record_ceir_render` replaying a per-pass CEIR plan (`build_fullscreen_ceir`/`build_mesh_indirect_ceir`/`build_amplify_ceir`) built at frame LOAD (`build_frame_plans`); the imperative `record_fullscreen_raster`/`record_mesh_indirect`/`record_amplify_raster` + wrappers + temp A/B flags are DELETED (register count stays 14). NEW ops `render.mesh_dispatch_indirect` (args now wired) + `render.mesh_dispatch_list` (0-operand per-draw amplify TEMPLATE the record-time walk EXPANDS over `ctx.draws()`); PATCHES vocab added. ⛔ the flag-ON REAL-renderer sweep caught 2 live-path bugs the device-free A/B MISSED (plan-table must rebuild at EVERY frame-install site; `groups‖patches` authored-name read). **16b-tail (atomic transfer/compute/rt executors) = DECIDED-DEFERRED to 16z** — their seam shape depends on how 16c/16d drive compute/transfer/rt, so building now is speculative. Detail → design doc STATUS §16b + `context.md`. | §127 |
| ✅ | CEIR-16c | **✅ DONE 2026-08-14 — Scene ITEM-VIEW + `pass_texture` resolver, in use by the migrated scene.raster.** `RasterAmplifyItem`→`RasterDrawItem` = the full RenderDrawItem projection (program/storage/texture/args/counts/indexed; `dispatch_groups` EXCLUDED = compute); `fs_draws_item` fills the full set; the `pass_texture` resolver (`fs_pass_texture` — the shadow/moment atlas + its is_depth/comparison sampler shape). These are LOAD-BEARING in the migrated §128 path (`emit_scene_list` reads `RasterDrawItem` via the resolvers; the shadow-atlas seam drives the CSM cascade passes — scene-render 1539/72 incl. the CSM device tests). ⛔ **RECLASSIFIED (not dropped):** the `scene.resolve_*` host intrinsics (§45) + the scene consolidation / cull / draw-item-assembly resolver semantics (blocks 3,4,5,8 + the E3a shadow fit+cull seam) are the **CEIR-17 scene bridge** — ✅ CEIR-17a/17b DONE (the `scene` dialect: 4 resolve_* host-intrinsic ops + 5 opaque type-classes + `find_scene_misuse` + the `RenderResolvers` host-callback seam + `evaluate_scene_resolve`); 17c (query→DrawList) / 17d (GPU cull+indirect) / 17e (skinned) in progress. The CEIR-16 band delivered the composite-EXECUTOR migration; the scene RESOLVER/query/cull layer is CEIR-17 by design (host-side behind the IFrameGraphHost seam, DrawList carries pre-resolved handles — §4b/§45). | §100 §45 |
| ✅ | CEIR-16d | **✅ DONE 2026-08-14 — `scene.raster` §128 proof COMPLETE (device-free core + the full LIVE path + THE DELETION, all gated).** DONE: the ONE `render.scene_draw_list` template op (0-operand) + `draw_shape_of` + opgen regen; `emit_scene_list` = the VERBATIM `record_scene_raster` single-colour verb ladder (indirect→indexed-sampled→combined/tex→plain-COALESCE, asymmetries preserved) with the atlas scar helpers (map@1 / atlas@4 + comparison‖plain sampler@5) REPLICATED in ceir-gpu + `scope_has_color` hoisted from the MATERIALIZED scope (advisor Gap-1: a depth-only pass binds NO textures); `build_scene_ceir` (colour?+depth?+scene_draw_list; a 0-COLOUR depth-only scope); `kMaxSceneRun` single-source. A device-free test (47 asserts, `ceir 16d`) PROVES the verb ladder + the 0-colour-scope acceptance (was inferred — advisor blocker #2) + Gap-1. PENDING (LIVE): ✅ **16d-live-1 `fs_target` depth branch + 3 depth-acquisition modes DONE + gated** (real-vs-real A/B `ceir 16d-live-1b`) · ✅ **16d-live-2 `build_frame_plans` SCENE ARM DONE + gated** (SceneBuildDesc from authored params; ⛔ scene added to the reserve COUNT loop; mrt colour-write≥2 = SKIP-plan+INFO not LOUD [a false return disables the whole frame]; base `load` only; crd-frame-cook 2751/96 4-config + crd-scene-render 1539/72 real GPU) · ✅ **16d-live-2b per-instance `load_override` at record DONE + gated** (RenderResolvers.force_load; plan bakes statics, payload carries per-instance deltas; cached-cascade = record-nothing, legacy's null-program fallback is a verified device no-op; real-vs-real A/B `ceir 16d-live-2b` + crd-scene-render 1539/72 real GPU) · ✅ **16d-live-3a N-draw COMMAND-PARITY DONE + gated** (SceneParityEncoder real-vs-real A/B `ceir 16d-live-3a` across the full verb ladder incl. the REN-38 plain-COALESCE command-structure gate; crd-render-graph 333/14 4-config) · ✅ **16d-live-3b DONE** (RECLASSIFICATION: the variant ladder is ALREADY host-side scene.resolve_program per fork-c/§4b, so 16z deletes the EXECUTOR not the ladder — E1 anchor corrected ~L6280-6379; the one code deliverable = the untested null-program→default record-time fallback A/B, emit_scene_list already matched legacy, locked; 0d-registry declaration named-forward) · ✅ **16d-live-3c FLAG DANCE `CRD_SCENE_VIA_CEIR` DONE + PROVEN** (runtime env swap at register_builtin_records, count==14 both states; **flag-ON == flag-OFF = crd-scene-render 1539/72 on real GPU both Vulkan+DX12 + `crd-sandbox --smoke-test` PASS** [forward_csm_agx, 11 passes, 4 PCSS cascades]; plan-table-rebuild scar clean; render-graph 363/14 4-config) — flag DEFAULT stays OFF until 16d-live-4 deletes record_scene_raster · ✅ **16d-live-4 DONE (ADVISOR-LOCKED: BUILD mrt≥2, op-mode repr, no dialect change)**: ✅ **4a-1 blend vocab DONE** (full 7-mode BlendMode = the CEIR `blend` closed vocab both directions; WBOIT revealage_multiply/multiply/reveal_composite no longer fold to Opaque; `ceir 16d-live-4a-1`; crd-ceir-gpu 477/32 + crd-ceir 3317/433 + opgen-drift 4-config) · ✅ **4a-2 desc/builder DONE** (SceneBuildDesc mrt_n/blend[4] + build_scene_ceir N-colour emission via blend_str; single-colour byte-identical; GAP ii clear — find_render_misuse accepts the ≥2-colour scope+scene_draw_list; `ceir 16d-live-4a-2`; crd-ceir-gpu 489/33 4-config) · ✅ **4a-3 walk expansion DONE** (execute_render_lowered defers ≥2-colour begin → emit_scene_list_mrt per-item MRT scope = legacy arm; 14z-4c single-draw preserved via deferred-flush; gap iv force_load-not-on-MRT; `ceir 16d-live-4a-3` device-free + render-graph real-vs-real A/B; crd-ceir-gpu 499/34 + crd-render-graph 384/15 4-config) · ✅ **4a-4 un-skip DONE → ⭐⭐ 4a COMPLETE (mrt≥2 CEIR representation DONE)** (build_frame_plans sets mrt_n + blend0..3; the 16d-live-2 mrt≥2-SKIP INVERTED [gap iii]; shipped assets stay mrt_n=1; crd-frame-cook 2752/96 4-config + scene-render 1539/72) · ✅ **4b DONE (on-device legacy-vs-CEIR A/B)** (only 3 tests use scene.raster: run_scene_drawlist/mrt_blend/graph_gpu; a GraphExecutorTable::replace_record seam; scene_drawlist + mrt_blend assert IDENTICAL pixels legacy-vs-CEIR both backends; graph drops the geometry-slot fallback; ⭐⭐ the A/B CAUGHT A REAL BUG — fs_target resolved ALL colour attachments to "color", MRT color1..3 unwritten → fixed with a color_slot index attr; render-graph 384/15 + ceir-gpu 499/34 + gpu-tests raf7 395/2 real GPU + llvmpipe 197/1 + scene-render flag-ON 1539/72) · ✅✅ **4c DONE — THE DELETION (the-deletion-is-the-proof)**: `record_scene_raster` + its scar helpers (bind_map/bind_atlas/attach_textures) + the orphaned `kMaxSceneRun` + `scene_via_ceir()` + the `CRD_SCENE_VIA_CEIR` flag + `#include <cstdlib>` DELETED; `register_builtin_records` maps scene.raster → `record_ceir_render` UNCONDITIONALLY (count still 14). The 4 device-free A/B tests (1b/2b/3a/4a-3) + 2 on-device A/B tests (run_scene_drawlist_gpu/run_mrt_blend_gpu) converted SINGLE-PATH with ABSOLUTE asserts (post-deletion a "legacy" arm is a vacuous CEIR-vs-CEIR — run_mrt_blend_gpu, the color_slot-bug catcher, now asserts absolute per-attachment blend values; 3a/4a-3 transcribe emit_scene_list's exact verb ladder). **PROOF (no flag):** render-graph device-free 309/15 + gpu raf7 363/2 (Vk+DX12)/181/1 (llvmpipe); the WHOLE **scene-render suite 1539/72 UNCONDITIONALLY** + `crd-sandbox --smoke-test 2` PASS (forward_csm_agx, 11 passes) + frame-cook 2752/96 + ceir-gpu 499/34; win-debug + win-asan + linux-gcc-debug + linux-gcc-asan; tidy clean. **⭐⭐ scene.raster is now a pure CEIR-authored executor — the §128 migration is COMPLETE.** REMAINING → 16z: visbuffer.raster dissolution (re-author scene_visbuffer onto scene.raster + a uint attachment, delete record_visbuffer_raster). | §128 |
| ✅ | CEIR-16z | **DONE 2026-08-14 — the legacy composite-executor path DELETED per 0h.** visbuffer.raster dissolved into scene.raster (§41): a visbuffer is now a scene.raster pass with a PROCEDURAL geometry mode (`geometry=procedural` on `render.scene_draw_list`, 16z-2) + a uint typed clear (`SceneBuildDesc.clear_is_uint`, 16z-1) derived from the R32Uint target format. `record_visbuffer_raster` + the `visbuffer.raster` executor (schema + record) DELETED; the KindRow remaps `raster.visbuffer`→kExecSceneRaster+procedural (authoring vocabulary kept); the 15c `VisbufferNeedsUintTarget` contract re-keyed to a union trigger (procedural ‖ clear_id ⇒ R32Uint). **14 built-in executors → 13.** GATE: render-pass 46/5 + render-graph 309/15 + gpu [raf7] 363/2 + frame-cook 2751/96 + scene-render 1539/72 (incl. the DEVICE id read-back) + `crd-sandbox --smoke-test 2` PASS + gpu-context vulkan/dx12 REN-38-A11 visbuffer cook + device gates; win-debug + win-asan + linux-gcc-debug + linux-gcc-asan; tidy clean. **→ broad feature development resumes as CEIR program assets.** | §127 §178 |

---

## ⛔⛔⛔ BAND-18 CORRECTION — "authored asset" INCLUDES the PROGRAMS, not just the frame graph (user 2026-08-15, in anger)

**The scar.** Under CEIR-18a-1 I authored the Forward+ light cull as `build_cluster_light_cull` — a hand-written C++
`KGraph` builder in `engine/kir/include/crd/kir/ckir_light_cull.hpp`, with NO disk declaration and NO disk-first cook.
The user caught it and ELEVATED the band's bar: **every rendering/compute ALGORITHM a pass runs must ALSO be an
authorable asset program** — cooked disk-first, default-loaded for every app, partially modifiable or completely
replaceable by an app asset — NOT a hand-built `ensure_*_program`/`ensure_*_kernel` registered from C++. (AGENTS.md
§Agent Conduct "THE SAME LAW BINDS THE PROGRAMS"; docs/PRINCIPLES.md; memory `feedback_everything_is_an_authorable_asset_ceir`.)

**What the band as-written permitted (and must no longer).** The CEIR-18 row treats "renderer as authored asset" = the
`.frame.toml` is authored; shader programs referenced by id may be host-registered via `register_raster`/`register_kernel`
with a HAND-BUILT graph. CEIR-18c shipped exactly this: `deferred.frame.toml` is authored + correct, but the
`engine://scene/deferred_lighting` program behind it is hand-built C++ (`ensure_deferred_lighting_program`,
scene_renderer.cpp:2180). The frame-asset + device gates are GREEN and STAY; the PROGRAM's provenance is the re-opened debt.

**The elevated bar (binding for all remaining 18 work + a re-open sweep):** a renderer slice is DONE only when BOTH its
frame graph AND every program it runs are authorable assets cooked disk-first, and the hand-built C++ builder is DELETED.
Test before done: can an app replace the algorithm by editing/shadowing an asset, WITHOUT recompiling the engine?

⛔⛔⛔ **LOAD CONVENTION (user correction 2026-08-15, in anger — I had loaded every `.ckir` via the WRONG seam):** an authored
program asset is loaded through the **RAF-9/10 canonical-id convention**, APP-FIRST, so an app can REPLACE it with no recompile —
NOT bare `asset_text` (Engine-mount-only, an app's mount is unreachable) and NEVER `ifstream`. `.ckir` is now a first-class asset
TYPE (`render-asset-core/identity.cpp`: `infer_type("ckir")=Program`, `asset_extension("ckir")=".ckir"`); the files live at
`assets/ckir/<name>.ckir`; the engine loads via `Impl::resolve_program_text("ckir/<name>")` = try `app://ckir/<name>` (app file
WINS), else `engine://ckir/<name>`. Later `.chir` gets its own folder the same way. Memory:
`feedback_authored_programs_load_via_resolve_program_text_engine_app_convention`. ⛔ 18a-2's cull wiring MUST use this seam too.

**The MAP — hand-built C++ program/kernel builders in `scene_renderer.cpp` (provisional, by cook-vs-`create_program`
signature; confirm each at conversion):**
- FRAGMENT (fullscreen ALGORITHM → asset): ~~`ensure_deferred_lighting_program`~~ ✅ (18c, `deferred_lighting.ckir`) ·
  ~~`ensure_taa_program`~~ ✅ (18p, `taa_resolve.ckir` — load-time lower + runtime NDC±Y sign) · ~~`ensure_hzb_program`~~ ✅
  (18p, `hzb_build.ckir` — param-free, exercises `tex_gather`+`Min`; library `body_hzb_build` STAYS as oracle) ·
  ~~`ensure_moment_program`~~ ✅ CONVERTED 2026-08-15 (18p — 3 assets + spec-const params, REN-40-D 43/2 BOTH backends via
  asset-load; advisor-LOCKED: **LOAD-TIME PARAM SPECIALIZATION** — the asset carries the ALGORITHM,
  host patches `layer`/direction constant-NODES per variant before `create_program` into the existing `moment_prog[kind][index]`
  slots; `inv_size` DERIVED in-graph via **`TexSize`** [op EXISTS] so it's NOT a param; assets = `moment_convert_evsm.ckir` +
  `moment_convert_msm.ckir` + `moment_blur.ckir`. ⛔ bake-all is a §128 KILL [`inv_size`=live 1/map_size]; TAA-buffer is dead
  [`layer` varies per-pass-per-frame]. ✅ D12 CHECK DONE (2026-08-15): params = D12 **SPEC-CONST nodes** (`g.spec_constant`);
  `axes`+`iidx` round-trip FREE (default-elided) ⇒ **NO `[[param]]` table / form change**; both backends use the node `cval`
  (Vulkan `constant_id` default / DX12 baked literal — gpu-context has NO `VkSpecializationInfo`), so the host patches `cval`
  via the new **`KGraph::set_spec_const(id,val)`** after `ckir_read` + before `create_program`. **STEP1+2 ✅ DONE (2026-08-15):**
  `body_moment_convert`(layer→spec) + `body_moment_blur`(layer/dir/**inv**→spec); ⛔ inv is a SPEC-CONST fed from live
  `csm.map_size`, NOT a TexSize derive (the arrayed atlas `textureSize` is ivec3 but `KOp::TexSize` types a Tex2D as ivec2 —
  a rank miscompile on materialization); caller UNCHANGED (spec defaults = passed values ⇒ bit-identical bake). REN-40-D
  **43/2 BOTH backends** (EVSM+MSM soft shadows unchanged). 3 assets emitted (`moment_convert_evsm`/`_msm` + `moment_blur`) +
  kir `[ckir18q]`=**246/8** (round-trip + NON-VACUITY pin: `set_spec_const` 2 inv values ⇒ graphs DIFFER). **STEP3-5 ✅ DONE:**
  `ensure_moment_program` is a THIN per-kind asset-load (convert reads evsm/msm by soft_mode; blur reads `moment_blur`;
  `set_spec_const(layer=index)` + blur `(dir, inv=1/LIVE-map_size)`; create into `moment_prog[kind][index]`); the
  `body_moment_*` build calls are DELETED (bodies STAY in ckir_technique.hpp as oracle/regen source). **REN-40-D 43/2 BOTH
  backends via the ASSET-LOAD path** (spec-const value reaches the pipeline on Vulkan constant_id-default + DX12
  baked-literal); emit test deleted; kir `[ckir18q]` 246/8 win+linux; tidy-clean.) ·
  ~~`ensure_velocity_debug_program`~~ ✅ (18p, `velocity_debug.ckir` — env-hook emit, backend-neutral 1-read encode, no
  params) · ~~`ensure_post_program_named`~~ ✅ ALREADY ASSET-DRIVEN (reads `post/*.crdp` DISK-first via
  `resolve_asset_text`+`parse_post_toml`+`cook_post_graph`, app-shadowable RAF-10 — the C++ is the COOKER/frontend, NOT a
  hand-built algorithm; PASSES the app-replace test) · ~~`cook_velocity_fs`~~ ✅ CONVERTED 2026-08-15 (18p — 2 assets `velocity_fs_{plain,dither}.ckir`; the dither LOD discard
  is a STRUCTURAL variant, the backend clip-Y `vsgn` is a SPEC-CONST host-patched per backend; `lower_entry` moved to the
  caller + emit PRE-lower; ⛔ `build_velocity_fs_cooked` **FULLY DELETED** from scene_renderer.cpp — the DELETION IS THE
  PROOF. REN-41 velocity 22/2 BOTH backends via asset-load + kir `[ckir18q]` 267/10 win+linux [+ discard_cond non-vacuity
  pin] + tidy). **⟹ ALL FRAGMENT builders now converted-or-compliant.**
- MESH / VS+FS: ~~`ensure_cluster_mesh_program`~~ ✅ CONVERTED 2026-08-15 (18p, `cluster_mesh.ckir` — 1st MESH-stage asset;
  backend-neutral [view_proj is a runtime header read, no baked flip], lower-on-load; REN-41 cluster-mesh 22/2 both
  backends + kir round-trip pins mesh_prim/mesh_primitives/position) · ~~**`ensure_impostor_program`**~~ ✅ CONVERTED 2026-08-15 (18p — 4 assets `impostor_{vs,fs}_{plain,dither}.ckir`; VS runtime-header/no-spec-const, FS 18 D12 spec-consts pinned at kImpostorMaxMips=16; builders+emit DELETED, renders from disk PIXEL-EXACT 133/169/36 BOTH backends + kir round-trip 361/14 pins 18 spec-consts/VS-zero; STEP 0.5 also FIXED a pre-existing cull-kernel emitter bug — see below). The historical build notes:
  2 programs — VS (KStage::Vertex, backend-neutral, NO lower) + FS (KStage::Fragment, LOWERS; coverage+dither `discard_cond`).
  EACH has a `has_dither` STRUCTURAL variant, COUPLED (FS `abase` at loc 3 vs 2) ⇒ **4 assets** `impostor_{vs,fs}_{plain,dither}.ckir`
  selected by ONE dither bool at load (cook_velocity_fs pattern). ⛔ FS is CONFIG-STRUCTURAL: `grid`/`tile`/`mips` come from the
  LIVE `lod_policy` (`set_lod_policy_asset`) and `mips` unrolls the mip select-CHAIN LENGTH. ADVISOR FIX (parameterized, mandatory
  since lod_policy is live): PIN the chain at **kImpostorMaxMips=16** (`impostor_num_mips=log2(tile)+1 ≤ 16` for tile ≤ 32768; LOUD
  error if `>kMax`) + demote every config number to a D12 **SPEC-CONST VALUE** (`gt=grid*tile`, the `mips-1` clamp [2 sites, same
  id], per-level offsets `kImpostorLvl0..15`) patched at ensure-time from `crd::lod::impostor_level_offset(live grid,tile,k)`
  (0 for k ≥ live mips — unreachable, the clamp keeps `mi_u` below live mips). Bit-identical when patched to the same config.
  ⛔⛔ COVERAGE GAP → **CLOSING via STEP 0.5** (advisor REVERSED "don't invent" once grep CONFIRMED zero coverage — the only
  impostor ref in scene-render tests is 1 comment in a gate that turns the impostor OFF; convert-blind is BANNED by the
  cook-only-gates + new-execution-path-full-suite scars). **STEP 0.5 = ADD an impostor ENGAGEMENT GATE on the CURRENT hand-built
  code, GREEN both backends BEFORE the refactor** (it is ALSO the emit vehicle — it's the run that finally calls
  ensure_impostor_program). SETUP: `set_lod_policy_asset("lod/scene_default.crdlod")` [has `impostor_grid=8`/`tile=64`] BEFORE the
  first sync (build_lod_chain refuses a 2nd build) + a mesh + FAR/tiny instances (<12px projected → the impostor slot, sr:6184) +
  render `forward_csm_gpu`; the atlas bakes at mesh-load (`bake_impostor_atlas` sr:4841/5682). GATE: `ValidationCapture`
  error_count==0 + an ENGAGEMENT pin (impostor DREW — a pixel probe at a far instance vs bg, or a draw stat; A==A, no vacuous
  pass). ⛔ if it FAILS on current code = a PRE-EXISTING defect (impostor path never gated) → fix FORWARD first (17e precedent).
  The SAME gate then carries STEP1 (refactor, output-identical) + STEP3 (asset-load) + STEP5. NOT dormant (sandbox main.cpp:757).
  ✅✅ **STEP 0.5 DONE 2026-08-15 — GREEN BOTH BACKENDS** (`CEIR-18p IMPOSTOR STEP 0.5 GATE …(Vulkan)` + `(DX12)` in
  `test_scene_render_gpu.cpp`, `[impostor]` tag, 33 assertions). Mesh = subdivision-4 **icosphere** (`meshgen::make_icosphere`,
  5120 faces) NOT a cube: a 12-tri cube is ≤ `min_triangles`(64) so `build_lod_chain` gives ONE level → no impostor
  (lod_chain.cpp:383); the icosphere builds a full 7-level chain (impostor at group idx 6 == `lod_slots-1`). 25 far icospheres
  (~7.4 px, z=60) all route to the impostor slot; pins = `read_gpu_cull_counts` (groups>0 + v0>0 — the backend-agnostic
  "GPU-cull graph ran" discriminator; `compute_dispatch_count()` is VULKAN-ONLY) + `impostor_draws>0` (emission) +
  sign-agnostic centre-vs-sky luma `delta>20` (measured 36; the 204-grey billboard reads DARKER than the ~169 sky — a directional
  pin would be WRONG). ORDER: `set_lod_policy_asset` BEFORE `init_programs` (sandbox order; the cull kernel cooks lazily at record
  time, so ordering was NOT the fault — header doc "before first sync" is correct). ⛔⛔ **PRE-EXISTING DEFECT FOUND + FIXED (fix
  forward, as mandated):** the SHIPPED `forward_csm_gpu`+`scene_default.crdlod` (7 slots, dither 0.05) GPU-cull path had NEVER been
  device-tested (no scene-render gate installed an LOD policy). `scene_cull_compact.crdv` `create_program`-NULLED: the DITHER
  DUAL-WRITE emits two sibling `stmt_if` blocks sharing `in_band_u`/`alpha_fine`, declared inside the first block, read out of scope
  by the second → `'t571' undeclared` (the if-block shared-temp scar, [[feedback_ckir_if_block_shared_temp_scope_materialize]]).
  Fix = `g.stmt_materialize(primary_val/sec_vis/sec_val)` before the `keep` block (vertex_asset.cpp, backend-agnostic → GLSL+HLSL
  both fixed; DX12 twin green same fix). The null had recorded 0 compute passes SILENTLY → LOUD-FAILURE fix: `create_program`
  (vulkan_context.cpp) now `g_log_vkctx` CRD_LOG_ERRORs the failing link + the discarded shaderc `error_message`. No regressions:
  full `crd-scene-render-tests` 1728/80 green. ⛔ 2 secondary defects deferred to slice close: (a) DONE — the loud create_program
  log; (b) `render()` returns normal-looking stats (`draws=7`) AFTER an authored graph fails to record (the dispatch pin guards
  this gate, but the silence is real — harden at 18p close).
  ✅ **STEP 1 DONE 2026-08-15 — FS config-structural spec-const conversion, PIXEL-EXACT both backends** (centre=133 sky=169
  delta=36 unchanged; 33/2). The FS cook-time config demoted to D12 SPEC-CONST VALUES so the program STRUCTURE is
  policy-independent: `kImpostorGtSpec`(0,F32)=grid*tile, `kImpostorMipsM1Spec`(1,F32)=mips-1 (ONE node, both trilinear-clamp
  sites), `kImpostorLvl0Spec+k`(2..17,U32)=the 16 level offsets. The select chain PINNED at `kImpostorMaxMips=16`
  (impostor_atlas.hpp; LOUD CRD_LOG_ERROR+null if a tile needs more) — mi_u clamps to live mips-1 so k≥mips are dead branches
  patched to 0 (pixel-identical, not GLSL-identical). Spec ids in scene_renderer.hpp. Order per advisor: build →
  set_spec_const(live, non-vacuity count>0 check) → lower_entry → create_program (both backends bake the node cval — no
  VkSpecializationInfo — so patch-before-lower IS the mechanism). VS unchanged (grid/tile from kHdrAtlasDims at runtime).
  NEXT STEP 2 = extract VS/FS build into dither-parameterized units + env-guarded emit of the 4 impostor_{vs,fs}_{plain,dither}.ckir.
  (tidy+WSL+full-binary sweeps wait for STEP 5 per advisor.)
  ✅ **STEP 2 DONE 2026-08-15 — extract + emit, still PIXEL-EXACT** (133/169/36, 33/2). The VS/FS builds are extracted into
  two dither-parameterized private methods `build_impostor_vs`/`build_impostor_fs(crd::kir::KGraph&, KEntry&, bool has_dither)`
  (a verified Python move → same class-member indent, no re-indent; the caller owns create/patch/lower so ONE build serves both
  the device program AND the asset). Env-guarded emit (CRD_EMIT_IMPOSTOR, recipe B) writes the 4 assets under asset_root/ckir/
  via ckir_write. VERIFIED on disk: `assets/ckir/impostor_{vs,fs}_{plain,dither}.ckir` — the FS assets carry exactly **18
  spec-const nodes** (axes=kSpecConstFlag, iidx 0..17), the VS assets carry **0** (VS reads grid/tile from kHdrAtlasDims at
  runtime). ⚠ the 4 assets are UNCOMMITTED — commit them before STEP 3. NEXT STEP 3 = thin-load via resolve_program_text
  ("ckir/impostor_{vs,fs}_{plain,dither}") → ckir_read → (FS: set_spec_const(live)+lower) → create, select by has_dither, DELETE
  the inline builders + emit (deletion is the proof).
  ✅✅ **STEP 3 + STEP 4 DONE 2026-08-15 — the impostor is now DISK-DRIVEN; the builders are DELETED (deletion is the proof).**
  STEP 3: ensure_impostor_program is a THIN loader — resolve_program_text (app://ckir shadows engine://ckir) → ckir_read →
  (VS: create, no lower/patch; FS: set_spec_const(live grid*tile / mips-1 / 16 offsets) with non-vacuity count>0 → lower →
  create) → create_raster_program; LOUD on missing/invalid asset. build_impostor_vs/build_impostor_fs + the CRD_EMIT_IMPOSTOR
  emit are DELETED (−21 KB from scene_renderer.cpp). Renders PIXEL-EXACT 133/169/36 BOTH backends FROM DISK (the asset IS the
  program). STEP 4: kir round-trip gate (test_ckir_asset.cpp, [ckir18q], 361/14) pins the STRUCTURAL proof the pixel gate can't
  see — FS carries EXACTLY 18 spec-consts (ids 0..17, via g.node/is_spec_const iteration), VS carries ZERO, the dither variant
  is structurally larger, and a set_spec_const patch LANDS (two values → different serialized graphs). tidy-clean (4 files).
  STEP 5 remaining = full-binary + WSL sweeps + D-007 MAP strike (DONE above) → 18p CLOSE. ⚠ COMMIT: the 4 assets +
  scene_renderer.cpp/.hpp + impostor_atlas.hpp + test_ckir_asset.cpp.
- COMPUTE: ~~`ensure_skin_compute_kernel`~~ ✅ CONVERTED (18p, `ckir/skin_compute.ckir` — 1st COMPUTE-stage asset; inline
  statement kernel [2 FK passes + kHdrGpuSkinActive gate], NO lower, backend-neutral, all params header-read; REN-40-F 37/2
  both backends via the engine://ckir load) · ~~`ensure_palette_snapshot_kernel`~~ ✅ CONVERTED (18p, `ckir/palette_snapshot.ckir` — COMPUTE; inline statement kernel
  [palette→prev_palette copy + kHdrGpuSkinActive gate], NO lower, backend-neutral; 17e snapshot-build + REN-40-F 37/2 via
  engine://ckir load + kir round-trip) · ~~**`build_cluster_light_cull`**~~
  ✅ CONVERTED + DELETED (18a-1, `scene_light_cull.ckir`).
- ALREADY asset-driven (cook disk-first — KEEP): `cook_fs` surface FS (`.crdm`/`.crdt`/`.crdl`) · cull kernels
  (`scene_cull*.crdv`) · rt kernels (`scene_rt_*.crdv`) · tess/mesh/visbuffer VS.
- PASS-KIND CONTRACT PRIMITIVES (§100/§101 Level C — NOT algorithms; explicitly exempt, re-confirm at conversion):
  `ensure_visbuffer_fs` fixed id-write (1672) · the empty depth-only shadow FS. `ensure_flat_fs` (1634) is a HYBRID
  (cooks `flat.crdm` via `unlit()` but keeps a C++ `flat_build_surface` fallback) → convert the fallback.

**Re-opened / re-scoped rows (status flips below):**
- **CEIR-18a-1 ◧ RE-OPENED** — the cull PRODUCER must be an authored program asset (declaration + cooker + disk-first
  cook), `build_cluster_light_cull` DELETED. Test kit + gate SURVIVE; re-prove against the asset-cooked kernel.
- **CEIR-18c ✅ RESOLVED 2026-08-15** — `deferred.frame.toml` + device gates STAY GREEN; `ensure_deferred_lighting_program`
  is now a THIN disk-first load of `assets/scene/deferred_lighting.ckir` (node-graph `.ckir`); the hand-built FS is DELETED.
  Verified: scene-render `CEIR-18c*` 44/2 both backends + kir `[ckir18q]` 208/5 (win + linux-gcc); tidy-clean.
- **NEW CEIR-18p (cross-cutting)** — author + delete every hand-built program/kernel in the MAP; each a deletion-proven
  sub-slice; advisor at form-lock. (Depth/shadow depending: some ride their renderer slice; skin/palette ride 18b/18e.)

⛔⛔⛔ **SUPERSEDED 2026-08-15 (user escalation — "CEIR AND CKIR MUST BE AUTHORABLE AND ASSET DRIVEN, I DON'T ACCEPT ANY
OTHER THING"): the TERMINAL authorable form is the IR ITSELF** — a CEIR **text** program (`crd::ceir` `parse`/`print`) or a
CKIR **`.ckir` serialized asset** (a NEW `ckir_write`/`ckir_read` over `KGraph`+`KEntry`, the text twin of `ckir_serialize`'s KGPH blob).
"Declaration + cooker + disk-first cook" is **RETRACTED as the terminal bar** — cookers/declarations/C++ builders survive
ONLY as NON-PRIVILEGED FRONTENDS emitting the same canonical IR (tracker §38-39). A C++ builder may exist ONLY as a
BOOTSTRAP whose PRINTED graph is committed as the text asset, then the builder is DELETED. **The per-class table below is
RETAINED as the frontend map, NOT the terminal bar.** The conversion is CEIR-18q (build the CKIR text form) → the identity-
preserving bootstrap recipe (print builder → commit text → parse disk-first → `serialize(parse(print(g)))==serialize(g)` by
hash → device gate re-runs against the parsed graph → delete the builder) → applied to every 18p builder. `cook_light_cull`
(the declaration+cooker attempt) is DELETED.

⭐⭐ **CEIR-18q PROGRESS (2026-08-15): the `.ckir` NODE-GRAPH FORM SHIPS + is GATE-GREEN.** `engine/kir/include/crd/kir/ckir_asset.hpp`
— `ckir_write(KGraph,KEntry)→String` + `ckir_read(text)→{KGraph,KEntry}`. ⛔ REWRITTEN 2026-08-15 (user rejected the first flat
token-stream as "assembly"): the form is now a NODE-EDITOR-READABLE **TOML node graph** — `[[entry]]` + `[[node]] id="n5"
op="Add" in=["n2","n3"]` with ONLY non-default fields printed + a `[[stmt]]` list — the human/editor TWIN of the KGPH cook
blob (op/stmt/enum NAMES, `cval` as an exact hex bit pattern, `serial_restore` reconstruction; HAND-parsed so kir stays
toml++-free — core IR dependency-clean).
GATE `tests/kir/test_ckir_asset.cpp` `[ckir18q]` = **384 assertions / 4 cases GREEN (win-debug)**: name-table BIJECTION (all
~180 KOps + 24 KStmtKinds, no dup collision) · BYTE-EXACT round-trip `serialize_graph(parse(print(g)))==serialize_graph(g)`
on the compute light cull + a scale kernel + a raster FS · malformed-input REPORTED (never thrown). `cook_light_cull`
reverted. **NEXT (advisor-locked):** WSL linux leg (gcc portability: `%lld`/signed-char tokenizer) → BOOTSTRAP the cull (a
one-shot emitter TEST writes `assets/compute/scene_light_cull.ckir`; scene_renderer loads it disk-first via `asset_text` +
`register_kernel("engine://scene/light_cull")`, LOUD on a missing asset; a COMMITTED gate proves
`serialize(parse(read_file))==serialize(builder)` BEFORE `build_cluster_light_cull` is deleted, then the RHS drops and the
device parity-vs-oracle-vs-analytic gate carries it) → 18p sweep, ONE builder at a time, each with its own suite. ⛔ the
committed cull text bakes the GATE scene's constants (16 clusters / 6 lights / cap 8) — 18a-2 step 2 needs the shipped-`.crdl`
variant (D3 variants / D12 spec-constants, already answered).

⭐⭐ **CEIR-18a-1 CONVERSION IN PROGRESS (2026-08-15): the cull is now a committed `.ckir` ASSET.**
`assets/compute/scene_light_cull.ckir` (39,162 bytes, 332 nodes, readable op names) is MACHINE-WRITTEN by the hidden
`[.emitckir]` regen tool (`build_cluster_light_cull` → `ckir_write` → file, never scrollback-pasted). The COMMITTED
identity gate (`tests/kir/test_ckir_asset.cpp` `[ckir18q]`, now **389 assertions / 5 cases GREEN**) proves
`serialize_graph(ckir_read(read_file(scene_light_cull.ckir))) == serialize_graph(build_cluster_light_cull(...))`
byte-exact — the asset IS the source of truth. **NEXT = the DELETION:** re-point the gpu-context Vulkan/DX12 cull gates
(+ `ckir_light_cull_test.hpp`) to `ckir_read` the `.ckir` instead of calling the builder, DELETE `build_cluster_light_cull`
+ `engine/kir/include/crd/kir/ckir_light_cull.hpp`, degrade the kir identity gate to "asset parses + drives the device
parity-vs-oracle-vs-analytic" (no A==A), verify both backends. Then 18a-2 wires scene_renderer to load the SAME `.ckir`
(shipped-`.crdl` variant). WSL leg + MEMORY.md compaction still owed.

⭐⭐ **CEIR-18p TAA CONVERSION DONE (2026-08-15): `ensure_taa_program` is now a THIN `.ckir` load — 3rd conversion, 1st needing
LOAD-TIME LOWERING.** `assets/scene/taa_resolve.ckir` (32,914 bytes, >100 nodes: 3×3 variance box + Catmull-Rom history
reconstruction + matrix reproject) is the authored FS graph; `ensure_taa_program` does `asset_text → ckir_read →
lower::lower_entry → create_program → create_raster_program`, LOUD on missing. The inline builder is DELETED. **Two FORKS
from the deferred recipe (advisor-locked):** (1) the asset is stored **PRE-lower** (authored high-level graph — Step/Floor/
ternary VISIBLE for a node editor), and the load path LOWERS it (deterministic ⇒ load→lower == the old build→lower, bit-
identical); deferred needed no lower (its ops are create_program-native). (2) the graph BAKED the NDC±Y sign
(`ndc_y_points_down()?+1:-1`) into constants — a single committed asset can't carry both backends' signs (the ⛔⛔⛔ NDC±Y
scar), so STEP 1 first moved the sign to a RUNTIME constant (TAA constants **word 20**, filled at upload; FS reads `rload(20)`)
making the graph BACKEND-NEUTRAL, verified bit-identical (velocity gate 22/1 V + 22/1 DX12) BEFORE the emit. GATE (all green):
scene-render REN-41 velocity/TAA gate **22/1 Vulkan + 22/1 DX12** on the thin-load path · kir `[ckir18q]` **215/6** (the new
committed-asset round-trip gate — proves tex_sample_at's texture-INDEX operand + int_bits_to_float + storage_load survive the
ckir form) on **win-debug AND linux-gcc-debug** · deferred CEIR-18c 44/2 both backends UNREGRESSED · tidy-clean. Bootstrap =
an env-guarded (`CRD_EMIT_TAA`) one-shot `ckir_write` inserted before `lower_entry` (exact builder ⇒ zero divergence), fired
via the 17d gate, then REMOVED with the builder. ⛔⛔⛔ SCAR SURFACED: `powershell -File X.bat` is a SILENT NO-OP (outer exits
0) — every scene-render "rebuild" this session was stale until caught; the fix is `cmd /c` + check `$LASTEXITCODE`
(memory `feedback_powershell_file_on_a_bat_is_a_silent_noop_use_cmd_c`). **NEXT = `ensure_moment_program`** (moment shadows FS).

**LOCKED FORM (2026-08-15, advisor + source-verified) — the authored-program form per class:**
| Class | Authored form (declaration → cooker → disk-first cook + `engine://` register) | Example / precedent |
|---|---|---|
| **Surface fragment** (per-material shading) | `.crdm` material + `.crdt` technique + `.crdl` lighting → material/technique/lightcook → `cook_fs` | ✅ ALREADY DONE (`scene.crdm`/`forward_csm.crdt`/`scene_forward.crdl`) |
| **Fullscreen fragment** (deferred_lighting, taa, hzb, moment, velocity_debug, post) | ⛔ SUPERSEDED by the `.ckir` node-graph (per the block above — the terminal form is the IR ITSELF): the FS `KGraph`+`KEntry` is emitted to a committed `.ckir` (`ckir_write`) + loaded disk-first (`asset_text`→`ckir_read`→`create_program`→`create_raster_program`), builder DELETED. ~~`.crdp` node-graph → draw post-cook → `register_raster`~~ | ✅ `deferred_lighting.ckir` (18c) — first FS conversion |
| **Geometry-stage compute/vertex** (frustum cull, mesh, tess, rt, visbuffer VS) | `.crdv` + `[stage]`/`[cull]` decl → vertcook → `cook_stage_named` → `register_kernel` | ✅ `scene_cull*.crdv` |
| **Lighting compute** (cluster light cull, and any light-set-derived kernel) | ⛔ SUPERSEDED by the `.ckir` node-graph (as above): the compute `KGraph`+`KEntry` → committed `.ckir` → disk-first `ckir_read` → `register_kernel`, builder DELETED. ~~`.crdl` decl → lightcook `cook_light_cull`~~ (`cook_light_cull` was REVERTED — the IR itself is the asset, not a decl+cooker) | ✅ `scene_light_cull.ckir` (18a-1 — DONE) |
| **Pass-kind contract primitives** (visbuffer id-write FS, empty depth FS) | native, §100/§101 Level C — NOT algorithms; explicitly exempt | `ensure_visbuffer_fs` |

⛔ Compute kernel = **declaration + cooker + disk-first cook** (form A), NOT a general node-by-node compute-graph TOML. The
user's two requirements map onto the two EXISTING seams: **"partially modifiable"** = edit the declaration; **"completely
replaced by new CEIR programs"** = shadow the `engine://` id via the RAF-9/10 registry with a program of ANY provenance
(replacement does NOT require the default's graph to be TOML — tracker line 38: "C++ builder" is a non-privileged authoring
frontend; the user: "does not mean we cannot do C++ … main point is authorable asset CEIRs").
⭐ **RESIDUAL GAP (surfaced, not built here):** a general graph-authored compute asset (`.crdc` — the compute twin of `.crdp`,
so an app authors a NOVEL compute algorithm as text, not just params) is NEW authoring infra = OUT of the "NO new native pass
algorithm / PROMOTION" band contract. If wanted, it is a future spine addition (§86 CKIR frontier), one explicit slice — never
smuggled inside an 18 row.
⭐ **18a-1 layout-scar dissolved:** cull PRODUCER (`cook_light_cull`) + consuming FS (`cook_lighting`) now cook from ONE
`LightingDesc` ⇒ the producer/consumer record-layout mismatch (the scar recorded in the 18a-1 row) is structurally impossible.

**Sequence:** (FORM LOCKED above) → redo 18a-1 (move `build_cluster_light_cull` graph into lightcook `cook_light_cull`; scene_renderer
cooks disk-first from the resolved `.crdl` + `register_kernel("engine://scene/light_cull")`; delete the kir header; re-point
`ckir_light_cull_test.hpp` + both backend gates at the lightcook cook from the shipped `.crdl` — blast radius light-cook/scene-render/kir/gpu-context×2)
→ 18c program (`ensure_deferred_lighting_program` → `.crdp`) → 18p sweep → resume 18a-2 → 18b → 18z. **NO more hand-built program builders under an asset-titled slice.**

---

## CEIR-17…35 — one gated row each; splits into sub-slices AT BAND OPEN (sized by CEIR-0z)

Each row cites the **dialect/subsystem §§ it builds**, the **proof §§ it must pass**, and the **corpus §§ it must
eventually express** — plus a reuse note (what already exists, so the band starts from the engine, SANITY #8).

| ✔ | Band | Scope → **gate/proof** · reuse | → §§ |
|---|---|---|---|
| ✅ | CEIR-17 | **DONE 2026-08-15 — Scene/ECS/geometry bridge (BAND OPEN 2026-08-14).** scene/query/resource resolver semantics; PROOF rigid/skinned/indirect scene rendering + GPU culling as CEIR — all 6 sub-slices ✅ (17a-e + 17z). A PROMOTION not a rewrite (the resolution/cull/skin were already authored CEIR; the band DECLARED their identity + proved them). ⛔ CURRENCY RULE held: ECS extract stayed HOST, CEIR sees handles/ids only (ADR-0106 #3); RenderResolvers EXTENDED not reinvented; no new ADR. ⭐⭐ The band CLOSE (17z deletion-gate re-run + HEAD baseline) SURFACED + FIXED 3 latent CEIR-replay drops the per-slice gates missed (§128 null-plan/MissingCeirPlan, WBOIT composite-blend, VRS shading_rate) — a STRONGER proof than one that found none. reuse: REN-39 indexed-pull, 40-A device cull, GVA-2 skinning, crd-geometry CPU. Design + STATUS → `docs/design/ceir-17-scene-bridge.md`. NEXT = CEIR-18. | §45 §46 §47 |
| ✅ | CEIR-17a | **DONE 2026-08-14 — DECLARED the `scene.resolve_*` intrinsic ops.** A NEW `scene` dialect: scene.ceirop.toml (resolve_material/technique/program/geometry, each `[op.native] provider=host` — the FIRST TOML intrinsics per ADR-0110; honest read effects SceneRead/HostStateRead [NOT Pure — hot-reload-aware]; ExternalNondeterminism) + FIVE opaque Extern type-classes (draw/material/technique/program/geometry — the attachment precedent, distinct TypeIds; hand-written scene.hpp/cpp) + `find_scene_misuse` (its OWN misuse enum — the typed-chain walk; phase a closed-vocab attr). `ceir 17a` tests (well-formed chain + mistyped negatives + phase vocab + intrinsic reflection). Gate: ceir [scene] 30/3 + full 3349/436 + opgen-drift; win-debug + win-asan + linux-gcc-debug + linux-gcc-asan; tidy clean. | §45 §100 |
| ✅ | CEIR-17b | **DONE 2026-08-14 — the host-impl seam + the chain evaluator.** Extended `RenderResolvers` with four scene-resolve `(fn,user)` pairs (currency = opaque `u64 SceneResolveHandle`, id-keyed §22-18); `evaluate_scene_resolve` (chain-shaped, NOT a general interpreter) walks the resolve ops SSA-order, VERIFIER-FIRST (find_scene_misuse → SceneChainMisuse), calls one callback/op with the RESOLVED upstream handle(s); null/0 callback → UnresolvedSceneHandle. `ceir 17b` device-free SENTINEL tests (chain THREADS + per-phase distinct + refusals). Gate: ceir-gpu [scene] 12/2 + full 540/37 on 2 Win + 2 Linux + tidy. ⛔ BOUNDARY: proved the chain THREADS, NOT "== the C++ path's handles" (17c wires the real REN-37 ladder + re-verifies thread_safe). | §45 |
| ✅ | CEIR-17c | **DONE 2026-08-14 (advisor RE-SCOPED then DOUBLE-RETRACTED): the phase-discriminating parity ORACLE, DEVICE-FREE, ZERO renderer changes.** The oracle proves *the CEIR resolve chain reproduces add_draws_scene's per-phase twin selection (frame_runtime.cpp:192-193), pointer-equal, EXHAUSTIVE over phases × fallback arms* (4 items {both twins / depth-null / velocity-null / both-null} × 5 phases = 20 chains; `chain(phase).program == the twin` + `chain.geometry == storage`). The real-render + device-test prescription was RETRACTED against UNIFORMITY (fill() passes twins through verbatim; selection is the uniform 192-193 branch; SceneHost is .cpp-file-local) + POINTER-OPACITY (assertion is pointer equality — sentinel programs serve identically) ⇒ no renderer change, scene-render leaves the blast radius. thread_safe re-verified → STAYS UNDECLARED (extract single-threaded). `ceir 17c` in tests/ceir-gpu/test_execute.cpp. Gate: ceir-gpu [scene] 73/3 + full 601/38; 2 Win + 2 Linux + tidy; frame-cook/scene-render untouched.** ⛔ The query→DrawList bridge ALREADY EXISTS (`frame.draw_list` op, ceir.frame band 15; `IFrameGraphHost::draw_list_query` REN-36.3-b runs the ECS query in the renderer → pre-resolved RenderDrawItems; the REN-37 ladder runs INLINE in the extract) — so the ⚠ new-ADR contingency is CLOSED (commitment made at band 15) and the flag-dance/parallel-path is MOOT. ⛔⛔ OPTION (b) REJECTED (per-draw evaluate_scene_resolve in the live path — contradicts 16d-live-3b's host-side reclassification + the perf constraint; the intrinsics are the DECLARED replaceable-tier identity, not a hot-path interpreter). DELIVERABLE: a host-side frame-scoped draw-handle table (index+1, id-keyed); the four real resolve callbacks over the extract's ladder; a PHASE-DISCRIMINATING parity oracle (`chain(opaque)==program`, `chain(depth\|shadow)==program_depth?:program`, `chain(velocity)==program_velocity?:program`) over EVERY DrawList item (the gpu_cull_verify pattern) — RIGID first (skip+COUNT skinned, no silent caps); re-verify `[op.native] thread_safe` from the real ladder. Gate: parity test on a REAL device + scene-render suite green untouched + ceir-gpu device-free; 2 Win + 2 Linux + tidy. | §46 |
| ✅ | CEIR-17d | **DONE + gated 2026-08-14 (advisor RE-SCOPED at band-entry): PROOF slice + shipped-config verify gate — GPU cull + INDIRECT are ALREADY authored CEIR.** GATE GREEN: `CEIR-17d GATE` (18 assertions) asserts `gc.instances[0]==gc.cpu_instances[0]` on shipped `forward_csm_gpu` (first-ever headless run of the full 1M config, clean) + step-down/non-vacuous pins + `bounds_mismatch==0`; win-debug (ctest #6119) + win-asan + linux-gcc-debug (llvmpipe FULL path) + linux-gcc-asan (ctest #5914, ASan-clean) + tidy gated-clean; ZERO renderer changes (one TEST_CASE, no new file). Vulkan-only (advisor-blessed; DX12 occ twin covers the indirect layout).** Orientation verified 4 facts: (1) the orchestration IS an authored frame graph — `scene_gpu_cull.frame.toml` + `forward_csm_gpu.frame.toml` (cull_reset/cull_view0..4/occlusion_* all `kind="compute"`; five-view = five authored passes, view_index a cook-time constant), records through `record_ceir_render` since CEIR-16b migrated compute.dispatch; (2) every cull kernel (incl. occlusion+mark) is HOST-RESOLVED via `program_registry.register_kernel` (scene_renderer.cpp:2996-3016) — NO inline dispatch; (3) INDIRECT is scene.raster (16d) — `cull_args` is `indirect_args`, raster passes draw off the device count (`DrawItem::args`); (4) `gpu_cull_verify` produces the CPU-vs-GPU counts (`read_gpu_cull_counts`) but the verdict-equality is INFO-LOGGED, CHECKed NOWHERE — and the only verify test (occ A/B) runs a BESPOKE inline TOML, not a shipped config (gates-run-configs scar). ⛔⛔ TRAP: post-frame count-equality is INVALID on the CASCADE views (phase-2 `occlusion_reset` zeroes them; `occlusion_cull` refills only view0). ⛔ The only no-phase-2 config (`scene_gpu_cull`) is STALE/UNCOOKABLE (references the unregistered `engine://scene/cull_compact` — superseded per-desc design; a compute-only FRAGMENT never rendered standalone; `test_frame_template_bridge.cpp:407` excludes it) → disposition fix-or-DELETE at **17z**. DELIVERABLE (advisor RE-DECIDED — test-only, zero renderer changes, real device): **ONE gate** on the SHIPPED live `forward_csm_gpu` — the EXACT `CHECK(gc.instances[0]==gc.cpu_instances[0])` on VIEW0 via the UNOCCLUDED-scene construction (well-separated cubes ⇒ HZB rejects nothing ⇒ occlusion is a provable no-op, gated pixel-wise by REN-40-G3); `>0` + ref-sanity `cpu_instances[0]∈[1,near_count]` (far cubes frustum-culled) + `bounds_mismatch==0` + `bounds_checked>0`; **pin** the `requires=["shadows"]`→forward_agx step-down (SKIP if `!set_shadows_enabled(true)`; post-render `gpu_cull()` effective + `compute_dispatch_count>0` + `draws>0`); views 1-4 UNASSERTED (zeroed by phase-2, comment cites TOML L239-254). Ordering scar: cull/verify/shadows flags BEFORE `set_frame_graph_toml`. No median-of-five (diagnostic readback). DX12 twin if the body ports. Design + STATUS → ceir-17-scene-bridge.md. | §46 §47 |
| ✅ | CEIR-17e | **DONE + gated 2026-08-14 (advisor RE-SCOPED): PROOF slice — SKINNED as CEIR; SURFACED + FIXED a real engine defect.** The palette compute (`gpu_skin` CKIR) is already authored CEIR (registered 3014); the skinned variant selection is the resolve_program dimension (6276-6305); REN-40-F already gates GPU==CPU palette bit-identical but on a BESPOKE cull-less TOML. The shipped-config gate (bit-identical CPU-vs-GPU skin on `forward_csm_gpu`) SURFACED a real defect: the `gpu_skin` kernel lacked the `kHdrGpuSkinActive` gate that its sibling `palette_snapshot` has (the whole-class-effects scar) — under a cull graph the cull sets dispatch_groups>0 even when skinning is OFF, so `gpu_skin` dispatched, read UN-uploaded skel/clip (upload gated at 5181), and CLOBBERED the CPU palette (diffs=2036). FIX: wrap the kernel body in `if (kHdrGpuSkinActive != 0)`, mirroring palette_snapshot verbatim → post-fix diffs==0 (confirms diagnosis + fix + gate). ⛔ NOT test-only (engine kernel change). ⭐⭐ Strongest gates-run-configs vindication yet (the bespoke TOML's dispatch_groups==0 masked it for the feature's whole life). GATE: win-debug FULL suite 1587/74 + [skinning] 67/3 (REN-40-F V+DX transparent when active=1) + 17e 30/1; linux-gcc-debug + linux-gcc-asan [skinning] 49/2; tidy clean; win-asan FULL suite 1587/74 (ASan-clean). Design + STATUS → ceir-17-scene-bridge.md. | §47 |
| ✅ | CEIR-17z | **DONE + gated 2026-08-15 (advisor-scoped): band proof + 0h deletion — and SURFACED + FIXED 2 MORE latent CEIR-replay regressions (WBOIT + VRS).** ⛔ NO WIRING of `scene.resolve_*` into the live path (locked by 16d-live-3b host-side reclassification + 17c-option-b "replace at the seam WHOLESALE" — the ladder was given a declared identity, never replaced; wiring touches the extract hot loop). ⇒ **0h deletion is LITERAL** (band 17 was PROOF/re-scope — resolution/cull/skin already authored CEIR; imperative deletion was §128/CEIR-16). ONLY deletion artifact: **DELETE (not fix)** `assets/frame/scene_gpu_cull.frame.toml` (unregistered `cull_compact`, superseded per-desc design, zero consumers; fixing = untested asset, fixture-locked-valid scar) + update 3 references (test_frame_template_bridge.cpp:407, test_vulkan_frame_graph.cpp:7831, ceir-15 doc L171); gate on full frame-cook + gpu-context-vulkan suites. BAND PROOF = row-per-claim table (rigid/skinned/indirect × V/DX + smoke), each RE-RUN not inherited (GATE-reverifies-rows); holes: DX12 indirect via the occ pixel-A/B (`k40gOccCullToml`), sandbox smoke `--gpu-skin`+verify (CRD_ASSETS_DIR + LOAD-not-clear scars). ⭐⭐ CLOSE: scene_gpu_cull DELETED (+3 refs); band-proof matrix COMPLETE (rigid/skinned/indirect × Vulkan/DX12 + sandbox `--gpu-skin --gpu-cull-verify` smoke 185 frames/5031 instances). ⛔⛔ The deletion-gate RE-RUN + a HEAD baseline SURFACED 3 latent CEIR-replay drops the per-slice gates all missed — every one FIXED forward: (a) §128 null-plan (build_frame_plans install-site hole) → LOUD `MissingCeirPlan` + execute_frame_graph plan-build; (b) WBOIT composite-blend DROPPED by build_fullscreen_ceir (emit the blend attr); (c) VRS shading_rate/conservative DROPPED post-migration → payload-forward like force_load (RenderResolvers + record_ceir_render + materialize_draw_packet). GATE (full 2 Win + 2 Linux + tidy): gpu-context-vulkan 260/0 win-debug + win-asan (ASan-clean); 259/1skip linux-gcc-debug + linux-gcc-asan (VRS caps-skips on llvmpipe = correct); blast radius (frame-cook 2751/96 + render-graph 309/15 + scene-render) green all 4; tidy clean. MEMORY.md compacted (16 dormant scars→nav). Design + STATUS → ceir-17-scene-bridge.md. | §127 §178 |
| ✅ | CEIR-18 | **✅ BAND CLOSED 2026-08-16 — every renderer (Forward+ 18a / Clustered 18b / Deferred 18c / Visibility 18d / GPU-driven 18e / impostor 18p / .ckir form 18q) is an AUTHORED CEIR asset; the shipped path has ZERO hand-built KGraph (18z ensure_* audit found+converted the last one, visbuffer_fs); 18z band-proof matrix green 4 configs + llvmpipe + sandbox smoke. Renderer proof suite (absorbs RPL) — BAND PLANNED 2026-08-15 (advisor-scoped at CEIR-17 close).** Forward+ · Clustered · Deferred · Visibility · GPU-driven as AUTHORED `.frame.toml`/`.crdr` assets; **NO new native pass algorithm** (SANITY #8 PROMOTION — every ingredient already ships). ⛔ BAND-OPEN EXISTENCE CHECK **RESOLVED** (the advisor's Forward+/Clustered risk): clustered light-binning ALREADY EXISTS as **B8-l** — `ckir_render.hpp` `cluster_z_slice`/`cluster_coord` (exponential Doom-2016 froxel: screen tiles × log depth slices) + the light-sphere-vs-cluster cull + `ckir_scan` compaction, and it is a COOKED lighting-asset feature tested in `test_lighting_asset.cpp` (lights-per-froxel cap ⇒ smaller program) + `ckir_raster_triangle.hpp:2254`. So Forward+ (degenerate 1-slice = tiled) and Clustered (full froxel) are ASSEMBLY of the shipped B8-l cull + REN-37 forward shading, **not** new passes; the retired IRenderPath "Clustered Forward+" cornerstone (ADR-0105 deletion) is now this asset-driven form. Deferred=B8 (green); Visibility=16z visbuffer (green, dissolved into scene.raster); GPU-driven=17d device cull + REN-38 bindless/multidraw + REN-39 indexed-pull indirect. ⛔ EXECUTION ORDER (advisor): the CONFIRMED-green promotions FIRST (18c Deferred, 18d Visibility) so 18z's proof-harness shape — authored asset + real-device gate + DETERMINISTIC reference verdict/image — locks early; then 18e (17d+REN-38/39), then 18a/18b (assemble the B8-l froxel cull into forward). ⛔ 18z PROOF is ABSOLUTE: each renderer an authored asset running the FULL suite on a REAL device against deterministic reference verdicts/images (NEVER CEIR-vs-CEIR — the delete-ref→A==A scar); may have nothing imperative left to delete ⇒ the proof IS the authored-asset device gate + a deletion-gate re-run + HEAD baseline (the 17z discipline that caught 3 latent drops). Sub-slices below; advisor at each slice-entry; gate each 2 Win + 2 Linux + tidy. | build §40; corpus §87 §88 §89; proof §130 §131 §132; test §169 |
| ✅ | CEIR-18a | **✅ DONE 2026-08-16 — Forward+ (tiled) renderer as an authored asset, end-to-end device light-cull (18a-1 + 18a-2 all stages, full 4-config gate). Forward+ (tiled) renderer as an authored asset — EXPANDED to 18a-1/18a-2 (advisor-scoped at entry 2026-08-15).** ⛔ ENTRY SWEEP corrected the band-open "assembly, not new passes" optimism (gate-reverifies-rows): the B8-l froxel MATH ships (`ckir_render.hpp` cluster_z_slice/cluster_coord/light_cluster_cull/sphere_aabb_sq_dist) and a per-FRAGMENT math viz test exists (`tests/gpu-shared/ckir_raster_triangle.hpp:2256` build_cluster — one box vs one light, NOT a list-builder), and the FS CONSUMPTION is cook-capable (lighting_asset.hpp E5: FS reads a per-cluster index list bounded by `max_per_cluster`) — but the **PRODUCER (cull compute → per-tile light-list buffer), the tile-AABB table, the scene point-light-array provisioning (B8-m — scene_renderer.cpp:2173 confirms a FIXED key light today, no point-light plumbing), and the parity/image gates DO NOT EXIST.** ⇒ real work, two sub-slices below. reuse: B8-l math + REN-37 forward stack. advisor at each sub-slice design lock. **✅ STAGE 2a DONE + GATED 2026-08-16 (CPU-written per-tile list consumed by the clustered forward FS; E5 first-device defect FIXED forward).** The renderer now provisions a per-VIEW froxel light-list: a FIXED `kSceneClusterListWords` (16 clusters × cap 8 = 128) section placed RIGHT AFTER the atlas in the group layout (⛔ INSIDE the consolidated region — `region_words` extended to `cluster_list_off + kSceneClusterListWords`; skel/clip/prev_* live only in the PRIVATE buffer so a section after them would fall OUTSIDE a consolidated group's region, and this is the FIRST section a plain group uploads to BOTH buffers); `header[kHdrClusterOff]` published ABSOLUTE (`region_base + cluster_list_off`, mirroring the cull's bounds base) since the clustered FS reads `hdru(cluster_off)` from word 0 (canonical header) and the list is NOT inside the header. `set_cluster_light_list(ConstSpan<u32>)` stores it; uploaded each frame in both the use_region and private branches. The clustered technique = `forward_authored_clustered` (a SEPARATE `scene_forward_clustered.crdl` because `cluster.enabled` bakes a different FS unroll; `body_scene_authored` PARAMETERIZED via `Technique.user` = the LightingDesc*, one body serving both non-clustered + clustered). ⛔⛔ THE E5 DEFECT (advisor-predicted, fixed forward): `body_scene_authored` hard-coded `in.frag_xy = -1` → the cluster index fell back to a const (0.5,0.5) → EVERY pixel mapped to one froxel (no discrimination). Root was a UNIT CONFLICT: `frag_xy` is documented PIXEL coords (PCF/ign per-pixel rotation) but the cluster block did `frag_xy * grid` expecting NORMALIZED [0,1). FIX (advisor Option C): feed `FragCoord.xy` (pixel) to `in.frag_xy`, and the cluster block NORMALIZES by NEW viewport-dims header words `kHdrViewportW/H`=111/112 (`LightingHeaderMap.viewport_w/h`, crdl `[header] viewport_w/h`, parse+serialize+variant-id-hash-under-cluster.enabled+cook-time BadCluster-reject-if-0; renderer writes `target.width()/height()` each frame; init_programs LOUD-validates the crdl words == the engine kHdr ABI). Also CLAMPED the floored froxel axes to [0,grid-1] (edge pixels floored to `grid` = OOB). GATE (ABSOLUTE, discriminating, never CEIR-vs-CEIR): `point_cluster_gate_body` (Vk+DX12 twins) — a screen-filling WHITE quad, 2 point lights BOTH at (0,0,3) r10 (each fully covers the quad) RED+GREEN, CPU list stripes col1→RED col2→GREEN col0→row-alternating else null(black record 4); probe A(96,128)=froxel9 reads RED-not-green (g<25 IS the clustering proof — an all-lights FS would leak green), B(160,128)=froxel10 GREEN-not-red, C/D column-0 different rows land opposite colours (row-term proof, Y-flip-immune column striping + sign-agnostic row check); `key`={0,0,-1} (directional behind ⇒ channel purity), ValidationCapture val_err==0. GREEN: **win-debug Vulkan + DX12 (scene-render 1800/84, cluster gate 37/2), linux-gcc-debug llvmpipe (cluster+Stage1 36/2), light-cook 151/12 win+linux (new viewport contract + non-vacuous no-viewport→BadCluster negative test)**; scene_renderer.cpp/lighting_asset.cpp + both test files tidy-clean (LLVM 20). ⛔ GATE (per the ratified 2-Win+2-Linux+tidy contract, module-scoped): **win-debug ✅ + linux-gcc-debug/llvmpipe ✅ + linux-gcc-asan ✅ (ASan-CLEAN 2026-08-16: light-cook 151/12 + [ceir18a] 36/2 on the ASan-instrumented scene-render + light-cook targets, exit 0) + tidy ✅**. **win-asan** module run rides the 18a-2 CLOSE (this is a Stage sub-slice, not the slice close; disclosed, not silently skipped — the 18c disclosure pattern). ⛔ 2b-BLOCKING ROW FILED (advisor item 4, deferred not dropped): the empty-slot sentinel is TODAY a BLACK point-light RECORD (record 4, color 0) — valid for 2a because I set only 2 lights, but NOT a robust convention. **Stage 2b MUST lock the canonical sentinel `0xFFFFFFFF` + an FS guard `slot < record_count` (cook-time constant) ⇒ zero contribution**, when the GPU light-cull writes the list (record 4 stays valid under that guard, so the 2a gate survives). NEXT: 2b (GPU producer — the light-cull `.ckir` from 18a-1 writes the per-tile list; advisor at design lock), Stage 3 image kit, then 18a-2 close. **⛔ STAGE 2b DESIGN-LOCKED (advisor, 2026-08-16).** The 18a-1 `scene_light_cull.ckir` (baked 16 clusters / **6** lights / cap 8, VIEW-space AABBs, output = 0-based POINT-array indices, null=num_lights) does NOT match the scene (4 = kMaxScenePointLights, world-space header records, FS reads slots as ABSOLUTE record indices, black-record sentinel). SEQUENCE: **(2b-i ✅ DONE 2026-08-16 — win Vk+DX12 70/4 + light-cook 151/12 + llvmpipe 36/2 + tidy) CONSUMER CONTRACT** — the cluster slot becomes a 0-based POINT index: FS `record_of` = `light_base + (first_point + slot)*stride` (symmetric w/ the non-clustered `(first+i)` path), GUARD `slot < count.point` (cook-time const) ⇒ ZERO contribution. ⛔ DROP the 0xFFFFFFFF magic — the GUARD is the contract, and the kernel's `null_index = num_lights` already satisfies `≥ count.point` (a re-bake to 0xFFFFFFFF is a pointless axis). ⛔ NaN LANDMINE: `NaN×0=NaN` — CLAMP slot to [0,count-1] for the ADDRESS **and** SELECT-zero (Mix/ternary, NOT mul) the contribution. Stage 2a gate list re-values (RED 1→0, GREEN 2→1, null 4→≥count.point); re-run 2a gates + llvmpipe. **(2b dispatch-cell VERIFY ✅ DONE 2026-08-16 — CONFIRMS the design lock, no reshape)** — the frame-graph compute-pass model: a pass declares `kernel="engine://scene/X"` + `writes=["instances"]` (the group-buffer proxy) / `["cull_args"]`; the kernel binds those named resources. TWO kernel families exist: the scene FRUSTUM culls are `.crdv` (vertex-cook decl + `[header]` word map + base from `cull_args` params[0]=cdst_base), but the LIGHT-cull must follow the **`palette_snapshot.ckir` precedent** — a `.ckir` COMPUTE kernel (1 group-buffer BufferDecl, addresses sections via `hdru(offset)` header words + the invocation-index Builtin, writes the group buffer in place) dispatched per-group via a frame-graph compute pass. So the re-emit reads the light records + the froxel-AABB section from the group buffer and writes the list at `base + cluster_list_off` (base-offset write axis, base from params — the cull precedent). ⛔ Binding the group buffer as a naive flat list-output writes at word 0 = the HEADER; the base offset is mandatory. **(2b kernel RE-EMIT, not a sibling — inert-copy-rot scar)** ⛔ CORRECTION 2026-08-16: `build_cluster_light_cull` is NOT in git (`git log -S` empty — the whole 18a-1 builder lived + died inside uncommitted work; everything past CEIR-16 c44adbf is uncommitted). RE-AUTHOR it instead from the STILL-PRESENT froxel helpers in `ckir_render.hpp` (`light_cluster_cull` / `sphere_aabb_sq_dist` / `cluster_coord` / `cluster_z_slice` — the hard math survives; re-write only the per-cluster assembly + serial compaction), following the **`palette_snapshot.ckir`** buffer model (group buffer + `hdru` offsets + base), re-param num_lights=4 (= kMaxScenePointLights = crdl `count.point`, triple-equality stated + parity-enforced), list writes at `base + cluster*cap + slot`; port the 18a-1 kit to 4 lights (keep MARGIN/NON-VACUOUS: single-tile, corner-exact multi-tile, far-culled, ≥1 empty), re-pin `g.size()`, both backends. **(2b scene WIRING)** cull-input sections (16×6 AABB + 4×4 view-space lights, CPU-computed per frame) in the group buffer INSIDE the region; kernel `engine://scene/light_cull` via resolve_program_text; a compute pass in forward_plus.frame.toml (reads header / writes the list section), plan-table-rebuild at install sites. **(2b GATE)** ⛔ Y-FLIP = the named first-device risk (FragCoord y-down vs view-space y-up; a CPU reference from the SAME AABB helper SHARES the flip ⇒ parity passes, image wrong): parity needs an INDEPENDENT ANALYTIC leg (light→tile from projection math) AND the image gate needs one ORIENTATION-PINNED probe (top-screen light colours the TOP tile — 2a's sign-agnostic row check can't catch a mirror). **`set_cluster_light_list` precedence:** when the installed graph carries a light_cull pass the DEVICE pass OWNS the section + the CPU upload is SKIPPED with a LOUD log (probe-CONTROL scar); the API STAYS (provisioning seam + adversarial-list harness). **⛔ 2b DESIGN-LOCK REFINED (advisor, 2026-08-16, supersedes the view-space/base-from-params/count/git details above):** (a) DISPATCH = PER-GROUP (one 1-wg dispatch/group; each reads ITS word-0 header → its offsets → writes its cluster section) — REQUIRED because the gate runs shadows-on ⇒ consolidate is EXCLUDED (scene_renderer ~5471 "shadow-free frame") ⇒ the PRIVATE-buffer path, so a single per-view write would leave other groups stale; per-group redundancy = named follow-up; consolidated-path clustering = same expression, untested-on-device till a shadow-free clustered frame exists (disclose). (b) WRITE LOCATION = the kernel reads `hdru(kHdrClusterOff)` from word 0 (the SAME word the FS reads ⇒ producer/consumer agree by construction, raw private / absolute consolidated) — DROP "base from params". Kernel = palette_snapshot form (1 BufferDecl + baked header-word constants); the re-authored builder takes the WORD INDICES as PARAMETERS (kir must not include scene-render's kHdr headers), the emit site passes the kHdr* constants (triple-equality `num_lights==kMaxScenePointLights==crdl count.point` stated there). (c) BIN IN WORLD SPACE (render() has only `proj*view`, no view matrix): unproject each tile's 4 corners at two NDC depths via `inverse(view_proj)` (✅ exists mat.hpp:206), AABB over the 8 world pts; lights go in UNTRANSFORMED from `impl.point_lights` (position/radius as-is — NOT 1/sqrt(falloff)). ⛔ REVERSE-Z LANDMINE: `perspective_reverse_z` is infinite-far ⇒ can't unproject ndc_z=0 → pick a finite far (unproject at a small ndc_z), document zmax, gate lights inside it. (d) DROP the count output (no consumer; the ported kit derives counts from the list = first-null index); KEEP every-slot-written + serial ascending compaction (bit-exact `eval_cpu_kernel` parity). (e) SECTIONS: AABB table (96 words) + lights (16 words) chained after the cluster list INSIDE the region (region_words=last section end); header words **113/114** (✅ free). (f) KEEP the re-authored builder IN-TREE (the git dead-end scar [[feedback_uncommitted_delete_loses_the_source_keep_regen_in_tree]]) — deletion is a LATER slice after the user commits. **SUB-STEPS:** 2b-ii ◧ IN PROGRESS 2026-08-16 — **builder ✅ WRITTEN + COMPILED + round-trip-verified + tidy** (`engine/kir/include/crd/kir/ckir_light_cull.hpp`, kept IN-TREE per the git-dead-end scar; `LightCullParams` carries the word indices; palette_snapshot header-indirection form: 1 group BufferDecl, section bases from `buffer_load(buf, list/aabb/light word)`, per-thread cluster=GlobalInvocationId.x, serial ascending compaction storing POINT indices, tail padded with null_index; new kir gate `[ceir18a2]` builds it + pins Compute@64 + refuses bad params + `ckir_roundtrip_diff==-1`; **NODE PIN: 213 nodes / 25 stmts** for 16/4/8, vs the old 6-light 332). **✅ EMITTED + kir-green** (2026-08-16): the builder is FINALIZED and `scene_light_cull.ckir` regenerated from it via the `[.emitckir]` regen test (`crd-kir-tests "[.emitckir]"`). ⛔⛔ TWO eval-compat fixes the first build surfaced (the compute/statement tier is SCALAR): (1) `LocalInvocationIndex` (scalar uint; 1 workgroup ⇒ = the cluster), NOT `GlobalInvocationId` (a uvec3 — `eval_cpu_kernel` returns 0.0 for it AND asserts on the vec3+swizzle); (2) the sphere-vs-AABB test written COMPONENT-WISE on scalar nodes (the `render::light_cluster_cull` vec3/dot helpers are RASTER-tier — `eval_cpu_kernel` asserts on Vec*/Dot). The u32-buffer float reads use `int_bits_to_float(cast(load,I32))` — ✅ `eval_cpu_kernel`'s `apply_unary` models IntBitsToFloat (memcpy-reinterpret), so the oracle stores u32 bit-patterns as f64. NODE PIN re-measured: **250 nodes / 25 stmts** (component-wise > the vec3 213). kir asset tests **368/15 green** (18a-1 g.size 332→250; `[ceir18a2]` builder round-trip); builder + test tidy-clean. **✅ 2b-ii DONE 2026-08-16 — the gpu-context DEVICE leg is GREEN both backends.** The gpu-shared kit is ported to the SYNTHETIC GROUP BUFFER (one u32 array: header words [110]=list_off [113]=aabb_off [114]=light_off → AABB (96 f bit-patterns) / light (16 f {center,radius}) / list (128 u32) sections); ClusterCullScene 4-light (single-tile c0 + corner-exact c5/c6/c9/c10 + c15 + far-culled L3 + 10 empty); cull_oracle uses an f64 bit-pattern buffer + eval_cpu_kernel; cull_dispatch binds ONE buffer; (list,count) signatures kept, count DERIVED from the list. The 2nd Vulkan scenario CONSOLIDATED to the kit (device==analytic seam). GATE: **Vulkan `[lightcull]` 285/2 + DX12 `[lightcull]` 152/1 (device list == eval_cpu_kernel == analytic, BIT-EXACT both backends) + kir `[asset]` 369/15** (builder round-trip + GLSL-emit + 250 nodes/30 stmts pin) + **linux-gcc-debug/llvmpipe ✅ (kir 369/15 + [lightcull] 285/2 — bit-exact on a 3rd device)**. ⛔⛔ TWO first-device defects fixed forward: (1) a SIGSEGV — a failed GLSL-emit/compile returned a NULL pipeline that `cull_dispatch` deref'd; added a null-pipe POISON-list guard so `emit_ok` fails LOUD. (2) the shaderc reject behind it = the IF-BLOCK SHARED-TEMP SCAR ([[feedback_ckir_if_block_shared_temp_scope_materialize]], 3RD recurrence) — the serial-compaction write cursor `w` + the list base `lbase` were declared inside light-0's store-`if`, `undeclared` in the siblings; fixed with `stmt_materialize(lbase)` + `stmt_materialize(w)` after each accumulate (the eval_cpu_kernel oracle passed the same graph — only the real backend caught it). ⛔ TIDY DISCLOSURE: engine files (ckir_light_cull.hpp) + test_ckir_asset.cpp tidy-CLEAN; the shared-header `ckir_light_cull_test.hpp` (snake_case static-constexpr members) + test_vulkan_context.cpp (pre-existing main-gate multi-decl) trip the manual tidy-files.ps1 over-strictness (the [[reference_manual_header_filter_tidy_stricter_than_gate_on_shared_test_headers]] case — a shipped sibling `ckir_visbuffer_test.hpp` uses the identical accepted pattern). REMAINING 2b: 2b-iii (scene provisioning + frame-graph pass) → 2b-iv (parity + orientation-pinned image gate). **2b-iii ◧ FOUNDATION landed 2026-08-16 (compiles):** header words kHdrFroxelAabbOff=113 / kHdrLightViewOff=114 + section sizes kSceneFroxelAabbWords=96 / kSceneLightViewWords=16; MeshGroup froxel_aabb_off / light_view_off chained after the cluster list INSIDE the region (region_words → light_view_off + kSceneLightViewWords, the one-derivation tail moved); header publishes both bases ABSOLUTE (region_base + off). **(b) ✅ PROVISIONING DONE 2026-08-16 (compiles):** `compute_froxel_aabbs(view_proj, gx, gy, ndc_z_far, out)` (header-exposed, near frustum_aabb) — per-tile world AABB by unprojecting the 4 tile corners at ndc_z ∈ {1 (near), 0.01 (a FINITE far substitute for perspective_reverse_z's infinite far)}, ndc_y=2v-1 (the Y-flip convention, PROVEN by the 2b-iv probe); computed ONCE per frame in render() into impl.froxel_aabb_scratch, the light-input {position,radius} copied from point_lights (null slots far+radius-0); both uploaded into each group's froxel_aabb_off / light_view_off (use_region + private branches). ⛔ INERT until the kernel pass is wired (c); the froxel Y-flip is UNVERIFIED until the 2b-iv device gate. **(c) ✅ WIRING DONE 2026-08-16 (compiles):** `ensure_light_cull_kernel` (resolve_program_text "ckir/scene_light_cull" → ckir_read → create_program, palette_snapshot's exact shape) + `register_kernel(engine://scene/light_cull)`. ⛔⛔ ADVISOR KEY CATCH: do NOT add the pass to forward_plus (the device-owns precedence would SKIP the 2a gate's hand-written adversarial list ⇒ 2a fails). Authored a SIBLING **`forward_plus_gpu.frame.toml`** (like forward_csm vs forward_csm_gpu): `instances` external_buffer + a `light_cull` compute pass (kind=compute, draw_list=visible_geometry, writes=["instances"]) + the forward pass `reads=["shadow_atlas","instances"]`. The empirical Q is ANSWERED: reads=["instances"] is a SAFE ordering edge (indirect is keyed on the indirect_args KIND / SceneDraw.cull_args the scene sets, NOT a plain external_buffer read; fs_constants capture is fullscreen-only). Cross-referenced in both headers. WHICH is the shipped default = an 18a-2-close decision. ⛔ 2a gate keeps forward_plus (CPU list); 2b-iv installs forward_plus_gpu (device list); precedence (d) unchanged. **(d) DEFERRED (not gate-blocking — the 2b-iv gate sets NO CPU list ⇒ count==0 skips the upload anyway; the device pass last-writes regardless). A small robustness add: LOUD one-time log + skip when BOTH set_cluster_light_list AND forward_plus_gpu (probe-CONTROL); do after the gate.** ⛔ MULTI-INSTANCE WW race (per-group dispatch re-computes the same list) filed as a named follow-up. **2b-iv GATE DESIGN (advisor):** ⛔ RADIUS INVERTS from 2a — 2a wanted radius-10 (FS exclusion proof); 2b-iv proves the CULL BINS, so a big sphere overlaps many froxel AABBs → use TIGHT radii, each light placed so its PROJECTION lands at its tile's CENTRE (margin from boundaries, the 18a-1 MARGIN discipline pushed through projection), depth well inside [near, ndc_z_far world]. PARITY legs: device == eval_cpu_kernel = BIT-EXACT; the ANALYTIC leg = CONTAINMENT (each light present in its projected tile's run, absent from NON-ADJACENT tiles) — NOT exact-list equality (AABBs are conservative near shared corners → exact lists flake). ⛔ Y-FLIP: build NO per-backend sign — world→FragCoord is backend-invariant (impostor pixel-exact on Vk+DX12+llvmpipe proves it); ONE ndc_y sign serves both, the ORIENTATION-PINNED probe (tight light near screen-top → TOP tile lit) decides it; if it fails, FLIP ONE LINE in compute_froxel_aabbs (the 2v-1 sign — treat as a coin flip, the engine patches clip-Y per-backend elsewhere). Both backends + llvmpipe. **✅ 2b-iv DONE 2026-08-16 — the DEVICE Forward+ light-cull works END-TO-END, both Windows backends + llvmpipe ([ceir18a] 103/6 = Stage 1 + 2a + 2b-iv Vk+DX12; linux-gcc-debug/llvmpipe 53/3 Vk).** point_light_cull_gate_body installs forward_plus_gpu, sets 3 TIGHT lights (RED left, GREEN right, BLUE world-up), NO set_cluster_light_list ⇒ the device pass produces the list; probes prove channel purity (each tile only its light — the cull binned correctly) + orientation. ⛔⛔ TWO first-device defects fixed forward: (1) THE CULL PASS DIDN'T DISPATCH — `dispatch_groups` is derived only from `d.cull_groups` (the GPU-cull's count), which is 0 in forward_plus_gpu (no cull) ⇒ 0 groups ⇒ skipped; symptom = the list stayed default-0 ⇒ every tile read slot 0 = point 0 = RED (RED showed only where it illuminates). FIX: a `device_light_cull` flag detected at rebuild_frame_plans (scan the frame passes for a compute pass w/ kernel engine://scene/light_cull, via pass_is_compute + pass_str) sets dispatch_groups=1 for slot-0 items even w/o a GPU cull (+ drives the precedence). (2) THE NDC±Y MIRROR — the froxel is computed from RAW view_proj (backend-neutral) but the FS tiles by FragCoord (post-viewport, Vulkan ndc_y=-1 top / DX12 +1 top), so the froxel needs a PER-BACKEND ndc_y sign: `compute_froxel_aabbs` gained `flip_y = !IRasterContext::ndc_y_points_down()` (the velocity-spec-const precedent — NOT a new mechanism; the advisor's "one sign both backends" was disproven empirically, DX12 blu invisible w/ the Vulkan sign). ⛔ ORIENTATION assert is SIGN-AGNOSTIC (BLUE lit at EXACTLY one vertical extreme — catches the mirror = invisible; the absolute top/bottom follows each backend's geometry-Y). ⛔ FOLLOW-UP FILED: DX12's forward renders world-up at screen-BOTTOM (by elimination — BLUE illuminates + shows at the bottom on DX12 only); the CULL correctly follows it, but whether that DX12 geometry-Y is itself correct is a pre-existing NDC±Y question for the 18a-2 close. **18a-2 CLOSE (2026-08-16):** (d) ✅ precedence — a `logged_cluster_precedence` one-time LOUD log + the CPU cluster-upload SKIPPED when `device_light_cull` (probe-CONTROL); [ceir18a] still 103/6. REGRESSION ✅: full win-debug scene-render **1833/86** (was 1800/84 — +2 gates, no regression) + linux-gcc-debug/llvmpipe 53/3 + **linux-gcc-asan 53/3 ASan-CLEAN + win-asan [ceir18a] 103/6 ASan-CLEAN** ⇒ the FULL ratified gate (2 Win + 2 Linux + tidy) is GREEN. **✅ CEIR-18a-2 FULLY CLOSED — the Forward+ renderer is an AUTHORED ASSET end-to-end: authored frame graph + authored lighting `.crdl` + authored light-cull `.ckir` compute pass producing the per-tile list on-device, FS consumes it; no hand-built builder in the path (build_cluster_light_cull kept in-tree ONLY as the regen source, deleted after the user commits).** MEMORIES ✅: [[feedback_frame_graph_compute_pass_dispatch_groups_from_cull_groups]] + [[feedback_cpu_frustum_data_vs_fragcoord_needs_per_backend_ndc_y]]. context.md + commit-proposal ② updated (the new files: ckir_light_cull.hpp, scene_light_cull.ckir regen, forward_plus_gpu.frame.toml, forward_plus header xref, ensure_light_cull_kernel + device_light_cull + froxel provisioning). ⛔ OPEN FOLLOW-UP (filed, needs RenderDoc/orientation tooling — NOT blocking 2b, the cull correctly follows the geometry either way): does DX12's forward render world-up at screen-BOTTOM (a real upside-down bug) or is it a handled convention? Evidence: with the froxel per-backend flip, BLUE(world+y) illuminates+shows at screen-BOTTOM on DX12 only; needs a Y-asymmetric upright-proof (Vk vs DX12) to settle. ⛔ MEMORY.md at 19.8KB — compaction owed (still < 24.4KB read limit). REMAINING: win-asan result; then CEIR-18b. 2b-iii = provisioning (sections + header words + per-frame CPU fill: AABB via unproject, lights copied) + the frame-graph compute pass (⛔ resolve EMPIRICALLY: does adding `reads=["instances"]` to a plain geometry pass change its draw emission? — declare `instances` in forward_plus, plan-table rebuild, set_cluster_light_list device-owns precedence). 2b-iv = gates: parity (device == eval_cpu_kernel == INDEPENDENT analytic light→tile via project-center-to-UV-and-floor, NOT the AABB helper) + channel-purity image gate through the device path with an ORIENTATION-PINNED probe (top-screen light colours the TOP tile). | §40 §130 |
| ✅ | CEIR-18a-1 | **✅ DONE 2026-08-15 — THE FIRST MANDATE CONVERSION: the cull SHIPS as an authored `.ckir` asset; the C++ builder is DELETED.** `assets/compute/scene_light_cull.ckir` (332 nodes, readable) is loaded disk-first via the shared `read_cull_ckir` helper (`asset_text`-style, from `CRD_REPO_DIR`); `build_cluster_light_cull` + `engine/kir/include/crd/kir/ckir_light_cull.hpp` **DELETED** (git history = the regen escape hatch; the `.ckir` is the source, `ckir_read→mutate→ckir_write` is the regen path — NOT frozen). Device parity (list==oracle==analytic, provenance-independent) GREEN on BOTH backends: **gpu-context-vulkan `[lightcull]` 286/2** (the 18a-1 gate + the seam REWORKED onto the shipped 6-light asset driven by 2 real + 4 far-culled dummies — strictly stronger, proves the shipped asset) + **gpu-context-dx12 152/1**; **kir `[ckir18q]` 389/4** (identity gate DEGRADED to self-consistency `serialize(read)==serialize(read→write→read)` + structural pins 332-nodes/Compute/@64 — no builder RHS, no A==A). ✅ WSL/gcc leg GREEN (linux-gcc-debug `crd-kir-tests [ckir18q]` 389/4 — the tokenizer + `.ckir` read work on gcc/Linux paths). Shipped-config variant (different grid/cap) is 18a-2's (same asset + dummies / D12 spec-constants). ORIGINAL: **DONE 2026-08-15 — Forward+ cull PRODUCER + ABSOLUTE list-parity gate.** Authored `build_cluster_light_cull` (`engine/kir/include/crd/kir/ckir_light_cull.hpp`): DETERMINISTIC thread-per-cluster SERIAL survivor scan, NO atomics ⇒ ascending-index-ordered lists ⇒ exact `==`; per-cluster count + indices into a U32 storage buffer; over a caller-supplied tile-AABB table SHARED verbatim GPU+oracle (17d bounds discipline). Shared harness `tests/gpu-shared/ckir_light_cull_test.hpp` (ClusterCullScene / cull_oracle / cull_dispatch<MakePipe>) + Vulkan gate (test_vulkan_context.cpp) + DX12 twin (test_dx12_compute.cpp). PROOF (ABSOLUTE, never CEIR-vs-CEIR): device per-cluster list `==` the CPU oracle EXACTLY (all 144 out words, diffs==0) AND the oracle `==` the analytic `expected[]` (locks the reference); margin construction (small centred lights + a corner-exact dist²=0 multi-tile light + a far always-culled light) + non-vacuous (9 empty clusters, a light culled everywhere, unequal counts not at total). Parametric `num_slices` (gated at 1 = tiled) so 18b instantiates the same kernel. ⛔ SCAR HIT + ROOT-FIXED: the statement-tier `eval_cpu_kernel` is SCALAR — it ASSERTS on Vec/Swizzle/Dot nodes (ckir_kernel_eval.hpp:294); the sphere-vs-AABB test was rewritten component-wise scalar (mirrors the raster-tier vec `light_cluster_cull`). GATE (module-scoped, CI owns whole-repo): win-debug Vulkan 31 assertions + win-debug DX12 30 + linux-gcc-debug/llvmpipe 31 + linux-gcc-asan 31 (ASan-clean); tidy: engine header clean (naming errors a manual header-filter artifact — the shipped sibling ckir_visbuffer_test.hpp trips identical ones + ships green ⇒ the gate doesn't enforce them on tests/gpu-shared). ⛔ LAYOUT RECONCILE (advisor at 18a-2 entry): the ORIGINAL `[count, idx…]` stride-(1+cap) list MISMATCHED the cooked E5 consumer (`lighting_asset.cpp:800` reads a FLAT stride-`max_per_cluster` list of light INDICES, NO count word; the loop bound is `min(set.count[type], cap)` so under-full slots must be ZERO-CONTRIBUTION). REWORKED the producer to the consumer's contract — flat stride-`cap` list + a `null_index` sentinel padding the tail (the cook guarantees a zero-colour record there at 18a-2b ⇒ provably 0), and the per-cluster COUNT moved to a SEPARATE binding-3 diagnostic buffer (the 17d read_gpu_cull_counts pattern; the FS never binds it, the gates + 18a-2b engagement pin do). Harness/gate updated (152 assertions), re-run all 4 configs GREEN. Reconciled while the producer was a day old with one consumer (its own gate). | §40 §130 |
| ✅ | CEIR-18a-2 | **✅ DONE — the device Forward+ light-cull works END-TO-END (Stages 1→2b-iv); completed as part of CEIR-18b (see that row + context.md; task #26 completed). The marker was STALE (the detail below is the mid-slice snapshot, kept as history).** ORIGINAL: Forward+ consumption via the LIVE forward pass — MERGED (advisor scoping correction 2026-08-15; was 2a/2b). ⛔ PIVOT RATIONALE: a standalone shader-level consumption gate was ABANDONED — dispatching the cooked lighting FS standalone requires REIMPLEMENTING the live forward-pass dispatch inside the test (the gates-run-configs class — proving a config nothing ships), NOT because the compute wall made it hard. TWICE the smaller isolated proof needed the production machinery it was isolating from (compute-wrap → StorageLoad-is-fragment-only; raster-standalone → the live dispatch) ⇒ the cooked FS's contract is INSEPARABLE from its frame plumbing; when isolation costs more than integration, integrate. ⛔ FINDINGS KEPT: E5 cluster loop (lighting_asset.cpp:800) was COOK-ONLY tested (program size, never device-run) ⇒ this is E5's first device execution (the 18c never-run class); the cooked FS reads via StorageLoad (FRAGMENT op set0/binding0, stage-dual with the compute emitter's Input, ckir_glsl.hpp:1661); the light section is RUNTIME/HEADER-based (light_base=hdru(header.light_off), cook bakes no light values) so null_index is a RUNTIME-UPLOAD convention; record fields position=0/falloff=3/color=4 (default stride 16); point_light=Filament (falloff=1/radius²; NaN if light_pos==world_pos ⇒ null-record position≠surfel + color=0 ⇒ 0 contribution); brdf·light_color PER-CHANNEL ⇒ red light → (·,0,0) exactly. KEPT ARTIFACT: the compute gate's cull SEAM (133 asserts, device producer list == analytic end-to-end) stays as a standing chained-dispatch regression (`CEIR-18a-2 GATE` in test_vulkan_context.cpp). STAGED PLAN — each layer verified before the next: (1) B8-m FIRST, clustering OFF — widen the scene light-section fill (per-type counts + point records via a set_point_lights-shaped API + the null record at index=total); gate: a point light illuminates a probe through the LIVE forward pass with cluster.enabled=false (exercises the cooked FS non-clustered point loop — E1 multi-light was cook-only too — in isolation). (2) CLUSTER ON — the cull compute pass in the frame graph (host-registered build_cluster_light_cull, 17d pattern) + the CPU froxel-AABB builder (inverting the HEADER cluster_grid mapping, NOT ckir_render::cluster_z_slice) + the list buffer as the frame-graph transient cluster_off points at; gate: 2-light discriminating probes (cluster A red/blue==0, cluster B blue/red==0) + empty-cluster + count-buffer engagement pin. (3) authored forward_plus.frame.toml + the 18e image kit + DX12/llvmpipe/asan. advisor at each step design lock. ⚙ **STAGE 1 IN PROGRESS 2026-08-15 — 3 FINDINGS (all never-device-run paths):** (F1) the shipped `forward_csm` technique (`body_forward_csm`, ckir_technique.hpp:553) is DIRECTIONAL+CSM ONLY — NO point-light loop; the point-consuming FS is `forward_authored` (`cook_lighting` from scene_forward.crdl, point=4), which NO frame graph wired ⇒ authored `assets/frame/forward_plus.frame.toml` (= forward_csm with the forward pass technique swapped to `forward_authored`). (F2) ⛔ the frame-graph pass `technique` field does NOT drive the forward FS — it is cooked from `m_impl->forward_technique` at init_programs (scene_renderer.cpp:3100); the gate needs `set_forward_technique("forward_authored")` before init_programs. ⛔ PENDING VERIFY (advisor): CSM worked in 17d/18e WITHOUT set_forward_technique, so a per-pass record-time resolution must exist (grep ~:3445 gb.tech) — if it honours pass.technique, forward_plus's field SHOULD have worked and the real bug is that resolution failing for forward_authored; if nothing per-pass exists, the graph `technique` field is decorative = a defect vs the asset-driven mandate. Resolve before shipping green. (F3) with forward_authored cooked (point_count=4 confirmed) the DIRECTIONAL works (side probe lit) but the POINT light gives EXACTLY delta=0 — fill PROVEN correct (diag: lb=148 count=1 r=5 colx=3 posz=4), indexing correct (lighting_type_first(Point)=1), shadow-mask RULED OUT (per-type d.shadow.mode[ti]; point=None). ~~Remaining suspect = in.world_pos garbage~~ ⛔ DISPROVEN — the ROOT CAUSE was F2 (the gate drew fs_shadowed=forward_csm, which has NO point loop; world_pos was fine). Fixed by set_shadow_technique("forward_authored"). See STAGE 1 DONE below. ⛔ 3 TEMP diags live (console sink + scene_lighting_desc log + fill log) — keep until the point lights. ⚠ set_point_lights API + fill SHIPPED (scene_renderer.cpp/.hpp), forward_plus.frame.toml + the gate authored — UNCOMMITTED. ✅✅ **STAGE 1 DONE 2026-08-15 — point-light consumption GREEN all 3 platforms** (Win Vulkan delta=138, Win DX12 139, Linux llvmpipe 138 — face_on≈138 lit / face_off=0 grazed, band [95,190] TIGHT; full scene-render 1763/82, tidy clean, 3 TEMP diags REMOVED). ⛔⛔ **F2 RESOLVED = THE BUG'S ROOT (advisor-flagged entanglement):** the forward FS is NOT taken from the frame-graph pass `technique` field — the renderer cooks TWO variants per-draw and selects by shadows_active: fs_flat from `forward_technique` (no-shadow) and fs_shadowed from `shadow_technique` (scene_renderer.cpp:3101 `csm=find(shadow_technique)`, :3460 `rs.tech=csm`). shadow_technique DEFAULTS to "forward_csm" (`body_forward_csm`: directional+CSM, NO point loop). The gate runs shadows-ON ⇒ drew fs_shadowed=forward_csm ⇒ points dropped. FIX = `set_shadow_technique("forward_authored")` (+ set_forward_technique for the flat arm) before init_programs — forward_authored (cook_lighting from scene_forward.crdl, point=4, [shadow]csm) does CSM shadows AND the point loop. ⛔ IMPLICATION: the graph `technique` field is DECORATIVE for the forward FS (the technique is a renderer setting, not the asset) — a defect vs "the technique lives in the graph" (forward_csm.frame.toml:12); forward_plus.frame.toml's swapped field documents intent but does nothing until wired to honour pass.technique. FLAG FOR ADVISOR at Stage-1 close: is honouring pass.technique per-pass the CEIR-18a fix, or is set_shadow/forward_technique the accepted seam? NEXT = Stage 2 (cluster) after the advisor + commit. ⚙ **STAGE 2 DESIGN-LOCKED 2026-08-15 (advisor) + started.** DECOMPOSITION: **2a** = the consumption seam with a CPU-WRITTEN list (NO cull compute — E5 is another never-device-run path, hit it with a memcpy first; the 2a gate is the FINAL gate, 2b only swaps who writes the list). **2b** = the GPU producer (froxel-AABB builder inverting the FS's OWN cluster_coord mapping lighting_asset.cpp:783-792 NOT ckir_render::cluster_z_slice; cull-input provisioning b1-from-radius; the compute pass in forward_plus; + a list==CPU-oracle parity pin). Config-flexibility (the .ckir bakes 16/6/8 via 6 UNROLLED blocks — a value spec-const can't change an unroll) is DEFERRED: build the 2a/2b scene to MATCH 16/6/8; the unroll→stmt_for re-author is a separate program-authoring slice (18a-2 exit criterion "cull asset generalization" or fold into 18b). ⚙ 2a IN PROGRESS: (a) `kHdrClusterOff=110` added (110-119 free by absence-of-USE; 111 RESERVED) + compiles. (b) ✅ authored `assets/lighting/scene_forward_clustered.crdl` ([cluster] enabled, grid=[4,4,1]=16 clusters/grid[2]=1 z-inert, max_per_cluster=8, [header] cluster_off=110) + registered the `forward_authored_clustered` technique (scene_renderer.cpp — body_scene_authored PARAMETERIZED via Technique.user=&LightingDesc so ONE body serves both; a 2nd static scene_lighting_clustered_desc parsed from the clustered crdl disk-first at init_programs; techniques.define both) — COMPILES. REMAINING 2a: (c) group cluster-list SECTION — add group.cluster_list_off after prev_palette in the relayout (scene_renderer.cpp:4578; size kSceneClusterListWords=16*8=128; ⛔ rebasing: write header[kHdrClusterOff]=group.cluster_list_off, region_base-aware like kHdrBoundsOff — but the 1-group gate is non-consolidated so raw works, note the consolidated case) + write it in the header fill. (d) `set_cluster_light_list(ConstSpan<u32>)` API + Impl storage + per-frame upload_storage to group.cluster_list_off (mirror set_point_lights). (e) the gate ([ceir18a2]): 2 point lights RED@tileA/GREEN@tileB + set_cluster_light_list(tileA→[0,null…], tileB→[1,null…]) + set_shadow_technique("forward_authored_clustered") + discriminating probes (tile A r>0/g≈0, tile B g>0/r≈0, empty dark) — 18e kit, measure→band→llvmpipe. ⛔ EXPECT an E5 first-device-run defect. FACTS PINNED: the list is read via `loadu(hdru(cluster_off)+…)` = the GROUP buffer (so 2b's cull writes into the group buffer, the `instances` external-buffer pattern — NOT a standalone transient); `[header]` DOES parse `cluster_off` (lighting_asset.cpp:219) so the clustered crdl sets it to 110; group sections are placed at computed offsets w/ relayout (a new cluster-list group section needed). REMAINING 2a: author scene_forward_clustered.crdl ([cluster] enabled, grid=[4,4,1] so 16 clusters + grid[2]=1 z-inert, max_per_cluster=8, [header] cluster_off=110) + a `forward_authored_clustered` technique (like scene_authored_technique, a 2nd static desc; the gate selects it via set_shadow_technique) + a cluster-list group section + CPU-write a hand-chosen 2-light list (tile A: light1 include/light2 exclude; tile B inverse) + the discriminating gate (advisor: distinct R/G light COLORS as the discriminator not luma — "red light → (·,0,0) exactly"; empty-cluster dark from both; set_shadow_technique seam; measure-first→band→llvmpipe). advisor at 2b design lock. | §40 §130 |
| ✅ | CEIR-18a-3 | **✅ DONE 2026-08-16 — the frame graph's forward-pass `technique` DRIVES the forward FS cook (REN-37's F2 defect CLOSED: the field was decorative, the FS was cooked from set_forward_technique/set_shadow_technique). Verified cross-platform: Win Vulkan+DX12 (1934 assns / 89 cases, tidy-clean) + Linux GCC/lavapipe (1514 / 65). CLOSE NOTES: (1) init_programs resolves the forward FS from the INSTALLED frame, TWO-TIER (capability tiers): main frame `requires=["shadows"]` ⇒ csm ← main.forward.technique, fwd ← fallback.forward.technique (forward_basic → standard_forward); main frame flat ⇒ fwd ← main.forward.technique. ⛔ NOT `csm=fwd` unconditionally — that cooked the DEFAULT renderer's FLAT variant as forward_csm, which samples an UNBOUND shadow atlas with shadows off ⇒ black/inverted shading; the module suite (REN-2/REN-3.2-b/REN-38) caught it. A field-less forward pass ⇒ the C++ setter (byte-identical, no-regression); a GRAPH-named technique that does not resolve FAILS (never a silent default). (2) ⛔ `material_pass` is an ENUM (read with pass_u32 vs FrameMaterialPass), NOT a string — the first cut used pass_str, read empty, and dropped every pass to the setter fallback; the 18a-3 gate caught it. (3) set_frame_graph_toml AND the RAF-11 frame_commit reload now RE-COOK the FS + ATOMIC ROLLBACK (stash prev frame + override, restore + re-cook the old frame on a re-cook failure — never a half-installed frame with retired programs). (4) forward_csm.frame.toml + forward_plus.frame.toml F2 "aspirational / does nothing" comments STRUCK in place — the `technique` field is load-bearing now. (5) REN-37.2 gate reconciled (installs graphs that NAME the technique, zero mandatory setters; + (e) asset-outranks-setter inverse pin, (d-cold) first-cook bad-name FAILS, (d-hot) atomic-rollback pin); CEIR-18a-2 Stage 1 + Stage 2a point-light gates reconciled to graph-drives-technique. Was 🔄 IN PROGRESS (user: fix now, pause 19b). DESIGN-LOCKED (advisor): OPTION A — source the forward technique NAME from the installed frame's forward pass (`pass_str(p, "technique")` over impl.frame.passes; the raster.geometry material_pass=Forward pass), drive BOTH the flat (fcfg.tech) AND shadowed (scfg.tech) cook from it (⛔ NOT flat-on-setter = re-creating the F2 split); the setters demote to a LOUD fallback (absent field → setter, byte-identical no-regression; present-but-unresolvable → FAIL init_programs like :3438, NEVER silent setter-fallback). Reject OPTION B (a per-pass technique→variant-matrix MAP — the variant cook is ALL-OR-NOTHING gated on shadow_programs_ok; a map multiplies that per name = 19c complexity for a defect fix) → documented multi-technique-per-frame BOUNDARY. TIMING (the crux): init_programs @3161 already reads impl.frame; but gates call set_frame_graph_toml AFTER init_programs (:3104 installs the frame + rebuild_frame_plans but does NOT re-cook the FS = the F2 mechanism) ⇒ set_frame_graph_toml must RE-COOK (re-run init_programs). ⛔ forward_csm.frame.toml's forward pass HAS `technique="forward_csm"` (:90) + a COMMENT (:15-18) documenting this exact F2 defect + naming 18a-3 ⇒ my fix: shadowed arm stays forward_csm (no change), flat arm standard_forward→forward_csm (contained — forward_csm draws SHADOWED in the shipped/gated path). GATE: two graphs differing ONLY in forward `technique` → different pixels, ZERO setter calls; + the INVERSE pin (same graph, setter changed → pixels UNCHANGED, proving the asset outranks the setter); blast radius = scene-render FULL suite (the setter path is load-bearing; set_shadow_technique test callers keep working via fallback), 2 Win + 2 Linux + tidy. ⛔ edits ride scene_renderer.cpp (shared w/ 18a-2/18b/18z/19b) — surgically separate, own commit ⑥. ⚙ CORE IMPLEMENTED 2026-08-16 (crd-scene-render COMPILES): init_programs resolves the forward technique NAME from the installed frame's `material_pass="Forward"` pass (`pass_str(gp, kTechnique)`), drives BOTH fwd + csm cook from it when present (loud-fail if unresolvable), else the setters (field-less fallback); set_frame_graph_toml RE-COOKS via init_programs(*impl.ctx) (RAF-11 re-runnable) so a frame swap re-shades. ⭐ standard_forward = the default Cook-Torrance forward (no shadows); forward_csm = same family + CSM ⇒ the forward_csm flat-arm change (standard_forward→forward_csm-no-shadow) is a NEAR-NOOP (advisor outcome i). CASUALTY LIST (advisor's pre-run classification): AGREE-with-graph (fine, setter==graph technique) = the 4 forward_authored/clustered/_3d gates (6235-6719) + raf10 app_tint. FIGHT-the-graph (WILL FLIP — they PROVE setter-drives-technique, the exact thing 18a-3 inverts) = the REN-37.2 technique-selection test (sample helper @1283 sets the technique via setter + uses the DEFAULT forward_csm graph with NO set_frame_graph_toml ⇒ the graph's forward_csm now overrides the setter → standard_forward/unlit/forward_csm/forward_authored all draw forward_csm → the (a)lit/(b)unlit-differs/(c)shadowed/(c2)authored/(d)bad-name asserts break) — RECONCILE: the sample helper installs a GRAPH whose forward pass names `technique` (swap the default text's `technique="forward_csm"`), ZERO setter calls ⇒ becomes the asset-drives-technique proof; (d) → a graph with technique="no_such_technique" fails set_frame_graph_toml (the re-cook). REMAINING: (1) reconcile the 37.2 test → graph-driven; (2) the 18a-3 GATE = two graphs differing ONLY in forward technique → different pixels + the INVERSE pin (⛔ graph WITH the field, setter changed → pixels UNCHANGED = asset outranks setter; a field-less graph legitimately obeys the setter — state it in the comment); (3) full scene-render suite (Win+Linux) + tidy; commit ⑥. ORIGINAL: The frame-graph pass `technique` field must DRIVE the forward FS cook (the F2 defect, found at CEIR-18a-2 Stage 1).** ⛔ `forward_csm.frame.toml:12` claims "the LIGHTING itself → `technique` on the forward pass… every material re-shades," but the renderer IGNORES the pass field: the forward FS is cooked from `set_forward_technique`/`set_shadow_technique` (scene_renderer.cpp:3101 `find(shadow_technique)` / :3460 `rs.tech=csm`), a per-draw `fs_flat`/`fs_shadowed` split keyed on shadows_active — NOT the graph. Changing the asset's `technique` re-shades NOTHING (proven: forward_plus.frame.toml swapped it to `forward_authored` and drew forward_csm anyway; only `set_shadow_technique` fixed it). "Proven-to-parse, never-wired" = the exact class this band kills; the mandate ("only authored frame graphs") requires the asset, not a C++ setter, to select the technique. SCOPE (advisor): key the forward cook set by technique NAME sourced from `pass.technique`, thread it through the ALL-OR-NOTHING twin cooks (indexed/RO/skinned/GBuffer, :3397-3453) + the RAF-11 hot-reload re-cook. GATE: two frame graphs differing ONLY in the forward pass `technique` render DIFFERENTLY with NO renderer setter calls (the asset-drives-the-FS proof). advisor at design lock. | §40 §130 |
| ✅ | CEIR-18b | **Clustered renderer as an authored asset. DONE + gated 4 configs + a 3rd device (llvmpipe), 2026-08-16.** 3D froxel (Doom-2016 exponential z-slice) clustered shading. reuse: B8-l `cluster_z_slice`/`cluster_coord` + light-sphere-vs-cluster cull + `ckir_scan` compaction. PROOF: per-cluster froxel light-list == a CPU reference cluster-cull (ABSOLUTE); device render. advisor at entry. **DESIGN-LOCKED (advisor 2026-08-16):** (1) grid **4×4×4=64=wg** — load-bearing: `cluster=LocalInvocationIndex`, 1 wg, `dispatch_groups=1`, kernel re-param is CONSTANTS-only; grid>64 (cluster-id restructure) = named follow-up. (2) SIBLING config — tiled untouched = regression floor: new crdl `grid[4,4,4]`, 3rd technique via `scene_authored_technique_impl`, 2nd emission of the ONE in-tree builder → `scene_light_cull_3d.ckir` (`LightCullParams{num_clusters=64}`; `[.emitckir]` emits BOTH), new id `engine://scene/light_cull_3d`, frame sibling; `rebuild_frame_plans` distinguishes the 2 ids (drives dispatch + FILL SHAPE: 16 full-depth vs 64 per-slice boxes); grow section consts list 128→512 AABB 96→384 (tiled=prefix); fix 2a gate `REQUIRE(list.size()==grid*grid*cap)` → assert `<=` capacity. (3) view-depth = `clip.w` computed INSIDE the cluster block in `cook_lighting` keyed `grid[2]>1` (contact-shadow ~995 precedent; no technique can forget); `grid[2]==1` must cook BYTE-IDENTICAL. (4) z-partition = publish the boundary **TABLE not a formula** (kills the 2-derivation drift class — frag_xy/Y-flip/viewport were all this bug): CPU computes N+1=5 exponential boundaries in clip.w terms → header words **115–119** (VERIFIED free, 120=LodCount hard-caps N=4); FS `slice=Σ step(b_i,clip.w)` (4 unrolled cmps, bit-consistent w/ AABB table BY CONSTRUCTION); crdl parse/serialize/hash/cook-reject CONDITIONAL on grid[2]>1 (18a crdl keeps cooking) + ABI-validate. (5) GATE TRUTH — pixels CANNOT fully discriminate z-binning (a far light mis-binned near contributes nothing there anyway): PRIMARY proof = **LIST PARITY** (extend 2b-ii gpu-shared kit to a 3D scene, lights at distinct depths, device==eval_cpu_kernel==analytic-from-same-table); IMAGE gate proves CONSUMPTION + z-term-dead → needs **geometry at TWO depths** (near quad partially covering far, probe 2 px in SAME tile), NO per-backend flip (depth matches across backends), carry sign-agnostic Y. (6) DON'Ts: no `ckir_scan` (serial compaction=bit-exact eval; row's "reuse scan"=perf follow-up), no runtime grid config (fill shape from `desc.cluster.grid` OK; capacities=compile-time maxima). BUILD ORDER: crdl+header+FS-z (light-cook green, 18a byte-stable) → builder re-param + 3D kit + emit BOTH (kir + gpu-context parity) → scene fill branch + registration + frame sibling → two-depth image gate both backends + llvmpipe. **PROGRESS (2026-08-16):** ✅ STEP 1 (light-cook/FS/crdl) — `kHdrSliceBoundsOff=115`/`kNumSliceBounds=5` (+static_assert below LOD), `LightingHeaderMap.slice_bounds_off`, parse/serialize/hash(3D-only)/cook-reject(grid[2]>1), FS z-block = boundary Step-sum on clip.w (2D stays byte-identical via cook-time grid[2]==1 branch); light-cook gate green (`[ceir18b]` 9 assn + all 12 [light-cook] cases; 2D byte-stability proven by word-mutation). ✅ STEP 2-KIR — builder is fully param (num_clusters 16→64, threads stay 64=wg; ZERO code change); `[.emitckir]` emits BOTH `scene_light_cull.ckir` + `scene_light_cull_3d.ckir` (same 250n/30s shape, differ only in the guard const); committed 3D asset gated `[ceir18b]` (load+roundtrip+DISTINCT-from-2D, guards the inert-copy trap); 2D+18q regression 369/15 green. ✅ STEP 2-GPU (the LIST-PARITY primary proof) — templatized the gpu-shared cull kit on `<Scene>` (2D call sites unchanged via deduction) + added `ClusterCullScene3D` (64 clusters, exponential per-slice z-bounds {0.5,1,2,4,8}, lights DISCRIMINATING by depth: L0/L1 share screen tile but land in different slices, L2 on a z-boundary → two clusters, L3 culled everywhere); added a 3D device==oracle==analytic gate on BOTH backends (loads scene_light_cull_3d.ckir) with an explicit z-discrimination assert (c0 has L0 not L1, c32 has L1 not L0). GATE: Vulkan [clustered3d] 589 assn + [lightcull] 874/3 (2D no-regression); DX12 [clustered3d] 588 + [lightcull] 740/2. Housekeeping: MEMORY.md 21→19.9KB (11 done-slice project pointers → on-demand nav, zero active-recall loss). ✅ STEP 3 (SCENE FILL branch) — `kMaxSceneClusters=64`; grew kSceneClusterListWords 128→512 + kSceneFroxelAabbWords 96→384 (2D writes a prefix); `compute_froxel_slices` (unproject near/far corners, GLOBAL w_near/w_far=1/h.w, exponential boundaries, LINEAR interp in clip.w per slice → 64 per-slice AABBs + N+1 boundary table); full renderer wiring (`device_light_cull_3d` flag + scan distinguishing light_cull vs light_cull_3d by EXACT equality, `ensure_light_cull_3d_kernel`+register, dispatch-groups/precedence/upload-guards extended, render() fill branch, boundary publish to header words 115-119); assets scene_forward_clustered_3d.crdl (grid[4,4,4], slice_bounds_off=115) + forward_clustered_3d_gpu.frame.toml + forward_authored_clustered_3d technique + init_programs ABI-validate (incl slice_bounds_off); fixed Stage 2a gate 128→<=capacity. GATE (win-debug): the DRIFT-catching CPU test [ceir18b] 39 assn (world clip.w via matrix row 3 → slice → AABB-contains — NON-CIRCULAR, catches fill-vs-FS drift + confirms global w_near/w_far); 2D device regression [ceir18a2] 4 gates (2 VK+2 DX12) 70 assn green (section growth safe); full scene-render suite 1872/87 green (relayout broke nothing across the module). ✅ STEP 4 (the two-depth DEVICE IMAGE gate — advisor-designed) — FAR full-screen quad (d=7→slice3) + NEAR quad (d=2→slice2) covering the LEFT of tile col 1; RED light confined to slice 2 (lights near quad), GREEN to slice 3 (lights far quad) by sphere d-span; probe A(80,128)/near=RED, B(112,128)/far=GREEN, SAME tile col 1; X-separation + Y-straddle at y=128 ⇒ backend-SYMMETRIC (no Y-orientation claim). A dead z-term makes B read slice-0's empty list ⇒ B dark. PASSED FIRST DEVICE RUN both backends (no first-emit surprise): VK 16 assn + DX12 15 assn; [ceir18b] scene-render 3 cases 70 assn (CPU drift + 2 image gates); [ceir18a2] 2D regression 4 cases 70 assn. ✅ ABSOLUTE 4-CONFIG SWEEP GREEN: win-debug ([ceir18b] 70 + [ceir18a2] 70 + full scene-render 1872/87); win-asan ([ceir18b]+[ceir18a2] 140/7 + LC 9 + KR 11, ASan-clean); linux-gcc-debug/llvmpipe (SR 91/4 + LC 9 + KR 11 — the 3D image gate RAN on llvmpipe); linux-gcc-asan (SR 91/4 + LC 9 + KR 11, ASan-clean, no leak); gpu-context [clustered3d] VK 589 + DX12 588. ⛔ advisor's sweep-gap catch honored ([ceir18b] run EXPLICITLY on both Linux legs). TIDY: all changed .cpp + engine files clean; the shared header ckir_light_cull_test.hpp is gate-INVISIBLE (HeaderFilterRegex '.*/crd/.*' excludes tests/gpu-shared/) so its case-style is a tidy-files.ps1 artifact — the .cpp isolate-declaration + widening-cast were REAL (tidy-onion) and fixed. ⛔ tidy fixes were declaration splits (scene-render) + a usize-base index rewrite (gpu-context only); win-debug RE-VERIFIED post-fix ([lightcull] VK 874/DX12 740, [clustered3d] VK 589/DX12 588, [ceir18b] 70). ADVISOR-CAUGHT BLOCKER (close review): the templatized ckir_light_cull_test.hpp had only been compiled by MSVC → built crd-gpu-context-vulkan-tests on GCC (compiles clean, no two-phase-lookup issue) + ran [lightcull],[clustered3d] on LLVMPIPE = 874/3 (3D list-parity on a THIRD device). ⛔ NO gpu-context ASan coverage claimed ([clustered3d] never ran under win-asan — same as the 18a 2D twin). DELETION follow-up: build_cluster_light_cull + ckir_light_cull.hpp stay in-tree till the user commits (uncommitted-delete-loses-source). | §40 §131 |
| ✅ | CEIR-18c | **✅ CONVERTED 2026-08-15 (program-provenance re-open RESOLVED — the user's EXEMPLAR): `engine://scene/deferred_lighting` is now an authored `.ckir` asset; the hand-built C++ FS is DELETED.** `assets/scene/deferred_lighting.ckir` (node-graph form, `[[entry]] stage="Fragment"` + `[[out]]`, ASCII header) is loaded disk-first by `ensure_deferred_lighting_program` (scene_renderer.cpp) — `asset_text("scene/deferred_lighting.ckir")` → `ckir_read` → `ctx->create_program(fg,fe)` → `create_raster_program`, LOUD `CRD_LOG_ERROR`+return-null on a missing/malformed asset; the hand-built `render::deferred_shade` FS body is GONE (the emitter that bootstrapped the asset is also removed — git history is the regen path). This is the FIRST FRAGMENT conversion (the recipe proven for compute at 18a-1 now proven for fragment). GATE (all green, unchanged device correctness): **scene-render `CEIR-18c*` 44/2 BOTH backends** (win-debug, `CRD_ASSETS_DIR` set — the ABSOLUTE lit verdict carries provenance-independently) + **kir `[ckir18q]` 208/5** (loads the committed `.ckir`, byte-exact round-trip) on **win-debug AND linux-gcc-debug** (the deferred asset read works on gcc); scene_renderer.cpp + test_ckir_asset.cpp tidy-clean (per-slice, LLVM 20). NO C++ CKIR builder remains for deferred. ⛔ the `.frame.toml` + all ORIGINAL device gates below STAY GREEN — only the program's provenance changed (hand-built → authored asset). ORIGINAL RE-OPEN: **◧ RE-OPENED 2026-08-15 (program provenance only — see §BAND-18 CORRECTION above): `deferred.frame.toml` + all device gates STAY GREEN; the shader program `engine://scene/deferred_lighting` behind it is hand-built C++ (`ensure_deferred_lighting_program`) and must become an AUTHORED PROGRAM ASSET, then deleted.** ORIGINAL: **DONE + FULL-GATED 2026-08-15 — Deferred renderer as an authored asset (do-first; surfaced + FIXED 6 DEFECTS — 4 in the never-run indexed-MRT-scene path + 2 pre-existing holes targeted gates missed).** engine://frame/deferred renders a scene through a real G-buffer (pack_gbuffer MRT → deferred_shade fullscreen) on BOTH backends. GATE (all green): win-debug (scene-render 1631/76 + gpu-context-vulkan 5278/260 + gpu-context-dx12 1652/142 + render-graph 309/15 + frame-cook 2751/96 + render-pass 46/5) + win-asan + win-tidy (per-slice-check, 6382/6382 after the em-dash fix) + linux-gcc-debug (llvmpipe: scene-render 1325/58 + rg 309/15 + fc 2751/96 + rp 46/5) + linux-gcc-asan (ASan-CLEAN: 288/12 + 309/15 + 2751/96 + 46/5). Deferred Vk+DX12 twins 5/5 stable, val_err=0, ABSOLUTE lit verdict (135,105,89) matching NVIDIA vs llvmpipe to ±1. THE 6 FIXES (all forward): (1) GBuffer program twin wired live (scene_renderer.cpp program_gbuffer_idx/_textured_idx cook+fill+select; frame_runtime.cpp material_pass selection; DrawItem/SceneDraw field); (2) emit_scene_list_mrt indexed-pull (render_materialize.cpp); (3) draw_storage_indexed_mrt NEW RHI verb (vulkan+dx12 raster contexts + command_lowering Indexed r.color.size()>=2 branch, pushes first_draw_index + explicit depth + nullable map/atlas); (4) a PRE-EXISTING §128 DX12 hole (REN-38-A5 present-pass MissingCeirPlan — CEIR-17's gate ran gpu-context-VULKAN only; fixed test_dx12_frame_graph.cpp FramePlans); (5) 4 PRE-EXISTING CEIR-16d em-dash TEST_CASE names (test_frame_graph.cpp — the no-non-ascii guard + ctest-argv-mojibake; — → --). ⛔ 18e-BOUNDARY: command_lowering's Indirect case (GeometryKind::Indirect) is ALSO single-RTV → GPU-driven deferred hits the SAME wall (needs an indexed-MRT-indirect verb) — inherited knowledge for 18e. ⛔ LESSON: binary-direct + targeted gates missed BOTH pre-existing holes; only the FULL per-slice-check (ctest, both gpu-context backends) caught them. ⛔ DOCUMENTED GATE BOUNDARY (no-silent-caps): gpu-context-vulkan was NOT re-run on the 2 Linux configs this slice (it is compile-clean on gcc — the linux-gcc-debug/asan BUILDS include it; the NEW verb draw_storage_indexed_mrt is exercised on llvmpipe via the scene-render deferred gate; the existing gpu-context verbs are UNCHANGED by 18c + historically llvmpipe-validated). Re-running 260 device tests × 2 configs on software Vulkan for unchanged code is disproportionate; disclosure is the mitigation. ORIGINAL: **VULKAN GATE GREEN 2026-08-15 (do-first).** ✅ The Vulkan deferred gate is STABLE (5/5 exit 0) + ABSOLUTE-LOCKED: `engine://frame/deferred` renders a deterministic orange quad through the full G-buffer round-trip to a LIT (135,105,89 ±band, r>g>b Cook-Torrance hue) centre + black background, `val_err=0` (ValidationCapture permanent), `drawn_instances=1`. ⭐⭐ The "do-first" strategy surfaced + fixed 3 REAL engine gaps in the never-run indexed-MRT-scene path (all forward-fixes): (1) the GBuffer material variant was proven-to-COMPILE (B8-k) but NEVER wired live — added the `program_gbuffer` twin (scene_renderer cook+fill+select, frame_runtime material_pass selection, DrawItem/SceneDraw field); (2) `emit_scene_list_mrt` drew indexed items as non-indexed (`vertex_count`=0 → 0 fragments) — select the per-item verb like the single-colour ladder; (3) the RHI had NO indexed-MRT draw verb — added `draw_storage_indexed_mrt` (Vulkan+DX12) that pushes `first_draw_index` (the stale-DrawIndex-row trap) + explicit depth + nullable map/atlas, and the command_lowering Indexed `r.color.size()>=2` branch. ✅ DX12 GATE TWIN GREEN + STABLE (2026-08-15): added the CEIR-18c DX12 twin (test_scene_render_gpu.cpp, #if _WIN32) — my draw_storage_indexed_mrt DX12 verb now RUNS (compile-only-claims scar satisfied): 21 asserts pass, same lit band (deferred_shade over the DX12 N-RTV PSO + DrawIndex root-const + explicit DSV). Both twins together 5/5 exit 0 (the earlier lone 255 was a device-init/teardown transient — GPU-flake class). ⭐ GATE PROGRESS (win-debug blast radius): scene-render 1631/76 ✅, gpu-context-vulkan 5278/260 ✅, gpu-context-dx12 1652/142 ✅. ⛔⛔ The gpu-context-DX12 run CAUGHT + FIXED a PRE-EXISTING §128 DX12 HOLE (REN-38-A5 GATE (DX12) present-pass: rec.record returned false = MissingCeirPlan): the CEIR-17z §128 fix built stack FramePlans for the VULKAN REN-38-A5 twin but CEIR-17's gate ran gpu-context-VULKAN ONLY (260/0) — so the DX12 twin's identical hole went uncaught for the whole band. FIX: mirror the Vulkan fix (build_frame_plans once + pass &plans to both present-records + the DiagnosticList include) in test_dx12_frame_graph.cpp. ⛔ LESSON (reinforces gates-run-configs + new-execution-path-full-suite): a §128/CEIR-migration gate MUST run BOTH gpu-context backends, not just Vulkan. FULL WIN-DEBUG BLAST RADIUS GREEN: render-graph 309/15 + frame-cook 2751/96 + render-pass 46/5 ✅ (with the above). ⛔⛔ FIX #5 (the canonical per-slice-check win-debug+win-asan+win-tidy caught 5 failures — ALL one PRE-EXISTING root cause from CEIR-16d): 4 render-graph TEST_CASE names in test_frame_graph.cpp (16d-live-1b/2b/3a/4a-3) have EM-DASHES (—) → the `crd-no-non-ascii-test-names` guard fails AND ctest mojibakes argv via the Active Code Page so Catch2 can't match them (they show "Failed"). MY earlier binary-direct render-graph run passed all 309 precisely because it BYPASSES ctest name-filtering (the binary-direct-misses-ctest scar). No ASan/tidy issues in the CEIR-18c changes — the other 6377/6382 pass on all 3 configs. FIX: — → -- in the 4 names (ascii-only-test-names scar). ⛔ LESSON: binary-direct gates + targeted ctest both miss the non-ASCII guard; only the FULL per-slice-check (ctest) catches it — reinforces run-the-full-per-slice-check at close. ✅ EM-DASH FIX CONFIRMED (win-debug ctest 10/10: the 4 renamed tests run + `crd-no-non-ascii-test-names` passes). WINDOWS GATE GREEN (win-debug blast radius + both deferred backends + the em-dash fix; win-asan + win-tidy clean modulo the now-fixed name mojibake — the 5 were ctest-name-matching, NOT real/ASan/tidy failures). ⭐ This session's thorough per-slice-check caught TWO pre-existing holes targeted gates missed (DX12 §128 + CEIR-16d em-dashes). ⭐ LINUX PROGRESS (WSL /mnt/d live source, gcc 13.3): linux-gcc-debug BUILDS CLEAN (my new RHI verb + twin wiring + command_lowering branch compile under gcc — no -Werror=switch gap); deferred [ceir18] gate on LLVMPIPE ✅ val_err=0, lit (134,104,89) — within ±1 of NVIDIA's (135,105,89), the absolute band holds (no widening); render-pass 46/5 ✅. (DX12 twin is #if _WIN32-excluded on Linux — Vulkan twin only, 23 asserts.) Full frame-cook/render-graph/scene-render on linux-gcc-debug IN FLIGHT; then linux-gcc-asan. REMAINING: linux-gcc-asan + flip ✅ + advisor + commit proposal + open 18d. REMAINING: win-asan (ASan-clean — the new RHI verb + twin allocations; ctest --preset win-asan handles the ASan DLL PATH) + 2 Linux (WSL ~/cerid-build/linux-gcc-{debug,asan}; llvmpipe — the deferred VULKAN twin only, no DX12 on Linux; widen the (135,105,89 ±25) band if llvmpipe's Cook-Torrance differs; llvmpipe-exposes-kernel-bugs scar) + tidy (LLVM 20, changed files) + flip ✅ + advisor at 18c-done + commit proposal. ORIGINAL ENTRY: **IN PROGRESS 2026-08-15 (DO FIRST; advisor-scoped at entry — locks the 18z harness on the confirmed-green B8 base).** APPROACH LOCKED: author `engine://frame/deferred` (a DECLARED target — `gpu-platform-capabilities.toml:494`, gold-std research names `default-deferred`) = a G-buffer `raster.mrt` pass (`material_pass="GBuffer"`, `writes=[gbuffer_albedo, gbuffer_normal, gbuffer_depth]` — depth-format routes to the depth slot) → a deferred-lighting `raster.fullscreen` pass (`reads` the G-buffer targets, `writes=scene_hdr`, `shader="engine://scene/deferred_lighting"`) → a `post` fullscreen tonemap → `@output`. ⛔ SCHEMA FACTS VERIFIED (4 greps, none blocked the approach): (1) MRT multi-`writes` at .frame.toml level = `raster.mrt` (proven by forward_csm_gpu's velocity+scene_depth prepass); (2) `material_pass="GBuffer"` is a VALID cook value (frame_asset.hpp:98 enum + parse:274 + validate frame_ceir.cpp:125 + emit:72); (3) NO `engine://` deferred-lighting shader is registered (only impostor/hzb_build/taa_resolve at scene_renderer.cpp:2986-2992) ⇒ I AUTHOR + register `engine://scene/deferred_lighting` via `register_raster`, PROMOTED from the green B8-l deferred-lighting CKIR (test_vulkan_context.cpp:4749 + gpu-shared ckir_raster_triangle.hpp:2741) — still assembly (B8 lighting math ships), within "no new pass algorithm"; (4) G-buffer targets are `transient_image` (default kind). MRT→fullscreen SYNTAX REFERENCE = forward_csm_gpu.frame.toml's depth-prepass(MRT)→hzb(fullscreen) + forward→taa(5-read fullscreen). ⛔ SCARS WIRED: NDC±Y mirror (lighting samples G-buffer RTTs by UV), 1-read≠multi-read fullscreen binding (follow TAA's 5-read path not the 1-read one), DX12 PSO fmt=RT (G-buffer formats match PSO on the DX12 twin), RMW-not-RWM (depth READ-ONLY in the lighting pass), transient aliaser slot-size + WAR-lifetime. SMALLEST-FIRST: 2-target G-buffer (albedo+normal)+depth + lighting on a DETERMINISTIC scene, device-green BOTH backends, THEN widen. PROOF/GATE (ABSOLUTE): deterministic scene + LOCKED pixel verdicts (the B8-l column-probe pattern) through the COOKED shipped `deferred.frame.toml` (gates-run-configs scar — no bespoke inline TOML; CRD_ASSETS_DIR), real device, Vulkan + DX12 twin (16z REN-38-A11 precedent); deferred-vs-forward A/B only a SECONDARY check (deterministic-clock scar, G-buffer quantization tolerance) — never CEIR-vs-CEIR. ⚙ IMPL 2026-08-15: authored `assets/frame/deferred.frame.toml` (2-pass smallest-first: G-buffer `raster.mrt`/material_pass=GBuffer → deferred-lighting `raster.fullscreen` writing @output directly since `deferred_shade` returns clamp01 — LDR, no tonemap hue-shift; HDR+tonemap+scene-lights = widen) + `ensure_deferred_lighting_program()` in scene_renderer.cpp (3-read bindless heap, `render::deferred_shade`, full-intensity fixed key) + `register_raster("engine://scene/deferred_lighting")` — **crd-scene-render COMPILES (exit 0)**; authored the device gate `CEIR-18c GATE` in test_scene_render_gpu.cpp (shipped asset via read_shipped_asset — gates-run-configs; deterministic orange quad; probes covered-centre albedo HUE r>g>b + near-black corner). ⛔ ROOT CAUSE (first device run, advisor-diagnosed): the asset now COOKS + VALIDATES (parse ok + validate_ceir_frame kind=None, after adding `scale=1.0`+`sampled=true` to the transients) and `r.draws>0`, but it renders **BLACK** — because a `raster.mrt` pass selects `it.program_velocity` (frame_runtime.cpp:192-193) and the pass's `material_pass=GBuffer` is NEVER consulted; there is NO `program_gbuffer` twin. So the G-buffer pass draws the VELOCITY FS (2 outputs), not `pack_gbuffer` (4). `PassType::GBuffer` is proven-to-COMPILE (B8-k) but was NEVER wired live — exactly the gates-run-configs class the slice closes. FIX (advisor-confirmed, wire a 4th program twin — 4 corrections): (1) the GBuffer twin is **FORWARD-shaped** (per-material surface pack via `cook_scene_fs` `cfg.pass=PassType::GBuffer`, the impl.program mechanism), NOT velocity-shaped/global (a global would silently wrong-render textured materials); (2) selection: the `material_pass==GBuffer` branch must come **BEFORE** the `pass_flag(kMrt)` branch (a GBuffer pass IS MRT), keyed on `pass_u32(d,kMaterialPass)`; (3) **NO silent `?:program` fallback** — a null gbuffer twin FAILS LOUD / skips-with-count (forward-FS-as-albedo = the §128 silent-wrong class); (4) consumer audit: the 17c parity oracle mirrors this selection + `scene.resolve_*` is a CLOSED VOCAB (decide gbuffer-arm vocab-entry-now vs documented-boundary, re-run ceir-gpu `[scene]` either way), RasterDrawItem new field = header-stale `.obj` scar (clean-build affected configs), TWO copy sites (scene_renderer 5788-89 + the 17b `draws_item` resolver fill). Strip the temp `[18c-diag]` block before the gate. ⛔ 18c files (asset/scene_renderer/frame_runtime/test) = a SEPARATE commit from the CEIR-17 batch. ⚙ DEBUG (2026-08-15, all 9 twin edits landed + compile-green): the deferred asset COOKS+VALIDATES (validate_ceir_frame kind=None after `scale`+`sampled` fix) but renders the CLEAR at covered pixels. Isolation (advisor-guided): lighting fullscreen RUNS + writes @output (constant orange filled the target) ✓; bindless multi-read sampling of gbuffer0 WORKS (red-clear test read the clear) ✓; `program_gbuffer_idx` COOKED ✓ (diag: not-blue); but the MRT geometry draw NEVER lands a fragment — the covered centre = the EXACT clear across every twin variant (forcing program_gbuffer for any MRT: still clear), so the draw is SKIPPED or DEVICE-KILLED, upstream of the twin/material_pass. ⛔ Advisor: a **non-GPU-cull `raster.mrt` scene pass with 4 color + depth is UNPRECEDENTED** — the ONLY shipped MRT scene pass is the velocity prepass (1 color + depth, `writes[1]`=depth, inside the GPU-cull graph); `build_scene_ceir`'s mrt op-mode for 4-color+depth may not exist/be untested, and the depth-at-`writes[4]` routing may be positional. Bundled diagnostic (r.drawn_instances + ValidationCapture) IN FLIGHT: drawn_instances==0 ⇒ never issued (plan/payload — inspect `build_scene_ceir`); ==1 ⇒ device-killed (validation names it: 4-output PSO vs attachments, or RGBA8-as-depth). ⛔ REVERT CHECKLIST before the gate (none gate-legal): frame_runtime selection (DONE — material_pass branch + velocity arm restored), shader diag→`deferred_shade`, asset clear→[0,0,0,0], test `[18c-diag]`+`ValidationCapture` blocks. Then lock ABSOLUTE pixels + DX12 twin, gate 2 Win + 2 Linux + tidy. ⭐ FIX #1 LANDED (render_materialize.cpp `emit_scene_list_mrt`): the MRT scene arm drew each item as a NON-indexed StoragePull (`vertex_count`), IGNORING `it.index_count`/`indexed`/`first_index` — written for the non-indexed WBOIT/14z cases; my INDEXED-pull G-buffer quad (index_count=36, vertex_count IGNORED=0) drew 0 vertices → 0 fragments. FIX: select the per-item verb like the single-colour ladder (indirect/DrawIndexed/plain + textures), non-indexed branch byte-identical. NOW the draw EXECUTES (r.drawn_instances=1) — 2nd LAYER: the MRT SCOPE's colour-attachment count/order is wrong vs the FS's 4 pack outputs (Vulkan val_warn=3: "FS writes output Location 1/2/3 but no VkRenderingInfo::pColorAttachments[N]"), and the readback is inconsistent (g0.r=clear-127 but g1.r/g2.r=0-written) ⇒ a colour-count/order/aliasing layer in the deferred-G-buffer MRT plan/scope (mrt_n vs the pack's 4; the transient-aliaser + WAR scars are live here). AT the mrt-op-mode fork (advisor pre-designated). ValidationCapture installed in the gate (keep — the oracle). ⛔⛔ FINAL ROOT CAUSE (from_ceir_frame numbers: writes=5, mrt_n=4, matpass=3=GBuffer, mrt=1 — the DESC + PLAN are CORRECT; drop is DOWNSTREAM): **the RHI has NO INDEXED MRT DRAW VERB.** `command_lowering` `GeometryKind::Indexed` (390-413) has only single-colour verbs (`draw_storage_indexed_depth` / `_sampled_depth`, both bind `*color0` = 1 attachment); the MRT verb `draw_storage_mrt` (355-371) lives ONLY in the `StoragePull` (non-indexed plain-vertex) case (the B5 G-buffer was only ever tested plain-vertex, comment 166). So my INDEXED-pull (REN-39) G-buffer draw falls to a single-colour indexed verb → 1 attachment → FS writes 4 → 3 val warns + garbage. The scene geometry MUST be indexed (vertex-pull by index value; a non-indexed draw would gl_VertexIndex-OOB the 8-vertex quad). ⇒ ARCHITECTURAL: add `draw_storage_indexed_mrt` (indexed + N colour attachments) across IRasterContext + Vulkan + DX12, and an MRT branch in command_lowering's Indexed (+ MultiIndexed) case + emit_scene_list_mrt uses it. ADVISOR CONFIRMED (fix #3, NOT a contract change — below CEIR, verb-growth is the pattern): add a SIBLING `draw_storage_indexed_mrt` (do NOT widen draw_storage_mrt — WBOIT/14z callers). ⛔⛔ MUST push `first_draw_index` (the DrawIndex-row trap: draw_storage_indexed_depth 408-410 lacks it → the REN-40-C2 rebased VS reads a STALE row → garbage pull → why attachment 0 never got a fragment — ONE cause, both symptoms). ONE verb, nullable map/atlas (draw_storage_multi_indexed_indirect precedent — textured G-buffer rides it). EXPLICIT depth_target (rd.depth.target=scene_depth; draw_storage_mrt uses color0's bundled companion which a bare transient lacks). `r.color.size()>=2` branch FIRST in the Indexed case (selection-order trap). Leave MultiIndexed alone. BOTH backends now (DX12: all N RTV formats PSO-fmt=RT + root-const DrawIndex). KEEP ValidationCapture permanently (CHECK error_count==0). ⭐⭐ FIX #3 LANDED + confirmed (draw_storage_indexed_mrt in vulkan_raster_context.cpp + dx12_raster_context.cpp + the command_lowering Indexed r.color.size()>=2 branch; command_lowering is a TEMPLATE so NO IRasterContext virtual needed — the verb is a backend method both instantiations call): the G-BUFFER NOW PACKS — **val_warn=0** (all 4 attachments bound, was 3 warns) + the raw-attachment probe (g0.r,g1.r,g2.r)=(204,127,0) = base.r 0.8 + normal_enc.x 0.5 + emissive 0, EXACTLY pack_gbuffer's albedo/normal/emissive. So all 3 fixes together render the deferred G-buffer over REN-39 indexed scene geometry. REVERTS DONE (shader→deferred_shade, clear→[0,0,0,0], stripped [18c-diag], ValidationCapture PERMANENT with CHECK(error_count==0)+CHECK(drawn_instances==1)). Final lit render + ABSOLUTE pixel lock IN FLIGHT; then DX12 gate twin + gate 2 Win + 2 Linux + tidy. 18e-boundary: the command_lowering Indirect case (415-440) is ALSO single-RTV → GPU-driven deferred's wall (record in 18e row). | §40 §132 |
| ✅ | CEIR-18d | **DONE-BY-VERIFICATION 2026-08-15 — Visibility-buffer renderer as an authored asset (the PROOF pre-existed; 18d = the ABSOLUTE device gate re-verified on the required configs + a falsifiability gap CLOSED).** The visibility renderer already SHIPS as `assets/frame/scene_visbuffer.frame.toml` (16z dissolved the visbuffer into `scene.raster` = `geometry=procedural` role + R32Uint typed clear, `clear_id=7`) and already renders through the FULL SceneRenderer with a DEVICE id-read-back verdict in `REN-38-F6 GATE` — both the **Vulkan** twin (`test_scene_render_gpu.cpp` ~1604) and the **DX12** twin (~1861): two procedural primitives → two distinct non-zero ids at covered probes (Vk `a=read_pixel(96,24)` / `b=read_pixel(24,96)`; DX12 `a=read_pixel(98,64)` / `b=read_pixel(30,64)`; `CHECK(a!=0); CHECK(b!=0); CHECK(a!=b)`). So 18d is a PROMOTION with NOTHING new to author — the slice's work is (a) RE-VERIFY that device gate green (it rode the CEIR-18c gate; re-run this slice: **win-debug both twins 44 assertions/2 test cases ✅ + linux-gcc-debug/llvmpipe Vulkan twin 23 assertions ✅**, DX12 `#if _WIN32`-excluded on Linux), and (b) CLOSE an advisor-caught FALSIFIABILITY hole: the `a!=0/b!=0/a!=b` verdict has an A==A escape — a primitive that FAILS to draw reads the authored `clear_id=7` background yet could still satisfy `a!=b` (a covered probe reading the CLEAR passes) — so added `CHECK(a!=7); CHECK(b!=7)` to BOTH twins; a covered probe that leaks the background now FAILS LOUD. ⛔ NOT dressed as authoring; NO incidental id values locked — the `clear_id=7` constant is the ONE authored value asserted-against (7 is the background a covered pixel must never be). The 18d test edit RIDES commit ③ (`test_scene_render_gpu.cpp`) — no new commit. advisor confirmed done-by-verification + REQUIRED the falsifiability fix. | §40 §132 §169 |
| ✅ | CEIR-18e | **DONE 2026-08-15 — GPU-driven renderer as an authored asset (test-only PROOF slice; the ABSOLUTE image verdict on the shipped GPU-driven config).** The GPU-driven renderer already SHIPS as `forward_csm_gpu.frame.toml` — device cull (`cull_reset`/`cull_view0..4`/`occlusion_*` compute passes host-resolved as CEIR) → indirect multidraw off the device count via the (Indirect × 1-colour) verb `draw_storage_multi_indexed_indirect` (pushes `first_draw_index`). ⭐ VERB-EXISTENCE CELL CHECK at entry ([[feedback_rhi_verb_vocabulary_per_geometry_x_attachment]]): the contract is FORWARD-shaped (single RTV) → the verb EXISTS, NO new sibling needed (an indexed-MRT-INDIRECT verb would be a GPU-driven DEFERRED asset's need; none exists → stays a recorded boundary). ⛔ WHAT WAS MISSING (the real 18e work): every device proof on the shipped config was DIFFERENTIAL — 17d asserts cull-COUNT parity (args readback: proves the cull WROTE args, not that the multidraw CONSUMED them + rasterized), 17e/G3 are CEIR-vs-CEIR A/Bs (G3 on a bespoke inline TOML). NO image-level ABSOLUTE verdict existed. NEW `CEIR-18e GATE` (Vk + DX12 twins in `test_scene_render_gpu.cpp`): deterministic 3-cube scene (well-separated/unoccluded ⇒ cull exact + far cubes frustum-culled), shipped `forward_csm_gpu`, real device, LOCKED lit pixels (never CEIR-vs-CEIR): 3 distinct-object flat top-face interiors each luma ≈198 ∈[185,215] + distinctly brighter than the ≈169 sky + lit_px extent — measured NVIDIA, verified TIGHT on llvmpipe (the tight `pl>bg+15` margin holds); DrawIndex-row pull load-bearing (a stale row drops one cube to the sky). Cull VERDICT re-verified self-contained (device survivor==CPU frustum cull, groups>0, bounds_mismatch==0). ValidationCapture val_err==0 (Vk). ⛔ A==A HOLE CLOSED (advisor): traced every host writer of `cull_args` — host uploads only the HEADER words [0,kCullArgsHeaderWords); per-view count words (aw+1, argw≥header) are DEVICE-written + read via `download_storage`; `cpu_instances` is the CPU cull's OWN `visible_count_cpu`/`cascade_visible_count` (the CPU-cull path uploads the visible LIST to the SCENE buffer, not cull_args counts) ⇒ device-vs-CPU, never A==A. ⛔ DISCLOSED BOUNDARY (not work): `compute_dispatch_count()` is a VULKAN-only diagnostic (base-class default `return 0U`, unwired on DX12 — [[feedback_registered_default_empty_reads_as_provably_none]]); Vulkan twin uses it as the step-down pin, DX12 twin uses the READBACK (`gc.groups>0` + count parity — which DOES work on DX12) as the engagement+parity discriminator (full strength). ⛔⛔ LOCAL PROOF SCOPE ([[feedback_whole_repo_build_and_test_is_cis_job_not_local]]): test-only change → verified the `crd-scene-render-tests` MODULE only — win-debug (18e twins 5/5 stable + the FULL scene-render binary passed under win-debug ctest 6384/6384) + WSL linux-gcc-debug + linux-gcc-asan (18e Vulkan twin, ASan-clean, llvmpipe; DX12 twin `#if _WIN32`-excluded on Linux). Whole-repo win-asan/win-tidy sweep = CI's job, NOT run locally (a whole-repo win-asan ctest is a ~3h run over 6384 ASan-instrumented tests — CI's scale, not the local loop's; a local whole-repo attempt was abandoned mid-run). 18e gate rides commit ③ (test_scene_render_gpu.cpp, same file as 18d). | §40 §130 |
| ✅ | CEIR-18z | **✅ DONE + gated 4 configs + llvmpipe + sandbox smoke 2026-08-16 (advisor at entry + close) — Proof + deletion close.** **STEP (A) ensure_* AUDIT DONE (gate-reverifies-rows, not inheriting 18p's claim):** all 26 `ensure_*` in scene_renderer.cpp classified against the discriminator "output STRUCTURE hardcoded in C++ vs derived from a disk asset" — 25 LOADERS/cooker-seams (cull family=cook_cull_stage_named .crdv; tess/mesh/visbuffer/post/taa/moment/hzb/velocity/deferred/impostor/cluster_mesh/rt/cull/skin/palette/light_cull/light_cull_3d = cook_stage/resolve_program_text+ckir_read; `ensure_flat_fs`=cooker-seam over disk flat.crdm w/ C++ fallback surface; `ensure_post_program_named`=disk .crdp→cook_post_graph) + **1 BUILDER FOUND: `ensure_visbuffer_fs`** — the LAST hand-built KGraph (id→graded-grey (primId+1)·0.25 the F6 gate reads; 3 authorable choices +1/·0.25/grey ⇒ a builder, NOT a fixed pass-contract; the "fixed contract" comment was the 18p-era decision UNDER AUDIT, not a witness — advisor-confirmed). ⭐⭐ CONVERTED (a close that FINDS something = the 17z-stronger-close precedent): emit `assets/ckir/visbuffer_fs.ckir` via a `[.emitckir]` regen test (`build_visbuffer_fs` IN-TREE till the user commits — uncommitted-delete scar), `ensure_visbuffer_fs`→thin resolve_program_text loader; GATED: kir load gate `[ceir18z]` (parse+roundtrip+Fragment+1out) + REN-38-F6 VISBUFFER device gate renders THROUGH the loaded asset both backends ([ren38] VK 303/13 + DX12 64/4, the 0.25/0.5 grey byte-exact). ⛔ FRAME-COOK GAP CONFIRMED (advisor catch): test_frame_template_bridge's SECTION list is HARDCODED (forward_csm_gpu present) — `forward_plus` / `forward_plus_gpu` / `forward_clustered_3d_gpu` have NEVER been through the frame-cook A/B fidelity + plan-build gate. ⏭ REMAINING: add those 3 frame SECTIONs (A/B + plan-build); the band-proof MATRIX = FULL module suites 4 configs (gpu-context vk+dx12 unrun since 17z 260/0 pre-relayout/templatize; win-asan scene-render stale since 17e; gpu-context-vulkan on linux-gcc-asan w/ the templatized header) + frame-cook 4 configs; sandbox smoke (CRD_ASSETS_DIR); the row-per-renderer table; DECIDE the shipped-default frame graph (forward_csm_gpu today; plus_gpu/clustered_3d gate-only). DELETION (build_cluster_light_cull + ckir_light_cull.hpp + build_visbuffer_fs) PARKED post-commit (+retire the [.emitckir] regen tests that call them). **STEP (B) BAND-PROOF MATRIX GREEN (the 17z full-suite discipline, not per-slice tags):** win-debug (gpu-context VK 6152/263 + DX12 2392/144, frame-cook [bridge] 1803/9 incl. the 3 NEW frame SECTIONs, scene-render full 1872/87 + [ren38] visbuffer); win-asan ASan-CLEAN (frame-cook 1803/9, gpu-context VK 6152/263 + DX12 2392/144 ⇒ the disclosed "no [clustered3d] ASan coverage" CLOSED, scene-render full 1903/89 ⇒ the "stale since 17e" gap CLOSED); linux-gcc-debug (kir [ceir18z] 7/1, frame-cook [bridge] 1803/9, scene-render [ren38]+[ceir18b]+[ceir18a2] 330/13, gpu-context [lightcull]+[clustered3d] 874/3); linux-gcc-asan ASan-CLEAN, SAME (⇒ the advisor's gcc-compile-of-the-templatized-header gap CLOSED — 2nd gcc config); TIDY clean (scene_renderer.cpp + test_ckir_asset.cpp + test_frame_template_bridge.cpp; shared header gate-invisible). SANDBOX SMOKE: shipped default renders REAL (CRD_ASSETS_DIR=assets → "PASS 242 frames, 5251 instances, 11 gpu passes" — NOT overlay-only; the false-green scar cleared). **ROW-PER-RENDERER PROOF TABLE:** Forward+ tiled → scene_forward_clustered.crdl + forward_plus/forward_plus_gpu.frame.toml + scene_light_cull.ckir → scene-render [ceir18a] image gate + gpu-context [lightcull] parity + frame-cook [bridge]; Clustered 3D → scene_forward_clustered_3d.crdl + forward_clustered_3d_gpu.frame.toml + scene_light_cull_3d.ckir → [ceir18b] two-depth image + CPU-drift + [clustered3d] parity; Deferred → deferred_lighting.ckir + deferred.frame.toml → CEIR-18c deferred gate (rides scene-render full); Visibility → scene_visbuffer.frame.toml + **visbuffer_fs.ckir (NEW 18z)** → REN-38-F6 [ren38] VK 303/13 + DX12 64/4; GPU-driven → scene_cull*.crdv + forward_csm_gpu.frame.toml → CEIR-17d/18e cull + REN-40-G (rides full); Impostor → impostor_{vs,fs}_{plain,dither}.ckir → impostor pixel gate (rides full). The full module suites SUBSUME the per-renderer gates; the table maps them. **⛔ SHIPPED-DEFAULT DECISION (advisor: decide-or-carry-explicitly — CARRIED):** `forward_csm_gpu` STAYS the shipped default; `forward_plus_gpu` (tiled) + `forward_clustered_3d_gpu` (3D) are AUTHORED + PROVEN alternatives an app selects via set_frame_graph_toml, NOT the default. Rationale: flipping the default changes EVERY consumer's rendering + needs a PERF-gated slice (clustering overhead for low-light scenes; default-path image/perf gates) — a future "make clustered the default" slice, not this authored-asset proof band. **✅ CEIR-18 BAND CLOSED: every renderer (Forward+/Clustered/Deferred/Visibility/GPU-driven/impostor) is an authored CEIR asset; the shipped path has ZERO hand-built KGraph (the ensure_* audit found + converted the last one, visbuffer_fs — a stronger close for FINDING one, per 17z).** Proof + deletion close.** All 5 renderers as authored assets running the FULL suite on a REAL device against DETERMINISTIC reference verdicts/images (ABSOLUTE — never CEIR-vs-CEIR); whatever imperative renderer code remains (if any) DELETED (the-deletion-is-the-proof); the deletion-gate RE-RUN + a HEAD `git stash` baseline (the 17z regression-hunt discipline — it caught 3 latent CEIR-replay drops the per-slice gates missed). GATE: 2 Win + 2 Linux + tidy; blast radius green. advisor at entry. | §130 §131 §132 §178 |
| ✅ | CEIR-19 | **`ceir.rt` band — BAND ✅ 2026-08-16 (advisor-scoped; 19a/19b/19c/19z all ✅). RT orchestration as authored assets.** §49 DIALECT + §133 HYBRID RT (raster-primary → RT shadows/AO/reflections → denoise → composite; NO hybrid-renderer class) + §134 WAVEFRONT PATH TRACER (queue/state dynamic-work: ray-q → trace → hit/miss-q → shade → shadow-q → next-ray-q → compact → repeat — a CONTROL-FLOW test of CEIR, NOT image quality). ⛔ EXISTENCE CHECK: RT INFRA ships (vulkan_ray_tracing_context pipelines, inline rayQuery ckir_rt.hpp, hair LSS, RT kernels as .crdv via ensure_rt_kernel, scene_rt frame + REN-38-F6 RT gate; B14 SVGF/ReSTIR denoise) — but NO wavefront path tracer / queue-orchestration ships ⇒ 19a/19b = declare/assemble (promotion), 19c = NET-NEW. ⛔ PIN (verify before the sub-rows, don't inherit): (a) DX12 RT parity — the F6 RT gate's backend coverage decides both-backends vs Vk+DX12-disclosure; (b) llvmpipe has NO RT — every RT device gate needs a caps-SKIP guard (17z VRS precedent), Linux legs' value = compile+dialect+oracle. ⛔ DETERMINISM = a DESIGN INPUT for 19c (fixed seed + B17-c deterministic-hash + FIXED queue ordering + bit-exact serial compact — atomic-append = nondeterministic ORDER = oracle-parity dies). Order 19a→19b→19c→19z; advisor at each entry; gate 2 Win + 2 Linux + tidy; NEVER CEIR-vs-CEIR. | build §49; corpus §90; proof §133 §134 |
| ✅ | CEIR-19a | **`ceir.rt` DIALECT declaration (the 17a mold).** `rt.ceirop.toml` → opgen: the §49 vocabulary (blas_build/update/compact, tlas_build/update, instance_populate, sbt_build, trace/trace_indirect, ray_query, group/geometry attrs; motion/OMM/SER as DECLARED capability attrs even w/o a backend impl — declared≠implemented, the open-world contract). Opaque Extern type-classes for BLAS/TLAS/SBT handles (the 17a attachment precedent). Honest effects (AS builds = writes + ExternalNondeterminism on build timings). `find_rt_misuse` chain-walk. Device-free sentinel tests. ⛔ FIRST the 17a CURRENCY CHECK: grep how vulkan_ray_tracing_context + the scene_rt frame NAME these steps — declare THAT surface, not the Khronos spec. **DESIGN-LOCKED (advisor 2026-08-16, after the currency check):** the two rules operate on DIFFERENT layers — "declare THAT [engine] surface" governs NAMING/SEMANTICS (the engine is the witness a step is real), §49 governs DECOMPOSITION (the vocabulary future lowerings cite, not a transcript of today's convenience wrappers). ⇒ **6 OPS, fine-grain**: `blas_build`, `tlas_build`, `instance_populate`, `sbt_build`, `trace`, `ray_query` — each op's docs line NAMES its engine lowering (the vulkan_ray_tracing_context method that realizes it today; the engine's fused `build_scene_as` = ONE host lowering of blas_build→instance_populate→tlas_build; `sbt_build` = the group-binding step the host FOLDS into `trace_rays_pipeline`, no standalone SBT object — legit as an op ONLY because that sentence is honest). HARD BOUNDARY: no op whose semantic the engine can't witness AT ALL → `blas_refit`/`blas_compact` + motion/OMM/SER/displacement/AS-residency go in the **capability-ATTR tier** (declared≠implemented, the open-world contract). **3 TYPE-CLASSES (load-bearing, NOT future-proofing): `rt.blas` / `rt.tlas` / `rt.sbt`** — the ROLE rides the TYPE so find_rt_misuse verifies the chain: `tlas_build` consumes instance-populated blas; `trace` consumes tlas+sbt; `ray_query` consumes tlas ONLY (the inline-vs-pipeline distinction, checkable only if tlas≠sbt TypeIds). The engine's fused `RtScene` is the host value backing %blas+%tlas (note the mapping in docs, don't weaken the types — the 17b u64-SceneResolveHandle precedent). **EFFECTS**: AS builds = GPUCommand + MemoryReadWrite/Write (device-memory write) + native determinism ExternalNondeterminism; `trace`/`ray_query` carry `compute.dispatch`'s device-execute effect `[GPUCommand, MemoryReadWrite]` (they run USER shader groups — raygen/chit do arbitrary writes; NOT a resolve-style read effect); `ray_query` ≈ compute.dispatch+a tlas operand (DOC the closeness, don't invent parallel semantics; the inline rayQuery MATH is CKIR-level in ckir_rt.hpp — the CEIR op is the DISPATCH). **PROVIDER**: host, ExternalNondeterminism, hot_reload_safe=false (mirror scene verbatim). ⛔ BEFORE the TOML: read render.ceirop.toml/frame.ceirop.toml for how a DEVICE-EXECUTE-effect op is spelled (scene has only host-read intrinsics; trace needs the GPUCommand vocab — compute.ceirop.toml dispatch is `[GPUCommand, MemoryReadWrite]` + kernel_ref). TESTS = the 17a shape (well-formed blas→tlas→trace chain + one mistyped-operand negative per op + the closed-vocab attr negative + intrinsic reflection); ⛔ I6 — find_rt_misuse switches on TypeId/traits, NEVER op.kind. Gate: ceir device-free + opgen-drift, 4 configs; ZERO renderer changes. **✅ DONE + gated 4 configs + tidy 2026-08-16 (advisor at design-lock + at close).** `engine/ceir/ops/rt.ceirop.toml` (6 ops: blas_build/instance_populate/tlas_build/sbt_build/trace/ray_query) → opgen (rt_ops.hpp/cpp + .ops.json/md; opgen-drift + validator ctests GREEN). Hand-written `engine/ceir/{include/crd/ceir/rt.hpp, src/rt.cpp}` (the scene.hpp/cpp mold): `register_dialect` = register_rt_ops + 3 opaque Extern type-classes (rt.blas/tlas/sbt, DISTINCT TypeIds — the ADR-0111 landmine, load-bearing so `trace` consumes %tlas+%sbt while `ray_query` consumes %tlas ONLY); `{blas,tlas,sbt}_class` + `type_{blas,tlas,sbt}` factories; `find_rt_misuse` type-chain walk (its OWN RtMisuseKind enum, I6-clean — `ctx.op_name(op->kind())` string-match, NEVER a kind switch). ALL 6 ops = native{provider=host, ExternalNondeterminism, hot_reload_safe=false} + effects[GPUCommand, MemoryReadWrite]; ray_query carries kernel_ref (the inline CKIR kernel). ⛔⛔ **NATIVE-vs-DEVICE-EXECUTE resolved (advisor, after I oscillated B→A→doubt): ALL 6 native.** `trace` has NO kernel_ref (its shader groups ride the SBT) ⇒ it can ONLY be a host intrinsic (trace_rays_pipeline) ⇒ forced native; splitting ray_query to device-execute would fracture one capability surface across tiers. `native`+`kernel_ref` is NOT contradictory (kernel_ref = cook-time dependency extraction + §107 interface pinning, orthogonal to execution tier — the host intrinsic dispatch_inline_ray_query consumes the symbol). Cheap-check PASSED: grepped engine/ceir + tools/ceir_opgen — the §106 collector reads `intrinsic` and `kernel_ref_symbol` INDEPENDENTLY, dialect.cpp only asserts `intrinsic⇒has-provider`; nothing assumes `kernel_ref⇒not-native`. [[feedback_native_gpucommand_capability_tier_kernel_ref_is_cooktime_not_execution_tier]]. ⛔⛔ **VERIFIER GAP-FILL (advisor-caught BLOCKER before close — docs promised checks that didn't exist):** the TOML docs named enum values that don't exist (InstanceBlasNotBlas/TraceTlasNotTlas/…) + promised access-token arity + operand-type checks the walk didn't implement — thinner than its own `find_dispatch_misuse` mold while claiming parity. FIXED (the mold IS the floor — refs=floor): (a) corrected every doc enum name to the real one + re-attributed the SymbolRef/count/presence claims to the GENERATED verify_* (which owns structural conformance); (b) implemented the dispatch-shape checks in `scan_rt_region` mirroring find_dispatch_misuse for trace/ray_query — DimNotIndex (dims 0-2 Index-typed), AccessTokenInvalid (kind-fold + {r|w|rw} tokens), AccessArityMismatch (token-count == binding-count), BindingNotResource (variadic bindings resource-kinded); the AS-build operand-resource-kind refinement DEFERRED to 19b (with the lowering in hand — advisor-scoped, in-pattern with the declared≠implemented tier). GATE (all GREEN, 80 assertions/5 cases): win-debug (full crd-ceir-tests 3429/441 + [rt] + opgen-drift/validator) + win-asan ([rt], post-gapfill) + linux-gcc-debug ([rt]+opgen, `-Werror=switch` clean on the widened enum) + linux-gcc-asan ([rt]+opgen, the new parse_access byte-scan ASan-clean) + tidy (rt.cpp/rt.hpp/test_rt.cpp, LLVM 20). crd-ceir NEVER links gpu-context (I3/I4) — DECLARE-only, ZERO renderer changes. Test-list: added test_rt.cpp to tests/ceir/CMakeLists.txt; the generated test_rt_gen_smoke.cpp auto-globbed (zero-edit per §2d). | §49 |
| ✅ | CEIR-19b | **✅ DONE 2026-08-16 — the AUTHORED HYBRID RT-SHADOW renderer (rt_shadow.frame.toml: raster forward → COMPUTE worldpos-reconstruct → raytrace.pipeline shadow → composite) works END-TO-END on THREE devices: Win Vulkan + Win DX12 (DXR) + Linux lavapipe. ABSOLUTE gate (CEIR-19b GATE ×2 backends): receiver-only raster + TLAS-only OFFSET occluder + baked light(0,8,0); STAGE 1 worldpos re-projection round-trip (MIRROR-PROOF) + .w validity; STAGE 2 RT shadow-mask vs a per-pixel CPU ray-quad test (disagree==0) + non-empty class floors. Suites GREEN: Win scene-render 1967/90 + frame-cook 2973/96; Linux GCC/lavapipe scene-render 1548/66; tidy clean. THREE NEVER-RUN DEFECTS FIXED: (1) the runtime compute-storage-slot order is READS-FIRST (frame_runtime.cpp), OPPOSITE frame_template_bridge map_compute's writes-first — light_cull never exposed it (1 read+write buffer); rt_worldpos.ckir authored rt_constants@0/worldpos@1 to match (contract in its header). (2) ⛔⛔ a SECONDARY depth-format write routed to the depth attachment ONLY under `pass_flag(kMrt)` (frame_runtime.cpp:1085) → a plain raster.geometry forward pass declaring a depth transient (for a compute to sample) had scene_depth DROPPED → fell to a COMPANION depth → the compute sampled a CLEARED (0) transient → far-plane worldpos; FIXED: gate on `!first_write` (a depth write FOLLOWING a colour primary → depth_target), preserving raster.depth_only's primary. (3) lavapipe now RUNS RT pipelines (no longer caps-SKIP): the composite (shader-object) pass records AFTER the monolithic RT bind → its vkCmdSet* volley trips VUID-vkCmdTraceRaysKHR-None-08608 (graphics-only state, does NOT affect the trace; Win-SDK VVL didn't check it). ⛔ FILED **CEIR-19b-F1**: the encoder must re-set dynamic state / re-bind shader objects on the first GRAPHICS pass AFTER an RT bind (BOTH backends); the gate asserts a BOUNDED VUID-string-matched exception (error_count==n_08608, n_08608<=8) so a 2nd VUID class can't hide — REMOVE the exception when F1 lands. ⛔ LAVAPIPE = a live 3rd RT device now → upgrade the 19c/19z Linux ritual ([[reference_wsl_linux_sweep_and_llvmpipe_exposure]]). ⛔ FILED (latent): the bridge-vs-runtime desc→PassPayload lowerings DRIFTED (writes-first vs reads-first slot order + the depth-format split); the 18z A/B template-bridge fidelity gate covers NEITHER — fix ONE deliberately later. ⛔⛔ SCOPE HONESTY: the composite is STILL STEP-1 PASSTHROUGH — the RT shadow term is proven via mask READBACK (STAGE 2), NOT yet VISIBLE in the final image; the composite multiply (scene_hdr × shadow_mask) is **FILED CEIR-19b-F2** (rt_composite.ckir gains the multiply; gate = a composited-darkening image check). ⛔ **CEIR-19b-F1 DoD INCLUDES deleting the bounded VUID-08608 exception block** in test_scene_render_gpu.cpp (the ENCODER fix owns the exception removal — else it fossilizes: n_08608→0 keeps error_count==n_08608 green). Rides commit ⑦. ~~🔄 IN PROGRESS~~. Was: HYBRID RT as an authored asset (the 18c mold; §133 RT SHADOWS). DESIGN-LOCKED 2026-08-16 (advisor at entry + a reconcile round).** SCOPE: ONE RT contribution (RT shadows), raster-primary → RT-shadow → composite; NOT shadows+reflections+AO. **⛔ VERIFY-FIRST FINDINGS (the 18a assembly-optimism scar — 4 entry checks, each changed the design):** (1) **RT pass kinds EXIST** — `raytrace.pipeline` (raygen/miss/chit SBT, F6-proven BOTH backends) + `raytrace.dispatch` (inline ray_query kernel); both are authored `.frame.toml` `kind=` values (frame_asset.hpp kExec*, frame_ceir.cpp emits a `frame.pass` op executor+kernel). (2) **SVGF is NOT an invocable pass** — `ckir_svgf.hpp` kernel + test only ⇒ denoise stays OUT of the assert path (raw shadow-mask is the verdict; denoise = a later wiring sub-slice). (3) **⭐ POSITION B (advisor, after I surfaced the evidence): 19b assembles via the EXISTING `raytrace.pipeline` frame.pass, NOT a ceir.rt bridge.** The RT frame pass is ALREADY authored-CEIR (frame.pass op + `ensure_rt_kernel`=cook_stage_named LOADER over disk `scene_rt_*.crdv` — passes the 18z audit discriminator + a REGISTERED executor = the same "backends execute" tier every CEIR-18 renderer was certified on). "RT is imperative" was FALSE — assembly-on-this is NOT the disguised-failure class. **⛔ THE ceir.rt→gpu BRIDGE IS RE-HOMED TO CEIR-19c** (its real consumer = the §134 wavefront-PT orchestration program; a bridge shaped by a trivial one-dispatch shadow consumer would be wrong — 19c's queues/indirect/loop-until-empty shapes it). (4) **DX12 inline twin EXISTS** (`Dx12RayTracingContext::trace_dispatch`) but the frame-graph INLINE executor is NEVER-RUN (no `.frame.toml` uses `raytrace.dispatch` — the 18c compile-proven-never-run class) ⇒ **MECHANIC = `raytrace.pipeline`** (F6-proven both backends; the smaller honest slice per advisor). **⛔ DEPTH-HANDOFF SOLVED w/ ZERO executor engine-work (the design-lock blocker):** RT + compute kernels bind STORAGE BUFFERS + accel (NOT sampled images — trace_dispatch/trace_rays_pipeline bind TLAS@0 + buffers), BUT the `compute.dispatch` payload arm binds `p.sampled[0]` as a TEXTURE (frame_runtime.cpp ~309). RT arms bind accel (no sampled); compute binds sampled (no accel) ⇒ NO single pass reads depth AND traverses ⇒ a clean TWO-pass split, all existing bindings. **AUTHORED HYBRID GRAPH:** (a) raster lit → scene_hdr(color)+scene_depth(depth); (b) `compute.dispatch` shadow_prepass: reads scene_depth(sampled) → writes worldpos_buf(storage) [authored CKIR: depth→world-pos via inverse-proj — the NDC±Y + depth-as-float scars live here]; (c) `raytrace.pipeline` rt_shadow: reads worldpos_buf(storage)+scene_tlas(accel) → writes shadow_mask_buf(storage) [authored scene_rt_shadow_{raygen,miss}.crdv — miss=lit, TERMINATE_ON_FIRST_HIT toward the fixed light]; (d) `fullscreen` composite: scene_hdr × shadow_mask → @output. **PROOF (ABSOLUTE, never CEIR-vs-CEIR):** deterministic fixed occluder + fixed light; assert the RAW shadow-mask FIRST (occluded probe ≈0 vs unoccluded ≈1, analytic), the composited darkening 2nd; ValidationCapture; both backends caps-SKIP (`supports_rt_pipeline()`); Linux legs = cook + dialect + oracle ONLY (llvmpipe has NO RT). ⛔ the never-run-inline lesson still applies to the compute+RT WIRING (18c isolation discipline — staged diagnostics, val oracle). **PROGRESS 2026-08-16 (format study + first artifact):** ⭐ FORMAT FINDINGS (the do-first study, each shapes the build): (a) the RT shaders are RIGID cook_rt TEMPLATES (vertex_asset.cpp `cook_rt` — the raygen hardcodes a primary ray origin(0,0,0) dir(0,0,1)+Z; params are only payload_words/bindings/alpha_cutoff/sphere_radius/use_callable) ⇒ the shadow raygen needs a cook_rt GENERALIZATION (new `[rt]` params: a buffer-read ray ORIGIN + a fixed LIGHT direction), authoring-tier cook-code — NOT executor engine-work (⛔ vertex_asset.cpp is the dither-shared-temp scar's home — materialize any dual-write temps). (b) ⭐ FEASIBILITY CONFIRMED: a COMPUTE CKIR kernel CAN sample a texture — `tex_sample_lod` (ckir.hpp) is "legal in ANY stage (no derivatives)"; the frame_runtime `compute.dispatch` arm binds `p.sampled[0]` as a texture (~line 311) ⇒ the worldpos compute prepass reading the depth image is real. (c) the depth-handoff two-pass split holds: RT arms bind accel+storage (no sampled), compute binds sampled (no accel) ⇒ neither reads-depth-AND-traverses ⇒ 2 passes. (d) worldpos_buf/shadow_mask_buf = `external_buffer` (host-provisioned + host-sized, the scene_rt `hits`/18a-2 `instances` precedent — the renderer provisions them + the inverse-VP matrix). ✅ FIRST ARTIFACT: `assets/frame/rt_shadow.frame.toml` AUTHORED (the 4-pass hybrid: forward raster→scene_hdr+scene_depth; compute shadow_prepass reads scene_depth sampled→worldpos_buf; raytrace.pipeline rt_shadow reads worldpos_buf+scene_tlas→shadow_mask_buf; raster.fullscreen composite scene_hdr×shadow_mask→@output). ✅ SHADER UNIT 1 DONE 2026-08-16 (cook_rt generalization + the 3 shadow shaders, vertex-cook GREEN 445/25): `cook_rt` (vertex_asset.cpp) GENERALIZED — new `[rt]` params `to_light`/`origin_binding`/`light_x,y,z`/`miss_value`/`hit_value` (parsed + EMITTED, the inert-copy-rot round-trip scar honored + the RtDesc struct). The raygen now reads its ray ORIGIN per-launch from `origin_binding`[LaunchId] (vec4 stride, materialized — the dither-shared-temp scar) + traces toward the fixed light (dir = normalize(light-origin), tmax = the light distance ×0.999 so the light is not an occluder) when `to_light`, else the byte-identical F6 primary +Z ray (445 assertions unchanged prove it). Miss/chit write the authored `miss_value`/`hit_value`. Authored `assets/vertex/scene_rt_shadow_{raygen,miss,chit}.crdv` (raygen: to_light, origin_binding=1, out_binding=2, light=(0,8,0); miss_value=1.0 LIT; hit_value=0.0 SHADOWED). ⛔ BINDING LAYOUT (verify at the gate — never-run wiring): accel=0, worldpos_buf(read)=1, shadow_mask_buf(write)=2 (the F6 out_binding=1 "storage" precedent + reads-before-writes). ✅ SHADER UNIT 2a DONE 2026-08-16 (feasibility + raygen guard + TOML — advisor at the reconstruction-math checkpoint): ⭐ FEASIBILITY CONFIRMED (advisor-mandated greps, the compute-emitter-lag scar): BOTH compute emitters have the texture arms — GLSL `emit_compute_kernel_glsl` (Texture/Sampler decls + SampleLod→textureLod + VEC/MAT parity + separable-texture bindings), HLSL `emit_compute_kernel_hlsl` (Texture/Sampler + SampleLod→SampleLevel). ⛔ NO live compute-stage `tex_sample*` caller exists (raster/vertex only) — the worldpos kernel is the FIRST to exercise it (arm present ⇒ proceed, but exercise at the device gate). ⛔⛔ 2 ADVISOR-CAUGHT DEFECTS FIXED: (i) the raygen indexed LaunchId.x only ⇒ pinned 1D launches BOTH passes (groups_x=W*H, groups_y=1) + STATIC params in the TOML (was {1,1,1} default = the REN-38-A16 one-ray scar); (ii) a SKY pixel (cleared far depth 0.0 through the infinite-far inverse-VP ⇒ homogeneous w==0 ⇒ NaN origin ⇒ val_err spike) — the raygen now reads a VALIDITY flag from worldpos_buf[idx].w and GUARDS: valid→trace+store, sky→store 1.0 LIT (two sibling stmt_if, idx/valid materialized). Raygen RESTRUCTURED to a clean to_light-branch vs else-primary (F6 byte-identical — vertex-cook GREEN 445/25 unchanged). ⛔ PINS: `ndc_y_sign` provisioned host-side from `ndc_y_points_down()` (never hardcoded); `shadow_prepass` gets `depth_as_float=true` (else the sampled depth binds as a comparison sampler — the depth-as-float scar's landing point); `rt_constants` = a dedicated F32 external_buffer (inverse-VP 16f + screen w,h + ndc_y_sign) — declare DType::F32, skip int_bits_to_float. TOML `rt_shadow.frame.toml` UPDATED (rt_constants resource + shadow_prepass reads it + depth_as_float + 1D params). ⭐ GATE STAGE 1 (advisor-endorsed): a worldpos-readback-vs-CPU-analytic probe (isolates reconstruction from RT wiring — the only oracle a vec-tier kernel gets). ✅ SHADER UNIT 2b DONE 2026-08-16 (the worldpos kernel — kir GREEN, EMITS BOTH backends): ⭐ WIRING FULLY VERIFIED before authoring (the never-run compute-texture path): the frame-graph SAMPLED-COMPUTE binding layout is `sampled_compute_set_layout` (vulkan_raster_context / dx12_raster_context, ONE set 0, kMaxKernelBuffers=8) — buffers 0..N-1 in reads-then-writes order, sampled image @8, sampler @9; the executor ROUTES compute+sampled → `dispatch_kernel_sampled` via command_lowering.hpp:603 (the HZB `occlusion_cull` precedent, a live frame-graph compute+sampled pass); ALL emitter arms present BOTH backends (GLSL emit_compute_kernel_glsl + HLSL emit_compute_kernel_hlsl: Texture/Sampler decls + SampleLod→textureLod/SampleLevel + VecComp/Swizzle/Vec2). ⭐ THIS ALSO CONFIRMED the shadow .crdv bindings: the RT layout is "binding 0=TLAS, 1..N=SSBOs" (vulkan_raster_context:6160) ⇒ as=0/worldpos=1/shadow_mask=2 CORRECT. WROTE `engine/kir/include/crd/kir/ckir_rt_worldpos.hpp` (build_rt_worldpos — IN-TREE regen source, the ckir_light_cull.hpp precedent): rt_constants b0 (read F32: inverse-VP[0..15] ROW-MAJOR + screen_w[16]/h[17] + ndc_y_sign[18], NO int_bits_to_float), worldpos_buf b1 (write), depth texture b8, sampler b9; gid flat (WorkgroupIndex*threads+LocalInvocationIndex) → guard gid<w*h → x=gid-(gid/w)*w → uv → tex_sample_lod LOD0 → .r → ndc=(2u-1, ndc_y_sign·(2v-1), depth, 1) [reverse-Z: raw depth=ndc_z] → world=invVP·ndc component-wise ROW-MAJOR → /w (materialized) → worldpos_buf[gid*4+0..2]=world.xyz, +3=Step(1e-7,depth) validity. GATE `[ceir19b]` (13 assertions, 2 cases + the [.emitckir] regen 3rd): build_rt_worldpos = Compute + local_size 64 + byte-exact ckir_roundtrip + ⭐ EMITS GLSL **AND** HLSL (the first compute-stage texture sampler works on both backends) + the committed `assets/ckir/rt_worldpos.ckir` parses as Compute. Tidy clean (ckir_rt_worldpos.hpp + vertex_asset.cpp + test_ckir_asset.cpp). ⛔⛔ MATRIX-LAYOUT BUG CAUGHT + FIXED 2026-08-16 (before any device run — the matrix-layout scar): crd::math::Mat4f is COLUMN-major (a memcpy gives column-major words — the TAA-constants precedent scene_renderer:5685), so the kernel's inverse-VP read MUST be column-major (world_h[r] = Σ_c word(c*4+r)·ndc[c]); the initial ROW-major read would have computed invᵀ·ndc = garbage world-pos. VERIFIED the whole reconstruction against `compute_froxel_aabbs` (the authoritative CPU unprojection: inv=inverse(view_proj); clip=(2u-1, flip_y?1-2v:2v-1, reverse-Z-depth, 1); h=inv*clip; w=h.xyz/h.w) — my ndc_y_sign = ndc_y_points_down()?+1:-1 MATCHES its flip_y convention (= the TAA word-20 precedent). Fixed mrow → column-major, re-emitted rt_worldpos.ckir, [ceir19b] re-GREEN (13 assertions, both backends). ⭐ WIRING PLAN GROUNDED (the render-path install site scene_renderer ~5644-5692, mirroring the froxel + TAA-constants provisioning): (a) rt_constants = ensure_rt_constants() → create_storage_buffer(20*4) + per-frame fill `consts[0..15]=memcpy inverse(view_proj) COLUMN-major; [16]=W; [17]=H; [18]=ndc_y_points_down()?1:-1` → upload_storage (the exact TAA-constants mold @5682); (b) worldpos_buf/shadow_mask_buf = SIZED external buffers (create_storage_buffer(W*H*16 / W*H*4), not the 4096 ensure_scratch) via the storage_buffer() resolver arms (@5254) + debug_scene_buffer arms (@3091) for gate readback; (c) the shadow .crdv load via cook_stage_named + rt_worldpos/rt_composite via resolve_program_text→ckir_read; register via program_registry (the ensure_rt_kernel block @2592); (d) PIN the gate 64×64 so the static frame.toml params (compute 64, rt 4096) are correct — no renderer dispatch-override (disclosed). ✅ SHADER UNIT 3 (rt_composite passthrough) DONE 2026-08-16 — ALL 19b CKIR/shader authoring is now complete (worldpos + composite + 3 shadow .crdv). build_rt_composite (inline in test_ckir_asset.cpp, the visbuffer_fs precedent): a FULLSCREEN FS — stage_in(vec2 UV loc 0) → tex_sample(scene_hdr @ binding 1 single texture [the 1-read-binds-single-texture scar, NOT the bindless heap], sampler @ binding 2, UV) → @output. ⛔ STEP 1 = PASSTHROUGH (samples scene_hdr, ignores shadow_mask) — unblocks the frame + presents while the RAW shadow term is the BUFFER-READBACK verdict (advisor stages 1/2 are primary); STEP 2 (the §133 composited darkening, gate stage 3) = a focused FOLLOW-UP that multiplies by shadow_mask_buf[flat pixel] (deferred — needs W + an NDC±Y-correct fullscreen-UV→shadow-row map, since the RT wrote by 1D LaunchId.x). GATE `[ceir19b]` now 22 assertions/4 cases: build_rt_composite = Fragment + byte-exact roundtrip + committed rt_composite.ckir loads (+ the worldpos build/emit/load). Tidy clean. ✅ SCENE_RENDERER WIRING DONE 2026-08-16 (crd-scene-render COMPILES clean): members buf_worldpos/buf_shadow_mask/rt_constants + kern_rt_shadow[3]/kern_rt_worldpos; ensure_rt_shadow_kernel(0..2)→cook_stage_named(scene_rt_shadow_*.crdv); ensure_rt_worldpos→resolve_program_text("ckir/rt_worldpos")→ckir_read→create_program; ensure_rt_composite→post_fullscreen VS + rt_composite.ckir FS→create_raster_program; ensure_rt_constants(20*4) + ensure_rt_buffer(sized); REGISTERED engine://scene/rt/shadow_{raygen,miss,chit} + rt_worldpos (register_kernel) + rt_composite (register_raster); storage_buffer() resolver arms + debug_scene_buffer arms (worldpos_buf/shadow_mask_buf) for gate readback; PER-FRAME provisioning @ the render install site (size buffers to W*H + upload rc_consts[0..15]=memcpy inverse(view_proj) COLUMN-major, [16]W [17]H [18]ndc_y_points_down()?1:-1 — the TAA-constants mold). ⛔ one compile fix: create_storage_buffer takes u32 (was u64). REMAINING: (5) the ABSOLUTE device gate in test_scene_render_gpu.cpp (mirror the REN-38-F6 RT gate ~1855; Vk+DX12 twins, PIN 64×64, caps-SKIP supports_rt_pipeline): DETERMINISTIC scene = receiver + occluder between it and light (0,8,0), TLAS from the same triangles; STAGE 1 worldpos-readback-vs-CPU-analytic FIRST (the reconstruction oracle), STAGE 2 shadow-mask occluded≈0/unoccluded≈1, [STAGE 3 = the real composite = follow-up]; ValidationCapture val_err==0; run + FIX every never-run-hybrid defect (18c 6-defects precedent); then 4-config gate + tidy (scene_renderer.cpp). advisor at the gate design + at close. | §133 |
| ✅ | CEIR-19c | **WAVEFRONT PATH TRACER (NET-NEW — the band's real work). STAGE 1 ✅ (bridge, 3 devices); STAGE 2 ✅ (wavefront queues/compact, 3 devices).** ⛔ OWNS the ceir.rt→gpu BRIDGE (re-homed from 19b 2026-08-16): lower ceir.rt.trace/ray_query/blas_build/tlas_build onto vulkan/dx12 ray-tracing-context — the wavefront PT's queue/indirect/loop-until-empty orchestration is the bridge's true first consumer, so it SHAPES the seam (a bridge shaped by a trivial one-dispatch shadow would be wrong). This is where the 19a ceir.rt dialect gets its execution bridge + first live consumer.** §134 = a CONTROL-FLOW test of CEIR, NOT rendering quality: queues/compaction/dynamic-work as AUTHORED CEIR (persistent queues = group-buffer sections + counters; the compact = the 18a serial-compaction pattern scaled up; `trace_indirect` consumes the queue count). SCOPE the image claim: SINGLE-BOUNCE direct lighting on a trivial scene is a sufficient §134 proof; multi-bounce GI quality is NOT this band. ⛔ DETERMINISM = design input: fixed seed + B17-c deterministic-hash (bit-exact u32 avalanche GPU==oracle==both backends) + FIXED queue ordering + a BIT-EXACT serial/ordered compact on the proof path (an atomic-append queue is nondeterministic in ORDER → oracle-parity dies; atomics = a perf follow-up with an order-insensitive verdict). ⛔ CHECK EARLY: does `eval_cpu_kernel` model the loop-until-empty shape, or must the oracle drive iterations from the host? <br>**═══ DESIGN-LOCKED 2026-08-16 (advisor ×2 + orientation) ═══** <br>**• ORACLE EXISTS (CHECK-EARLY answered):** `eval_cpu_kernel` HAS `TraceRayClosest`/`TraceRayHit` (ckir_kernel_eval.hpp:610) — f64 brute-force watertight Möller-Trumbore over the AS-as-triangle-buffer (`data[0]=ntri`, then 9 floats/tri), returns closest-hit t + prim-id (0xFFFFFFFF=miss). The HOST drives iterations (calls eval_cpu_kernel per dispatch per iteration); the oracle does NOT model the loop internally. Write the loop ONCE as a shared harness shape so GPU-driver + oracle-driver cannot drift. <br>**• SEAM (the bridge's real work):** `execute_lowered` (ADR-0126, ceir-gpu) has NO TLAS/accel binding slot. The PROVEN inline-rayQuery GPU path (RT-1..RT-IB1, `ckir_rt.hpp` builders + the RT tests' `trace_dispatch` harness, both backends, GPU==oracle bit-exact, incl. ReSTIR persistent host↔device multi-pass reservoirs = the submit-readback-resubmit contract) uses the STANDALONE `RayTracingContext` (vk/dx12) with a bespoke root sig (TLAS=root SRV t0, buffers UAV u1+). **THE BRIDGE = unify that TLAS binding under the ADR-0126 execute seam** (add an accel-binding slot to the compute-kernel execute surface; lower `blas_build`/`tlas_build`→RayTracingContext AS builds; `ray_query`→dispatch+TLAS bound = `dispatch_inline_ray_query`). `trace_dispatch` is the REFERENCE for WHICH calls to make, NOT the shipping path (a parallel ceir.rt-only driver = the 3rd drifted lowering, the bridge-vs-runtime scar). X-ref `docs/design/ceir-0a-execution-path-inventory.md`. <br>**• BIT-EXACT SCOPE:** the traversal oracle is UNROUNDED f64 (unlike the rounded-per-op kernel arithmetic that makes GPU==oracle ULP-exact elsewhere) ⇒ **t is tolerance-class BY CONSTRUCTION** even if the trivial scene lands exact. The per-iteration hash checkpoint (hash after each compact, compared per-iteration GPU vs oracle) covers **DECISION-DERIVED INTEGERS ONLY**: prim-id, hit/miss, pixel-id, queue indices, counts — NO t-derived float in the hashed queue records. Radiance + t compare with tolerance, SEPARATELY. Design out FP divergence at SCENE-DESIGN time: axis-aligned exact-FP geometry, rays with clean separation (no ε-edge/silhouette) ⇒ decisions bit-exact. <br>**• NO NEW OP / indirect NOT proven:** host-driven readback-count loop (read count→const-grid dispatch) IS the §134 proof of the queue/compact machinery; GPU-indirect (count never touching host) = a FILED perf follow-up (+atomic-append note). Do NOT add a `trace_indirect`/`ray_query_indirect` op (op with no witness = the declared≠implemented line 19a drew) — name it forward in dialect docs at most. <br>**• DEFER trace-pipeline:** bridge lowers `ray_query`+AS-builds at 19c; `rt.trace`/`rt.sbt_build` lowering DEFERRED (named home: 19z or a filed row) — STRIKE-IN-PLACE any dialect-doc "the bridge lowers trace/…" claim. <br>**• KERNEL BINDING TABLE (reads-first, iidx low→high — runtime binds reads-first, 19b scar; contract line in each .ckir header):** raygen: reads{consts}@iidx0 → writes{ray_queue,ray_count}; trace-shade: reads{ray_queue,ray_count,TLAS}@low → writes{hit/radiance,hit_count}; compact: reads{queue_in,count_in}@low → writes{queue_out,count_out}. Decide ALL kernel iidx up front. <br>**• #34 BOUNDARY:** the RT-ladder builders (`ckir_rt.hpp` build_pathtrace/restir/…) are RT-research + ORACLE substrate in engine/kir, NOT scene_renderer render-technique C++ (what #34 deleted: light_cull, rt_worldpos). 19c authors the wavefront as .ckir with ZERO new builders; ckir_rt.hpp stays as parity/oracle substrate, NOT converted here (a separate unordered slice). <br>**• STAGED BUILD:** (1) bridge + a TRIVIAL one-`ray_query` gate — get the seam green ALONE (a single authored ray_query dispatch = the stage-1 bridge witness, NOT the DoD) → (2) author the wavefront kernels as .ckir → (3) host-driven loop harness → (4) full gate (single-bounce direct-light image + per-iteration decision-hash GPU==oracle both backends + lavapipe). Don't author kernels against an unverified seam. <br>**═══ STAGE 1 ✅ 2026-08-16 — the ceir.rt→gpu EXECUTION BRIDGE, verified on 3 RT devices ═══** <br>**• LOWERING:** LoweredKind +RayQuery/+AccelBuild (lower.hpp); lower_region recognizes rt.ray_query→RayQuery (grid from operands 0..2) + rt.{blas,instance_populate,tlas}_build→AccelBuild (handle-chain RAW barriers, inert in the RT executor). Widen-closed-enum AUDIT: validate_lowered/execute_lowered + render_materialize's switch reject RT kinds TYPED (gcc -Werror=switch validated on the lavapipe build). rt.trace/rt.sbt_build DEFERRED to 19z — a COMMITTED PIN (test: they lower to no command) + the per-op dialect-doc strike (rt.ceirop.toml + regen). <br>**• EXECUTOR:** `execute_rt_lowered` (execute.hpp/.cpp) — a caller-HOOK surface (ceir-gpu names NO backend, the KernelResolveFn precedent): RtHooks{build_scene, kernel_bytes→SPIR-V/DXIL blob, trace_dispatch, user} + host-span RtHostBinding (TLAS implicit@0, SSBOs by binding-operand order). `dispatch_inline_ray_query` realized; `validate_rt_lowered` pure half; barriers inert (submit+wait/dispatch). <br>**• AUTHORED KERNEL:** `assets/ckir/rt_witness.ckir` (mandate #1, NO builder) — the one-ray_query inline-rayQuery kernel (TraceRayHit: AccelStructDecl@0, rays@1, hit_t@2 F32, hit_prim@3 U32). ⛔⛔ + a FORMAT FIX in ckir_asset.hpp (the global ext/sbegin operand pools are now `[[ext]]`/`[[sbegin]]` SECTIONS, not bare keys — the FIRST asset with a non-empty serial_ext pool exposed the latent bare-key collision; scar `feedback_ckir_text_ext_pool_needs_section_not_bare_key`). <br>**• 3-DEVICE GATE:** an authored ceir.rt program (blas→instance→tlas→ray_query; ray_query hand-built w/ 3 bindings via create_operation) → lower_region → execute_rt_lowered over the RT context → GPU prim-id/hit-miss **BIT-EXACT** vs eval_cpu_kernel's TraceRayHit oracle (SAME loaded graph, AS-slot=triangle-soup) + MANDATORY ANALYTIC pins (t=[2,miss,miss,1], prim 0/miss/miss/0 — the cf(node) symmetric-bug guard). **GREEN: Win Vulkan (RTX) 144/1 + Win DX12 (DXR) 144/1 + Linux lavapipe (gcc) 144/1**; crd-ceir-gpu-tests [rt] 34/3 + crd-kir-tests [ceir19c] 17/2 + [asset] 415/20; all changed files tidy-clean; full kir suite exit 0. <br>**• ⛔ STAGE-1 SCOPE (NOT-YET — band-close re-verify must not overclaim):** (a) the FRAME-RUNTIME path is NOT routed — the gate drives execute_rt_lowered from a hand-assembled ceir.rt program in a TEST HARNESS; nothing routes a FRAME's ceir.rt region through it yet; (b) AccelBuild is witnessed for the FUSED-IDENTITY single-instance shape only (instance_populate's %xf transform is ignored by the fused build_scene hook); (c) the %tlas keys/handles threading is tested for N=1 (one TLAS, one handle). <br>**• ⭐ STAGE 2 (next):** the WAVEFRONT queues/compact — the loop calls execute_rt_lowered PER ITERATION with mutated host bindings (ReSTIR same-array precedent); the executor API already supports this (bindings are per-call), so stage 2 = harness + authored .ckir kernels, ZERO bridge changes expected (a bridge change in stage 2 = a red flag). <br>**═══ STAGE 2 DESIGN-LOCK 2026-08-16 (advisor band-open) — the WAVEFRONT ═══** <br>**• STRUCTURE** (single-bounce direct light AS a wavefront; all kernels authored .ckir; ZERO bridge changes): trace (ray_query camera rays → per-ray {hit_flag, prim, t}) → compact (SERIAL single-thread scan of hit_flags → dense hit-index queue + hit_count) → [host reads count] → shade (shadow-ray each compacted hit → lit/shadowed decision + radiance). <br>**• LOOP (Q1):** the harness is `while (count > 0)` — single-bounce runs iteration 1 (primary batch) then reads count==0 (shade emits zero continuation BY CONSTRUCTION) and EXITS. ⛔ assert BOTH: the loop body ran EXACTLY ONCE + it TERMINATED VIA THE COUNT READBACK (not a hardcoded single call). No 2nd bounce (the scope line stands). <br>**• COMPACT (Q2/Q6):** dispatch groups=(1,1,1); the kernel INTERNALLY tid==0-guarded (an If) — else 64 threads race the serial write (the FIRST If-gated serial body; If/Materialize text encoding proven by rt_worldpos). Author + gate it FIRST in ISOLATION (synthetic flag array, dead TLAS, GPU==oracle==hand-computed dense queue). The 18a per-workgroup prefix-sum = a PERF follow-up (filed). <br>**• ⛔ CONSTRAINT 1 (advisor-caught):** the compact has NO TLAS but the bridge's RayQuery path REQUIRES operand(3)=%tlas@descriptor0. RESOLUTION: compact + shade are BOTH ray_query ops against the SAME %tlas — shade traces (shadow rays), compact binds the TLAS@0 but NEVER traces (a DEAD binding, noted in the kernel header). Straddling two executors (compute-seam ComputeBuffer* vs RT-seam host-span) = complexity for zero §134 value → a host-span compute path is FILED. ⛔ CHECK (2-min, before authoring): eval_cpu_kernel tolerates an AccelStructDecl node no TraceRay reads (the decl only materializes a binding). <br>**• ⛔ CONSTRAINT 2 (advisor-caught):** shade's shadow ray needs the hit POSITION → shade RE-DERIVES o+t·d from the original ray buffer (bind rays to shade too; smaller queue than writing hitpos). t crosses trace→shade as f32 — FINE (feeds only the shadow-ray ORIGIN; lit/shadowed on the clean-separation scene is decision-robust to 1-ULP) — NOTE the scene-separation argument (looks like a "no t-derived in decisions" violation otherwise). <br>**• HASH (Q5):** HOST-SIDE, NOT a kernel — a plain C++ triple32 fold over the readback arrays (compacted indices in order, count, lit/shadowed flags) after EACH iteration, identically over the oracle's arrays. Fold ONLY decision ints (NO t-derived float — the stage-1 trap). A hash kernel = a 4th asset with only the test as consumer = scope creep. <br>**• QUEUES (Q3):** host arrays carried in-out (the ReSTIR precedent); each execute_rt_lowered is submit+wait → readback N coherent before upload N+1 (the synchronous seam the loop determinism rests on; GPU-indirect LOSES it → filed). <br>**• SEQUENCE:** compact kernel + isolated gate → trace kernel + gate → shade kernel + gate → the while-loop harness + per-iteration host hash + termination assert → 3 devices. One kernel green at a time; the loop harness LAST (when every stage it drives is already proven). <br>**═══ STAGE 2 ✅ 2026-08-16 — the §134 WAVEFRONT PATH TRACER as authored .ckir, on 3 RT DEVICES ═══** <br>**• KERNELS** (all authored .ckir, NO builder — the serializer-exercise→canonical-text→committed→DECOUPLED-load-gate path): `wavefront_compact` (BRANCHLESS serial stream-compaction, local_size=1, DEAD TLAS@0, unrolled Materialize/BufferStore cursor N=8) + `wavefront_trace` (≈rt_witness + hit_flag=cast(CmpNe(prim,0xFFFFFFFF),u32) + hit_t, local_size=64) + `wavefront_shade` (re-derive hitpos o+t·d + P+s·(L-P) shadow ray tmax<1 to baked L=(0,8,0), local_size=1, 4 SSBO bindings). <br>**• HARNESS (the §134 loop):** raygen(host-fill camera rays) → trace → compact#1 → [readback hit_count] → shade(groups=hit_count, the count-driven dispatch) → compact#2, driven by a host `while(count>0)` loop through execute_rt_lowered per dispatch. ⭐ ADVISOR'S DOUBLE-COMPACT continuation: the 2nd compact runs on HOST-ZEROED cont_flags → next_count=0 BY CONSTRUCTION (single-bounce); the loop TERMINATES via that readback (CHECK next_count==0 + iters==1). ⛔ ZERO bridge changes across ALL 4 new executor consumers — the strongest confirmation of the stage-1 seam design. <br>**• PROOF:** per-iter HOST triple32 fold over DECISION INTS ONLY (hit_count, compacted[0..count-1], decisions[0..count-1], next_count) GPU==oracle (eval_cpu_kernel mirrors the SAME host loop) + direct GPU==oracle + the MANDATORY ANALYTIC pin (decisions=[0,1,0,1]) + the count-driven-dispatch pin (shade gx==hit_count). Clean-separation scene (camera clears the occluder @ x=4; shadow rays cross y=4 @ 0.5·cx). <br>**• 3 DEVICES:** Win Vulkan (RTX) [ceir19c] 352/5 + Win DX12 (DXR) 354/5 + Linux lavapipe (gcc) 351/5 (the ±2 delta = the stage-1 Vulkan gate's 2 VUID-08608 assertions, which DX12 has no VVL twin for — pre-existing, NOT wavefront-related). kir [ceir19c] 71/8. All changed files tidy-clean. <br>**• ⛔ STAGE-2 SCOPE (NOT-YET — band-close re-verify must not overclaim) [CEIR-19z-4 deferral-ledger L8 HOME]:** (a) shade emits NO continuation flags yet (single-bounce has none; cont_flags is HOST-ZEROED) — MULTI-BOUNCE = one flag-store added to shade, the harness + compact ALREADY consume it (forward-correct). (b) the compact's queue width is COMPILE-TIME-UNROLLED (N=8) — a queue WIDER than its unrolled N is SILENTLY TRUNCATED to the first N (today by design: the real rays sit in slots 0..7; nothing checks the queue fits). (c) + stage-1's carried scope (frame-runtime path NOT routed / AccelBuild fused-identity only / keys N=1). <br>**• ⭐ AUTHORING-COUPLING RESOLVED (the deferred question):** 3 kernels went serializer-exercise → committed asset → decoupled gates; NO committed test couples any asset to any builder; the assets have since been EDITED as source (headers) WITHOUT touching the builders — the mandate-#1 coupling rule held under load. Memory: [[feedback_wavefront_double_compact_continuation_and_no_races_in_proof_assets]]. **CEIR-19c ✅ DONE — the band's real work; next = 19z (the deferral pins + VUID-08608-F1 + trace/sbt_build lowering + the filed follow-ups come due).** | §134 |
| ✅ | CEIR-19z | **Band proof + close (the 18z discipline).** Full-suite MATRIX 4 configs + tidy; the ensure_*/builder AUDIT (re-classify, don't inherit); the ROW-PER-CLAIM table (each RT capability → the gate proving it → re-run result + date); the deletion/deferral ledger. <br>**═══ BAND-OPEN 2026-08-16 (advisor) — a CLOSE, not a 4th feature slice ═══** <br>**• RESOLVE (claim-integrity — items that make an existing band claim false/fragile):** ⛔ **19z-1 = 19b-F1** (VUID-08608 encoder bind-hygiene: re-set dynamic state / re-bind shader objects on the FIRST graphics pass AFTER a monolithic RT bind, BOTH backends; then DELETE the bounded VUID-string exception in test_scene_render_gpu.cpp — that deletion IS the DoD. A band whose close-row says "3 devices" while a gate tolerates 8 spec violations is an asterisk on the whole matrix). **DO FIRST** (widest blast radius — encoder code both backends). ⛔ **19z-2 = the bridge-vs-runtime slot-order drift** (frame_template_bridge writes-first vs frame_runtime reads-first — a latent filed THIS band; fix ONE deliberately, FLIP the rt_worldpos.ckir kernel iidx in the SAME commit, + extend the 18z A/B gate so the drift class can't silently reopen). ⛔ **19z-3 = 19b-F2** (composite shadow-multiply: EDIT the committed rt_composite.ckir — add the shadow_mask read + multiply → shadow VISIBLE, converting 19b from "proven via readback" to "visible"; ALSO the first real .ckir-edit-as-source exercise). <br>**• LEDGER FORWARD (new capability — filed rows, NOT 19z work):** rt.trace/rt.sbt_build LOWERING to trace_rays_pipeline (a genuine feature, NO consumer yet — the inline path is by design; comes due when the pipeline path is needed); the 19c NOT-YETs (multi-bounce = one flag-store on shade; compact-N generalization; FRAME-RUNTIME ROUTING of a ceir.rt region [CEIR-20-adjacent — "dynamic work meets the frame"]; instanced/transform AccelBuild; N>1 %tlas keys). <br>**• 19z-4 = the CLOSE:** the 3 resolves green → full-suite MATRIX (4 configs + lavapipe; kir/ceir/ceir-gpu/gpu-context-{vulkan,dx12} + the scene-render 19b gates, since F1/F2 touch them) → the ensure_*/builder AUDIT re-classified (expect NO new builders; the 3 wavefront inline serializer-exercise builders = TEST-SUBSTRATE, classify EXPLICITLY so the audit doesn't trip) → the ROW-PER-CLAIM table → the deferral ledger. Advisor at close. SEQUENCE: 19z-1 (F1) ✅2026-08-16 [VUID-08608 was an UPSTREAM VVL FALSE POSITIVE — markers proved the bind→trace window CLEAN; fires@VVL-1.3.275, CLEAN@1.4.313; encoder net-zero (no bug); RESOLVED via version-scoped `whitelist(0x29056f6a)` + `REQUIRE(validation_layer_spec_version()!=0)` false-green guard → unconditional `error_count()==0`; BOTH lavapipe arms pass (stock whitelist-arm + 1.4.313 strict-arm), Win/DX12 skip clean; scar #3 rewritten; commit ⑩] → 19z-2 (drift) ✅2026-08-16 [bridge-vs-runtime compute/RT storage-slot DRIFT: runtime=reads-first (the shipping CONTRACT), bridge=writes-first. FIXED the BRIDGE (map_compute + map_rt_common) → reads-first; NO iidx flip (rt_worldpos.ckir already matched canonical) — rewrote its stale header comment. Safety: build_frame_graph_template has ZERO device callers. GATE: added rt_shadow to the CEIR-15e A/B section + a dedicated [ceir19z] test parsing the REAL rt_shadow.frame.toml asserting absolute slots (reads-first + accel + depth-transient→depth). frame-cook 3060/97 green; lavapipe 19b both arms green; tidy-clean; commit ⑪] → 19z-3 (F2) ✅2026-08-16 [composite shadow-multiply: the RT shadow term is VISIBLE (scene_hdr × shadow_mask). Authored in rt_composite.ckir (StorageLoad shadow_mask_buf[py*TexSize.x+px] via FragCoord builtin 11 → IntBitsToFloat → Splat×Mul). ⛔ ROOT was a BINDING gap, not the asset: a 1-read fullscreen pass's per-pixel storage buffer was never cooked into the plan (build_fullscreen_ceir emitted the constants operand ONLY in the bindless branch) → never bound → StorageLoad read 0 → black. FIX (advisor Option B): PLAIN cook branch emits constants@0 + NEW draw_textured_storage verb (both backends) + command_lowering dispatch branch. lavapipe both arms 34/34 (STAGE 3 darkening + error_count==0); Win Vk+DX12 build clean, gates skip; tidy; scene_renderer.cpp UNTOUCHED; commit ⑫; scar one_read_fullscreen updated] → 19z-4 ✅2026-08-16 [BAND CLOSE — full-suite MATRIX GREEN on 5 legs (linux-gcc-debug + linux-gcc-asan + lavapipe + win-debug + win-asan), ZERO failures; the initial Windows red was a HARNESS run-env gap (CRD_ASSETS_DIR + shaderc + ASan-DLL PATH), fixed + re-run green — no 19-band defect. AUDIT: zero render-technique KGraph builders remain (wavefront/RT-gate builders = test-substrate; scene_renderer's 3 = pre-band cook-callbacks, c3ac005-verified). ROW-PER-CLAIM table + DEFERRAL LEDGER homed here: L1 rt.trace/rt.sbt_build lowering → CEIR-20 inbound; L2 bridge-EXEC-vs-runtime pixel gap, L3 depth-only representational equivalence, L4 bridge writes-loop KIND-unfiltered, L5 add_draws fullscreen-payload dead-code (migrated pass), L6 draw_textured_storage DX12 device-run owed, L7 TexSize serialization single-consumer → THIS 19z row; L8 the 19c NOT-YETs (multi-bounce, compact-N, frame-runtime routing, instanced AccelBuild, N>1 keys) → CEIR-19c row. FULL detail → docs/sessions/2026-08-16-ceir-19z-band-close.md. ⛔⛔ CEIR-19z ✅ + the CEIR-19 (RT / ceir.rt) BAND ✅ — every RT algorithm + rendering decision is an authored .ckir/.crdv asset; scene_renderer.cpp gained ZERO render-technique C++ across 19a-19z]. | §133 §134 |
| ✅ | CEIR-20 | `ceir.work` dynamic/device work → one semantic program on ≥2 lowerings: compute+indirect fallback FIRST, then Work Graphs/DGC/shader-enqueue/ICB/CUDA-graph · reuse: F16 amplification, 40-A indirect. ⛔ L1 RULED OUT of CEIR-20 scope (advisor, band-open 2026-08-16): `rt.trace` / `rt.sbt_build` lowering is SBT/pipeline plumbing on the RT contexts — nothing about work generation; folding it here recreates the "two unrelated NET-NEWs in one band" shape 19c avoided. Stays INBOUND-DEFERRED, homed to a future RT-EXECUTION band (not CEIR-20). <br>**• SUB-SLICES (advisor band-open 2026-08-16, the 19-band declare→author→device-gate→close mold):** **20a** DECLARE `ceir.work` (the 19a mold — work.ceirop.toml→opgen + work.hpp/cpp; opaque Externs work_queue<T>/work_record<T>; the SUBSET a consumer needs — produce/consume/compact/dispatch core, NOT all 12 §43 concepts; find_work_misuse at find_dispatch_misuse depth; ⛔ reconcile vs the `async` dialect in the doc header — device work-gen ≠ async.launch [device-side production vs host-side structured concurrency]; ⛔ the orchestration program is an AUTHORED CEIR-IR artifact, NEVER a C++ builder). **✅ 2026-08-16** — authored work.ceirop.toml (4 ops: queue_alloc/produce/consume/compact + 2 opaque Externs work.queue/work.record) → opgen → work.hpp/cpp (register_dialect + find_work_misuse, the rt.cpp mold; consume/compact have NO host grid — the INDIRECT device-count distinction) + test_work.cpp; async reconciliation in the dialect header (device work-gen ≠ async.launch); GATE GREEN 4 configs (linux-gcc-debug/asan + win-debug/asan, [work] 53/5 each) + opgen no-drift + tidy. ⛔ NO-FOLLOW: `work.record` is DECLARED-UNUSED (no op takes a %record today — the type-system companion for a future produce_one/consume_one; declared-forward, not an orphan). commit ⑭. → **20b** the FALLBACK lowering + AUTHORED consumer: re-express the §134 wavefront's host `while(count>0)` loop as a `ceir.work` program lowered to the generic queue-buffer + indirect fallback (REUSE wavefront_{compact,trace,shade}.ckir as-is; retire the 19c C++ host loop); gate = decision-hash IDENTICAL to the 19c host-loop triple32 oracle. ⛔ 20b DESIGN-LOCK SEAM (advisor, 20a close): decide (i) WHERE the ceir.work orchestration program LIVES as an authored asset (a .crdv-class asset / inline-IR-from-asset — never a C++ builder) + who EXECUTES it (a crd-ceir-gpu work-executor, the execute_rt_lowered caller-hook precedent), and (ii) the queue→BUFFER RESOLVER: `work.queue_alloc` is abstract, but the 19c wavefront queues are host-provisioned NAMED buffers the gate reads back via debug_scene_buffer — the fallback lowering needs a %queue→named-buffer resolver hook (the RtHooks precedent) so the gate can still inspect them. **⭐ 20b DESIGN-LOCK RESOLVED (advisor ×2, 2026-08-17) — Design Z (frame-executor):** (i) LIVES as an authored `.frame.toml` (BRANCH 1: `resolve_program_text` is kernel-only, but `parse_frame_toml`→`FrameGraphDesc`→`to_ceir_frame`→a **ceir.frame Module** IS the module-level authored→CEIR path — NOT `frame_template_bridge.cpp` which emits `rp::PassPayload` runtime payloads). Queues = `resource.declare` with the EXISTING `counter`/`indirect_args`/`structured` open attrs; stages = `frame.pass` naming NEW work executors (`work.produce`/`work.consume`/`work.compact` registered as ExecutorTypeIds — the `compute.dispatch`/`kExecComputeDispatch` precedent, frame_ceir.cpp:26; "never one-op-per-executor" NOT violated: the pass stays generic, executor names match the op names); kernel/access as OPEN attrs cooked vs the executor schema (15c). (ii) EXECUTED by `build_work_ceir` in ceir-gpu (COPY the `build_fullscreen_ceir` mold, render_fullscreen_build.hpp — the F2 file) materializing queue_alloc + produce/consume/compact ops → `lower_region` → LoweredCommand → NEW `execute_work_lowered` + `WorkHooks{resolve_queue, dispatch, trace_dispatch_indirect}` (the `execute_rt_lowered`/`RtHooks` caller-hook precedent; ceir-gpu names no backend); `resolve_queue`: %queue→caller-provisioned NAMED buffer (gate reads via `debug_scene_buffer`). **MAPPING (all 4 ops DEVICE-EXERCISED — cook-only-gates scar):** trace=`work.produce` (fixed=4 gx,gy,gz,queue; has_grid; bindings `rww`=rays r/hit_flag w/hit_t w; TLAS via hook), compact#1 & compact#2=`work.compact` (flags→compacted+count / cont_flags→next+count), shade=`work.consume` (INDIRECT over count; compacted→decisions/cont_flags; TLAS occluder); queue_alloc materialized from the declares. **INDIRECT (adopt advisor iv):** consume/compact SIZE the dispatch from the DEVICE count (the 20a semantics + the charter's "indirect fallback" — NOT deferred to 20c=DGC/Work-Graphs, else the dialect ships unexercised); termination stays host-read + submit+wait per stage (the line-2789 sync seam INTACT — only sizing moves to device); queue header `(count,1,1)` doubles as the indirect args ⇒ item offsets REBASE ⇒ matching authored-side edit in the reused kernels. `LoweredKind` NEEDS a NEW `DispatchIndirect` (closed-enum WIDEN: audit ALL 5 consumers `validate_lowered`/`execute_lowered`/`execute_rt_lowered`/`execute_render_lowered`/`render_materialize` → TYPED-reject; `execute_work_lowered` OWNS it). `compute.dispatch_indirect` verb EXISTS on both compute backends (vk_compute:227, dx12_compute:212); `RtHooks.TraceDispatchFn` is direct-grid ONLY ⇒ `WorkHooks` adds the INDIRECT trace twin (add to BOTH backends, RHI verb rule). **BUILD ORDER:** (1) frame schema (`parse_frame_toml`/validate/`emit_frame_toml` + a `pass_is_work` predicate) + `to_ceir_frame` work-pass mapping → (2) `build_work_ceir` → (3) `LoweredKind::DispatchIndirect` widen + 5-consumer audit → (4) `execute_work_lowered` + `WorkHooks` + per-backend wiring → (5) author `assets/frame/wavefront_work.frame.toml` (REUSE the 3 `.ckir`) → (6) GATE = 19c triple32 oracle + analytic pin `[0,1,0,1]` VERBATIM, same per-iteration hash points, Win Vk + lavapipe + DX12 twin (new execution path ⇒ real devices both backends). **✅ STEP 1 DONE 2026-08-17** — the frame-schema + `to_ceir_frame` work-pass mapping: 3 new ExecutorTypeIds `kExecWork{Produce,Consume,Compact}` (frame_asset.hpp) + kKindTable rows `work.produce`/`work.consume`/`work.compact` (frame_asset.cpp — makes pass_mechanic_from_kind/pass_kind_string/is_builtin_executor handle them) + `pass_is_work*` predicates + a kernel-required validation (MissingShader mirror) + `builtin_executor_name` entries (frame_ceir.cpp) → the executor SYMBOL is the op name (the compute.dispatch precedent). GATE GREEN (win-debug): 2 new `[ceir20b]` tests (the 15a-3 emit-identity round-trip over the 3 work mechanics + the .frame.toml parse→validate→to_ceir_frame-clean path, both find_frame_misuse-clean) + the FULL [framecook] suite 3070/99 ZERO regressions. work passes NOT wired into the live frame runtime (pass_is_migrated_ceir untouched — 20b's gate is cook→execute_work_lowered, the frame-runtime home is ledgered). NEXT step 2 = build_work_ceir. **✅ STEP 2 DONE 2026-08-17** — `build_work_ceir` (NEW engine/ceir-gpu/{include/crd/ceir/gpu/work_build.hpp,src/work_build.cpp}), the `build_fullscreen_ceir` MOLD: a `WorkBuildDesc` (queues[8] {capacity,record_stride,source_param} + stages[8] {kind,kernel,grid,queue,src_queue,bindings[8]{source_param,access}}) → register work/arith/resource on a fresh Context → create module+block → emit `work.queue_alloc` per queue (capacity/record_stride required attrs + a `source` OPEN attr = the resolve_queue identity) + a produce/consume/compact op per stage (produce: grid arith.const + %queue + binding resource.declares; consume: %queue + bindings, NO grid; compact: %src+%dst + bindings; kernel=attr_symbol, access=comma-joined {r|w|rw} tokens) → verify `find_work_misuse` (verifier-first, returns nullptr on dirty/malformed). GENERIC machinery (nothing wavefront-specific — the algorithm is the authored .frame.toml + .ckir). GATE GREEN (win-debug): 2 new `[ceir20b]` tests (tests/ceir-gpu/test_work_build.cpp — the wavefront mapping trace=produce/compact=compact/shade=consume emits find_work_misuse-clean + 3 malformed-desc rejections) + full ceir-gpu suite 640/43 ZERO regressions. NEW files → reconfigure-preset.bat + tests/ceir-gpu/CMakeLists.txt entry. NEXT step 3 = LoweredKind::DispatchIndirect widen + lower_region(work). **✅ STEP 3 DONE 2026-08-17** — `LoweredKind::DispatchIndirect` (appended at END, lower.hpp) + the 5-CONSUMER TYPED-REJECT AUDIT (the widen-enum scar): validate_lowered + execute_lowered add an explicit DispatchIndirect→UnsupportedCommand reject (execute.cpp — it would else fall through to check_dispatch since dispatch_kind defaults Direct); execute_rt_lowered/validate_rt_lowered's catch-all (line 205) already rejects it; render_materialize's EXHAUSTIVE switch gains the `case DispatchIndirect` (the ONLY exhaustive LoweredKind switch repo-wide — grep-verified + gcc-confirmed). `lower_region` maps the work ops: work.produce → `Dispatch` (const grid from operands 0-2, the direct-dispatch precedent); work.consume + work.compact → `DispatchIndirect` (device-count grid, resolved at execute); work.queue_alloc → nothing (continue — the resolver provisions at execute). `build_work_ceir` now LOWERS into an `out_plan` param (after find_work_misuse). GATE GREEN on 2 configs: win-debug + linux-gcc-debug ([ceir20b] 12/3 + full ceir-gpu 647/44) + gcc `-Werror=switch` CLEAN (the scar's specific check — MSVC hides switch gaps). 3 `[ceir20b]` tests (lowered-shape produce=Dispatch/compact+consume=DispatchIndirect + malformed rejection + the DispatchIndirect typed-reject by validate_lowered + validate_rt_lowered). NEXT step 4 = execute_work_lowered + WorkHooks. **🔄 STEP 4 PART 1 DONE 2026-08-17** (host-only) — `WorkHooks{kernel_bytes, dispatch, dispatch_indirect, user}` + `validate_work_lowered` + `execute_work_lowered` in execute.{hpp,cpp} (the execute_rt_lowered MIRROR; ceir-gpu names no backend). The GENERIC host driver: a `Dispatch` (produce) → the DIRECT hook (authored const grid); a `DispatchIndirect` (consume/compact) → the INDIRECT hook over the %queue (operand 0) — the DEVICE reads the count (vkCmdDispatchIndirect over the (count,1,1) header), the host NEVER sizes it; bindings (operands after the fixed prefix produce4/consume1/compact2) resolve to host spans (the RtHostBinding model). ⛔⛔ SELF-CAUGHT DIVERGENCE (advisor design-lock re-checked mid-build): the first cut host-read the count via a `resolve_queue` hook → grid=count → a DIRECT dispatch — EXACTLY the "host-read count + direct dispatch" the advisor RULED WRONG (it makes the "indirect fallback" claim false + adds a redundant host round-trip). CORRECTED to the two-hook device-indirect design BEFORE the backend wiring baked it in; host-read is TERMINATION-only (multi-bounce, declared-forward). The caller's lambda binds a %tlas when work_op traces (produce=trace/consume=shade — the RtHooks single-hook precedent). ExecuteError widened +2 (UnresolvedQueue/WorkDispatchFailed). ⛔⛔ SECOND SELF-CAUGHT DIVERGENCE + ADVISOR RECONCILE (2026-08-17): part-1 mirrored execute_rt_lowered's `RtHostBinding` HOST-SPAN model (bindings uploaded/read-back per dispatch) — but that ROUND-TRIPS the queue count through host, which IS the 19c host-driven loop 20b retires (device-generated-work theater; hands 20c nothing). Advisor confirmed DEVICE-RESIDENT named buffers. REFACTORED: `WorkBufferHandle=u64` (the RtSceneHandle precedent) + `WorkResolvedBinding{Value* resource, WorkBufferHandle buffer}` (renamed from WorkBinding — COLLIDED with work_build.hpp's desc WorkBinding); the executor RESOLVES descriptors → handles (hooks CONSUME handles, no per-backend operand-walking); ⛔ the QUEUE is a DESCRIPTOR the producer writes (not skipped) — the grid-prefix skips ONLY the launch grid (produce 3, consume/compact 0); the indirect queue handle = the first descriptor. `build_work_ceir` gains a `WorkAssetResources` out-param (queue+binding Values ↔ source_params — the CeirDispatchAsset precedent) so the caller builds the table. GATE GREEN win-debug + linux-gcc-debug: 5 `[ceir20b]` tests (executor routes produce=direct/consume+compact=indirect + resolves descriptors→handles, the queue handle IS the device-count buffer; validate rejects foreign kind + unresolved kernel) + full ceir-gpu 666/46 + gcc `-Werror=switch` CLEAN. **PART 2 (next tick)** = wire both backends: WorkHooks lambdas over a COMPUTE context (the smoke needs no TLAS) — `dispatch_indirect` does vkCmdDispatchIndirect / ExecuteIndirect reading the queue's (count,1,1) header (the verb EXISTS both backends: vk_compute:227/dx12_compute:212, args-buffer+offset) + 2 AUTHORED `.ckir` smoke kernels (assets/ckir/work_smoke_{produce,consume}.ckir; header-write + gid-marker) + a MINIMAL on-device work smoke (queue_alloc→produce→consume; produce writes the device count, indirect consume reads it device-side) on Win Vk + lavapipe + DX12. **🔄 PART-2 PROGRESS 2026-08-17:** ✅ the 2 AUTHORED `.ckir` smoke kernels (work_smoke_produce writes queue[0]=5; work_smoke_consume `atomicAdd(out[0],1)`/invocation → out[0]==count proves device-count sizing; cvals right 5.0=0x4014…/1.0=0x3ff…). ✅ the DEVICE SYNC MODEL (advisor ×3 — the device-resident model made barriers NON-inert, unlike the 19c host-span mirror): SINGLE-submit + REPLAYED barriers (the 6-dispatch FFT precedent; per-stage submits return ONLY for multi-bounce host-decision termination, ledgered). `WorkBarrierFn` hook added (nullable=inert); execute_work_lowered REPLAYS Barrier commands via `hazard_access` (nullptr resource ⇒ every table buffer) + the executor-owned `(queue, ShaderWrite→IndirectRead)` before each indirect dispatch (the C5 `ComputeAccess::IndirectRead` — the one hazard the plan can't express). Hooks RECORD into a caller-owned recorder. GREEN host-only win-debug + linux-gcc-debug: [ceir20b] 33/5 (num_ind_read==2 + num_barriers≥2) + ceir-gpu 668/46. ✅ **VULKAN DEVICE SMOKE GREEN — Win Vulkan + lavapipe** (tests/ceir-gpu-vulkan/test_work_smoke_vulkan.cpp, NEW; the test_ceir_add mold): the AUTHORED work_smoke_{produce,consume}.ckir compile (ckir_read→GLSL→SPIR-V — the .ckir format VALIDATED on device), a STORAGE|INDIRECT queue buffer, WorkHooks over VulkanComputeContext (u64=ComputeBuffer*; dispatch/dispatch_indirect RECORD; barrier→rec.barrier), ONE submit; execute_work_lowered ran on a REAL device → **out[0]==5 (consume ran N=device-count invocations — the indirect grid was DEVICE-sized, never host), out[1]==5 (queue[0] read), queue[0]==5 (produce wrote the device count)** — the migrated-executor scar's device leg, both Vulkan ICDs (20 assertions each). produce writes the FULL (5,1,1) header (the C5 barrier covers the whole indirect args). ✅ **DX12 TWIN GREEN — Win DX12** (tests/ceir-gpu-dx12/test_work_smoke_dx12.cpp — the Vulkan mirror, emit_compute_kernel_hlsl + create_pipeline_from_hlsl; the .ckir atomicAdd→InterlockedAdd + BufferStore→UAV emit cleanly for DXIL; out[0]==5/out[1]==5/queue[0]==5, 17 assertions). The DX12 `IndirectRead`→D3D12_RESOURCE_STATE_INDIRECT_ARGUMENT mapping + the ExecuteIndirect dispatch signature ALREADY existed (dx12_compute:36/212 — verified, no fix). ✅✅ **STEP 4 DONE — execute_work_lowered PROVEN ON-DEVICE, BOTH BACKENDS** (Win Vk + lavapipe + DX12): the ceir.work FALLBACK executor runs an AUTHORED device-generated-work program end-to-end — produce writes the device count, an INDIRECT consume is device-count-sized, the single-submit + replayed-barrier + C5 IndirectRead sync model holds on all 3 devices. NEXT = STEP 5 (the authored wavefront_work.frame.toml). **⭐ STEP 5 DESIGN-LOCK (advisor, 2026-08-17) — the scope tension resolved, step 5 SIMPLIFIED:** (0) ⛔ CUT device-resident RT from 20b — the RT stages (trace/shade) KEEP the 19c HOST-SPAN trace_dispatch path; device-indirect was proven on the COMPUTE surface (step 4); the RT device-resident + indirect-ray-query capability is LEDGERED (a future RT-execution band, next to rt.trace lowering). ⛔ HONEST SCOREBOARD: "device-indirect proven on compute (step 4); RT stages host-span in 20b, ledgered" — NEVER "the wavefront is device-indirect". (i) THE COOK = `extract_work_desc(const FrameGraphDesc&, WorkBuildDesc&)` in FRAME-COOK (it may name ceir-gpu types — frame-cook already links crd-ceir-gpu, the build_fullscreen precedent): walk the PARSED desc.passes (NOT the ceir.frame Module — avoids re-parsing symbol attrs); pass_is_work_*→WorkStageKind, pass_str(kKernel)→kernel, reads/writes→resources. ⛔ QUEUE-vs-BINDING BY RESOURCE KIND: a referenced CounterBuffer = a queue (20a's declared authored intent), else a binding; 0 or ≥2 CounterBuffers per work pass = a NAMED cook error. compact src=the counter READ, dst=the counter WRITTEN (access distinguishes). (ii) NO new RT verb — the wavefront WorkHooks wrap the 19c trace_dispatch (submit+wait inside) for trace/shade + the compute context for compacts. (iii) PER-STAGE submits (RT + compute = different contexts = separate command buffers → single-submit impossible; WorkBarrierFn=nullptr, coherence rides the host round-trip like 19c — the compute smoke keeps single-submit covered; both models now have a proven consumer). ⛔ NO kernel edits — the 19c layouts (flags/compacted/count SEPARATE buffers) reuse VERBATIM; the (count,1,1) header rebase was only the compute surface. ⛔ LOWERING FIX (folded in): the 19c compact is SERIAL (grid 1,1,1 direct) → `work.compact` lowers to `Dispatch`, only `work.consume` → `DispatchIndirect` (routes to the hook, which HOST-READS the count for the RT wavefront). RESOURCE LIST (from the 19c binding tables): rays, hit_flags, hit_t, compacted, count=CounterBuffer, decisions, cont_flags, next_queue, next_count=CounterBuffer. ⭐⭐ REFINED MAPPING (advisor, impedance reconcile — the 19c per-buffer layout ≠ 20a's queue-with-embedded-count, so the honest map is): **trace = `compute.dispatch`** (NOT produce — a plain grid dispatch, no queue; the existing kExecComputeDispatch executor); **compact#1 = PRODUCE** (scans hit_flags → materializes compacted + a device-written `count` — that IS 20a produce; %queue = the CounterBuffer `count` WRITTEN, bindings = hit_flags r + compacted w); **shade = CONSUME** (%queue = `count` READ [the indirect source], bindings = compacted/rays/hit_t r + decisions w); **compact#2 = PRODUCE** (cont_flags → next_queue + `next_count`). ⛔ `work.compact` is UNEXERCISED by the wavefront — stays lower/execute-tested host-side; device consumer = the 20c parallel-compaction path (LEDGERED, the work.record precedent — do NOT relabel the serial scan as compact to fake coverage, the GOLD rule bans the disguise). ⛔ COUNTER RULE (refined): produce = exactly 1 CounterBuffer WRITTEN; consume = exactly 1 READ; else = named cook error (compact's 2-counter rule ships with a real compact consumer). ⛔ OPERAND-ORDER vs SSBO-SLOT dissolves at the HOOK: the gate's hook orders descriptors by a FIXED per-kernel binding table (keyed off the kernel symbol, mirroring the .ckir binding-contract comments), NOT the operand order — extract_work_desc guarantees IDENTITY (source_param→buffer), not order. ⛔ MIXED frame (1 compute pass + 3 work passes) — extract_work_desc TOLERATES/skips non-work passes; the gate runs trace via the compute path it already has. ⛔ SCOREBOARD: "the wavefront exercises produce + consume on-device; work.compact is lower/execute-tested host-side, device consumer = 20c parallel compaction (ledgered)". ✅ LOWERING FIX DONE (work.compact→Dispatch serial; only work.consume→DispatchIndirect; [ceir20b] 33/5 + ceir-gpu 668/46 both configs + Vulkan device re-green) — the wavefront no longer leans on it (compact#1/#2 are PRODUCE = const-grid Dispatch already). GATE (step 6) = authored .frame.toml → parse → extract_work_desc → build_work_ceir → execute_work_lowered w/ 19c-style hooks → per-iteration triple32 over {hit_count,compacted[],decisions[],next_count} == oracle + [0,1,0,1] pin, 3 devices; advisor at the gate. AUTHOR ORDER: extractor + cook-error negatives FIRST (host-only both configs), then the asset, then the gate. **✅ EXTRACTOR DONE 2026-08-17** — `extract_work_desc(FrameGraphDesc&, WorkBuildDesc&, where)` (NEW engine/frame-cook/{include/crd/framecook/frame_work.hpp,src/frame_work.cpp}): walks desc.passes, SKIPS non-work (mixed frame), maps pass_is_work_*→WorkStageKind + pass_str(kKernel)→kernel; CounterBuffer (by FrameResourceDesc.kind) = the %queue (deduped by name-hash: `count` written by produce + read by consume → ONE queue), else a WorkBinding (source_param=name-hash, access from r/w); refined counter rule (produce=1 written / consume=1 read / compact=1+1) → `FrameCookError::WorkQueueNotOne` (NEW code + error_text). GATE GREEN win-debug + linux-gcc-debug: 2 `[ceir20b]` frame-cook tests (the produce+consume cook → build_work_ceir find_work_misuse-clean [the extractor→builder chain end-to-end] + the 0/≥2-counter cook-error negatives) + full [framecook] 3083/101 + gcc `-Werror=switch` clean. tests/frame-cook links crd-ceir + crd-ceir-gpu (for build_work_ceir). **✅ ASSET DONE 2026-08-17** — assets/frame/wavefront_work.frame.toml AUTHORED (MIXED: trace=`compute` [wavefront_trace — the ray_query TLAS is a runtime capability the executor binds, NOT an acceleration-structure resource, so no RayTraceNeedsAccel]; compact1/compact2=`work.produce` [count/next_count=counter_buffer queues]; shade=`work.consume`; rays+cont_flags=external_buffer [host-owned inputs, exempt from ResourceNeverWritten], the rest structured/counter; REUSE wavefront_{trace,compact,shade}.ckir VERBATIM). ⛔ @OUTPUT RELAXATION (frame_asset.cpp validate): a HEADLESS frame (no raster passes — only work/compute/raytrace/transfer) needs NO @output (it produces device buffers, not a swapchain image) — the wavefront_work case; a rasterising frame still requires it. GATE GREEN win-debug + linux-gcc-debug: a `[ceir20b]` test PARSES the SHIPPED wavefront_work.frame.toml (headless) → extract_work_desc → 3 work stages (produce/consume/produce; trace=compute skipped) + 2 queues (count/next_count) → build_work_ceir find_work_misuse-clean (the cook→IR chain end-to-end — the mandate-#1 proof at the COOK layer) + full [framecook] 3096/102. **✅ STEP 6 DONE — THE DEVICE GATE (advisor at the gate, verdict "real and stands"): the AUTHORED wavefront_work.frame.toml DRIVES the device wavefront, BOTH BACKENDS.** New `[ceir20b]` TEST_CASE in tests/gpu-context-vulkan/test_vulkan_rt.cpp (+ the DX12 twin in tests/gpu-context-dx12/test_dx12_rt.cpp — 19c HAS a DX12 RT wavefront gate [test 4562], so the both-backends mandate applies, NOT Vulkan-only): PARSE the SHIPPED asset → `extract_work_desc` → `build_work_ceir` → the plan; DRIVE trace FIRST (a plain `rt_gate_trace_dispatch`, real TLAS), THEN `execute_work_lowered` over the 3 work stages, its `WorkHooks` WRAPPING the 19c host-span trace_dispatch (produce=compact = a const-grid `dispatch` hook; consume=shade = the `dispatch_indirect` hook; barrier=nullptr = per-stage submit+wait). The hooks reorder descriptor→SSBO by a FIXED per-kernel binding table (writes-then-reads → the kernel's slots; `count`'s handle carries the queue's host storage). Decision-hash (triple32 over {hit_count,compacted[],decisions[],next_count}) == the 19c oracle + the ANALYTIC pin `[0,1,0,1]` + `hit_count==4` + `shade_gx==hit_count` (the count-driven pin) — the retired C++ `while(count>0)` loop's behaviour, IDENTICAL, now READ FROM THE ASSET. **GREEN: Win Vulkan (RT-capable) + Win DX12 (DXR-1.1), 53 assertions each; both TUs clang-tidy-20 clean (`s.data()[i]`→`s[i]` simplify-subscript fix).** ⛔⛔ SELF-CAUGHT AT THE GATE: `wf_kernel_bytes` first routed on `op_name=="consume"`, but `op_name` is DIALECT-QUALIFIED (`"work.consume"` — execute.cpp compares the full name); the mismatch dispatched the COMPACT kernel for shade → the zero-initialized decisions readback made 2 of 4 slots (the SHADOWED ones) COINCIDENTALLY pass, a *plausible* partial-pass, not garbage (memory scar written). ⛔⛔⛔ HONEST SCOREBOARD (advisor item 5, carried verbatim): (a) the consume dispatch GRID is HOST-READ from the produce-written count in this FALLBACK (submit+wait per stage) — NEVER "device-indirect"; device-indirect sizing was proven on the COMPUTE surface (step 4); the RT stages host-span in 20b (the trace/shade trace_dispatch), ledgered to a future RT-execution band. (b) the asset drives the WORK-STAGE SEQUENCE (which kernel, which buffers, what order); the trace pass is executed by the HARNESS at the asset-declared position — a frame-level executor that runs compute+work passes uniformly inside a LIVE frame does NOT exist yet (ledgered). ⛔ 20b LOCAL close: Win Vk + DX12 device gates GREEN + tidy clean; **linux-gcc-debug WSL sweep DONE — gcc `-Werror` compiled the whole tree (incl. the Vulkan gate TU — the mandatory leg the advisor flagged) + the [ceir20b] gate ran/skipped green on lavapipe (not among the failures).** ⛔ THE SWEEP EARNED ITS KEEP: it caught a REAL regression MSVC's filtered `run-ctest -R` hid — 3 prior-tick 20b TEST_CASE names had non-ASCII (`→`/`—` → `crd-no-non-ascii-test-names` FAIL, tests/ceir-gpu/test_work_build.cpp + test_work_smoke_{vulkan,dx12}.cpp); FIXED (`->`/`--`), check re-GREEN, full [ceir20b] 14/14 win-debug re-green. The other 5 sweep failures are PRE-EXISTING/ENVIRONMENTAL, NOT 20b (verified): `crd-no-malloc-allocator` is red on HEAD (44 MallocAllocator refs in the committed tests/ceir/test_plan.cpp alone; the whole ceir-gpu test suite incl. the test_ceir_add_vulkan MOLD violates it — a repo-wide policy-debt slice, whole-repo=CI's job); `crd-no-std-transcendental-check` = 0 real hits standalone / flags scene-render (20b touches ZERO scene-render); 3 `dsp spectral` SEGFAULTs = the documented llvmpipe multi-thread-pressure flake (hesap-dsp, 20b-unrelated). commit ⑮. **✅ CEIR-20b DONE — the §134 wavefront's orchestration is now an AUTHORED asset (mandate #1); the 19c C++ host loop is retired.** NEXT band-review PAUSE for the human, then 20c (ONE modern device lowering). → **20c** ⛔ LEDGER (step 6): the RT indirect-TRACE verb takes NAMED device buffers + indirect args (NOT host arrays); the 19c host-span trace_dispatch stays for the 19c test only; step 6 mixes RT-trace + compute-compact so single-submit may not survive surface mixing — per-stage submits return there WITH explicit visibility barriers (not bare fences). ⛔ DX12: verify its barrier maps IndirectRead→D3D12_RESOURCE_STATE_INDIRECT_ARGUMENT (verify, don't inherit). **LEDGER:** frame-RUNTIME integration (work passes inside a LIVE frame) NOT required for 20b's charter (gate = parse→cook→`execute_work_lowered`, the 19c gate shape) → homed here. BRANCH 2 (a generic CEIR-module TEXT form / "20b-0") does NOT fire — `.frame.toml` already owns the parser/validator/round-trip-identity/diagnostics; a module text IR is a FUTURE universality slice, not 20b. → **20c** ONE modern device lowering (§135 "at least one"): DGC (VK_NV_device_generated_commands — Windows-NVIDIA-only + compile-verified elsewhere per lavapipe's NV-ext skip; a 19z-1-style per-leg honesty note) OR D3D12 Work Graphs (needs a NEW `supports_work_graphs()` cap query — 20c's first stone); ~~Work-Graphs-AND-DGC = scope creep, pick ONE~~ ⛔ SUPERSEDED 2026-08-17 (user directive): do BOTH device lowerings + KEEP the fallback = **one program, THREE lowerings**. **✅✅ 20c-1 (D3D12 Work Graphs, SM 6.8) DONE 2026-08-17** — cook `emit_work_graph_{node,library}_hlsl` (.ckir→node HLSL, union-deduped decls, mandate #1) → `compile_work_graph_library_to_dxil` (lib_6_8) → `Dx12WorkGraphContext` state-object + WORK_GRAPH subobject + `DispatchGraph` (system d3d12.h/OS runtime, NO Agility SDK) → out[0]==count==5 on the 4070 Ti; the PRODUCER writes the (count,1,1) header + emits a grid-launch RECORD, the GPU SELF-SCHEDULES the consumer sized by it (removes 20b's host submit boundary); MODULE-DRIVEN via `build_work_graph_plan` (the SAME WorkBuildDesc the 20b fallback consumes); isolated test target copies the WG-capable Windows-SDK dxc next to its exe. commit ⑯. **✅✅ 20c-2 (VK_EXT_device_generated_commands, CROSS-VENDOR) DONE 2026-08-17 (advisor at the gate)** — **(a)** VulkanGpuContext `m_dgc_ext`/`device_generated_commands_ext()` (mirrors NV `m_dgc`): VK_EXT_device_generated_commands + VK_KHR_maintenance5 (the preprocess buffer's 64-bit usage flag) + BDA, DGC-EXT PROPERTIES require COMPUTE in supportedIndirectCommandsShaderStages. **(b)** `VulkanDgcContext` rig (engine/gpu-context-vulkan, pimpl, the RT-context mold): lone DISPATCH-token indirect commands layout, `indirectExecutionSet=VK_NULL_HANDLE` + consume pipeline via `VkGeneratedCommandsPipelineInfoEXT` in pNext (spec-MINIMAL — INDIRECT_BINDABLE is execution-set-ONLY, validusage.json-confirmed ⇒ PLAIN pipeline, no metadata machinery), `VkBufferUsageFlags2` preprocess buffer (maintenance5), LEGACY `COMMAND_PREPROCESS` barrier (sync2 off headless), `vkCmdExecuteGeneratedCommandsEXT`. **(c)** AUTHORED `assets/ckir/work_smoke_produce_dgc.ckir` (mandate #1): token@0 = five (1,1,1) VkDispatchIndirectCommand payloads + count@1[0]=5; REUSE `work_smoke_consume.ckir` VERBATIM (count doubles as its queue@0). **(d)** device gate (tests/ceir-gpu-vulkan/test_vulkan_dgc.cpp) MODULE-DRIVEN via `build_work_graph_plan` → **out[0]==5 (FIVE device-generated dispatch commands each ran), out[1]==5, count_rb[0]==5, token_rb[0]==1 on the 4070 Ti.** ⛔⛔⛔ SCOREBOARD: **20b = host-recorded dispatch + DEVICE-supplied grid** · **20c-1 = GPU-SCHEDULED node launch** · **20c-2 = DEVICE-GENERATED command STREAM** (the produce writes the sequence COUNT + every dispatch PAYLOAD; the host records ONLY ExecuteGeneratedCommands — a single dispatch-indirect cannot express N distinct commands). ⛔ SCAR: [[feedback_ckir_node_refs_are_positional_n_index_id_ignored]] (the first produce_dgc.ckir used ids v1/i0..i14 → ckir_read rejected; refs resolve by POSITIONAL n<pool-index>, id IGNORED → renamed n0..n18). ⛔ ADVISOR AT THE GATE caught a source-vs-scoreboard mismatch: the "module-driven" claim was DECORATIVE (plan computed-then-dropped) → now the plan's kernel symbols select the SPIR-V (the 20c-1 is_prod resolution mirrored). Blast radius (RT-1 inline rayQuery Vulkan + 20b ceir.work Vulkan executor — shared device-creation/BDA/feature chain) + per-slice clang-tidy-20 (5 files) + linux-gcc-debug gcc `-Werror` (WHOLE tree incl. the DGC rig + gate TU, exit 0) ALL GREEN. commit ⑰. **✅✅✅ CEIR-20c COMPLETE — one program, THREE device lowerings; NEXT = 20z band close.** → **20d** §44 device-side control flow (IF/SWITCH/WHILE/producer-consumer) — ONLY if 20c makes it cheap; PRE-AUTHORIZED to defer to the ledger. → **20z** band close (the 19z discipline verbatim). FIRST = 20a (task #38). **✅✅✅ CEIR-20 BAND CLOSED 2026-08-17 (advisor design-locked) — one authored `ceir.work` program, THREE device lowerings (§135 "at least one modern lowering" EXCEEDED: two modern + the portable fallback).** MATRIX (band-scope × 3 legs, re-run at close, never inherited): **win-debug** 20/20 band + 2/2 NV blast-radius, 0 failed · **linux-gcc-debug (lavapipe)** whole tree gcc `-Werror` compile-clean + CEIR-20 band + blast-radius green (sole 3 reds = the pre-existing `dsp spectral` llvmpipe flake, unrelated) — ⭐ the VK_EXT_DGC gate RAN the full 14-assertion path on Mesa lavapipe (a genuine SECOND-VENDOR device proof) · **win-asan** the 2 manual-readback-memcpy device gates (DGC #4582 + WG #4527) clean, ZERO AddressSanitizer errors (bounded to the new memory-handling code; whole-band×asan = CI). ZERO-BUILDER AUDIT clean (no ceir.work builder in scene_renderer; C++ = cooker [`build_work_ceir`/`extract_work_desc`/`emit_work_graph_*`/`build_work_graph_plan`] + device mechanism [`Dx12WorkGraphContext`/`VulkanDgcContext`] only). DEFERRAL LEDGER (7 items, each homed): L1 20d §44 device control-flow (pre-auth) · L2 work.record payload flow · L3 ⛔ work.compact DEVICE consumer RE-HOMED (the 20b ledger homed it to "20c parallel-compaction" but 20c did NOT discharge it — both 20c smokes are produce+consume only → future parallel-compaction band) · L4 RT device-resident/indirect trace (RT stages host-span) · L5 live-frame work-pass executor · L6 build_work_graph_plan single-producer · L7 work_graph.hpp naming (not renamed — cosmetic). ⭐ CLOSE-HYGIENE (advisor-at-the-gate): a NEW device-free `work_smoke_*.ckir` load gate (#3961, tests/kir/test_ckir_asset.cpp — the 3 assets were format-validated ONLY inside cap-skipping device gates; now parse + round-trip byte-exact + emit GLSL with no device) GREEN win-debug + linux-gcc-debug (gcc `-Werror`) + tidy-20; the ledger count corrected 6→7 (L7 is a row). AUTHORITATIVE close record: `docs/sessions/2026-08-17-ceir-20z-band-close.md`. commit ⑱. | build §43 §44; proof §135 |
| ✅ | CEIR-21 | `ceir.tensor`/`ceir.shape` high-level tensor IR → chained tensor workflow, no premature dispatch lowering · reuse: hesap-tensor dtypes/layouts, v14 I/O. <br>**• BAND-OPEN 2026-08-17 (advisor design-locked; §21/§22/§35/§51/§70 + engine/hesap-tensor/dtypes.hpp + engine/hesap-dense/layout.hpp READ):** the value-op DIALECTS over the 3d tensor TYPES (Dim/Shape/Tensor/SparseTensor already declared); crd-ceir-only, the 12a/20a declare mold (Pure + typed NoSemantics; ⛔ NO kernel_ref / NO lowering hook — §70 wants a NATIVE graph backend ingesting the graph WHOLE, never flatten-to-100-CKIR-dispatches). **SUB-SLICES:** **21a** `ceir.shape` (§35: shape-value construct / rank / extent + `shape.broadcast`/`shape.reshape_compat` wrapping the 3d `shapes_broadcast`/`shapes_reshape` predicates + `shape.assert` = the 3d-promised 'Unknown = a CEIR-21 runtime check' DISCHARGE, TOML-documented semantics) + `find_shape_misuse`. **21b** `ceir.tensor` (§51 STRUCTURAL SIX: elementwise[generic + closed-enum `fn`]/broadcast/reshape/transpose[`perm`=true permutation of rank]/reduce[`axis` bounds-checked]/matmul[contraction-dim]) + shape-aware `find_tensor_misuse`; element = the 3a scalar kinds; ⛔ NO `tensor.constant` (blob serialization = a whole design; block args feed the gate). **21c** §22 layout = an OPTIONAL constraint ('compiler/provider chooses physical layout where possible'; reuse hesap-dense RowMajor/ColMajor + §22 blocked/tiled/strided/AoS-SoA/swizzle; mechanism [appended TypeKind vs attr] locked at 21c — 3d append-after-View = no version bump either way). **21z** the COMPOSING gate (12z mold) = the charter's chained tensor workflow (§70): ONE C++-built module (input block-args → elementwise → reshape → transpose → matmul → reduce) verifies clean + text AND binary round-trip (5d). **VERIFIER contract:** Incompatible → misuse w/ the 3z pointing position; Unknown → ACCEPT (the deferral; `shape.assert` discharges); Compatible → accept + result-type check; `perm`/`axis` declared-words-scar validated; attr valid-checked reader; check order contractual. **⛔ BOUNDARY STRIKES (advisor, §22-decided):** sparse ENCODING + quantized/fp8 dtypes → **CEIR-23** (ship WITH `ceir.sparse`/`ceir.quant` that consume them — no orphan) → the CEIR-3d row's "sparse ENCODING is CEIR-21" clause STRUCK IN PLACE; FFT/solve → CEIR-22, gather/scatter/scan/sort/conv → named-forward rows. Advisor at each sub-slice gate; 60s cadence. **✅ 21a DONE 2026-08-18** — `ceir.shape` DECLARED: `engine/ceir/ops/shape.ceirop.toml` (6 PURE value-ops make/rank/extent/broadcast/reshape/assert — typed NoSemantics, ⛔ NO kernel_ref, §70) → opgen → `shape.hpp`/`shape.cpp` with `find_shape_misuse` (14 `ShapeMisuseKind`: operand/result KIND checks + RESULT-IDENTITY checks [make: members==operand dims; extent: result==member[axis]; reshape: result==target; assert: result==lhs — the 12a `underlying==operand` precedent, advisor-caught] + the 3d predicates WRAPPED [Incompatible→a POINTING misuse w/ the 3z right-aligned position, Unknown→ACCEPT, Compatible→accept] + extent `axis` bounds + assert `relation` closed vocab). `tests/ceir/test_shape.cpp` (3 cases: a well-formed 6-op module + 13 malformed [exact kind each] + the ⭐ Unknown→ACCEPT tri-state discharged by `shape.assert`). ⛔ broadcast RESULT-EXACTNESS name-forwarded to 21b (`shapes_broadcast_result`, its `ceir.tensor` elementwise consumer — the producer 3d lacked). **GATE ×4 configs GREEN** (win-debug/asan + linux-gcc-debug/asan: `[shape]` 3/3 + gen-smoke 3/3 each) + opgen drift/validator + gcc `-Werror=switch` (the 14-case name switch) + tidy-20 (3 files) + `crd-ceir-invariants` — RE-RUN after the advisor-at-gate fixes (never inherited). ⛔ advisor-at-the-gate caught + I fixed 4: the owed 4th config, the result-identity gap (KINDS not IDENTITIES), the missing Unknown-branch test, the un-run gen-smoke. NEXT = 21b. commit ⑲. **✅ 21b DONE 2026-08-18** — `ceir.tensor` DECLARED: `tensor.ceirop.toml` (the STRUCTURAL SIX PURE ops — elementwise[`fn`]/broadcast/reshape/transpose[`perm`]/reduce[`axis`,`fn`]/matmul; typed NoSemantics, NO kernel_ref; the target shape RIDES THE RESULT Tensor type; element = a 3a scalar kind; NO `tensor.constant`; ⛔ FFT/solve→CEIR-22, gather/scatter/scan/sort/conv→named rows, sparse/quant→CEIR-23) → opgen → `tensor.hpp`/`tensor.cpp` with the shape-aware `find_tensor_misuse` (16 `TensorMisuseKind`) + NEW `Context::shapes_broadcast_result` (the 3d-lacked numpy-broadcast RESULT producer, 21a name-forward — NON-const, interns). CHECK ORDER (contractual): Tensor-kind → element-consistency [members[0]] → per-op shape — elementwise/broadcast via `shapes_broadcast` + `shapes_broadcast_result` EXACTNESS [Incompatible→3z pointing, Unknown→ACCEPT, Compatible→exact]; reshape via `shapes_reshape`; transpose PERM parse-as-true-permutation + `permute_shape` identity; reduce AXIS-bounds + `drop_axis_shape` identity; matmul rank≥2 → K CONTRACTION → BATCH (right-aligned) → `matmul_shape` identity. `test_tensor.cpp` (well-formed six-op + 18 malformed [exact kind each] + Unknown-ACCEPT). **GATE ×4 configs GREEN** (win-debug/asan + linux-gcc-debug/asan: `[tensor]` 3/3 + gen-smoke 3/3 each) + opgen drift/validator + gcc `-Werror=switch` (16-case) + tidy-20 (5 files) + `crd-ceir-invariants` — RE-RUN after the advisor-at-gate fixes (never inherited). ⛔ advisor-at-the-gate caught + I fixed 3 blockers: (a) `tensor.broadcast` had ZERO negative/Unknown coverage; (b) matmul BATCH-dim mismatch FALSE-GREENED (`matmul_shape` copied lhs's batch) → NEW `BatchMismatch` kind + an explicit right-aligned batch check; (c) elementwise/matmul read `operand(1)` unguarded → a per-op min-arity FOLD guard. ⛔ NOTES (no-silent-caps): `shapes_broadcast_result` exactness is DEFERRED above rank 16 (a bounded cap; no real tensor exceeds it); reduce-to-rank-0 (`!shape<>`) is a 21z-chain case; element-PROMOTION is name-forwarded (TOML). NEXT = 21c (§22 layout). commit ⑳. **✅ 21c DONE 2026-08-18** — `ceir.layout` DECLARED (§22): `layout.ceirop.toml` (ONE Pure op `layout.constrain(%tensor) {kind, …params} -> %tensor` — an OPTIONAL data-layout CONSTRAINT). ⛔ MECHANISM (advisor-locked): a CONSTRAINT OP, NOT a tensor-type member — an optional 3rd Layout member would make tensor<f32,4x3> and tensor<f32,4x3,row_major> DISTINCT TypeIds, reopening every 21b result-identity check + rippling into type_is_canonical / the binary decoder / the grammar / the fuzz corpus (a type-system slice). §23 `tensor<Shape,Element,Layout>` (layout-in-type) = a **Dxxx DECLARED DIVERGENCE** named-forward to the first type-IDENTITY consumer (a §150 provider / a CEIR-22 interop boundary). `kind` closed vocab {row_major,col_major,strided,blocked,aos,soa,swizzle} — `tiled` FOLDED into `blocked` (§22 lists 'blocked/tiled' as ONE item); sparse/packed-quantized EXCLUDED→CEIR-23; canonicalizer-non-fold→CEIR-6. → opgen → `layout.hpp`/`layout.cpp` with `find_layout_misuse` (8 `LayoutMisuseKind`: Tensor-kind, PASSTHROUGH type identity [result==input], `kind` vocab, KIND-GATED params [ParamKindMismatch — the 12a nonsense-by-construction], strides/block arity vs the tensor RANK, block extents ≥1) + a min-arity FOLD guard. `test_layout.cpp` (well-formed per kind incl. a RANK-3 strided [arity-follows-rank, not ==2] + 9 malformed [exact kind each] + the arity-0 fold). **GATE ×4 configs GREEN** (win-debug/asan + linux-gcc-debug/asan: `[layout]` 2/2 + gen-smoke 3/3 each) + opgen drift/validator + gcc `-Werror=switch` (8-case) + tidy-20 (3 files) + `crd-ceir-invariants` — RE-RUN after the advisor-at-gate fixes. ⛔ advisor-at-the-gate caught + I fixed: the arity-0-constrain param-skip ROBUSTNESS LEAK (min-arity fold guard), the rank≠2 test gap (rank-3 pass + arity-fail), the §22 blocked/tiled one-item citation. NEXT = 21z. commit ㉑. **✅ 21z DONE + CEIR-21 BAND CLOSED 2026-08-18** — `tests/ceir/test_band21_gate.cpp` the COMPOSING gate (12z mold): ONE C++-built module threads all THREE dialects — `shape.make`/`shape.rank` alongside a chained rank-1 row tensor → `tensor.broadcast` UP → `layout.constrain{blocked}` → `elementwise{mul}` → `transpose{1,0}` → `matmul` → `reshape` → `reduce{axis=0,sum}`-to-RANK-0 (§21 `!shape<>`, the 21b-deferred case: `drop_axis_shape`→`type_shape({})` interns consistently so the reduce result-identity matches). BAND PROPERTY PROVEN: (1) `find_shape_misuse` + `find_tensor_misuse` + `find_layout_misuse` + `find_structure_error` ALL None on the ONE module (the verifiers COMPOSE — no walk falsely flags another dialect's ops); (2) ⭐ FIRST serializer crossing for shape/tensor/layout ops — TEXT round-trip (print→parse→re-walk all-None + print==reprint byte-exact, the 5d discipline) AND BINARY round-trip (serialize→deserialize→re-walk all-None) byte-clean. **BAND-CLOSE re-run ×4 configs GREEN** (the 21a matrix rows are INHERITED greens — context.cpp changed at 21b — so the FULL band re-ran, never inherited): win-debug `[shape/tensor/layout/band21]` 9 hand + 51 gen-smoke; win-asan 9 + 51; linux-gcc-debug 60 (9+51, REBUILT 74/74 — a genuine compile+run, not a stale binary); linux-gcc-asan 60 (REBUILT) + opgen-drift (#665)/validator (#666) + `crd-ceir-invariants` (#1305) + gcc `-Werror=switch` + LLVM-20 tidy (1 file: `test_band21_gate.cpp`). ⛔ DETERMINISM (reduce/matmul are ORDER-SENSITIVE — float accumulation) DOCUMENTED in the reduce/matmul TOML docs → opgen-regen → drift-clean (two ways: `--check` byte-compare + ctest #665); the bit-exact contract → CEIR-22's ORACLE gate (ledger L9). builder AUDIT: DECLARE-only, crd-ceir gained ZERO gpu-context edges, no render-technique builder, no kernel_ref (§70 whole-graph). **§23 layout-in-type = a Dxxx declared divergence** (a constraint-op vs a type-member — the identity-ripple cost) named-forward to the first type-identity consumer. 10-item DEFERRAL LEDGER each homed (L1 `ceir.linalg`/FFT→22, L2 gather/scatter/scan/sort/conv→named, L3 sparse/quant→23, L4 element-promotion, L5 type-carried layout, L6 rank-16 cap, L7 reduce-keepdim, L8 canonicalizer-non-fold→CEIR-6, L9 reduce/matmul determinism→CEIR-22 oracle, L10 `shape.assert` runtime-execution). AUTHORITATIVE: `docs/sessions/2026-08-18-ceir-21z-band-close.md` (sub-slices + MATRIX + ROW-PER-CLAIM + builder audit + ledger). ⛔ advisor-at-the-gate at EVERY sub-slice caught real defects (21a result-identity gap; 21b matmul BATCH false-green; 21c arity-0 leak) — all fixed + re-verified, never inherited. commit ㉒. **NEXT BAND = CEIR-22 (CRD-Hesap integration — GEMM→FFT→reduction as ONE oracle-gated CEIR asset).** | §51 §21 §22 §70 |
| ✅ | CEIR-22 | CRD-Hesap / numerical GPU integration → **GEMM→FFT→reduction→viz-prep as ONE oracle-gated CEIR asset, no CPU round-trip** (§59: "no separate Hesap GPU scheduler — CEIR is the common orchestration substrate"). <br>**• BAND-OPEN 2026-08-18 (advisor design-locked; §52/§59/§93/§137 + the hesap/CKIR substrate survey READ):** ⛔ SCOREBOARD CORRECTION AT OPEN — there is NO `hesap-gpu` module; the GPU compute substrate is **CKIR (`crd-kir`)**: GEMM = `KOp::Contract` + `ckir_tile` (`WarpTiled` fast ≈0.75–1.04× cuBLAS / `Naive` bit-exact; the ~89% coopmat2 tensor-core path is research/bench-tier, NOT a checked-in schedule), FFT = `ckir_fft.hpp build_fft*`, reduction = `ckir_reduce.hpp build_reduce*`; hesap-dense/hesap-fft/hesap-tensor are CPU-ONLY reference numerics (an INDEPENDENT cross-oracle, not the GPU path). The single bit-exact numerical oracle for all three = **`eval_cpu`** (`ckir_eval.hpp`, the CKIR CPU reference interpreter, dtype-faithful) — discharges CEIR-21 ledger L9. Live GPU-vs-oracle harness = `tests/gpu-shared/ckir_kernel_dispatch.hpp` (Vk+DX12, bit-for-bit). "No CPU round-trip" is already the `ceir-gpu/execute.hpp` model (GpuOnly buffers, one submitted list). **SCOPE = declare + native-provider EXECUTION + oracle** (the FIRST band a CEIR tensor graph RUNS on-device, oracle-gated — §137). ⛔ MANDATE #1 (advisor-locked): CEIR DECLARES the high-level ops (algorithmic intent, §52) + AUTHORS the pipeline as a CEIR ASSET; the pre-existing CKIR `build_*` kernels are the §52 "native tensor/data-graph provider" MECHANISM (like a vendor library — the `kernel_ref is cooktime not execution tier` precedent), NOT algorithm-builders the band hand-writes. CONDITION: the provider is a GENERIC per-op mapping (op-kind→kernel+bindings) + the stage SEQUENCE / buffer-wiring is DERIVED FROM THE PARSED ASSET (a bespoke `run_gemm_fft_reduce()` with the asset as decoration = the 20c-2 decorative-"module-driven" defect; the 22c gate PROVES the asset drives the order). The `crd-ceir-gpu → crd-kir` edge (ADR-0109 §4.2 / ADR-0125, pre-declared "added when consumed") lights up HERE; crd-ceir CORE stays host-only (`crd-ceir-invariants`-gated). ⭐ = roadmap band-heading "CEIR-19 — CRD-Hesap integration" (tracker-authoritative band numbering; the §-refs are roadmap SECTIONS, not band headings — flagged so a future grep doesn't cross-wire). **DECISIONS (advisor-locked BEFORE 22a):** (1) `ceir.tensor.fft` complex = **SPLIT re/im** (two real F32 tensor operands → two results) MIRRORING the CKIR c2c split-buffer ABI (`in_re`/`in_im`/`out_re`/`out_im`); NO new complex element TypeKind (the 21c identity-ripple precedent) → a true complex `TypeKind` is a **Dxxx named-forward**; attrs `direction`{forward,inverse}+`axis`; r2c/c2r/2d named-forward. (2) `ceir.linalg.gemm` = the BLAS identity `alpha·op(A)·op(B)+beta·C` {alpha,beta,trans_a,trans_b} — DISTINCT from 21b `tensor.matmul` (matmul = gemm α=1,β=0,no-trans,no-C; relationship stated in the TOML docs; verifier REUSES the 21b shape machinery); a bare duplicate matmul = op-unification debt. (3) fft home = `ceir.tensor.fft` (§59 literal). (4) MINIMAL op set = gemm + fft ONLY. **SUB-SLICES:** **22a** declare `ceir.linalg`(gemm) + `ceir.tensor.fft` + verifiers (the 21 declare mold; ⛔ the fresh 21x scar — EVERY declared contract gets a MisuseKind + one malformed test + min-arity fold). **22b** the CEIR→CKIR NATIVE PROVIDER in ceir-gpu (wire the crd-kir edge; map gemm/fft/reduce → CKIR by OP-KIND). ⛔ SUPERSEDED 2026-08-18 (the CKIR-substrate survey + advisor reconcile): ~~PRODUCTIZE `ckir_kernel_dispatch.hpp` into ceir-gpu; single-op device gate = matmul bit-exact vs `eval_cpu`~~ → CKIR has TWO tiers with TWO oracles: **GRAPH tier** (`g.input`/`g.contract`/`g.reduce` → `KirBackend{Vulkan,Dx12,Cpu}::run`, Cpu wraps `eval_cpu`) owns gemm (`g.contract`, DetTier::Exact = bit-exact on an ODD shape e.g. 32×48×24) + reduce (`g.reduce`); **KERNEL tier** (`buffer_decl`+`stmt_*` → `ckir_kernel_dispatch.hpp::dispatch_kernel_1wg` vs `eval_cpu_kernel`) owns fft (`build_fft1d_batched`, 6 buffers incl. host-precomputed twiddles). So NOTHING is "productized" — `KirBackend::run` IS the graph-tier device path; the harness stays in tests (moving a tests-only header into the engine = the dead-edge smell). ⛔ ENGINE-side (ceir-gpu) = SYNTHESIS ONLY (op → KGraph/plan/buffer-contract); `crd-kir` = a PUBLIC header-only edge (ceir-gpu stays backend-free). The DEVICE GATES live in `tests/ceir-gpu-{vulkan,dx12}` (the `test_ceir_add_*` precedent, they link the backends); the engine-side runner is 22c's executor (ledgered). ⛔ DEVICE-GATE EVERY OP THE PROVIDER CLAIMS (a CPU-oracle-only mapping = the 19b cook-only scar) — gemm + reduce + fft each on Vk + DX12 + lavapipe. ⛔ THE 20c-2 TRAP: `eval_cpu`/`eval_cpu_kernel` run the SAME synthesized graph, so each gate ALSO needs an INDEPENDENT semantic reference (a triple-loop gemm / serial sum / naive DFT — or the hesap-dense/hesap-fft CPU cross-oracle pulled forward). ⛔ TYPED-REJECT outside the bit-exact envelope (α/β/trans/`fn` — a plain-contract synthesis of an α=2 gemm = a wrong-result false-green; every rejection gets a test); DetTier::Fast (WarpTiled, ULP) → name-forward. Dialect-qualified op-names (`"linalg.gemm"`/`"tensor.fft"` — the work.consume scar) + valid-checked attr reads (the absent-reads-as-zero scar). **22c** the §137 GEMM→FFT→reduction→viz-prep proof. ⛔ DESIGN-LOCKED 2026-08-18 (advisor + the executor-feasibility survey — SUPERSEDES the band-open one-liner ~~asset=FILE, viz via build_fullscreen_ceir, reuse 11c plan~~): ⭐ FEASIBLE — the graph/kernel tier split DISSOLVES at the `GlslKernel`→compile→`ComputePipeline`→`ComputeRecorder` layer (graph-tier `emit_contract_glsl`/`emit_reduce_glsl` + kernel-tier `emit_compute_kernel_glsl` all record into ONE recorder; drive `VulkanComputeContext` DIRECTLY, NOT `KirBackend::run` [one-shot; run_graph rejects Contract]; the `execute_lowered` shape). ⭐ HLSL graph-tier emitters EXIST (`emit_contract_hlsl`/`emit_reduce_hlsl`) → the DX12 pipeline leg IS feasible (Vk + DX12 + lavapipe, no named-forward). **ARCHITECTURE (TWO-PHASE, the lower_region→execute_lowered mold, §158 compile≠run):** `plan_tensor_pipeline(ctx, module, alloc) → TensorPipelinePlan` (PURE, device-free: verify-clean first [find_*_misuse], synth each op [ckir_synth], derive the buffer wiring from SSA DEF-USE edges [op A's result Value → op B's operand ⇒ same buffer — the asset-drives-it mechanism; a rewired asset plans differently BY CONSTRUCTION]; per stage carries op*/tier/synth-product/binding-slots/grid/push-blob; typed-reject outside the envelopes) + `execute_tensor_pipeline(plan, rec, resolver, bindings)` (records dispatches + inter-stage ShaderWrite→ShaderRead barriers into the CALLER's recorder; the RESOLVER = the backend half [synth-product→emit(glsl/hlsl)→compile→ComputePipeline], caller-side). Both in ceir-gpu (backend-free — it names ComputeRecorder&/ComputePipeline*/ResolvedBinding, the execute_lowered precedent). **MEMORY-PLAN = the TensorPipelinePlan itself** (buffers w/ role external-in/intermediate/output/ALIAS + bytes + per-stage bindings — §137 "memory plan visible" as an inspectable struct; ⛔ NOT 12d plan_block_memory [keys off effects; the Pure tensor ops have none → effect-plumbing = a ledgered slice]). **PROFILING = the 11c mold + its scar:** per-stage STRUCTURAL self-time (non-negative+measurable, NEVER quantitative) + counts/bytes + one total submit wall-time (per-stage GPU timestamps → named-forward). **VIZ-PREP = an AUTHORED `.ckir` compute kernel** (magnitude/normalize → a display-ready buffer) as the 4th stage — REQUIRED (§137 "mixed high-level tensor + CKIR"): the 3 tensor ops are SYNTHESIZED (§52 provider), the viz kernel is an ALGORITHM → authored `.ckir` via ckir_read/18q, a `compute.dispatch` in the same module; on-screen PIXELS → named-forward. **RANK BRIDGE (locked) = reshape-as-plan-ALIAS:** gemm [M,N] → `tensor.reshape` → [M·N] (pow2) → fft (zero-copy, a plan alias entry, exercises 21b reshape live, leaves the 22b fft envelope UNTOUCHED — no provider regating). ⛔ PIN the PRECISE emitters (emit_contract_glsl/emit_reduce_glsl; fast/T2 = the ULP tier → named-forward). ⛔ the zero-imaginary buffer = a PLANNED external EXPLICITLY uploaded-as-zeros (never assume create_buffer zero-inits) + twiddles = a planned external (the 22b contract); the upload TransferDst→ShaderRead barrier scar. **ORACLE = TWO RUNS:** the PROOF run (one submit, FINAL readback only, end-to-end vs a composed reference within a DERIVED declared bound — propagate the fft 2e-3·maxmag through the reduce sum, derivation IN the gate comment, NEVER a tuned epsilon) + the INSTRUMENTED run (per-stage readbacks vs per-stage oracles: gemm/reduce bit-exact vs eval_cpu, fft within its tol); the INDEPENDENT end-to-end ref = **hesap-dense gemm → hesap-fft → serial sum → the viz formula** (discharges the band-open cross-oracle; the test links hesap-dense/hesap-fft, CPU-only, legal in tests). ⛔ GENERICITY GATE (the 20c-2 rule): a SECOND asset with different wiring/shapes PLANS differently (assert stage order + buffer wiring change with the asset). **SUB-SLICES:** 22c-1 the PLAN (+ device-free tests: wiring/roles/bytes/aliases + typed-rejects + a 2nd differently-shaped module for genericity); 22c-2 the EXECUTOR + the Vulkan proof gate; 22c-3 the authored `.ceir` asset + the viz `.ckir` + parse→verify→plan→execute + the remaining legs (DX12 + lavapipe + the two oracle runs + the hesap cross-oracle + the memory-plan/profiling gates). §137 CLAIM MAP (for the 22z row): asset=the .ceir file · mixed=the viz .ckir · no-CPU-round-trip=the one-submit proof run · memory-plan=TensorPipelinePlan · profiling=TensorPipelineProfile · oracle=per-stage+end-to-end+hesap. **✅ 22c-1 DONE 2026-08-18** — `plan_tensor_pipeline` (`engine/ceir-gpu/{include/crd/ceir/gpu/tensor_pipeline.hpp,src/tensor_pipeline.cpp}`): PURE + device-free — verify-clean (find_linalg/tensor_misuse) → walk the func body → per-op-kind stage + the SSA DEF-USE buffer graph (a prior op's result Value → the SAME buffer; resource.declare → ExternalIn CallerData; reshape [numel-preserved] → a zero-copy ALIAS [the rank bridge]; fft → the 6-buffer contract [chain in_re, the imaginary operand re-marked Zeros, 2 FftTwiddle externals, out_re/out_im intermediates]; terminal → Output) + per-stage synth typed-rejects (mirroring SynthReject). The plan IS the §137 memory-plan (`PlanReject`[7] / `StageKind`{Gemm,Fft,Reduce,VizDispatch} / `BufferRole`{ExternalIn,Intermediate,Output,Alias} / `FillKind`{CallerData,Zeros,FftTwiddle}). `test_tensor_pipeline.cpp` (3 device-free): the well-formed gemm→reshape[alias]→fft→reduce WIRING (fft in_re binds the alias→gemm-output; reduce in binds fft out_re) + roles/bytes/fft-externals; typed-rejects (NotVerifyClean, SynthRejected=ElementNotF32); ⭐ GENERICITY (the 20c-2 rule — a 4×4 vs 8×8 asset plans DIFFERENT bytes [64 vs 256 B], same 3-stage structure). GATE ×4 (win-debug/asan + linux-gcc-debug/asan 3/3 each) + gcc `-Werror=switch` + tidy-20 (3 files). ⛔ the ascii-only-test-names scar bit AGAIN (an em-dash in the genericity test name → ctest can't register it → a false red that passes standalone; fixed to `--`). NEXT = 22c-2 (the executor). commit ㉕. **✅ 22c-2 DONE 2026-08-18** — `execute_tensor_pipeline` + `validate_tensor_pipeline` (`engine/ceir-gpu/{include/crd/ceir/gpu/tensor_pipeline_exec.hpp,src/tensor_pipeline_exec.cpp}`): the §158 RUN-half (backend-free) — RECORDS a TensorPipelinePlan into a caller-owned `crd::gpu::ComputeRecorder` (each stage's dispatch, bindings assembled from the plan's DEF-USE buffer indices in the 13a positional order + an inter-stage ShaderWrite→ShaderRead barrier on each written output [the last `n_out` binds]); the CALLER's `StageResolveFn` does the backend half (re-synth→emit→compile→ComputePipeline + grid + push). ⭐⭐⭐ **THE sec-137 PROOF (`test_ceir_pipeline_vulkan.cpp`) — GREEN ON THE 4070 Ti + win-asan:** an authored ceir module (linalg.gemm→tensor.reshape→tensor.fft→tensor.reduce) is PLANNED + EXECUTED as ONE SUBMIT — GEMM (graph tier, `emit_contract_glsl`) + FFT (kernel tier, `emit_compute_kernel_glsl`) + reduce (graph tier, `emit_reduce_glsl`) all record into the ONE recorder, the intermediates DEVICE-RESIDENT (NO CPU round-trip), the reshape a ZERO-COPY ALIAS (the fft in_re SHARES the gemm output buffer) — and the final scalar matches an INDEPENDENT composed reference (triple-loop GEMM → naive DFT → serial sum, f64) within a DERIVED declared tol (`kL·2e-3·maxmag`, propagating the fft float-butterfly error through the reduce sum, NOT a tuned epsilon). ⛔ the graph/kernel tier split DISSOLVES at GlslKernel→ComputePipeline (drive `VulkanComputeContext` DIRECTLY, not `KirBackend::run`). Externals seeded per FillKind (CallerData A/B / Zeros C+im / FftTwiddle cos,-sin), ONE begin/submit, a FINAL GpuToCpu readback only. GATE GREEN win-debug + win-asan (Vulkan 4070 Ti) + tidy-20 (3 files). ⛔ FIXES: C4324 alignas-padding-treated-as-error (dropped the ResolvedStage push `alignas` — ≤16B naturally 4-aligned); ⛔ the ResolvedStage push is INLINE (a `const void*` to the resolver's local would DANGLE); ⛔⛔⛔ the ascii-only-test-names scar bit a 3RD time this band (a `§` in the gate name → ctest can't register it → "No tests found"; fixed to `sec-`). NEXT = 22c-3. commit ㉖. **22c-3 IN PROGRESS** (advisor DESIGN-LOCK 2026-08-18 → **design B**: gemm→reshape→fft→**mag(CKIR dispatch)**→reduce(**max**)→**normalize(CKIR dispatch)**, viz-prep TERMINAL = the [64] normalized spectrum — charter-literal `reduction→viz-prep`; 2 CKIR dispatches interleaved w/ the tensor ops; the normalize dispatch READ-BINDS a RANK-0 tensor [the max scalar] — ⭐ `ceir_is_resource_kind` covers `TypeKind::Tensor` so a Tensor Value binds a compute.dispatch DIRECTLY, NO bridge op; the asset is a PARSE-LOADED `.ceir` text file — `parse(Context&,StringView)→ParseResult{Module*}` in engine/ceir/parse.hpp EXISTS). ✅ **22c-3a DONE** — plan_tensor_pipeline handles `compute.dispatch`→VizDispatch (verify-clean NOW also requires `find_dispatch_misuse==None`; `arith.const` [the grid consts] skipped; bindings from operand 3; a `w`-token binding's ExternalIn buffer RE-MARKED Intermediate [the fft im0 precedent]; the `w` bindings must be a CONTIGUOUS TRAILING run else new `PlanReject::DispatchOutputsNotTrailing`; n_out = the trailing `w` count). ⛔⛔ THE Output-RULE FIX (advisor-caught correctness): `compute.dispatch` is RESULTLESS → the old `last_result` marks the WRONG buffer under a terminal dispatch → Output = the FINAL stage's trailing n_out binds (behavior-identical for the reduce-terminal 22c-2 shape). 3 device-free plan tests GREEN (design-B 5 stages [Gemm/Fft/VizDispatch/Reduce/VizDispatch] + roles/def-use wiring + the non-trailing-write reject) + 22c-2 STILL green (Output-rule fix verified behavior-identical) + tidy-20 (3 files). Sub-slices: 3b viz `.ckir` ×2 (magnitude+normalize) + device-free oracle · 3c `.ceir` text + parse + verify/plan/roundtrip + collect_dependencies · 3d the 6-stage Vulkan gate + per-stage 2-run oracle + hesap(dense::gemm+fft::fft) cross-oracle + uninstrumented==instrumented + memory-plan/tol-derived asserts · 3e DX12 leg · 3f lavapipe leg. ⛔ `.ckir` compute grammar: node refs POSITIONAL `n<idx>` (id cosmetic); buffer writability INFERRED from BufferStore-target (no explicit field); gid via `Builtin`(U32 vec3)+`Swizzle`.x; L=64=local_size ⇒ grid (1,1,1), no bounds guard needed. ✅ **22c-3b DONE** — authored `assets/ckir/tensor_viz_{magnitude,normalize}.ckir` (mag[i]=sqrt(re²+im²); norm[i]=mag[i]/max, the max a RANK-0 scalar read at index 0) + the device-free oracle `tests/kir/test_ckir_viz.cpp` (ckir_read→eval_cpu_kernel vs closed-form + emit_compute_kernel_glsl smoke), 2 tests GREEN + tidy-20. ⛔⛔ SCAR (cost a hang-debug cycle, memory written): eval_cpu_kernel is SCALAR — a uvec3 Builtin(GlobalInvocationId)+Swizzle ASSERTS → Windows HANGS on the abort dialog (empty-output ctest); FIX = index via the SCALAR `Builtin` LocalInvocationIndex (KBuiltin 4, `iidx=4`, no Swizzle) which == gid.x for a single workgroup; and eval rounds the store to the buffer's F32 dtype so the f64 ref needs an f32 tol (1e-6·(1+|ref|)), not 1e-9. ✅ **22c-3c DONE** — committed `assets/ceir/tensor_pipeline.ceir` (the AUTHORED design-B asset as MLIR-like text: gemm→reshape→fft→dispatch@viz_magnitude→reduce(max)→dispatch@viz_normalize) + the device-free parse-load gate in `test_tensor_pipeline.cpp` (needs CRD_REPO_DIR, added to tests/ceir-gpu). Gate: `parse()` ok → ANTI-DRIFT `print(parse(file))==print(build_pipeline_b)` (regenerate the file if the builder changes) → roundtrip stable → verify-clean (linalg/tensor/dispatch + find_unregistered_op) → plans the SAME 5 stages → `collect_dependencies().ckir_refs` == [viz_magnitude, viz_normalize] (sorted, UNPINNED — no kernel_interface authored, legal per the compute.dispatch TOML). 5 device-free tests GREEN + tidy-20. ⛔ the `.ceir` parser skips WHITESPACE only (no `#`/`//` comments) → the asset is the EXACT `print()` output, no header; `print()` IS the canonical serializer (the ckir_write analogue), bootstrapped once via a temp-gen test then removed. NEXT = 3d. ⭐ **22c-3d CORE GREEN on the 4070 Ti** (win-debug 0.43s) — `test_ceir_pipeline_vulkan.cpp` extended: the AUTHORED `tensor_pipeline.ceir` is PARSE-LOADED (not C++-built) → planned → executed as ONE device-resident submit; the resolver gained a VizDispatch arm (reads the `@kernel` SYMBOL → `assets/ckir/tensor_viz_<sym>.ckir` → `ckir_read` → `emit_compute_kernel_glsl`; grid from the asset's OWN `arith.const` operands via `const_grid`/`defining_op`, the asset-drives-it rule; `pipes[4]→[8]`). All 4 §137 stage TYPES run: gemm+reduce (graph tier) + fft + the TWO viz dispatches (kernel tier) into ONE recorder; A/B seeded via `plan.stages[0].bind` (parsed module ⇒ no C++ Value handles); the terminal [64] normalized spectrum matches an INDEPENDENT composed f64 ref (triple-loop gemm → naive DFT re+im → magnitude → max → normalize) within a DERIVED tol `2·√2·2e-3 ≈ 5.7e-3` (fft-bin err → ×√2 magnitude → ×2 quotient). tidy-20 clean. ✅ **22c-3d DONE** — added a 2nd gate "per-stage oracle + determinism": a `run_once(readable)` lambda runs the pipeline TWICE — UNINSTRUMENTED (intermediates GpuOnly) + INSTRUMENTED (intermediates GpuToCpu, host-readable) — and asserts (a) DETERMINISM: the instrumented terminal == the uninstrumented terminal BIT-EXACT (readback didn't perturb; catches nondeterminism), (b) PER-STAGE: every device stage output (gemm D, fft Fr/Fi, magnitude, reduce-max, normalize) matches the INDEPENDENT composed f64 ref within a DERIVED per-stage tol (gemm f32-exact 1e-3; fft 2e-3·sig_max; mag √2×; norm 5.7e-3). GREEN win-debug + ⭐ win-asan (both gates, no ASan errors — the two-run manual buffer/barrier/map management is memory-clean). tidy-20. ⛔ DEFERRAL LEDGER: hesap (dense::gemm/fft::fft) cross-oracle NOT added — the composed triple-loop/naive-DFT/closed-form PER-STAGE reference is ALREADY synthesis-independent (shares ZERO code with the synth path), so it MEETS the advisor's "independent semantic reference" requirement; hesap would be a redundant 3rd reference at the cost of two new lib links (crd-hesap-dense/fft) incl. the Linux/lavapipe leg — ledgered as belt-and-braces, not a proof gap. Memory-plan roles/bytes/alias asserted EXHAUSTIVELY device-free in 22c-3a (design-B) + 22c-3c (parsed). ✅ **22c-3e DONE** — the DX12 leg (`tests/ceir-gpu-dx12/test_ceir_pipeline_dx12.cpp`): the SAME parse-loaded `tensor_pipeline.ceir` runs the full 5-stage pipeline on a REAL D3D12 device via a DX12 resolver (emit_contract_hlsl / emit_reduce_hlsl / emit_compute_kernel_hlsl + `create_pipeline_from_hlsl`) + the ⛔ PORTABLE copy-readback contract (DX12 forbids a UAV on upload/readback heaps): per logical buffer a GpuOnly `dev` UAV + a CpuToGpu `up` (copy→dev, TransferDst→ShaderRead barrier before the pipeline) + a GpuToCpu `rb` (ShaderWrite→TransferSrc + copy→rb after); the `execute_tensor_pipeline` records the 5 dispatches + inter-stage barriers UNCHANGED (backend-free). Terminal [64] normalized spectrum vs the composed f64 ref within the derived 5.7e-3 bound. GREEN win-debug + win-asan (D3D12), tidy-20. ⭐ the §137 pipeline now runs on BOTH backends (Vulkan 4070 Ti + DX12) from ONE authored asset — the migrated-executor-runs-both-backends rule. ✅ **22c-3f DONE** — the lavapipe/WSL leg: built the linux-gcc-debug preset (native ext4 cache) under WSL + ran ALL 7 `22c-3` tests under Mesa **lavapipe** (the software Vulkan ICD, LP_NUM_THREADS=4) — INCLUDING both 22c-3d device gates (CORE + per-stage/determinism) on the CPU driver. 100% pass. ⭐ the §137 pipeline is now proven CROSS-VENDOR from ONE authored asset: NVIDIA 4070 Ti (Vulkan) + DX12 + Mesa lavapipe — the cook-only-gates-ship-device-impossible-programs scar fully mitigated. ⛔ SCAR VALUE: gcc -Werror caught a LATENT `fr_ref` set-but-not-used in the 22c-2 test that MSVC missed ([[feedback_msvc_hides_switch_gaps_gcc_werror_switch_catches_real_bugs]] class, unused-but-set flavor) — removed the dead array, re-verified 22c-2 green on win-debug + tidy-20. ✅ **22c-3g DONE** (advisor band-close: "profiling" is an explicit §137 charter word, blocked the close) — `TensorPipelineProfile`/`TensorStageProfile{kind,gx,gy,gz,workgroups,bytes_in,bytes_out}` in `tensor_pipeline_exec.hpp`; `execute_tensor_pipeline` gains an OPTIONAL `TensorPipelineProfile* profile=nullptr` (zero churn at the 4 call sites) populated RECORD-time from the plan binds + ResolvedStage grid; the TOTAL submit wall-time is CALLER-stamped (std::chrono around submit_and_wait — the executor stays backend-free/untimed; per-stage GPU-timestamp queries → ledger). Gate rides the core 22c-3d Vulkan test: 5 rows, kinds match the plan, `bytes_in+bytes_out == Σ the stage's bind bytes` (row/byte COHERENCE — proves it measured THIS pipeline, the empty-frame scar), gemm-out=norm-out=256 B, `wall_ns>0`; ⛔ NO time threshold (soft-perf — a threshold flakes + drags in median-of-five). GREEN win-debug + tidy-20 (cross-config via the 22z-1 sweep). **✅✅ CEIR-22c-3 COMPLETE (3a→3g all green).** NEXT = **22z** band close. **22z-1 IN PROGRESS**: fresh family sweep — win-debug 29/29 ✅; ⭐ **release 26/26 ✅ + whole family compiles clean /O2/GL/WX** — the release sweep CAUGHT + FIXED a real LATENT bug (`engine/ceir/src/dialect.cpp:22` — an `EffectRecord& e` only read by CRD_ASSERT_MSG → C4189 unused-but-set under NDEBUG/WX; `[[maybe_unused]]`, tidy-20; the [[feedback_msvc_hides_switch_gaps_gcc_werror_switch_catches_real_bugs]] class again — this is WHY the advisor forced release); ⛔ LEDGER: the 2 crd-kir-tests viz EVALs (22c-3b) did NOT enumerate on release (a `catch_discover_tests`-on-release tooling gap for that target — the exe built; pure-CPU `eval_cpu_kernel`, config-invariant, GREEN on win-debug + lavapipe). ✅ **win-asan 27/27** (no ASan errors — the asan crd-ceir rebuild w/ asserts ACTIVE also confirms the dialect.cpp fix on debug-like configs) + ✅ **lavapipe 25/25** (the full non-DX12 family incl. the kir viz evals — which DO enumerate on Linux; the 22c-3b release/asan enum-gap is Windows-catch_discover_tests-specific). ✅ **22z-1 SWEEP COMPLETE** (win-debug 29 / win-asan 27 / win-release 26 / lavapipe 25 — DX12 Linux-skipped by design, kir-viz Windows-release/asan enum-gap ledgered). ZERO-BUILDER AUDIT ✅: algorithms all AUTHORED (viz .ckir + the .ceir pipeline); `synth_*`=the native PROVIDER (declared-op→CKIR lowering mechanism, 22b-established, NOT an authored algo); `build_pipeline_b`=the KEPT anti-drift oracle for the committed .ceir (per [[reference_ceir_text_asset_authoring_via_print]]). ✅✅✅ **CEIR-22 → BAND ✅ (2026-08-18)** — the §137 numerical-workflow-through-CEIR proof STANDS: ONE authored `.ceir` asset (gemm→fft→mag(CKIR)→reduce(max)→normalize(CKIR)), parse-loaded + planned + executed device-resident as one submit, CROSS-VENDOR (NVIDIA Vulkan + D3D12 + Mesa lavapipe), memory-plan visible + PROFILING + per-stage numerical oracle + determinism, vs independent f64 refs. Sweep: win-debug 29 / win-asan 27 / win-release 26 (+ the dialect.cpp /WX catch) / lavapipe 25. Close doc: `docs/sessions/2026-08-18-ceir-22z-band-close.md` (row-per-claim + row-per-config + deferral ledger + zero-builder audit + scars). commits ㉖+㉗. NEXT BAND = CEIR-23.

**CEIR-23 BAND-OPEN LOCK (advisor 2026-08-18)** — `ceir.quant` (§54) + `ceir.sparse` (§53); charter (tracker) = **"sparse op + quantized-MLP path"** (BOTH are charter words → 23z gates on both, but the ASYMMETRY is charter-baked: "sparse op" SINGULAR = ONE op device-proven, "quantized-MLP PATH" = the crown). Decomposition (SPLIT the proofs — 22c's 7-subslice balloon is the lesson; crown banked EARLY): **23a** quant declare → **23b** quant provider + Q8 device gates → **23c** the quantized-MLP crown (cross-backend) → **23d** sparse declare → **23e** sparse SpMV device gate → **23z** close. LOCKED DESIGN: ⛔ NO new TypeKind (split-complex precedent) — quant = `quantize`/`dequantize` ops, storage-type RIDES the result, scale/zero-point are OPERANDS (rank-0 per-tensor / rank-1 per-axis, one form), scheme tokens closed-vocab attrs, declare element-agnostic (Q8 = provider SynthReject), calibration = name-forward. ⛔ sparse = EXPLICIT-TRIPLE operands `sparse.spmv(row_ptr,col_idx,values,x)` (first-class in vocab+verifier, NOT opaque buffers), CSR-only (CSC/COO/block = verifier name-forwards), type-encoding stays CHIR-headroom name-forward. ⛔ fusion: the `dequantize→gemm` window IS 23's job (§54 "fused into provider programs"); general fusion = CEIR-26; gold = unfused-then-fused, fused==unfused==f64. hesap = C++ ORACLE only; device kernels = authored `.ckir`. ⛔ BOUNDARY: the "MLP path" ≠ `ceir.ml` (CEIR-24) — 23c scoped to the v14-m Q8 LAYER shape, not a full MLP. Reuse: `ckir_neural.hpp`/v14-m Q8 + `docs/bench/2026-07-14-fused-mlp-cublas-gold.md` + hesap sparse. ⛔ BAND# = tracker-authoritative (the type.hpp "SparseTensor encoding is CEIR-18" comment is OLD pre-renumber numbering — memory [[reference_ceir_band_numbering_tracker_is_authoritative]]). PRE-LOCK VERIFY (do at the slice): 23a CEIR scalar i8/u8 vs u32-pack; 23b VK_KHR_8bit_storage vs bitops (test_ckir_bitops); 23d `stmt_for_begin` runtime bound. Advisor again at 23b/23d. **23a IN PROGRESS**: ✅ declare-half — `engine/ceir/ops/quant.ceirop.toml` → opgen → `quant.quantize`/`quant.dequantize` (scale/zero_point OPERANDS [rank-0 per-tensor / rank-1 per-axis], storage int rides the result via type_int(8,signed)=i8/u8 [pre-lock item 4 ✅], `axis`+`scheme`{symmetric,asymmetric} closed-vocab attrs, Pure+NoSemantics element-agnostic); generated quant_ops + smoke + register_quant_ops COMPILE + GREEN (smoke + opgen-validator/drift). ✅ **23a DONE** — `find_quant_misuse` (`engine/ceir/{include/crd/ceir/quant.hpp,src/quant.cpp}`, the linalg.cpp mold, const Context&): 10-kind QuantMisuseKind walk over quant.quantize/dequantize — operand/result Tensor-kinded, output.shape==input.shape, scale.shape==zero_point.shape, scale rank-0|1 (rank-1 ⇒ axis in [0,rank) ∧ scale.dim0==input.dim[axis]), the VALUE(float)/STORAGE(int) element ROLES SWAPPED between the two ops (scale=value side, zero_point=storage side), scheme∈{symmetric,asymmetric}. `tests/ceir/test_quant.cpp` (2 gates: well-formed per-tensor+per-axis clean; every misuse → exact kind) GREEN + tidy-20 + opgen-validator/drift green. **23b ADVISOR LOCK (2026-08-18)** — split into **23b-1** (unfused dequant + gates) → **23b-2** (the fused window). ⭐ KEY: the `dequantize→gemm` FUSION is a PLAN-LEVEL 2-ops→1-stage COLLAPSE (`plan_tensor_pipeline`'s FIRST 2→1; a distinct `StageKind::QuantGemm`, deterministic + inspectable — NOT a synth_gemm patch, NOT a silent rewrite; the §137 memory-plan-visible doctrine extends to fusion), fuse ONLY when the dequantize result has EXACTLY ONE use (multi-use → unfused fallback + its own negative test); the dequantize's would-be output buffer NEVER allocated (the §54 win). ⛔ FINDING: the "v14-m Q8 reuse" RESOLVED to the ORACLE tier — `quantize_q8_0` (hesap dtypes) is ggml PER-BLOCK (block-32 + f16 scale, §54 name-forward), NOT per-tensor/per-axis; `build_mlp_fwd_fp32` is FP32. So AUTHOR per-tensor/per-axis Q8 `.ckir` FRESH; keep `quantize_q8_0` as a ROUNDING cross-check on the test-data generator (round-half-away-from-zero, pinned). ⛔ STORAGE: int8 U32-PACKED (4/u32) + BITOPS sign-extend — ⭐ PINNED IDIOM `(packed_i32 << (24−8·lane)) >> 24` on a SIGNED I32 (KOp::Shl+Shr emit `>>` IDENTICALLY in GLSL:215/HLSL:113 → arithmetic on I32); ⛔ AVOID `BitfieldExtract` (emits `((a>>b)&((1<<c)-1))` = ZERO-extend, no sign — the dx12-hlsl-masks scar class). PIN N%4==0 (typed SynthReject / zero-pad). Minimal Q8: per-tensor SYMMETRIC (zp≡0) first, per-axis extra, asymmetric zp≠0 UNFUSED-only (fused=symmetric; ledger). ⛔ scale/zp = ExternalIn CallerData PlanBuffers (rank-0=4B, the mx-scalar precedent); the QuantGemm bind order = a DECLARED positional contract (like Fft's 6-bind). ⛔ `synth_quantize` ABSENT (host-side quantize this band) = a LEDGER ROW, not a gap (else 23z declared-words gate trips). ⛔ BLAST RADIUS: 23b-2 re-runs the 22c-3 §137 family. **23b-1 IN PROGRESS**: ✅ AUTHORED `assets/ckir/quant_dequantize_q8.ckir` (per-tensor: out[i]=(int8(packed[i])−zp)·scale; int8 U32-PACKED 4/u32) + ✅ device-free eval oracle (`tests/kir/test_ckir_viz.cpp` 23b-1 case: ckir_read→eval_cpu_kernel vs CPU `(int8−zp)·scale`, N=64 spanning [−128,127], asymmetric zp=3) GREEN first-try. ⭐ THE SIGN-EXTEND WORKED via the FLOAT idiom `byte=(word>>8·lane)&0xFF` (logical Shr/BitAnd on U32) then `signed=f32(byte)−256·f32(byte>>7)` (KOp::Cast U32→F32 + float arith) — NO arithmetic-Shr, NO BitfieldExtract (zero-extend), fully eval-safe + emitter-portable. ✅ **23b-1 DONE** — the authored Q8 dequant kernel proven CROSS-VENDOR: eval + Vulkan (4070 Ti, `tests/ceir-gpu-vulkan/test_ceir_quant_vulkan.cpp`) + DX12 (`tests/ceir-gpu-dx12/test_ceir_quant_dx12.cpp`) + lavapipe — all GREEN + tidy-20. ⭐ the DX12 leg CONFIRMS the HLSL/GLSL EMITTER AGREEMENT (the float sign-extend + Cast + logical shifts emit identically — the dx12-hlsl-masks scar avoided by design). dispatch_kernel_1wg is float-only ⇒ the packed U32 buffer uploads as the BIT-REINTERPRET of each word. ⛔ LEDGER (→ 23b-2/23c): the `quant.dequantize`→plan-stage INTEGRATION (the op consumed by the plan) rides 23b-2 (which touches plan_tensor_pipeline for the fusion anyway); the ggml `quantize_q8_0` ROUNDING cross-check rides 23c (where the HOST f32→int8 quantizer that generates the crown's Q8 weights lives — 23b-1 picks int8 values directly, no quantizer to cross-check); per-axis kernel variant = name-forward. **23b-2 ADVISOR LOCK (2026-08-18)** — ✅ PREREQ DONE: `tensor_bytes` ELEMENT-SIZE fix (`element_bytes` f32=4/i8=1/f16=2/fp8=1, ⛔ reject-unknown=0 NO silent default — the POD-hash scar; N int8 = N bytes == the u32-packed device size) + §137 family (12 tests) RE-RUN GREEN + tidy-20 (the only change touching the closed band's arithmetic — done as its OWN verified step, the advisor's sequence). ⛔⛔ ONE shared helper `fusable_dequant_into_gemm_weight` — used by the dequant-side SKIP + the gemm-side DETECT + the resolver RE-DERIVE (3 hand copies WILL drift): fusable ⇔ dequant result SINGLE-USE ∧ that use is SPECIFICALLY the gemm's operand-1 (weight) slot (quantized-A/C → unfused fallback + a negative test PER slot — else the two predicates disagree and reject a legal module). Sequence: **2a** StageKind::Dequant (unfused: quant.dequantize→Dequant stage {W_q8,scale,zp→out} n_out=1, resolver loads the proven 23b-1 kernel; device gate; assert synth_gemm still ElementNotF32-rejects int8) → **2b** StageKind::QuantGemm (collapse: bind {A,W_q8,scale,zp,D} n_out=1 — A-first, dequant triple in operand order, D trailing, DECLARED in the comment). ⛔ TRAP1: grid/M,K,N from the GEMM operand (f32 [K,N]) but the stage BINDS the dequant INPUT (int8 [K,N]) — legal ONLY via dequantize shape-preserve (plan test asserts W-bind bytes==K·N·1). ⛔ TRAP2: stage op=the gemm ⇒ resolver RE-DERIVES scale/zp (gemm→operand1→defining_op→operands) via the SAME helper. Fused kernel `quant_gemm_q8.ckir` (per-tensor SYMMETRIC, serial k-loop matching unfused ⇒ fused==unfused is DERIVED-tol not bit-exact, L9). Gate: fused (single-use) vs unfused (a 2nd consumer forces it = the multi-use negative) vs f64. ⛔ BLAST: re-run 22c-3 §137. **23b-2a IN PROGRESS**: ✅ plan-side — `StageKind::Dequant` + verify-clean now runs `find_quant_misuse` + the walk maps `quant.dequantize`→a Dequant stage; §137 (22c-1 plan) RE-RUN GREEN + tidy-20. ⛔ ZP DESIGN FORK RESOLVED → **SYMMETRIC** for the plan path: the IR zp is int8 [1] (= a 1-byte buffer; the 23b-1 kernel used f32 zp — IR-inconsistent), so the Dequant stage binds {W_q8, scale, out} 3-bind (zp VALIDATED-present but NOT bound; the symmetric kernel drops the subtract — the advisor's "fused=symmetric" floor); the ASYMMETRIC int8/int32-zp plan path is NAME-FORWARD (the 23b-1 standalone gate already proved the asymmetric dequant MATH). ✅ authored `assets/ckir/quant_dequantize_q8_sym.ckir` (3-buffer, no zp — the 23b-1 kernel minus the subtract) + ✅ device-free plan test (`test_tensor_pipeline.cpp` 23b-2a: quant.dequantize→1 Dequant stage, 3-bind, ⭐ W_q8 int8 [64] = **64 B NOT 256** [the element-size fix], out f32=256 B + Output role) GREEN + tidy-20. REMAINING = the DEVICE gate (a module w/ quant.dequantize → plan → execute via a Dequant RESOLVER [ckir_read the sym kernel + emit + grid] → device dequant vs CPU `int8·scale` ref, Vk+DX12+lavapipe; ⛔ the zp int8 rank-0 = 1-byte ExternalIn buffer materialized-but-unused — watch create_buffer(1)). **23b-2 DONE (2026-08-18)** — the FUSED dequantize→gemm collapse (StageKind::QuantGemm) proven CROSS-VENDOR. Shared `fusable_dequant_into_gemm_weight` gates the FULL fused-kernel contract (single-use ∧ weight-slot ∧ scheme=symmetric ∧ scale rank-0 ∧ α=1,β=0,no-trans — advisor caught scheme+rank, I caught α/β/trans by applying the same principle) + a `dequant_is_symmetric_per_tensor` TYPED reject (PlanReject::UnsupportedQuantScheme, name-forward, NEVER silent-symmetric — protects the unfused Dequant walk too). 6 device-free plan tests (fused + wrong-slot/multi-use/asymmetric/per-axis/non-plain-gemm negatives) + the 155-node `assets/ckir/quant_gemm_q8.ckir` (fixed M4·K8·N8, dequant-INLINE unrolled-K, eval oracle GREEN first-try) + Vk+DX12 device gates (`test_ceir_pipeline_{vulkan,dx12}.cpp`: fused == unfused Dequant+Gemm == oracle; pays the folded 2a sym-dequant device debt via the unfused leg's Dequant stage). §137 22c blast RE-RUN GREEN (12/12); all 5 changed files tidy-20 clean. Deferrals (ride 23z / name-forward): lavapipe leg + release/asan configs, general-dims fused kernel (fixed [4,8,8]), asymmetric/per-axis PLAN path. **23c DONE (2026-08-18)** — the quantized-MLP CROWN proven CROSS-VENDOR. A 2-layer uniform-width (batch 4 × width 8) MLP as a PARSE-LOADED `assets/ceir/quant_mlp.ceir` (bootstrap-via-print; `build_quant_mlp` = the anti-drift oracle): x → dequant(W1_q8)+gemm → QuantGemm → relu → dequant(W2_q8)+gemm → QuantGemm → out. Every layer REUSES the baked [4,8,8] quant_gemm_q8.ckir (uniform width — no general-dims work); relu = `assets/ckir/relu.ckir` (Max(x,0)) rides compute.dispatch→VizDispatch (no new StageKind). **23c-a** relu + eval oracle. **23c-b** the .ceir asset + device-free parse/verify(incl find_quant_misuse)/plan(QuantGemm→VizDispatch→QuantGemm)/roundtrip — the printer/parser handle !i8 + quant.dequantize + rank-0 !shape<> with NO gap (the advisor's flagged risk did not materialize). **23c-c/d** Vulkan + DX12 device gates: the composed MLP (device-resident, no CPU round-trip) == the float MLP oracle BIT-EXACT (0.25-mult x + power-of-2 scales; run_quant_module generalized to a multi-seed list x,W1,s1,W2,s2; relu added to BOTH resolver name-maps). **23c-e** the charter's quantize_q8_0 ROUNDING cross-check — DECOUPLED from the crown (crown uses DIRECT int8; block-32/f16-scale Q8_0 doesn't fit the per-tensor path) as a self-contained device-free hesap gate (controlled half-boundary rounding + f16-encode + round-trip bound), COMPLEMENTS the existing v14-a byte-exact-vs-ggml test. §137 22c family RE-RUN GREEN (Vk+DX12); all changed files tidy-20. ⚠ em-dash-in-TEST_CASE-name bit again (ctest-by-name NotRun/exit8 while direct-by-tag passes) — ASCII names. Deferrals (ride 23z / name-forward): lavapipe leg + release/asan configs, general-dims fused kernel. **23d DONE (2026-08-18)** — ceir.sparse declared. ONE op `sparse.spmv(row_ptr, col_idx, values, x)->y` (CSR SpMV): the CSR triple + dense x are explicit rank-1 tensor OPERANDS (no new TypeKind; op-name IS the format, no `format` attr; CSC/COO/block = future ops name-forward). `engine/ceir/ops/sparse.ceirop.toml` -> opgen -> generated ops + `find_sparse_misuse` (`engine/ceir/{include/crd/ceir/sparse.hpp,src/sparse.cpp}`): 7 kinds — OperandNotTensor/ResultNotTensor, RankInvalid (all 5 rank-1), IndexElementNotInt (row_ptr+col_idx Int), ValueElementMismatch (values/x/y Float+EQUAL), NnzMismatch (col_idx.dim0==values.dim0), RowPtrLengthMismatch (row_ptr.dim0==y.dim0+1, .count arithmetic + both-static). Out of verifier scope (DATA, documented): monotonicity, col_idx<N, nnz==row_ptr[M]. two-gate `tests/ceir/test_sparse.cpp` (well-formed CSR clean; every kind exact) GREEN + opgen-drift guard GREEN + tidy-20. ⛔ NO plan touch (sparse.spmv->default UnsupportedOp by construction). **23d-1 For micro-gate** (the stmt_for "verify first"): the ckir For BOUND is UNIFORM/lockstep (eval reads active[0]'s count) -> a bare divergent stmt_for(per-thread count) SILENTLY runs thread-0's trips; divergent loop = UNIFORM max bound + per-thread ForBreakIf (the while_loop form). SERIALIZED (.ckir text) runtime-For round-trips byte-exact + evals + emits GLSL. Pins the 23e SpMV kernel shape [[feedback_ckir_eval_for_bound_is_uniform_use_max_plus_forbreakif]]. **23e DONE (2026-08-18)** — the CSR SpMV proven CROSS-VENDOR. `assets/ckir/spmv_csr.ckir` (the ceir.sparse.spmv provider): y[i] = sum_{k in [row_ptr[i], row_ptr[i+1])} values[k]*x[col_idx[k]] — the 23d-1 segmented-sum shape + the col_idx INDIRECTION; runtime For + per-thread ForBreakIf (uniform baked MAX=3 = max row length; the lockstep/GPU-portable form) + RMW-accumulate on the output. **23e-a** bootstrap-via-ckir_write -> committed asset -> DELETED the builder (18a-1 mold) + device-free eval oracle (test_ckir_viz.cpp "ceir 23e-a": ckir_read -> eval == CPU CSR SpMV). **23e-b** the Vulkan + DX12 gates (test_ceir_sparse_{vulkan,dx12}.cpp, dispatch_kernel_1wg, u32 row_ptr/col_idx uploaded as bit-reinterpret): the runtime-For + ForBreakIf + col_idx indirection compile (GLSL+HLSL loop-lowering agreement) + run on real 4070 Ti (Vk) + D3D12, y == the CPU CSR SpMV ref. The FIRST CKIR asset with DATA-DEPENDENT control flow. tidy-20 clean. Deferrals (name-forward): general max-row-length (spec-const/push), SpMM/CSC/COO. NEXT = 23z BAND CLOSE (the 22z mold: fresh sweep + zero-builder audit + session log + tracker BAND row + context.md + commit msg). Drive → CEIR-35. (proof + oracle matrix + ledger). **BACKEND MATRIX (declared at open):** 22b/22c gate Vk + DX12 + the lavapipe Linux leg; the **kir-cuda** native-provider leg = a LEDGER row w/ a home (the ⛔ CUDA-required mandate surfaces at close). **BAND-OPEN LEDGER (each homed):** L1 §52 QR/LU/Cholesky/LDLT/SVD/eigen/solve/iterative/sparse-factor → named-forward `ceir.linalg` rows; L2 a true complex element `TypeKind` (the Dxxx above) → a type-system slice; L3 fft r2c/c2r/2d/batched variants → named `ceir.tensor.fft` rows; L4 the **hesap→CEIR producer FRONTEND** (`A*B → ceir.linalg.gemm` emission — the §59 producer half) → a named home; L5 the **kir-cuda** native-provider leg → a CUDA-provider row (the CUDA-required mandate); L6 scan/sort as `ceir.tensor` ops → named rows. Advisor at each sub-slice gate; 60s cadence. **✅ 22a DONE 2026-08-18** — `ceir.linalg` + `ceir.tensor.fft` DECLARED (the 21 declare mold). **`ceir.linalg.gemm`** (`linalg.ceirop.toml` → opgen → `linalg_ops.*` + hand `linalg.hpp`/`linalg.cpp` `find_linalg_misuse` [9 `LinalgMisuseKind`: operand/result Tensor-kind + element consistency + A/B rank≥2 + the trans-applied CONTRACTION + BATCH [right-aligned, static-differ⇒misuse] + result [batch..,M,N] identity + accumulator C.shape==D.shape]) = the BLAS3 identity D=α·op(A)·op(B)+β·C {alpha,beta,trans_a,trans_b}, DISTINCT from 21b `tensor.matmul` (a 3-operand accumulating/scaled/transposed form; matmul==gemm α=1,β=0,no-trans). **`ceir.tensor.fft`** (added to `tensor.ceirop.toml`; the fft branch of `find_tensor_misuse` + 3 NEW `TensorMisuseKind` [FftInputShapeMismatch/FftResultShapeMismatch/FftDirectionInvalid] + reused AxisInvalid/element/kind) = a c2c FFT, complex = SPLIT re/im (2 real f32 tensor operands → 2 results) MIRRORING the CKIR c2c split-buffer ABI (`ckir_fft.hpp` in_re/in_im/out_re/out_im); NO complex element TypeKind (a true complex `TypeKind` = Dxxx name-forward). ⭐ fft is the FIRST fixed 2-result op in these dialects — a `[fft][linalg]` ROUND-TRIP test PROVES it crosses BOTH serializers byte-clean (the printer emits the SINGLE create_operation result type, the parser reads N result SSA ids + that one type; gemm rides along for Float/Bool attr printing) → this UNBLOCKS 22c's TEXT-asset path. ⛔ the im_out-specific verifier lines are DEFENSIVE-redundant under the single-result-type model (re_out≡im_out by construction; live only for a future per-result-type path / a corrupted blob — no independent malformed test constructible; documented in code). **GATE ×4 configs GREEN** (win-debug/asan + linux-gcc-debug/asan: `[linalg]` 3 + `[tensor][fft]` 3 [well-formed incl. trans_a+trans_b+batch / rejects-every-kind / Unknown-defer / round-trip] = 6 hand + gen-smoke [linalg 3 + tensor 3, fft build-through] + `ceir.21` 9 RE-RUN not inherited [find_tensor_misuse changed]) + opgen drift/validator + gcc `-Werror=switch` (linalg 9-case + tensor +3 fft cases) + tidy-20 (6 files) + `crd-ceir-invariants`. ⛔ advisor-at-the-gate caught + I fixed 3: (1) the ceir.21 greens were INHERITED (find_tensor_misuse changed) → re-ran ceir.21 ×4; (2) `trans_b` had ZERO coverage (the fresh 21x scar) → a well-formed trans_b + a trans_b ContractionMismatch negative; (3) the multi-result serializer was UNPROVEN → the round-trip test + the finding that distinct re_out/im_out types are unconstructible. **⛔ 22b CONTRACT (advisor ruling): the native provider REJECTS non-F32 elements — declare stays element-agnostic (the 21b precedent; the CKIR kernels are F32-only) so the F32 restriction rides 22b WITH A TEST, not a bare forward.** MINIMAL op set held (gemm + fft only; §52 solve/QR/LU/Cholesky/SVD/eigen → ledger L1). NEXT = 22b. commit ㉓. **✅ 22b DONE 2026-08-18** — the CEIR→CKIR NATIVE PROVIDER (`engine/ceir-gpu/{include/crd/ceir/gpu/ckir_synth.hpp,src/ckir_synth.cpp}`): a declare-only ceir.linalg/ceir.tensor op → a CKIR construction, SYNTHESIS-ONLY (`crd-kir` = a PUBLIC header-only edge into ceir-gpu — its FIRST consumer, resolving the ADR-0109 §4.2 "added-when-consumed" dead edge; ceir-gpu STAYS backend-free). ⛔ TWO TIERS (the survey correction): `synth_gemm` + `synth_reduce` = GRAPH tier (`g.input`/`g.contract`/`g.reduce` → `KirBackend{Vulkan,Dx12,Cpu}::run`, Cpu wraps `eval_cpu`); `synth_fft` = KERNEL tier (`build_fft1d_radix2` → `dispatch_kernel_1wg` vs `eval_cpu_kernel`). BIT-EXACT ENVELOPES + TYPED-REJECT (**12** `SynthReject` kinds, never a silent wrong-result subset): gemm = plain contract (α=1∧β=0∧!trans, else GemmEpilogueUnsupported); reduce = {sum,prod,max,min}→KOp (mean → ReduceFnUnsupported); fft = rank-1 pow2 fwd/inv (non-pow2 → FftLengthNotPow2). F32-only (the 22a element-agnostic declare's F32 restriction DISCHARGED HERE with a test — ElementNotF32 ×3). **DEVICE GATES** (`tests/ceir-gpu-{vulkan,dx12}` — they link the backends): each op SYNTHESIZED → run on-device → bit-exact (gemm/reduce) or within a DECLARED 2e-3·maxmag tol (fft, the float-butterfly precedent, NOT a tuned epsilon) vs the tier oracle AND an INDEPENDENT reference (⛔ the 20c-2 guard: eval_cpu runs the SAME synthesized graph, so a Bᵀ·A / max→min / fwd↔inv mis-synthesis matches its own oracle — the triple-loop gemm / 4× serial-reduction / naive-DFT catch it). gemm on the odd 32×48×24 (Naive/bit-exact). **MATRIX ×3 legs GREEN:** win-debug 4070 Ti Vulkan + D3D12 (`ceir.22b` 10/10 = 4 device-free synth + 6 device gates) + Linux lavapipe Vulkan (7/7 extended) + gcc `-Werror=switch` (the 12-case `synth_reject_name`) + tidy-20 (9 files) + `crd-ceir-invariants` (crd-ceir CORE stays host-only despite the ceir-gpu→crd-kir edge) + the touched targets' INHERITED greens RE-RUN (ceir.13 42 + ceir.14 41 + work_smoke 2 + dgc 3, all green). ⛔ REAL BUG the device gate caught: `synth_fft` first used `build_fft1d_batched` (picks radix by log2(n): n=8→radix8) but the twiddle/local_size contract is radix-2 → `eval_cpu_kernel` mis-simulated + HUNG 1500s → fixed to `build_fft1d_radix2` + a device-free `CHECK(local_size==n/2)` pin ([[feedback_ckir_fft_batched_radix_dispatch_breaks_fixed_twiddle_contract]]). ⛔ advisor-at-the-gate caught + I fixed 3 blockers: (1) reduce ran `sum` ONLY (a max→ReduceMin swap passed) → all 4 fns vs DISTINCT serial refs; (2) fft `inverse` had ZERO exec coverage (a distinct kernel path) → fwd+inverse legs (inverse ref = the UNSCALED +i DFT, same fwd twiddles conjugated internally); (3) inherited greens after the lib edge → full re-run of the 3 targets. **DEFERRAL LEDGER (each homed):** gemm epilogue α/β/trans (typed-rejected) → a graph-tier scale/add/transpose slice; batched gemm (rank>2) → named; reduce `mean` → a post-scale slice; fft batched-radix perf-dispatch + Bluestein/non-pow2 + rank>1/axis≠0 → schedule/variant slices; ShapeNotStatic (dynamic dims) → the runtime-shape slice; rank>8 (kir kMaxRank); DetTier::Fast/WarpTiled ULP tier → band-open; the kir-cuda provider leg → band-open L5; the engine-side RUNNER (a live executor walking a module) = 22c's job. NEXT = 22c (the §137 GEMM→FFT→reduction proof asset — composes these 3 providers). commit ㉔. | build §52; §59; corpus §93; proof §137 |
| ✅ | CEIR-23 | `ceir.sparse` + `ceir.quant` → sparse op + quantized-MLP path · reuse: hesap sparse formats, v14-m Q8 kernels · **BAND ✅ 2026-08-18** (23a-e + 23z; quant+sparse dialects + fused QuantGemm + the quant-MLP crown + CSR SpMV, cross-vendor Vk/DX12/lavapipe; close: `docs/sessions/2026-08-18-ceir-23z-band-close.md`) | §53 §54 |
| ✅ | CEIR-24 | **BAND ✅ 2026-08-18** — `ceir.ml` (ml.mlp variadic + ml.attention SDPA) declared + `find_ml_misuse`; the CKIR EXPANSION provider (`expand_ml_ops`: ml.mlp→gemm/relu, ml.attention→transpose.ckir→gemm→softmax.ckir→gemm) device-resident on Vk+DX12+lavapipe (ZERO new StageKinds); the §69 partitioner (`partition_ml`, availability=caps INPUT, inspectable) + the coopvec NATIVE MLP CLAIM (VK_NV_cooperative_vector, NVIDIA-gated) + the §136 CROWN (ONE ml.mlp, TWO partitions — CKIR-expand f32 vs coopvec-native fp16 — same numbers on the 4070 Ti). Baked-kernel shape mismatch = a TYPED reject (`BakedKernelShapeUnsupported`, advisor-caught at close). Assets transpose/softmax.ckir + attention.ceir (bootstraps deleted; reading+anti-drift gates). Sweep GREEN 5 legs (win-debug/release/asan + WSL-gcc -Werror + lavapipe). Close record → `docs/sessions/2026-08-18-ceir-24z-band-close.md` (row-per-claim + row-per-config + zero-builder audit + 8-item deferral ledger + scars). `ceir.ml` + provider partitioning (absorbs MLR) → MLP + attention via CKIR AND a native-provider strategy · reuse: NRC fused MLP (2.37× cuBLAS), C6 coopvec. **BAND-OPEN LOCK (advisor 2026-08-18)** — reads DONE (§69 provider-advertise+partitioner-assign · §136 one-region-≥2-strategies · §138 MLP-then-attention/high-level/fusion/quantized/coopvec-where-avail/CKIR-fallback/native-where-avail · MLR = no roadmap §-obligation · kir-cuda L5 NOT homed to 24 ⇒ CUDA native = LEDGER row). **ARCHITECTURE = expand-vs-claim:** the CKIR provider EXPANDS an ml op into the proven 22/23 vocab (ml.mlp→dequant/gemm/relu; ml.attention→gemm(QKᵀ)+softmax `compute.dispatch`+gemm(·V)) consumed by the EXISTING `plan_tensor_pipeline` (ZERO new StageKinds — relu's VizDispatch precedent); the native provider CLAIMS the op WHOLE (coopvec MLP); the partition is INSPECTABLE data (§137 visible-plan doctrine → §69). **Decomposition:** 24a declare `ml.mlp`+`ml.attention` (SDPA single-head) + `find_ml_misuse` (width-chain via .count + activation vocab {relu}) → 24b the CKIR EXPANSION provider + `softmax.ckir` (rowmax-stable; ⛔ verify KOp::Exp wired eval+both emitters; bit-exactness DIES at exp ⇒ DERIVED tol, fft-2e-3 mold) + Vk/DX12 gates BOTH ops → 24c the §69 partitioner (advertise/assign/inspectable + can't-claim→CKIR-fallback negative) + coopvec native MLP + CROWN (§136 ONE module, TWO partitions == within tol) → 24z close. **Backend matrix:** CKIR path Vk+DX12+lavapipe; coopvec native NVIDIA-capability-GATED (honest scoreboard, lavapipe lacks it). **Boundary:** ml = FULL MLP/attention (NOT 23c's Q8 layer); training/autodiff = CEIR-25 (§56/§57 hard boundary, ops Pure/mode-free). **Ledger (homed):** native attention · CUDA/kir-cuda provider · mask/causal/multi-head/KV-cache · MoE/conv/embedding/norm/sampling/recurrent · gelu+other activations · training/autodiff→25. Advisor again at 24b + 24c gates. | build §55 §56; §69; corpus §94; proof §136 §138 |
| ✅ | CEIR-25 | `ceir.autodiff` as compiler transform → differentiable compute/render w/ custom VJP + checkpointing · reuse: v15/v16 AD architecture + graph_ad IR. **BAND-OPEN LOCK (advisor 2026-08-18):** `build_gradient(ctx, m, loss, wrt[])` in ceir-gpu differentiates the HIGH-LEVEL vocab (linalg.gemm/tensor.reduce/ml.mlp) BEFORE expansion into ONE module — backward ops REFERENCE forward SSA values (planner materializes intermediates as buffers; "store" free; checkpointing=a later recompute transform) + emit in the EXISTING vocab (ZERO new StageKinds, the 24b echo — ⛔ SHIPPED FALSE, struck at 25z-1: 25b-3 APPENDED StageKind::{Transpose,Broadcast,Elementwise}; the OPS are existing [tensor.hpp:4-5] but the PLANNER gained 3 stages). ⛔ `MissingVjp` TYPED reject DAY-ONE (unregistered op ≠ zero-gradient — registered-default-empty scar in AD form; negative-gate SAME slice). ⛔ promote baked transpose.ckir → cook-time `synth_transpose` (ckir_synth, the synth_gemm mold) since gemm VJP dA=dC·Bᵀ/dB=Aᵀ·dC emits per-shape transposes — RETIRES the 24z dimension-general ledger row. Loss=tensor.reduce(sum); seed=caller-uploaded ExternalIn (quant/softmax-scale precedent). Oracles: hesap-autodiff `nn_reverse.hpp` (analytic) + `gradient_check.hpp` (central-diff, its tol). §139 crown = differentiable COMPUTE (differentiable RENDER=ledger). §56/§57 HARD boundary — autodiff is a TRANSFORM not a runtime mode; forward ops stay Pure. **Decomposition:** 25-0 ✅ (reads + synth_transpose decision, 2026-09-04): oracles CONFIRMED — analytic VJPs in `hesap-autodiff/nn_reverse.hpp` (matmul_vjp `gA=gC·Bᵀ,gB=Aᵀ·gC`; relu/bias/softmax/layernorm/conv) + the 3-way `gradient_check.hpp` (analytic Jet / complex-step 1e-30 / central-FD) + `einsum_reverse.hpp` (general permuted-spec VJP). ⭐ SANITY#8 REUSE: `synth_transpose` is NOT a from-scratch generalize of baked `transpose.ckir` — `crd::kir::build_transpose2d` (ckir_fft.hpp:1462) is an EXISTING dimension-general coalesced R×C→C×R transpose KERNEL (oracle-tested). DECISION: `build_gradient` emits HIGH-LEVEL `tensor.transpose`+`linalg.gemm`(+`tensor.reduce`) — existing vocab, ZERO new StageKinds [⛔ struck 25z-1: 3 new PLANNER StageKinds landed at 25b-3, ops unchanged]; 25b `synth_transpose` = kernel-tier synth (synth_fft mold) WRAPPING `build_transpose2d` (retires 24z baked-transpose ledger). ⚠ 25b risk: `build_transpose2d` needs `tile | rows,cols` → non-aligned shapes need a divisor/padded/tile=1 fallback (gate covers) → **25a** (advisor-refined 2026-09-04, SPLIT): **25a-1** registry (⛔ KEY-TYPE decision at tick top: op-name vs op-name∪kernel-symbol — the latter makes an authored `relu.ckir↔relu_vjp.ckir` a DECLARED differentiable PAIR, the asset-driven way any kernel becomes differentiable; advisor-with-choice) → DECIDED op-kind∪kernel-symbol (two-map registry); `grad.hpp` HEADER ✅ 2026-09-04 (advisor-blessed: rules read forward values through `fwd` SSA only [§57, why a raw fn-ptr suffices]; compute.dispatch resolves ONLY via the kernel map [absent kernel attr ⇒ nullptr→MissingVjp, no generic-dispatch VJP]; `grads`=`Span<Value*>` carries its size; two-consumer adjoints SUM via `tensor.elementwise{add}`). .cpp ✅ 2026-09-04 (`vjp_gemm` [2 tensor.transpose perm=[1,0] + 2 plain linalg.gemm; operand_adjoints[2]=null β·C], two-map `lookup` [compile-time `fnv1a_ct("compute.dispatch")` detect → kernel-map-only, const-safe], `register_builtin_vjps`, `grad_error_name`; crd-ceir-gpu builds win-debug /W4 /WX — a real fnv1a_64 1-arg-overload miss caught+fixed at the build). **25a-1 ✅ CLOSED 2026-09-04** (test_grad.cpp: STRUCTURAL [2 tensor.transpose+2 linalg.gemm, dA=[M,K]/dB=[K,N], operand_adjoints[2]=null, find_tensor/linalg/structure None] + PARTIAL numeric [synth_gemm→eval_cpu == `nn_reverse::matmul_vjp` @tol 1e-6] + `MissingVjp` negatives [unregistered op; dispatch w/ unregistered kernel; dispatch w/ no kernel attr]; GATE GREEN: win-debug 45/45 + WSL-gcc 13.3.0 `-Werror` 45/45 + tidy-20 clean; advisor-blessed). ⛔ SCOREBOARD CORRECTION (advisor, verified not inherited): backward ops are in the existing DIALECT vocab, but `tensor.transpose` is NOT in the PLAN vocab — only synth_gemm covers the gemms (that's WHY the numeric is PARTIAL) → plan-layer coverage of `tensor.transpose` (synth_transpose + plan_tensor_pipeline routing) = 25b; do NOT claim 'zero new StageKinds' until 25b proves it. **25a-2 CODE ✅ 2026-09-04** (advisor-refined): `vjp_reduce` (sum → reshape[keepdim]+broadcast, dodges the 1D right-align scar; non-sum=`ReduceFnUnsupported`) + `build_gradient` orchestrator (reverse-topo snapshot walk, leaf-skip, value→adjoint map, `tensor.elementwise{add}` two-consumer accumulation, seed=caller-uploaded ALL-ONES ExternalIn). ⛔ advisor BLOCKERS fixed same tick: (1) `MissingVjp`/`ArityUnsupported` are PURE rejects via a dry pre-pass (module byte-identical on reject — the expand_ml "reject before emit" precedent); (2) fixed radj/oadj caps → `ArityUnsupported` typed reject, never silent adjoint truncation. crd-ceir-gpu builds win-debug /W4 /WX. **25a-2 ✅ CLOSED 2026-09-04** (test_grad 5 cases / 89 assertions: build_gradient on gemm(A,A)→reduce(axis=1,sum) — STRUCTURAL [seed+reshape+broadcast+2 transpose+2 gemm+elementwise{add}; grads[0]=add of the 2 bwd gemms; anchor gone (5 declares not 6); verifiers None] + PARTIAL numeric [2 bwd gemms == matmul_vjp ga/gb @1e-6; ⛔ seed→dC path is BY-CONSTRUCTION (dc=all-ones), full-chain eval=25b] + NEGATIVES [unregistered reduce→MissingVjp + BLOCK op-count unchanged=pure pre-pass; reduce{max}→ReduceFnUnsupported]; GATE GREEN win-debug 89/89 + WSL-gcc 13.3.0 89/89 + tidy-20; advisor-blessed). ⛔ AMEND (25b-1 found): the "89/89 win-debug" was a TAG-FILTERED exe run — the ctest-by-name path CI uses was BROKEN for the 25a-2 case (an em-dash in the TEST_CASE name mojibaked under Windows ACP → `No test cases matched` → Failed), invisible to the tag filter; FIXED at 25b-1 (em-dash→hyphen). ⛔ PROCESS: per-tick win gate = `run-ctest.bat <dir> <regex>` BY-NAME, never `exe "[tag]"` ([[feedback_ascii_only_test_names]]). ⛔ NAMED 25b/25c RISK (advisor): the pre-pass reachability ignores per-operand differentiability → a COMPUTED β·C operand (not a fresh leaf declare) can FALSE-REJECT a valid module; FIX = rules declare a `differentiable_operand_mask` at registration, consulted by the pre-pass — land when a forward corpus feeds a computed C. **✅ 25a COMPLETE.** **25b-0 ✅ INVENTORY 2026-09-04** (advisor-mandated, before code): the 4 backward op kinds vs plan+synth coverage — **reshape** ROUTES today (tensor_pipeline.cpp:274 zero-copy Alias, no synth); **transpose/broadcast/elementwise{add}** → PlanReject::UnsupportedOp (not in pipeline vocab) + no ckir_synth. ⭐ KEY FINDING (overturns 'transpose is the only gap' AND the kernel-tier fear): ALL THREE map to EXISTING graph-tier KGraph nodes — `g.permute(a,[1,0])`=KOp::Permute (ckir.hpp:1625), `g.broadcast`=KOp::Broadcast (:1631), `g.binary(KOp::Add)` (:1204) — SAME graph tier as contract/reduce, NO tier-mixing, NO build_transpose2d kernel-tier detour, all eval_cpu-backed (layernorm/softmax use them); the ckir_synth.hpp:13 'no 2D-transpose graph node' was about the GEMM-EPILOGUE mapping, not the KGraph. ⛔ EMITTER CHECK (advisor-mandated, VERIFIED not assumed): **eval_cpu ✅** handles Permute (ckir_eval.hpp:207-212) + Broadcast (:213-218) + Reshape (:206) as full root nodes; **device emitter ✗** — only emit_contract_glsl/emit_reduce_glsl (Vk backend_vulkan.cpp:192,204) + emit_contract_hlsl (DX12) exist; Broadcast appears ONLY as a FUSED contract epilogue (ckir_glsl.hpp:2727,2823), Permute/Reshape absent as roots. ~~So a STANDALONE transpose/broadcast/elementwise-add output node has NO device codegen~~ ⛔ CORRECTED at 25b-2a: **elementwise HAS a device emitter** — the FUSED-elementwise cone emitter (emit_elementwise_glsl/hlsl; is_fusable at ckir_glsl.hpp:76,79 covers Add/Sub/Mul/Div/Max/Min/Pow) reached by run()'s fall-through (backend_vulkan.cpp:286 / backend_dx12.cpp:265) on EVERY backend; the 25b-0 grep looked for Broadcast/Permute ROOTS and never checked the fused-elementwise path (2nd inventory miss from grepping the EXPECTED node, not the CONSUMING code path — the recurring lesson). Only PERMUTE + BROADCAST lack root emitters → the emitter subslice is needed for THOSE TWO only (the '3 wraps in one tick' plan was wrong). 25b DECOMPOSES (4): **25b-1** = synth_transpose/synth_broadcast/synth_elementwise into ckir_synth (graph-tier wraps of g.permute/g.broadcast/g.binary, shared synth_gemm mold, device-free gate eval_cpu-vs-oracle numeric + typed rejects — ⛔ synth_elementwise MUST typed-reject shape-mismatched operands [g.binary takes shape from `a`, the bin-bcast OOB scar] AND map the FULL binary vocab add/sub/mul/div→KOp, unknown fn→reject); **25b-2** = device codegen for the backward vocab: **25b-2a** elementwise DEVICE GATE (emitter already exists — SANITY #8) + **25b-2b** ✅ emit_broadcast_nd+emit_permute for BOTH GLSL & HLSL (+uint suffix — emitters-lag-wire-both) + both run() branches + both device gates; **25b-3** ✅ plan_tensor_pipeline StageKind::Transpose/Broadcast/Elementwise routing (device-free plan + typed reject); **25b-4a** ✅ wired the 3 kinds into BOTH device-test resolvers (StageResolveFn — test-side only, NO engine default) + a multi-stage device pipeline gate (transpose‖broadcast→elementwise chain) on Vk+DX12+llvmpipe; **25b-4b** = backward-MLP end-to-end Vk+DX12 vs analytic (nn_reverse) + numerical (gradient_check) — the full-chain dC close 25a-2 deferred + the pre-pass β·C risk's first real corpus.

**25b-1 ✅ CLOSED 2026-09-04** (ckir_synth.{hpp,cpp} + test_ckir_synth.cpp): synth_transpose (g.permute), synth_broadcast (g.broadcast), synth_elementwise (g.binary, FULL vocab add/sub/mul/div/max/min/pow) — graph-tier wraps in the synth_gemm mold; 4 typed rejects appended (TransposePermInvalid, BroadcastShapeUnsupported [same-rank envelope, the reshape-first canonical form], ElementwiseFnUnsupported, ElementwiseShapeMismatch [⛔ the bin-bcast OOB scar guard — g.binary is same-shape]). GATE: eval_cpu-vs-oracle NUMERIC — transpose [2,3]→[3,2], broadcast BOTH axes ([3,1]→[3,4] last + [1,4]→[3,4] first, the two branches of eval_cpu:217), elementwise add/sub/mul exact; div/max/min/pow proven TO MAP (reject None) not numerically; ⛔ device parity for ALL = 25b-4. NEGATIVES: every reject exercised (incl. shape-mismatch on a verifier-legal broadcast/[2,1] operand). GREEN win-debug run-ctest BY-NAME 25b-1 3/3 + full 22b|25a|25b 8/8 (incl. 22b Vk+DX12 device gates) + WSL-gcc 13.3.0 175 assertions/12 cases + tidy-20 clean (ckir_synth.{cpp,hpp}, test_ckir_synth.cpp). NEXT = **25b-2** (GLSL+HLSL emitters for permute/broadcast/elementwise): ⛔ wire BOTH backend dispatch sites (backend_vulkan.cpp:192-204 contract/reduce→emit_*_glsl-else-fail; backend_dx12.cpp:182) or `run` rejects the graph (emitters-lag-wire-both scar); fill GlslKernel n_inputs + input_iidx[] in iidx order (backend checks kern.n_inputs != n_inputs at :192); DESIGN Q first line = ONE generic gather emitter vs three (permute/broadcast/binary are all "one thread/output-elem: unflatten→per-input index-map→load→op→store"; eval_cpu:206-218 is the spec) — read emit_reduce_glsl skeleton (ckir_glsl.hpp:2581) BEFORE deciding

**25b-2a ✅ CLOSED 2026-09-04** (test_ceir_elementwise_{vulkan,dx12}.cpp — DEVICE gate, NO production code: SANITY #8, the emitter already exists): synth_elementwise output (a KOp binary of two Inputs) runs on a REAL device via the EXISTING fused-elementwise emitter (emit_elementwise_glsl/hlsl reached by run()'s fall-through), proven vs BOTH eval_cpu (KirBackendCpu = f32-faithful, backend.hpp:36) AND an INDEPENDENT scalar ref (the fn→KOp 20c-2 guard) for the FULL vocab {add,sub,mul,div,max,min,pow}. ⛔ PER-OP contract (advisor): add/sub/mul/div/max/min BIT-EXACT (`==`; div exact for b∈{1,2}=exact reciprocal); pow tol DECLARED (1e-5) but NOT exercised — data-exact in practice (b∈{1,2}: pow(a,1)=a, pow(a,2)=a² both f32-exact; a genuinely-inexact pow needs non-integer b = a kir-level emitter test, not a synth gate). GREEN win-debug Vulkan #4714 + DX12 #4790 (real devices) + WSL-gcc llvmpipe (2149 assertions/4 cases, elementwise ran not skipped) + tidy-20 clean. NEXT = **25b-2b** (broadcast + permute root emitters): ⛔ BEFORE writing grep EVERY backend_*.cpp for an existing KOp::Broadcast/KOp::Permute root consumer + read graph_uses_vec (ckir_glsl.hpp:274); then emit_broadcast+emit_permute GLSL+HLSL (baked static-shape index math via app_uint — the uint-suffix scar), slot both run() branches BEFORE the fused fall-through (like Gather at :215), both device gates (rank-2 [1,0] AND a rank-3 non-trivial perm — prove the emitter not just the shape). MOLD = emit_gather_glsl (ckir_glsl.hpp:2652) + its run() branch (backend_vulkan.cpp:215) + DX12 twin (backend_dx12.cpp:199) — the three-file gather mold (baked-vs-push index math); ⛔ check GlslKernel in_comps/out_comps (dispatch reads them at backend_vulkan.cpp:259 — a bare emitter leaving them zero may break vec-aware byte math on one backend); 25b-4 ref from hesap-autodiff (genuinely dialect-independent), not the CEIR fn definitions

**25b-2b ✅ CLOSED 2026-09-04** (ckir_glsl.hpp/ckir_hlsl.hpp NEW root emitters + backend_vulkan.cpp/backend_dx12.cpp run() wiring + test_ceir_bcast_perm_{vulkan,dx12}.cpp): emit_broadcast_nd_{glsl,hlsl} + emit_permute_{glsl,hlsl} — SHARED baked static-shape index body (glsl_detail::emit_gather_index_body + broadcast_coef/permute_coef; ONE body for GLSL+HLSL = the wire-both scar guard), wired into run() BEFORE the fused fall-through on BOTH backends. GATE: device parity vs eval_cpu AND an INDEPENDENT index-map ref (pure data movement = EXACT `==`). broadcast BOTH axes ([3,1]→[3,4], [1,4]→[3,4]); transpose rank-2 [1,0] AND ⭐ rank-3 [2,3,4] perm=[2,0,1] — the NON-ADJACENT swap DISCRIMINATES coef[k]=in_stride[perm[k]] from a naive in_stride[k] (rank-2 [1,0] passes both formulas; the rank-3 case is the real emitter proof). GREEN win-debug Vulkan #4715/#4716 + DX12 #4793/#4794 + regression 22b 16/16 + WSL-gcc llvmpipe (2272 assertions/6 cases) + tidy-20 clean (my additions + both backends). ⛔ NAME-COLLISION (3rd inventory miss, [[feedback_coverage_inventory_grep_the_consumer_not_the_node]] amended w/ name-grep clause): a pre-existing SCALAR emit_broadcast_glsl (numel==1 dtype-aware, radix fan-out, NOT run()-wired) collided at LINK → renamed mine _nd_, two-way pointer comments. ⛔ DEBT w/ TRIGGER: two Broadcast emitters (scalar dtype-aware direct-call + N-D F32 run()) — UNIFY when an int N-D broadcast has a consumer, not before. ⛔ 3 PRE-EXISTING ckir_hlsl.hpp tidy issues (806/956/964, mesh/node-shader helpers, unrelated to 25b-2b, my additions clean): header IS in CI filter (crd/) but win-tidy CI = NON-fatal warnings → CI green; fatal only under manual tidy-files.ps1. **✅ 25b-2 COMPLETE.** NEXT = **25b-3** (plan_tensor_pipeline StageKind routing + executor): ⛔ widen-enum scar — audit EVERY StageKind consumer (executor per-kind bind/grid/n_out/barrier), not just the plan switch

**25b-3 ✅ CLOSED 2026-09-04** (tensor_pipeline.{hpp,cpp} + test_tensor_pipeline.cpp): StageKind::{Transpose,Broadcast,Elementwise} appended; plan_tensor_pipeline routes tensor.transpose/broadcast (1-in-1-out) + tensor.elementwise (2-in-1-out) as stages, tensor.reshape stays an Alias; synth envelope-check per stage, def-use buffer wiring, n_out=1. ⛔ WIDEN-ENUM AUDIT (advisor): NO exhaustive StageKind switch exists anywhere (execute_tensor_pipeline is GENERIC — resolve()+bind/n_out/barrier; profile copies the value; the device-test resolvers are if/else-if) → widening is safe; gcc -Werror confirmed (no switch-gap). GATE: device-free plan gate — a 3-stage backward-vocab module (transpose+broadcast+elementwise-add+reshape-alias) → stage kinds/nbind/n_out + def-use wiring + broadcast byte-growth (out=2×in) + reshape alias_of; TYPED reject (verifier-legal broadcast-compat [3,2]+[3,1] elementwise → SynthRejected, the same-shape envelope). GREEN win-debug 25b-3 2/2 by-name + tensor-pipeline regression 24/24 (incl. device pipeline) + WSL-gcc -Werror 340 assertions/28 cases + tidy-20 clean. ⛔ RESOLVERS UNWIRED: the new kinds route in the PLAN, but the device-test StageResolveFn (test_ceir_pipeline_{vulkan,dx12}.cpp:127-168, if/else-if, NO engine default) fall through to UnresolvedKernel for them → 25b-4a wires them (⛔ emit_elementwise_glsl needs a scratch alloc, :113 signature). NEXT = **25b-4a** (resolvers + multi-stage device gate) → **25b-4b** (backward-MLP e2e) + negative gate + gemm VJP rule (emits `tensor.transpose`+`linalg.gemm`); gate = STRUCTURAL (exact ops/wiring/result shapes dA=[M,K],dB=[K,N]) + PARTIAL numeric (test CPU-transposes operand, synth_gemm→eval_cpu == `nn_reverse::matmul_vjp`) — FULL numeric closes 25b. **25a-2** reduce(sum) VJP (→broadcast/seed) + seed `ExternalIn` + a two-op chain. ⛔ VERIFY before 25a closes: `tensor.transpose` was NEVER plannable (only expand_ml's baked dispatch) — cross-check every backward op vs `plan_tensor_pipeline` StageKind + `ckir_synth`; if a transpose planner stage is needed, CORRECT the 'ZERO new StageKinds' claim (source=scoreboard). relu-in-MLP backward = opaque dispatch ⇒ resolved by the 25a-1 key-type choice → 25b synth_transpose + backward-of-MLP end-to-end Vk+DX12 vs analytic+numerical → 25c custom composite ml.mlp VJP (§136-style two-strategies: custom vs differentiate-parts AGREE) + checkpointing (minimal-or-ledgered) → 25z close. **Ledger (homed):** differentiable render (frame/raster VJPs) · attention backward (softmax VJP) · opaque post-expansion dispatch VJPs · training/optimizer loop (§56, ops Pure) · coopvec on-device train. Advisor again at 25b + 25c.

**25b-4a ✅ CLOSED 2026-09-04** (test_ceir_pipeline_{vulkan,dx12}.cpp — TEST-ONLY; the executor is generic, no engine production code changed): wired StageKind::{Transpose,Broadcast,Elementwise} into BOTH device-test StageResolveFn if/else-if chains (synth_transpose/broadcast/elementwise → emit_permute / emit_broadcast_nd / emit_elementwise[takes res.alloc scratch, :113 sig]; grid=ceil(out_numel/256), PC{nout}, nbind from the plan) + a MULTI-STAGE device gate: in0[2,3]→transpose[1,0]→t[3,2]; in1[3,1]→broadcast→bc[3,2]; elementwise-add(t,bc)→e[3,2] — 3 NEW-kind stages in ONE submit via run_quant_module, device out == independent index-map ref `e[r,c]=in0[c*3+r]+in1[r]` (EXACT). GREEN win-debug Vulkan #4729 + DX12 #4804 (2/2 by-name) + regression 18/18 pipeline device tests (resolver additions break nothing) + WSL-gcc llvmpipe (2951 assertions/12 cases, [ckir-synth],[tensor-pipeline]) + tidy-20 clean (both files). ⛔ SCOPE (advisor): this proves the resolver + the plan's def-use wiring + n_out REACHED and were CONSUMED by the generic executor — it does NOT discriminate barrier correctness (stages 0/1 are independent, 6 floats too small to race a missing ShaderWrite→ShaderRead; same shape as the 22c-2 chain). Do NOT claim barrier proof. ⛔ NOTE: `run_quant_module` is now the GENERIC pipeline harness under a quant name (4 non-quant callers) — rename is not this slice. NEXT = **25b-4b** (backward-MLP e2e) — STEP 1 (device-FREE, catches routing gaps before a device is touched): plan the 25a-2 corpus `f(A)=sum(gemm(A,A),axis=1)` via build_gradient, read the stage list, CONFIRM (a) the gemm VJP's explicit `tensor.transpose` routes as a Transpose stage feeding gemm operand-1 (synth_gemm trans_a/trans_b never set → fine), (b) plan-finalize marks the LAST backward op = `grads[0]` (the dA elementwise-add for gemm(A,A)) as BufferRole::Output = the readback target — verify emit order or the readback is wrong, (c) stage count ~9; STEP 2 device numeric on Vk+DX12+llvmpipe vs a ref computed with hesap `nn_reverse::matmul_vjp` (ga+gb summed for gemm(A,A), dC=broadcast(reshape(dLoss=all-ones))) + a `gradient_check` FD witness on the C++ lambda `f(A)=sum(gemm(A,A))` — ref from hesap fns, NEVER hand-computed (dialect-independence). Split 4b at 'plan+readback confirmed' / 'device numeric' if one tick can't hold both.

**25b-4b STEP 1 ✅ CLOSED 2026-09-04** (device-FREE plan gate: test_tensor_pipeline.cpp + grad.{hpp,cpp} seed-handle API + test_grad.cpp lock): the WHOLE backward pass of the 25a-2 corpus (`loss[3]=reduce(gemm(A[3,3],A[3,3]),axis=1,sum)` → build_gradient) PLANS as ONE device-resident pipeline — proven EMPIRICALLY (not asserted from reading). 8 dispatched stages, order pinned: [0]fwd-gemm [1]fwd-reduce [2]broadcast(dC=[3,1]→[3,3]) [3]transpose(Aᵀ) [4]transpose(Aᵀ) [5]gemm(dA0=dC·Aᵀ) [6]gemm(dA1=Aᵀ·dC) [7]elementwise-add(dA=dA0+dA1)==grads[0] (reshape seed[3]→[3,1] is a zero-copy ALIAS, not a stage). CONFIRMED: (a) explicit transpose materializes + FEEDS backward-gemm operands via DEF-USE (dA0.B==transpose[3].out, dA1.A==transpose[4].out; Gemm binds {A,B,D}, C dropped at β==0); (b) READBACK = plan-finalize marks the FINAL stage's trailing write Output = grads[0] AND it is the ONLY Output buffer (the assertion that guards a future emit-order regression); (c) the reduce-VJP reshape ALIASES the caller-uploaded seed. ⛔ ADVISOR-CAUGHT GAP FIXED (blocked STEP 2 + made (c) non-discriminating): grad.hpp promised a "caller-provided seed" but build_gradient created it internally with NO handle → added `Value* seed` to GradResult (set right after mk_decl), so an executor can NAME the buffer to upload ALL-ONES into; (c) tightened to `alias.alias_of.value == gr.seed` (DISCRIMINATING — was any-ExternalIn). GREEN win-debug STEP-1 test #821 + [autodiff]+[tensor-pipeline] 344 assertions/25 cases + WSL-gcc -Werror 344/25 + tidy-20 clean (grad.hpp/cpp + both tests). NEXT = **25b-4b STEP 2** (device numeric on Vk+DX12+llvmpipe) — PRE-FLIGHT GREPS before device code: ⛔ #1 seed handle = DONE (this tick); #2 how do run_quant_module / execute_tensor_pipeline map caller arrays onto ExternalIn buffers? (positional-over-ALL-ExternalIn in plan order [supply 5 arrays, 3 dead: Carg/c0/c1] vs by-Value [need gr.seed] vs bound-only) — grep the upload loop for `BufferRole::ExternalIn`; #3 has the device Reduce stage (emit_reduce_glsl) EVER run a PARTIAL-axis reduction? every prior pipeline reduce is rank-0 full — a partial-axis [3,3]→(keepdim)[3,1] dead forward stage is the LIKELIEST first STEP-2 failure (shader-compile/dispatch on a stage nobody reads); grep emit_reduce_glsl callers + reduce device gates for a non-full mask. Ref (STEP 2): hesap nn_reverse::matmul_vjp (ga+gb for gemm(A,A), dC=all-ones) + gradient_check FD on `f(A)=sum(gemm(A,A))` — hesap fns, NEVER hand-computed. ⛔ NOTE: synth_reduce silent-accepts a keepdims [3,1] output vs the op's declared [3] (same numel, verify-first covers it) — comment when touching the resolver.

**25b-4b STEP 2 ✅ CLOSED 2026-09-04 → ✅✅ 25b-4 COMPLETE → ✅✅ 25b COMPLETE** (test_ceir_pipeline_{vulkan,dx12}.cpp + both CMakeLists link crd-hesap-autodiff): the WHOLE backward pass of `sum(gemm(A,A))` — build_gradient → plan → execute_tensor_pipeline — runs DEVICE-RESIDENT (ONE submit, 8 stages, intermediates GpuOnly) on ALL THREE device paths and matches a DIALECT-INDEPENDENT ref. PRE-FLIGHT greps (advisor STEP-2 gate, resolved BEFORE device code): #2 run_quant_module seeds ExternalIn BY SSA-VALUE identity (QuantSeed list) + unmatched externals auto-zero → seed {a, A} + {gr.seed, all-ones}; the dead β=0 carg/c0/c1 auto-zero; readback grads[0] by-Value; #3 partial-axis reduce (mask 1<<1 on rank-2) is DEVICE-PROVEN (the 22b reduce gate runs [6,8] axis=1 bit-exact) → the dead forward reduce executes cleanly. REF = hesap `nn_reverse::matmul_vjp(A,A,dC=all-ones).ga + .gb` (grads[0] = d sum(C)/dA), itself FD-cross-validated (`gradient_check::grad_fd` on `f(A)=sum(gemm(A,A))`, both f64, tol 1e-5) BEFORE it judges the device — never hand-computed; device f32 vs ref tol = 1e-4 relative (DERIVED: 3-elt f32 dot, |ref|~O(6)). GREEN win-debug Vulkan #4731 + DX12 #4807 + WSL-gcc llvmpipe (25b-4b 24 assertions/1 case — device path RAN, not skipped; [tensor-pipeline] 703 assertions/7 cases regression) + pipeline regression 17/17 BOTH backends + tidy-20 clean. ⛔ SCOPE (advisor, scoreboard): the all-ones dC (what a reduce(SUM) VJP produces) makes `dC·Aᵀ` and `Aᵀ·dC` share structure → this corpus proves the reverse-pass TOPOLOGY (a dropped transpose IS caught) but a NON-UNIFORM dC (a weighted/composite loss = 25c's corpus) discriminates the transpose harder. ⛔ DEBT w/ TRIGGER: `SumGemmAA` FD functor is duplicated verbatim across the 2 device TUs — hoist to tests/gpu-shared/ when the THIRD FD functor appears (same mold as the run_quant_module dup). NEXT = **25c** (custom composite ml.mlp VJP + checkpointing): §136 two-strategies — register_kernel relu↔relu_vjp (custom) vs differentiate-parts (expand then build_gradient) must AGREE + checkpointing (minimal-or-ledgered); ⛔ advisor at 25c OPEN before choosing the corpus (the loss SHAPE matters — pick a non-sum/weighted loss so dC is non-uniform). Then → **25z** band close.

**25c OPEN 2026-09-04 (scoping + design lock, advisor-mandated at open):** ⛔⛔ LOAD-BEARING FINDING (advisor-confirmed): "differentiate-the-EXPANSION" is BLOCKED — expand_ml.cpp:139-142 emits relu as a RESULTLESS `compute.dispatch(@relu, {h, relu_out} r,w)` (relu_out a fresh resource.declare); build_gradient's reverse walk keys on `op->result()` (grad.cpp:281, nres==0 ⇒ on_path false ⇒ skipped) so a resultless dispatch is INVISIBLE to the def-use walk. ⛔ REFRAME (source=scoreboard, NOT the naive tracker text): the "§136 two strategies that AGREE" are (A) CEIR custom composite `register_op(ml.mlp, vjp_mlp)` — differentiates the COMPOSITE before expansion (ml.mlp HAS a result; vjp_mlp wires the backward gemm-VJP chain + relu-VJP dispatches EXPLICITLY, no def-use walk needed) → plan → device → grads; vs (B) hesap `nn_reverse` (relu_vjp + matmul_vjp composed by hand in f64) FD-cross-validated via grad_fd on `f(W)=loss(mlp(x,W))`. (A)=CEIR, (B)=hesap — the dialect-independence. ⛔ LEDGER: differentiate-the-expansion needs either a RESULT-producing relu (tensor.elementwise can't — its fn vocab is add/sub/mul/div/max/min/pow, NO comparison, so relu' has no tensor form) OR build_gradient reading `w` bindings as pseudo-results — name-forward. relu_vjp FORMULATION (verified): `gx = Select(CmpGt(x,0), gy, 0.0)` — CmpGt gives EXACT `x>0?1:0` (matches hesap strict `>0` at x==0 where Step differs); CmpGt+Select BOTH wired in emit_compute_kernel_{glsl,hlsl} (bool-typed cmp → Select consumes the bool). CORPUS: `loss=sum(mlp(x[8,4],W1[4,4],W2[4,4]) ⊙ M)` — weighted mask M (ExternalIn) ⇒ dOut=M NON-UNIFORM (fixes the 25b-4b all-ones-dC scope gap); x[8,4] so M·hidden=8·4=32 dodges relu.ckir's BAKED local_size=32 (else BakedKernelShapeUnsupported). DECOMPOSITION (one tick each): **25c-0** author relu_vjp.ckir via BOOTSTRAP (build_relu_vjp KGraph: buffer_decl x@0/gy@1/gx@2, builtin LocalInvocationIndex, const 0, CmpGt, select → stmt_buffer_store; local_size=32) → ckir_write → eval_cpu_kernel-verify vs hesap relu_vjp → commit .ckir + READING gate + device gate (Vk+DX12) → DELETE bootstrap; register_kernel("relu", vjp_relu_dispatch) in register_builtin_vjps → **25c-1** vjp_mlp rule (register_op ml.mlp) + structural gate (op counts, dW shapes [4,4]) device-free → **25c-2** device numeric crown Vk+DX12+llvmpipe vs (B) hesap + grad_fd → **25c-3** checkpointing = LEDGER (vjp_mlp needs h_i_pre = the forward gemm result, an SSA value the planner already materializes; recompute = a 2nd gemm, strictly worse here) with trigger "when a forward activation exceeds a plan-visible budget".

**25c-0 ✅ CLOSED 2026-09-04** (assets/ckir/relu_vjp.ckir + test_ckir_asset.cpp bootstrap generator + reading gate): authored relu_vjp.ckir via the BOOTSTRAP idiom — a HIDDEN `[.emit-relu-vjp]` generator (build_relu_vjp KGraph → ckir_write → file; KEPT in-tree, the [.emit-fft-cuda] regen idiom reconciling mandate-#1 with regen-in-tree) + a permanent READING gate (ckir_read the COMMITTED file → eval_cpu_kernel: gx==(x>0)?gy:0 EXACT over 32 neg/zero/pos threads, x∈{-1,0,2}/gy=i/32 f32-exact so the F32 store round-trips). Formulation `Select(CmpGt(x,0), gy, 0.0)` — CmpGt serializes dtype=Bool, gives EXACT strict `x>0` (matches hesap relu_vjp at x==0 where GLSL step() differs), consumed by Select. GREEN win-debug reading gate 36 assertions + [asset] regression 542/29 + tidy-20 clean; WSL-gcc reading gate 36 (committed .ckir reads cross-compiler/cross-platform). ⛔⛔ ADVISOR ADJUSTMENTS to the decomposition: (1) `register_kernel("relu", …)` REMOVED — DEAD under the reframe: build_gradient's pre-pass (grad.cpp:236-259) + main walk (272-316) both key on `num_results` (nres==0 for a resultless dispatch ⇒ on_path false ⇒ skipped), so a relu kernel-VJP registry entry has ZERO callers (the registered-default-empty scar in a new form). vjp_mlp emits the relu_vjp dispatch DIRECTLY via an `emit_relu_vjp_dispatch` helper (it knows the activation from ml.mlp's attr). ⛔ grad.hpp:7-9 (the "authored kernel becomes differentiable by DECLARING its VJP-pair via register_kernel") = SUPERSEDED-clause — STRIKE IN PLACE in 25c-1 (register_kernel the API stays; the CLAIM that relu rides it does not). (2) axes PRE-FLIGHT done: `axes` bit0=writable (ckir.hpp:176); relu_vjp n0/n1 are readonly (bit0=0) = the FIRST authored compute kernel with readonly-declared inputs; HLSL emit drops readonly→all-UAV, GLSL emits `readonly buffer` → the standalone device gate is NOT redundant with 25c-2 (a readonly-SSBO codegen a failure would tangle in the full MLP). (3) both resolver symbol→path maps (test_ceir_pipeline_{vulkan,dx12}.cpp:~206-213) LACK `relu_vjp` → `UnresolvedKernel` until added. NEXT = **25c-1 (revised)**: `vjp_mlp` rule (register_op ml.mlp — differentiates the COMPOSITE before expansion) + `emit_relu_vjp_dispatch` helper + STRIKE grad.hpp:7-9 + add `relu_vjp` to BOTH resolver maps + the STANDALONE relu_vjp device gate (Vk+DX12, exercises the readonly-input compute emit) + a device-free structural gate (op counts, dW shapes [4,4]) → **25c-2** device numeric crown Vk+DX12+llvmpipe vs (B) hesap + grad_fd → **25c-3** checkpointing LEDGER. Then → **25z** band close. | build §57; proof §139 |

**25c-1 ✅ CLOSED 2026-09-04** (grad.{hpp,cpp}: `vjp_mlp` + `emit_relu_vjp_dispatch`/`mk_const1`/`mk_dispatch`/`dim_count` helpers + `register_op(ml.mlp)` + GradError widened ×2 + grad.hpp:7-9 STRUCK-IN-PLACE; test_grad.cpp structural gate + 2 typed-reject gates): `vjp_mlp` differentiates the COMPOSITE ml.mlp BEFORE expansion (the resultless-dispatch reframe — ml.mlp HAS a result, so no def-use walk of an opaque relu is needed). Emits INTERIOR recompute (1 gemm `z1` + 1 `@relu` dispatch `h1`; h[0]=x, z_nw=live result never re-derived) + backward (i=nw..1: 4 gemm `dh1`/`dW2`/`dx`/`dW1`, 4 transpose `W2ᵀ`/`h1ᵀ`/`W1ᵀ`/`xᵀ`, 1 `@relu_vjp` dispatch) — for the 2-layer corpus: gemm=5, transpose=4, dispatch=2, ml.mlp=1. `dW1`==grads[0] is the TERMINAL op (the plan's single-Output readback target); `@relu_vjp` reads the PRE-activation `z1` (operand(3), the grid+3 binds) NOT post-relu `h1` — the gate ASSERTS `operand(3)==z1->result(0)` (h1>0 ⇔ z1>0 makes the mask identical so a swap passes every NUMERIC gate but violates the contract — identity-not-category). ⛔ TWO typed rejects: `activation != relu` → `MlpActivationUnsupported`; an INTERIOR relu'd layer's `M·hidden != 32` → `MlpBakedShapeUnsupported` (mirrors expand_mlp:120 — relu.ckir/relu_vjp.ckir BAKE local_size=32 with NO bound guard, so a mismatch OOB-writes / leaves an uninitialized tail on device = the cook-only-ships-device-impossible scar; STRUCTURE-clean, DEVICE corrupts). ⛔ WORDING FIX (advisor): the baked-shape reject is a RULE-level reject before emission but NOT byte-identical — build_gradient's seed declare precedes the walk — so the gate ASSERTS the rule emitted NO backward vocab (gemm/transpose/dispatch==0, ml.mlp==1), a CHECKED purity claim not prose; discard the module per grad.hpp's rule-level-reject contract. grad.hpp:7-9 (the SUPERSEDED "authored kernel differentiable by DECLARING its VJP-pair via register_kernel") STRUCK-IN-PLACE (register_kernel API retained; the CLAIM that relu rides it does not — resultless dispatch invisible to the reverse walk). ⛔ ENUM-WIDEN AUDIT: GradError gained 2 members; `grad_error_name` is the ONLY switch consumer (grep-confirmed) + gcc -Werror -Wswitch = no gap. ⛔ ADVISOR (guard correctness): `i<nw` covers exactly the shape set BOTH baked kernels touch (`z_1..z_{nw-1}`); `dim_count`→0 on a dynamic dim rejects (0·x≠32); `nw==1` skips (no relu); placed before `mk_const1` so the rule emits nothing. GREEN win-debug [autodiff] 192/9 (was 186; +2 typed-reject +4 rule-purity) + full device-free suite 1157/87 + WSL-gcc -Werror 192/9 (enum switch cross-compiler) + tidy-20 clean (grad.cpp/hpp/test_grad.cpp). NEXT = **25c-1b** (BEFORE any 25c-2 device numeric): (1) add `relu_vjp` to BOTH resolver symbol→path maps (test_ceir_pipeline_{vulkan,dx12}.cpp ~206-213); (2) STANDALONE relu_vjp DEVICE gate Vk+DX12 — the FIRST authored compute kernel with readonly-declared inputs (HLSL drops readonly→all-UAV; GLSL emits `readonly buffer`), so NOT redundant with 25c-2; (3) ⛔ device-free PLAN gate for the WRITE-THROUGH-DECLARE pattern (advisor #1/#2): `relu_out`/`gx` are `resource.declare` = ExternalIn WRITTEN by a RESULTLESS dispatch then READ by the next gemm — the plan has NO def-use edge dispatch→declare, so correctness rides submit-order + the executor's post-stage barrier (same shape as 24b-4's expanded MLP); assert the PlanBuffer role + that the `@relu` VizDispatch's `w` bind IS that buffer and the `dW2` gemm reads it through `h1ᵀ`'s transpose; (4) decide the TWO-Output readback harness (dW1+dW2, ONE Output slot today) BEFORE 25c-2. Then **25c-2** device numeric crown Vk+DX12+llvmpipe vs (B) hesap nn_reverse (relu_vjp+matmul_vjp composed) + grad_fd → **25c-3** checkpointing LEDGER → **25z** band close. ⛔ MINOR (noted, not a tick): the tanh negative gate reuses the dirty block (a 2nd orphaned seed declare) — harmless (discarded), the baked-shape gate uses a FRESH module. | build §57; proof §139 |

**25c-1b-1 ✅ CLOSED 2026-09-04** (test_ceir_pipeline_{vulkan,dx12}.cpp — TEST-ONLY, no engine production code): the authored `relu_vjp.ckir` runs DEVICE-RESIDENT on BOTH backends + llvmpipe, matching hesap `nn_reverse::relu_vjp`, AND the readonly-input codegen path is proven TAKEN. (1) resolver maps (both TUs): `relu_vjp` → `assets/ckir/relu_vjp.ckir` (additive else-if next to `relu`). (2) STANDALONE gate: hand-built `compute.dispatch(grid×3, x, gy, gx){kernel=@relu_vjp, access="r,r,w"}`, grid=`arith.const{value=1}:index` → `plan_tensor_pipeline` → ONE VizDispatch stage (nbind=3, n_out=1); `x`/`gy` seeded BY-VALUE (QuantSeed), `gx` read back. ⛔ ROLE IDENTITY (advisor, the 25c-1b-2 baseline): asserts `plan.buffers[bind[0]].role==ExternalIn` (x) + `plan.buffers[bind[2]].role==Output` (gx) — gx is a `resource.declare` with NO SSA writer, distinguished as Output ONLY by being the terminal dispatch's trailing WRITE bind (the write-through-declare pattern; without the role check the readback could pick the wrong buffer class). (3) ⛔ READONLY CODEGEN asserted BOTH WAYS (advisor — a device-numeric match alone can't tell "readonly works" from "readonly silently dropped"; identity-not-category): GLSL source HAS `readonly buffer` (x@0/gy@1 axes=0 → `readonly` SSBOs, ckir_glsl.hpp:735 — the FIRST authored kernel with readonly-declared inputs); HLSL source has NO `readonly` + IS `RWByteAddressBuffer` (the ledgered HLSL-drops-readonly→all-UAV, ckir_hlsl.hpp:217). CORPUS: 32 elts (= baked local_size, one workgroup), `x∈{-1,-.5,0,.5,1}` cycling (neg/ZERO/pos — `x==0` is the strict-`>0` discriminator vs `>=`/step()), `gy=2⁻³+i·2⁻⁴` (exact f32), hesap `relu_vjp` f64 over the SAME f32 inputs (pure select ⇒ EXACT `==`). PRE-FLIGHT (advisor): `find_dispatch_misuse`/scan_dispatch validates the kernel is a *SymbolRef* (not against a registry) → a new symbol has NO plan-time reject; the resolver is the ONLY symbol→path map. GREEN win-debug Vulkan #4734 + DX12 #4811 (REAL devices) + full ceir-band device regression 817/817 (the additive resolver entry breaks no existing VizDispatch stage) + WSL/llvmpipe Vulkan #4565 (device path RAN, not skipped) + tidy-20 clean (both TUs). NEXT = **25c-1b-2**: (a) device-free PLAN gate for the write-through-declare pattern on the `vjp_mlp` OUTPUT (plan the 25c-1 module; find `relu_out` = recompute `@relu`'s `w` bind + `dz1` = `@relu_vjp`'s `w` bind; assert each is written by a resultless dispatch AND read by a LATER gemm stage; assert their roles — expect NOT ExternalIn = the plan sees them produced; if ExternalIn, that is the finding — and stage order dispatch-before-consumer); (b) the TWO-Output readback decision (dW1+dW2, ONE Output today) — advisor picks option (b): extend `run_quant_module` to read ANY buffer by-Value regardless of role (what the seed-handle/readback-by-Value contract already implies). Then **25c-2** device numeric crown Vk+DX12+llvmpipe vs hesap + grad_fd → **25c-3** checkpointing LEDGER → **25z** band close. | build §57; proof §139 |

**25c-1b-2a ✅ CLOSED 2026-09-04** (test_tensor_pipeline.cpp device-free plan gate + grad.hpp forward-liveness contract doc — NO engine code change): the `vjp_mlp` backward (the 25c-1 corpus, forward composite ERASED) plans as a device-resident pipeline, and the write-through-declare pattern + the two-output situation are PROVEN device-free. ⛔ CONTRACT CHOICE (i) (advisor): `build_gradient` only APPENDS the backward — it does NOT expand or remove the differentiated forward op; the forward's liveness is the CALLER's decision (a training loop wants loss + grads; a gradient-only kernel wants neither), so a caller planning the backward ALONE erases a dead forward COMPOSITE before planning. Engine-side DCE of a dead differentiated op is ledgered **CEIR-26** (a real DCE pass, not a special case in the transform). Documented in grad.hpp's build_gradient comment. ⛔ NEGATIVE GATE (the reason the erase exists — a LOCKED header claim needs its own gate, the 24z baked-32 scar): the UNERASED module plans → `PlanReject::UnsupportedOp` with `reject_op == mlp` (the composite itself); `plan_tensor_pipeline` is PURE so the pre-plan is safe. Then `REQUIRE(!mlp->result(0)->has_uses())` (loss dead — Operation::erase asserts on live users, the guard fails LOUD) → `mlp->erase()` → plan → None. GATE (device-free): 11 stages = 5 gemm + 4 transpose + 2 VizDispatch (NO reduce/broadcast — loss IS the mlp result, seed dOut=M directly). ⛔ WRITE-THROUGH-DECLARE ×2 (advisor): `relu_out` (recompute `@relu`'s w-bind = h1) and `dz1` (`@relu_vjp`'s w-bind) are resource.declares the RESULTLESS dispatch WRITES (no SSA edge) — BOTH marked `Intermediate` (the finding would have been ExternalIn — the plan DOES see resultless-dispatch writes, tensor_pipeline.cpp:455), and the FIRST reader (min stage index, a UNIVERSAL "every reader after the writer" invariant — catches a RAW reader-before-writer) is STRICTLY after the dispatch AND of the expected kind (`relu_out` → h1ᵀ Transpose; `dz1` → dW1/dx Gemm) — submit order carries the non-SSA edge. ⛔ TWO-OUTPUT proof: `grads[0]`=dW1 (TERMINAL op) is the plan's SINGLE Output (the final stage's trailing write); `grads[1]`=dW2 (emitted earlier, unread) is `Intermediate` = GpuOnly. gr.seed (dOut) is ExternalIn (STEP 1 (c) mirror). GREEN win-debug [tensor-pipeline] 213/18 + full device-free 1192/88 + WSL-gcc -Werror [autodiff] 223/10 + tidy-20 clean (test + grad.hpp). ⛔ (b) SPEC VERBATIM for 25c-2 (advisor, so 25c-2 doesn't re-derive): `run_quant_module` sets memory class BY ROLE at CREATE time (:273-275: ExternalIn→CpuToGpu, Output→GpuToCpu, else GpuOnly) and reads ONE `out_idx` via `map()` (:317) — a GpuOnly Intermediate can't be mapped, so reading dW2 fails. EXTEND: (1) force `GpuToCpu` for every buffer whose `.value` matches a NAMED out_val regardless of role, (2) accept a LIST of `{out_val, dst, dst_len}`. ⛔ 25c-2 PRE-FLIGHT: confirm `ComputeBuffer::map()` on a GpuOnly buffer returns nullptr (not UB) on BOTH backends — that makes "set the class at create time" mandatory, not a post-hoc fix. NEXT = **25c-2** device numeric crown Vk+DX12+llvmpipe: seed dLoss=M NON-UNIFORM, read back BOTH dW1 (Output) + dW2 (Intermediate, via (b)) vs hesap `matmul_vjp`+`relu_vjp` composed (f64, FD-cross-validated via `grad_fd` on `f(W)=loss(mlp(x,W))`) — never hand-computed. Then **25c-3** checkpointing LEDGER → **25z** band close. | build §57; proof §139 |

**25c-2a ✅ (Vulkan leg) 2026-09-04 — ⛔ 25c-2 REMAINS OPEN pending 25c-2b (DX12)** (test_ceir_pipeline_vulkan.cpp — TEST-ONLY): the `vjp_mlp` backward of a 2-layer MLP (x[8,4]·W1[4,4]·relu·W2[4,4]) runs DEVICE-RESIDENT on Vulkan + llvmpipe, reading back BOTH dW1 + dW2, matching a DIALECT-INDEPENDENT hesap reference. ⛔ (b) HARNESS EXTENSION (advisor spec, ONE buffer loop): `run_quant_module_n(…, const QuantOut* outs, n_outs)` — memory class by role BUT FORCE `GpuToCpu` for every buffer whose `.value` matches a named out (an Intermediate/GpuOnly out is otherwise UNMAPPABLE, vulkan_compute_context.cpp:61/370, PRE-FLIGHT-confirmed), and record EVERY ShaderWrite→HostRead barrier BEFORE the single submit_and_wait (a barrier after submit is a no-op → the 2nd readback races). `run_quant_module(…single…)` kept as a THIN wrapper (grep-confirmed one `create_buffer`; all pre-25c-2 callers unchanged). ⛔ ALL-HESAP reference chain (advisor): forward `nn::matmul`+`nn::relu` → z1/h1/z2, backward `matmul_vjp`(×2)+`relu_vjp` composed → dW1/dW2; the FD witness `MlpLoss` (grad_fd on L=<M,mlp(x,W)>) calls HESAP FORWARD (not a hand-written MLP — a hand-written forward repeats the device's own indexing so a shared mistake would pass; grad_fd instantiates only f64 so a plain-double functor is honest). ⛔ CORPUS (advisor): x ALL-POSITIVE + W1 column-signed (cols 0,1 +, cols 2,3 −) ⇒ z1 columns cleanly ± and WELL-SEPARATED (|z1|>0.05 ASSERTED) — relu BITES (cols 2,3 dead) AND the FD stays OFF the relu kink (central differences corrupt at a kink); seed dLoss=M NON-UNIFORM (asserted ≥2 distinct — the 25b-4b all-ones-dC gap). ⛔ ZERO-BLOCK PROOF (advisor): half-dead hidden ⇒ dW1[:,c≥2]==0 (dz1 zeroed) AND dW2[c≥2,:]==0 (h1 zeroed) EXACT + the c<2 blocks meaningfully nonzero (|·|>0.05) — a dropped/wrong-half relu_vjp or relu breaks these; device f32 vs f64 ref tol 1e-4 rel. ⛔⛔ TWO FINDINGS (not test bugs): (1) the Vulkan `Resolver::pipes[8]` cap + `res.n >= 8` guard TRUNCATED an 11-stage plan (stages 9-11 got a null pipeline → execute failed) → bumped to 16; (2) the relu-KINK corrupts the FD witness where z1≈0 (gradient_check.hpp: FD "NOT valid at kinks") → the column-signed off-kink corpus. GREEN win-debug Vulkan #4736 (REAL device) + Vulkan device regression 258/258 (the (b) refactor + pipes bump break NOTHING) + WSL/llvmpipe #4567 (device path RAN, post-rename) + tidy-20 clean. NEXT = **25c-2b** (DX12 leg → 25c-2 CLOSE): mirror in test_ceir_pipeline_dx12.cpp — (b) `run_quant_module_n` + `MlpLoss` + the gate; ⛔ the DX12 `Resolver` has its OWN `pipes[8]` copy (~line 87) — BUMP it to 16 or the DX12 gate fails identically; ⛔ confirm `Dx12ComputeContext` barrier on a GpuToCpu buffer has a real HostRead transition (not a no-op) before readback. ⛔ AT 25c-2b CLOSE: `SumGemmAA`+`MlpLoss` = two FD functors × two TUs — DECIDE hoist to tests/gpu-shared/ (pure f64, trivial) vs ledger w/ named trigger (don't let it ride silently). Then **25c-3** checkpointing LEDGER → **25z** band close. | build §57; proof §139 |

**25c-2b ✅ CLOSED (DX12 leg) 2026-09-04 → ✅✅ 25c-2 COMPLETE** (test_ceir_pipeline_dx12.cpp + the FD-functor hoist): the `vjp_mlp` backward of the 2-layer MLP runs DEVICE-RESIDENT on DX12 too (dW1+dW2 vs the same all-hesap ref), so the §138 MLP-backward numeric crown is proven on **Vk + DX12 + llvmpipe**. ⛔ DX12 (b) ASYMMETRY (design fact, not a gap): DX12's portable pattern makes EVERY dev buffer GpuOnly and reads back via a dedicated GpuToCpu `rb` + transfer copy — so an Intermediate output (dW2) reads back EXACTLY like an Output, NO GpuToCpu role-forcing needed (unlike Vulkan, which maps the buffer directly so must force it). `run_quant_module_n` = one `rb`+copy per named out, ALL barriers+copies recorded BEFORE the single submit (a barrier after submit is a no-op → the 2nd readback races); thin single-out wrapper kept. DX12 `Resolver::pipes[8]→[16]` + guard `8→16` (its OWN copy — the 11-stage plan needs it, same finding as Vulkan). ⛔ FD-FUNCTOR HOIST (advisor decision at 25c-2b close — chose HOIST over the "3rd functor" trigger under the full-gold-standard mandate): `SumGemmAA`+`MlpLoss` (4 copies = 2 functors × 2 TUs) → ONE definition in `tests/gpu-shared/autodiff_fd_functors.hpp` (namespace `crd::ceir::gpu_test`, relative `#include "../gpu-shared/..."`, NO CMake change — headers aren't compiled TUs; `using` at each TU). ⛔ HOIST-TIME GUARD (advisor): `MlpLoss`'s `z1/h1/z2[64]` scratch carried the 25c-2 corpus assumption (m·d1≤32) — now shared+reusable, a bigger caller would silently overflow the stack (the baked-32 scar one level up), so added `kCap=64` + a deliberately-wrong `return 0.0` on overflow (the FD gate fails HARD, never silent) + the cap named in the header. GREEN win-debug DX12 #4814 + Vulkan #4736 (REAL devices, post-hoist) + DX12 device regression 223+8 (the (b) refactor+pipes bump+hoist break NOTHING) + Vulkan regression 258/258 + WSL/llvmpipe 25b-4b+25c-2 3/3 (shared header compiles gcc 13.3) + tidy-20 clean (shared header + both TUs) + 25b-4b 3/3 + 25c-2 2/2 both backends post-hoist. ⛔ FINDING (memory): `-R "A|B"` through `cmd /c` = a CMD PIPE not alternation → two single-term ctest calls ([[reference_ctest_regex_pipe_is_a_cmd_pipe]]). **✅✅ 25c-2 COMPLETE — the numeric crown is done.** NEXT = **25c-3** (checkpointing LEDGER, ONE tick): record that `vjp_mlp` recompute is the ONLY strategy today; a store-forward-activations variant lands when `plan_tensor_pipeline` exposes per-buffer bytes to a VJP rule AND a corpus exists where `M·hidden·4B` exceeds a plan-nameable budget (NEITHER exists in CEIR-25 — the 8·4 corpus makes recompute free). Then **25z** band close: every 25 row ✅ + re-verify §57/§139 vs what SHIPPED (source=scoreboard — grad.hpp still says "differentiates linalg.gemm / tensor.reduce / **ml.***"; `ml.*` = `ml.mlp` ONLY — fix the header or the claim at 25z). | build §57; proof §139 |

**25c-3 ✅ CLOSED 2026-09-04 (checkpointing LEDGER — a ledger row, NO implementation: no consumer exists in CEIR-25)**: `vjp_mlp` ALREADY IS the fully-checkpointed strategy — it RECOMPUTES every interior activation (z_i via a re-emitted gemm, h_i via a re-emitted `@relu`) in the backward instead of storing a forward pass. That IS textbook gradient checkpointing (recompute-in-backward, Griewank-Walther / Chen-2016), and it is PROVEN numerically on device (25c-2: the device backward that USES the recompute matches hesap). So checkpointing is DONE, not deferred; what is ledgered is the store-vs-recompute *hybrid* (store some activations, recompute others — trade memory for compute). ⛔ IT HAS NO CONSUMER: (a) post-reframe the differentiated COMPOSITE (ml.mlp) has NO SSA interior to store — the forward activations are never materialized as plan buffers a VJP rule could reference (the forward composite is ERASED before planning, 25c-1b-2a); (b) `plan_tensor_pipeline` does not expose per-buffer BYTES to a VJP rule, so a rule cannot decide "this activation exceeds budget → store not recompute"; (c) the 8·4 corpus makes recompute FREE (1 small extra gemm + 1 relu dispatch — no memory pressure). ⛔ TRIGGER for the store-forward variant (both must hold; NEITHER does in CEIR-25): (i) `plan_tensor_pipeline` exposes per-buffer bytes to a VJP rule AND (ii) a corpus exists where a forward activation's bytes (M·hidden·4B) exceed a plan-nameable budget. ⛔ SUPERSEDES the stale 25c-0-row scoping note ("vjp_mlp needs h_i_pre = the forward gemm result, an SSA value the planner already materializes; recompute = a 2nd gemm, strictly worse here") — that was the pre-reframe (differentiate-the-expansion) view; post-reframe the composite has NO SSA interior, so recompute is the ONLY strategy, not "strictly worse." ⛔ CROSS-REF CEIR-26 (DCE): the backward emits `dx` (operand-adjoint[0], the INPUT gradient) as a gemm even when `wrt`=[W1,W2] doesn't request it — a DEAD stage in the plan (one of the 25c-1b-2a 5 gemms) that runs on device correct-but-wasteful; DCE removes it at CEIR-26 (needs a dead-stage sweep over the plan/module). ⛔ DCE removes TWO stages, not one (advisor): `dx = gemm(dz1, W1ᵀ)` is the ONLY reader of the `W1ᵀ` transpose, so DCE'ing `dx` also removes `W1ᵀ` — after CEIR-26 the 25c-1b-2a plan gate's `stages==11`/`n_gemm==5`/`n_tr==4` become `9`/`4`/`3`; CEIR-26 must UPDATE that gate (expected shift), not "fix" it back. No test change (25c-2 already exercises recompute end-to-end); this row is the honest record + trigger, per "absorb with a real consumer, never build speculatively." NEXT = **25z** BAND CLOSE (advisor pre-flight, ORDERED so no tick burns on discovery): **(1)** verify every CEIR-25 row ✅. **(2)** HEADER SWEEP (source=scoreboard) — sweep the WHOLE grad.hpp header comment, NOT just line 4 (advisor: several stale claims, same as the 25c-1 :7-9 strike): (a) "differentiates linalg.gemm / tensor.reduce / **ml.***" → `ml.*` = `ml.mlp` ONLY; (b) the "compute.dispatch kernel symbols (relu ↔ relu_vjp)" register_kernel line — `register_kernel` has NO built-in caller (25c-0 finding, resultless-dispatch reframe); (c) the `register_builtin_vjps` "the ml / authored-kernel pairs" doc — authored-kernel pairs never landed via the registry; STRIKE each IN PLACE (SUPERSEDED-clause rule) — DO THIS BEFORE the recipe so the recipe describes what the header NOW says. **(3)** CORRECTNESS BOARD (README item 6, do NOT skip as "N/A"): CEIR-25 is a CORRECTNESS band (no perf numbers), so the source=scoreboard artifact is `docs/bench/2026-09-04-ceir25-autodiff-scoreboard.md` — each gate × backend × assertion count × the reference it matched (hesap/FD/eval_cpu); the recipe LINKS it (never quote results from memory). **(4)** STUDY→RECIPE: `docs/recipes/2026-09-04-ceir-reverse-mode-autodiff.md` — the whole-band teaching doc: VJP registry, composite-vs-expansion REFRAME (resultless dispatch invisible to the result-keyed walk), the 4 RECIPE TRAPS (write-through-declare, two-output readback, forward-liveness/caller-erases, readonly-input GLSL-vs-HLSL codegen), recompute=full checkpointing + the store-hybrid trigger, the device numeric crown, and the PARAMETERS table (`vjp_mlp` depth cap `nw≤8`/`h[9]`/`z[9]`, the baked-32 guard, relu_vjp `local_size=32`). **(5)** BAND-CLOSE SUITE (per-exe, NOT whole-repo=CI's job) — record numbers each: `crd-ceir-gpu-tests` (device-free, BOTH compilers: win-debug + WSL-gcc -Werror), `crd-ceir-gpu-vulkan-tests` (Windows REAL + WSL llvmpipe), `crd-ceir-gpu-dx12-tests` (Windows REAL), `crd-kir-tests` (the relu_vjp.ckir reading gate). | build §57; proof §139 |

**25z-1 ✅ CLOSED 2026-09-04 (band-close bookkeeping part 1: row verification + grad.hpp header sweep — DOC-only, no code, no test, comment-only header edits ⇒ no compile this tick; grad.hpp recompiles in the 25z-5 suite run)**: ⛔ ITEM (1) VERIFY EVERY CEIR-25 ROW ✅ (source=scoreboard, all rows swept): **25a-1** (gemm VJP registry + `vjp_gemm`) ✅ + **25a-2** (`vjp_reduce` sum + `build_gradient` orchestrator) ✅ — both recorded INSIDE the CEIR-25 summary table row (line 952), NOT standalone bold rows (the band-numbering-is-tracker-authoritative convention); **25b-1** (ckir_synth transpose/broadcast/elementwise) ✅ · **25b-2a** (elementwise device) ✅ · **25b-2b** (broadcast/permute root emitters) ✅ · **25b-3** (StageKind widen) ✅ · **25b-4a** (resolvers + multi-stage device) ✅ · **25b-4b STEP 1** (device-free plan) ✅ · **STEP 2 → 25b-4 → 25b COMPLETE** ✅; **25c-0** (relu_vjp.ckir) ✅ · **25c-1** (vjp_mlp) ✅ · **25c-1b-1** (relu_vjp device) ✅ · **25c-1b-2a** (write-through-declare plan) ✅ · **25c-2a** (Vulkan) + **25c-2b** (DX12) **→ 25c-2 COMPLETE** ✅ · **25c-3** (checkpointing ledger) ✅. NO HOLE. The band-level `🔄` on the CEIR-25 table row is CORRECT (25z items 3/4/5 — bench board, recipe, suite run — remain); it flips `🔄→✅` at 25z-5 close, NOT before (CLOSE/DoD: never claim the band done while partial). The historical "25c OPEN" (design-lock row) and "25c-2a … 25c-2 REMAINS OPEN" markers are RESOLVED by their successor rows (25c-2b→25c-2 COMPLETE) — left in place as history, not rewritten. ⛔ ITEM (2) grad.hpp HEADER SWEEP (source=scoreboard, STRUCK/CORRECTED IN PLACE) — the advisor named 3 stale claims; the sweep found a **4th** the band itself had flagged: **(a)** line 4 "differentiates linalg.gemm / tensor.reduce / **ml.***" → `tensor.reduce(sum)` + `ml.mlp` (the ml dialect has EXACTLY one VJP rule, not the whole dialect); **(b)** registry doc "compute.dispatch kernel symbols **(relu ↔ relu_vjp)**" → the kernel-symbol space is MACHINERY with NO built-in registrant this band (25c-0 resultless-dispatch reframe; register_kernel has no caller) — dropped the misleading live-pair example; **(c)** `register_builtin_vjps` doc "**25b**: the ml / **authored-kernel pairs**" → "**25c**: ml.mlp — all OP-KIND rules via register_op; NO authored-kernel pair register_kernel'd"; **(d) ⛔ THE BIG ONE (not advisor-named — flagged by the 25b-3 row's own "CORRECT the 'ZERO new StageKinds' claim if a transpose planner stage is needed"):** line 6 "**ZERO new StageKinds** (… 'store' is free)" SHIPPED FALSE — CEIR-25b-3 APPENDED planner `StageKind::{Transpose,Broadcast,Elementwise}` (verified in tensor_pipeline.hpp:54-58, comment "CEIR-25b-3 autodiff backward vocab"; tensor.transpose was NEVER plannable pre-25b, only expand_ml's baked dispatch) → corrected to "no new OP-kinds; THREE new planner StageKinds", and "store is free" qualified (true for the tensor-vocab VJPs; the ml.mlp COMPOSITE has no SSA interior and RECOMPUTES = 25c-3 checkpointing). ⛔ grad.cpp CONFIRMED ALREADY ACCURATE (grep: `register_op`×3 = linalg.gemm/tensor.reduce/ml.mlp; `register_kernel`×0; the :463 comment already documents "relu is NOT register_kernel'd"). ⛔⛔ ALL-HOMES SWEEP (advisor: item (2) is "re-verify §57/§139 vs SHIPPED" = EVERY home, not just grad.hpp): grepped `docs/` for `"ZERO new StageKinds"|register_kernel|"relu ↔ relu_vjp"|ml.*` — the claim's ORIGIN is the CEIR-25 **band-open LOCK** (table row line 952, TWO occurrences: "(ZERO new StageKinds, the 24b echo)" + "existing vocab, ZERO new StageKinds; 25b synth_transpose") → BOTH STRUCK IN PLACE (SUPERSEDED-clause rule, not deleted). CEIR-24's row + the 2026-08-18 24z session log also say "ZERO new StageKinds" but were TRUE in their own time (CEIR-24 expand_ml reused existing stages) — LEFT UNTOUCHED. Line 960 (25b-3) is the forward-FLAG that predicted this fix ("CORRECT the claim if a transpose planner stage is needed") — RESOLVED by this row, left as history. ⛔ §57 (build) / §139 (proof): grep found NO home for the stale strings in tracked `docs/` — these §-numbers reference the UNTRACKED CEIR master spine (§139 is defined nowhere in tracked docs), OUT OF REPO-EDIT SCOPE per CLAUDE.md (generic-repo rule); recorded, not editable here. ⛔ SUB-CLAIM VERIFIED (advisor — "no new OP-kinds" could itself be false if an op was declared in 25b): tensor.hpp:4-5 — `elementwise / broadcast / reshape / transpose / reduce / matmul` are the ORIGINAL tensor-dialect ops (generated from tensor.ceirop.toml over the CEIR-3d Tensor type, pre-25); NONE declared in 25b → "no new OP-kinds; three new planner StageKinds" STANDS. ⛔ CODE-TREE SWEEP (advisor: item (2) = EVERY home incl. code, not just docs): grepped `{engine,tests,assets}/**/*.{hpp,cpp,ckir}` for the stale strings — NO third home for the autodiff claim (the 2 "ZERO new StageKinds" hits = CEIR-24 `expand_ml.hpp:5` / `test_expand_ml.cpp:3`, TRUE in their own time, LEFT; NO `relu ↔ relu_vjp`/`ml.*` in code; every `register_kernel` hit is the live API or unrelated scene-render). ⛔⛔ FIFTH STALE CLAIM (advisor, source=scoreboard — the one the shipped gates contradict most directly): `LossNotScalar` + the seed wording. grep grad.cpp: `LossNotScalar` appears ONLY in `grad_error_name` — NEVER RETURNED = a DEAD enum member wearing an unenforced "loss must be rank-0 scalar" contract (the registered-default-empty scar in ENUM form). SHIPPED CONTRADICTS IT: 25b-4b loss=[3] (rank-1), 25c loss=[8,4] (rank-2, seed=M NOT all-ones) — both differentiated fine. FIXED in 3 sub-homes: (i) enum :34 marked DEAD (KEPT — append-only enum, grad_error_name stays exhaustive) with the any-rank note; (ii) `GradResult.seed` doc + (iii) `build_gradient` doc — "uploads ALL-ONES / d loss÷d loss=1" → the seed = **∂L/∂loss for the caller's IMPLICIT scalar objective L** (ALL-ONES ⇔ L=sum(loss); WEIGHTED seed M ⇔ L=⟨M,loss⟩; "=1" holds ONLY at rank-0). So item (2) drift lived in grad.hpp (FIVE claims: 4 header + LossNotScalar/seed) + the band-open lock (2 occ) — ALL now honest; §57/§139 = untracked spine (out of scope). NEXT = **25z-2** (CORRECTNESS bench board, README item 6 — do NOT skip as "N/A" for a correctness band): `docs/bench/2026-09-04-ceir25-autodiff-scoreboard.md` — one row per gate × backend × assertion count × the reference it matched (hesap nn_reverse / grad_fd / eval_cpu_kernel); the source=scoreboard artifact the 25z-3 recipe LINKS (never quote results from memory). Then **25z-3** recipe → **25z-4** (fold: recipe done) → **25z-5** per-exe suite run → **25z-6** BAND-CLOSE RITUAL (AGENTS.md session-end + the 24z precedent `docs/sessions/2026-08-18-ceir-24z-band-close.md`): write `docs/sessions/2026-09-04-ceir-25z-band-close.md` + update `context.md` (current-focus/last-shipped/next-up) + `docs/ROADMAP.md` ("CEIR-25 band-open (2026-08-18)" → "Bands through CEIR-25 ✅") + propose a Conventional-Commits message (NO AI co-author trailer) → flip the CEIR-25 table row `🔄→✅`. | build §57; proof §139 |

**25z-2 ✅ CLOSED 2026-09-04 (band-close bookkeeping part 2: the CORRECTNESS bench board — DOC-only, no code)**: wrote `docs/bench/2026-09-04-ceir25-autodiff-scoreboard.md` (README item 6, the source=scoreboard artifact the 25z-3 recipe LINKS) + indexed it at the TOP of `docs/bench/README.md`. FORMAT mirrors the `2026-07-19-oit-tier-scoreboard.md` (the correctness/accuracy analog, not a perf board): **Config** (host i9-14900K; devices Vulkan `VK_EXT_shader_object` + DX12 FL12 on RTX 4070 Ti SUPER + WSL2 llvmpipe; harness `crd-ceir-gpu-tests`[autodiff,tensor-pipeline,ckir-synth] + `crd-kir-tests`[asset] + device Vk/DX12 TUs; build-target.bat/run-ctest.bat) + **References** (hesap `nn_reverse::matmul_vjp`/`relu_vjp` analytic f64 [SEPARATE codebase = dialect-independent], `gradient_check::grad_fd` FD, `eval_cpu_kernel` CKIR oracle) + **Board** (15 rows 25a-1…25c-3: gate × backend × evidence × reference × discriminates) + **Verdict**. ⛔ ADVISOR-CAUGHT (3 cell-honesty fixes before the row): (1) the evidence column CONFLATED gate-vs-regression — 5 cells (25b-2a 2149/4, 25b-2b 2272/6, 25b-4a 2951/12, 25c-1 1157/87, 25c-1b-2a 1192/88) cited the WHOLE tag/suite total as if the gate's own count → SPLIT into "gate (by-name #IDs / cases) · rode: regression bucket" with a legend (a reader was reading 2951 assertions for a 6-float transpose+add); (2) 25c-2 tolerance now DERIVED with the check ("nonzero 1e-4 rel [4-elt f32 dot chain thru relu]; zero blocks EXACT ==0") mirroring 25b-4b·2's derivation; (3) the NEGATIVE gate NAMED per row (the LOCKED-item memory's "name the gate that goes RED if the guard is removed") — `MissingVjp` on an unregistered on-path reduce (PURE, op-count unchanged), `tanh`→`MlpActivationUnsupported`, `x[8,8]·W1[8,8]` (64≠32)→`MlpBakedShapeUnsupported` (both ZERO backward vocab), `[3,2]+[3,1]` broadcast-compat elementwise→`SynthRejected`. PLUS an honest Verdict line: the board is NOT exhaustive over `GradError` — any-rank loss is only implicit (25b-4b [3], 25c [8,4]); `LossNotScalar` is a DEAD member (no gate ⇒ no row, 25z-1). ⛔ EXTERNAL-PEER N/A WITH THE CHECK: this is a compiler-transform CORRECTNESS band scored vs oracles, not a perf crush — the AD-math peers (torch/JAX) live on the v15/v16 hesap boards; hesap is the reference here. ⛔ PROVENANCE (README retro-port/consolidation form): assertion counts are as-of each slice's close (cited to D-007 rows written at measurement time); the 25z-5 band-close suite run re-measures the whole band in one pass and reconciles the board IN PLACE. Advisor confirmed: v15/v16-as-perf-home framing + the provenance block are correct, keep both. NEXT = **25z-3** STUDY→RECIPE `docs/recipes/2026-09-04-ceir-reverse-mode-autodiff.md` (whole-band teaching doc — VJP registry, composite-vs-expansion REFRAME, the 4 recipe TRAPS [write-through-declare, two-output readback, forward-liveness/caller-erases, readonly GLSL-vs-HLSL codegen], recompute=checkpointing + store-hybrid trigger, the device numeric crown, the PARAMETERS table [`vjp_mlp` depth cap nw≤8/h[9]/z[9], baked-32 guard, relu_vjp local_size=32]) — LINKS this board. Then **25z-4** (fold) → **25z-5** per-exe suite run (reconcile board) → **25z-6** band-close ritual → flip `🔄→✅`. | build §57; proof §139 |

**25z-3 ✅ CLOSED 2026-09-04 (band-close bookkeeping part 3: the STUDY→RECIPE teaching doc — DOC-only, no code) — ⛔ 25z-4 "fold" ABSORBED (recipe is done ⇒ no separate tick)**: wrote `docs/recipes/2026-09-04-ceir-reverse-mode-autodiff.md` (the whole-band lesson, the STUDY→RECIPE standing rule) + indexed it at the TOP of `docs/recipes/README.md`. FOLLOWS the mandatory 7-section convention in order: (1) PARAMETERS FIRST (the `build_gradient` signature, any-rank `loss`, the `seed`=∂L/∂loss, the registry rules, `vjp_mlp` depth cap, baked-32 guard, `relu_vjp.ckir` shape, tolerances, + the two HARNESS knobs `Resolver::pipes[16]` and `MlpLoss::kCap=64`); (2) what/why (reverse-mode as a COMPILER transform, not a tape); (3) maths (VJP = Jᵀ·adjoint; gemm/reduce/relu/MLP rules; checkpointing); (4) full assembly (build_gradient PURE pre-pass → registry → vjp_mlp → plan_tensor_pipeline → device); (5) the TRAPS (the composite-vs-expansion REFRAME, write-through-declare, two-output readback, forward-liveness/caller-erases, readonly GLSL-vs-HLSL codegen, baked-32, the FD relu-kink, don't-add-a-scalar-check); (6) measured numbers → LINKS the 25z-2 board; (7) where the code lives. ⛔ ADVISOR-CAUGHT (3 fact/precision fixes before the row): (1a) the intro vocab list missed `broadcast/reshape` (the 25b-4b·1 plan has a Broadcast stage) → added; (1b) the maths `x̄=z̄1·W1ᵀ` is the DEAD `dx` stage (emitted even when `wrt` omits `x`; the board's `n_gemm==5` counts it; DCE=CEIR-26) → marked; (2) the README mandates PRECISE citation — `vjp_mlp` recompute-ALL is Griewank-Walther 2008 *Evaluating Derivatives* ch.12 (the zero-store `revolve` limit), NOT Chen 2016 (arXiv 1604.06174 §3 = the √n store-vs-recompute HYBRID = the ledgered variant) → corrected; (3) the PARAMETERS table lacked the cap's reject (`nw>8 ⇒ ArityUnsupported`, verified grad.cpp:390) + the two harness knobs → added. PLUS structural: demoted the ZERO-StageKinds / LossNotScalar corrections from a "trap" to a build-lesson ("do NOT add a scalar-loss check" — a rebuilder who enforces the dead `LossNotScalar` breaks the shipped [3]/[8,4] corpora), since Traps = what bites a REBUILDER, not doc-hygiene. VERIFIED `nw≤8`/`h[9]`/`z[9]` against grad.cpp:390/419-420 before shipping the parameter. NEXT = **25z-5** per-exe BAND-CLOSE SUITE run (the substantive remaining work; 25z-4 "fold" is absorbed here) — record numbers each: `crd-ceir-gpu-tests` (device-free, BOTH compilers win-debug + WSL-gcc -Werror), `crd-ceir-gpu-vulkan-tests` (Windows REAL + WSL llvmpipe), `crd-ceir-gpu-dx12-tests` (Windows REAL), `crd-kir-tests` (relu_vjp.ckir reading gate) — and RECONCILE the 25z-2 board in place with the fresh consolidated numbers (⛔ this run also RECOMPILES the grad.hpp comment edits from 25z-1, the first compile since the header sweep). Then **25z-6** BAND-CLOSE RITUAL (session log + context.md + ROADMAP flip + commit message) → flip the CEIR-25 table row `🔄→✅`. | build §57; proof §139 |

**25z-5a ✅ (device-free MSVC leg) 2026-09-04 — ⛔ 25z-5 REMAINS OPEN (gcc + device + asset legs pending)**: the FIRST recompile of the grad.hpp header edits from 25z-1 (a comment-only sweep) — `scripts\build-target.bat win-debug crd-ceir-gpu-tests` → BUILD_EXIT=0, `grad.cpp` rebuilt + relinked CLEAN (no syntax damage from the 5-claim sweep + the LossNotScalar/seed edits); then the full `crd-ceir-gpu-tests` device-free suite = **1192 assertions / 88 cases, exit 0** (matches the last-recorded 1192/88 — NO drift; the 13 CEIR-25 device-free gates #802–804, #819–822, #833–838 all green). ⛔ SCOPE (advisor-consistent): the device gates (#4720+ Vulkan / #4802+ DX12) live in SEPARATE exes and need the assets+shaderc run-env (`run-ctest.bat`'s broad `-R` would pull them in and fail-on-env — the [[reference_windows_test_run_env_assets_shaderc_asan_dll]] leg), so they are their OWN subslice ticks; the asset reading gate #4077 (`CEIR-25c-0`) is in `crd-kir-tests`, also its own leg. Bench board reconciliation table STARTED (docs/bench/2026-09-04-ceir25-autodiff-scoreboard.md §Band-close reconciliation): device-free MSVC row filled; gcc/asset/Vulkan/DX12 rows pending. NEXT = **25z-5b** (device-free gcc `-Werror` leg via WSL `linux-gcc-debug` — the cross-compiler proof the header edits build under gcc too) → **25z-5c** Vulkan device leg (real + llvmpipe, run-env) → **25z-5d** DX12 device leg → **25z-5e** asset `crd-kir-tests` #4077 → reconcile board → **25z-6** ritual + flip. | build §57; proof §139 |

**25z-5b ✅ (device-free gcc leg) 2026-09-04 — ⛔ 25z-5 REMAINS OPEN (Vulkan + DX12 + asset legs pending)**: WSL `linux-gcc-debug` (gcc 13.3.0, `-Werror`) — `grad.cpp` recompiled CLEAN cross-compiler (the 25z-1 header sweep builds under gcc's stricter `-Werror` too, not just MSVC), full `crd-ceir-gpu-tests` device-free suite = **1192 assertions / 88 cases, EXIT=0** — IDENTICAL to the MSVC leg (no cross-compiler drift). Board reconciliation gcc row filled. The device-free leg is now COMPLETE on BOTH compilers. NEXT = **25z-5c** Vulkan device leg (`crd-ceir-gpu-vulkan-tests`, real + WSL llvmpipe; needs the assets+shaderc run-env — `-R "ceir 25"` scoped to the Vulkan exe's #4720–4736) → **25z-5d** DX12 (`crd-ceir-gpu-dx12-tests` #4802–4814) → **25z-5e** asset `crd-kir-tests` #4077 (`relu_vjp.ckir` reading gate) → reconcile board → **25z-6** ritual + flip `🔄→✅`. | build §57; proof §139 |

**25z-5c ✅ (Vulkan device leg — real + llvmpipe) 2026-09-04 — ⛔ 25z-5 REMAINS OPEN (DX12 + asset legs pending)**: built `crd-ceir-gpu-vulkan-tests` (win-debug BUILD_EXIT=0, only the Vulkan test TU recompiled — grad.hpp already in libcrd-ceir-gpu). Real device (RTX 4070 Ti SUPER) via `run-ctest.bat win-debug "ceir 25.*Vulkan"` = **7/7 passed, 0 failed, exit 0** (#4720 25b-2a, #4721/#4722 25b-2b, #4733 25b-4a, #4734 25b-4b, #4735 25c-1b relu_vjp readonly, #4736 25c-2 MLP-backward). ⛔ FINDING (de-risks the remaining device legs): **ctest AUTO-APPLIES the registered run-env** — the CMake test ENVIRONMENT props set CRD_ASSETS_DIR+shaderc, so #4735/#4736 loaded `relu_vjp.ckir` + compiled GLSL with NO manual env (the [[reference_windows_test_run_env_assets_shaderc_asan_dll]] env is baked into the test properties, not something the runner must set). WSL llvmpipe (software Vulkan, `CRD_PLATFORM_HEADLESS=1`) = **7/7 passed, exit 0** (#4551–4567 — the device path RAN, not skipped). Board reconciliation Vulkan row filled. NEXT = **25z-5d** DX12 device leg — `run-ctest.bat win-debug` scoped to the DX12 CEIR-25 tests; ⛔ the DX12 names MIX "D3D12" (#4802–4811) and "DX12" (#4812–4814) → a single `-R "…D3D12…DX12…"` needs a `|` = the cmd-pipe trap, so use TWO single-term calls (`-R "ceir 25.*D3D12"` then `-R "ceir 25.*DX12"`) per [[reference_ctest_regex_pipe_is_a_cmd_pipe]]. Then **25z-5e** asset `crd-kir-tests` #4077 → reconcile board → **25z-6** ritual + flip. | build §57; proof §139 |

**25z-5d ✅ (DX12 device leg) 2026-09-04 — ⛔ 25z-5 REMAINS OPEN (asset leg pending)**: built `crd-ceir-gpu-dx12-tests` (win-debug BUILD_EXIT=0, only the DX12 test TU recompiled). TWO single-term ctest calls (the D3D12/DX12 name split, avoiding the cmd-pipe): `-R "ceir 25.*D3D12"` = **4/4 passed, exit 0** (#4802 25b-2a, #4803/#4804 25b-2b, #4811 25b-4a); `-R "ceir 25.*DX12"` = **3/3 passed, exit 0** (#4812 25b-4b, #4813 25c-1b relu_vjp [HLSL readonly→UAV drop], #4814 25c-2 MLP-backward). All 7 DX12 CEIR-25 gates green on the REAL D3D12 device; the recompiled grad.hpp works through the DX12 path too. Board reconciliation DX12 row filled. NEXT = **25z-5e** asset leg — `run-ctest.bat win-debug` for the `relu_vjp.ckir` reading gate (`crd-kir-tests` #4077 `CEIR-25c-0`): confirm the committed `.ckir` reads + `eval_cpu_kernel` == analytic ReLU-VJP, on BOTH compilers (win + WSL-gcc). Then reconcile board Verdict → **25z-6** BAND-CLOSE RITUAL (session log + context.md + ROADMAP flip + commit message) + advisor before the flip → flip `🔄→✅`. | build §57; proof §139 |

**25z-5e ✅ (asset leg) → ✅✅ 25z-5 COMPLETE (band-close suite ALL GREEN) 2026-09-04**: built `crd-kir-tests` (win BUILD_EXIT=0) → `relu_vjp.ckir` reading gate `-R "CEIR-25c-0"` = **1/1 passed, exit 0** (#4077, committed `.ckir` reads + `eval_cpu_kernel` == analytic); WSL-gcc leg = **1/1 passed, exit 0** (#4059 — reads cross-compiler/cross-platform). ✅✅ **THE FULL 25z-5 BAND-CLOSE SUITE IS GREEN** (fresh single-pass re-measure, source=scoreboard, ZERO drift from per-slice closes, ZERO failures): device-free MSVC 1192/88 + gcc `-Werror` 1192/88; Vulkan real 7/7 + llvmpipe 7/7; DX12 real 7/7 (4 D3D12 + 3 DX12); asset win 1/1 + gcc 1/1. The 25z-1 grad.hpp header sweep recompiled CLEAN on every path (MSVC + gcc + device backends). Bench board §Band-close reconciliation fully filled + a reconciliation verdict added. NEXT = **25z-6** BAND-CLOSE RITUAL (the LAST tick of CEIR-25): (1) ADVISOR before the flip (the "declaring done" gate — confirm every 25 row ✅, the board honest, nothing masked); (2) `docs/sessions/2026-09-04-ceir-25z-band-close.md`; (3) `context.md` current-focus/last-shipped/next-up; (4) `docs/ROADMAP.md` "CEIR-25 band-open (2026-08-18)" → "Bands through CEIR-25 ✅"; (5) propose a Conventional-Commits message (NO AI co-author trailer per project policy); (6) flip the CEIR-25 table row `🔄→✅`. Then **CEIR-26** (Optimizer phase 1 — canonicalize/DCE/CSE/…, incl. the ledgered dead-`dx`/`W1ᵀ` DCE that shifts the 25c-1b-2a gate 11/5/4→9/4/3). | build §57; proof §139 |

**25z-6 ✅ CLOSED → ✅✅✅ CEIR-25 BAND CLOSED 2026-09-04 (the band-close ritual — advisor declaring-done gate PASSED)**: ⛔ ADVISOR confirmed "the band is genuinely closed — every 25 row ✅, six legs green with zero drift, board honest, the header sweep verified across every compiler and backend. Flip it." Ritual done: **(1)** advisor gate PASSED (+ 3 carry-ins applied: the trailer decision, the surprising-lessons section, the #802–838 range fix → `#802–804, #819–822, #833–838`); **(2)** session log `docs/sessions/2026-09-04-ceir-25z-band-close.md` (what-it-proves + row-per-claim + row-per-config fresh sweep + asset inventory + 8-item deferral ledger + the SURPRISING-LESSONS process section [forward-flag-not-a-fix, all-homes sweep] + proposed commit); **(3)** `context.md` LIVE marker flipped (CEIR-25 ✅ + board headline; CEIR-26 open; NEXT=26a); **(4)** `docs/ROADMAP.md` "Bands through CEIR-24 ✅ … CEIR-25 band-open" → "Bands through CEIR-25 ✅ (…+ reverse-mode autodiff) … CEIR-26 (optimizer) band-open (2026-09-04)"; **(5)** proposed Conventional-Commits message (⛔ NO AI co-author trailer — the project rule wins over the mid-session system-reminder, CLAUDE.md "overrides any harness default"; the conflict is noted one-line in the session log); **(6)** the CEIR-25 table row flipped `🔄→✅`. ✅✅✅ **CEIR-25 (`ceir.autodiff`) IS DONE — reverse-mode AD as a compiler transform, device numeric crown on Vk+DX12+llvmpipe, full gold standard.** NEXT = **CEIR-26 BAND-OPEN** (Optimizer phase 1 — canonicalize/DCE/CSE/partial-eval/specialize/inline/basic-fusion/memory/schedule, each pass differential-tested vs reference; §73/§74/§76/§77 + §120): ⛔ advisor at band-open (a DESIGN tick — choose the first pass); the natural **26a = the dead-`dx`/`W1ᵀ` DCE** because it HAS a consumer handed over by CEIR-25 (the 25c-1b-2a plan gate's `11/5/4→9/4/3` shift IS the differential test). | build §57; proof §139 |
| 🔄 | CEIR-26 | Optimizer phase 1 → canonicalize/DCE/CSE/partial-eval/specialize/inline/basic-fusion(→generated CKIR)/memory/schedule — each pass differential-tested vs reference | §73 §74 §76 §77; correctness §120 |

**CEIR-26 BAND-OPEN LOCK (advisor 2026-09-04) — Optimizer phase 1, the actual PASSES on the EXISTING CEIR-8g frameworks.** ⛔ RECON (source=scoreboard): the framework EXISTS, the passes do NOT — `pass_manager.hpp` (`PassManager`/`AnalysisManager`, `Pass{name, run(ctx,m,diag)→changed, preserved}`; "FRAMEWORKS ONLY — no optimization passes, those are CEIR-26/27") + `rewrite.hpp` (`RewritePattern`/`ConversionTarget`/`try_apply`; ⛔ "the greedy/worklist DRIVER — traversal order, fixpoint, iterator invalidation — IS the CEIR-26 work and is deliberately RESERVED") + `transaction.hpp`. CEIR-26 WRITES the passes + the rewrite driver, each **differential-tested vs reference**. ⛔⛔ 26a-0 RESOLVED (the deadness query, the gate on whether DCE is even buildable): `Context::op_has_trait(op, OpTrait::Pure)` + `op_effects(kind)` EXIST (context.hpp:645/652); the CORRECT deadness rule is **`op_info(kind)!=nullptr` (registered) ∧ `op_has_trait(Pure)` ∧ no-result-has-uses** — the ⛔ EMPTY≠UNKNOWN guard (context.hpp:648-651: an UNREGISTERED kind is MAXIMALLY EFFECTFUL, never dead) means a resultless `compute.dispatch` (`@relu`/`@relu_vjp`, effectful/not-Pure) is LEFT — the write-through-declare pattern (25c-1b-2a) survives DCE, [[feedback_effect_narrowing_must_suppress_all_whole_class_effects_not_just_the_obvious_one]]. ⛔ ADVISOR-LOCKED (6): **(1)** deadness = the rule above (NOT naive no-uses — the scar); **(2)** DCE differential test = THREE legs — (a) STRUCTURAL: the 25c-1b-2a corpus 11/5/4→9/4/3 (UPDATE the gate, don't fix it back), (b) NUMERIC: the DCE'd module through the 25c-2 device pipeline Vk+DX12+llvmpipe, dW1/dW2 unchanged vs hesap (= "differential-tested vs reference" for a semantics-preserving pass), (c) NEGATIVE: a resultless dispatch writing a declared buffer a later stage READS → DCE must LEAVE it (`changed==false`) — without (c) the scar ships green; **(3)** iterator invalidation — collect dead ops to a worklist REVERSE-TOPOLOGICAL, erase AFTER the walk, iterate to FIXPOINT (the `W1ᵀ` transpose only dies AFTER `dx` is erased; `Operation::erase` asserts on live users so reverse order is LOAD-BEARING); NOT erase-inside-walk (the reserved-driver trap); **(4)** the pass LIVES in crd-ceir (dialect-agnostic — `engine/ceir/src/passes/dce.*`), consumed by a ceir-gpu test (its first consumer); **(5)** band shape: **26a DCE** (fixpoint loop + Pure query) → **26b canonicalize/fold** (needs the `RewritePattern` GREEDY driver = the reserved work; first consumer arith.const fold or reshape-of-reshape) → **26c CSE** (value-hash; ⛔ find the real consumer — the 25c recompute `z1` gemm would have matched the ERASED forward, so that consumer is gone) → **26d specialize/inline** → **26e basic-fusion→CKIR** (the 23b QuantGemm plan-collapse precedent — decide plan-level vs module-level at 26e-open) → **26f memory/schedule**; only 26a locked now; **(6)** ⛔ DO NOT touch `vjp_mlp` to stop emitting `dx` — that is the "special case in the transform" the 25c-1b-2a contract REJECTED in favour of a real DCE pass; DCE earns its keep by removing what the transform legitimately emits. NEXT = **26a** DCE pass: author `engine/ceir/src/passes/dce.{hpp,cpp}` (the `Pass` contract: worklist reverse-topo + fixpoint + `changed`; deadness = registered∧Pure∧no-uses) + a crd-ceir device-free gate (a synthetic dead-op module: dead gemm+transpose removed, a live chain kept, + the NEGATIVE resultless-dispatch-left gate) → then the ceir-gpu consumer gate (UPDATE 25c-1b-2a 11/5/4→9/4/3) → then the 25c-2 numeric re-run on device (unchanged). advisor after 26a implementation. | build §73/§74; proof §120 |

**26a-1 ✅ CLOSED 2026-09-04 (the DCE pass + its device-free unit gate — the FIRST optimization pass on the CEIR-8g PassManager framework)**: authored `engine/ceir/include/crd/ceir/passes/dce.hpp` (HEADER-ONLY — inline `dce_run`/`dce_is_dead`/`dce_collect` + `dce_pass()`→`Pass{"dce", &dce_run, {}}`; no CMake lib change) + `tests/ceir/test_dce.cpp` (added to the EXPLICIT `crd-ceir-tests` add_executable list — the hand tests aren't globbed). ⛔ DEADNESS = **leaf (`num_regions==0`) ∧ registered (`op_info!=nullptr`) ∧ `OpTrait::Pure` ∧ no-result-uses**; COLLECT-then-erase + FIXPOINT (worklist from `ctx.allocator()`, arena never frees so pointers survive sibling erases) — never erase-mid-walk (the rewrite.hpp reserved-driver trap). ⛔ 26a-0 confirmed IN CODE: `op_has_trait` already returns false for an unregistered kind (`has_trait`: `op_info==nullptr⇒false`, dialect.cpp:162-165), so the explicit `op_info` check is REDUNDANT-BUT-INTENTIONAL (states the EMPTY≠UNKNOWN maximally-effectful contract so a future trait-lookup change can't make an unknown op dead). GATE (3 tests, device-free, GREEN win-debug #222-224, tidy-20 clean BOTH files): **(1)** the fixpoint chain `%a→%b` (both Pure, `%b` unused) collapses in ONE `dce_run` (round 1 kills `%b`, round 2 the now-dead `%a`) — ⛔ ADVISOR-STRENGTHENED to IDENTITY not count: `REQUIRE %a->has_uses()` at t=0 (so its death PROVES round 2) + `CHECK a/b->is_erased()` (robust when Release disables `erase()`'s live-user assert) + IDEMPOTENCE (2nd run `changed==false` — the PassManager "did nothing ⇒ invalidate nothing" contract); **(2)** NEGATIVE — an effectful op (`k_eff`, not Pure) + an UNREGISTERED op (`k_unknown`, `op_info==nullptr`) + a USED Pure op all KEPT, `changed==false`; **(3)** REGION RECURSION — a dead Pure op inside a container's region removed while the container (Pure BUT region-bearing) is kept (`is_erased` identity). ⛔ ADVISOR RETRACTION (primary-source): the "resource.declare device-free in 26a-1" point was WRONG — `register_resource_ops` is ceir-gpu-layer (render_fullscreen_build.cpp), so `crd-ceir-tests` (crd-ceir only) can't include it; the synthetic `k_eff` proves the non-Pure-KEPT LOGIC, and the REAL trait-registration proof (`resource.declare`/`compute.dispatch` non-Pure; `linalg.gemm`/`tensor.transpose` Pure) rides 26a-2 where those ops exist. NEXT = **26a-2** (the ceir-gpu CONSUMER gate): ⛔ PRE-FLIGHT grep the real trait registrations FIRST — `linalg.gemm` + `tensor.transpose` MUST be `Pure` (else DCE won't remove `dx`/`W1ᵀ` = a FALSE "DCE does nothing" that looks like a pass bug), `compute.dispatch` + `resource.declare` MUST be non-Pure; if gemm/transpose lack Pure, adding it (with the dialect.cpp "Pure⇒zero effects" assert as guard) is 26a-2's first edit. Then in test_tensor_pipeline.cpp the 25c-1b-2a corpus → `mlp->erase()` → **`dce_run`** → plan → UPDATE the existing `11/5/4` REQUIREs to `9/4/3` (UPDATE in place, not a parallel gate) + assert the 2 write-through-declare buffers STILL `Intermediate` (the dispatches survived DCE). Then **26a-3** = the 25c-2 device re-run with `dce_run` before plan on Vk+DX12+llvmpipe (dW1/dW2 unchanged). advisor at 26a-2. | build §73/§74; proof §120 |

**26a-2 ✅ CLOSED 2026-09-04 (the ceir-gpu CONSUMER gate — DCE on the real vjp_mlp backward + the LIVENESS-CONTRACT negative gate; test-only + comment-only header edits, no engine logic change)**: `dce_run` on the 25c-1b-2a backward shifts the plan `11/5/4/2 → 9/4/3/2` (Gemm 5→4, Transpose 4→3, VizDispatch 2→2) and the write-through-declare + two-output invariants all SURVIVE on the pruned plan. Edited `test_tensor_pipeline.cpp` (the 25c-1b-2 test, IN PLACE per the 25c-3 ledger "UPDATE the gate, don't fix it back") + added the NEGATIVE `TEST_CASE`; comment-only touches to `dce.hpp` (LIVENESS CONTRACT + the chain wording) + `grad.hpp` (build_gradient caller note); doc touch to the recipe. GREEN win-debug both 26a-2 gates (#825 positive + #826 negative), tidy-20 clean. ⛔⛔ THE FINDING (advisor-confirmed, caught BEFORE writing the gate — the 25c-3 ledger's "DCE removes dx/W1ᵀ" was INCOMPLETE): `grads[0]=dW1`/`grads[1]=dW2` are **readback-by-Value** (the plan marks the terminal write `Output` by TRAVERSAL), NOT SSA-consumed — so a naive no-uses DCE deletes THEM too (Pure gemms, no uses) and cascades the WHOLE backward. FIX (advisor route (i), NOT a DCE root-param — a pass reads the program, not a side-channel): **PIN** the requested gradients with a `func.return(dW1,dW2)` FIRST (Terminator ⇒ NOT Pure ⇒ roots its operands; the planner SKIPS `func.return`, tensor_pipeline.cpp:211, so the plan is unaffected). New memory [[feedback_plan_output_by_traversal_is_not_ssa_liveness_pin_readback_before_any_pass]]. ⛔ 3 OPS not 2 (empirical, source=scoreboard): DCE prunes the dead `dx` branch to FIXPOINT = **3 ops** — the `dx` gemm (dead, no user) frees, in round 2, BOTH its output `resource.declare` (`mk_gemm` emits a paired output declare, grad.cpp:86) AND the `W1ᵀ` transpose (each used only by the dx gemm); the plan drops 2 STAGES (the declare isn't a stage). The declare dying in round 2 is the chain-collapse proof (a single-round sweep would leave it). `b->num_ops()` 24→21. ⛔ TEST-HELPER TRAP (ground-truthed via CAPTURE, memory): `mkmain` returns the `func.func` BODY block (one region deep, func.cpp create_func); `m->body()->first_block()` is the OUTER block holding just the func.func CONTAINER (1 op) — a non-recursive top-level walk reports a FALSE 0-delta; count on the func body block `b` directly. `erase()` unlinks + tombstones (ir.hpp:370-371) so `num_ops()` is the true live count AND `dce_collect`'s fixpoint re-walk never revisits erased ops (no double-erase — dce.hpp is provably correct); the func.func container being region-bearing means this gate ALSO exercises `dce_collect`'s region recursion on REAL IR. ⛔⛔ THE NEGATIVE GATE (the LOCKED-guard rule [[feedback_locked_checklist_item_needs_a_gate_or_it_silently_doesnt_land]] — advisor: pinned-survives can't distinguish "kept" from "kept anyway"): a twin `TEST_CASE`, SAME corpus, **NO `func.return`** → `REQUIRE_FALSE(grads[0/1]->has_uses())` (the t=0 premise) → `dce_run` → `CHECK(grads[0/1]->defining_op()->is_erased())` (the CATASTROPHE: unpinned gradients DELETED) + SURVIVOR IDENTITY (the two resultless `@relu`/`@relu_vjp` dispatches stay LINKED in b, found by `kernel` attr — NOT Pure, DCE can't touch them). Plus the POSITIVE t=0 twin `REQUIRE(grads[0]->has_uses())` right after the pin (mirrors the unit gate's has_uses fact). ⛔ PRE-FLIGHT CONFIRMED (no trait edit needed): `linalg.gemm` + `tensor.transpose` ARE Pure (DCE removes dx/W1ᵀ), `compute.dispatch` (`.traits=0U`+2 EffectRecords) + `resource.declare` (⛔ STRUCK 26c-2b: declare is now NON-Pure — an Allocate effect [it names a distinct resource, CSE must not merge two]; DCE keeps it as a non-Pure op, and a now-dead declare stays in the IR but is planner-invisible [unreachable ⇒ no stage/buffer], so the 26a-2 prune is 2 ops not 3) NOT removed. ⛔ FORWARD-CLAIM LEDGER (strike-in-the-tick-its-trigger-fires): dce.hpp line 6 "differential-tested vs … the 25c-2 device numerics unchanged" is a claim about **26a-3** (not yet run) — LEFT, but 26a-3 MUST re-verify that header line at close. NEXT = **26a-3** (the 25c-2 device numeric re-run — the differential-test-vs-reference leg): in the 25c-2 Vulkan + DX12 device TUs, PIN `grads[]` with `func.return` → `dce_run` BEFORE `plan_tensor_pipeline` → read back dW1/dW2 → UNCHANGED vs hesap `matmul_vjp`+`relu_vjp` on Vk + DX12 + llvmpipe (⛔ confirm `run_quant_module_n`'s named-out readback is unaffected — the planner skips `func.return`, so it should be; ⛔ D3D12/DX12 name split = two single-term ctest calls); at close re-verify dce.hpp line 6. Then **26a CLOSE** → **26b** canonicalize/fold (the reserved RewritePattern greedy driver). advisor at 26a-3. | build §73/§74; proof §120 |

**26a-3a ✅ (Vulkan device leg — real + llvmpipe) 2026-09-04 — ⛔ 26a-3 REMAINS OPEN (DX12 leg 26a-3b pending)**: the DCE'd `vjp_mlp` backward runs device-resident on Vulkan and is **BIT-EXACT** vs the RAW (un-DCE'd) device output — the differential-test-vs-reference leg (b) of the band lock. Edited `test_ceir_pipeline_vulkan.cpp` (the 25c-2 crown test, structure (B) advisor-chosen): AFTER the existing raw dW1/dW2 device exec + hesap asserts (raw crown UNTOUCHED), a SECOND pass PINs `grads[]` (`func.return` roots them — readback-by-Value, not SSA-consumed), `dce_run` (11→9 stages), re-plan + re-execute the SAME corpus/SAME device/SAME seeds → `CHECK(dw*got_dce[i] == dw*got[i])` bit-exact every element + the DCE'd output vs hesap standalone. GREEN win-debug real device (RTX 4070 Ti SUPER) #4740 + WSL llvmpipe (software Vulkan, `CRD_PLATFORM_HEADLESS=1`, gcc 13.3) #4567 + tidy-20 clean. ⛔⛔ THE CEIR-26 DIFFERENTIAL-TEST STANDARD (advisor — write once, applies to 26b–26f): for a SEMANTICS-PRESERVING pass the reference is NOT hesap-within-tolerance (a 1e-5 perturbation would pass) — it is **the unoptimized program's OWN device output, BIT-EXACT** (`dce'd(P) == raw(P)`, same device/kernels/inputs). Bit-exact HELD on this corpus (4-elt sequential dot per thread, elementwise relu, copy-transpose — nothing reduces across threads); a failure here would be a device-determinism FINDING, not a tolerance to loosen. ⛔ DEVICE-INDEPENDENCE: `dce'd == raw` held across a REAL RTX and llvmpipe (software) — the proof is device-independent, not just corpus-independent. ⛔ PLAN IDENTITY: `REQUIRE(plan_dce.stages.size() == 9U)` proves DCE ACTED on this device corpus (not merely `changed==true`). ⛔ TOMBSTONE NOTE: the raw 11-stage `plan` is executed FIRST and NOT touched after `dce_run` — its `dx`/`W1ᵀ` `stage.op` pointers are tombstones post-DCE (harmless, already executed; a future reader must not re-execute it). Seeds (xin/w1/w2/gr.seed by Value) all survive DCE (unchanged array). NEXT = **26a-3b** (DX12 leg → 26a-3 CLOSE → 26a CLOSE): mirror the second pass in `test_ceir_pipeline_dx12.cpp`'s 25c-2 test; ⛔ VERIFY FIRST (advisor, don't assume the mirror): `QuantOut` struct fields at ~:248-262 match; `run_quant_module_n` (DX12) caches NOTHING by `plan.buffers.size()` or stage index (11→9 changes both — grep its body for `stages[`/`.size()` as a key); the DX12 harness makes every dev buffer GpuOnly + reads back via a fresh `rb`+copy so the 2nd exec's fresh `rb`s are fine. Run `-R "25c-2.*DX12"` (one term, matches only #4814). ⛔ AT 26a CLOSE re-verify + SHARPEN dce.hpp line 6 "25c-2 device numerics unchanged" → "BIT-EXACT vs the raw program on Vk+DX12+llvmpipe". advisor at 26a-3b. | build §73/§74; proof §120 |

**26a-3b ✅ CLOSED (DX12 device leg) → ✅✅ 26a-3 COMPLETE → ✅✅✅ CEIR-26a (DCE) CLOSED 2026-09-04**: the DCE'd `vjp_mlp` backward runs device-resident on DX12 and is **BIT-EXACT** vs the RAW device output — the DX12 mirror of 26a-3a, completing leg (b) on **Vk + DX12 + llvmpipe**. Edited `test_ceir_pipeline_dx12.cpp` (25c-2 crown, structure (B)): the second DCE pass after the untouched raw crown — PIN `grads[]` (`func.return`) → `dce_run` (11→9) → re-plan (`REQUIRE stages==9U`) + re-execute SAME corpus/device/seeds → `CHECK(dw*got_dce[i] == dw*got[i])` bit-exact + DCE'd-vs-hesap standalone. GREEN win-debug real D3D12 (RTX 4070 Ti SUPER) #4818 + tidy-20 clean. ⛔ VERIFIED-BEFORE-TYPING (advisor, the DX12 mirror is NOT assumed): `QuantOut{value,dst,len}` identical to Vulkan; `run_quant_module_n` (DX12) reads `plan.buffers` FRESH each call (fixed dev[40]/up[40]/rb[8] caps, `nb=plan.buffers.size()` a per-call local, `out_idx` re-scanned each call) — NO caching by buffer-count or stage-index, so the 9-stage plan's fresh `rb`+copy readback works unchanged; the DX12 all-GpuOnly+dedicated-rb pattern needs no role-forcing. Ran `-R "25c-2.*DX12"` (one term, #4818 only — no cmd-pipe). ⛔ 26a CLOSE re-verify DONE: dce.hpp line 6 SHARPENED "25c-2 device numerics unchanged" → "the 25c-2 device output BIT-EXACT vs the RAW program on Vk+DX12+llvmpipe (semantics-preserving ⇒ reference = the unoptimized program's own device output, not a tolerance vs an oracle)" — the forward-claim ledger from 26a-2 is now discharged. ✅✅✅ **CEIR-26a (Dead-Code Elimination) IS DONE** — the FIRST optimization pass on the CEIR-8g PassManager framework: `dce_run` (registered∧Pure∧no-uses, fixpoint, region-recursive, collect-then-erase), proven across 3 legs — (a) STRUCTURAL device-free unit gate (#222-224) + the 26a-2 consumer gate (11/5/4→9/4/3, 3-op prune) + the LIVENESS-CONTRACT negative gate (#826, unpinned⇒deleted), (b) NUMERIC bit-exact-vs-raw on Vk+DX12+llvmpipe, (c) the negative resultless-dispatch-LEFT proof (the write-through-declare survives). The pin-before-any-pass liveness contract ([[feedback_plan_output_by_traversal_is_not_ssa_liveness_pin_readback_before_any_pass]]) is the reusable CEIR-26 finding. NEXT = **26b** canonicalize/fold — the FIRST pass needing the RESERVED `RewritePattern` GREEDY/worklist driver (rewrite.hpp: "traversal order, fixpoint, iterator invalidation IS the CEIR-26 work"); advisor at 26b-open (a DESIGN tick — the driver contract + the first fold consumer: arith.const fold or reshape-of-reshape). ⛔ the 26a-3a differential-test STANDARD (reference = raw program's own device output, bit-exact) carries to 26b–26f. | build §73/§74; proof §120 |
**26b-1 ✅ CLOSED 2026-09-04 (the GREEDY REWRITE DRIVER + device-free unit gate — the RESERVED rewrite.hpp fixpoint driver, the FIRST piece of 26b canonicalize/fold)**: added `engine/ceir/include/crd/ceir/passes/rewrite_driver.hpp` (`greedy_rewrite(ctx, m, ConstSpan<RewritePattern>, diag, pass_name)→changed` + `greedy_collect`) + `tests/ceir/test_rewrite_driver.cpp` (4 gates) + the CMake row. ⛔ DESIGN = COLLECT-PER-ROUND, not a worklist (advisor design-gate + a reconcile call): the reserved `RewritePattern::rewrite` is `void(Context&,Operation&)` — it CANNOT return an op it created, so a persistent worklist has no handle to re-queue a folded-and-itself-foldable NEW op; a fresh region-recursive collect each round SEES the newly-inserted op via its block link (the `dce_run` skeleton generalized from one fixed rule to a pattern set — ONE shape for 26c–26f to copy). ⛔ TWO PATTERN-AUTHOR CORRECTIONS baked into the header contract + gated: (1) a fold's `rewrite` MUST `op.parent_block()->insert_before(new_op,&op)` BEFORE the RAUW — a floating (created-not-linked) op is invisible to the planner (a silent dropped stage, the 19z binding-drop shape) AND to re-collect; (2) the fold RAUWs but does NOT erase the dead input — DCE reclaims it (canonicalize→dce compose at the PassManager; each pass stays observable as its own job). ⛔ CAP = INITIAL-seeded-op-count + 1, FIXED from the first collect (NOT recomputed per round — an insert+RAUW fold GROWS op count, so a per-round cap would let an inserting oscillator outrun its bound); op-count is NOT the termination measure — the bound holds because a monotone pattern's measure (matchable-site count / reshape-chain depth) is ≤ the initial live-op count and strictly drops each round; exceeding ⇒ a FATAL diag naming the pass (the caller/PassManager stops on has_fatal()). ⛔ 4 GATES (device-free, synthetic `ct.*` dialect, capture-free `OpId{fnv1a_ct("ct.op")}` matches pinned to intern_op): (1) FIXPOINT — a dead prod→cons chain collapses in ONE call (prod dies round 2 after the re-collect frees it, the DCE dx→W1ᵀ shape) + idempotence on the emptied module; (2) is_erased SKIP — a producer erasing its later-in-array consumer, with a ⛔ LOAD-BEARING COUNT gate (`dead_erase_count()==1`, the function-local-static idiom from test_pass_manager.cpp): DELETE the driver's `is_erased`-on-entry skip and the dead-remover ALSO fires on the tombstoned consumer ⇒ 2 (erasing a tombstone is benign, so num_ops/is_erased alone can't tell — the count is what fails; the ⛔⛔ locked-guard-needs-a-gate scar, advisor-caught: my first draft was a can't-fail gate); (3) reshape-of-reshape FOLD — inserts+RAUWs, `parent_block!=nullptr` asserted, folds to one-hop reshape(%x), LEAVES both dead reshapes, then `dce_run` reclaims them (r2 round1, r1 round2) + a NON-TRIVIAL idempotence check on the converged real state before DCE; the `ct` ops are REGISTERED here (src/reshape Pure, sink NOT Pure = the effectful output consumer that keeps the folded op live) so DCE can reclaim; (4) OSCILLATION — a genuine non-monotone PAIR (flip pick's operand a↔b via set_operand) hits the cap ⇒ `has_fatal()`, never hangs. GREEN win-debug [ceir][rewrite] 4/4 (#539-542) + tidy-20 clean (both files). ⛔ per WHOLE-REPO=CI: the linux/gcc -Werror leg is CI's job (local = changed module + blast-radius, green here). NEXT = **26b-2**: `canonicalize_pass()` binding the REAL `tensor.reshape∘reshape` fold pattern + the 26a bit-exact-vs-RAW device differential (advisor: reshape is a zero-copy plan alias, so reshape∘reshape→reshape shifts stage count by 0 or 1 — assert whichever it is, don't assume 1); FIRST grep whether `vjp_reduce`'s `mk_reshape` consumes a forward `tensor.reshape` in the §137-pipeline autodiff (a NATURAL consumer if yes; a constructed corpus with the row saying so if no). | build §73/§74; proof §120 |

**26b-2a ✅ CLOSED 2026-09-04 (canonicalize/fold — the REAL `tensor.reshape∘reshape` fold on the greedy driver + a device-free gate)**: added `engine/ceir/include/crd/ceir/passes/canonicalize.hpp` (`canonicalize_pass()`/`canonicalize_run` + `kCanonPatterns[]` + `canon_detail::{match,rewrite}_reshape_of_reshape`) + 2 `[ceir][tensor-pipeline]` gates in test_tensor_pipeline.cpp (#831-832). ⛔ BOUND ARRAY, not a registry (advisor): canonicalize has no typed-reject contract (an op with no pattern is left alone) so there is nothing to look up + no completeness question — MLIR's op-provided-patterns indirection collapses to one `static constexpr RewritePattern kCanonPatterns[]` here (one Context, no plugin boundary); patterns grouped by dialect, `passes/` depends on the tensor dialect (`gen/tensor_ops.hpp`) ON PURPOSE (a per-dialect canonicalize header fragments for zero benefit until a 2nd dialect folds). ⛔ CONSTRUCTED corpus (advisor, consumer grep DONE): NO natural `reshape∘reshape` in the autodiff — no `vjp_reshape` rule, and `vjp_reduce`'s `mk_reshape` consumes `dout` NOT a forward reshape — so the redundant double-reshape is hand-built (row says so). FOLD = `tensor.reshape(tensor.reshape(x))→tensor.reshape(x)`: build_reshape(x, outer_result_type), insert_before-then-RAUW, LEAVE both dead reshapes for DCE (compose). ⛔ Fix 1 (advisor): the match EXCLUDES `T_out==T_in` — folding a rank-preserving chain (T→U→T) would emit an IDENTITY reshape `reshape(x:T):T`, a SEPARATE canonicalization (`reshape(x:T):T→x`) DEFERRED to 26b-3; a NEGATIVE gate proves the T→U→T chain is left intact (canonicalize_run returns false, both reshapes kept). GATE (device-free, gemm[4,4]→reshape[16]→reshape[2,8]→reduce): raw plan 2 stages (Gemm+Reduce; reshapes are aliases) + 2 Alias buffers → canonicalize (fold fires) → dce_run (r1/r2 reclaimed, folded kept) → POST plan STAGE-COUNT UNCHANGED (the 0-shift the design promised — reshape is a zero-copy alias, NEVER a stage) + Alias 2→1 + reject None + idempotent 2nd run. ⛔⛔ TWO advisor-caught can't-fail gates FIXED: (1) the unpinned terminal reduce → DCE pruned reduce→folded→plan reject 6 — the 26a-2 LIVENESS CONTRACT reproducing in a new consumer (pin `rd` via `func.return` before DCE; planner skips it, plan unaffected — [[feedback_plan_output_by_traversal_is_not_ssa_liveness_pin_readback_before_any_pass]]); (2) the Fix-2 `find_tensor_misuse==None` gate is INERT (ANY 16-elt shape passes) — added the IDENTITY-not-category pin: capture `r2->result.type()` BEFORE the fold, assert `folded->result.type()==t_out` AND `rd->operand.type()==t_out` (a wrong T_out passes the verifier but breaks the downstream shape — the device leg's whole reason). ⛔ intern-contract PIN added (`intern_op("tensor","reshape")==OpId{fnv1a_ct("tensor.reshape")}`) — a hash change would silently stop the fold (greedy_rewrite false, plan still succeeds — EMPTY≠UNKNOWN); canonicalize.hpp header states the contract. GREEN win-debug [ceir][tensor-pipeline] 2/2 (#831-832) + tidy-20 clean (canonicalize.hpp + test). ⛔ per WHOLE-REPO=CI: linux/gcc -Werror = CI's job (local blast-radius green). NEXT = **26b-2b** (the Vk bit-exact-vs-RAW device differential — insert a redundant reshape into the §137 device pipeline, run raw → canonicalize+dce → re-plan → re-run, `CHECK(opt[i]==raw[i])`; ⛔ advisor: root the chain at the GEMM output `[side,side]→[2,L/2]→[L]→fft` so the outer's `T_out=[L]≠[side,side]` fires WITHOUT hitting the identity exclusion — work it out on paper first) → **26b-2c** (DX12 leg → 26b-2 complete). | build §73/§74; proof §120 |

**26b-2b ✅ CLOSED (Vulkan leg) 2026-09-04 (the canonicalize reshape-fold BIT-EXACT vs the RAW device output on real Vulkan)**: added a `[ceir][tensor-pipeline][gpu]` gate in test_ceir_pipeline_vulkan.cpp (#4747) — the §137 pipeline gemm[8,8]→reshape[2,32]→reshape[64]→fft→reduce (the DOUBLE reshape ROOTED AT THE GEMM OUTPUT per advisor: outer `T_out=[64]`≠`T_in=[8,8]` so the fold FIRES without hitting the identity exclusion) runs RAW on a real Vulkan device, then `canonicalize_run`+`dce_run` collapse it to the single-reshape pipeline, re-runs, and the reduce scalar is BIT-EXACT (`opt_s==raw_s`) + both CORRECT vs the independent triple-loop→naive-DFT→serial-sum f64 ref within the propagated tol. RAW plan 3 stages (Gemm/Fft/Reduce — the two reshapes are aliases) + 2 Alias buffers → OPT 3 stages (0-shift) + 1 Alias. ⛔ reused the 22c-2 materialization as a LOCAL runner lambda (run_quant_module_n zero-fills fft twiddles, can't run an fft pipeline; a shared helper can't cross Vk/DX12 contexts — DX12 gets its own at 26b-2c). ⛔ PIN the reduce output via func.return before DCE (26a-2 liveness contract — else DCE prunes the reduce→reshape chain). ⛔⛔ WITNESS-STRENGTH caveat (advisor, source=scoreboard): unlike 26a-3 (DCE removed DISPATCHED stages — a bad prune changed which kernels ran, bit-exact caught it), THIS fold touches only ALIAS buffers — the 3 dispatches are identical before/after — so bit-exact here proves alias-WIRING preservation (the "wrong T_out → fft reads a wrong-shaped alias" concern) NOT kernel-level correctness; the witness regains its 26a teeth at a DISPATCHED-stage fold (26e fusion). ⛔ ALIAS-ORDER GATE (advisor): the runner's one-level `bufs[i]=bufs[alias_of]` resolves the r_out→r_in→gemm chain transitively ONLY because plan buffers are walk-ordered producer-first — `REQUIRE(alias_of >= 0 && alias_of < i)` per Alias buffer gates it (a reorder would silently null-deref the raw run). GREEN win-debug real Vulkan (RTX 4070 Ti SUPER) #4747 + tidy-20 clean. ⛔ llvmpipe DEFERRED to band-close (a fold that doesn't touch kernels is trivially bit-exact raw-vs-opt on any device — advisor-confirmed not a shortcut); per WHOLE-REPO=CI the linux/gcc leg is CI's. NEXT = **26b-2c** (DX12 leg → 26b-2 COMPLETE): mirror in test_ceir_pipeline_dx12.cpp — ⛔ check the DX12 §137 gate's own twiddle materialization + its `Resolver`/`resolve_stage` names + add the alias-order gate; then **26b-3** = the LEDGERED identity-reshape elimination `reshape(x:T):T→x` (a RAUW-to-OPERAND pattern, NO new op — a different shape from 26b-2, own header note). | build §73/§74; proof §120 |

**26b-2c ✅ CLOSED (DX12 leg) 2026-09-04 → ✅✅ 26b-2 COMPLETE**: the canonicalize reshape-fold is BIT-EXACT vs the RAW device output on DX12 too — the DX12 mirror of 26b-2b. Added a `[ceir][tensor-pipeline][gpu]` gate in test_ceir_pipeline_dx12.cpp (#4826): the SAME §137 gemm[8,8]→reshape[2,32]→reshape[64]→fft→reduce(sum) corpus (double reshape rooted at the gemm output), RAW run → `canonicalize_run`+`dce_run` → OPT run, `opt_s==raw_s` bit-exact + both correct vs the independent f64 ref. Uses the DX12 PORTABLE dev/up/rb materialization (the 22c-3e pattern — GpuOnly UAV dev + CpuToGpu up + GpuToCpu rb, copy-based upload/readback + fft twiddles by fft-stage bind; run_quant_module_n zero-fills twiddles so a LOCAL runner mirrors 22c-3e). RAW 3 stages + 2 Alias → OPT 3 stages (0-shift) + 1 Alias. ⛔ SAME advisor gates as Vk: the alias-order gate (`alias_of >= 0 && alias_of < i`, the one-level deref is order-safe only producer-first) + `n_out==1` (a 2nd Output would bit-exact the WRONG scalar) + the alias-WIRING-not-kernel witness caveat. GREEN win-debug real D3D12 (RTX 4070 Ti SUPER) #4826 + Vulkan #4747 re-green (post n_out fix) + tidy-20 clean (both TUs). ⛔ SCOREBOARD (source=scoreboard, advisor): 26b-2 proven on **Vk + DX12 REAL devices**; llvmpipe is DEFERRED to **26z band-close** (a fold touching only aliases is trivially bit-exact raw-vs-opt on any device — NOT a device-independence claim run here; contrast 26a which claimed Vk+DX12+llvmpipe). ✅✅ **26b-2 (the canonicalize `tensor.reshape∘reshape` fold) COMPLETE** across 3 legs: (a) 26b-2a device-free (fold + DCE compose + plan-neutral + identity-exclusion + T_out identity pin + intern pin), (b) 26b-2b Vk bit-exact #4747, (c) 26b-2c DX12 bit-exact #4826. NEXT = **26b-3** = the LEDGERED identity-reshape elimination `reshape(x:T):T → x` — a RAUW-to-OPERAND pattern (`op.result(0)->replace_all_uses_with(op.operand(0))`, NO create_operation / insert_before, so correction #2 is VACUOUS — canonicalize.hpp's header needs a sibling clause "a fold to an EXISTING value RAUWs directly, no insert"); ⛔ DESIGN Q for the 26b-3 advisor call: with the identity pattern present, does the pair {reshape∘reshape-excluding-identity, identity-elimination} converge on a `T→U→T` chain and to what? (the 26b-2a identity-exclusion NEGATIVE test changes meaning — advisor). ⛔ 26z BAND-CLOSE CHECKLIST (accumulating): (1) **llvmpipe cross-vendor re-run of the 26b-2 reshape-fold differential** (WSL, deferred here per WHOLE-REPO=CI). | build §73/§74; proof §120 |

**26b-3 ✅ CLOSED 2026-09-04 → ✅✅✅ 26b (canonicalize/fold) COMPLETE**: identity-reshape elimination `reshape(x:T):T → x` — the RAUW-to-OPERAND fold (the SIBLING of the reshape-of-reshape build+insert+RAUW: it folds to an EXISTING value, so NO create_operation/insert_before — correction #2 vacuous; canonicalize.hpp's header gained the sibling clause + the measure note). ⛔ EXCLUSION REMOVED (advisor option B): `match_reshape_of_reshape` no longer excludes `T_out==T_in`, so the pair {reshape∘reshape, identity_reshape} COMPOSES to FULLY COLLAPSE a net-identity chain `T→U→T → x` (fold emits `reshape(x:T):T`, identity-elim RAUWs it to x — 2 internal rounds). Added to `kCanonPatterns`. ⛔ MEASURE (advisor, precise): the fixpoint measure is LIVE RESHAPES ON ANY x→consumer PATH (≤ N0, the rewrite_driver cap holds) — NOT total op count (the fold GROWS it +1/fire pre-DCE; identity-elim SHRINKS live ops via RAUW-to-operand). 2 device-free gates (test_tensor_pipeline.cpp #832-833): (1) FULL-COLLAPSE T→U→T→x (reduce reads the gemm directly) + ⛔ PRE-DCE FIXPOINT gate (advisor: `canonicalize_run` true then FALSE on the 2nd call BEFORE dce — proves fixpoint with the dead reshapes STILL LINKED, catching a re-match-on-tombstone bug that idempotence-after-DCE would hide); (2) DIRECT identity `reshape(gemm:[4,4]):[4,4]→gemm` (identity-elim fires standalone) + ⛔ PLAN LEG (advisor): RAW Alias==1 → after elim+DCE Alias 1→0 with the reduce reading the gemm buffer DIRECTLY (2 stages Gemm+Reduce) — a plan topology 26b-2 NEVER produced (there one Alias always remained); the plan SEES RAUW-to-operand vs RAUW-to-a-same-typed-alias, the IR check can't. ⛔ NO DEVICE LEG (advisor, source=scoreboard): identity-elim changes no dispatch + no buffer — a device run would compare a buffer to itself; the 26e dispatched-stage fusion is the first pattern whose device leg is load-bearing. ⛔ the 26b-2a identity-EXCLUSION negative test is SUPERSEDED — STRUCK IN PLACE, replaced by the full-collapse gate. ⛔ FINDING: a same-type reshape IS authorable (`find_tensor_misuse==None`). GREEN win-debug [ceir][tensor-pipeline] 9/9 (incl. #831 fold survives the exclusion removal, #4748 Vk + #4827 DX12 differentials re-green) + tidy-20 clean (canonicalize.hpp + test). ✅✅✅ **26b (canonicalize/fold) COMPLETE** = 26b-1 (the greedy driver) + 26b-2 (reshape∘reshape, 3 legs: device-free/Vk/DX12) + 26b-3 (identity-elim + full-collapse). ⛔ NO 26b-4 for arith.const-fold (advisor): const-fold has ZERO live consumers (grid consts are leaves) — "absorb with a real consumer, never build speculatively." NEXT = **26c** (CSE — common-subexpression elimination: the next optimizer pass, same collect-per-round skeleton; advisor at 26c-open). ⛔ 26z checklist carries: (1) llvmpipe re-run of the 26b-2 reshape-fold differential. | build §73/§74; proof §120 |

**26c-1 ✅ CLOSED 2026-09-04 (Common-Subexpression Elimination — the pass + a device-free unit gate; the plan + device legs are 26c-2)**: added `engine/ceir/include/crd/ceir/passes/cse.hpp` (`cse_pass()`/`cse_run` + `cse_region`/`cse_is_candidate`/`cse_structurally_equal`) + `tests/ceir/test_cse.cpp` (5 gates) + the CMake row. ⛔ STANDALONE PASS, NOT a canonicalize RewritePattern (advisor design-gate): CSE needs a per-block `seen` set — STATE a capture-free `match(const Context&, const Operation&)` fn-ptr cannot carry — so it copies the `dce_run` shape (collect-then-erase + fixpoint), not the 26b greedy driver. ⛔ SELF-CONTAINED — CSE RAUWs then ERASES the dup DIRECTLY, NO dce compose (advisor source≠scoreboard fix): a dup's operands are pointer-shared with its surviving twin, so erasing the dup removes the last use of NOTHING and orphans no op for DCE — unlike canonicalize's RAUW-and-leave (its fold leaves a dead input). ⛔ ELIGIBILITY = `leaf ∧ registered ∧ Pure ∧ has-results ∧ NOT-positively-nondeterministic` (dce_is_dead's first four + §27): a POSITIVE nondeterminism claim (`Nondeterministic`/`ExternalNondeterminism`) DISQUALIFIES — CSE replaces an op with an earlier twin ASSUMING twin-result==op-result, which rests on device-bit-exact-by-construction (same kind+operand Values+device ⇒ same kernel ⇒ same bits, 26c-2 proves it); a positive nondeterminism claim breaks that. ⛔ `Unspecified` does NOT disqualify (advisor): absence-of-claim ≠ a nondeterminism claim (the §27 doctrine — EMPTY≠UNKNOWN in CSE form); no op-kind carries `Nondeterministic` TODAY so the clause disqualifies nothing live, but it lives in the eligibility fn so it LANDS the day one does (registered-default-empty), and a synthetic `cs.nd` op GATES it so it can never silently regress to a no-op. ⛔ STRUCTURAL EQUALITY = same kind + operand ARITY with each operand Value* POINTER-equal (SSA values are unique — DEEP operand equality would be WRONG, fusing reads of two distinct-but-equal-typed inputs) + result arity/each type equal + attrs equal ORDER-INDEPENDENTLY (attr names are unique per op — set_attr overwrites — so equal `num_attrs` + per-name lookup of every `a` attr in `b` is total; an absent name reads back an invalid AttrId≠a's valid one). ⛔ NEVER add a RAUW'd dup to `seen` (advisor correction #1 — the monotone-tombstone-chain scar in CSE form): a THIRD identical op must find the SURVIVOR, not the just-eliminated tombstone. ⛔ SAME-BLOCK dominance scope (earlier-in-block dominates a graph-region successor); cross-block/region CSE needs a real dominance tree — name-forward this slice (the DCE-leaf precedent). ⛔ 5 GATES (device-free, synthetic `cs.*` dialect: src/mul/add Pure, sink/eff NOT Pure, nd Pure+Nondeterministic; GREEN win-debug [ceir][cse] 5/5 #543-547 + tidy-20 clean both files; ⛔ the cascade gate's FIRST-RUN red was a NON-ASCII em-dash in the TEST_CASE name [ctest codepage → "no tests ran", the 20b non-ASCII scar], NOT a logic failure — renamed `—`→`-`, re-green): (1) a duplicate `mul` eliminated + its consumer REWIRED to the survivor + fixpoint (2nd run false); (2) THREE identical → all to the FIRST in one pass, s3 reads %m1 NOT the tombstoned %m2 (the load-bearing correction-#1 gate); (3) CASCADE — `add(%a2)` collapses onto `add(%a1)` only AFTER a2→a1 rewired its operand (b2 erased is the cascade proof); (4) STRUCTURAL negatives — ops differing by OPERAND (pointer), attr value, or result type KEPT, each pair given a unique tag so pairs never cross-collide (a dropped tag = an accidental twin, caught during authoring); (5) ELIGIBILITY negatives — an effectful (non-Pure) twin (the effect-narrowing scar in CSE form — CSE'ing would drop a side effect) AND a Pure-but-Nondeterministic twin both KEPT. ⛔ ADVISOR RECONCILE (primary-source, to raise post-impl): RAUW is IMMEDIATE, so a straight-line cascade collapses in ONE productive round (the later dup's operand is rewired before it is scanned), NOT the advisor's sketched two — the end-state is identical (gates assert end-state, not round count); the fixpoint loop is the DCE-shaped convergence guarantee for any op ordering + the future cross-block extension. ⛔ per WHOLE-REPO=CI: linux/gcc -Werror = CI's job (local blast-radius green). NEXT = **26c-2** (the plan + device legs): a §137-pipeline corpus with a DUPLICATED gemm (`D1=gemm(A,B,C)`, `D2=gemm(A,B,C)`) → CSE collapses D2→D1, ONE dispatched stage disappears (plan stages N→N-1 — the first CSE-observable-in-the-plan leg, unlike 26b's alias-only fold) AND the device output stays BIT-EXACT vs the RAW program (the 26a witness at FULL teeth again — a removed DISPATCH, Vk then DX12). ⛔ VERIFIED-BEFORE-THE-NEXT-TICK (advisor #2 — the band-open lock's "find the real consumer" flag): `linalg::build_gemm(ctx, a, b, c, α, β, trans…)` takes ONLY the A/B/C DATA tensors as operands (result D = a fresh planner-assigned buffer, NO per-op `resource.declare` operand — the paired declare is grad.cpp's `mk_gemm` output-buffer bookkeeping, NOT part of the dialect op), so two identical `gemm(A,B,C)` with the same operand Values + attrs ARE structurally equal → CSE fires → the duplicate-gemm corpus is VIABLE (advisor case (b), the fresh-result-buffer case; NOT the dead-on-arrival case (a) where a per-op declare would make every gemm unique). 26c-2 opens on authoring that corpus. ⛔ 26z checklist carries: (1) llvmpipe re-run of the 26b-2 reshape-fold differential + (2) llvmpipe re-run of the 26c-2 CSE differential. | build §73/§74; proof §120 |

**26c-2a ✅ CLOSED 2026-09-04 (the CSE PLAN leg — CSE collapsing a duplicate gemm REMOVES a dispatched stage, device-free)**: added a `[ceir][tensor-pipeline]` gate in test_tensor_pipeline.cpp (#840) + the cse.hpp include. Corpus = two IDENTICAL `gemm(A,B,C):[4,4]` feeding two DISTINCT reduces (`reduce(d1,axis0,"sum")` + `reduce(d2,axis0,"max")` — different attr ⇒ NOT CSE-equal ⇒ both KEPT), both terminals pinned via `func.return` (26a-2 liveness contract). RAW plans 2 Gemm + 2 Reduce = 4 stages → `cse_run` (d2→d1, g2 erased, r2 rewired onto d1) → 1 Gemm + 2 Reduce = 3 stages — the FIRST CSE-observable-in-the-plan drop (26b's alias-only folds were stage-invariant). ⛔ ADVISOR-STRENGTHENED to TOPOLOGY not count (3 blocking gaps caught + fixed this tick): **(#2, identity-not-category)** RAW's two Gemm stages write DIFFERENT output buffers (`bind[2]`, β=0 drops C ⇒ nbind=3) — if they aliased, the planner had already deduped and CSE would prove NOTHING; **(#1, plan wiring)** in OPT both Reduce stages' input `bind[0]` == the ONE surviving Gemm's output `bind[2]` (the 22c-1 `fft_in_re`-aliases-gemm precedent — "3 stages" is a count, this is the topology a wrong-buffer Reduce would fail) + both reduces write DISTINCT `bind[1]` outputs (kept, not merged); **the `CHECK_FALSE(cse_run)` after the first run IS the pass-level attr negative** (a bug ignoring attrs would merge sum/max and return true) and `opt_reduce==2` is its PLAN twin. ⛔⛔ FINDING (source=scoreboard, empirical — corrected the advisor's n_out==2 sketch): the planner marks EXACTLY ONE terminal as `Output` (the readback target); other terminals are `Intermediate`/GpuOnly (the 26a-2 `g0`=dW1 Output / `g1`=dW2 Intermediate precedent), so `n_out` is a planner ROLE choice not "how many results" — the role-agnostic "two results" fact is the two Reduce stages' DISTINCT output buffers (gated). GREEN win-debug [ceir][tensor-pipeline] #840 (25 assertions) + tidy-20 clean. ⛔ per WHOLE-REPO=CI: linux/gcc leg is CI's. NEXT = **26c-2b** (the Vk device BIT-EXACT leg — the 26a witness at FULL teeth on a REMOVED DISPATCH, unlike 26b's alias-only fold): the §137-style duplicated-gemm corpus runs RAW on real Vulkan → `cse_run` (one gemm dispatch removed) → OPT → read back BOTH reduce scalars, `opt==raw` bit-exact + correct vs an independent f64 ref. ⛔ DECISION for the 26c-2b tick (advisor #3, NOT an assumption): `max` of a gemm output is a SELECTION (bit-exact on any device trivially — the WIRING witness), `sum` is the load-bearing ACCUMULATION bit-exact witness — so either KEEP sum+max (name which output carries which teeth) OR make BOTH `sum` over different axes IF `find_tensor_misuse` accepts `axis=1` (check first, never assume); the readback reads the Output-role scalar directly + the other by-value (the single-Output finding). ⛔ 26z checklist carries: (1) llvmpipe re-run of the 26b-2 reshape-fold differential + (2) llvmpipe re-run of the 26c-2 CSE differential. | build §73/§74; proof §120 |

**26c-2b ✅ CLOSED (Vulkan device leg) 2026-09-04 — CSE duplicate-gemm removal BIT-EXACT vs RAW + FOUND & FIXED a real CSE correctness bug (resource.declare was Pure → CSE merged distinct inputs)**: added a `[ceir][tensor-pipeline][gpu]` gate in test_ceir_pipeline_vulkan.cpp (#4756, 69 assertions) — two IDENTICAL `gemm(A,B,C):[8,8]` each reshaped `[8,8]→[64]` (alias) then FULL-reduced to a scalar by TWO DISTINCT consumers that keep BOTH gemms live in RAW (`max` = a SELECTION, the wiring witness; `sum` on d2 = the REWIRED consumer, an ACCUMULATION ⇒ the load-bearing bit-exact teeth); the rank-1→rank-0 device-proven reduce path (⛔ `emit_reduce_glsl` rejects out_n>1, so NO rank-2 partial reduce — reshape-to-scalar). RAW 2 Gemm + 2 Reduce → `cse_run` (one gemm DISPATCH gone) → 1 Gemm + 2 Reduce; both reduce scalars BIT-EXACT `opt==raw` + correct vs an independent f64 ref (the 26a witness at FULL teeth on a REMOVED DISPATCH — unlike 26b's alias-only fold). ⛔⛔⛔ THE FINDING (the device leg's whole reason — invisible to 26c-2a's plan gate): the OPT run gave `opt_max==opt_sum==37` and `o_ga==o_gb==0` (the gemm bound buffer 0 for BOTH A and B) → **CSE had merged the three structurally-identical `resource.declare` inputs `a_in`/`b_in`/`c_in` into ONE buffer → the gemm computed A@A.** ROOT CAUSE: `resource.declare` was registered `Pure`, but a declare is an EXTERNAL SOURCE / allocation — its value is keyed by IDENTITY (host upload), NOT by operands/attrs, so it is Pure (side-effect-free) but NOT referentially transparent; CSE's same-structure⇒same-value assumption is violated (the MLIR alloc-is-not-CSE-able rule; `arith.const` by contrast IS transparent — the discriminator is allocation-vs-computation, NOT 0-operands). ⛔ FIX (advisor-designed, principled): `resource.ceirop.toml` declare `traits=[]` + `effects=[{family="Allocate", result=0}]` → regenerate (`python tools/ceir_opgen/ceir_opgen.py`, drift-clean). Blast radius VERIFIED clean: **all 501 device-free ceir tests green** (12b intent / 12c alias / 12d memory-planner untouched — planner marks declare ExternalIn by TYPE not trait); DCE unaffected (non-Pure⇒kept, declare always-used); §27 mode-legality untouched (Allocate is not a determinism claim — why tagging `ExternalNondeterminism` would have been WRONG: it breaks Deterministic mode for every program with inputs). ⛔ REGRESSION GATE (LOCKED-guard): test_cse.cpp registers the REAL resource dialect — two `resource.declare` KEPT (not merged), two same-named `resource.import` (Pure, same external) STILL merge (the declare-specific proof, teeth). Scar [[feedback_cse_must_not_merge_external_source_ops_declare_is_alloc_not_pure]]. ⚠ SIBLING flagged not fixed: anonymous `resource.import` still Pure (no consumer merges two today). GREEN win-debug: 26c-2b Vulkan real RTX 4070 Ti (69 assertions) + all 501 ceir (3923 asserts) + 26c-2a #840 + [cse] 6/6 (75 asserts, incl. the new gate) + opgen-drift + tidy-20 clean (test_cse.cpp, test_ceir_pipeline_vulkan.cpp). ⛔ per WHOLE-REPO=CI: linux/gcc leg is CI's. NEXT = **26c-2c** (the DX12 device leg — mirror the duplicate-gemm bit-exact differential in test_ceir_pipeline_dx12.cpp; the declare fix already lands via the shared crd-ceir lib. ⛔ (advisor) NOT merely "same corpus green": the 26c-2b runner FORCES both reduce outputs GpuToCpu because the planner marks only ONE terminal Output — on DX12 that maps to the fresh-rb+copy-per-output readback mechanism (the 22c-3e portable dev/up/rb pattern), so open 26c-2c on the RUNNER shape (both reduce outputs need their own rb+copy), not the assumption. Then 26c-2 COMPLETE → 26c band-close review). ⛔ 26z checklist carries: (1) llvmpipe re-run of the 26b-2 reshape-fold differential + (2) llvmpipe re-run of the 26c-2 CSE differential. | build §73/§74; proof §120 |

**26c-2c ✅ CLOSED (DX12 device leg) 2026-09-04 → ✅✅ 26c-2 COMPLETE → ✅✅✅ CEIR-26c (CSE) COMPLETE**: the CSE duplicate-gemm-removal is BIT-EXACT vs RAW on DX12 too — the DX12 mirror of 26c-2b (#4836, 71 assertions). SAME corpus (two identical gemms → reshape[8,8]→[64] alias → two DISTINCT scalar reduces: max=wiring witness + sum=rewired accumulation teeth), the DX12 portable dev/up/rb materialization with a FRESH rb+copy PER reduce output (advisor: the planner marks only ONE terminal Output — the 26a-2 g0/g1 finding — so each output gets its own rb, NOT a single-Output readback). RAW 2 Gemm + 2 Reduce → `cse_run` (one gemm DISPATCH gone) → 1 Gemm + 2 Reduce; both scalars `opt==raw` bit-exact + correct vs f64 ref. ⛔ the resource.declare Allocate-effect fix (26c-2b) rode the shared crd-ceir lib → GREEN FIRST TRY (the gemm operand value-identity check `o_ga==a_in ∧ o_gb==b_in` confirms declares NOT merged on DX12 either). GREEN win-debug real D3D12 (RTX 4070 Ti) #4836 + tidy-20 clean. ⛔ FULL blast-radius of the declare change RE-VERIFIED this session: 501 device-free ceir + full crd-ceir-gpu (1291, incl. the 26a-2 prune 3→2) + the 25c-2 crowns carrying the 26a-3 DCE legs (Vk #4754 / DX12 #4834) + 26b-2 reshape-folds (#837/#4755/#4835) + 26c-2a #840 + [cse] 6/6 + opgen-drift — all green. ✅✅✅ **CEIR-26c (Common-Subexpression Elimination) IS DONE** — 26c-1 (the standalone `cse_run` pass: leaf∧registered∧Pure∧has-results∧not-positively-nondeterministic, structural equality with Value-pointer operands + order-independent attrs, never-tombstone-in-seen, 5 device-free unit gates) + 26c-2 (device legs: 2a plan-drop 4→3 device-free + 2b Vk #4756 + 2c DX12 #4836 bit-exact on a REMOVED DISPATCH — the 26a witness at full teeth, unlike 26b's alias-only fold). ⛔⛔⛔ THE BAND'S BIGGEST FIND: the 26c-2b device leg exposed `resource.declare` being wrongly `Pure` → CSE merged distinct inputs → gemm A@A; fixed with an Allocate effect (non-Pure), [[feedback_cse_must_not_merge_external_source_ops_declare_is_alloc_not_pure]]. NEXT = **26d** (specialize/inline). ⛔⛔ ADVISOR STEER (26c-close): 26d is §73's single row but TWO passes with DIFFERENT consumer situations — open it as a **CONSUMER CENSUS, not a pass** (the 26b-4 "no live consumer ⇒ don't build it, absorb with a real consumer" rule applies to EACH separately): **(a) inline** (`func.call`→body subst) — census the `func.call` count in EVERY planned corpus (§137 assets / quant_mlp / attention / autodiff-backward are all a single `main` body; does `plan_tensor_pipeline` even follow a call?) → likely ZERO live consumers ⇒ SKIP + record why; **(b) specialize** (the real candidate) — census every op with an `arith.const` OPERAND and whether a const-aware form exists or is worth building (26b-4 noted grid consts are leaves; the 18p spec-const + 23b QuantGemm-full-attr-selection precedents) → the design Q is whether any op's BEHAVIOR changes given a const operand. ⛔ HONEST OUTCOME may be "26d has no live phase-1 consumer, ABSORBED into 26e (a fused kernel IS the specialization) — a correct one-line row, NOT a skipped slice; don't build a pass to have one." The 26d-open advisor call presents the census (a/b counts + outcome), it does NOT design a pass. ⛔ 26e-open bar is ALREADY SET (26c-2 delivered the removed-dispatch witness early): a fusion that changes which kernels run, bit-exact vs raw, on BOTH backends. ⛔ 26z BAND-CLOSE CHECKLIST (carries): (1) llvmpipe re-run of the 26b-2 reshape-fold differential + (2) llvmpipe re-run of the 26c-2 CSE differential + (3) the anonymous-`resource.import` CSE sibling (still Pure — flag/decide at band-close). | build §73/§74; proof §120 |

**26d-0 ✅ CLOSED 2026-09-04 (the CONSUMER CENSUS — §73 specialize/inline is TWO passes; census each per the 26b-4 "no live consumer ⇒ don't build it" rule; NO pass built this slice)**: the advisor-mandated census (a census, not a pass — this IS a legitimate one-slice unit). ⛔ **(a) INLINE (`func.call`→body subst) = NO live consumer → DROPPED, name-forward.** Censused BOTH axes: (i) `func.call` = **0** in every authored asset (tensor_pipeline/quant_mlp/attention.ceir are each a SINGLE `func.func{main}` body + `func.return`); (ii) `call_kind`/`build_call`/`intern_op("func","call")` = **0** across `engine/ceir-gpu` + all three ceir-gpu test dirs — no call is EVER constructed in the optimizer's domain. `plan_tensor_pipeline` (tensor_pipeline.cpp:184-211) finds the FIRST `func.func`'s body + walks that ONE block; an unhandled op hits `UnsupportedOp` ⇒ a call would REJECT, it does NOT follow calls. ⛔ the `plan.cpp:784` call-graph work-list IS a real call graph but it's the GENERAL CEIR CPU-eval/compiled-function path (CEIR-4a/5c `eval_call`), a DIFFERENT planner — NOT the optimizer's tensor-pipeline domain. → inline lands the day a corpus emits `func.call` (a shared-subgraph asset); until then, building it is speculative (26b-4). ⛔ **(b) SPECIALIZE = HAS a live consumer → 26d becomes a SHAPE-SPECIALIZATION pass** (advisor CORRECTED my too-narrow const-operand-only census — the real axis is SHAPE): const-OPERAND census confirmed `arith.const` operands are dispatch-grid/draw-count LEAVES resolved at LOWER time (`resolve_const_u32` lower.cpp:123 / render_materialize.cpp:86) — NO behavior-changing const consumer (the attr-aware kernel SELECTION that exists — 23b QuantGemm fusion, 24c coopvec claim — keys on full semantic ATTRS at the plan/provider layer = 26e fusion territory, not a const fold); BUT the **baked-kernel SHAPE rejects are a live consumer**: `expand_ml.cpp:113-122` (relu.ckir bakes `local_size=32`, EVERY relu'd MLP intermediate must be `M·hidden==32` else `BakedKernelShapeUnsupported`) + `expand_ml.cpp:166-171` (transpose.ckir bakes Sk=3,D=4 + softmax.ckir bakes Sq=2,Sk=3 — ANY other attention dims rejected) + `grad.cpp:406` (relu/relu_vjp both bake `local_size=32`, NO bound guard). An `ml.mlp` hidden width≠32 / `ml.attention` dims≠(Sq=2,Sk=3,D=4) is **REJECTED TODAY** — shape-specializing a kernel for its actual operand shape IS §73 specialization with a REAL, non-speculative consumer. ⛔ SOURCE=SCOREBOARD self-catch: I first censused only const operands + nearly recorded "no consumer" — that would have been a FALSE negative against the documented 24z reject (the source≠scoreboard shape); the advisor widened it to shape, verified before recording. GREEN: census is grep-only (no code built) — no gate this slice. NEXT = **26d-1** (the MECHANISM advisor gate — NOT a build-yet): examine relu.ckir's `local_size` structure + decide **SpecConst-on-authored-.ckir** (the 18p precedent — `local_size` as a spec-const set at pipeline-creation from the actual shape; keeps relu.ckir ONE authored asset, mandate #1) vs **cook-time per-shape synth** (the CEIR-25 `synth_transpose` precedent); name the first consumer = the non-32-width `ml.mlp`. ⛔ THE PROVING GATE (26d-2, both backends): the `BakedKernelShapeUnsupported` NEGATIVE flips POSITIVE — an `ml.mlp` with hidden width≠32 runs DEVICE-RESIDENT bit-exact vs the float oracle on Vk+DX12 (the 24z scar [[feedback_locked_checklist_item_needs_a_gate_or_it_silently_doesnt_land]] discharged: the reject-test becomes a run-test). ⛔ 26z checklist unchanged (carries the 3 items above). | build §73/§74; proof §120 |

**26d-1 ✅ CLOSED 2026-09-04 (the MECHANISM advisor gate — DECISION only, no build; the shape-specialization mechanism + sub-slice plan locked)**: examined `relu.ckir` (elementwise `out[gid]=max(in[gid],0)`, `local_size=[32,1,1]`, grid (1,1,1), gid=LocalInvocationIndex, NO bound guard — ONLY the dispatch extent is baked, the algorithm is shape-agnostic) + the CKIR spec-const machinery (D-007 "D12"/18p: `KGraph::spec_constant(id,default,dt)` + `set_spec_const` patches the loaded node BEFORE emit; emitter `layout(constant_id=N)` both backends; DX12 BAKES the default per-cook) + `synth_transpose`/`synth_broadcast`/`synth_elementwise` (25 cook-time shape-generic KGraph builders). ⛔ **DECISION = M2 = COOK-TIME BINDING OF THE AUTHORED ASSET** (advisor: M2 needs NO new emitter feature — spec-const is ALREADY cook-time bake, and `entry.local_size` is the SAME kind of field, so the mechanism is load→patch→emit). ✅ CONFIRMED FEASIBLE this tick: `ckir_glsl.hpp:724` emits `layout(local_size_x = <entry.local_size[0]>…)` — the emitter READS local_size from the entry at emit time, so patching `entry.local_size[0]` before emit is zero-emitter-change. TWO knobs, both existing-pattern: **(i)** entry `local_size` ← write-operand NUMEL (relu, relu_vjp — body UNTOUCHED, single-WG, LocalInvocationIndex still covers `[0,numel)` exactly, NO bound guard, the scalar `eval_cpu_kernel` model preserved); **(ii)** body spec-consts ← dims via `set_spec_const` (softmax Sq/Sk — the 18p mechanism verbatim, softmax bakes row length into body MATH not just local_size). ⛔ REJECTED **M1** (fixed local_size + multi-WG + bound guard: changes gid Local→Global, re-derives the grid — violates `tensor_pipeline.cpp:208` "asset drives the grid" — and stresses the single-WG scalar oracle) + **M3-for-relu** (synth_relu / delete relu.ckir: `synth_*` is the lowering for DIALECT ops with declared semantics [tensor.transpose/broadcast]; relu is NOT a dialect op — it's the 23c crown's AUTHORED activation, deleting it to move `max(x,0)` into C++ regresses the emphatic everything-authorable mandate, [[feedback_everything_is_an_authorable_asset_ceir]]). ⛔ FIVE BUILD BLOCKERS (advisor, ordered — the 26d-2 spec): **(1)** OPT-IN must be ASSET-DECLARED not an implicit cook override (blanket-patching every VizDispatch kernel's local_size would silently rewrite fft's `local_size=n/2`): SENTINEL `local_size=[0,1,1]` in the entry = "bind x from the write operand's numel at cook" (0 is an invalid literal local_size → schema-neutral, unambiguous); check whether `ckir_read` rejects 0 (if so that validation moves POST-bind) + an UNBOUND 0 reaching emit is a TYPED COOK ERROR ([[feedback_declared_header_words_must_be_validated_at_cook_time]]; ⛔ NOTE emitter line 1799 already treats local_size 0→1 in ONE variant → the sentinel MUST be caught pre-emit, never silently →1); document the sentinel in relu.ckir's header. **(2)** TRANSPOSE-in-attention: CHECK whether `expand_ml` can emit `tensor.transpose` (→ `synth_transpose` ALREADY shape-generic, the 25 decision) instead of the baked transpose.ckir dispatch — if yes that reject dies with ZERO new mechanism; DON'T build knob (ii) for transpose. **(3)** CAP + typed reject: numel > device `maxComputeWorkGroupSize[0]` (1024 on 4070 Ti/DX12; 128 portable floor) → a NEW typed reject `LocalSizeExceedsLimit` at the resolver (device known at cook), NOT a silent fallthrough; REMOVE `expand_ml.cpp:113-122`'s `M·hidden==32` pre-check (can't know the device; capping at the 128 floor would LEVEL DOWN — [[feedback_use_every_api_ability_never_level_down_to_the_common_denominator]]); multi-WG name-forward. **(4)** ORACLE follows ONLY if `eval_cpu_kernel` reads its lane count from `entry.local_size` (26d-2 FIRST check — the ckir_eval.hpp grep found no local_size there, the lane path is in eval_cpu_kernel/dispatch_kernel_1wg; if it hardcodes, that's a 2nd patch site). **(5)** PATCH SITE = the VizDispatch resolver (where `.ckir` loads by symbol via resolve_program→KGraph + the stage's bound-buffer Values carry the tensor types) — READ FIRST in 26d-2. ⛔ HONEST SCOREBOARD (source=scoreboard): this is **COOK-TIME KERNEL-SHAPE SPECIALIZATION at the CEIR→CKIR boundary, NOT a CEIR IR pass** (nothing in the module to rewrite — the shape is ALREADY in the operand types); it lives in ceir-gpu, NOT `engine/ceir/passes`. §73 "specialize" satisfied — this is the KIND. ⛔ SUB-SLICE PLAN: **26d-2 (relu ONLY, don't collapse)** = an `ml.mlp` hidden width ≠32 (pick non-pow2-multiple, e.g. hidden=64 or M·hidden=96) runs DEVICE-RESIDENT Vk+DX12 == the FLOAT ORACLE (⛔ the 24b device-vs-oracle standard — there is NO "raw" for a previously-REJECTED program, so the 26 bit-exact-vs-raw differential does NOT apply here; SAY SO in the gate) + the 32-width path UNCHANGED (23c crown + 24b MLP re-green bit-exact = the REGRESSION leg) + a numel>cap NEGATIVE at the resolver (so `BakedKernelShapeUnsupported`'s teeth aren't merely deleted — [[feedback_deleting_reference_in_ab_parity_test_degrades_to_can_t_fail]]); **26d-3** = softmax spec-consts + transpose-via-synth; **26d-4** = relu_vjp/grad. NEXT = **26d-2** (build relu shape-specialization: read the VizDispatch resolver FIRST [blocker 5] + confirm the eval_cpu_kernel lane source [blocker 4], then the sentinel + cook-bind + cap-reject + the non-32 device gate). ⛔ 26z checklist unchanged (carries the 3 items above). | build §73/§74; proof §120 |

**26d-2a ✅ CLOSED 2026-09-04 (the 26d-2 ORIENTATION — advisor blockers 4+5 resolved + the exact seam/helper/call-sites pinned; NO code, read-before-write per the advisor)**: **BLOCKER 4 (oracle lane source) RESOLVED** — `eval_cpu_kernel(g, entry, bufs, nbufs, local_size, …)` (ckir_kernel_eval.hpp:54) takes `local_size` as an EXPLICIT CALLER PARAM, it does NOT read `entry.local_size` — so the SENTINEL (local_size=0 in the entry) is INVISIBLE to every eval-based reader (they pass a literal), and the 26d-2 device gate's oracle is a float-MLP computed directly in the test (not eval_cpu_kernel of relu.ckir) ⇒ NO 2nd patch site. **BLOCKER 5 (patch site) RESOLVED** — `execute_tensor_pipeline` is BACKEND-FREE and calls a CALLER-supplied `StageResolveFn` per stage; each device gate has its OWN `resolve_stage` whose VizDispatch branch (test_ceir_pipeline_vulkan.cpp:213-239) `ckir_read`s the kernel .ckir → `emit_compute_kernel_glsl` → grid from the authored arith.const operands (`const_grid`). ⛔ THE SEAM = BETWEEN `ckir_read` (produces the KEntry `ve` w/ local_size) and `emit_compute_kernel_glsl` — patch `ve.local_size[0]` there. Since ceir-gpu owns no .ckir file loading (the resolve_program convention is caller-side), the cook-bind is a SHARED ceir-gpu HELPER the resolvers call: `bind_authored_kernel_shape(const Context&, const PlanStage&, kir::KEntry& ve, u32 max_local_size) → KernelShapeError{None,LocalSizeUnbound,LocalSizeExceedsLimit}` — if `ve.local_size[0]==0` (sentinel) set it to the numel of the stage's TRAILING WRITE operand's tensor type (relu is elementwise, out numel==in numel; grid stays (1,1,1) from the asset ⇒ ONE workgroup of numel threads covers `[0,numel)` exactly, body untouched), capped at `max_local_size` (device `maxComputeWorkGroupSize[0]`, known at resolve = cook) else `LocalSizeExceedsLimit`; a non-sentinel local_size is left as-authored; an unbound-0 with no resolvable write ⇒ `LocalSizeUnbound` (NEVER silently 1 — the ckir_glsl.hpp:1799 `0→1` variant must never see it). ⛔ CALL-SITE INVENTORY (the coupled atomic change — the sentinel breaks ONLY EMIT sites, not eval sites): the TWO device resolvers `test_ceir_pipeline_vulkan.cpp:219` + `test_ceir_pipeline_dx12.cpp:211` EMIT relu.ckir; `test_ckir_viz.cpp:189` (the 23c-a gate) READS relu.ckir → CHECK in 26d-2b whether it passes a literal 32 to eval_cpu_kernel (safe) or reads `ke.local_size` (would need the numel); expand_ml.cpp / grad.cpp reference relu only in the pre-check to REMOVE. ⛔ SUB-SPLIT (26d-2 is big — coupled multi-file + device gate): **26d-2b** = the atomic mechanism — relu.ckir local_size `[32,1,1]→[0,1,1]` + sentinel header note + the cook-bind helper [⛔ SHIPPED as the inline PURE policy `bind_authored_local_size(u32&,u64,u32)` in tensor_pipeline.hpp — NO new file, no reconfigure; the planned `bind_authored_kernel_shape(ctx,stage,ve,…)` signature was simplified to pure u32/u64 so the resolver supplies the write-numel it already computes] + wire BOTH device resolvers + confirm test_ckir_viz safe + BUILD + the 32-width REGRESSION green (23c crown + 24b MLP unchanged, oracle-tolerance); **26d-2c** = the non-32-width `ml.mlp` DEVICE gate Vk+DX12 == float oracle + the numel>cap NEGATIVE (`LocalSizeExceedsLimit`) + REMOVE `expand_ml.cpp:113-122`'s `M·hidden==32` pre-check. NEXT = **26d-2b** (the atomic relu cook-bind mechanism; advisor after it lands). ⛔ 26z checklist unchanged (carries the 3 items above). | build §73/§74; proof §120 |

**26d-2b ✅ CLOSED 2026-09-04 (the ATOMIC relu cook-bind MECHANISM — sentinel + helper + both device resolvers wired + 32-width regression GREEN on real Vk+DX12)**: the M2 cook-time shape-specialization mechanism LANDED. **(1)** `assets/ckir/relu.ckir` local_size `[32,1,1]→[0,1,1]` (the SENTINEL) + a header note documenting "0 = bind from the write numel at cook; unbound-0 at emit is a resolver error, never silently 1". **(2)** `bind_authored_local_size(u32& local_size_x, u64 write_numel, u32 max_local_size)→KernelShapeError{None,LocalSizeUnbound,LocalSizeExceedsLimit}` + `kMaxAuthoredLocalSize=1024` (the guaranteed single-WG floor for BOTH Vulkan maxComputeWorkGroupSize[0]≥1024 AND D3D12 max-threads/group=1024 — the CORRECT max, NOT a level-down) added to `tensor_pipeline.hpp` — a PURE u32/u64 POLICY (no ceir/kir coupling ⇒ device-free unit-testable): sentinel-0 ⇒ set to write_numel capped, non-sentinel ⇒ left as authored (fft n/2, viz, softmax/transpose baked dims untouched). ⛔ HONEST: this is a COOK-TIME bind at the CEIR→CKIR boundary, NOT a CEIR IR pass (§73 "specialize" satisfied; the kind named). **(3)** BOTH device resolvers wired (test_ceir_pipeline_vulkan.cpp + _dx12.cpp) — BETWEEN `ckir_read` and `emit_compute_kernel_{glsl,hlsl}`, `if (st.n_out>=1) bind_authored_local_size(ve.local_size[0], tnumel(trailing-write operand), kMaxAuthoredLocalSize)`; a non-None error returns an unresolved stage (⇒ UnresolvedKernel). Trailing-write operand index = `3 + nbind - n_out` (grid 0..2 then binds). **(4)** `test_ckir_viz.cpp` (the 23c-a CPU relu gate — an EMIT site, not a resolver) sets `ke.local_size[0]=32` before the emit smoke (its eval_cpu_kernel already passes len=32 explicitly — the sentinel is invisible to eval, blocker-4 confirmed). ⛔ BUILD GREEN win-debug: crd-ceir-gpu (lib rebuilt, header helper compiles) + crd-ceir-gpu-vulkan-tests + crd-ceir-gpu-dx12-tests + crd-kir-tests all link clean. ⛔ 32-WIDTH REGRESSION GREEN on REAL hardware (ctest `-R relu`, 19/19, RTX 4070 Ti + DX12): 23c-c/d quant-MLP crown (QuantGemm→relu→QuantGemm) Vk+DX12, 24b-4 expanded ml.mlp (gemm/relu) Vk+DX12, 25c-1b relu_vjp device Vk+DX12, 25c-2 vjp_mlp backward (dW1+dW2) Vk+DX12, + the kir 23c-a relu-viz eval — the cook-bind yields local_size=32 from the numel = the old baked value, so the existing 32-width gates RE-GREEN at oracle-TOLERANCE (advisor: NOT a bit-exact compare — the 26d-2c non-32 oracle compare is the correctness proof; this leg proves the mechanism didn't regress the shipped corpora); relu_vjp.ckir UNCHANGED — still local_size=32, only relu.ckir carries the sentinel this slice. ⛔ ADVISOR-HARDENED THIS SLICE (blocker-1, not deferred): an UNBOUND sentinel reaching `emit_compute_kernel_glsl`/`_hlsl` now RETURNS FALSE (a typed refuse-loudly ⇒ UnresolvedKernel) — closes the ckir_glsl.hpp:1799 silent `0→1` risk (that path is the MESH emitter, NOT compute-reachable for relu, but the guard on BOTH compute emitters is belt-and-braces). ⛔ ADVISOR-VERIFIED: relu `access="r,w"` (expand_ml:141, grad:430) + relu_vjp `"r,r,w"` (grad:141) — out always LAST, no `rw` in-place ⇒ `wop=3+nbind-n_out` picks the correct write operand. ⛔ TIDY: all 5 changed C++ files tidy-clean (tensor_pipeline.hpp + the 2 resolvers + test_ckir_viz.cpp + the ckir_glsl.hpp guard); ckir_hlsl.hpp's manual header-filter tidy flags 3 issues at 806/956/964 (2× local-const 'N' case + 1 subscript) — VERIFIED PRE-EXISTING at HEAD c35548a (stash-reverted the guard → they persist), NOT from the 1-line guard, gate-excluded (the [[reference_manual_header_filter_tidy_stricter_than_gate]] scar); NOT fixed (out of scope for a relu slice + hand-formatted shared header — flagged for a future kir-tidy cleanup). NEXT = **26d-2c** (the PROVING gate — the reject flips to a run): a non-32-width `ml.mlp` (hidden width e.g. 64) runs DEVICE-RESIDENT Vk+DX12 == the float oracle (24b device-vs-oracle standard — NO "raw" for a previously-REJECTED program) + REMOVE `expand_ml.cpp:113-122`'s `M·hidden==32` pre-check + a numel>cap NEGATIVE (`LocalSizeExceedsLimit`) so the reject's teeth aren't merely deleted + (advisor's blocker-1 hardening) make an unbound sentinel reaching `emit_compute_kernel_{glsl,hlsl}` a TYPED error. advisor after 26d-2c. ⛔ 26z checklist unchanged (carries the 3 items above). | build §73/§74; proof §120 |

**26d-2c ✅ CLOSED 2026-09-04 (the PROVING gate on Vulkan — the 24z BakedKernelShapeUnsupported MLP reject FLIPS to a device-resident run; pre-check RETIRED with replacement teeth)**: shape-specialization PROVEN — a non-32-width `ml.mlp` RUNS. **(1)** REMOVED `expand_ml.cpp:113-122`'s relu `M·hidden==32` pre-check (kept rank-2); ANY width ≤ cap runs via cook-time specialization (the attention transpose/softmax pre-check at 166-171 STAYS until 26d-3). **(2)** SUPERSEDED-IN-PLACE: `test_expand_ml.cpp`'s 24z MLP-non-32 sub-block (was `CHECK BakedKernelShapeUnsupported + expanded==0`) → now `CHECK None + expanded==1`, title→"24z/26d"; the attention Sk=4 reject sub-block UNCHANGED. **(3)** POSITIVE DEVICE GATE (test_ceir_pipeline_vulkan.cpp #4750, `[ceir][ml][gpu]`): a 2-layer float ml.mlp for TWO widths — hidden=16 (h1=4·16=64, non-{pow2-mult-of-32}) AND hidden=24 (h1=96, non-pow2) — EXPANDS→PLANS→runs DEVICE-RESIDENT on real Vulkan (RTX 4070 Ti), relu.ckir local_size cook-bound to the intermediate numel, == the CPU float MLP oracle (1e-4 rel-tol — the device gemm is f32 with a device-specific accumulation ORDER vs the oracle's sequential f32, a real small numerical difference NOT slop, and NOT introduced by the shape-specialization; ⛔ NO "raw" for a previously-REJECTED program — 24b device-vs-oracle standard, stated in-gate). **(4)** REPLACEMENT TEETH (delete=can't-fail scar): a device-free PURE-POLICY unit (test_tensor_pipeline.cpp #818, `[ceir][tensor-pipeline]`) gating `bind_authored_local_size` (sentinel+in-range→None+set; +0→LocalSizeUnbound never-silently-1; +over-cap→LocalSizeExceedsLimit; ==cap→binds; non-sentinel→untouched) PLUS a DEVICE oversize negative in #4750 (h1=4·300=1200 > 1024 cap ⇒ resolver unresolved ⇒ execute UnresolvedKernel ⇒ run fails) — ⛔ ADVISOR-STRENGTHENED (can't-fail trap): a DISCRIMINATING in-gate check mirrors the resolver's `bind_authored_local_size` on the relu VizDispatch stage's write numel and asserts `LocalSizeExceedsLimit` specifically for h1=1200 (and `None`+bound-to-numel for the in-range widths), so the negative pins THAT reject, not any failure; ⛔ VERIFIED (advisor item 2): `run_quant_module_n` returns `false` on `ee!=None` BEFORE `submit_and_wait` (line 340) — no half-recorded submit leaks into the next test on the reject path. ⛔ GREEN win-debug: #818 device-free + #4750 real Vulkan + FULL regression — `expand` 17/17 (incl. the superseded 24z/26d test), `24b` 10/10, `relu` 20/20 (Vk+DX12) + all 4 changed files tidy-clean. NEXT = **26d-2d** (the DX12 MIRROR of the non-32 ml.mlp device gate — the [[feedback_migrated_executor_gate_runs_both_gpu_backends]] both-backends mandate; mechanism + reject teeth already ride the shared lib, so it's the device-gate mirror + advisor after 26d-2d closes 26d-2). Then 26d-3 (softmax spec-consts + transpose-via-synth), 26d-4 (relu_vjp/grad). ⛔ 26z checklist unchanged (carries the 3 items above). | build §73/§74; proof §120 |

**26d-2d ✅ CLOSED 2026-09-04 (the DX12 MIRROR — closes 26d-2; the both-backends mandate satisfied) → ✅✅ 26d-2 (relu shape-specialization) COMPLETE**: the non-32-width `ml.mlp` runs DEVICE-RESIDENT on D3D12 too (test_ceir_pipeline_dx12.cpp #4833, `[ceir][ml][gpu]`, 0.52s real RTX 4070 Ti) — a straight mirror of the Vulkan #4750 gate with DX12 dev/up/rb materialization (the 26c-2c pattern): TWO widths (h1=4·16=64, h1=4·24=96) EXPAND→PLAN→run == the CPU float MLP oracle (relu.ckir local_size cook-bound via the SHARED-lib bind_authored_local_size — the DX12 resolver was wired at 26d-2b, so ZERO new mechanism) + the same DISCRIMINATING oversize negative (h1=1200 > 1024 ⇒ `LocalSizeExceedsLimit` pinned ⇒ execute UnresolvedKernel ⇒ `CHECK_FALSE`). GREEN win-debug real D3D12 #4833 + tidy-clean. ⛔ GREEN FIRST TRY (the mechanism + reject teeth rode the shared crd-ceir-gpu lib from 26d-2b/2c — the [[feedback_migrated_executor_gate_runs_both_gpu_backends]] mandate discharged by a device-gate mirror, no new code beyond the test). ✅✅ **26d-2 (relu cook-time shape-specialization) COMPLETE** = 26d-2a (orientation) + 26d-2b (mechanism: sentinel + bind_authored_local_size + both resolvers + emit-guard + 32-width regression) + 26d-2c (Vk proving gate + pre-check retirement + pure-policy teeth) + 26d-2d (DX12 mirror). The 24z `BakedKernelShapeUnsupported` MLP reject is RETIRED and PROVEN as a run on BOTH backends; the [[feedback_locked_checklist_item_needs_a_gate_or_it_silently_doesnt_land]] scar (baked relu local_size=32) DISCHARGED for the MLP path (relu_vjp/grad = 26d-4). NEXT = **26d-3** (the ATTENTION path — advisor steer: do the TRANSPOSE CENSUS FIRST, it may collapse 26d-3 to one knob). **(a) TRANSPOSE = likely ZERO new mechanism** — `synth_transpose` is ALREADY shape-generic and ALREADY the `tensor.transpose`→`StageKind::Transpose` lowering (25b-4a proved it device-resident); attention's `transpose.ckir` dispatch exists ONLY because 24b predated 25's synth promotion. So the fix is a ~ONE-LINE `expand_ml.cpp` change (emit a `tensor.transpose` op perm=[1,0] instead of `compute.dispatch @transpose`) + DELETE the `transpose.ckir` path from BOTH resolvers' kernel-symbol maps — NOT a spec-const on transpose.ckir (don't build knob (ii) for a kernel that shouldn't exist). **(b) SOFTMAX = the real 26d-3 work** — `softmax.ckir` bakes Sq=2,Sk=3 into the BODY (row-length index math, not just local_size): CHECK whether it uses a `Const` node for Sk (⇒ knob (ii): mark it `spec_constant(id,default,U32)` in the asset + `set_spec_const(id,Sk)` at the resolver, the relu-mirror) and whether local_size is ALSO baked (Sq·Sk? ⇒ the sentinel too — two binds one kernel). **(c) GATE**: an `ml.attention` dims≠(2,3,4) — e.g. Sq=3,Sk=5,D=4 — runs device-resident Vk+DX12 == the CPU SDPA oracle (24b-4 has it); REMOVE the expand_ml.cpp:166-171 attention pre-check in the SAME edit; the `test_expand_ml.cpp` attention Sk=4 sub-block becomes the SECOND superseded-in-place conversion. Then 26d-4 (relu_vjp/grad — relu_vjp.ckir sentinel + grad.cpp:406 path). ⛔ DON'T write "26d specialize COMPLETE" until 26d-3 (attention) AND 26d-4 (relu_vjp) land — the baked-shape reject lives in THREE places, 26d-2 retired ONE (the MLP relu). ⛔ HYGIENE (advisor, non-blocking): #4750/#4833 use a dedicated device-lifetime allocator (Vk `root`/DX12 `devroot`) distinct from the per-width inner root — JUSTIFIED by the multi-width structure (the 24b-4 single-module gates use one root); left as-is. ⛔ 26z checklist unchanged (carries the 3 items above). | build §73/§74; proof §120 |

**26d-3a ✅ CLOSED 2026-09-04 (the ATTENTION transpose census + transpose→synth REFACTOR — ZERO new mechanism, the advisor's collapse-to-one-knob)**: the census confirmed + executed the advisor's 26d-3 blocker-2. **CENSUS**: `plan_tensor_pipeline` ALREADY plans `tensor.transpose`→`StageKind::Transpose`→`synth_transpose`/`emit_permute` (tensor_pipeline.cpp:293, proven device-resident at 25b-4a); attention's baked `transpose.ckir` dispatch existed ONLY because 24b predated 25's synth promotion. **REFACTOR**: `expand_ml.cpp` `expand_attention` now emits Kᵀ via `mk_transpose2d` (= `tensor::build_transpose(ctx, k, perm="1,0", kt_t)`, mirroring grad.cpp) instead of `mk_dispatch(@transpose)` — so Kᵀ carries NO baked (Sk,D) constraint (the transpose leg of the 24z attention reject retired); added the `<crd/ceir/gen/tensor_ops.hpp>` include + the helper. REMOVED the dead `@transpose` kernel-symbol entry from BOTH resolvers (test_ceir_pipeline_{vulkan,dx12}.cpp — attention no longer emits it; verified the ONLY `@transpose` emitter was expand_attention). ⛔ PRE-CHECK KEPT AS-IS (expand_ml.cpp:168-171 still Sq=2∧Sk=3∧D=4) — softmax.ckir STILL bakes Sq/Sk (26d-3b) so the reject must stand; the transpose refactor is INTERNAL (proven by regression, not a new capability yet). GREEN win-debug: crd-ceir-gpu lib + vulkan + dx12 + device-free rebuilt clean; `attention` 13/13 (the 24b-4 attention gates Vk #4778 + DX12 #4831 REGRESS clean — the SDPA oracle match proves synth_transpose produces the correct Kᵀ at (2,3,4), else it would mismatch) + `expand` 17/17 (the 24z/26d Sk=4 attention reject STILL holds — softmax pre-check intact). ⛔ ADDED A STRUCTURAL WITNESS (advisor — the NUMERIC device gate proves numbers, not plan SHAPE): the 24b-3 device-free attention expand test (#843) now walks the expanded block + CHECKs EXACTLY 1 `tensor.transpose` + EXACTLY 1 `compute.dispatch` (softmax only) — catches a silent regression to the baked @transpose dispatch (which would be 0 transpose ops + 2 dispatches). All 4 changed files tidy-clean. ⛔ transpose.ckir asset now RESOLVER-ORPHANED (only reading-gate references may remain) — flag for retirement at 26d-3c (verify/update any reading gate, then delete the asset — the DELETION-IS-done discipline). NEXT = **26d-3b-open = a SOFTMAX CENSUS + advisor, NOT an edit** (advisor: the relu mechanism may NOT transfer — softmax is ROW-WISE with a reduction over Sk, not elementwise). Read softmax.ckir + answer THREE questions before touching the asset: **(1)** how is Sk baked? — a body `For i in [0,Sk)` with Sk a `Const` node ⇒ knob (ii) `spec_constant(id,3,U32)`+`set_spec_const(id,Sk)` WORKS; Sk == the `local_size` (one lane/column, subgroup/shared reduction) ⇒ knob (i) and the REDUCTION STRUCTURE is fixed, not a constant; a hardcoded unrolled 3-term sum ⇒ NEITHER knob, re-author the body as a loop first; **(2)** what does `local_size` mean — `Sq·Sk` (one lane/element, the sentinel-from-write-numel is CORRECT) or `Sq` (one lane/row loops Sk, ⇒ the bind must be `Sq` NOT numel, `bind_authored_local_size`'s write-numel contract is WRONG here — may need a 2nd policy / per-kernel bind descriptor); **(3)** is there a `Shared` array sized by Sk? ⇒ shared-decl length is a THIRD baked dim that `set_spec_const` does NOT resize (check whether the emitter accepts a spec-const shared-decl length or needs cook-time node-`shape` patching). REPORT the 3 answers to the advisor at 26d-3b-open. Then 26d-3c = REMOVE the 168-171 pre-check + a GENERIC attention gate (Sq=3,Sk=5,D=4) Vk+DX12 == SDPA oracle + supersede the Sk=4 reject test in place + retire transpose.ckir (grep tests/kir for its reading gate, delete both). ⛔ 26z checklist unchanged (carries the 3 items above). | build §73/§74; proof §120 |

**26d-3b-census ✅ 2026-09-04 (the SOFTMAX census — the advisor's 3 questions answered; softmax does NOT transfer from relu, confirmed)**: read softmax.ckir. **(1) Sk is a HARDCODED UNROLL** (header: "Sk=3 UNROLLED, no For/accumulator/div-mod") — three explicit `scores[base+{0,1,2}]` loads, 2 `Max`, 3 `Exp`, 2 `Add`, 3 `Div`+`BufferStore` straight-line ⇒ the advisor's Q1 "NEITHER knob — re-author the body as a loop over a runtime Sk FIRST" case (spec-const knob (ii) can't apply to an unroll). **(2) local_size = Sq (one lane per row)** — `local_size=[2,1,1]`, `r=LocalInvocationIndex in [0,Sq)`, each lane loops Sk (unrolled) ⇒ NOT Sq·Sk, so `bind_authored_local_size`'s write-numel contract (probs numel=Sq·Sk=6) is WRONG here — the bind must be Sq = the OUTPUT ROW COUNT (dim 0 of scores/probs), a DIFFERENT policy than relu's total-numel. **(3) NO Shared array** — each lane computes its row from global loads, no `SharedDecl` ⇒ no third baked dim (one complication avoided). ⛔ CONCLUSION: softmax generalization needs (a) the BODY re-authored unroll→`For c in [0,Sk)` (a UNIFORM For — all rows share Sk, eval_cpu_kernel-legal per [[feedback_ckir_eval_for_bound_is_uniform_use_max_plus_forbreakif]]) with a two/three-pass or online structure, (b) Sk as a runtime bound (spec-const `spec_constant`+`set_spec_const`, OR a push/uniform), (c) local_size bound to Sq=dim0 (a NEW bind — not the relu write-numel policy). ⛔⛔ ADVISOR DECISION (26d-3b-open): **RE-AUTHOR softmax.ckir as a LOOP kernel — it STAYS an authored asset (NOT a synth)** — everything-authorable applies as it did to relu (softmax is 24b's authored kernel, not a dialect op); the body must be rewritten either way (the unroll is shape-baked), so rewrite it to its general form. The relu mechanism transfers HALF: **knob (ii) for Sk** (a `KOp::Const` U32 with `kSpecConstFlag` in `axes` + `constant_id` in `iidx`, default 3; resolver `g.set_spec_const(id,(f64)Sk)` after ckir_read — the SAME seam) + **local_size bound to Sq=dim0(write), NOT numel** (don't change `bind_authored_local_size` — its contract is "bind to the extent the resolver supplies"; relu supplies total numel, softmax supplies row count; the per-kernel extent lives in the resolver's kernel-symbol switch — the 20b per-kernel-binding-table precedent — 2 lines/resolver; generalize the tensor_pipeline.hpp comment "write_numel"→"extent", no code change). BODY = three UNIFORM `For c in [0,Sk)` passes with loop-carried scalars: (a) `m=max(m,s·scores[r·Sk+c])` from -FLT_MAX; (b) `denom+=exp(e_c−m)`; (c) `probs[r·Sk+c]=exp(e_c−m)/denom` — ⛔ RECOMPUTE exp in pass 3, do NOT store-to-probs-in-pass-2-and-reload (the inline-load RAW scar [[feedback_ckir_inline_buffer_load_read_after_write]]). One-lane-per-row kept, NO guard/If (the if-block temp-scope scar). ⛔ HONEST: the loop `Σ` order differs from the unroll `((x0+x1)+x2)` ⇒ the 24b-4 (2,3,4) regression re-greens at ORACLE TOL, not bit-identical to the old asset. ⛔ SUB-SLICES (3-4 ticks, advisor: don't collapse): **26d-3b-1** (reads+advisor, NO edit — the 3 ⚠ BLOCKERS: (i) how ckir `For` carries a scalar across iterations [read spmv_csr.ckir's carried dot-sum + `KStmtKind::For`; carried temp/phi ⇒ authorable, shared/scratch-slot carry ⇒ pattern changes — THE first thing to answer]; (ii) the For emitter accepts a spec-const node as its BOUND in GLSL+HLSL [the ckir_glsl.hpp:474 `_spec<id>` path] AND eval_cpu_kernel reads the patched `cval`; (iii) the TOML spec-const encoding [`axes=2147483648`? grep the 18p asset] so ckir_write emits + ckir_read round-trips it) → LOCK the builder shape; **26d-3b-2** (builder→`ckir_write`→eval-verify vs CPU softmax at (2,3)+(3,5) within 1e-6·(1+|ref|)→round-trip byte-exact→commit→update test_ckir_viz 24b-2 eval gate [set local_size=Sq + set_spec_const(id,3) + add the (3,5) case]→reading gate→DELETE builder [the 23e-a mold, softmax.ckir's own header records DELETE — NOT relu_vjp's keep-exception]); **26d-3b-3** (both resolvers: `set_spec_const` Sk + `local_size`←dim0 → 24b-4 attention regression Vk+DX12 at oracle tol); **26d-3c** (remove 168-171 pre-check + GENERIC attention gate (Sq=3,Sk=5,D=4) Vk+DX12 == SDPA oracle + supersede the Sk=4 test in place + retire transpose.ckir + its kir reading gate). NEXT = **26d-3b-1** (the 3 reads + advisor; no edit). ⛔ 26z checklist unchanged. | build §73/§74; proof §120 |

**26d-3b-1 ✅ 2026-09-04 (the 3 blocking reads — no edit; ⛔ SURFACED A DESIGN CONFLICT with the advisor's step-4)**: **(i) KERNEL-TIER For carries via BUFFER RMW, NOT a register/phi** — spmv_csr.ckir accumulates in the OUTPUT buffer (`y[gid]=0` init, then per-iteration `cur=y[gid]` → `y[gid]=cur+val·x[col]`); the DAG re-evaluates each iteration, only buffer/shared writes persist. ⛔ `KOp::LoopAcc`/`LoopIndex`/`KOp::For` (a REGISTER-carried functional reduce, ckir.hpp:154/1243) is GRAPH-TIER ONLY — NO match in ckir_kernel_eval.hpp, so it does NOT eval on the scalar kernel oracle softmax uses. ⛔⛔ THIS CONTRADICTS the 26d-3b advisor step-4 ("loop-carried scalars m/denom") — kernel-tier carry is buffer-only, so softmax's per-row max + sum reductions need a SCRATCH BUFFER (spmv's y[gid]-accumulator pattern × 3 passes; probs can't cleanly double as scratch — it's the output). **(ii) spec-const-as-For-BOUND WORKS both backends** — GLSL (ckir_glsl.hpp:1171) + HLSL (ckir_hlsl.hpp:632) emit the For bound via `pv(st.value)` (the value-expr path that routes a spec-const Const → `_spec<id>`); eval reads the patched `cval` as a plain Const (set_spec_const patches it). **(iii) TOML spec-const = `axes=2147483648` (0x80000000=kSpecConstFlag) + `iidx=<constant_id>` + `cval=<default>`** (moment_blur.ckir has 3; ckir_write emits + ckir_read round-trips). ⛔ RECONCILE TAKEN TO ADVISOR (source≠assumption): the buffer-carry finding forces a scratch buffer — the softmax kernel ABI changes (a new binding), which ripples to expand_ml (mk_decl the scratch) + plan/resolver (allocate it). Advisor re-locks the builder shape (scratch layout + the 3-pass structure) at this reconcile. ⛔⛔ RESOLVED (advisor's decision tree + 2 greps — no scratch buffer, no ABI change): **(1)** NO register carry — the `KStmtKind` enum (ckir.hpp:609) has BufferStore/SharedStore/For/If/ForBreakIf/atomics/`Materialize` (freezes a snapshot, NOT a mutable cross-iteration register); no LocalVar/Assign-to-register stmt; **(2)** SHARED IS in the kernel-tier eval — ckir_kernel_eval.hpp handles SharedDecl/SharedStore/SharedLoad (shared_pool per workgroup, one f64/slot). ⇒ **USE `groupshared` for m/denom** (register-carry absent, Shared present — the advisor's preferred path over a scratch buffer): NO new binding, NO expand_ml/plan/resolver change, each lane RMWs only its own `acc[r]` across the For (NO barrier — no cross-lane read). ⛔ LOCKED SOFTMAX BUILDER SHAPE (26d-3b-2): one-lane-per-row (`r=LocalInvocationIndex`, exact-fit `local_size=Sq`, no guard/If); TWO `SharedDecl` (m_sh, d_sh) sized to a FIXED CAP (avoid a 3rd cook-bind — size ≥ max Sq, r<Sq indexes safely; 4KB shared is nothing) — the shared length is NOT cook-bound this way; Sk = a spec-const `Const` U32 (`axes=2147483648`, `iidx=<id>`, default 3), resolver `set_spec_const(id,Sk)`; THREE uniform `For c in [0,Sk)` (Sk the spec-const bound, NO ForBreakIf — uniform not divergent, the 23e distinction): (a) init m_sh[r]=-FLT_MAX (`cval=0xC7EFFFFFE0000000`, f64→f32 exact) + d_sh[r]=0, then `m_sh[r]=max(m_sh[r], s·scores[r·Sk+c])`; (b) `d_sh[r]+=exp(s·scores[r·Sk+c]−m_sh[r])`; (c) `probs[r·Sk+c]=exp(s·scores[r·Sk+c]−m_sh[r])/d_sh[r]` (RECOMPUTE exp in pass (c), no store-and-reload — the inline-load RAW scar). local_size←Sq=dim0(write) at the resolver (per-kernel extent, the 20b binding-table precedent — NOT bind_authored_local_size's numel). ⛔ HONEST: the loop `Σ` order differs from the unroll ⇒ 24b-4 (2,3,4) re-greens at ORACLE TOL, not bit-identical. NEXT = **26d-3b-2** (build softmax.ckir via ckir_write → eval-verify (2,3)+(3,5) within 1e-6·(1+|ref|) → round-trip → commit → update test_ckir_viz 24b-2 eval gate → reading gate → DELETE builder). | build §73/§74; proof §120 |

**26d-3b-2 ✅ 2026-09-04 (the loop-based softmax BUILDER + eval-verify — the hard part; the generalized kernel is PROVEN dimension-general device-free)**: wrote `build_softmax(KGraph&)` in test_ckir_asset.cpp (the anon-namespace bootstrap mold, beside build_relu_vjp) EXACTLY per the 26d-3b-1 locked shape — 3 buffers (scores@0/scale@1/probs@2) + 2 `shared_decl` (m_sh/d_sh, fixed cap=1024) + `spec_constant(0, default 3, U32)` for Sk + `builtin(LocalInvocationIndex)` for the row r + base=r·Sk; body = init (m_sh[r]=−FLT_MAX via cval −3.4028234663852886e38, d_sh[r]=0) then THREE uniform `stmt_for_begin(sk)`/`kernel_loop_var`/`stmt_for_end` passes ((1) m=max via shared RMW, (2) d+=exp(·−m) via shared RMW, (3) probs=exp(·−m)/d — exp RECOMPUTED, no store-reload). ⛔ EVAL-VERIFY GREEN (#4096, device-free): `eval_cpu_kernel` (spec-const Sk set + local_size=Sq) == a CPU scaled row-wise softmax at BOTH (Sq=2,Sk=3) [the 24b proof dims] AND (Sq=3,Sk=5) [a generic width — previously BakedKernelShapeUnsupported] within 1e-6·(1+|ref|) — the groupshared carry + spec-const For-bound + 3-pass structure ALL work in the scalar kernel eval. Also wrote the `[.emit-softmax]` HIDDEN GENERATOR (mirrors [.emit-relu-vjp]; DELETE-after-commit per the 23e-a mold). GREEN win-debug crd-kir-tests + tidy-clean (renamed locals cap/zero_u/zero_f per the [[feedback_local_constexpr_is_localconstant_lowercase_under_gate]] gate). NEXT = **26d-3b-2b** (RUN [.emit-softmax] → overwrite softmax.ckir with the loop version + rewrite its header for the generalized form → round-trip byte-exact → UPDATE the test_ckir_viz 24b-2 reading gate [set local_size=Sq + set_spec_const(0,Sk) + add the (3,5) case, evaling the COMMITTED asset] → DELETE build_softmax + [.emit-softmax] + the eval-verify [the bootstrap scaffolding]) → then 26d-3b-3 (both resolvers: set_spec_const Sk + local_size←Sq=dim0 → 24b-4 attention regression Vk+DX12 at oracle tol) → 26d-3c (remove pre-check + generic gate + supersede Sk=4 + retire transpose.ckir). | build §73/§74; proof §120 |

**26d-3b-2b/3 ✅ 2026-09-04 (COMMIT the loop softmax + RESOLVERS + a real DEVICE find; the generalized softmax runs on-device) → the softmax kernel generalization LANDS**: ran `[.emit-softmax]` → softmax.ckir is now the LOOP version (Materialize + 2 SharedDecl + spec-const Sk + 3 For passes) + re-added the prose header (ckir_write emits a generic one — the header is re-added post-regen, noted in the asset + generator). UPDATED the test_ckir_viz 24b-2 READING GATE: evals the COMMITTED softmax.ckir at BOTH (Sq=2,Sk=3) AND (Sq=3,Sk=5) (set local_size=Sq + set_spec_const(0,Sk)) == CPU scaled softmax. BOTH RESOLVERS (vulkan/dx12) softmax SPECIAL-CASED (keyed by kernel symbol — the 20b binding-table): `local_size ← dim0(probs)=Sq` + `g.set_spec_const(0, dim1(probs)=Sk)` before emit (the relu sentinel path is the `else`). ⛔⛔ THE DEVICE FIND (the device leg's whole reason — the 24b-2 emit-smoke + eval could NOT catch it): the first device run FAILED (`run_quant_module`→false) — the emitted GLSL declared `base = r·Sk` (t7) INSIDE pass-1's For loop, but passes 2/3 referenced it OUT OF SCOPE ⇒ glslang REJECTED it (compile fail ⇒ null pipeline ⇒ UnresolvedKernel). ROOT: the emitter materializes a multi-use node at its FIRST use = inside loop 1. FIX = `g.stmt_materialize(base)` at the body scope (the temp-scope materialize scar [[feedback_ckir_if_block_shared_temp_scope_materialize]]) → base declared at main() scope, shared by all 3 loops. ⛔ ALSO: the spec-const scalar-eval DEFAULT (3) is NOT applied on device ⇒ the resolver MUST `set_spec_const` explicitly (done). GREEN win-debug: eval-verify (2,3)+(3,5) #4096 + 24b-2 reading gate (27 assns) + kir `asset` 78/78 (softmax.ckir round-trip byte-clean + all reading gates) + 24b-4 attention+mlp Vk+DX12 (real RTX) + `attention` 13/13 + 4 files tidy-clean. ⛔ BUILDER KEPT (build_softmax + [.emit-softmax] + eval-verify) as the regen source — NOT an override of the mold: the "DELETE builder" mold's PRECONDITION is a COMMITTED asset, and softmax.ckir is still uncommitted (the user commits in batches), so the [[feedback_uncommitted_delete_loses_the_source_keep_regen_in_tree]] scar and the mold DON'T conflict — the scar says WHEN (delete at the user's commit of softmax.ckir), the mold says WHAT. ⛔ ADVISOR-FOLLOW (this tick, the device-find's lesson made durable): **(a)** the 24b-2 reading-gate comment + the file header DE-OVERCLAIMED — `emit_compute_kernel_glsl/_hlsl` return true once a STRING is produced, they do NOT compile; the emit-smoke could NOT catch the out-of-scope temp, only the device compile did (⛔ ADR-0103 moved shaderc into the Vulkan backend, so kir CANNOT link a device-free compiler — option (a) "compile in the reading gate" is architecturally blocked; the 24b-4 device gates are the compile authority). **(b)** FOR-SCOPE CENSUS of assets/ckir/ (the multi-loop shared-temp hazard): softmax=3 For sharing `base`→FIXED (Materialize); skin_compute=2 sequential For (SHIPPED + passing both backends → its device gate already compiled it through glslang+dxc = proven safe, not "lucky"); palette_snapshot / spmv_csr / transpose = 1 For each (safe by construction — first use == the only loop). NO other kernel carries the hazard. **(c)** SCAR written [[feedback_ckir_emitter_materializes_multiuse_node_at_first_loop_use_out_of_scope_for_later_loops]] (the For sibling of the if-block temp-scope scar). NEXT = **26d-3c** (the ATTENTION generalization capstone: REMOVE expand_ml.cpp:166-171's (Sq=2,Sk=3,D=4) pre-check + a GENERIC attention DEVICE gate Sq=3,Sk=5,D=4 Vk+DX12 == CPU SDPA oracle [the softmax runs any Sk now; transpose is synth; the gemms synth per-shape] + SUPERSEDE-in-place the test_expand_ml Sk=4 attention reject + RETIRE transpose.ckir [grep tests/kir for its reading gate, delete both]). Then 26d-4 (relu_vjp/grad). ⛔ 26z checklist unchanged. | build §73/§74; proof §120 |

**26d-3c ✅ CLOSED 2026-09-04 (the ATTENTION GENERALIZATION CAPSTONE — a generic-dims ml.attention runs device-resident on BOTH backends; the baked pre-check RETIRED with teeth transferred; transpose.ckir retired) → ✅✅ 26d-3 (attention path) COMPLETE**: the 24z attention `BakedKernelShapeUnsupported` reject FLIPS to a device-resident run at generic dims. ⛔ ADVISOR-ORDERED (teeth-transfer, no window): **(1)** wrote the GENERIC attention DEVICE gate (Sq=3,Sk=5,D=4,Dv=2) in BOTH resolvers' test files FIRST + ran it → it FAILED `REQUIRE(er.error==None)` with `4 == 0` (4=BakedKernelShapeUnsupported) — PROVING the pre-check's teeth were live (not merely deleted; the [[feedback_deleting_reference_in_ab_parity_test_degrades_to_can_t_fail]] discipline). **(2)** REMOVED `expand_ml.cpp`'s attention baked-shape pre-check (was `dim0(q)≠2 ∨ dim0(k)≠3 ∨ dim1(q)≠4 → reject`) — 26d-3a made Kᵀ a shape-generic `tensor.transpose` (synth, the cooker) + 26d-3b made softmax a spec-const loop kernel, so the composite is dimension-general (any Sq/Sk/Dv; the 2 gemms synth per-shape). **(3)** RE-RAN → the generic gate GREEN on REAL RTX 4070 Ti: **Vulkan** (test_ceir_pipeline_vulkan.cpp #26d-3c, 12 assns) + **DX12** (test_ceir_pipeline_dx12.cpp #26d-3c, 12 assns) == the CPU SDPA oracle (1e-4 rel-tol; exp is float — a DERIVED tol; ⛔ NO "raw" for a previously-REJECTED program, the 24b device-vs-oracle standard, stated in-gate). ⛔ D HELD at 4 (advisor): scale=1/√D is caller-computed = 0.5 for D=4; a D-generic gate would recompute 1/√D (trivial, name-forward) — named in the gate. **(4)** SUPERSEDED-IN-PLACE (2nd conversion, mirror of 26d-2c's MLP): `test_expand_ml.cpp`'s Sk=4 attention reject sub-block (was `CHECK BakedKernelShapeUnsupported + expanded==0`) → now Sq=3,Sk=5 `CHECK None + expanded==1`; the TEST_CASE title updated ("now EXPANDS a non-32-width MLP AND a generic-dims ATTENTION"). **(5)** RETIRED `transpose.ckir` (the DELETION-IS-done discipline — [[feedback_authored_asset_slice_done_only_when_cpp_deleted_and_renders]]): DELETED the asset + its `ceir 24b-1` reading gate (test_ckir_viz.cpp) + the stale bootstrap comments (test_ckir_kernel.cpp) + fixed the 2 stale 24b-4 gate headers that named it. ⛔ SANCTIONED (not a mandate breach): transpose is a `tensor.transpose` DIALECT op lowered by `synth_transpose` (the cooker), NOT a hand-authored algorithm — the 26d-1 rule "synth_* is the lowering for DIALECT ops with declared semantics" [[feedback_no_cpp_kgraph_builders_author_ckir_directly]]; the authored asset existed only because 24b predated 25's synth promotion. PROVEN: the 24b-4 (2,3,4) attention gate PASSES with transpose.ckir DELETED (Vk 122 assns/6 cases + DX12 71/4) — synth_transpose is the live path, not the asset. **(6)** removed the now-dead `dim_count` helper (only the pre-check used it — clang-diagnostic-unused-function). ⛔ GREEN win-debug: `expand` 17/17 (superseded reject) + kir `[ceir]` 384/9 + `[asset]` 563/30 (transpose.ckir gone, the round-trip ENUMERATES the dir — one fewer case, no hardcoded count) + `[ml][gpu]` Vk 122/6 & DX12 71/4 (24b-4 attention+mlp + 26d-3c generic attention + 26d-2c non-32 mlp, all on real RTX, AFTER the deletion) + all 6 changed files tidy-clean. ✅✅ **26d-3 (attention path) COMPLETE** = 26d-3a (transpose→synth) + 26d-3b (softmax→spec-const loop kernel) + 26d-3c (pre-check retired + generic gate both backends + supersede + transpose.ckir retired). The 24z baked-shape reject is RETIRED for the ATTENTION path (relu MLP was 26d-2). ⛔ ADVISOR-FOLLOW (before wakeup): **(a)** REPO-WIDE grep of `BakedKernelShapeUnsupported` (not just test_expand_ml) — ZERO remaining POSITIVE assertion sites (the only one was test_expand_ml's Sk=4, superseded this slice; every other hit is a "was …"/comment or a `== None` positive) AND ZERO `return MlExpandError::BakedKernelShapeUnsupported` in engine/ ⇒ the enum value is a ZOMBIE (relu retired 26d-2, attention 26d-3c). Fixed the 2 stale engine doc comments NOW (expand_ml.cpp:121 said "still guards ml.attention"; expand_ml.hpp:32 named the deleted transpose.ckir + retired relu/softmax baked dims) → both now say UNREACHED/retire-at-26d-4. RETIRE the enum value itself at 26d-4 (grad.cpp in hand) AFTER auditing every `switch` on MlExpandError (the widen/narrow-enum scar; gcc `-Werror=switch` catches a gap, MSVC won't). **(b)** DX12 Sk=5 GREEN proves `set_spec_const` OVERRIDES the DX12 per-cook baked spec-const DEFAULT with a NON-default runtime value — the 26d-1 "DX12 BAKES the default" concern is DISCHARGED for the runtime-set path. **(c)** context.md:10 de-referenced the deleted transpose.ckir (2 live-state claims fixed); the 25b-4a citation corrected (it's the transpose+broadcast+elementwise CHAIN gate, not "standalone"). ⛔ REMAINING baked-shape reject = ONE place: `grad.cpp:406` (relu_vjp/grad, relu_vjp.ckir still bakes local_size=32) → **26d-4**. ⛔ ADVISOR-NOTED (non-blocking hardening, name-forward): the softmax resolver special-case sets `local_size=Sq` WITHOUT a device-cap check (unlike relu's bind_authored_local_size LocalSizeExceedsLimit) — a huge Sq (>1024 rows) would emit an over-cap local_size that pipeline-creation rejects (UnresolvedKernel, graceful but untyped); the retired pre-check was NEVER a device cap (baked-dims equality), so removing it is correct, but a TYPED softmax-local_size cap is a 26d-4/26e follow-up. NEXT = **26d-4** (relu_vjp/grad: relu_vjp.ckir sentinel [the relu 26d-2b mechanism, mirrored] + grad.cpp:406 path + a relu_vjp non-32 device gate Vk+DX12 == the vjp oracle + supersede any baked reject). ⛔ 26z checklist unchanged (carries the 3 items). | build §73/§74; proof §120 |

**26d-4a ✅ 2026-09-04 (the relu_vjp/grad ORIENTATION + advisor plan-lock — NO code, read-before-write; the 26d-4 sub-split + traps locked)**: read `relu_vjp.ckir` (elementwise, `local_size=[32,1,1]`, gid=LocalInvocationIndex, 3 buffers x@0/dy@1/dx@2-write, body `dx[gid]=x[gid]>0?dy[gid]:0` — an EXACT relu mirror ⇒ the 26d-2b sentinel mechanism transfers directly, the resolver's else-branch already cook-binds any sentinel dispatch) + `grad.cpp:405-417` (a SEPARATE baked guard `GradError::MlpBakedShapeUnsupported` — rejects when an interior relu'd layer's `M·hidden_i != 32`; distinct enum from `MlExpandError::BakedKernelShapeUnsupported`) + the full consumer/switch census. ⛔ TWO ZOMBIE ENUMS after 26d-4b: `MlExpandError::BakedKernelShapeUnsupported` (already zombie post-26d-3c; def expand_ml.hpp:32 + a `_name` switch) + `GradError::MlpBakedShapeUnsupported` (becomes zombie once grad's pre-check goes; def grad.hpp:50 + name-case grad.cpp:160 + return grad.cpp:415 + test_grad.cpp:494-514 reject). ⛔⛔ THE n_out TRAP (advisor, one-line for the next reader): the `.ckir` ENTRY field `n_out` (relu_vjp.ckir line 16 = 0) is a DIFFERENT field from the PlanStage `st.n_out` the resolver keys on (`if (st.n_out>=1) bind_authored_local_size(...)`) — the stage's output count (=1 for the elementwise write), NOT the entry field; the cook-bind fires on the STAGE, so relu_vjp binds correctly once sentinel-ized. ⛔ ADVISOR-LOCKED SUB-SPLIT (4b→4c→4d): **26d-4b** (mechanism + proving gate, relu_vjp only — the 26d-2b mold): (1) relu_vjp.ckir `[32,1,1]→[0,1,1]` + ADD a real prose header (it has only the generic `ckir_write` preamble now); (2) REMOVE grad.cpp:405-417 + grep `dim_count` in grad.cpp (the dead-helper trap — same as expand_ml 26d-3c; check own-copy vs shared); (3) SUPERSEDE test_grad.cpp:494-514 (`MlpBakedShapeUnsupported`→`None` + structural: backward vocab EMITTED not zero); (4) a non-32 `vjp_mlp` backward DEVICE gate Vk+DX12 (h1=4·16=64, the 26d-2c width) == the CPU vjp oracle, MIRRORING the 25c-2 dW1+dW2 gate — ⛔ VERIFY the 25c-2 gate's current width: if h1=32 KEEP it as the regression leg + ADD the non-32 case (don't replace); ⛔ VERIFY the grad `mk_dispatch(@relu_vjp)` binds[] order is `{z, dh, dz_out}` write-LAST to match access "r,r,w" (pin it, don't rely on the elementwise coincidence that wop picks right either way). TEETH-FIRST (26d-3c order): write the gate, run it, watch `MlpBakedShapeUnsupported` at REQUIRE, THEN remove 405-417, re-run green. **26d-4c** (retire BOTH zombie enums — closes the §73 census 3→0): remove the enum values + `_name` cases (grep `case MlExpandError::` in expand_ml.cpp for the 2nd name switch) + ⛔ grep `default:` in every switch on both enums BEFORE deleting (the narrow-enum direction: a `default:` switch silently swallows — the widen/narrow scar [[feedback_widening_a_closed_enum_audit_every_consumer_not_just_total_switches]]; removing a NAMED case from a total no-default switch is compile-clean either way, MSVC included) + ⛔ READ `engine/hesap-autodiff/nn_reverse.hpp` (a THIRD consumer namespace) BEFORE deleting + a FULL device-free WSL linux-gcc sweep after ([[reference_wsl_linux_sweep_and_llvmpipe_exposure]] — the real gate here, not -Werror=switch). **26d-4d** (the TYPED softmax local_size cap — advisor's pinned hardening): sentinel-ize softmax.ckir `[2,1,1]→[0,1,1]` + route the resolver's softmax branch through `bind_authored_local_size(ve.local_size[0], Sq, kMaxAuthoredLocalSize)` (one mechanism, cap free, the 26d-2b emit-guard now protects softmax's unbound-0; groupshared cap=1024==kMaxAuthoredLocalSize makes the OOB-guard EXPLICIT not incidental); regen via `[.emit-softmax]` (builder `e.local_size[0]=2`→emit 0→re-add header); the 24b-2 reading gate sets local_size=Sq explicitly ⇒ unaffected. ⛔ DON'T write "26d specialize COMPLETE" until 26d-4c lands (a zombie enum with a "retire me" doc is NOT a retired reject — the census says 3 sites → 0). NEXT = **26d-4b** (the mechanism + proving gate; advisor after it lands). ⛔ 26z checklist unchanged (carries the 3 items). | build §73/§74; proof §120 |

**26d-4b ✅ 2026-09-04 (the relu_vjp/grad MECHANISM + Vulkan PROVING gate — the grad MlpBakedShapeUnsupported reject FLIPS to a run; a coupled BOTH-BACKENDS emit-site fix)**: shape-specialization PROVEN for the vjp path. **(1)** `relu_vjp.ckir` local_size `[32,1,1]→[0,1,1]` (the SENTINEL) + a real PROSE HEADER (it had only the generic ckir_write preamble). **(2)** REMOVED grad.cpp's `M·hidden_i != 32` baked guard (lines 397-409) + removed the now-dead `dim_count` helper (grad.cpp:52-59 — only the guard used it, the same dead-helper trap as expand_ml 26d-3c). relu.ckir (recompute) + relu_vjp.ckir (backward) both cook-bind local_size via the resolver's `bind_authored_local_size` (the SHARED lib path — ZERO new mechanism). **(3)** SUPERSEDED-IN-PLACE (3rd conversion, mirror of 26d-2c MLP + 26d-3c attention): test_grad.cpp:494-521's `MlpBakedShapeUnsupported`+zero-vocab reject → now `None` + grads non-null + backward vocab EMITTED (gemm/transpose/dispatch > 0); the mlp_baked→mlp_wide rename. **(4)** the non-32 Vulkan PROVING gate (test_ceir_pipeline_vulkan.cpp #26d-4b, `[autodiff][gpu]`): a `vjp_mlp` backward at h1 = M·d1 = 8·8 = **64** (≠32, was rejected) — x[8,4]·W1[4,8]·relu·W2[8,4] — EXPANDS→PLANS→runs DEVICE-RESIDENT on real Vulkan (RTX 4070 Ti), reads back BOTH dW1+dW2 == the DIALECT-INDEPENDENT hesap ref (matmul_vjp+relu_vjp), FD-cross-validated, with the column-signed W1 (cols [0,dhalf)+, [dhalf,d1)−) making half the hidden units DEAD ⇒ PROVEN zero blocks (dW1[:,c≥dhalf]==0, dW2[c≥dhalf,:]==0) a dropped/wrong-half relu_vjp breaks (⛔ NO "raw" for a previously-REJECTED program). TEETH-FIRST (26d-3c order): wrote the gate, ran it → FAILED `gr.error==None` with `'\n'==0` — a NON-None reject at ordinal 10 (= MlpBakedShapeUnsupported by enum declaration order; ⛔ Catch2 stringifies the u8 enum as a char, it does NOT print the name — the identity is inferred from the enum order, the honest-scoreboard caveat), THEN removed the guard, re-ran green. ⛔ THE dhalf CORPUS GENERALIZATION (d1=4→8) was VERIFIED BY THE GATE'S OWN REQUIREs, not assumed: `neg>0` (mask bit — not identity) ∧ `neg<mrows·d1` (some units alive) ∧ `minabs>0.05` (z1 off the relu kink so FD is valid) ∧ the dead-block `dW1[:,c≥dhalf]==0`/`dW2[c≥dhalf,:]==0` CHECKs all passed at d1=8. ⛔⛆ THE COUPLED BOTH-BACKENDS EMIT-SITE FIX (the sentinel's blast radius — the 26d-2a "sentinel breaks ONLY emit sites" inventory, now for relu_vjp): sentinel-izing relu_vjp.ckir tripped the 26d-2b emit-guard (`entry.local_size[0]==0 → return false`) at THREE non-resolver EMIT sites that load+emit WITHOUT cook-binding — (a) test_ceir_pipeline_vulkan.cpp's 25c-1b `load_emit_ckir` (readonly-codegen check) + (b) test_ceir_pipeline_dx12.cpp's 25c-1b `load_emit_ckir_hlsl` + (c) test_ckir_asset.cpp's 25c-0 reading gate (asserted `local_size==32`). FIX: added an optional `local_size_x` (default 0 = as-authored) to BOTH `load_emit_ckir*` helpers that patches `ke.local_size[0]` before emit (non-sentinel callers unaffected), passed `nel`(=32) at the relu_vjp call sites; the 25c-0 gate now asserts the SENTINEL (0) + its eval already passes ls=32 explicitly (eval takes local_size as a param — sentinel-invisible, the blocker-4 fact). ⛔ ADVISOR #1 (integrity re-check): grepped `relu.ckir` in test_ckir_asset.cpp — NO relu twin of the 25c-0 gate exists (only a prose comment, fixed), so the 26d-2b "[asset] green" claims HOLD (a red relu `local_size==32` gate would have failed 26d-2b's green runs). ⛔ SCAR written [[feedback_sentinel_izing_a_kernel_asset_breaks_every_emit_site_grep_the_asset_path_not_the_symbol]] — sentinel-izing an asset = grep the ASSET PATH across tests/ (every load+emit site + every local_size==N gate, BOTH backends), not the kernel symbol; under-inventoried TWICE (relu 26d-2b, relu_vjp 26d-4b), caught by red tests both times. ⛔ ADVISOR-VERIFIED: `emit_relu_vjp_dispatch` binds `{x, gy, gx}` write-LAST (grad.cpp:140) matching access "r,r,w" ⇒ wop picks gx correctly; the 25c-2 gate is at M·d1=8·4=32 (the baked width) so it STAYS as the regression leg + I ADDED the non-32 case (didn't replace). ⛔ GREEN win-debug on REAL RTX: Vulkan `ceir 25c*`+`26d-4b` 422 assns/3 cases (25c-1b + 25c-2 32-regression + 26d-4b non-32) + DX12 `ceir 25c*` 221/2 (32-regression with the sentinel+emit-fix) + device-free 25c-1 grad supersede (74/2) + kir `[asset]` 563/30 (relu_vjp.ckir round-trip byte-clean, sentinel + prose header) + 6 changed files (grad.cpp + 4 tests + relu_vjp.ckir) tidy-clean. NEXT = **26d-4c** (the DX12 non-32 vjp PROVING gate MIRROR — the [[feedback_migrated_executor_gate_runs_both_gpu_backends]] both-backends mandate; mechanism rides the shared lib so it's the device-gate mirror + fix the DX12 25c-2 stale "baked local_size" comment; advisor after). Then 26d-4d (retire BOTH zombie enums, WSL sweep) + 26d-4e (typed softmax cap). ⛔ SUB-SPLIT REFINED: the advisor's 4b/4c/4d became 4b(mech+Vk)/4c(DX12 mirror)/4d(enums)/4e(softmax cap) — the device-gate is 170 lines × 2, so Vk-proving + DX12-mirror split like 26d-2c/2d. ⛔ DON'T write "26d COMPLETE" until 4d (zombie enums retired). ⛔ 26z checklist unchanged. | build §73/§74; proof §120 |

**26d-4c ✅ 2026-09-04 (the DX12 non-32 vjp PROVING gate MIRROR — the both-backends mandate satisfied for the vjp path)**: the non-32-width `vjp_mlp` backward runs DEVICE-RESIDENT on D3D12 too (test_ceir_pipeline_dx12.cpp #26d-4c, `[autodiff][gpu]`, real RTX 4070 Ti) — a straight mirror of the Vulkan #26d-4b gate with DX12 `Dx12ComputeContext` + `run_quant_module_n` readback: h1 = M·d1 = 8·8 = 64 (≠32), x[8,4]·W1[4,8]·relu·W2[8,4] EXPAND→PLAN→run, reads back BOTH dW1+dW2 == the hesap ref (matmul_vjp+relu_vjp), FD-cross-validated, same dhalf column-signed corpus (dead-block PROVEN zeros). relu.ckir/relu_vjp.ckir cook-bind local_size via the SHARED-lib bind_authored_local_size (the mechanism rode the lib from 26d-4b — ZERO new code). ⛔ GREEN FIRST TRY win-debug on real D3D12: `ceir 26d-4c`+`ceir 25c*` 423 assns/3 cases (25c-1b + 25c-2 32-regression + 26d-4c non-32) + the DX12 file tidy-clean. Also fixed the DX12 25c-2 stale "baked local_size" comment (advisor #5 / the 4c checklist item — now "the 32-width regression leg; cook-bound via the sentinel"). ⛔ the [[feedback_migrated_executor_gate_runs_both_gpu_backends]] mandate DISCHARGED by a device-gate mirror (no new code beyond the test) — the vjp shape-specialization is now proven on BOTH backends. ✅ the non-32 vjp path is device-resident-proven Vk+DX12; the grad `MlpBakedShapeUnsupported` reject is RETIRED (its enum value now a zombie). NEXT = **26d-4d** (retire BOTH zombie enums — ⛔ ADVISOR-CORRECTED plan, the 26d-4a WSL/default/nn_reverse steer was over-cautious): **(1)** VERIFY both enumerators are LAST in their enums before deleting (MlExpandError: last, confirmed; GradError: read grad.hpp:40-51 to confirm MlpBakedShapeUnsupported is last) — a NON-last removal shifts later ordinals ⇒ breaks anything persisting an ordinal (cooked blob / diag code — the POD-hash/cooked-blob scar); last = nothing shifts. **(2)** `MlExpandError::BakedKernelShapeUnsupported` has NO `_name` switch (grep confirmed) ⇒ header edit alone + delete the 26d-3c "retire me" comment; `GradError::MlpBakedShapeUnsupported` ⇒ header value + the `case` at grad.cpp:160, same commit. **(3)** blast-radius BUILD = every target including expand_ml.hpp/grad.hpp = crd-ceir-gpu lib + its 3 test targets (MSVC errors on a stale `case`/reference as hard as gcc — NO WSL sweep needed, the WHOLE-REPO=CI rule; skip the `default:` audit [that's the ADD-direction check] + skip the nn_reverse.hpp "3rd consumer" fear [a false alarm — it matched `relu_vjp` the hesap fn, not the enum; confirm with one targeted grep of both enum names under engine/hesap-autodiff/]). **(4)** final grep both names repo-wide (ex docs/sessions + docs/bench) — every hit must be a "was …" comment. **(5)** UPDATE THE RECIPE `docs/recipes/2026-09-04-ceir-reverse-mode-autodiff.md:21,:121` (a recipe is a LIVING how-to, not a session log — strike in place: "retired 26d-4 — sentinel + cook-bind"); sessions + bench scoreboard stay as-written. Closes the §73 census 3→0). Then 26d-4e (typed softmax cap — ⛔ apply the sentinel-scar: `grep "softmax.ckir" tests/` FIRST [builder regen local_size=2, the 24b-2 reading gate sets sq explicitly=safe, both resolvers' direct-assign→bind_authored_local_size route; no load_emit_ckir* caller names softmax]). ⛔ DON'T write "26d COMPLETE" until 4d. ⛔ 26z checklist unchanged. | build §73/§74; proof §120 |

**26d-4d ✅ 2026-09-04 (retire BOTH zombie enums — CONVENTION-CORRECTED: DEAD-mark + KEEP, NOT remove; closes the §73 census 3 sites → 0)**: ⛔⛆ THE CONVENTION RECONCILE (source=scoreboard, surfaced to the advisor mid-tick): the advisor's 26d-4a/4c "REMOVE both enum values" plan CONFLICTED with a declared engine convention — `GradError` (grad.hpp:30) carries `⛔ append at END` AND KEEPS a dead value (`LossNotScalar`, DEAD-marked at 25z-1 for the identical source=scoreboard reason). A grep proved `⛔ append at END` is a PROJECT-WIDE convention for TYPED-reject/vocabulary enums (tensor_pipeline.hpp PlanReject/StageKind/BufferRole, lower.hpp, effect.hpp, type.hpp, work.hpp, +dozens). ⇒ the gold-standard, engine-convention move is DEAD-MARK + KEEP (never remove — removing would make these the ONLY TYPED-reject enums in the repo with a deleted value). Advisor AGREED + owned the correction ("ignore my 'remove both'"). **(1)** `GradError::MlpBakedShapeUnsupported` (grad.hpp:50) → DEAD-marked in the LossNotScalar mold ("⛔ DEAD 2026-09-04 (26d-4b): NEVER returned … Member KEPT (append-only enum), not live"); the `grad_error_name` case (grad.cpp:152) STAYS (exhaustive switch). **(2)** `MlExpandError::BakedKernelShapeUnsupported` (expand_ml.hpp:35) → DEAD-marked + ADDED the missing `⛔ append at END` head marker (it's a sibling TYPED-reject enum that just never got it — the advisor's sibling-consistency call; grep confirmed it has NO `_name` switch, so header-only). **(3)** RECIPE struck in place (docs/recipes/…autodiff.md:21 table row + :119-122 traps — a recipe is a LIVING how-to: "~~baked local_size=32~~ → COOK-BIND SENTINEL, RETIRED 26d-4"; sessions + bench stay as-written). **(4)** fixed the stale expand_ml.cpp:123 comment ("retired after the switch audit" → "DEAD-marked + KEPT, append-only"). ⛔ NO enumerator REMOVED ⇒ no ordinal shift ⇒ the POD-hash/cooked-blob scar is moot (both were LAST anyway — verified). ⛔ ADVISOR-CORRECTED (the 26d-4a over-caution dropped): NO WSL sweep (blast-radius = crd-ceir-gpu lib + 3 test targets; MSVC errors on a stale case as hard as gcc — WHOLE-REPO=CI); NO `default:` audit (the ADD-direction check); NO nn_reverse.hpp fear (grep of both enum names under engine/hesap-autodiff/ = ZERO — the earlier hit was `relu_vjp` the hesap fn). ⛔ GREEN win-debug: blast-radius build (lib + 3 test targets) clean + device-free `ceir 25c-1*`+`24z*` 96/4 (values KEPT ⇒ behavior unchanged) + expand_ml.cpp tidy-clean + final repo-grep (engine/): the ONLY live code reference is the grad.cpp:152 name-case (correct); all else is DEAD-marked members + accurate comments; ZERO return sites. ✅✅ **§73 CENSUS CLOSED: 3 reject SITES → 0** (relu MLP @26d-2, attention @26d-3c, relu_vjp/grad @26d-4b); the enum VALUES retired-per-convention (DEAD-marked + KEPT, append-only). ⛔ STILL NOT "26d COMPLETE" — 26d-4e (the typed softmax cap) is the last sub-slice. NEXT = **26d-4e** (⛔ apply the sentinel-scar FIRST: `grep "softmax.ckir" tests/` — the builder regen [local_size=2], the 24b-2 reading gate [sets sq explicitly = safe], both resolvers [the direct `local_size=dim0` assign → route through `bind_authored_local_size(ve.local_size[0], Sq, kMaxAuthoredLocalSize)`], and NO `load_emit_ckir*` caller names softmax; then sentinel-ize softmax.ckir `[2,1,1]→[0,1,1]` + regen + re-add header, so the groupshared cap=1024==kMaxAuthoredLocalSize OOB-guard becomes EXPLICIT not incidental + the 26d-2b emit-guard protects softmax's unbound-0). advisor after. ⛔ 26z checklist unchanged. | build §73/§74; proof §120 |

**26d-4e ✅ CLOSED 2026-09-04 (the TYPED softmax local_size cap — the last uncapped resolver assign gets teeth; a relu_vjp builder-divergence bug caught+fixed) → ✅✅✅ 26d (SPECIALIZE) BAND COMPLETE**: the softmax resolver's direct `local_size=Sq` assign (uncapped — a Sq>1024 would emit a device-illegal local_size that pipeline-creation silently rejects) now routes through the sentinel+`bind_authored_local_size` mechanism. ⛔ SENTINEL-SCAR APPLIED FIRST (grep `softmax.ckir` tests/): the emit sites all overwrite local_size before emit (both resolvers set it; the 24b-2 reading gate sets sq explicitly) so sentinel-izing is safe; `ckir_module.hpp`'s "softmax" is a DIFFERENT linked-module fn (unaffected). **(1)** `build_softmax` local_size `2→0` (sentinel) + REGEN softmax.ckir via `[.emit-softmax]` + RE-ADD the prose header (documents the 26d-4e sentinel+cap). **(2)** BOTH resolvers' softmax branch: `ve.local_size[0]=dim0(probs)` → `bind_authored_local_size(ve.local_size[0], Sq=dim0(probs), kMaxAuthoredLocalSize)` + `if (kse!=None) return rs` (⇒ a Sq > 1024 == the groupshared m_sh/d_sh cap is a TYPED LocalSizeExceedsLimit ⇒ UnresolvedKernel, not a silent device reject — the OOB guard on the [1024] shared arrays is now EXPLICIT). set_spec_const(Sk) unchanged. **(3)** `tensor_pipeline.hpp`: generalized the `bind_authored_local_size` doc "write_numel"→"EXTENT" (per-kernel, resolver-supplied: elementwise=numel, softmax=Sq rows) + RENAMED the param `write_numel→extent` (pure/local fn, positional callers unaffected). ⛔⛆ CAUGHT+FIXED a 26d-4b LEFTOVER (a variant of the sentinel scar — builder-divergence): `relu_vjp.ckir` is GENERATED by `build_relu_vjp` via `[.emit-relu-vjp]`, but 26d-4b hand-edited the ASSET to sentinel [0] WITHOUT aligning the builder (still `local_size=32`) — a regen would have silently REVERTED the sentinel; my "Authored DIRECT" header claim was also FALSE. FIX: `build_relu_vjp` local_size `32→0` (matches the committed asset — no regen needed, the graph was unchanged) + corrected the relu_vjp.ckir header ("GENERATED by build_relu_vjp; RE-ADD after regen"). Also confirmed relu.ckir has NO builder (hand-authored — no divergence). ⛔ GREEN win-debug on real RTX: `[ml][gpu]` Vk 122/6 + DX12 71/4 (24b-4 attention Sq=2 + 26d-3c attention Sq=3 + mlp all cook-bind softmax via bind_authored_local_size, unchanged local_size for valid Sq ⇒ no regression) + kir `[asset]` 563/30 (softmax.ckir regen round-trips byte-clean; build_softmax/build_relu_vjp eval-verify; the 25c-0 gate asserts sentinel) + kir `24b` 27/1 (the 24b-2 reading gate at (2,3)+(3,5)) + ceir-gpu `[tensor-pipeline]` 322/24 (the pure-policy bind_authored_local_size cap unit — the `extent` rename clean) + all 4 changed C++ files tidy-clean. ⛔ SOFTMAX OVERSIZE NEGATIVE = NAME-FORWARD (advisor-CONFIRMED, with the precise reason): the pure-policy unit PROVES `bind_authored_local_size` rejects `extent > 1024`; the ONE new line (the softmax branch's `if (kse != None) return rs;`) is a verbatim mirror of the relu else-branch's identical line, which IS device-proven (#4750/#4833 h1=1200 → UnresolvedKernel). Two identical one-line returns, one device-proven + one by inspection — a fair inference for a one-line mirror; a Sq=1025 device gate would allocate 1025·Sk floats + 2×1024-slot groupshared to test a single `return`. So name-forward, NOT merely "leaning". ✅✅✅ **26d (SPECIALIZE) COMPLETE** = 26d-0 (census) + 26d-1 (mechanism) + 26d-2 (relu MLP, 4 legs) + 26d-3 (attention: transpose→synth + softmax→spec-const loop + capstone) + 26d-4 (relu_vjp/grad + zombie enums DEAD-marked + softmax cap). The §73 "specialize" pass is DONE: cook-time kernel-shape specialization at the CEIR→CKIR boundary, all 3 baked-shape rejects RETIRED + proven device-resident on Vk+DX12. NEXT = **26e** (basic-fusion→CKIR — the next 26 sub-slice per the band shape 26a DCE→26b fold→26c CSE→26d specialize→**26e fusion**→26f mem/schedule→26z close). advisor to open 26e. ⛔ 26z checklist unchanged (carries the 3 items: llvmpipe re-run of the 26b-2 reshape-fold + the 26c-2 CSE differentials + the anon resource.import CSE sibling). | build §73/§74; proof §120 |

**26e-0 ✅ 2026-09-04 (the BASIC-FUSION census — advisor-steered, NO build; what fusion is NOT already done + which live consumer benefits)**: censused the fusion landscape (grep the CONSUMER, not the node). ⛔ **ALREADY FUSED (NOT 26e):** **(a)** `StageKind::QuantGemm` (23b-2b, tensor_pipeline.cpp:215-233) = a PRODUCER-FOLD — a single-use `quant.dequantize` (the gemm's weight operand) collapsed into the gemm stage (its output NEVER allocated), REUSING the authored quant_gemm_q8 kernel (not a synthesized fusion). **(b)** `StageKind::Elementwise` (25b-3) = ONE `tensor.elementwise` → one fused CKIR binary kernel (one-op-per-kernel, not a pair-fuse). **(c)** attr-aware SELECTION (23b QuantGemm-scheme detect, 24c coopvec claim, `partition_ml` advertise) = picks a specialized kernel per FULL semantic attrs at the plan/provider layer — a SELECTION of one op's kernel, NOT a fusion of two ops (the 26d-0 census already flagged this as "not a fold"). ⛔ **NOT FUSED — the 26e "basic-fusion(→generated CKIR)" candidate = gemm→relu ELEMENTWISE-ACTIVATION EPILOGUE fusion**: `synth_gemm` (ckir_synth.cpp:137-190) emits ONLY plain `g.contract(a,b)` — the "epilogue envelope" reject `GemmEpilogueUnsupported` is BLAS alpha/beta/transpose, NOT an activation; there is NO gemm+relu. The MLP interior layers are `linalg.gemm` → `compute.dispatch(@relu.ckir)` = TWO stages + an intermediate z buffer (expand_ml.cpp:427-431 forward; grad.cpp:428-431 vjp recompute). FUSE → ONE synthesized CKIR kernel (contract result → `KOp::Max(·,0)` before store), eliminating the intermediate buffer + the relu dispatch. ⛔ LIVE CONSUMER (the 26b-4 no-live-consumer rule PASSES): `ml.mlp` forward (23c crown + 24b-4) AND the `vjp_mlp` recompute — BOTH backends. ⛔ SEMANTICS-PRESERVING = BIT-EXACT: the contract K-accumulation order is identical fused-vs-unfused (f32 in-register == f32 stored-then-reloaded), relu is a pure select (no arithmetic) ⇒ the 26 differential (bit-exact vs the RAW unfused device output, [[feedback_semantics_preserving_pass_differential_test_is_bit_exact_vs_unoptimized_program]]) applies cleanly. ⛔ NAME-FORWARD (not "basic"): softmax→gemm (attention — flash-attention-style cross-shape fusion, NOT basic); gemm+bias epilogue (no bias op in the corpus); elementwise-CHAIN fusion (25b-3 is one-op-per-kernel; the MLP doesn't chain elementwise). ⛔ THE DESIGN QUESTION FOR THE ADVISOR (best/gold-standard way): is 26e a COOK-TIME stage-merge (the QuantGemm-mirror — a `StageKind::GemmRelu` + a fold in plan_tensor_pipeline + a synth, "at the CEIR→CKIR boundary NOT a CEIR pass" like 26d) OR a CEIR IR fusion PASS (a `ceir.rewrite` fusing `linalg.gemm`+`@relu`→a fused op, differential-tested at the IR level per the tracker's "each pass differential-tested")? I lean COOK-TIME stage-merge (26d + QuantGemm precedent, honest scoreboard: the fusion is a scheduling/emit decision, the semantics stay in the two ops), but the advisor decides. NEXT = **26e-1** (the mechanism advisor gate — lock cook-time-merge vs CEIR-pass + the StageKind/synth shape + the differential-gate plan; NO build yet). ⛔ 26z checklist unchanged (carries the 3 items). | build §73/§74; proof §120 |

**26e-1 ✅ 2026-09-04 (the MECHANISM advisor gate — mechanism LOCKED cook-time stage-merge, fold predicate + synth shape + differential plan fixed; NO build yet, mirrors the 26d-1 no-build mechanism tick)**: advisor RESOLVED 26e-0's design question. ⛔ **DECISION = COOK-TIME stage-merge, NOT a CEIR pass** (my lean confirmed, with the DECISIVE fact I lacked): `@relu` is a `compute.dispatch` with a `kernel` ATTR → an authored `.ckir`; a CEIR rewrite fusing `linalg.gemm`+`compute.dispatch{kernel=@relu}` would have to MINT a fused op CEIR has no vocabulary for (a "gemm-with-epilogue" is neither a `linalg.gemm` — no activation attr — nor a dispatch — no authored pair kernel), then lower it anyway ⇒ the IR would carry a SCHEDULING decision. QuantGemm precedent EXACTLY: the fold is DETECTED at plan time, the two ops STAY in the module (honest semantics), `plan_tensor_pipeline` emits ONE stage. Row it like 26d: "cook-time stage fusion at the CEIR→CKIR boundary, NOT a CEIR IR pass — the fusion is an emit decision, the two ops stay in the module"; the tracker's "each pass differential-tested vs reference" holds as FUSED-plan device output vs UNFUSED-plan device output, bit-exact. ⛔ **CENSUS COMPLETE = 2 producers** (advisor's undercount check, run THIS tick): grep `mk_dispatch(…StringView("relu")…)` across `engine/` = EXACTLY TWO emit sites — `expand_ml.cpp:144` (fwd) + `grad.cpp:414` (vjp recompute); every other `"relu"` hit is a predicate (partition_ml.cpp:78, grad.cpp:374 activation-check, ml.cpp:112 misuse), a name-switch, a comment, or generated-doc — NOT a producer. NO third technique/frame consumer ⇒ the fold predicate faces exactly the two MLP epilogues. ⛔ **THE FOLD PREDICATE = 3 TYPED conditions** (mirror `dequant_fuses_into_gemm`, all typed): **(1)** the gemm result has EXACTLY ONE use (`num_uses()==1`) and that use is a `compute.dispatch` (multi-use → UNFUSED, someone else reads z); **(2)** the dispatch's `kernel` attr == "relu" (full symbol, not prefix) AND its READ operand `bind[0]` (access `r,w`) IS the gemm result (not merely "gemm result is an operand somewhere" — assert bind[0] identity); **(3)** the gemm is PLAIN (`gemm_is_plain` — alpha 1, beta 0, no transpose) — the fold MUST check it too, not only synth: else a non-plain gemm folds into a stage whose synth then REJECTS → UnresolvedKernel where the UNFUSED path WOULD HAVE WORKED. ⛔⛔ condition (3) IS the fusion-select scar [[feedback_fusion_and_specialized_kernel_selection_must_check_full_semantic_attrs_not_just_structure]] VERBATIM (bit at 23b) — "the fold must never make a runnable program unrunnable"; check at the FOLD, not just synth. ⛔ **SYNTH = EXTEND `synth_gemm`, DO NOT write a sibling** (bit-exact by CONSTRUCTION): add an OPTIONAL epilogue param (enum `None|Relu`, default None ⇒ every existing caller untouched) to the SAME `g.contract` K-loop → the fused kernel's K-accumulation is LITERALLY the same emitter code ⇒ f32-in-register == f32-stored-reloaded holds by construction. A SEPARATE fused synth could accumulate in a different order → 1-ulp drift the bit-exact CHECK rejects. ⛔ **relu.ckir SURVIVES** (unlike transpose.ckir at 26d-3a): the fused kernel synthesizes `max(contract,0)` IN the gemm kernel and does NOT dispatch relu.ckir; relu.ckir stays for the STANDALONE `@relu` (a dispatch whose input is NOT a single-use gemm result) — it is the authored SEMANTICS of relu, the fused kernel is a SCHEDULE computing the same thing (the synth_transpose argument, but the asset survives because it has non-fusable consumers). Say it explicitly so the everything-authorable reading doesn't object to a synth "replacing" an authored kernel. ⛔ **NEGATIVE GATES (same slice, the LOCKED-item scar [[feedback_locked_checklist_item_needs_a_gate_or_it_silently_doesnt_land]]):** (i) multi-use gemm result → NOT fused (both stages present, both run, output correct); (ii) non-plain gemm (alpha=2) → NOT fused, runs UNFUSED (the "never make runnable unrunnable" check); (iii) `@relu` whose input is a plain declare not a gemm → NOT fused, relu.ckir dispatches as today (the 23c-a/25c-1b standalone gates ARE this — CITE, don't duplicate). ⛔ **SUB-SPLIT (advisor, same rhythm as 26d-2):** **26e-2** = build (the fold predicate in plan_tensor_pipeline + `StageKind::GemmRelu` + `synth_gemm` epilogue slot + a DEVICE-FREE plan gate: the MLP plans to N-1 stages, structural) → **26e-3** = Vk device differential (fused == unfused bit-exact + the 3 negatives) → **26e-4** = DX12 mirror. NEXT = **26e-2** (the BUILD — fold predicate + StageKind::GemmRelu + synth_gemm epilogue param + device-free N-1-stage plan gate; advisor after). ⛔ 26z checklist unchanged (carries the 3 items). | build §73/§74; proof §120 |

**26e-2a ✅ 2026-09-04 (the FUSION MECHANISM — synth_gemm relu epilogue + BOTH contract emitters unwrap the Max root; device-free eval+emit gate; NO plan wiring yet)**: the synth+emit half of gemm→relu fusion, split off from 26e-2 per advisor (one concern/tick). ⛔ **DISCOVERY (primary-source, surfaced to advisor before building): `emit_contract_glsl` is a FIXED matmul TEMPLATE** (ckir_glsl.hpp:2345 `if (c.op != KOp::Contract) return false;`), NOT a graph walker — so "extend synth_gemm" (26e-1) alone would NOT emit an epilogue; the emitter must unwrap it too. Advisor OWNED the refinement (Design 1): the GRAPH carries the Max, the emitter unwraps it — bit-exact survives (same K-loop, only the store changes). **(1)** `ckir_synth.hpp`: new `enum class GemmEpilogue{None=0,Relu}` (⛔ append at END) + `synth_gemm(…, GemmEpilogue epilogue = None)` — Relu appends `binary(Max, contract, constant(0,[M,N]))` (the contract node `c` UNCHANGED ⇒ same K-loop). Default None ⇒ every pre-26e caller untouched. Header §12 struck-in-place (the "gemm maps ONLY the plain contract" clause → "+ optional 26e relu epilogue"). **(2)** `emit_contract_glsl` + `emit_contract_hlsl`: accept EITHER a bare `Contract` root OR a `Max(Contract, Const 0)` root — unwrap to the contract + emit `max(acc, 0.0)` at the store (else the plain `acc`). ⛔ the emitter CHECKS `cval == 0.0` exactly (advisor #4): a `Max(Contract, c≠0)` is NOT a relu → REJECT (never a silent max-with-a-nonzero false-green). **(3)** device-free gate `test_ckir_synth.cpp "ceir 26e-2a"` (⛔ advisor #5 — a gemm with NEGATIVE outputs so relu ≠ identity: A=[[1,2],[3,4]]·B=[[1,0],[0,-1]] = [[1,-2],[3,-4]], relu=[[1,0],[3,0]]): (i) NUMERIC `eval_cpu(synth_gemm(Relu)) == relu(A·B)` elementwise; (ii) REGRESSION `eval_cpu(synth_gemm(None)) == A·B` (default-arg path intact, negatives survive); (iii) EMIT both backends — the Relu graph's GLSL+HLSL contain `max(acc`, the None graph's do NOT (identity, not presence); (iv) NEGATIVE — a hand-built `Max(Contract, Const 5)` → BOTH emitters return false (the cval guard). GREEN win-debug `[ckir-synth]` 8/8 (121 assns, incl. 22b/25b regression) + `ckir_synth.cpp`/`test_ckir_synth.cpp` tidy-20-clean (headers covered via includers). ⛔ the emitter change is ADDITIVE-ONLY (a bare-Contract root takes the byte-identical path — sub-check (ii)+(iii)-None prove it) ⇒ no existing contract-emit consumer regresses. ⛔ SOURCE-vs-DEVICE (advisor post-review): sub-check (iii) proves the emitted SOURCE STRING contains `max(acc` — NOT that the shader compiles or is bit-identical to the unfused chain on hardware; the mechanism is DEVICE-FREE-proven, the BIT-EXACT claim stays a claim until Vulkan says so at 26e-3 (do not read "8/8 GREEN" as device-proven). ⛔ MINOR (leave): the `cval != 0.0` guard passes `-0.0` (IEEE `-0.0 != 0.0` is false) → a `Max(Contract, -0.0)` root would emit relu; harmless (synth_gemm only ever writes `+0.0`, and `max(x,-0.0)` differs only in a result-zero's sign); switch to `bit_cast<u64>(cval)==0` only if a signed-zero-sensitive epilogue ever lands. NEXT = **26e-2b** (the PLAN wiring: `StageKind::GemmRelu` [append at END] + `fusable_gemm_into_relu(ctx, g)` predicate = **4 typed conds, cheapest-first**: (1) op-name `linalg.gemm`, (2) `gemm_is_plain(g)`, (3) ⛔⛔ `!fusable_dequant_into_gemm_weight(ctx, g->operand(1)->defining_op())` [advisor CRITICAL blind-spot: the quant MLP `dequant→gemm→relu` is BOTH a QuantGemm target AND a GemmRelu producer; without this the fold hijacks it → B is the never-allocated dequant f32 result → DanglingOperand + the 1131-1133 QuantGemm gates go red — f32-weight gemms ONLY], (4) `g->result(0)->num_uses()==1` ∧ sole use is a `compute.dispatch{kernel=="relu"}` with `operand(3)`(bind[0] read)`==g->result(0)` ∧ `num_operands()==5` (grid×3+r+w); + re-mark h1 ExternalIn→Intermediate [advisor #3] + fold-at-relu-dispatch/skip-at-gemm [skip BEFORE QuantGemm-detect at the gemm; GemmRelu-detect AFTER parse_write_flags+trailing-write check but BEFORE the VizDispatch `st.kind` assign at the dispatch]). ⛔ NO ADJACENCY GUARD (advisor RETRACTED his own prior #2): the SSA single-use cond already covers the real hazard; a write-through-declare overwriting A between gemm+relu is a PLAN-WIDE gap (per-stage-output barriers don't model per-external-in WAR — same for Gemm/QuantGemm/VizDispatch), belongs to **26f (memory/schedule)**, NOT a fold predicate. ⛔ the 26e-2b device-free plan gate: (a) MLP fuses `gemm z1→relu h1`→ONE GemmRelu — assert NO `PlanBuffer.value==z1` (intermediate GONE, not just unbound) ∧ `h1`.role==Intermediate (re-mark landed) ∧ GemmRelu `bind[2]==find_buffer(h1)` [named-buffer, not just N-1 count]; (b) negs — multi-use gemm→not fused, non-plain gemm→SynthRejected unchanged (not fused), standalone `@relu`→VizDispatch (cite 23c-a/25c-1b), (c) ⛔ quant-MLP-NOT-HIJACKED = a FULL PLAN (build the quant MLP / lift the 1131-1133 corpus, plan it, assert `stages[0]==QuantGemm ∧ [1]==VizDispatch ∧ NO GemmRelu ∧ count==pre-26e`) — a predicate unit returning false does NOT prove the fold didn't fire, the plan does. Then 26e-3 Vk device differential (fused==unfused BIT-EXACT + resolver GemmRelu case) → 26e-4 DX12. ⛔ 26z checklist UNCHANGED (the adjacency item was NEVER added — advisor un-invented it; still the original 3). | build §73/§74; proof §120 |

**26e-2b ✅ 2026-09-05 (the PLAN WIRING — StageKind::GemmRelu + the fold + device-free plan gate; ⛔ fp32-MLP DEVICE tests now pending 26e-3's resolver case)**: gemm→relu fusion lands in the plan. **(1)** `tensor_pipeline.hpp`: `StageKind::GemmRelu` (⛔ append at END) + `fusable_gemm_into_relu(ctx, g)` decl. **(2)** `tensor_pipeline.cpp`: the predicate = **4 typed conds, cheapest-first** — (1) op-name `linalg.gemm`, (2) `gemm_is_plain`, (3) ⛔⛔ `!fusable_dequant_into_gemm_weight(g->operand(1)->defining_op())` [the quant MLP `dequant→gemm→relu` is BOTH a QuantGemm target AND a GemmRelu producer — f32-weight gemms ONLY], (4) `result->num_uses()==1` ∧ sole use is `compute.dispatch{kernel=="relu",AttrKind::SymbolRef}` with `num_operands()==5` ∧ `operand(3)==result` (bind[0] read). The FOLD: at the GEMM site `if (fusable_gemm_into_relu(op)) continue;` (skip — z NEVER allocated) BEFORE the QuantGemm-detect (exclusion enforced not implied); at the relu DISPATCH site (AFTER the trailing-write check, BEFORE the VizDispatch `st` assign) emit `StageKind::GemmRelu{op=the GEMM, bind={A,B,h}, n_out=1}` + re-mark h `ExternalIn→Intermediate` (device-produced, advisor #3). **(3)** device-free gate `test_tensor_pipeline.cpp "ceir 26e-2b"` 5 blocks: (a) POSITIVE 2-layer f32 MLP x[2,3]·W1[3,4]→relu→·W2[4,5] → **N-1 stages** {GemmRelu, Gemm} + NAMED-BUFFER asserts (advisor: `buf_of(z1)==-1` intermediate GONE, `h1.role==Intermediate` re-mark landed, `bind[2]==buf_of(h1)`, predicate FIRED) — not just a count; (b) NEG multi-use (z1 read by 2 relus → `num_uses!=1` → not fused, stages[0]==Gemm); (c) NEG non-plain α=2 gemm → predicate false + `PlanReject::SynthRejected` (synth rejects it as it ALWAYS would — the fold declines, reject UNCHANGED; advisor's corrected (ii)); (d) NEG standalone `@relu` on a plain declare → VizDispatch (23c-a/25c-1b path); (e) ⛔⛔ NEG quant-MLP-NOT-HIJACKED = a FULL PLAN (advisor CRITICAL) via `build_quant_mlp` → `{QuantGemm, VizDispatch, QuantGemm}` UNCHANGED + NO GemmRelu (the `!fusable_dequant` exclusion holds). GREEN win-debug `[tensor-pipeline]` 25/25 (353 assns, incl. 23c-b QuantGemm + 23b-2b regression) + FULL device-free `crd-ceir-gpu-tests` 96/1356 (grad/expand_ml unaffected) + `tensor_pipeline.cpp`/`test_tensor_pipeline.cpp` tidy-20-clean. ⛔⛔⛔ **DEVICE DEBT (NOT device-free-broken, but pending 26e-3):** the plan fold is in the shared `crd-ceir-gpu` lib, so the fp32 `ml.mlp` DEVICE tests — **24b-4** (test_ceir_pipeline_vulkan.cpp:1431) + **26d-2c** (:1529) + **24c-3 run-A** (:1814 CKIR-expand leg) + their DX12 mirrors — now plan a `GemmRelu` stage the resolver if/else does NOT yet handle → `UnresolvedKernel` ON-DEVICE. This is the advisor-blessed split (26e-3 = Vk + resolver case); these live in the vulkan/dx12 targets (NOT the device-free suite), so nothing device-free regressed, but ⛔ do NOT call 26e device-proven until 26e-3/4 add the resolver case + re-green them. NEXT = **26e-3** (Vk device leg, advisor-scoped): **(1)** ⛔⛔ add `PlanOptions{ bool fuse_gemm_relu = true }` (a struct, ONE field — do NOT pre-populate, it's the 26f/27 "fusion IS a schedule decision" seed) threaded into BOTH fold sites (gemm-skip + relu-dispatch-emit) as a 4th defaulted arg to `plan_tensor_pipeline` — because the fold is UNCONDITIONAL today, so the ONLY honest raw-vs-opt differential (the [[feedback_semantics_preserving_pass_differential_test_is_bit_exact_vs_unoptimized_program]] contract wants raw-vs-opt on ONE program, NOT a hand-built twin = two different programs) is to plan the SAME fp32-MLP module TWICE (fuse=false vs fuse=true), run both on-device, assert BIT-EXACT — the first DISPATCHED-stage 26e witness at full teeth. **(2)** resolver: LIFT the `StageKind::Gemm` block into `if (kind==Gemm || kind==GemmRelu)` passing `kind==GemmRelu ? GemmEpilogue::Relu : None` into `synth_gemm` → `emit_contract_glsl` (push{M,K,N,1}, grid=M·N, nbind=3 IDENTICAL by construction — NOT a sibling branch). **(3)** ⛔ 24b-4/26d-2c/24c-3-runA: READ their assertions FIRST — if they `REQUIRE(stages.size()==3)`+index `stages[1]` as the relu VizDispatch → mechanical update (→GemmRelu+Gemm, one fewer stage); if they only assert `reject==None`+oracle-compare → NO assertion change, just the resolver case (do NOT add stage-shape asserts a test lacked — scope creep dressed as teeth). **(4)** ⛔ BARRIER NOTE: the fused path has strictly FEWER sync points (z's barrier gone — CORRECT); h still gets its `n_out=1` barrier. If the differential shows NON-deterministic FLICKER (not a consistent 1-ulp) suspect the h barrier, not the K-loop. → 26e-4 DX12 mirror. ⛔ 26z checklist unchanged (original 3). | build §73/§74; proof §120 |

**26e-3a ✅ 2026-09-05 (the Vk device RESOLVER + PlanOptions flag + the fp32-MLP device tests RE-GREENED; the FUSED MLP runs device-resident == oracle on real RTX)**: discharges the 26e-2b device debt on Vulkan. **(1)** `PlanOptions{ bool fuse_gemm_relu = true }` (tensor_pipeline.hpp, ONE field — the 26f/27 "fusion IS a schedule decision" seed) threaded into BOTH fold sites (`opts.fuse_gemm_relu && …`) as a 4th defaulted arg to `plan_tensor_pipeline` — so the SAME module plans BOTH ways (the raw-vs-opt differential at 26e-3b needs it). **(2)** the Vulkan resolver: LIFTED `StageKind::Gemm` → `if (kind==Gemm || kind==GemmRelu)` passing `kind==GemmRelu ? GemmEpilogue::Relu : None` into `synth_gemm` → `emit_contract_glsl` (the Max(Contract,0) unwrap; push{M,K,N,1} + grid M·N + nbind=3 IDENTICAL by construction — NOT a sibling branch). **(3)** ⛔ 26d-2c PINNED `fuse_gemm_relu=false` (a schedule-flag's FIRST real use): 26d-2c's whole point is the relu VizDispatch cook-bind + oversize `LocalSizeExceedsLimit` cap (the 26d shape-specialization mechanism) which exists ONLY when relu is a STANDALONE dispatch; fusion default-ON folds the MLP's relu into the gemm store (no VizDispatch to cook-bind), so pinning fuse-off KEEPS the 26d teeth (they still guard standalone relu / softmax / relu_vjp) — the FUSED path is 26e-3b's bit-exact witness. 24b-4 + 24c-3 needed NO assertion change (they only `REQUIRE(reject==None)` + oracle-compare — advisor: don't add stage-shape asserts a test lacked). GREEN on REAL RTX 4070 Ti: **24b-4 Vk #4752** (the FUSED fp32 MLP `gemm+relu` == the CPU float MLP oracle at 1e-4 rel-tol — ⭐ the resolver GemmRelu case + emit_contract relu-unwrap are device-correct vs the float oracle at TOL; the BIT-EXACT word is reserved for 26e-3b), **26d-2c Vk #4753** (unfused fuse-off relu cook-bind + oversize negative intact), **24c-3 Vk #4755** (the §136 crown: fused CKIR-expand == coopvec-native agree) + device-free `[tensor-pipeline]` 353/25 UNCHANGED (PlanOptions default preserves it) + tensor_pipeline.cpp/test_ceir_pipeline_vulkan.cpp tidy-20-clean. ⛔ SCOPE (advisor honest-scoreboard): 24b-4's `== oracle` is DEVICE-vs-ORACLE (1e-4 rel-tol, the 24b standard), NOT yet the 26 BIT-EXACT raw-vs-opt witness — that is 26e-3b. ⛔ DX12 STILL STALE: #4837/#4838 passed on the pre-26e DX12 exe (only crd-ceir-gpu-vulkan-tests was rebuilt; the DX12 exe links the OLD lib → old no-fusion plan) — NOT evidence; 26e-4 rebuilds crd-ceir-gpu-dx12-tests + adds the DX12 resolver Gemm||GemmRelu lift + re-greens. NEXT = **26e-3b** (the Vk BIT-EXACT raw-vs-opt differential — the first DISPATCHED-stage 26e witness at FULL TEETH): plan the SAME fp32-MLP module TWICE (`PlanOptions{fuse=false}` vs `{fuse=true}`), run BOTH on Vulkan, assert the outputs are **BIT-EXACT** equal (not tol — the [[feedback_semantics_preserving_pass_differential_test_is_bit_exact_vs_unoptimized_program]] contract; unfused = Gemm→relu.ckir dispatch, fused = GemmRelu's `max(acc,0)` store, same K-loop ⇒ bit-identical). ⛔ ADVISOR (26e-3b): (a) REUSE 26d-2c's EXACT seeds+shape (d1=16, h1 numel 64 ≤ 1024 — a KNOWN-GOOD unfused arm that cook-binds + ran green #4753; don't invent a corpus); (b) `REQUIRE` ≥1 CPU pre-activation `z1=x·W1 < 0` (else BOTH arms compute identity-relu → the differential passes VACUOUSLY — the 26e-2a advisor-#5 negative-output rule, on device now); (c) the compare loops EVERY output element + reports the FIRST-MISMATCH index + both values (not a boolean — a fail must be diagnosable in ONE run); (d) determinism: ⛔ a CONSISTENT wrong value in the unfused arm ⇒ the h/z barrier path, a FLICKER ⇒ same; NOT the K-loop. Then 26e-4 (DX12): ⛔ REBUILD `crd-ceir-gpu-dx12-tests` FIRST — EXPECT 24b-4/26d-2d to go RED before the resolver lift lands (that red is the PROOF the stale exe was masking; `build-target.bat <target>` relinks ONLY that target's exe, a sibling test exe keeps its old static lib until IT is rebuilt — a stale-binary trap, memory-worthy if it bites twice) → then the DX12 resolver Gemm||GemmRelu lift + re-green 24b-4/26d-2d + the DX12 bit-exact differential. ⛔ 26z checklist unchanged (original 3). | build §73/§74; proof §120 |

**26e-3b ✅ 2026-09-05 (the gemm→relu fusion is BIT-EXACT vs the unfused program on Vulkan — a NEW-EMITTER-PATH witness at FULL TEETH)**: the raw-vs-opt differential lands. A new `[ceir][ml][gpu]` gate `test_ceir_pipeline_vulkan.cpp "ceir 26e-3b"` (#4754, real RTX 4070 Ti): the SAME fp32-MLP module (x[4,8]·W1[8,16]→relu→·W2[16,2]) is planned TWICE — `PlanOptions{fuse=false}` → 3 stages (Gemm + VizDispatch(relu) + Gemm) and `{fuse=true}` (default) → 2 stages (GemmRelu + Gemm, N-1) — run BOTH on Vulkan, and every output element is **BIT-EXACT** equal (`out_f[i] == out_u[i]`, NOT a tolerance). ⛔ This is exactly the [[feedback_semantics_preserving_pass_differential_test_is_bit_exact_vs_unoptimized_program]] contract at FULL teeth. ⛔ NOVELTY (advisor-corrected — NOT "first dispatched-stage witness": 26a-3 ALREADY removed DISPATCHED stages [the dead backward branch] bit-exactly on device): 26e is the first where the SURVIVING kernel is DIFFERENT CODE — a SYNTHESIZED fused kernel (the `emit_contract` relu-unwrap store) REPLACES an authored relu.ckir dispatch — so the differential covers a NEW EMITTER PATH, not a pruned subset of the same kernels (26b was alias-only; 26c removed a DUPLICATE gemm; both left every surviving kernel byte-identical). It stays bit-identical because the contract K-loop is the SAME `emit_contract` (`precise float acc`) and an f32 store/load is identity + max is `FMax` either way. ⛔ ADVISOR-hardened (all landed): (a) REUSED 26d-2c's EXACT seeds+shape (d1=16, the KNOWN-GOOD unfused arm from #4753 — no new corpus); (b) `REQUIRE(neg_count > 0)` — a CPU pre-activation sweep proving ≥1 `z1=x·W1 < 0` so relu actually FLIPS an element (else the witness passes VACUOUSLY — the 26e-2a advisor-#5 rule, on device); ⛔ the 26d-2c negatives are FAR from zero (|z1| ≫ ulp), so the CPU sign (plain-float accumulation, NOT `precise` order) == the GPU sign — non-vacuity is robust here, but a future reader who reseeds to NEAR-ZERO sums inherits a vacuous gate (CPU/GPU signs can then diverge); keep the magnitudes large or assert the negativity on-device; (c) the compare reports the FIRST-MISMATCH index + both values via CAPTURE (diagnosable in ONE run); (d) determinism note in-gate (a consistent wrong value ⇒ the h/z barrier path, NOT the K-loop — the fused path has one fewer sync, z's barrier gone, which is correct). GREEN real RTX #4754 (0.41 s) + `test_ceir_pipeline_vulkan.cpp` tidy-20-clean. ✅✅ **26e-3 (Vk device leg) COMPLETE** = 26e-3a (PlanOptions + resolver lift + re-green 24b-4/26d-2c/24c-3) + 26e-3b (the bit-exact raw-vs-opt witness). The Vulkan half of gemm→relu fusion is PROVEN device-resident + semantics-preserving. NEXT = **26e-4** (the DX12 mirror → 26e COMPLETE): ⛔ REBUILD `crd-ceir-gpu-dx12-tests` FIRST — EXPECT 24b-4/26d-2d to go RED before the resolver lift lands (that red is the PROOF the stale exe was masking the fold — `build-target.bat <target>` relinks ONLY that target's exe, a sibling keeps its old static lib until IT rebuilds) → then the DX12 resolver `Gemm||GemmRelu` lift — ⛔ LIFT THE WHOLE `StageKind::Gemm` block (not just the synth line: the DX12 `pushsize`/root-const handling rides it), `synth_gemm(*st.op, Relu)`→`emit_contract_hlsl`, push{M,K,N,1}, grid M·N, nbind=3 — + (⛔ IFF 26d-2d has the VizDispatch-loop discriminating check like 26d-2c) pin it `fuse=false`, ELSE leave fuse-on (it then tests the fused path vs oracle, fine — check first, don't pin reflexively) + a DX12 26e-3b-MIRROR bit-exact differential (copy 26e-3b's body verbatim, swap the DX12 context/compute + the DX12 runner — adapt the CALL, NOT the assertions). ⛔ 26z checklist unchanged (original 3). | build §73/§74; proof §120 |

**26e-4 ✅ 2026-09-05 (the DX12 MIRROR — the fusion is BIT-EXACT on DX12 too) → ✅✅✅ CEIR-26e (basic-fusion → generated CKIR) COMPLETE**: the DX12 leg closes 26e on both backends. ⛔ THE STALE-EXE RED, OBSERVED FIRST (advisor — diagnostic not ceremony): rebuilt `crd-ceir-gpu-dx12-tests` (current lib, no resolver change) → 26d-2d went RED (`1 == 0`, 5 assns) — the fold WAS masked by the stale exe (`build-target.bat <target>` relinks ONLY that target; the DX12 exe kept its pre-26e static lib until rebuilt), and 26d-2d has the SAME VizDispatch-loop discriminating check as 26d-2c (relu fused → no VizDispatch → cook-bind probe fails) + `run_mlp` UnresolvedKernel. The red CONFIRMED the mental model before any fix. **(1)** DX12 resolver: LIFTED the WHOLE `StageKind::Gemm` block → `if (kind==Gemm || kind==GemmRelu)` (advisor: lift the whole block — the DX12 `pushsize`/root-const handling rides it), `synth_gemm(*st.op, ep)`→`emit_contract_hlsl` (the Max(Contract,0) unwrap; push{M,K,N,1} + grid M·N + nbind 3 identical). **(2)** 26d-2d PINNED `fuse=false` (it HAS the VizDispatch loop — checked, then pinned; mirror of 26d-2c). **(3)** new DX12 gate `test_ceir_pipeline_dx12.cpp "ceir 26e-4"` — the verbatim 26e-3b mirror (same shape d1=16, same 26d-2d seeds, `REQUIRE(neg_count>0)`, first-mismatch CAPTURE) with the DX12 context/runner. GREEN real RTX 4070 Ti: **26e-4 #4840** (fused == unfused BIT-EXACT on DX12), **24b-4 DX12 #4838** (the FUSED fp32 MLP == oracle — the resolver GemmRelu case now handles it, no longer stale), **26d-2d #4839** (unfused fuse-off cook-bind + oversize) + §137 pipeline gemm regression `26b-2`/`26c-2` BOTH backends #4762/#4763/#4846/#4847 GREEN (the shared resolver lift is additive — the plain-Gemm path unaffected) + `test_ceir_pipeline_dx12.cpp` tidy-20-clean. ✅✅✅ **CEIR-26e COMPLETE** = 26e-0 (census) + 26e-1 (mechanism: cook-time stage-merge) + 26e-2a (synth_gemm epilogue + both emitters unwrap Max(Contract,0)) + 26e-2b (StageKind::GemmRelu + fusable_gemm_into_relu 4-cond + fold + plan gate) + 26e-3a (Vk resolver + PlanOptions) + 26e-3b (Vk bit-exact) + 26e-4 (DX12 resolver + bit-exact). **gemm→relu epilogue fusion is a cook-time stage-merge at the CEIR→CKIR boundary, proven device-resident + BIT-EXACT vs the unfused program on BOTH backends** (the first fusion that synthesizes a NEW kernel replacing an authored dispatch). NEXT = **26f** (memory/schedule — the last 26 sub-slice before 26z band-close; the band shape 26a DCE→26b fold→26c CSE→26d specialize→26e fusion→**26f mem/schedule**→26z close). advisor to open 26f as a CENSUS (the 26b-4 no-live-consumer rule — is there a real memory/schedule consumer, or is it ABSORBED?). ⛔ ADVISOR 26f-census PRIOR (3 axes): **(1) schedule** = does any corpus have TWO INDEPENDENT stages (neither reads the other's output)? MLP/attention/§137 are CHAINS; the 25c-2 autodiff backward has dW1∥dW2 fan-out — grep it; if none, "schedule" has no live consumer → ABSORBED ("the plan is a topological serialization"). **(2) memory** = does any corpus have two Intermediates with DISJOINT lifetimes? ⭐ ATTENTION's kt/scores/probs each die before the next is born (kt after gemm1, scores after softmax, probs after gemm2) → a REAL memory-aliasing consumer; measure Σbytes(Intermediates) vs max(one) — ratio ~3 = a measurable win on a shipped corpus → 26f = a PlanBuffer-lifetime aliasing pass, first consumer attention, differential BIT-EXACT (aliasing changes ADDRESSES not kernels — the 26b alias-only WIRING-proof shape, say so). **(3) the WAR gap** (named at 26e-2b) = does any corpus write a buffer a later stage reads w/o an intervening barrier (only via `rw` or an ExternalIn written-then-read)? if none → a documented gap + a negative gate, NOT a pass. ⛔ 26z checklist **+1 = 4 items**: (1) llvmpipe re-run of the 26b-2 reshape-fold differential + (2) llvmpipe re-run of the 26c-2 CSE differential + (3) the anon `resource.import` CSE sibling + **(4) WRITE the stale-exe-trap `feedback_` scar** (`build-target.bat <target>` relinks ONLY that target's exe; a sibling test exe FALSE-GREENS against its stale static lib until IT rebuilds; remedy = rebuild EVERY test exe that links the changed lib before trusting a sibling's green — bit at 26e-4 with evidence: DX12 `1==0` on rebuild after the Vulkan tick showed green) — write it at 26z in housekeeping mode, alongside the deferred MEMORY.md compaction. | build §73/§74; proof §120 |

**26f-0 ✅ 2026-09-05 (the MEMORY/SCHEDULE census — 3 axes per advisor; NO build; the 26b-4 no-live-consumer rule applied to each)**: censused §73's last 26-band row. ⛔ **SCHEDULE = ABSORBED (name-forward to CEIR-28, NOT 26f).** Real fan-out EXISTS (the `vjp_gemm` backward emits `dA=gemm(dc, Bᵀ)` ∥ `dB=gemm(Aᵀ, dc)` + the two transposes Bᵀ∥Aᵀ — independent, neither reads the other), and `plan_tensor_pipeline` is a pure PROGRAM-ORDER walk (no reorder/overlap; the executor barriers every non-final stage's outputs = a FULLY SERIALIZED schedule). BUT scheduling (reorder/overlap of independent stages) is a LATENCY optimization: it changes TIMING, not VALUES ⇒ there is NO bit-exact witness (a schedule change is bit-identical whether serialized or overlapped — the 26 "bit-exact vs unoptimized" differential passes VACUOUSLY). Correctness-with-a-witness is the 26-band bar; perf-driven scheduling + cost model is **CEIR-28** (autotune/PGO). So 26f does NOT build a schedule pass — a one-line honest row ("the plan is a topological serialization; independent-stage overlap is a latency opt with no bit-exact witness, deferred to CEIR-28's cost model"). ⛔ **MEMORY = the ONE live 26f consumer → an INTERVAL-COLORING buffer-aliasing pass.** `PlanBuffer` has NO lifetime ({value, role, bytes, alias_of, fill}) — every Intermediate gets its OWN buffer, NEVER reused. The ATTENTION corpus (runs BOTH backends — 24b-4 + 26d-3c) has THREE Intermediates with DISJOINT lifetimes: kt=[D,Sk] live[transpose→gemm1], scores=[Sq,Sk] live[gemm1→softmax], probs=[Sq,Sk] live[softmax→gemm2] — kt∩probs = ∅ (kt dead before probs born), max-simultaneously-live = 2 ⇒ 3 Intermediates need only 2 PHYSICAL buffers. Measured win at Sq=3,Sk=5,D=4: Σbytes 200B (20+15+15 f32·4) → 140B (max(D,Sq)·Sk + Sq·Sk = 20+15) = **30% fewer intermediate bytes on a SHIPPED corpus**. ⛔ this IS the 12d `plan_block_memory` "interval-coloring/aliasing" the tensor_pipeline.hpp header PRE-DEFERRED as "a ledgered slice" — 26f finally builds it. ⛔ DIFFERENTIAL HAS TEETH (unlike schedule): aliasing changes ADDRESSES not kernels, so a bit-exact PASS proves the LIFETIME ANALYSIS is SOUND (a WRONG alias overlaps a live range → CLOBBER → wrong output — the witness catches it); the 26b alias-only WIRING-proof shape, but load-bearing here (a bad alias corrupts data). ⛔ **WAR = documented gap + negative gate (NOT a pass):** grepped the corpora — ZERO `rw` bindings (emit sites are `r,w`/`r,r,w`; the executor barriers every trailing output), so the write-through-declare WAR hazard (named at 26e-2b) can only bite via an `rw` binding or a dispatch-written ExternalIn read by a later stage, neither of which any corpus does → a negative gate (author one, assert reject/barrier), not a live pass. ⛔ **26f-1 MECHANISM LOCKED (advisor, same call — declared done):** (1) ⛔ NOT interval-coloring — the plan is a LINEAR CHAIN (each Intermediate one producer; usually one consumer), so aliasing collapses to a SINGLE PASS with a free-list of `(index, bytes)`: a buffer becomes FREE the moment its LAST reader has run; the next Intermediate born after may take it if it fits (smallest-fit, else largest-and-grow). No coloring/allocator. (2) ⛔ last-use = `max` over ALL uses (walk every use, NOT `first_use()` — the `num_uses()` discipline): the `vjp_gemm` backward's `dc` has TWO consumers (dA, dB) → freed only after BOTH. (3) ⛔ REPRESENTATION = WIDEN `alias_of`, NO new field: today `alias_of` = a VIEW (role Alias, same value, bytes copied); a storage TENANT = role Intermediate, DIFFERENT value, own bytes, `alias_of` = the LANDLORD (allocated) buffer. The runner skips allocation when `alias_of >= 0` REGARDLESS of role (a ONE-LINE change in `run_quant_module_n` + its DX12 twin — today it skips only on role==Alias); `role` disambiguates view(Alias) vs tenant(Intermediate). Landlord `bytes` GROWS to `max(landlord, all tenants)`. `alias_of` points at the LANDLORD, NEVER another tenant (one-level `bufs[i]=bufs[alias_of]`; the 26b-2 alias-order gate `alias_of < i` + realized). (4) ⛔ EXCLUDED from sharing (neither landlord nor tenant): ExternalIn (clobbers caller data), Output, `value==nullptr` (fft im/tw — no SSA lifetime), and ⛔⛆ **func.return operands (PINNED)** — the advisor's named-readback trap CONFIRMED REAL: 25c-2 reads back dW2 as an Intermediate via the runner's `is_named_out`→GpuToCpu path (test_ceir_pipeline_vulkan.cpp:294/325); a tenant dW2 would be allocation-skipped → readback reads the landlord's GpuOnly bytes → FAIL. RESOLUTION (option-b, plan-side + robust): the plan collects func.return operands (dW1/dW2 are func.return'd per the pin-readback scar) as PINNED → never shared. Subsumes the Output exclusion; knowable in the plan (no runner override, no caller flag). (5) `PlanOptions{ share_intermediate_storage = true }` (NOT "alias_intermediates" — collides with the Alias role; the fuse_gemm_relu default-true mold) so the SAME module plans both ways for the raw-vs-opt bit-exact differential + a FEWER-PHYSICAL-BUFFERS assertion. ⛔ DIFFERENTIAL TEETH: bit-exact PASS proves the LIFETIME ANALYSIS is SOUND (a wrong alias overlaps a live range → clobber → wrong output). NEXT = **26f-2** (the PLAN BUILD: the lifetime walk [max over all uses] + the free-list + `alias_of` widening + func.return-pinned + `share_intermediate_storage` + the runner one-line skip both backends + a DEVICE-FREE gate on ATTENTION: 3 Intermediates → 2 physical, landlord bytes==max, `alias_of`→landlord (<i, realized), + NEGATIVES: the backward `dc`-two-consumer NOT freed early, a func.return'd dW2 NOT shared). Then 26f-3 Vk bit-exact differential + physical-buffer-count assertion → 26f-4 DX12. ⛔ 26z checklist unchanged (the 4 items). | build §73/§74; proof §120 |

**26f-2 ✅ 2026-09-05 (the PLAN BUILD — the single-pass free-list buffer-aliasing pass + device-free gate; runner skip + device differential are 26f-3/4)**: memory-aliasing lands in the plan. **(1)** `PlanOptions{ share_intermediate_storage = true }` + `PlanBuffer::alias_of` doc WIDENED (view[Alias] vs tenant[Intermediate], both "don't allocate — use buffer N"). **(2)** `assign_shared_storage` (tensor_pipeline.cpp, a POST-PASS after Output-marking, guarded by the flag): per-buffer `produce_stage` (the trailing-output stage) + `last_read_stage` (⛔ MAX over ALL read binds — the num_uses discipline, NOT first_use); then the SINGLE-PASS free-list — a tenant `b` takes a ROOT landlord `L` iff `free_at[L] < produce[b]` (STRICT: L's last reader ran at a stage BEFORE b's producer, so the per-stage exec barriers serialize L's read < b's write) AND fits (smallest-fit, else largest-and-GROW to b); `b.alias_of = L`, `L.bytes = max`, `free_at[L] = last_read[b]` (a later buffer may take L again — sequential reuse, all `alias_of` point at the ROOT). ⛔ EXCLUDED (not landlord/tenant): ExternalIn/Output/`value==nullptr` + ⛔ **func.return operands PINNED** (the plan walks func.return, pins its operands — the 25c-2 dW2 named-readback trap: a tenant dW2 would be alloc-skipped → readback reads the landlord's GpuOnly bytes → FAIL). **(3)** device-free gate `test_tensor_pipeline.cpp "ceir 26f-2"` (3 blocks): (a) POSITIVE a 4-dispatch chain v0[2,2]→v1[4,4]→v2[4,4]→out — v2 TENANTS v0 (disjoint lifetimes), v0 GROWS 16→64B (the max), v1 a landlord, `alias_of<index` (realized), **3 Intermediates → 2 physical**; (b) NEG last_read=MAX — z read at s1 AND s3, a buffer `c` born s2 does NOT tenant z (if last_read were first_use=s1, z would free early → c clobbers z; the assert `c.alias_of != z` discriminates); (c) NEG func.return-PINNED — dw2 (returned, z free before it, would tenant z absent pinning) keeps its OWN buffer. GREEN win-debug `[tensor-pipeline]` 26/26 (372 assns, incl. 26e-2b fusion + §137 + 26b/26c fold regression — aliasing default-ON is INERT where no disjoint Intermediates exist) + FULL device-free `crd-ceir-gpu-tests` 97/1375 (grad/expand_ml unaffected) + tensor_pipeline.cpp/test tidy-20-clean (localconstant renames L→land, dW2→dw2). ⛔ **WAR = a proven FACT, not a hypothesis (advisor):** reusing L's storage is a Write-After-Read on the shared buffer (b writes L's bytes after L's last read), but NO WAR barrier is needed — the executor's per-stage `ShaderWrite→ShaderRead` barrier has a STAGE-WIDE EXECUTION dependency (`srcStage = COMPUTE_SHADER` orders ALL compute before it, reads included, before any after; the access masks scope only memory visibility), so the STRICT `free_at[L] < produce[b]` guarantees ≥1 such barrier sits between L's read and b's write ⇒ ordered by construction (DX12 UAV `ResourceBarrier` on the compute queue is the same). 26f-3's bit-exact CONFIRMS this, it does not discover it. ⛔⛆ **HOLE the advisor found (fixed at 26f-2b):** an `n_out==0` non-final stage emits NO barrier (barriers ride the trailing outputs), so a buffer whose LAST READER is such a stage has no barrier before a later write to its storage → the free-list must NEVER free it (set `last_read = INT_MAX`). No corpus has an `n_out==0` non-final stage today (latent), but the pass would alias across one. ⛔ the runner skip is INERT until 26f-3: the plan sets a tenant's `alias_of` but `run_quant_module_n` still skips only role==Alias ⇒ tenants are allocated separately (device tests unchanged) until 26f-3 widens the skip. NEXT = **26f-2b** (advisor sub-split, device-free/compile-only): (1) the `n_out==0` guard in `assign_shared_storage` (`last_read=INT_MAX` when `stages[s].n_out==0`) + a gate (a synthetic `n_out==0` reader ⇒ its buffer NOT aliased by anything born after); (2) gate block (d) — plan ATTENTION via `expand_ml_ops`, assert EXACTLY ONE tenant = the softmax write tenants kt (the NAMED consumer fires the pass, not only the synthetic chain); (3) add `n_allocated` (the `create_buffer` count) as an out-param to `run_quant_module_n` on BOTH backends (compile-only prep — 26f-3 asserts the EXACT delta `shared == unshared − tenant_count`, not "fewer"). Then **26f-3** (Vk): widen the Vulkan runner skip (`alias_of>=0` regardless of role, 1 line) + the ATTENTION bit-exact differential (`share_intermediate_storage` false vs true, run both, BIT-EXACT + the exact `n_allocated` delta) + RE-RUN 25c-2 (dW2 readback, share=true) = the func.return-pinning DEVICE proof → 26f-4 DX12 → 26z close. ⛔ 26z checklist unchanged (the 4 items). | build §73/§74; proof §120 |

**26f-2b ✅ 2026-09-05 (the free-list's TWO remaining edges — the `n_out==0` no-barrier GUARD + the NAMED-consumer gate; device-free)**: closes the advisor's 26f-2 review holes. **(1)** ⛔ the `n_out==0` GUARD (the ACTUAL fix, advisor): a stage with no trailing write (an all-read `"r,r"` dispatch, `n_out==0`) emits NO barrier (the executor's per-stage `ShaderWrite→ShaderRead` barrier rides the trailing outputs), so a buffer whose LAST reader is such a stage has no barrier before a later write to its storage → it must NEVER lend. ONE line in the last_read walk: `last_read[bi] = (st.n_out==0) ? never_free : s` (`never_free = 0x7fffffff` ⇒ `free_at`≥every produce ⇒ never a landlord; a later NORMAL read overwrites it, so the semantics is exactly "is the LAST reader n_out==0?" — the max-in-stage-order composition). ⭐ every ACTUAL tenancy stays safe BY CONSTRUCTION: a landlord is chosen only when its `last_read` is a real stage `< produce[b]`, and that stage (having a barrier-bearing output, `n_out>0`) orders the WAR. Latent (NO corpus has an `n_out==0` non-final stage — the planner accepts one: only the FINAL stage triggers `NoOutput`, `DispatchOutputsNotTrailing` only fires on a non-trailing write) but the free-list would happily alias across one. **(2)** device-free gate `test_tensor_pipeline.cpp "ceir 26f-2b"` (2 blocks): (d) NEG the guard — a 4-stage chain where s1 is an all-read `"r,r"` dispatch that last-reads `held`, `later` born s2 (fits, 64B) would tenant `held` absent the guard (free_at=1 < 2) with NO barrier between held's read and later's write → a WAR clobber; the guard ⇒ `later.alias_of<0` (does NOT tenant) + `held.alias_of<0` (stays a root). ⭐ IDENTITY not category (advisor): the block PINS `plan.stages.size()==4` + `plan.stages[1].n_out==0` — proving the hole-shaped stage EXISTS in the plan (else `later` would have nothing to tenant for the TRIVIAL reason that the planner dropped s1, and the gate would pass VACUOUSLY testing "aliasing refused for ANY reason" not "the guard fired"); (e) POS the NAMED consumer — a real `ml.attention` (Q[2,4]·K[3,4]·V[3,2]→out[2,2]) via `expand_ml_ops` → transpose(K)→Kt[4,3] (s0)·gemm1→scores (s1)·softmax→probs (s2)·gemm2→out (s3): EXACTLY ONE tenant in the whole plan (`n_tenant==1`) and it is the softmax WRITE — `probs` (24B) TENANTS `Kt` (48B, free after s1) — the pass fires on the SHIPPED op, not only a synthetic viz chain (scores can't tenant Kt: born s1, Kt not free until after s1; out never reads ⇒ neither; gemm C operands + softmax scale are ExternalIn ⇒ not shareable). GREEN win-debug `[tensor-pipeline]` 27/27 (389 assns after the 2 advisor identity asserts, +1 case/+17 assns over 26f-2's 372) + FULL device-free `crd-ceir-gpu-tests` 98/1392 (was 97/1375 — the +1 case/+17 assns is EXACTLY 26f-2b, ZERO regression: the guard is a proven NO-OP on every `n_out>0` stage — identical ternary result) + tensor_pipeline.cpp/test tidy-20-clean (`never_free`/`held`/`later`/`kt`/`probs` all lowercase — localconstant-clean by construction). ⛔ **SCOPE DEVIATION from the advisor's bundling (mine, surfaced):** the advisor put `n_allocated` (the `create_buffer` count out-param on both runners) in 26f-2b as "compile-only prep"; I FOLDED it into 26f-3 instead — an out-param with NO assertion until 26f-3 is ungated dead code for a tick (the LOCKED-but-no-gate scar); adding it in the tick that ASSERTS the exact delta keeps every landed line gated. NEXT = **26f-3** (Vk device leg). ⛔ ADVISOR SHARPENING (record, act at 26f-3): **(A)** the runner skip is NOT one global line — grep `run_quant_module_n` for EVERY `BufferRole::Alias` check (alloc loop, upload loop, per-stage binds, teardown/destroy loop) and switch each to `alias_of>=0`; ⛔ the TEARDOWN/destroy loop is the LIKELY BITE (a tenant shares a `ComputeBuffer*` with its landlord → double-destroy) — "one line" is per-loop-that-keys-on-Alias. **(B)** the re-marked decls (GemmRelu `h` @638, VizDispatch write = `probs` @664) set only `role`, NOT `fill` — they keep `fill=CallerData`; today harmless (they own storage), but under aliasing IF the upload loop keys on `fill==CallerData` it uploads into the LANDLORD's buffer at setup → confirm the upload predicate, and if it keys on fill, set `fill=GpuOnly` at BOTH re-mark sites (more honest anyway). **(C)** IF the 4 items don't fit one tick, sub-split: **26f-3a** = widen every runner skip + `n_allocated` + rerun the EXISTING attention device tests (24b-4, 26d-3c) with share=true default (aliasing goes live on device the FIRST time — a clobber shows HERE before the differential is written); **26f-3b** = the formal share-false-vs-true BIT-EXACT + the EXACT `n_allocated` delta (`shared == unshared − tenant_count`, NOT "fewer") + RE-RUN 25c-2 (dW2 readback, share=true) = the func.return-pinning DEVICE proof. → 26f-4 DX12 mirror → 26z close. ⛔ 26z checklist unchanged (the 4 items). | build §73/§74; proof §120 |

**26f-3a ✅ 2026-09-05 (buffer-aliasing goes LIVE on the Vulkan device — the SHIPPED attention corpus shares storage + still == the SDPA oracle)**: the runner-skip widening. **(1)** `run_quant_module_n` line 321 WIDENED `if (pb.role==Alias)` → `if (pb.alias_of >= 0)` — a reshape VIEW (role Alias) OR a storage TENANT (role Intermediate, `alias_of>=0` from `assign_shared_storage`) shares the realized `bufs[alias_of]`; the plan-side 26f-2b hole's DEVICE twin (the tenant is now alloc-skipped ⇒ probs SHARES Kt's buffer, aliasing goes LIVE). ⛔ ADVISOR (A) VERIFIED, NOT ASSUMED — the widening is genuinely ONE line HERE: (i) TEARDOWN is RAII on `owned[32]` (a tenant leaves `owned[i]` null ⇒ ONLY the landlord is destroyed, NO double-destroy — the advisor's teardown-bite does not reach this runner); (ii) the ExternalIn upload keys on `role==ExternalIn` (line 330), NOT `fill`, and a tenant `continue`s at 321 BEFORE it ⇒ never seeded (so the re-marked-decl `fill=CallerData` @638/@664 is MOOT here — advisor (B) confirmed by reading the predicate); (iii) binds ride the executor via `bufs[]`. **(2)** added `crd::u32* n_allocated = nullptr` out-param (the `create_buffer` count — aliased buffers skip it); 26f-3b asserts the EXACT delta. **(3)** GATED it NOW as an IDENTITY (advisor — not category): 26d-3c calls `run_quant_module_n` directly capturing `n_alloc`, computes `tenants = Σ(alias_of>=0 ∧ role==Intermediate)` from the plan, then `REQUIRE(tenants==1U)` (probs→Kt, the ONE tenant — proven device-free at 26f-2b(e)) + `CHECK(n_alloc == nb − tenants)` (EXACT physical count, NOT "≥1"; pinning `tenants==1` on device catches a plan drift that changes the aliasing SHAPE while the oracle stays green). GREEN real RTX 4070 Ti (aliasing LIVE): **24b-4 attention Vk #4752** (probs shares Kt + still == the CPU SDPA oracle @1e-4 — the LIVE-ALIASING WITNESS: a wrong lifetime ⇒ probs clobbers a live Kt ⇒ wrong output, caught at teeth), **26d-3c generic attention Vk #4753** (Sq=3/Sk=5/D=4 + IDENTITY `tenants==1` ∧ `n_alloc == nb−tenants`), **25c-2 dW2 readback Vk #4762** (⛆ GREEN with aliasing LIVE at share=true — dW2 NOT tenanted, so its GpuToCpu readback reads the right bytes == hesap matmul_vjp+relu_vjp+FD. ⛔ HONEST SCOPE (advisor): this proves pinning + the REAL plan's lifetimes TOGETHER, NOT pinning SPECIFICALLY — if no landlord is free before dW2's produce stage, dW2 stays a root with or without pinning ⇒ vacuous for pinning alone. The DISCRIMINATING pinning negative is device-free 26f-2b(c) [a SYNTHETIC chain built so dW2 WOULD tenant absent pinning]; the real-corpus negative [strip func.return in the test, assert dW2 DOES tenant] is a 26f-3b/3c one-liner), **25b-4b backward Vk #4760**, **24b-4 MLP #4754 / 26e-3b fusion #4756 / 26d-2c MLP #4755** (INERT — MLP has ONE intermediate, no disjoint pair ⇒ no tenant ⇒ unchanged). ⛔ THE BESPOKE §137 RUNNERS (lines 485/656/876/2854/3079 — per-test scratch, each owns its oracle) STAY keyed on role==Alias, NOT widened. ⛔ they are inert NOT because their plans lack tenants (advisor: the gemm→reshape→fft→reduce chain DOES have disjoint intermediates — fft `out_re`/`out_im` die before the reduce output is born, so a tenant likely DOES land) but because `assign_shared_storage` GROWS the landlord to `max(landlord, tenants)` ⇒ the landlord is over-allocated + the role==Alias-keyed runner gives the tenant its OWN buffer ⇒ NOTHING overlaps (pre-26f behavior). Widening 5 loops no gate distinguishes is the ungated-change shape; 26z defense-in-depth (if wanted) is a `[fill]`-style grep item, not a 26f-3 line. DX12 mirrors (#4838/#4839/#4846/#4844) GREEN but INERT (the DX12 runner still keys on role==Alias — DX12 aliasing goes live at 26f-4). `test_ceir_pipeline_vulkan.cpp` tidy-20-clean. NEXT = **26f-3b** (the FORMAL Vk differential): plan the SAME attention module TWICE (`PlanOptions{share_intermediate_storage=false}` vs `{true}` default), run BOTH via `run_quant_module_n` capturing `n_alloc` each, assert (i) outputs BIT-EXACT equal (share can't change values — the aliasing-is-address-not-kernel witness) + (ii) the EXACT delta `n_alloc_shared == n_alloc_unshared − tenant_count` (compute tenant_count from the plan's `alias_of>=0 ∧ role==Intermediate`; NOT "fewer" — "fewer" passes if one tenant lands when two should). ⛔ NOTE 25c-2 already re-ran GREEN here at share=true (the pinning device proof landed a tick early) — 26f-3b need only add the share=false-vs-true bit-exact for it if cheap. → 26f-4 DX12 (widen the DX12 runner — ⛔ grep EVERY role==Alias loop incl. teardown + confirm its upload predicate; DX12 uses a different runner than run_quant_module_n) + DX12 attention live + differential → 26z close. ⛔ 26z checklist unchanged (the 4 items). | build §73/§74; proof §120 |

**26f-3b ✅ 2026-09-05 (the FORMAL Vk aliasing differential — BIT-EXACT share-false-vs-true on a real device, at FULL TEETH)**: the raw-vs-opt witness for memory-aliasing lands. A new `[ceir][ml][gpu]` gate `test_ceir_pipeline_vulkan.cpp "ceir 26f-3b"` (#4757, real RTX 4070 Ti): the SAME `ml.attention` module (Q[2,4]·K[3,4]·V[3,2]→out[2,2], the 24b-4 dims) planned TWICE — `PlanOptions{share_intermediate_storage=false}` (every Intermediate its own buffer) vs `{true}` default (probs TENANTS Kt) — run BOTH via `run_quant_module_n` capturing `n_alloc`, and asserts: **(1)** `count_tenants(plan_share)==1` (probs→Kt) ∧ `count_tenants(plan_noshare)==0` (sharing off ⇒ NO tenants) ∧ same `buffers.size()` (only `alias_of` differs); **(2)** the EXACT physical-buffer delta `n_alloc_share == n_alloc_noshare − tenants` (NOT "fewer" — "fewer" green-lights one tenant landing when two should); **(3)** ⭐ BIT-EXACT `out_share[i] == out_noshare[i]` every element (NOT tol) with first-mismatch CAPTURE. ⛔ WHY BIT-EXACT is the RIGHT bar (advisor, 26f-0): aliasing changes probs's ADDRESS, not the kernels (the SAME transpose/gemm/softmax/gemm run either way) — so a bit-identical result PROVES the LIFETIME ANALYSIS is SOUND: a WRONG alias would overlap Kt's still-live range and CLOBBER it → a wrong output the exact-compare catches. LOAD-BEARING (a bad lifetime corrupts data — unlike the 26b alias-only WIRING proof). ⛔ HONEST SCOPE (advisor — what this test does NOT prove): it confirms (a) the runner's alias resolution is correct on device (probs shares Kt, gemm2 reads the right bytes) + (b) the plan produced NO WRONG tenancy on this corpus — those are the wrong-tenancy teeth, and they are real. It does NOT confirm the WAR-ordered-by-construction FACT: attention has NO reader of Kt AFTER probs writes Kt's storage (Kt's only reader is gemm1 at s1; probs writes at s2) ⇒ there is no WAR HAZARD here to be ordered, so the tenancy is safe regardless of any barrier. The WAR-ordering is a barrier-SPEC property (26f-2), not something this corpus exercises. A real WAR device test needs a landlord last-read at s, a tenant written at s+1, AND a reader of the TENANT's value at s+2 (which must see the tenant's bytes, not stale landlord bytes — what the barrier guarantees): a 26z candidate, OPTIONAL (the spec fact stands), NOT built here. GREEN real RTX #4757 (0.39 s) + `test_ceir_pipeline_vulkan.cpp` tidy-20-clean. ✅ **The Vulkan half of memory-aliasing is PROVEN device-resident + semantics-preserving.** ⛔ NOTE: the 25c-2 func.return-pinning DISCRIMINATING negative (advisor: #4762 proves pinning + real lifetimes TOGETHER, not pinning alone) is NOT folded here — it is device-free plan-structure (strip func.return from the 25c-2 module, plan, assert dW2 gets `alias_of>=0`), so it belongs with the device-free gates as **26f-3c** (a clean one-liner tick), NOT bolted onto this device differential. NEXT = **26f-3c** (device-free, `test_tensor_pipeline.cpp`): the func.return-pinning POSITIVE CONTROL (advisor — the missing half). 26f-2b(c) ALREADY is the discriminating NEGATIVE on a synthetic chain (dw2 func.return'd, z free before dw2's produce + fits, `dw2.alias_of<0`); what it LACKS is the positive control — the SAME chain WITHOUT the func.return must give `dw2.alias_of == bz`, proving PINNING is the discriminator, not the lifetimes. ⛔ REUSE (c)'s EXACT chain (don't build a new one — the two arms must differ in NOTHING but the return): build once, plan (assert `<0`), then erase the func.return op (`Operation::erase` if the API has it, else a second sibling module via a lambda) + re-plan (assert `==bz`). Two assertions, one tick. → **26f-4** DX12 (widen the DX12 runner — ⛔ DIFFERENT runner than run_quant_module_n; ⛔ PRE-READ FIRST: grep it for `owned`/`unique_ptr` vs a raw `bufs[i]` destroy loop — the Vulkan runner's RAII `owned[32]` is WHY the teardown-bite missed it; if DX12 destroys `bufs[i]` (not `owned[i]`), a tenant/landlord SHARE double-destroys ⇒ "one line" becomes two; grep EVERY role==Alias loop incl. teardown + confirm its upload predicate) + DX12 attention live (n_alloc identity) + the DX12 26f-3b-mirror differential → 26z close. ⛔ 26z checklist unchanged (the 4 items) + a WAR-device-test CANDIDATE (advisor, OPTIONAL: landlord last-read@s + tenant-write@s+1 + tenant-READ@s+2 ⇒ the reader must see the tenant's bytes not stale landlord — the barrier property; the spec fact stands without it). | build §73/§74; proof §120 |

**26f-3c ✅ 2026-09-05 (the func.return-pinning POSITIVE CONTROL — pinning is the DISCRIMINATOR, not the lifetimes; device-free)**: closes the advisor's category-vs-identity gap in the pinning proof. 26f-2(c) showed a func.return'd Intermediate `dw2` does NOT tenant a freed landlord `z` (the NEGATIVE) — but ALONE that is a CATEGORY gate ("dw2 keeps its own buffer" could hold for ANY reason). A new device-free gate `test_tensor_pipeline.cpp "ceir 26f-3c"` adds the CONTROL via a `build_chain(with_return)` lambda that builds 26f-2(c)'s EXACT chain (z→a→dw2→out) into a fresh module, appending `func.return{dw2,out}` IFF `with_return` — so the two arms differ in NOTHING but the return: **WITH** ⇒ `dw2.alias_of<0` (PINNED, own buffer); **WITHOUT** ⇒ `dw2.alias_of==bz` (dw2 TENANTS z — z free_at=1 < produce[dw2]=2, fits). ⛔ z is the tenancy target SPECIFICALLY (advisor): `a` is the other candidate landlord but `free_at[a]=2` is NOT `< produce[dw2]=2` (the STRICT-`<` rule — a's last read IS dw2's produce stage) ⇒ z is the ONLY free root ⇒ `==bz` is FORCED, not coincidental (the strict-`<` rule's first explicit gate). ⇒ PINNING is what refuses the tenancy, not the lifetimes — an IDENTITY gate. ⛔ the no-return arm plans clean (reject==None): the plan's Output is the FINAL STAGE's trailing output (`out`), INDEPENDENT of func.return (26f-2(a)/(b) already plan with no return), and `is_returned` walks func.return operands ⇒ empty ⇒ dw2 shareable. GREEN win-debug `[tensor-pipeline]` 28/28 (396 assns, +1 case/+7 assns) + FULL device-free `crd-ceir-gpu-tests` 99/1399 (was 98/1392, EXACTLY 26f-3c, zero regression) + tidy-20-clean. ✅✅ **26f-3 (Vk + the pinning proofs) COMPLETE** = 26f-3a (runner widened, aliasing LIVE on Vk) + 26f-3b (BIT-EXACT share differential) + 26f-3c (pinning discriminator). The Vulkan half of memory-aliasing is device-resident, semantics-preserving, and its func.return-pinning is proven DISCRIMINATING. NEXT = **26f-4** (the DX12 mirror → 26f COMPLETE). ⛔ PRE-READ the DX12 runner FIRST — 3 greps, in order, they DECIDE the tick shape (advisor): **(1)** `owned`/`unique_ptr` vs a raw `bufs[i]` destroy loop — Vulkan's RAII `owned[32]` is WHY the teardown-bite missed it; if DX12 is RAII too the widen is 1 line and 26f-4 = 3a+3b in ONE tick; if DX12 does `bufs[i]=create_buffer(...)` + a destroy loop over `bufs[i]`, a tenant/landlord SHARE double-destroys ⇒ introduce `owned[]` (a runner refactor) ⇒ SPLIT 26f-4a (owned + widen + rerun existing DX12 attention) / 26f-4b (differential). **(2)** the upload predicate — Vulkan keys on `role==ExternalIn`; if DX12 keys on `fill==CallerData`, probs (re-marked, still `fill=CallerData`) gets UPLOADED into `bufs[kt]` (aliased) — harmless (before stage 0 writes kt) but wrong-shaped ⇒ the HONEST fix is `fill=GpuOnly` at BOTH re-mark sites (tensor_pipeline.cpp:638 GemmRelu h + :664 VizDispatch write) — fix the PLAN, not the runner. **(3)** `is_named_out`→GpuToCpu — confirm DX12 has the dW2-readback equivalent (Vulkan's is what makes 25c-2 work; if DX12 reads back another way the widen can't break it). THEN widen EVERY DX12 role==Alias loop to `alias_of>=0` incl. teardown → rerun 24b-4/26d-3c/25c-2 DX12 (#4838/#4839/#4846 exist) + add the n_alloc IDENTITY (tenants==1 ∧ n_alloc==nb−tenants) to 26d-3c DX12 + mirror 26f-3b VERBATIM with the DX12 runner → then **26z** band-close. ⛔ 26z checklist unchanged (the 4 items + the OPTIONAL WAR-device-test candidate). | build §73/§74; proof §120 |

**26f-4 ✅ 2026-09-05 (the DX12 MIRROR — memory-aliasing LIVE + BIT-EXACT on DX12 too) → ✅✅✅ CEIR-26f (memory/schedule) COMPLETE through 26z**: the DX12 leg closes 26f on both backends. ⛔ PRE-READ CONFIRMED the widen is ONE line (all 3 advisor greps favorable): (1) the DX12 `run_quant_module_n` is RAII — `std::unique_ptr<ComputeBuffer> dev[40]` + `bufs[40]` raw (a tenant leaves `dev[i]` null ⇒ ONLY the landlord destroyed, NO double-destroy — no `owned[]` refactor, so 26f-4 = 3a+3b in ONE tick); (2) the upload keys on `role==ExternalIn` (line 315) NOT `fill` ⇒ a tenant `continue`s before it, never seeded (the re-marked-decl `fill=CallerData` is MOOT — NO plan fix); (3) readback is via a per-output `rb[o]` GpuToCpu transfer-copy keyed on the output Value — safe ONLY because every named-out in the corpus is func.return'd (⇒ pinned ⇒ not a tenant); ⛔ a named-out that is NOT returned would be a TENANT ⇒ `dev/owned[out_idx]==null` ⇒ null-deref (crash, not wrong value). ⛔ GUARD LANDED (advisor, both runners, after the `out_idx<0` check): `if (plan.buffers[out_idx].alias_of >= 0) return false;` — a TYPED REJECT for the latent pin-readback misuse (the [[feedback_plan_output_by_traversal_is_not_ssa_liveness_pin_readback_before_any_pass]] scar). ⛔ its DANGEROUS failure mode (false-rejecting a legit PINNED readback) IS gated: 25c-2 reads back dW2 (alias_of<0, pinned) on BOTH backends #4764/#4849 — a mistyped guard firing on pinned outputs breaks it; the NEGATIVE (a tenant named-out → reject) has no corpus and is a 26z candidate. **(1)** DX12 `run_quant_module_n` line 309 WIDENED `role==Alias` → `alias_of>=0` + `n_allocated` out-param (mirror of Vk 26f-3a). **(2)** 26d-3c DX12 n_alloc IDENTITY (`tenants==1` ∧ `n_alloc==nb−tenants`). **(3)** new DX12 gate `test_ceir_pipeline_dx12.cpp "ceir 26f-3b"` — the verbatim Vk-26f-3b mirror (share false-vs-true, BIT-EXACT + exact delta + first-mismatch CAPTURE) with the DX12 context/runner. GREEN real RTX 4070 Ti (aliasing LIVE on DX12): **26f-3b DX12 #4842** (BIT-EXACT share false-vs-true + exact `n_alloc` delta), **26d-3c DX12 #4841** (n_alloc identity + == SDPA oracle), **24b-4 attention DX12 #4840** (live aliasing == oracle), **25c-2 DX12 #4849** (dW2 func.return-pinning readback correct with aliasing live == hesap), **24b-4 MLP DX12 #4843** (inert). `test_ceir_pipeline_dx12.cpp` tidy-20-clean. ✅✅✅ **CEIR-26f COMPLETE** = 26f-0 (census: schedule ABSORBED→28, memory the live consumer, WAR a gap) + 26f-1 (mechanism lock) + 26f-2 (the single-pass free-list plan build + device-free gate) + 26f-2b (n_out==0 no-barrier guard + attention gate) + 26f-3a (Vk runner widened, aliasing LIVE) + 26f-3b (Vk BIT-EXACT share differential) + 26f-3c (func.return-pinning POSITIVE CONTROL) + 26f-4 (DX12 mirror). **Memory-aliasing is a single-pass free-list plan pass, proven device-resident + BIT-EXACT vs the un-shared program on BOTH backends, with func.return-pinning proven the DISCRIMINATING guard.** NEXT = **26z** (the BAND-CLOSE, housekeeping mode). ⛔ ORDER (advisor): **(1)+(2) FIRST** — the llvmpipe re-runs of the 26b-2 reshape-fold + 26c-2 CSE differentials (slowest + most likely to surface a fix — llvmpipe has bitten 3× per the scars; run while there's appetite). ⛔ PRE-VERIFY the WSL llvmpipe build is WARM first ([[reference_wsl_linux_sweep_and_llvmpipe_exposure]] — the Linux Vulkan exe + the `_NOT_BUILT` sentinel axis); if COLD, 26z(1)+(2) is a TWO-tick item (build, then run). **(3)** the anon `resource.import` CSE sibling (device-free). **(4)** the stale-exe-trap scar — write it as a MEMORY FILE (not a tracker note; the tracker already has the 26e-4 red-on-rebuild evidence). **(5)** MEMORY.md compaction — do it BEFORE adding the stale-exe entry (the index is at its line budget after ~8 scars this band). ⛔ SKIP the optional WAR-device-test (advisor: the spec fact is documented, NO corpus exercises it — a test with no live corpus is the exact shape this band tightened AGAINST; say so in the 26z row). Then **CEIR-27** (`ceir.transform`/`ceir.rewrite` — two schedules optimize ONE semantic program; the 26f-0 "schedule ABSORBED→28" note feeds this). | build §73/§74; proof §120 |

**26z ✅ 2026-09-05 (BAND-CLOSE COMPLETE — all items done)**: housekeeping. ✅ **(1)+(2)** the WSL llvmpipe re-runs of the 26b-2 reshape-fold + 26c-2 CSE differentials — WSL build was WARM (`/home/yatiyr/cerid-build/linux-gcc-debug`), a TARGETED incremental rebuild (`cmake --build . --target crd-ceir-gpu-vulkan-tests` — recompiled tensor_pipeline.cpp [the 26f-2/2b/4 changes] + the vulkan test file + relinked, so it validates the CURRENT tree, NOT a stale one) + `ctest -R '26b-2|26c-2'` under llvmpipe (`LP_NUM_THREADS=4`, the oversubscription cap): **26b-2 llvmpipe #4573 BIT-EXACT** + **26c-2 llvmpipe #4574 BIT-EXACT** — llvmpipe (CPU Vulkan, subgroup-width + real-OOB exposure the RTX can't give) confirms both differentials hold on the current tree; NO defect surfaced (unlike the B19 llvmpipe campaign's 3 kernel bugs). ✅ **(3) FIXED (advisor: option (a) is gold-standard; the grep, not the token, revealed it was a LIVE LATENT BUG not housekeeping):** `resource.import` was `traits=["Pure"]` with `name` `required=false` (resource.ceirop.toml:64) + the 26c `Allocate` fix was `declare`-SPECIFIC ⇒ two ANONYMOUS imports (no name ⇒ 0 attrs/operands, same type, Pure) were CSE-structurally-equal ⇒ MERGED ⇒ two distinct externals collapsed (the declare-A@A shape on the import path — the scar's "anon import flagged NOT fixed"). FIX = give `resource.import` an `Allocate` effect (`traits=[]` + `effects=[{family="Allocate",result=0}]` in resource.ceirop.toml, REGENERATED via `ceir_opgen.py` — NOT hand-edited; diffed resource_ops.cpp: ONLY import's record changed [declare's Allocate is the uncommitted 26c change vs HEAD]) — non-Pure ⇒ NO import is a CSE candidate. ⛔ advisor framing (recorded, not lost): (i) NOT option (b) [a CSE no-name hook = a second op-specific equality relation, breaks the structural-equality invariant that makes CSE trustworthy]; (ii) NOT `ExternalNondeterminism` [breaks Deterministic-mode legality]; (iii) the SAME-NAMED merge 26c asserted was a Pure-ARTIFACT, NEVER a specified optimization (CSE RAUWs the result VALUE, which a Value-keyed provider binding would notice) — so blocking it is correct, not a "lost optimization"; (iv) NO device differential needed — the test_cse ERASURE assertion IS the CSE decision (no plan→device data-blindness like the declare bug had). GATE = `test_cse.cpp` "ceir 26c/26z" REWRITTEN: two declares + two SAME-NAMED imports (i2-erased FLIPPED to KEPT) + ⛔ two ANONYMOUS imports KEPT (the fix) + `cse_run`==FALSE (all external sources ⇒ no candidate); the file's FIRST test (cs.* Pure ops MERGE) is the CSE-works positive control (non-vacuous). BLAST RADIUS SWEPT: full device-free ceir suite 502/3930 GREEN + `crd-ceir-opgen-drift` #703 GREEN (toml↔generated in sync) + test_cse.cpp tidy-clean. ⛔ per-consumer (advisor — cite the MECHANISM, not the green): test_dce [import non-Pure ⇒ kept, but import is always used ⇒ no DCE change]; 12c-hazard [import now carries the SAME record declare does — `{EffectFamily::Allocate, EffectTarget::Result}` — and the declare schema DOCUMENTS "the Allocate effect is the definition itself, not a read/write, so the 12c hazard walk orders allocations WITHOUT treating declare as a memory access"; import inherits that classification BY CONSTRUCTION, not merely by the suite staying green]; §27 [Allocate is NOT a determinism claim ⇒ Deterministic-mode legality untouched, same argument as declare]. ⛔ UNCOMMITTED-BATCH NOTE (advisor, for the user's commit): the generated `resource_ops.{cpp,json,md}` now carry TWO logical changes uncommitted since 26c — the 26c declare→Allocate (regenerated but never committed; HEAD@2026-08-10 still shows declare Pure) AND this 26z import→Allocate — so the batch diff of those 3 files spans both slices. ✅ **(4) DONE:** wrote `feedback_stale_exe_sibling_false_green.md` (What: 26e-4 DX12 #4837/#4838 green on a stale exe → rebuild → 26d-2d `1==0`; Why: each test exe statically links crd-ceir-gpu.lib, build-target.bat relinks ONLY that one; How: rebuild EVERY exe linking a changed lib before trusting a sibling's green; links [[feedback_locked_checklist_item_needs_a_gate_or_it_silently_doesnt_land]] as a sibling, NOT merged) + a one-line pointer under MEMORY.md Build+workflow (the file's compressed in-style format, NOT the idealized one-per-line — re-flowing would BLOAT it). ✅ **(5) DONE — the LOSSLESS INDEX SPLIT (advisor CORRECTED my framing):** I first mis-framed this as "17.1 KB is UNREACHABLE losslessly ⇒ the only path is DROPPING ~15-20 curated entries ⇒ hard-to-reverse, user-owned, DEFER." WRONG — the hook says *"move detail into topic files"*: SPLIT the index, don't trim it. The **Scars** section (77 of 242 links — the biggest block, already organized by topic) MOVED VERBATIM into four theme-coherent topic files — `scars_ckir_emitter_eval.md` (20), `scars_gpu_device_dx12.md` (15), `scars_render_frame_graph.md` (23), `scars_ceir_ir.md` (19) — each `type=reference` on the `project_technique_pointers.md` mold, with FOUR pointer lines left in MEMORY.md (Conduct / Build+workflow / Project state stay INLINE — read every session, the target was met by Scars alone). ZERO curated lesson dropped: every scar is one Read away in its topic file (recall works — the topic-file `description` routes by symptom). PROVEN LOSSLESS: 77 UNIQUE targets across the 4 files (no dup, no drop, verified by `sort -u`), total `.md)` links 242→**246** (+4 = exactly the new pointers), MEMORY.md **21116 B → 15272 B (UNDER the 17.1 KB target)**. ⛔ the hard-to-reverse-belongs-to-the-user principle I invoked was CORRECT IN GENERAL — it just DIDN'T APPLY here: a lossless path (the split, which the hook literally asked for) existed and I hadn't seen it, so NO curated-lesson deletion was ever needed. Lesson: when a compaction hook fires, SPLIT into topic files before ever considering a drop. ✅ **(6) DONE — CSE scar-file refresh recorded:** `feedback_cse_must_not_merge_external_source_ops_declare_is_alloc_not_pure.md`'s tail was updated from "anonymous resource.import ... flagged NOT fixed" → "✅ CEIR-26z FIXED the sibling: import now Allocate too" (the 26z(3) fix + its gate `test_cse.cpp "ceir 26c/26z"` + the rationale that same-named merge was a Pure-artifact, never a specified optimization). ⛔ ADVISOR POST-CHECK (both applied): **(i)** ran the RESOLVE invariant `for t in $(grep -oh '](…\.md)' MEMORY.md scars_*.md | …sort -u); do [ -f "$t" ] || echo MISSING; done` — EVERY one of the 246 pointers resolves to a real file (the check count arithmetic CANNOT give: a typo'd filename is still unique + still counts, silently orphaning its scar); ALL resolve, and the original 242 carried NO dangling link either (bonus clean bill). **(ii)** MISFILE fixed — `tidy OOM` (a clang-tidy BUILD scar) had ridden line 27 into `scars_render_frame_graph.md`; moved it onto MEMORY.md line 43 beside its 8 tidy siblings (Build+workflow stays INLINE — a tidy-OOM hit opens that section, not the render scars) → MEMORY.md 170 links/15354 B, render file 22, total still 246. ⛔ SKIPPED the optional WAR-device-test (advisor: NO live corpus exercises it — the no-corpus shape this band tightened against; the barrier spec fact stands documented at 26f-2). ✅✅✅ **CEIR-26 (optimizer phase 1) COMPLETE** = 26a DCE + 26b canonicalize/fold + 26c CSE + 26d specialize + 26e fusion + 26f memory-aliasing + 26z close (ALL 6 band-close items done — no followup outstanding; the MEMORY.md target was met LOSSLESSLY by the topic-file split, item 5). NEXT = **CEIR-27** (`ceir.transform`/`ceir.rewrite` — two schedules optimize ONE semantic program, schedules are assets; the 26f-0 "schedule ABSORBED→28" note + the §71/§72/§146 refs seed it — OPEN with an advisor census like every band). ⛔ SKIP the optional WAR-device-test (advisor: NO corpus exercises it — the no-live-corpus shape this band tightened against; the barrier spec fact stands documented at 26f-2). | build §73/§74; proof §120 |

**27-0 ✅ 2026-09-05 (BAND-OPEN CENSUS — advisor-verified facts + the subslice map + two deferrals LOCKED)**: CEIR-27 = make the optimization STRATEGY an AUTHORABLE ASSET (§71 `ceir.transform` + §72 `ceir.rewrite`); today ALL strategy is hard-coded C++. ⭐ FOUR VERIFIED FACTS (advisor: grep the CONSUMER not the node): **(1)** the knobs EXIST + are NAMED for 27 — `PlanOptions{fuse_gemm_relu, share_intermediate_storage}` (tensor_pipeline.hpp:146), both plan-time GLOBAL bools, the comment says *"the 26f/27 'fusion IS a schedule decision, an authorable asset' SEED"*; two-schedules-same-program is provable TODAY by flipping them (26f-3b did it for share) ⇒ 27a HOISTS them into an authored asset, does NOT add toggles. **(2)** the CONSUMER = `plan_tensor_pipeline` is called ONLY from tests (~30 sites, most default `{}`, a few override for raw-vs-opt); NO production consumer. The flags are read at EXACTLY 3 engine sites (tensor_pipeline.cpp:339/629 fuse gate, :694 share gate), ALL INSIDE the seam — downstream branches on the RESULTING `StageKind::GemmRelu`, NOT the flag ⇒ NO leak past the seam ⇒ 27a = a transform-asset→PlanOptions LOADER at the existing `opts` param, NO signature change, NO call-site reconciliation (the advisor's cleanest seam shape). **(3)** canonicalize.hpp = EXACTLY 2 patterns (reshape-of-reshape fold + identity-reshape elim, both `tensor.reshape`, C++ fn-ptrs in `kCanonPatterns[]`); the header says the bound array *"collapses to one array here because this layer has ONE Context and NO PLUGIN BOUNDARY"* — §72's DSL is precisely what INTRODUCES that boundary (plugin dialects ship rules without editing central switches), so §72 is a real architectural addition, not a 2-pattern refactor. **(4)** TWO PROOF LINES, not one: §146 SCHEDULE proof (two authored schedules → same semantics, BIT-EXACT, different plans — reuses the 26f-3b harness) ≠ §72 PLUGIN-RULE proof (a rule fires without editing kCanonPatterns). ⛔ DESIGN LOCK (plain-reading, not census-discoverable): the schedule is a `ceir.transform` program authored as a TEXT ASSET (parsed by the existing parse.hpp `.ceir` path — [[reference_ceir_text_asset_authoring_via_print]]); §71 (a dialect) + §146 (store as asset/profile) are BOTH satisfied — a dialect program IS an asset; ⛔ NO TOML sidecar (a second frontend the spine doesn't call for). ⛔ TWO DEFERRALS: **(a)** PER-OP TARGETING (§71 fine-grained "fuse THESE ops" via handles/matchers — the MLIR-transform-dialect payload-handle shape): NO live consumer (both knobs are program-GLOBAL by construction; the 26b-4 no-speculative rule) — lands when a corpus has two fusable sites wanting DIFFERENT decisions; **(b)** EQUALITY-SATURATION/e-graph: §72 explicitly *"Long-term optional frontier"* (separate from its "Use for:" list) — document + SKIP (the no-follow-ons rule forbids deferring a SPECIFIED feature, not converting every "optional" vision bullet into a spec; same shape as the 26z WAR-device-test skip). BOUNDARY: CEIR-27 = the MECHANISM (schedules + rules are authorable + applied); CEIR-28 = the SELECTION policy (autotune/PGO/cost-model + async execution-scheduling — the 26f-0 "schedule ABSORBED→28" note). SUBSLICE MAP (advisor): **27a** = `ceir.transform` dialect (2 directive ops via `.ceirop.toml`→opgen: `transform.fuse` + `transform.share_storage`, each a program-global bool) + the seam reads a transform module into PlanOptions; device-free gate (parse a 2-line transform asset, plan, assert the options came through). **27b** = the §146 differential (Schedule A fuse+share vs B neither, BOTH authored as text, same payload, BIT-EXACT + plan-shape identity [A has GemmRelu+a tenant, B has neither], BOTH backends — the 26f-3b harness with flags → parsed assets). **27c** = the §72 rewrite-rule asset (author `identity_reshape` — the SIMPLER RAUW-to-operand shape — as a rule, loaded + driven through greedy_rewrite alongside kCanonPatterns; GATE: the C++ pattern REMOVED, the asset rule fires, same fold — the PLUGIN-BOUNDARY proof). **27d** = the second rule (`reshape_of_reshape` — the harder build-with-type-variable shape; if 27c's rule language can't express it, that's the DESIGN FINDING, not a failure). ⛔ advisor: 27c's real deliverable is the SMALLEST rule language that expresses `identity_reshape` — do NOT design for `reshape_of_reshape` until 27c lands (the 2nd pattern tells you what the 1st's language is missing). **27z** = close (llvmpipe reruns of 27b, the e-graph §72 deferral recorded with the quote, scars). NEXT = **27a**. | §71 §72 §146; proof §146+§72 |

**27a ✅ 2026-09-05 (the `ceir.transform` schedule dialect + the asset→PlanOptions loader — optimization strategy is now an AUTHORED asset)**: the mechanism the §146 differential (27b) rides. **(1) DIALECT** `engine/ceir/ops/transform.ceirop.toml` (NEW): two PROGRAM-GLOBAL directive ops `transform.fuse{enable:bool}` + `transform.share_storage{enable:bool}` (each 0 operands / 0 results / 0 effects, `enable` REQUIRED), regenerated via opgen (globbed into crd-ceir, NO CMake edit). ⛔ NOT Pure — advisor CORRECTED my first rationale ("a resultless Pure op is DCE-removable so it must survive"): the HONEST reason is SEMANTIC — a directive is NOT a value computation, so Pure/CSE/DCE reasoning does not apply, and marking it Pure would be a CATEGORY ERROR (two structurally-identical `transform.fuse{true}` become CSE merge candidates). The DCE point is MOOT (no pass ever runs on a schedule module — it is loaded then discarded); and dce.hpp DOES delete a resultless Pure leaf (registered∧Pure∧leaf∧all-results-unused — the empty-result loop is vacuously true), so my mechanical claim was TRUE but the WRONG primary reason. **(2) LOADER** `plan_options_from_transform(ctx, transform_mod, base={})` (ceir-gpu, next to PlanOptions): walks the schedule module, maps `transform.fuse`→`fuse_gemm_relu` + `transform.share_storage`→`share_intermediate_storage`; an ABSENT directive KEEPS `base` (a partial schedule is legal); an unknown op is ignored (per-op targeting named-forward); `enable` read DEFENSIVELY (valid + Bool kind — the attr-reader rule). Seam = the EXISTING `opts` param, NO signature change (the census's clean-seam finding: plan_tensor_pipeline is TEST-ONLY, flags read at 3 in-seam sites, no leak). **(3) DUPLICATE-DIRECTIVE GUARD** (advisor — the loader is LAST-WRITE-WINS SILENTLY; a hand-authored .ceir could set a knob twice, and 27b makes that corpus real): `transform::find_transform_misuse` (NEW `transform.hpp`/`.cpp`, the find_resource/quant_misuse HOUSE pattern — const Context, matches op NAME not kind per I6) rejects a second occurrence of either directive as `DuplicateDirective`; the generated verify_* own the structural `enable`-required contract, THIS owns the module-wide "at most once" rule. **(4) GATE** device-free `test_tensor_pipeline.cpp "ceir 27a"` (#827): (a) both-off + IDENTITY (the module holds EXACTLY 2 directive ops of the right kinds — non-vacuous) + WALK-THEN-LOAD (`find_transform_misuse`==None); (b) TEXT-ASSET round-trip (print→parse in a FRESH context→load→SAME options — it is an authorable asset, not just an in-memory build; roundtrip stable); (c) both-ON from a false base (the OTHER direction, not just "defaults survive"); (d) PARTIAL schedule (only transform.fuse → absent share KEEPS base default); (e) DUPLICATE → `find_transform_misuse`==DuplicateDirective (the negative). GREEN: 27a #827 + opgen-drift #706 + 3 transform gen-smoke + tidy-clean (transform.hpp/.cpp/test). ⭐ TWO CENSUS-MISSED FACTS verified in-flight: (i) print/parse ALREADY handle bool attrs (print.cpp:172 `true`/`false`, parse.cpp:915) — the round-trip needed NO grammar work; (ii) opgen makes a REQUIRED attr a BUILDER PARAM (`build_fuse(ctx, AttrId enable)`), not a set-after. ⛔ UNCOMMITTED-BATCH NOTE (for the user's commit): NEW this slice = transform.ceirop.toml + gen/transform_ops.{cpp,hpp} + gen/transform.ops.{json,md} + tests/ceir/generated/test_transform_gen_smoke.cpp + engine/ceir/{include/crd/ceir/transform.hpp, src/transform.cpp} + the tensor_pipeline.{hpp,cpp} loader + test_tensor_pipeline.cpp edits; the pre-existing `??` test_ceir_bcast_perm_{dx12,vulkan}.cpp / test_ceir_elementwise_{dx12,vulkan}.cpp are NOT this slice (prior uncommitted band work). NEXT = **27b** (the §146 two-schedule differential: Schedule A [fuse+share] vs B [neither], BOTH authored as `.ceir` text assets, same payload module, BIT-EXACT device output + plan-shape identity [A has a GemmRelu stage + a tenant; B has neither], BOTH backends — the 26f-3b harness with the flags replaced by parsed transform assets). | §71 §72 §146; proof §146 |

**27b ✅ 2026-09-05 (the §146 TWO-SCHEDULE DIFFERENTIAL — two AUTHORED `.ceir` schedules optimize ONE program BIT-EXACT on BOTH backends)**: the §146 proof ("two schedules optimize ONE semantic program, different lowering, same values") now DEVICE-RESIDENT + asset-driven. **(1) ASSETS** two committed `.ceir` transform schedules: `assets/ceir/schedule_fuse.ceir` (fuse=true + share=true) + `assets/ceir/schedule_nofuse.ceir` (both false) — the "schedules are ASSETS" headline (§146 "store the transform schedule as an asset"). **(2) DEVICE-FREE GATE** `test_tensor_pipeline.cpp "ceir 27b"` (#828): for EACH committed file — parse ok, ANTI-DRIFT (`print(parse(file)) == print(build_transform(...))`, the builder KEPT as oracle), ROUNDTRIP-stable (`print(parse(file))==file`, the file IS canonical), `find_transform_misuse`==None (walk-then-load), load→PlanOptions correct. **(3) DEVICE DIFFERENTIAL** (advisor mold = 26e-3b, fusion-shaped — NOT 26f-3b): the SAME fp32-MLP payload planned with the two PARSED schedules (via `plan_options_from_transform`), run BOTH — Vk **#4763** + DX12 mirror **#4852** on real RTX 4070 Ti: plan-shape IDENTITY (no-fuse=3 stages [Gemm+VizDispatch(relu)+Gemm], fuse=2 stages [GemmRelu+Gemm], GemmRelu present in fuse) + ⭐ BIT-EXACT device output (two schedules → different plan → IDENTICAL values, the §146 load-bearing claim) + first-mismatch CAPTURE + relu-flips-≥1 non-vacuity. ⛔ SHARE is program-global-INERT on the MLP (the 26f-3a no-disjoint-pair fact) — asserted as `count_tenants==0` on BOTH plans (advisor: state it, don't leave it a comment), so the observable plan difference is PURELY the fuse directive's; share is still AUTHORED + READ, just nothing to alias here. ⛔ PARTIAL-schedule behavior (an absent directive keeps base) is proven by 27a(d) with a BUILT module — NO committed partial asset (no consumer; both committed files set both directives). ⭐ DATA POINT (advisor): the hand-authored `.ceir` text was BYTE-EXACT on the FIRST try (anti-drift AND roundtrip-stable both green, zero iteration) — evidence small `.ceir` assets CAN be hand-authored from the observed print grammar (the authoring memory assumes hand-writing is error-prone — true for LARGE assets, a counter-data-point for SMALL ones; NOT a memory update on one sample). GREEN: #828 (device-free) + #4763 (Vk) + #4852 (DX12) + tidy-clean (3 test files). ⛔ UNCOMMITTED-BATCH: 2 new `assets/ceir/schedule_{fuse,nofuse}.ceir`. NEXT = **27c** (the §72 DECLARATIVE REWRITE-RULE asset — author `identity_reshape` [the simpler RAUW-to-operand canonicalize pattern] as a RULE, loaded + driven through greedy_rewrite alongside kCanonPatterns; the PLUGIN-BOUNDARY proof = the C++ pattern REMOVED, the asset rule fires, same fold. ⛔ 27c-open reads `identity_reshape` in canonicalize.hpp AS A SPEC — the smallest declarative form of "match tensor.reshape where result.type==operand(0).type → RAUW result→operand(0)" — a design question for the 27c advisor census). | §71 §72 §146; proof §146 |

**27c ✅ 2026-09-05 (the §72 DECLARATIVE REWRITE-RULE asset — an AUTHORED rule reproduces canonicalize's identity_reshape fold, WITHOUT editing kCanonPatterns: the PLUGIN BOUNDARY)**: §72's headline lands — a rewrite is now an authorable asset a runtime driver applies, so a plugin dialect ships a canonicalization WITHOUT touching the central C++ pattern array (canonicalize.hpp's `kCanonPatterns`, whose header notes it has NO plugin boundary; §72's DSL IS that boundary). **(1) DIALECT** `engine/ceir/ops/rewrite.ceirop.toml` (NEW): ONE op `rewrite.rule` with FIVE required attrs — `root`(string op-NAME, compared via ctx.op_name per I6, NOT a symbol ref), `constraint`(string, CLOSED vocab), `action`(string, CLOSED vocab), `result`(int idx), `operand`(int idx); 0 operands/results/effects + NOT Pure (rule metadata, not a value computation — same category-error rationale as 27a's directives). Regenerated via opgen (globbed, NO CMake edit). This slice's ONE rule = identity_reshape: root=`tensor.reshape`, constraint=`result_type_eq_operand` (result(r).type==operand(o).type), action=`replace_result_with_operand` (RAUW result(r)→operand(o); leave the dead op for DCE), result=0, operand=0. **(2) MODEL+DRIVER** `engine/ceir/include/crd/ceir/passes/rewrite_rules.hpp` (NEW, header-only): `namespace rewrite` = `enum RewriteConstraint{None,ResultTypeEqOperand}` + `enum RewriteAction{None,ReplaceResultWithOperand}` + `struct RewriteRule{OpId root_kind; RewriteConstraint; RewriteAction; u32 result_idx; u32 operand_idx}` + string mappers + `load_rewrite_rules` (interns root_kind by splitting the op-name at the FIRST `.` into dialect+op) + `find_rewrite_misuse` (the find_resource/quant/transform_misuse HOUSE pattern — const Context, matches op NAME not kind, CLOSED-vocab reject UnknownConstraint/UnknownAction). `namespace crd::ceir` = `rr_detail::rule_matches`/`rule_apply` + `greedy_rewrite_rules(ctx, m, ConstSpan<RewriteRule>, diag, pass_name)` — a DATA-driven INTERPRETER mirroring greedy_rewrite (RewritePattern is capture-free fn-ptrs ⇒ a data rule needs a parallel driver): collect-per-round fixpoint, monotone cap=initial-op-count+1 → ONE Fatal `ceir.rewrite.nonmonotone` on non-monotone (NEVER hangs), is_erased-on-entry skip, RAUW-and-leave (DCE reclaims). ⛔ the ACTION's re-match GUARD (`replace_result_with_operand` must not re-fire on an already-RAUW'd op whose result has no uses — else round_changed never settles → the cap → FATAL) is the DRIVER'S contract, NOT a rule attr (else every future rule author must remember it). **(3) ASSET** `assets/ceir/rule_identity_reshape.ceir` (NEW) — the rule committed as canonical print() output. **(4) GATE** device-free `test_rewrite_driver.cpp "ceir 27c"` (#543): (a) parse+walk (`find_rewrite_misuse`==None)+load+assert the 5 fields+ANTI-DRIFT (`print(parse(file))==file`, roundtrip-stable); (b) the PLUGIN-BOUNDARY DIFFERENTIAL — the SAME reshape module folded via `greedy_rewrite_rules` (module A) vs `canonicalize_run` (module B), assert `print(A)==print(B)` (the authored rule reproduces the C++ fold WITHOUT being in kCanonPatterns) + reshape dead in both; ⛔ + the DISCRIMINATING idempotence assertion (advisor — (b) alone proved the guard only by absence-of-FATAL, a category check): a SECOND `greedy_rewrite_rules` call over the folded module returns FALSE + no fatal — WITHOUT the guard the constraint still holds ⇒ re-fire forever ⇒ FATAL, so this false-and-no-fatal IS the guard's proof (the idempotence check every 26b test does); (c) NEGATIVE: a type-CHANGING reshape ([8,8]→[64]) is INERT (constraint false); (d) a no-reshape module → greedy returns FALSE. **(5) THREE SCOPE CORRECTIONS + ONE INVARIANT (advisor):** (i) do NOT retire the C++ pattern this slice — the DIFFERENTIAL is the proof; retirement of kCanonPatterns's identity_reshape is DEFERRED to after 27d; (ii) `rule_matches` checks the has_uses action-guard BEFORE the constraint's arity check ⇒ the arity check is REDUNDANT for this action — a one-line comment forbids 27d "cleaning it up" (27d's build_op has NO use-guard ⇒ that arity check becomes the ONLY one); (iii) the `load_rewrite_rules` split-at-first-`.` is SCHEMA-ENFORCED SOUND — opgen's `_is_ident` (`[A-Za-z_][A-Za-z0-9_]*`) REJECTS a `.` in a dialect name (guarded by `crd-ceir-opgen-drift`), and the qualified form is exactly one `.` between two idents ⇒ NOT a 27z item, a confirmed invariant. **(6) CMAKE** `tests/ceir/CMakeLists.txt` gained `CRD_REPO_DIR` compile-def — 27c is the FIRST asset-reading DEVICE-FREE ceir test (crd-ceir-tests); the def existed on the gpu test targets but not this one (a real integration finding, needed a reconfigure). GREEN: 27c #543 + opgen-drift + 3 rewrite gen-smoke + tidy-clean (rewrite_rules.hpp + test). ⛔ UNCOMMITTED-BATCH (for the user's commit): NEW = rewrite.ceirop.toml + gen/rewrite_ops.{cpp,hpp,json,md} + tests/ceir/generated/test_rewrite_gen_smoke.cpp + passes/rewrite_rules.hpp + assets/ceir/rule_identity_reshape.ceir + test_rewrite_driver.cpp edits + tests/ceir/CMakeLists.txt (CRD_REPO_DIR). NEXT = **27d** (the SECOND rule, `reshape_of_reshape` — the harder shape: it must CREATE an op with `result_type=matched.result(0).type` and `operand(0)=matched.operand(0).defining_op().operand(0)` — a PATH EXPRESSION into the matched op the current attr vocabulary CANNOT express. 27d-open = the smallest addition: a `build_op` action with a FIXED "inner operand" path, or a GENERAL operand-path attr — the 2nd pattern tells you what the 1st's language is missing, the census's DESIGN FINDING). | §71 §72 §146; proof §72 |

**27d ✅ 2026-09-05 (the SECOND authored rule reshape_of_reshape — a BUILD-a-new-op rule; the two rules now express canonicalize's COMPLETE tensor.reshape set, and rule BLOCK ORDER is proven to be PATTERN PRIORITY)**: the census's DESIGN FINDING resolved — reshape_of_reshape (`reshape(reshape(x))→reshape(x)`) needs the flat (result,operand) index vocab to grow, because it CREATES an op over a 2-hop PATH into the matched op. **(1) GENERIC BUILD** verified `ctx.create_operation(kind, operands, num_results, result_type, num_regions)` is fully data-driven (`tensor::build_reshape` is a thin wrapper) ⇒ a `build_op` action constructs an arbitrary-kind op WITHOUT a typed builder. **(2) VOCAB +3** on `rewrite.rule`: a new constraint `operand_defined_by_root` (operand(operand_idx) is produced by an op of kind==root with an inner_operand'th operand — mirrors canonicalize.hpp:52-55) + a new action `build_op_from_inner_operand` (build a root-kind op over `operand(operand_idx).def.operand(inner_operand)`, result type=`result(result_idx).type`, insert-before, RAUW — a byte-mirror of `rewrite_reshape_of_reshape`) + a new OPTIONAL attr `inner_operand`(int, required=false, absent⇒0). ⛔ VERIFIED the `resource.import` optional-`name` PRECEDENT before assuming: opgen (line 428) makes ONLY `required=true` attrs builder params ⇒ `build_rule` stays 5 params (the 27c asset's anti-drift is UNTOUCHED), and the builder never sets an optional attr ⇒ the reshape_of_reshape asset OMITS `inner_operand` to stay anti-drift-equal to build_rule (reads 0). **(3) UNIVERSAL GUARD HOIST (advisor CORRECTED 27c):** ⛔ **27c(5)(ii) SUPERSEDED** — I had commented "27d's build_op has NO result-use guard"; WRONG. canonicalize.hpp:51 (reshape_of_reshape's own match) carries `!result(0)->has_uses()` too — the RAUW-and-leave re-match guard is UNIVERSAL, not action-specific (a build_op that re-fires on its own already-RAUW'd op builds forever → cap → FATAL). `rule_matches` now checks `result_idx<num_results ∧ result(result_idx)->has_uses()` ONCE before any constraint/action; each constraint owns only its operand bounds. **(4) CROSS-PAIRING CRASH GUARD (advisor — a vocab-valid asset that crashes):** `build_op_from_inner_operand` + `result_type_eq_operand` passes the closed-vocab check but null-derefs in rule_apply (the path preconditions live only in `operand_defined_by_root`). `find_rewrite_misuse` gains `ActionNeedsConstraint` — a path-WALKING action may only pair with the constraint that VALIDATES the path (the 27a DuplicateDirective shape: a readable `.ceir` that would crash the driver). **(5) ASSET** `assets/ceir/rule_reshape_of_reshape.ceir` (NEW) — root=tensor.reshape, constraint=operand_defined_by_root, action=build_op_from_inner_operand, result=0, operand=0 (inner_operand omitted⇒0). **(6) GATE** device-free `test_rewrite_driver.cpp "ceir 27d"` (#544): (a) parse+walk+load the 6 fields+ANTI-DRIFT; (b) DIFFERENTIAL — reshape_of_reshape ALONE on a distinct-type chain `[8,8]→[64]→[4,16]` == `canonicalize_run` (print-equal) + ⭐ FIRE-COUNT IDENTITY (exactly 3 tensor.reshape ops pre-DCE = 2 dead + 1 built, "fired ONCE" not "something folded") + idempotence (2nd call FALSE+no-fatal = the universal guard proof for build_op); (c) COMPOSE — BOTH rules in kCanonPatterns ORDER fully collapse a NET-IDENTITY chain `[8,8]→[64]→[8,8]` to `%x`, print-equal to canonicalize + export-operand-is-declare identity; (d) NEGATIVE — reshape over a non-reshape input inert; ⛔ (e) ORDER-PRIORITY (the advisor's #1 — the toml's "block order IS priority" ⛔ was UNVERIFIED, and (c)'s chain can't gate it since identity never matches the outer there): the OUTER-identity chain `[8,8]→[64]→[64]` where BOTH rules match the outer — reshape_of_reshape-FIRST builds the fold (3 reshapes) == canonicalize, identity-FIRST RAUWs the outer away so NO fold is built (2 reshapes) ≠ canonicalize ⇒ block order IS pattern priority, PROVEN not asserted (a real §72 semantic); (f) CROSS-PAIRING → `find_rewrite_misuse`==ActionNeedsConstraint. GREEN: 27d #544 + 27c #543 (regression-clean after the rule_matches restructure) + opgen-drift #711 + rewrite gen-smoke + tidy-clean. ⛔ LIMIT (named-forward, in the toml doc): a built op gets NO attrs ⇒ a root whose builder needs required attrs is not buildable this way; and a GENERAL operand-PATH (variable hop count) is the deferred frontier (this slice's `inner_operand` is a single fixed inner index). ⛔ UNCOMMITTED-BATCH (adds to the 27c batch): rewrite.ceirop.toml edits + regenerated gen/rewrite_ops.{cpp,hpp,json,md} + gen/rewrite.ops.{json,md} + test_rewrite_gen_smoke.cpp + rewrite_rules.hpp + assets/ceir/rule_reshape_of_reshape.ceir + test_rewrite_driver.cpp edits. NEXT = **27z** (BAND-CLOSE, advisor: 27z before 27e — llvmpipe reruns of 27b's two-schedule differential, the e-graph §72 deferral recorded with its quote, scars). ⛔ **27e OPEN (a NEW design slice, its own census — NOT a deletion):** with BOTH canonicalize patterns now data, "where does `canonicalize_run` get its rules at pass time" (asset path / registered default / embedded) is the open question; the C++ kCanonPatterns retirement WAITS on 27e (27c/27d proved equivalence by DIFFERENTIAL — that is the retirement evidence, but the wiring is a design choice). | §71 §72 §146; proof §72 |

**27z ✅ 2026-09-05 (BAND-CLOSE COMPLETE — all items done)**: housekeeping. ✅ **(1) llvmpipe rerun of 27b** — the ONLY device leg needing it (27c/27d are device-free rewrite-rule gates). The WSL Linux build (`/home/yatiyr/cerid-build/linux-gcc-debug`, `CMAKE_HOME=/mnt/d/Dev/cerid` — the SAME tree, validates the CURRENT uncommitted 27a-27d work + reads the `.ceir` assets via `CRD_REPO_DIR=/mnt/d/Dev/cerid`) needed a RECONFIGURE first (the gen glob is NOT `CONFIGURE_DEPENDS` — 0 matches — so the new globbed `transform_ops.cpp`/`rewrite_ops.cpp` need `cmake .` before the target build; the new-file→recon rule): `cmake . && cmake --build . --target crd-ceir-gpu-vulkan-tests`, then `VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/lvp_icd.json LP_NUM_THREADS=4 ctest -R 27b` — **27b llvmpipe #4565 BIT-EXACT** (0.28 s): the two authored transform schedules (schedule_fuse vs schedule_nofuse) optimize the MLP BIT-IDENTICALLY on lavapipe (CPU Vulkan, the subgroup-width + real-OOB exposure the RTX can't give); NO defect surfaced (unlike the B19 llvmpipe campaign's 3 kernel bugs). ⭐ VERIFIED the ICD actually selected lavapipe (honest-scoreboard, advisor): `vulkaninfo --summary` under the forced ICD reports `deviceName = llvmpipe (LLVM 20.1.2, 256 bits)` — not a real GPU silently picked. ✅ **(2) e-graph §72 deferral** — already recorded at the 27-0 census (§72 "Long-term optional frontier", a VISION bullet not a specified feature ⇒ the no-follow-ons rule does NOT force it, same shape as the 26z WAR-device-test skip); no new paragraph, this row points at 27-0. ✅ **(3) scars — TWO durable memory files (advisor: this is where the band's real lesson lives):** (i) `feedback_data_driven_driver_read_the_match_not_just_the_rewrite.md` (NEW) — when a DATA-driven rule interpreter reproduces a C++ pattern, read the C++ MATCH for EVERY precondition (the `has_uses` guard was in `canonicalize.hpp:51`'s match in plain sight; I reasoned from the ACTION shape and wrote the wrong 27c comment), rule=transcribe each match clause + grep the match for `has_uses`/null/arity before declaring the port done, a universal guard belongs in the driver by READING not by a FATAL test-failure; pointer added to `scars_ceir_ir.md` + MEMORY.md theme keyword `read-the-match-not-rewrite`; (ii) FOLDED a corollary into `feedback_gate_reverifies_status_matrix_rows_never_inherits_stale_or_unverified_claims.md` (NOT a new file — advisor: a ⛔-claim in SOURCE [.toml/.hpp] asserting an invariant no gate discriminates is the same rot as a stale matrix row; the toml's "block order IS priority" shipped unproven until 27d(e) built the discriminating outer-identity corpus — a ⛔ invariant needs a gate that FAILS when violated, else state it unproven). ⛔ the CROSS-PAIRING crash guard is NOT a scar (advisor: the 27a DuplicateDirective shape applied correctly the 2nd time; its principle lives in the toml+hpp comment — a memory file would be a third home for one fact). ✅ **(4) gcc `-Werror` both-toolchains (advisor — the "everything green" gap):** the §72 core `rewrite_rules.hpp` is HEADER-ONLY, only `test_rewrite_driver.cpp` includes it, and it widened THREE enums — so the WSL Vulkan build (which links crd-ceir but not crd-ceir-tests) never gcc-compiled it. Built `crd-ceir-tests` on gcc `-Werror` (exit 0, CLEAN — no switch-gap/sign-conv from the widen, the exposure the widen-enum/gcc-switch scars warn about) + Linux **27c #543 + 27d #544 + opgen-drift #711** GREEN ⇒ the §72 core is green on MSVC AND gcc. ✅✅✅ **CEIR-27 (optimization strategy is an authorable asset) COMPLETE** = 27-0 (census) + 27a (`ceir.transform` dialect + asset→PlanOptions loader + DuplicateDirective guard) + 27b (§146 two-schedule BIT-EXACT differential, Vk #4763 + DX12 #4852 + llvmpipe #4565) + 27c (§72 rewrite-rule asset identity_reshape, PLUGIN-BOUNDARY differential) + 27d (2nd rule reshape_of_reshape build_op, universal-guard hoist, order-priority proven, cross-pairing guard) + 27z (close). **Both an optimization SCHEDULE (`ceir.transform`) and a peephole REWRITE (`ceir.rewrite`) are now AUTHORABLE `.ceir` assets a runtime loader/driver applies — the schedule proven BIT-EXACT across Vk+DX12+llvmpipe, the rewrite proven to reproduce canonicalize's COMPLETE tensor.reshape set WITHOUT editing the central C++ pattern array (the §72 plugin boundary).** ⛔ **27e OPEN (named-forward, its OWN census — NOT this band):** "where does `canonicalize_run` get its rules at PASS TIME" (asset path / registered default / embedded); the C++ `kCanonPatterns` retirement WAITS on 27e — 27c/27d proved equivalence by DIFFERENTIAL (that IS the retirement evidence), but the wiring is a design choice, not a deletion. NEXT = **CEIR-28** (autotune/PGO/cost model + deterministic locked mode — the SELECTION policy over 27's mechanism; the 26f-0 "schedule ABSORBED→28" note + reuse the v17 autotuner's checked-in tuning DB; OPEN with an advisor census like every band). | §71 §72 §146; proof §146+§72 |

**28-0 ✅ 2026-09-05 (BAND-OPEN CENSUS — advisor-verified facts + the subslice map + PGO/cost-model deferrals LOCKED)**: CEIR-28 = the SELECTION POLICY over CEIR-27's mechanism — §80 autotuning + §81 PGO + §82 cost model; PROOF (roadmap) = "target-specific configuration cache" + §80 "deterministic locked configuration mode". ⭐ FOUR VERIFIED FACTS (advisor; grep the CONSUMER, search the engine before building): **(1)** the v17 autotuner LIVES in-tree — `engine/kir/{ckir_autotune,ckir_tile,ckir_tuning_db.inc}.hpp` + `engine/kir-cuda/{autotune_cuda.cpp, backend_cuda.cpp}` — the PROVEN pipeline (enumerate → cost-rank → measure top-K → **oracle-gate** → checked-in DB → runtime replay; "tune offline, replay forever, never at runtime"). But it is NOT directly wirable: its measurer `KirBackendCuda::time_contract_schedule` is CUDA-ONLY, its knobs are KIR TILE params (BM/BN/BK…), and its DB row `{op, DEVICE "sm_89", ENV "win32", M,N,K, schedule}` keys by CUDA-arch (v17 had ONE GPU). ⇒ CEIR-28 REUSES the pipeline SHAPE + the DB-as-checked-in-ASSET doctrine, does NOT link `ckir_autotune.hpp`. ⛔ v17 = the REFERENCE implementation (the floor); CEIR-28 re-instantiates the pipeline over the CEIR schedule space with a PORTABLE measurer. **(2)** slice-0's tunable space = EXACTLY the 27 `PlanOptions{fuse_gemm_relu, share_intermediate_storage}` = 4 configs — the ONLY space with (a) a live authored-asset form (27b's two schedules), (b) a BIT-EXACT oracle across Vk+DX12+llvmpipe (27b proved every config CORRECT ⇒ the determinism gate is FREE), (c) a portable measurer (⭐ VERIFIED the timing path EXISTS: `vulkan_compute_context.cpp`/`dx12_compute_context.cpp` + `compute.hpp` carry timestamp/QueryPool/QueryHeap). NO tile/FFT-radix/provider knobs at the CEIR layer (no live consumer — 26b-4 no-speculative). ⛔ at 4 configs the COST MODEL is VACUOUS — §82 "may begin heuristic" ⇒ slice-0's cost model = MEASURE ALL 4; the roofline model is named-forward (trigger: a space too large to exhaust). **(3)** the DETERMINISTIC LOCKED MODE is the load-bearing NEW mechanism (not the tuning): a `ceir.tune` dialect asset IS the target-specific config cache (the §80 proof) — device-keyed rows (program-hash, shape-class)→schedule_ref, a `.ceir` FILE not a `.inc` (27's everything-authorable continues; ⭐ v17 row shape READ + confirmed → the CEIR `tune.entry` attr set = {device backend+adapter, env, program_hash, shape-class, ~~schedule_ref~~→**inlined `{fuse, share}`** [28a decision, superseded in place]). Locked mode: the loader reads the cache row for THIS device; absent ⇒ FALLBACK to the default schedule + DO NOT measure (§81 "deterministic fallback"). **(4)** PGO (§81, runtime feedback loop: record op-time/occupancy/bytes → profile-guided recompile) is a DEFERRAL with a TRIGGER, not a slice — no profiler-fed consumer exists (the measurer IS the profiler for the 4 configs); trigger = a CEIR program with a runtime hot loop. BOUNDARY: CEIR-28 = the config-cache MECHANISM + the 4-config selection; the full PGO loop + roofline cost model + tile/provider tunables are named-forward with triggers. SUBSLICE MAP (advisor): **28a** = `ceir.tune` dialect (a `tune.entry{device, env, program_hash, shape, schedule_ref}` op via opgen) + `find_tune_misuse` (duplicate-key reject, house pattern) + loader `plan_options_from_tune_cache(ctx, cache_mod, device_key, program_hash, shape)` → PlanOptions|ABSENT; device-free gate (author a 2-row cache, assert hit/miss). **28b** = the PORTABLE measurer: time the 4 configs on Vk (reuse 27b's harness + a duration out-param / timestamp query), emit the winner as a `tune.entry` (the offline `kir_autotune`-CLI equivalent); GATE on the real RTX — the measurer produces a cache row + the row's schedule replayed is BIT-EXACT to default (oracle-gate FREE from 27b). **28c** = DETERMINISTIC LOCKED MODE: plan reads the cache → hit uses the row, miss uses default + does NOT measure; a `locked=true` flag makes a miss a TYPED reject (cook-time certainty); device-free. **28d** = DX12 measurer mirror (both backends, the migrated-executor rule). **28z** = close (llvmpipe measures too — a CPU device gets its own cache row, PROVING device-keying is real; PGO + roofline cost-model deferrals with triggers; scars). NEXT = **28a**. | §80 §81 §82; proof §80 |

**28a ✅ 2026-09-05 (the `ceir.tune` dialect + the config-cache reader + `plan_options_from_tune_cache` — the target-specific configuration cache is now an AUTHORABLE asset)**: the §80 "target-specific configuration cache" mechanism the measurer (28b) will emit + the locked mode (28c) will replay. **(1) DIALECT** `engine/ceir/ops/tune.ceirop.toml` (NEW): ONE op `tune.entry` = one cache ROW with 6 REQUIRED attrs — the KEY `device`(string, backend+adapter e.g. "vk:rtx4070ti", NOT a CUDA arch), `env`(string, OS/driver bundle), `program_hash`(int), `shape`(string, workload class) → the SCHEDULE `fuse`(bool) + `share`(bool) = the CEIR-27 `PlanOptions` INLINED. 0 operands/results/effects + NOT Pure (cache metadata). Mirrors v17's `ckir_tuning_db.inc` row but PORTABLE (a `.ceir` asset, not a `.inc`). Regenerated via opgen (`build_entry` takes 6 AttrId params). **(2) MODEL+READER** `engine/ceir/include/crd/ceir/tune.hpp` (NEW, header-only, the rewrite_rules.hpp mold): `struct TuneEntry` + `TuneMisuseKind{None,DuplicateKey}` + `find_tune_misuse` + `load_tune_entries` + defensive `read_entry`. **(3) LOADER** `plan_options_from_tune_cache(ctx, cache_mod, device, env, program_hash, shape, base={})` → `TuneCacheLookup{bool hit; PlanOptions opts}` (tensor_pipeline, ceir-gpu): finds the FIRST row whose FULL KEY matches → HIT returns its `{fuse,share}`; MISS returns `base` UNCHANGED (the caller — 28c — chooses fallback vs typed reject). ⛔ NEVER measures (the v17 replay discipline, made portable). ⛔ DESIGN DECISION (mid-tick): the loader matches the FULL four-attr key, CONSISTENT with find_tune_misuse's DuplicateKey (= all-four-equal); env is a REAL match dimension, not provenance. **(4) MODULE-WIDE FIX (advisor — the ⛔-claim-in-source-needs-a-gate lesson from 27z, ONE TICK OLD, applied SAME slice):** find_tune_misuse first checked DuplicateKey only WITHIN a block while the doc claimed "module-wide" + load walked module-wide ⇒ a duplicate split across two blocks: find says None, load loads both, the loader is silently ambiguous. FIXED: both now share `collect_tune_entries` (region-recursive, via the const-accessible `ctx.allocator()`) ⇒ genuinely module-wide. **(5) GATE** device-free `test_tensor_pipeline.cpp "ceir 28a"` (#834): (a) 2-row cache walks clean + loads 2 + IDENTITY (exactly 2 tune.entry ops); (b) HIT — each KEY replays its schedule; (c) MISS — unknown device → hit=false + base UNCHANGED; (d) ⭐ FULL-KEY DISCRIMINATION (IDENTITY not category): a query differing ONLY in env / ONLY in program_hash MISSES; (e) same-block DuplicateKey; (f) ⛔ MODULE-WIDE DuplicateKey — same key in DIFFERENT blocks rejected (a two-block module, the discriminating input for #4). GREEN: 28a #834 + opgen-drift + tune gen-smoke + tidy-clean. TWO BITES (fixed): `create_module` does NOT auto-create a body block (mirror `build_transform`'s `if(top==null) create_block`); `load_tune_entries` is `[[nodiscard]]` (C4834 → `(void)`). ⛔ DEFERRALS with TRIGGERS (advisor): **(i)** `env` BUNDLES driver+compiler-version (§80 lists them separately) — split out when "a driver update changes the winning config on the same device"; **(ii)** the COMMITTED cache `.ceir` + anti-drift lands at 28b (the measurer EMITS the cache — bootstrap-via-emit; the anti-drift oracle is `print(parse(file))==print(measurer_output)`, the measurer IS the builder — the cache has no source but the measurer); **(iii)** `program_hash` has NO producer in-tree (grep: no module_hash/content_hash) — 28b DEFINES it = hash the CANONICAL PRINT (the roundtrip identity 27's differentials use; print-equal modules share a row); the loader treats it as an opaque u32 key. ⛔ UNCOMMITTED-BATCH: tune.ceirop.toml + gen/tune_ops.{cpp,hpp,json,md} + tests/ceir/generated/test_tune_gen_smoke.cpp + tune.hpp + the tensor_pipeline.{hpp,cpp} loader + test_tensor_pipeline.cpp edits. NEXT = **28b** (the PORTABLE MEASURER: define program_hash [canonical-print hash], confirm the Vk timestamp path at the API level, time the 4 configs reusing 27b's harness, emit the winner as a `tune.entry` via build_entry, COMMIT the cache `.ceir` with print-equal-to-emit anti-drift; oracle-gate FREE from 27b). | §80 §81 §82; proof §80 |

**28b-1 ✅ 2026-09-05 (`program_hash` — the program-identity KEY producer: the fnv1a-64 of the canonical print)**: the KEY the 28b-2 measurer emits + the loader matches. **(1) PRODUCER** `tune::program_hash(Context&, const Module&, IAllocator*)` (tune.hpp) = `containers::fnv1a_64` of `print(module)`. THREE-CLAUSE definition (advisor): hash the module the PLANNER consumes — the **POST-EXPANSION** payload (an `ml.mlp` and its expanded gemm/relu are DIFFERENT programs) + **PRE-plan** (the plan depends on PlanOptions, the thing being CHOSEN) + print-equal ⇒ hash-equal BY CONSTRUCTION. **(2) U64 WIDEN** ⛔ **28a(3) SUPERSEDED** — `read_entry` truncated an i64-stored 64-bit hash to its low word (u32); `TuneEntry::program_hash`, the reader (→ `entry_u64`), and the `plan_options_from_tune_cache` param are now `u64` (stored as i64, read as an opaque u64 KEY — no truncation). **(3) GATE** device-free `test_tensor_pipeline.cpp "ceir 28b-1"`: (a) two SEPARATELY-built structurally-identical ml.mlp modules hash EQUAL; (b) a changed operand type (d_out 4→8) ⇒ different hash; (c) ⭐ DISCRIMINATOR — the SAME ml.mlp module hashed BEFORE vs AFTER `expand_ml_ops` ⇒ DIFFERENT (the "hash the POST-expansion payload" claim GATED). GREEN: `[tune]` 28a+28b-1 (32 assns, 2 cases) + full `crd-ceir-gpu-tests` 103/103 (1467 assns) + tidy-clean (tune.hpp, tensor_pipeline.{hpp,cpp}, test). ⛔ STALE-EXE + BULK-RUN DIAGNOSIS (advisor — "artifact" is not a diagnosis): a mis-parsed `ctest -R "ceir"` (whole-suite) reported 549 (CSE)+858 (26f-3c) FAILED; BOTH pass in isolation AND 858 passes in the full-exe default-order 103/103 run ⇒ NOT order-state, NOT a regression. Root cause: the bulk ctest overlapped a concurrent clang-tidy run (heavy CPU) ⇒ ctest per-test TIMEOUTS (the 14900K host-instability doctrine — a whole-suite run is CI's SEQUENTIAL job, never local + never concurrent with heavy work; staleness causes false-GREEN not false-RED). Separately `crd-ceir-tests.exe` WAS stale (links crd-ceir.lib which the 28a tune dialect changed) — REBUILT per the stale-exe scar (rebuild every exe linking a changed lib), independent of the timeout. ⛔ `shape` DECISION (28b-2 open): `shape` is REDUNDANT with program_hash for STATIC-shape payloads (the print encodes every dim) — 28b-2 emits a DERIVED canonical string (terminal output dims, e.g. "4x2") so it is not a magic constant; discriminating only for dynamic-shape/bucketed programs (named-forward, no taxonomy). ⛔ UNCOMMITTED-BATCH (adds to 28a): tune.hpp (program_hash + u64) + tensor_pipeline.{hpp,cpp} (u64 param) + test_tensor_pipeline.cpp (28b-1). NEXT = **28b-2** (the PORTABLE Vk MEASURER: check `compute.hpp`'s timestamp API FIRST; N warmup + K timed + MEDIAN of all 4 configs [fire-count 4]; each BIT-EXACT to default [free oracle-gate]; emit ONE `tune.entry`; replay → hit + BIT-EXACT; commit `assets/ceir/tune_cache_vk_rtx4070ti.ceir` by hand from the printed emit with `print(parse(file))==print(measurer_module)` anti-drift — a later re-tune flipping the winner FAILING anti-drift IS the designed "re-tune" signal; NO assertion on WHICH config wins). | §80 §81 §82; proof §80 |

**28b-2a ✅ 2026-09-05 (the PORTABLE Vk MEASURER — the §80 autotuner runs on a REAL device: enumerate → time → oracle-gate → emit → replay)**: the v17 `kir_autotune` loop re-instantiated over the CEIR schedule space, PORTABLE. **(1) MEASURER** `measure_tune_entry` (test_ceir_pipeline_vulkan.cpp, a free Vk-test-level fn so 28b-2b + the 28d DX12 mirror call it) enumerates the FULL 4-config `PlanOptions` space (fuse × share), TIMES each (N=3 warmup + K=7 timed, MEDIAN of `compute.last_gpu_ms()`). ⭐ VERIFIED the timing source at the API+harness level FIRST: `last_gpu_ms` = the whole-plan GPU time — 2 device timestamps bracket the ONE submit's command buffer (`vulkan_compute_context.cpp:541` TOP_OF_PIPE reset+write / `:549` BOTTOM_OF_PIPE), so the fixed `ShaderWrite→HostRead` barrier cost is CONSTANT across configs and cancels in the comparison (NOT a per-dispatch reset — that would compare gemm2-vs-gemm2, meaningless); `run_quant_module`'s single `submit_and_wait()` (line 382) is exactly that one bracketed submit. **(2) FREE ORACLE-GATE** every config's output BIT-EXACT vs the default `{fuse,share}={1,1}` (⛔ 27b proved only 2 of the 4 configs — `{1,0}`/`{0,1}` were NEVER run; "share should be inert" is what the frontier rule forbids, so all 4 are compared element-wise). **(3) ⭐ WINNER-COLLAPSE + IDENTITY GATE (advisor — the one flaw that would have made 28b-2b flaky by construction):** the argmin median is COLLAPSED to the canonical (lowest-index, default-most) config in its plan-EQUIVALENCE class (a per-config `plan_sig` = fnv1a over every stage-kind + every buffer `alias_of`). tt≡tf are the SAME plan (share 26f-3a-INERT), so a ~64 ns noise gap between them must NOT flip the emitted row — else 28b-2b's anti-drift fires on jitter, not on a hardware change. Only a plan-DISTINCT winner (the fuse win) is emitted. This ALSO gave the DISCRIMINATING gate that was missing: `CHECK(sig[0]==sig[1]) && CHECK(sig[2]==sig[3]) && CHECK(sig[0]!=sig[2])` — share inert (tt≡tf, ft≡ff), fuse LIVE (tt≢ft, 2-stage GemmRelu vs 3-stage Gemm+relu+Gemm). ⛔ REPLACED a `*n_configs=4 → REQUIRE(==4)` can't-fail pair (hardcode-then-assert-the-hardcode). **(4)** the plausibility gate asserts the VALUE USED: `REQUIRE(medians_out[c] > 0.0)` (median==0 ⇒ no device timestamps ⇒ measuring NOTHING ⇒ hard fail, NOT a skip). **(5) EMIT + REPLAY** the winner → ONE `tune.entry` via `build_entry` (device `"vk:"+adapter_name()` — ⭐ `adapter_name()` is `IGpuContext`'s pure-virtual, so 28d reuses it VERBATIM; `env` platform-derived `#ifdef _WIN32`→"win32"/else"linux"; `program_hash` of the POST-expansion payload; shape "mlp:4x8x16x2"); the emit round-trips back through the REAL loader `plan_options_from_tune_cache` → HIT gives the winner's schedule (== `winner`), a WRONG `program_hash` (`ph+1`) MISSES (key is real), and replaying the looked-up schedule is BIT-EXACT to the default. **MEASURED (RTX 4070 Ti SUPER, win-debug #4774, 69 assns):** medians_ms [tt,tf,ft,ff] = **[0.008096, 0.008128, 0.009184, 0.00912]** — fuse ~**12 %** faster (real, 5-15× the within-group spread), share = timing-noise (as 26f-3a predicts); winner collapsed to stable `{fuse=true, share=true}`; `program_hash`=`13485518615284092459` (u64)/`-4961225458425459157` (i64, the form the asset prints); device `"vk:NVIDIA GeForce RTX 4070 Ti SUPER"`. **⭐ LAVAPIPE VERIFY (advisor — "believe isn't a gate"):** rebuilt + ran 28b-2a on WSL `/home/yatiyr/cerid-build/linux-gcc-debug` lavapipe (`vk:llvmpipe (LLVM 20.1.2, 256 bits)`) — `REQUIRE(median>0.0)` PASSES (llvmpipe DOES implement timestamp queries ⇒ the new hard gate does NOT redden the Linux CI sweep), 69 assns GREEN, gcc build clean; ⭐ SAME `program_hash`=`13485518615284092459` (the fnv1a of the canonical print is PLATFORM-INDEPENDENT — program identity is portable, only device+timing differ), same stable winner. GREEN: RTX #4774 (69 assns) + lavapipe (69 assns) + tidy-clean (local-constexpr `kWarmup`/`kTimed`→`n_warmup`/`n_timed` per the localconstant-lowercase gate). ⛔ UNCOMMITTED-BATCH (adds to 28a/28b-1): `test_ceir_pipeline_vulkan.cpp` only (measurer + `plan_sig` + `median_of` + the 28b-2a TEST_CASE). NEXT = **28b-2b** (COMMIT `assets/ceir/tune_cache_vk_rtx4070ti_super.ceir` = the EXACT printed emit `tune.entry(){device="vk:NVIDIA GeForce RTX 4070 Ti SUPER", env="win32", fuse=true, program_hash=-4961225458425459157, shape="mlp:4x8x16x2", share=true}`; ⛔ anti-drift `print(parse(file))==print(measure_tune_entry(...))` must be DEVICE-GATED — on lavapipe `adapter_name()`≠RTX so compare only when the device matches the committed row, else assert the lookup MISSES by design; a winner flip on the RTX = the designed re-tune signal). | §80 §81 §82; proof §80 |

**28b-2b ✅ 2026-09-05 (the COMMITTED target-specific config cache is a real AUTHORED `.ceir` asset — bootstrap-via-print + device-gated anti-drift)**: closes CEIR-28b. **(1) THE ASSET** `assets/ceir/tune_cache_vk_rtx4070ti_super.ceir` (NEW) = 28b-2a's EXACT printed `-s` emit, no hand edits (the measurer IS the builder — the cache has no other source; bootstrap-via-print, the `.ceir`-asset authoring doctrine): `tune.entry(){device="vk:NVIDIA GeForce RTX 4070 Ti SUPER", env="win32", fuse=true, program_hash=-4961225458425459157, shape="mlp:4x8x16x2", share=true}`. **(2) ANTI-DRIFT (RTX only)** re-measure the SAME payload → the fresh emit's canonical print must equal `print(parse(file))`. ⛔ compared PRINT-vs-PRINT (both through `print`, parse normalizes the file's whitespace/trailing-newline) ⇒ robust to formatting, fails ONLY on a SEMANTIC drift — a re-tune that FLIPS the winning schedule (the DESIGNED "cache stale, re-commit" signal, NOT a bug). The committed row then REPLAYS through the real loader BIT-EXACT to the default. **(3) DEVICE-GATED on `adapter_name()`** — on the committed device: anti-drift + replay + loader HIT; on ANY OTHER device (lavapipe): the loader MISSES. **(4) ⭐ IDENTITY-not-category (advisor — the ⭐ gate=identity scar):** the lavapipe MISS alone is a category check (missing/wrong-hash/wrong-env/wrong-device all pass `CHECK_FALSE(hit)`) — so a DEVICE-INDEPENDENT identity block (runs on BOTH platforms) `load_tune_entries`→`REQUIRE(n_rows==1)` + `device==committed` + `program_hash==ph` + `shape==shape` proves the row is PRESENT + the SAME program ⇒ the MISS is "misses on the KEY", not "absent/corrupt". ⛔ + `program_hash==13485518615284092459ULL` — the i64 the file PRINTS (`-4961225458425459157`) reads back as this u64: the **28b-1 u64-widen claim now GATED end-to-end against a REAL parsed asset** for the first time (not just a device-free constructed module). ⛔ the lavapipe miss is device AND env (lavapipe env="linux"≠"win32"); device-ALONE discrimination is the device-free **28a(d)** (cited, not re-proven). **(5) REFACTOR** extracted `build_mlp_payload` (module + 3 ExternalIn SSA values), SHARED by 28b-2a + 28b-2b ⇒ both hash the IDENTICAL program by construction (the hash-lock; a divergent payload would give a hash the committed row misses). GREEN: RTX #4774+#4775 (win-debug) + WSL lavapipe **84 assns / 2 cases** (gcc `-Werror` clean — the else-path identity assertions exercised there) + tidy-clean. ⛔ UNCOMMITTED-BATCH (adds to 28a/28b-1/28b-2a): **`assets/ceir/tune_cache_vk_rtx4070ti_super.ceir`** (⚠ a `.ceir` with NO source but a test — the uncommitted-delete scar: must be committed or the anti-drift breaks) + `test_ceir_pipeline_vulkan.cpp` (`build_mlp_payload` + 28b-2b + the 28b-2a helper refactor). ✅✅✅ **CEIR-28b COMPLETE** = 28b-1 (`program_hash`) + 28b-2a (portable Vk MEASURER: measure→oracle-gate→emit→replay) + 28b-2b (COMMITTED device-keyed cache + anti-drift). The v17 "tune offline, replay forever, never at runtime" loop is now PORTABLE + the config cache is an authored asset. NEXT = **28c** (DETERMINISTIC LOCKED MODE — the SELECTION POLICY wrapping the 28a loader primitive: `plan_tensor_pipeline` gains a cache-aware entry — cache HIT uses the row's schedule, MISS uses default + does NOT measure, a `locked=true` flag makes a MISS a TYPED `PlanReject` [cook-time certainty, ships nothing unmeasured]; device-free gate authoring a 2-row cache). | §80 §81 §82; proof §80 |

**28c ✅ 2026-09-05 (the DETERMINISTIC LOCKED MODE — the §80 "deterministic locked configuration mode": the SELECTION POLICY over the 28a loader)**: **(1) API** `plan_tensor_pipeline_cached(ctx, m, alloc, cache_mod, device, env, shape, TunePolicy, base={})` (tensor_pipeline.{hpp,cpp}) selects the schedule the cache holds for THIS target, keyed (device, env, program_hash(m), shape). ⛔ `program_hash` is computed HERE from `m` (the POST-expansion payload the planner consumes) — the SAME producer 28b emits, so the plan's key can NEVER drift from the plan. HIT → plan with the row's {fuse, share}; MISS → `policy` decides. ⛔ NEVER measures (replay-only; the v17 discipline made portable). **(2) ⭐ TunePolicy ENUM (advisor — NOT a bare bool):** `enum class TunePolicy {Fallback, Locked}` — a bare `bool locked` would hide the cook-time-certainty-vs-runtime-fallback decision (the thing 28b PROVED matters) in an unlabeled literal; the house types such choices (`GemmEpilogue::Relu`, `ComputeMemory`). `Fallback` = MISS→plan with `base`, don't measure (permissive; an untuned device runs correct-but-unoptimized); `Locked` = MISS→TYPED reject. ⛔ **`Require` (hit's schedule must ALSO equal `base` — the default is already tuned-optimal) is NAME-FORWARD to 28z** — a new appended slot, NO signature change (the advisor's third-policy foresight). **(3) TYPED REJECT** `PlanReject::TuneCacheLockedMiss` (appended at END) — a Locked miss returns an EMPTY plan (0 stages) with this reject; ⛔ `reject_op == nullptr` (the miss is a KEY property of the cache, NOT a module property — no op is at fault). **(4) ⛔ WIDEN-ENUM AUDIT (the scar — audit every CONSUMER, not just total switches):** the ONLY consumer that SWITCHES on PlanReject is `plan_reject_name` (case `"tune-cache-locked-miss"` added); grep found NO count/enum-range/roundtrip consumer (the equality-checks in tests are append-safe). ⛔ BOTH TOOLCHAINS: MSVC AND gcc `-Werror` (the msvc-hides-switch-gaps scar — gcc `-Werror=switch` would catch a missing case; it is clean). **(5) GATE** device-free `test_tensor_pipeline.cpp "ceir 28c"` (#839): payload = a 2-layer `ml.mlp` EXPANDED whose PLAN SHAPE reads out the chosen schedule (fuse=T ⇒ GemmRelu+Gemm 2 stages; fuse=F ⇒ Gemm+VizDispatch(relu)+Gemm 3). **(a) HIT** the ROW's fuse=false drives the plan OVER a CONTRADICTING `base` (fuse=true) — ⭐ IDENTITY (VizDispatch present, GemmRelu ABSENT), NOT a bare stage-count — and a 2nd row at `program_hash+1` with the OPPOSITE schedule is the TRAP the wrapper would hit if it hashed anything but `m` (so the HIT ALSO proves the key IS program_hash(m)); **(b) MISS+Fallback** unknown device → `base` (GemmRelu present, not measured); **(c) MISS+Locked** unknown device → `TuneCacheLockedMiss` + `reject_op==nullptr` + 0 stages. GREEN: MSVC 28c #839 (0.07 s) + all 3 exes (crd-ceir-gpu-tests/vulkan/dx12) relink clean (the enum-widened header compiles in every TU — stale-exe scar) + WSL gcc 28c (12 assns, `-Werror` clean) + tidy-clean (3 files). ⛔ UNCOMMITTED-BATCH (adds to 28a/28b-*): `tensor_pipeline.hpp` (TunePolicy + TuneCacheLockedMiss + `plan_tensor_pipeline_cached` decl) + `tensor_pipeline.cpp` (the def + `plan_reject_name` case) + `test_tensor_pipeline.cpp` (28c). NEXT = **28d** (the DX12 MEASURER MIRROR — the ⛔ migrated=both-backends rule: mirror `measure_tune_entry` on the DX12 compute context [`last_gpu_ms`/QueryHeap timestamp path], measure the 4 configs on DX12, emit + replay BIT-EXACT; the autotuner path must run on BOTH GPU backends, not just Vk). | §80 §81 §82; proof §80 |

**28d ✅ 2026-09-05 (the §80 autotuner MEASURED on a REAL D3D12 device — the ⛔ migrated=both-backends rule)**: `measure_tune_entry` MIRRORED onto DX12 (`Dx12ComputeContext` + `last_gpu_ms`). ⛔ MIRROR-INLINE, NOT a shared template (**advisor reversal** of the 28b-2a "28d calls it" line): the two `run_quant_module`s are file-local to two TUs, so a shared measurer needs a RunFn param + a shared header + a refactor of green 28b-2a — churn for ~50 lines of TEST code the house already duplicates per backend. **(1) DEVICE KEY** `create_dx12_gpu_context()`→`adapter_name()` (the name lives on the gpu context, mirroring Vk reading it off `VulkanGpuContext`; `Dx12ComputeContext` doesn't expose it), `"dx12:"+adapter`. ⛔ **advisor reversal (WARP):** NOT a not-WARP assert — that FALSE-FAILS on a legitimately WARP-only box (`compute.valid()` true + adapter genuinely WARP); non-empty check only, strict same-adapter proof deferred (both contexts pick the DEFAULT adapter). On WARP the key `"dx12:Microsoft Basic Render Driver"` is CORRECT (WARP = D3D12's llvmpipe — 28z's "a CPU device gets its own row" covers it). **(2) MEASURED (RTX 4070 Ti SUPER, #4873, 78 assns, 0.88 s):** device `"dx12:NVIDIA GeForce RTX 4070 Ti SUPER"` (the REAL GPU, not WARP), `program_hash`=`13485518615284092459` — ⭐ IDENTICAL to Vk+lavapipe (portable program identity confirmed on a 3rd backend), medians_ms [tt,tf,ft,ff]=**[0.017344, 0.017344, 0.018432, 0.018432]**, winner `{fuse=true, share=true}` — **fuse wins on DX12 too** (~6 %). **(3) ⭐ CROSS-BACKEND PLANNER DETERMINISM (advisor — a real NEW result):** `plan_sig` is BIT-IDENTICAL to Vk's (fuse-class `0x83e33779c4351c48`, no-fuse-class `0x4e7803adf4d43172`) ⇒ `plan_tensor_pipeline` has NO backend-dependent branch in stage-selection/aliasing ⇒ ONE tune-cache shape works for ALL backends (not proven before this tick). **(4) ⭐ MEDIANS — the real finding (advisor):** DX12 medians QUANTIZE (tt==tf, ft==ff EXACTLY) — the fuse/no-fuse gap `0.001088 ms` (~1.09 µs, ~6 %) is 1-2 timer quanta, real but at the resolution FLOOR; the Vk gap (~1.1 µs, ~12 %) is the SAME ABSOLUTE ~1 µs delta on a 2× smaller base ⇒ **fusion saves ~1 µs on BOTH backends; the % differs only because DX12's begin→submit bracket includes the readback `copy(GpuOnly→GpuToCpu)` (D3D12 forbids a UAV on a READBACK heap — the portable copy-form), Vk maps GpuToCpu directly.** Fixed copy+resolve floor ≈ 17.3−8.1 = **~9 µs** (DX12 base − Vk base, same GPU/plan). ⛔ NEVER compare a DX12 median to a Vk one except after subtracting that floor. GREEN: MSVC 28d #4873 (78 assns) + tidy-clean. (NO lavapipe — D3D12 is Windows-only, the module self-skips on Linux.) ⛔ UNCOMMITTED-BATCH (adds): `test_ceir_pipeline_dx12.cpp` (`median_of` + `plan_sig` + the `measure_tune_entry` DX12 mirror + 28d). ⛔ 28z ITEMS ACCRUED (advisor): (i) the COMMITTED DX12 cache row is now the TRIVIAL bootstrap (fuse won both backends) — folds into 28z alongside llvmpipe/WARP rows (a 3-row asset or 3 assets is a 28z design choice, not a measurer slice); (ii) `TunePolicy::Require`; (iii) `n_warmup` DRIFT (DX12=5 vs Vk=3, justified by D3D12's heavier first-submit PSO work — 28z: both→5 or make it a param); (iv) medians backend-comparable only after subtracting the readback floor; (v) PGO/roofline/env-split deferrals. NEXT = **28z** (CLOSE CEIR-28: the SELECTION policy proven on Vk+DX12+lavapipe; commit the device-keyed cache rows [RTX-Vk done 28b-2b; RTX-DX12 + WARP trivial]; the accrued 28z items; scars). | §80 §81 §82; proof §80 |

**28z-1 ✅ 2026-09-05 (BAND-CLOSE, subslice 1/3 — the close SUBDIVIDED per the grant: warmup unify + the Require deferral)**: the mechanism-completion tick before the committed rows (28z-2) + the doc close (28z-3). **(1) ⛔ `n_warmup` UNIFY** the Vk measurer's warmup 3→5, matching DX12 (28d justified 5 by D3D12's heavier first-submit PSO work; ONE warmup count across both backends beats two separately-justified numbers). ⛔ PROVEN SAFE: the winner-collapse makes the emitted row warmup-INDEPENDENT, so 28b-2b's COMMITTED asset is UNCHANGED — reran 28b-2a #4775 + 28b-2b #4776 at 5 warmups, BOTH pass (the anti-drift `print(parse(file))==print(fresh re-measure)` still holds). NOT a param (the two measurers are mirror-inline — a param on two copies is two params). **(2) ⛔ `TunePolicy::Require` DEFERRED (advisor REVERSAL of the 28c "the one 28z needs" line):** the only Require semantic with a real CONSUMER — "HIT but the planner IGNORED the row's opts → reject" (a `tune_row_applied` plan bit) — needs a caller that must distinguish 'cache MISSING' from 'cache present but NOT honored', and NONE exists; the "hit must equal base" semantic I sketched at 28c has no consumer at all (you commit a row BECAUSE it differs from default). ⛔ the no-speculative rule (26b-4) WINS over filling the reserved enum slot — Require is name-forward with a TRIGGER (such a caller appears → Require appends at END + the plan bit), NOT a made-up third value with a test that proves nothing. The `TunePolicy` enum keeps its `⛔ append at END` slot (header comment updated to the reversal). GREEN: 28b-2a #4775 + 28b-2b #4776 (n_warmup=5) + tidy-clean (test_ceir_pipeline_vulkan.cpp + tensor_pipeline.hpp). ⛔ UNCOMMITTED-BATCH (adds): `test_ceir_pipeline_vulkan.cpp` (warmup 3→5) + `tensor_pipeline.hpp` (the Require-deferral comment). ⭐ 28z SUBDIVISION (advisor): **28z-1** (this — warmup + Require) → **28z-2** (the COMMITTED 3-row cache asset `assets/ceir/tune_cache.ceir` [rows: `vk:…SUPER`/win32, `dx12:…SUPER`/win32, `vk:llvmpipe…`/linux — all `fuse=true,share=true`; the 3 printed emits are already captured from the 28b-2a/28d/lavapipe `-s` runs] REPLACING 28b-2b's single-row asset + a per-row device-gated anti-drift + one `find_tune_misuse`→None proving no cross-backend duplicate key; NO WARP row — never measured, the no-unmeasured rule) → **28z-3** (CLOSE: benchmarks→`docs/bench/ceir-28-autotune-medians.md`; cross-config `[tune]` win+linux; the deferral ledger [PGO/roofline/env-split/Require/WARP/readback-floor, each with a trigger]; scars [winner-collapse-to-plan-class, print-vs-print anti-drift]; flip CEIR-28 ✅; context.md PRUNE, overdue). NEXT = **28z-2**. | §80 §81 §82; proof §80 |

**28z-2 ✅ 2026-09-05 (BAND-CLOSE 2/3 — the COMMITTED cross-backend config cache is ONE authored asset, 3 device-keyed rows)**: **(1) ASSET** `assets/ceir/tune_cache.ceir` (NEW) REPLACES `tune_cache_vk_rtx4070ti_super.ceir` (DELETED — both uncommitted, so a delete+create, NOT a rename): 3 rows, each bootstrapped from its measurer's printed `-s` emit — `vk:NVIDIA GeForce RTX 4070 Ti SUPER`/win32 (28b-2a), `dx12:NVIDIA GeForce RTX 4070 Ti SUPER`/win32 (28d), `vk:llvmpipe (LLVM 20.1.2, 256 bits)`/linux (28b-2a on WSL) — ALL `fuse=true, share=true`, `program_hash=-4961225458425459157`, `shape="mlp:4x8x16x2"`. ⛔ NO WARP row (WARP was never measured — the no-unmeasured rule; a CI runner w/o a GPU running the DX12 suite is the trigger, 28z-3 ledger). **(2) ⛔ ANTI-DRIFT is now PER-ROW FIELD-equality (a multi-row cache breaks 28b-2b's module-print-equality — 3 rows vs a 1-row fresh emit):** load the matching-device row + the fresh re-measure's entry (both via `load_tune_entries` — still NO raw-byte compare), 6 CHECKs (device/env/program_hash/shape/fuse/share); a re-tune winner FLIP changes `fuse` ⇒ the CHECK fails = the DESIGNED re-commit signal. **(3) TWO anti-drift tests, ONE PER BACKEND TU (advisor — migrated=both-backends; do NOT graft onto 28d, which is a closed device-gate slice):** Vk `28b-2b` (UPDATED in place — anti-drifts the vk:RTX row on Windows, the vk:llvmpipe row on WSL) + DX12 `28z-2` (NEW TEST_CASE — anti-drifts the dx12:RTX row), both reading the SAME 3-row file. **(4) SHARED GATES (both TUs):** ⛔ ONE `find_tune_misuse`→None on the 3-row module (no duplicate KEY ACROSS backends — the payoff of 28a(f)'s multi-block gate); ⭐ ROWS-ADDRESSABLE table — all 3 rows independently HIT their own key with the shared schedule (proves ONE cross-backend artifact, not 3 same-named files); ⛔ wrong-ENV MISS (the RTX device under env="linux" MISSES — env is a REAL key dimension, now proven on a REAL FILE; 28a(d) was a constructed module); the `program_hash` u64 pin. **(5) ⛔ the "device with NO row" else-branch is UNEXERCISED** on the RTX/lavapipe boxes (every device we run has a row) — kept for correctness, NOT claimed as covered (advisor: no fake coverage; a 4th device is its trigger). GREEN: Windows Vk 28b-2b #4776 + DX12 28z-2 #4874 + WSL lavapipe 28b-2b (**92 assns**, gcc-clean — the llvmpipe row3 anti-drift + rows 1-2 addressable) + tidy-clean (both TUs). ⛔ UNCOMMITTED-BATCH: **`assets/ceir/tune_cache.ceir`** (NEW — ⚠ REPLACES the DELETED `tune_cache_vk_rtx4070ti_super.ceir`; do NOT `git add` the stale name) + `test_ceir_pipeline_vulkan.cpp` (28b-2b rewrite) + `test_ceir_pipeline_dx12.cpp` (28z-2 new). NEXT = **28z-3** (the CLOSE tick: benchmarks→`docs/bench/ceir-28-autotune-medians.md` [3 boards]; cross-config `[tune]` win+linux; the deferral ledger [PGO/roofline/env-split/Require/WARP/readback-floor, each w/ trigger]; scars [winner-collapse-to-plan-class, print-vs-print anti-drift]; flip CEIR-28 ✅; context.md PRUNE overdue). | §80 §81 §82; proof §80 |

**28z-3 ✅ 2026-09-05 (BAND-CLOSE COMPLETE — CEIR-28 ✅)**: the close tick. **(1) BENCHMARKS → docs/bench** (the mandatory-at-close rule): `docs/bench/2026-09-05-ceir28-autotune-medians.md` (+ README index) — 3 boards (Vk RTX / DX12 RTX / Vk lavapipe). ⭐ FINDINGS: fusion is the measured winner on BOTH real backends by the SAME ~**1.088 µs** absolute margin (Vk ~12% on an 8.1 µs base, DX12 ~6% on a 17.3 µs base); the DX12 base is ~2× Vk = a **~9 µs fixed readback-copy floor** inside its timestamp bracket (D3D12 no-UAV-on-READBACK-heap → copy-form); `share` inert (plan-identical); `program_hash` + both `plan_sig`s IDENTICAL across all 3 backends **for the 4×8×16×2 MLP payload** (gemm/relu vocab) — EVIDENCE (one payload = one point, NOT a general proof) that stage-selection + aliasing carry no backend-dependent branch; the general claim is a **CEIR-29+ census question** if native providers introduce one. **(2) CROSS-CONFIG (RAF/REN one-win-one-linux):** win-debug 7/7 CEIR-28 tests + WSL gcc device-free `[tune]` 44 assns + Vk-lavapipe `[tune]` 169 assns (DX12 self-skips on Linux) — GREEN on MSVC AND gcc `-Werror`. **(3) ⛔ DEFERRAL LEDGER (each with a TRIGGER, no speculative build):** **PGO** (§81 runtime feedback loop) → a CEIR program with a runtime hot loop; **roofline cost model** (§82) → a config space too large to exhaust (4 configs = MEASURE ALL, the cost model is VACUOUS); **env driver/compiler-version split** (§80 lists them; `env` bundles them) → a driver update flips the winner on the same device; **`TunePolicy::Require`** → a caller that must distinguish 'cache MISSING' from 'cache present but planner IGNORED it' (a `tune_row_applied` plan bit); **WARP cache row** → a CI runner without a GPU runs the DX12 suite (WARP = D3D12's llvmpipe, its key is CORRECT when it appears); **readback-floor subtraction** → a consumer that compares medians across backends; **the else-branch** (a device with NO committed row) → a 4th device. **(4) SCARS — 2 NEW memory files (advisor):** `feedback_autotuner_winner_collapse_to_plan_equivalence_class` (a measurer over plan-INERT knobs must collapse the winner to its plan-class or it emits noise into the committed row — anti-drift fires on jitter) + `feedback_committed_asset_antidrift_through_the_printer_not_bytes` (compare through the canonical printer [single-row] or the loader's fields [multi-row], NEVER file-bytes vs a fresh emit) — MEMORY.md pointers added. **(5) context.md PRUNE** — the LIVE header (stale reading CEIR-25 for ~10 ticks) refreshed to CEIR-28-CLOSED. The DEEP structural prune of the >74KB historical CEIR-25/26/27 narration is DEFERRED (a 74KB single-line edit under a 60s cadence is the wrong place to attempt it) — **trigger: the FIRST action of the CEIR-29 census tick, BEFORE any 29 rows land** (a fresh band = the old narration is definitively dead weight; do it first or it slides forever). ✅ **DONE 2026-09-05** (CEIR-29 tick-1, before any 29 rows): context.md 344 lines/187 KB → **84 lines/11.6 KB**; the 90 KB `Current focus` blob + the CEIR-18/19 `Prior focus` stack + the stale CEIR-1z/RAH `Active state` archived VERBATIM (lossless, awk-extracted) → `docs/sessions/2026-09-05-context-md-history-archive.md` (mirrors the 2026-08-07 precedent); STANDING-ORDER line + evergreen tail byte-identical (diff-verified); all 7 filed follow-ups (19b-F1/F2, 08608, READS-FIRST, DX12-geometry-Y, WW-race, froxel-config-flex) confirmed already in the tracker before pruning. New `## Current focus` block carries a "REPLACED each tick, never APPENDED" rule so the blob can't regrow. **(6) 28-0 CENSUS RE-VERIFIED** (⛔ GATE re-verify + band-close, advisor-flagged: I closed by writing rows, not by reconciling the census promises): `schedule_ref` → **inlined `{fuse, share}`** (28a decision, STRUCK IN PLACE at the 28-0 attr-set); the "28b = 27b harness + a duration out-param" → `IComputeContext::last_gpu_ms()` (28b-2a, no out-param needed); the "28z = llvmpipe measures too, a CPU device gets its OWN cache row" → **MET** (28z-2, `tune_cache.ceir` row 3 = `vk:llvmpipe`/linux). No census promise left unreconciled. ✅✅✅ **CEIR-28 (the SELECTION POLICY over CEIR-27's mechanism) COMPLETE** = 28-0 (census) + 28a (`ceir.tune` dialect + module-wide `find_tune_misuse` + full-key loader) + 28b (`program_hash` + the portable Vk MEASURER + the committed device-keyed cache + anti-drift) + 28c (DETERMINISTIC LOCKED MODE: `plan_tensor_pipeline_cached` + `TunePolicy` + typed reject) + 28d (the DX12 measurer mirror + cross-backend planner determinism) + 28z (warmup unify + Require deferral + the 3-row cross-backend `tune_cache.ceir` + the close). **The §80 "tune offline, replay forever, never at runtime" loop (v17's kir-autotune) is now PORTABLE (Vk + DX12 + lavapipe), the config cache is an AUTHORED `.ceir` asset, and the locked mode ships nothing unmeasured.** NEXT = **CEIR-29** (native graph providers — one graph partitioned across two provider classes: D3D MLIR Programs / VK data-graph / NPU research; §70 §102 §69; OPEN with an advisor census like every band). | §80 §81 §82; proof §80 |

**29-0 ✅ 2026-09-05 (BAND-OPEN CENSUS — advisor-verified facts + subslice map + deferral ledger, no pre-design; the BAND is 🔄)**: CEIR-29 = the §70 NATIVE-GRAPH partitioner across PROVIDER CLASSES (§69 advertise/assign + §102 partition). ⭐ A NAMED DEFERRAL BEING FULFILLED, not greenfield: `provider.hpp:36` — "the FULL advertise surface (types/layouts/memory-domains/costs/determinism + compile→plan, §69/§70/§102/§103) lands with the partitioner band (CEIR-21/26)"; `partition_ml.hpp:5` — "the minimal device-free form; **CEIR-29 owns the full native-graph partitioner across provider classes**". EXISTS (the reference floor, grepped — the four seam reads): (i) `IExecutionProvider` (crd-ceir, backend-free abstract seam: `name`/`advertises(OpId)`/`execute`; ⛔ APPEND-AT-END vtable, ADR-0109/D135); (ii) `HostProvider` (crd-ceir-host — the ONE bridge implementation = the native-provider MOLD: bridge impls IExecutionProvider, backend machinery private); (iii) `MlProvider`+`partition_ml`+`apply_partition`/`expand_ml` (crd-ceir-gpu, DEVICE-FREE per-OP claim: first-available-provider-else-CKIR-fallback, `available` is an INPUT flag; `coopvec_can_claim_mlp` = the ONLY native provider today, a SINGLE-op fused MLP); (iv) `plan_cache` already keys plans by `IExecutionProvider::name()` (`plan_target`=FNV) — the cook consumer, and it says the compile→plan interface is "deferred to CEIR-21/26" = THIS band; (v) CEIR-27 `transform` dialect = the AUTHORED-PINNING home (`transform.fuse`/`share_storage` program-global directives → PlanOptions; §71 "assign provider" = a NEW `transform.assign_provider` directive HERE, never a C++ API). WHAT §70 ADDS beyond today: (1) MAXIMAL-SUBGRAPH claiming (greedy-grow in PRE-ORDER while claimable — keeps the partition inspectable + reproducible like today's per-op form); (2) ≥2 provider CLASSES; ~~(3) a REAL inserted TRANSFER/SYNC op/barrier at each provider boundary (§102), gate-ASSERTED (never a comment);~~ **[29a-3a RECONCILE: the boundary transfer is a plan STAGE (tensors are buffers at the plan step), NOT a resource IR op, and it has NO consumer until a provider EXECUTES across a boundary — `StageKind` has no Copy today + the planner is partition-blind; it LANDS at 29b (trigger: first executing provider whose memory_domain ≠ the fallback's), 29c ASSERTS it]** (4) provider choice auto/constrained/PINNED (§102) via the authored directive. ⛔⛔ RUNNABILITY (the census's central question — advisor + WebSearch, NOT recall): the tracker's named "D3D MLIR Programs / VK_ARM_data_graph" are NOT runnable on the RTX-4070-Ti-SUPER / lavapipe boxes — DirectX Compute Graph Compiler is PRIVATE PREVIEW (summer-2026; DXLA public-preview Apr-2026, per Microsoft's GDC-2026 DirectX blog), VK_ARM_data_graph is ARM-Mali-only. ⭐ DECISION: the runnable §70 native-graph provider = **CUDA GRAPHS** — CUDA is a WIRED ceir-gpu backend TODAY — ⭐ VERIFIED: `CudaComputeContext : public IComputeContext` (ADR-0100 — the SAME backend-agnostic dispatch surface as Vk+DX12, incl. `last_gpu_ms()` via CUDA events) ⇒ `plan_tensor_pipeline` RUNS on CUDA (the interface `plan_tensor_pipeline`/`run_quant_module_n` dispatch through is implemented). ⛔ BUT only `test_ceir_add_cuda` (a SINGLE `add` via `execute_lowered`, ADR-0126, device-locked) exists today — the MULTI-STAGE pipeline on CUDA was 24z-LEDGERED (deferred, per the test_ceir_pipeline_vulkan.cpp:2643 note), so 29b's FIRST step is net-new. Then the provider CAPTURES the pipeline's lowered launches into ONE `cudaGraph` + launches as one; it is the *launch-graph* item on §70's list (CUDA Graphs / Work Graphs / DGC) ⇒ **BIT-EXACT vs the CKIR/stream fallback BY CONSTRUCTION** (same kernels, no FP reorder — the correct house gate for a 2nd execution path, 28's pattern); single CUDA memory domain (no cross-API interop). ⛔ the *semantic-engine* §70 items (D3D Compute Graph Compiler / VK data-graph / NPU) REORDER FP accumulation ⇒ oracle-gated (linalg.gemm L9, [[feedback_oracle_must_round_every_elementary_op]]) NOT bit-exact — DEFERRED. SUBSLICE MAP: **29a** device-free partitioner CORE (subsliced 29a-1/2/3 at 29a entry) — extend `partition_ml` to subgraph-claim + `advertise_region` (region walk, FULL semantic attrs per op — the specialized-kernel scar squared, `partition_ml.hpp:12`) + descriptor GAINS `class` (`ProviderClass` host/gpu/npu/media/external, ADR-0110 — a NEW enum, spec-only today) + `memory_domain` + `deterministic` (REUSE 19a `ExternalNondeterminism`, don't invent) + transfer/sync insertion + `transform.assign_provider` authored pinning; testable DEVICE-FREE (available=false ⇒ all-fallback; pinned/auto/~~constrained~~ matrix **[CONSTRAINED mode STRUCK→29c (29a-3b-2 GATE re-verify): a `provider_class`-constrained pin ("this op class only on Gpu-class providers") needs TWO classes in ONE span to discriminate — 29c has them; pinned ✅ + auto ✅ (unpinned=first-available) landed 29a]**); costs → defer to a 28-tune-cache trigger. ⭐ CANONICAL SEAM (SETTLED at census — the two are LAYERED, not competing; grep-confirmed NOTHING in ceir-gpu/tests consumes both as one object): `MlProvider` = the PARTITIONER's INPUT (advertise, device-free, ceir-gpu) → GAINS the §69 advertise surface (`class`/`memory_domain`/`advertise_region`/`deterministic`); `IExecutionProvider` = the EXECUTION seam (crd-ceir, `execute`, HostProvider mold) → ~~GAINS the compile→plan pure-virtual APPENDED AT END (D135 vtable), which the CUDA-graph bridge implements~~ **[DISSOLVED at 29b-2b — NOT deferred, VOID: `IExecutionProvider` is the SCALAR reference seam (`execute`→`ExecResult{Array<i64>}`, the §118 Interpreter); a launch-graph provider (CUDA Graphs) is a plan LAUNCH-MODE (begin_capture/enqueue/launch), NOT an implementor of a compile→plan virtual — the tensor "compile→plan" IS `plan_tensor_pipeline`. NO such pure-virtual was ever added (inventing a 2nd CompiledPlan type = the is_memory_domain drift); the census's APPEND is void, do NOT ledger it as a deferral. See the 29b-2b row's RECONCILE.]**. ~~29a's seam work = the APPEND + the descriptor fields, no fork.~~ **[STRUCK→29b (29a-3b-2 GATE re-verify): 29a's seam work was the DESCRIPTOR fields only (MlProvider gained class/memory_domain/determinism/claims_subgraphs — the advertise half of provider.hpp:36); the IExecutionProvider compile→plan APPEND lands at 29b WITH its implementor (the CUDA-Graphs bridge) — appending an unimplemented pure-virtual now is speculative (26b-4). No fork either way.]** **29b** the CUDA-GRAPHS native-graph provider — ⭐ PRE-STEP (pays the 24z-ledgered CUDA-pipeline debt): the tensor-pipeline device gate on `cuda_compute_context` (a 28d-style backend mirror) so there IS an N-dispatch fallback to capture + BIT-EXACT-compare against; THEN capture → one `cudaGraph` (claims a maximal subgraph; Win + WSL2 device gate; BIT-EXACT vs the fallback by construction). **29c** the TWO-CLASS boundary proof on ONE (CUDA) memory domain (native-graph provider + CKIR/stream fallback; one graph partitioned; ⛔ ~~a REAL inserted transfer/barrier ASSERTED~~ **[STRUCK at 29c-1 — UNTRIGGERED, not degenerate: the transfer's own trigger is "a provider whose memory_domain ≠ the fallback's"; on ONE CUDA domain cuda_graphs + the fallback are BOTH `device_local`, so it never fires — a `StageKind::Transfer` now would be a value nothing produces (26b-4). Name-forward WITH the two-domain trigger (the cross-API-interop item already deferred). 29c's one-domain analogue is the per-stage-capture BOUNDARY: node_count == the claimed run's stage count, not the total — 29c-2]**; PINNED + AUTO both proven — §102's own `CKIR→engine→CKIR` example; native+fallback is TWO classes, sufficient). **29z** close (bench: N-dispatch fallback vs 1 graph launch medians; ~~via 28's harness — `last_gpu_ms()` works on CUDA [CUDA events], so the SAME harness times it, no cudaEvent surprise; launch overhead IS the cost for a small graph, the win is visible or the row says it isn't~~ **[STRUCK at 29z-1 — two of three claims BROKE: (a) `last_gpu_ms` [GPU-event] could NOT show the win — the win is the CPU SUBMIT PATH, a DIFFERENT column (steady_clock wall), 28's GPU-only harness would have shown ~1.0×; (b) there WAS a cudaEvent surprise — a bracket-structure artifact (summed brackets / 3 waits) faked a loss until the `enqueue` split matched the arms. The win IS visible (sandwich 1.36–2.24× CPU) once measured on the right column; GPU-time is a toy-dim LOSS, honestly recorded]**; ledger; scars; flip ✅). ⛔ NO 29d "cross-backend mirror" — a CUDA provider is NOT Vk/DX12 portable (advisor); portability lives in the DEVICE-FREE core (29a), the runnable proof is CUDA-only. DEFERRAL LEDGER (each w/ trigger): D3D12 Compute Graph Compiler / MLIR Programs → public-preview/GA in the Agility SDK on the 4070 (oracle-gated when it lands; status re-check: devblogs.microsoft.com/directx/evolving-directx-for-the-ml-era-on-windows/ + microsoft.github.io/DirectX-Specs/d3d/MlirPrograms.html) · VK_ARM_data_graph → an ARM Mali device · NPU → an NPU device (§70 "research") · D3D12 Work Graphs → a node-shader consumer + Agility plumbing · CUDA device-graph launch → a graph needing device-side launch · Metal ICB → an Apple box · cross-API interop transfers → a partition spanning TWO API memory domains · §103 multi-device sharding → a >1-GPU box · autotuned provider choice → two providers both claim + cost decides (28's tune cache is the home). SCOPE STRUCK IN PLACE in the CEIR-29 matrix row (SUPERSEDED rule): "D3D MLIR Programs / VK data-graph / NPU research" → CUDA-Graphs-first (launch-graph, runnable); semantic-engine items deferred to their devices. | §69 §70 §102; defer §103 §140 |

**29a-1 ✅ 2026-09-05 (the partitioner descriptor GAINS the §69 provider-class fields — device-free, fields INERT)**: the first 29a subslice. ADDED `enum class ProviderClass : u8 {Host, Gpu, Npu, Media, External}` (ADR-0110, NO Unspecified — a provider ALWAYS has a class) in **`semantics.hpp`** (NOT dialect.hpp as first sketched — its §-class SIBLINGS `DeterminismClass`/`EvalDomain`/`RealtimeClass` live there, and dialect.hpp includes it). `MlProvider` GAINS 3 APPENDED fields: `provider_class` (default `Gpu` — this partitioner IS crd-ceir-gpu, an honest module default), `memory_domain` (a `StringView`, ⭐ NOT a new enum — REUSES the §24 vocab `resource.declare` validates, an advisor RECONCILE after I found the existing 11-value `kMemoryDomainVocab`: a fresh enum would be a second vocabulary = drift), `determinism` (`DeterminismClass`, reuse 19a's; coopvec = `DeterministicWithinTarget` — a fixed-order fused kernel is target-reproducible, NOT bit-exact-across-backends). EXTRACTED `crd::ceir::is_memory_domain(StringView)` from context.cpp's anon-namespace `kMemoryDomainVocab`/`ceir_sv_in` (MOVED to a public predicate — one table, two readers, no copy); both readers routed through a shared `kMemoryDomainVocabCount` (advisor: I'd copied the `11U` literal — the count is now single-sourced, no drift on a 12th entry). ⛔ INERT: `assign` still reads only `available`+`advertise` (the fields land their consumers at 29a-2 subgraph-claim / 29a-3 transfer-insertion — NAMED, not speculative). ⭐ `static_assert(std::is_trivially_copyable_v<MlProvider>)` ADDED (the house `ConcurrentQueue<T>` pattern) — my first-pass "type_traits used NOWHERE" was a SUBSET grep (ceir/gpu-context only); the advisor caught it (`<type_traits>` is in 11 `containers/` headers), so the pin stands, no deviation. GATE `test_band24_gate.cpp "ceir 29a-1"` (#873, device-free `crd-ceir-gpu-tests`) — a 2-provider span {coopvec=Gpu/device_local, ext_stub=External/host_visible_device/never-claims}: (1) IDENTITY on a 2-class span (claimed_by(0)==1 mlp→coopvec, claimed_by(1)==0 the External stub, fallback()==1 attention — a NEW 2-provider input; the actual REGRESSION pin is that 24c/24z still pass), (2) fields round-trip BY INDEX (IDENTITY: `provs[1].provider_class == External`, not `!= Gpu`), (3) `is_memory_domain` in-vocab {device_local, host_visible_device, unified}=true / {bogus, ""}=false, (4) caps-off Gpu row → all-fallback (the 24c negative re-asserted post-widening). Built `crd-ceir-gpu-tests` on **MSVC win-debug AND WSL linux-gcc-debug** (`-Werror` — inert for switch, no `switch(provider_class)` consumer yet; clean both). ctest `-R partition`: the **4 ml-partition tests PASS on BOTH compilers** (24c-1 ×2 + 24z regression + 29a-1 new); the `-R partition` sweep's other 18 matches (hesap/AMG/wavelet — the regex is a name substring, unrelated) also green. Tidy 6 files clean. ⛔ SCAR re-hit: the TEST_CASE name's `§69` mangled to `�` in the ctest→Catch2 filter round-trip (0 matched → false-FAIL) — the ASCII-only-test-names rule; renamed "(sec 69)". ⛔ 29b SEED (was tagged 29a-3; RECONCILED at 29a-3a — the transfer is a plan STAGE that lands at 29b, not a 29a op): the transfer-insertion use-site must VALIDATE `memory_domain` via `is_memory_domain` before comparing domains — a typo in a provider row is a TYPED reject, never a silent "different domain ⇒ insert transfer". NEXT = **29a-2** (maximal-subgraph claiming: an `advertise_region` fn-ptr + greedy-grow pre-order; the first field consumer, where `-Werror=switch` starts earning). | §69 §102 |

**29a-2 ✅ 2026-09-05 (MAXIMAL-SUBGRAPH claiming via greedy-grow — device-free; the first descriptor-FIELD consumer: `claims_subgraphs`, added THIS slice; the 29a-1 trio `provider_class`/`memory_domain`/`determinism` stays INERT until 29a-3)**: the second 29a subslice. ⭐ DESIGN CORRECTION (advisor at entry): the census-seed's `advertise_region(const Region*)` → a `bool claims_subgraphs` on `MlProvider` — a Region is the WRONG granularity (the mixed 24z region holds one claimable `ml.mlp` + one un-claimable `ml.attention`; a region-level predicate can't say yes-to-one-no-to-the-other). `false` = PER-OP claiming (coopvec: each op its own kernel, subgraph=-1); `true` = the partitioner GREEDY-GROWS consecutive claimable SIBLING ml ops into ONE run. The claim-test stays the EXISTING per-op `advertise`. `MlAssignment` GAINS `subgraph` (i32, -1=singleton) — ⛔ MONOTONIC across the whole module (a `next_subgraph` counter threaded through `scan_region`, never reset per-block/provider: the monotone-watermark scar — two runs of one provider in different blocks MUST get distinct ids, else 29c merges across a boundary). `MlPartition` GAINS `subgraphs_of(provider)` (distinct-run count) + `subgraph_size(id)` (run op-count) — inspection for the gate + 29b (`subgraphs_of` is O(n²) on op-count — a >~1k-ml-op module makes it a sort-by-id, a NAMED trigger, not today). GREEDY-GROW rule (restructured `scan_region`): a run extends op[i]→op[i+1] iff SAME BLOCK + op[i+1] is ml + SAME provider's `advertise` accepts; ANY non-ml op / advertise refusal / different provider / block-end CLOSES it (⛔ NO stepping over a non-ml op — that dependency is 29a-3's transfer question). §102 "maximal subgraph" WITHOUT a graph-cut algorithm; real subgraph search (two providers competing for overlapping runs, ops-between analysis) → NAMED-FORWARD trigger. ⛔ FIXTURE FINDING (advisor: read build_band24 FIRST): build_band24's ml ops are SPLIT by the q/ky/v declares (block order: declare×4, ml.mlp, declare×3, ml.attention) — so it can only prove the SPLIT, never the MERGE. Added `build_two_mlps` (all 6 inputs declared UP FRONT → two ADJACENT ml.mlp ops) so the merge-on-adjacent is provable (a never-merging greedy-grow MUST be able to fail — the frontier-gate rule). GATE `test_band24_gate.cpp "ceir 29a-2"` (#818 MSVC / #871 gcc, device-free): (1) MERGE — 2 adjacent mlps + `claims_subgraphs=true` → subgraphs_of(0)==1, subgraph_size==2, both share the id; (2) DISCRIMINATOR — same, `claims_subgraphs=FALSE` → subgraphs_of(0)==0, both subgraph==-1 (still claimed_by==2); (3) SPLIT — build_band24 + `claims_subgraphs=true` claiming BOTH kinds → subgraphs_of(0)==2, distinct ids, each size 1 (the declares split); (4) DEFAULT PINNED — coopvec (default false) → mlp per-op(-1)+attention fallback, subgraphs_of==0; (5) MONOTONIC ACROSS BLOCKS — an mlp in EACH of two func bodies (`build_two_blocks`), claims_subgraphs=true → subgraphs_of(0)==2 + distinct ids (a per-block-RESET counter would give 1 — the monotone-watermark scar made FALSIFIABLE; advisor-caught: check (3)'s distinct-ids was on ONE block, so it couldn't catch a reset). Built MSVC win-debug + WSL linux-gcc-debug (`-Werror` — greedy-grow has no switch, inert as predicted); 29a-1/29a-2 PASS both compilers + partition regression 22/22 both; tidy 3 files clean. ⛔ PROCESS SCAR: the 29a-2 test name lacks "partition" ("subgraph run") ⇒ `-R partition` built-but-DIDN'T-RUN it (the count omitted #818) — caught it, ran `-R 29a`; a new test in a band must share the blast-radius regex substring OR you run both regexes. NEXT = **29a-3** (~~transfer/sync insertion at subgraph boundaries; 29a-3 must VALIDATE `memory_domain` via `is_memory_domain` before comparing domains — a typo is a typed reject; a fallback op BETWEEN two runs of the SAME provider [run·CKIR·run] inserts TWO boundary transfers, not one — the -1-singleton + fresh-id-after-reset already carries this~~ **[SUPERSEDED at 29a-3a → these two are 29b SEEDS, kept struck: the transfer is a plan STAGE with NO consumer until an executor crosses a boundary (`StageKind` has no Copy today + the planner is partition-blind), so it LANDS at 29b — the `is_memory_domain` validation of the domain strings and the run·CKIR·run double-transfer both apply at 29b]** + the `transform.assign_provider` authored-pinning directive). | §102 |

**29a-3a ✅ 2026-09-05 (the partitioner honours an AUTHORED PROVIDER PIN — a preference among claimers, NEVER a force; device-free; the `transform.assign_provider` directive+loader that DRIVES it is 29a-3b)**: ⭐ TWO advisor RECONCILES reshaped 29a-3 at entry. **(1) the census-seed's "REAL inserted TRANSFER/SYNC at each provider boundary" (29-0 point 3) is NOT a 29a op — it's a plan STAGE, and it has NO CONSUMER until a provider actually EXECUTES across a boundary.** Grep-confirmed: `plan_tensor_pipeline` is PARTITION-BLIND (no MlPartition/provider ref); `StageKind` has NO Copy/Transfer (the 10 kinds are all compute — Gemm/Fft/Reduce/…), so adding one would fire `-Werror=switch` across every emitter/oracle/`plan_sig`/CUDA consumer with a body no executing producer feeds. The only executor today is the CKIR fallback; the second executor (CUDA Graphs, output in a CUDA `memory_domain` the fallback reads) is 29b. ⛔ the no-speculative rule (26b-4) WINS: **the transfer stage lands at 29b** (trigger: the first executing provider whose `memory_domain` ≠ the fallback's), where its emitter/oracle/sig cases land against a REAL producer; 29c ASSERTS on it, 29b CREATES it. **So 29a-3 = AUTHORED PINNING only** — a real device-free slice with a real consumer (the partitioner honouring the pin over first-available). **(2) SUBSLICED 29a-3a/29a-3b** (the pin is two mechanisms): **29a-3a (this)** = the partitioner PIN mechanism; **29a-3b** = the `transform.assign_provider {provider}` directive + loader (opgen from the TOML, its own tick). ⭐ the pin names `MlProvider::name` (equality), NOT the inert `provider_class` (two Gpu-class providers make a class-pin ambiguous — 29c HAS two; `provider_class`'s consumer is 29b's `IExecutionProvider`). `partition_ml` GAINS `crd::i32 pinned = -1` (a span INDEX; the loader maps name→index at 29a-3b): `assign` tries `pinned` FIRST, but only if it is available AND `advertise`s the op — a pinned provider that REFUSES an op falls through to first-available (⛔ a pin is a PREFERENCE among claimers, never a force — the "gelu on a relu kernel" scar); an unavailable or out-of-range pin ⇒ unpinned. The default `-1` keeps EVERY existing caller (24c/24z/29a-1/29a-2) byte-identical. GATE `test_band24_gate.cpp "ceir 29a-3"` (#819 MSVC / #872 gcc, device-free): (a) DISCRIMINATOR — two providers BOTH claiming the mlp, SAME module: unpinned→index 0 (first-available, the 24c invariant), pinned=1→index 1 (IDENTITY on the WINNING index, not the class — both Gpu); (b) PREFERENCE-NOT-FORCE — pin coopvec on build_band24: the mlp goes to the pin, the attention it REFUSES goes to the OTHER claimer (index 1), never forced onto 0; (c) CAPS — an unavailable pin is skipped, first-available takes over; (d) OUT-OF-RANGE pin = no-op (the partitioner is defensive; the loader rejects unknown names at 29a-3b). Built MSVC win-debug + WSL linux-gcc-debug (`-Werror` — the pin has no switch, inert); 29a-1/2/3 PASS on BOTH compilers, and `-R partition` (22/22) + `-R expand` (MSVC 17/17, gcc 14/14 — the 3 DX12 device tests self-skip on Linux) ran GREEN on both; tidy 2 files clean. ⛔ the DUPLICATE-NAME guard (a pin by name over two same-named providers is ambiguous) belongs at 29a-3b (the loader resolves name→index) — a pinned INDEX here is unambiguous by construction, so no guard this slice (NAMED-forward to the loader). ✅ LANDED 29a-3b-1 as `ProviderPin::DuplicateProviderName` (a pairwise span scan in `provider_from_transform`). ⛔ UNCOMMITTED-BATCH (adds): `partition_ml.hpp`/`partition_ml.cpp` (the `pinned` param) + `test_band24_gate.cpp` (the 29a-3 gate). NEXT = **29a-3b** (the `transform.assign_provider {provider: string}` program-global directive in CEIR-27's dialect [copy the `transform.fuse` mold, opgen from the TOML] + a `provider_from_transform` loader mapping the string→a `providers` index by name equality, with an UNKNOWN-name TYPED reject + a DUPLICATE-name guard; the key is `name`, `provider_class` stays inert). | §69 §102 §71 |

**29a-3b-1 ✅ 2026-09-05 (the AUTHORED-PIN loader: transform.assign_provider {provider} → an MlProvider span INDEX partition_ml honours; the DIRECTIVE + loader MECHANISM, in-memory-gated; the committed .ceir asset is 29a-3b-2)**: the authored SOURCE of 29a-3a's `pinned` index. **DIRECTIVE** — added `transform.assign_provider` (a required string `provider`) to `transform.ceirop.toml` (the transform.fuse mold: NO results/operands/effects, NOT Pure — config metadata, Pure would make two pins CSE candidates) + REGENERATED via `python tools/ceir_opgen/ceir_opgen.py` (⛔ NO per-dialect flag — it rewrites EVERY dialect's gen/*_ops.{hpp,cpp,json,md} + the gen-smoke test; the `crd-ceir-opgen-drift` ctest guards generated==TOML). `find_transform_misuse` GAINS `seen_assign` (a third module-wide bool, the fuse/share mold) → a second assign_provider is DuplicateDirective. **LOADER** `provider_from_transform(const Context&, transform_mod, providers)` → `ProviderPin{index, op, kind}` — lives in **partition_ml (the MlProvider seam), NOT tensor_pipeline**, so the tensor planner stays PARTITION-BLIND (no MlProvider dep). ⛔ matches op NAME (I6, never op.kind — the advisor caught that plan_options_from_transform's `op->kind()==fuse_k` is the I6 anti-pattern its own header warns of; the loader is `const Context&`, interns nothing) + reads `provider` DEFENSIVELY (valid + String + non-empty). REJECT taxonomy: `None` (no directive → no pin), `Resolved` (exactly one name match → the span index), `UnknownProvider` (unknown/empty/invalid name — NEVER a silent -1: a typo'd pin must ERROR, not silently run first-available), `DuplicateProviderName` (⛔ a PAIRWISE scan over the WHOLE span FIRST — a shared name is a span defect ambiguous by construction, reported before resolving whichever name the pin targets; the advisor: counting only the pinned name misses a span broken on an UNtargeted duplicate). GATE `test_band24_gate.cpp "ceir 29a-3b"` (#876 MSVC / gcc, device-free, transform module built IN MEMORY via build_assign_provider): (a) RESOLVE + END-TO-END (coopvec→index 1, then partition_ml pins the mlp to 1, attention escapes to 0 — the resolution AND the pin), (b) DISCRIMINATOR (same span, "native_graph_stub"→index 0), (c) UnknownProvider (op set), (d) None (no directive), (e) DuplicateProviderName ({coopvec,coopvec}), (f) empty-name + empty-span → UnknownProvider, (g) two directives → find_transform_misuse DuplicateDirective at the SECOND op. GREEN MSVC + gcc: `-R 29a` 4/4, `-R transform` (MSVC 56 / gcc 54 — 2 DX12 self-skip; gen-smoke now REQUIRE(schemas==3U) + builds assign_provider via attr_string), `-R opgen` 2/2 (drift + validator clean), `-R 26c` (MSVC 9 / gcc 8 — the 1 diff is the DX12 26c-2 bit-exact test #4876, self-skips on Linux; `-N` name-diff CONFIRMED both discover #549, NOT a discovery gap). Tidy clean (transform.cpp, partition_ml.cpp, test_band24_gate.cpp). ⛔ OPGEN-REGEN SCAR (advisor → memory [[feedback_opgen_regen_is_all_dialects_diff_HEAD_lies_in_an_uncommitted_batch]]): the regen ALSO synced `resource_ops.*` (declare/import Pure→Allocate, the 26c/26z CSE-no-merge fix) — `git diff HEAD` showed it as if THIS slice made it, but HEAD is c35548a (pre-26c) so the diff surfaces every prior-band TOML edit; discriminated NOT by git-diff but by rebuild+run (`-R 26c` GREEN both compilers = Allocate semantics correct, declares not merged; opgen-drift clean). ⛔ DRIVE-BY ASCII FIX (surfaced by this run): #549 (a 26c CSE test) FAILED under Windows `ctest -R 26c`, PASSED run-direct, and PASSES under ctest after an ASCII rename; the ONLY 2 tests in the suite with a non-ASCII em-dash `—` in their NAME (test_cse.cpp #549, test_tensor_pipeline.cpp 26f-3c) were both renamed `—`→`--` per the hard ASCII-test-names rule. ⚠ MECHANISM NOT ISOLATED (advisor): the em-dash filter round-trip (the ASCII scar) is the likely cause, but a stale ctest test-list (catch_discover_tests regenerates at BUILD time, and the pre-rename exe predated the seen_assign/regen rebuild) is an alternative not ruled out — the rename is correct regardless (the rule is the rule). ⛔ UNCOMMITTED-BATCH (adds/mods): `transform.ceirop.toml` + regenerated `gen/transform_ops.*`/`gen/transform.ops.*`/`test_transform_gen_smoke.cpp` (3 ops); `transform.cpp`/`transform.hpp` (seen_assign); `partition_ml.hpp`/`.cpp` (ProviderPin + provider_from_transform); `test_band24_gate.cpp` (the gate); the em-dash renames in `test_cse.cpp` + `test_tensor_pipeline.cpp` (⚠ `resource_ops.*` + the other dialects' gen are 26c/27/28-batch content the regen refreshed, NOT a 29 semantic change). NEXT = **29a-3b-2** (the COMMITTED `.ceir` assign_provider asset, mirroring `schedule_fuse.ceir`: bootstrap-via-print → parse-load through the reader → provider_from_transform resolves it → anti-drift through the printer [the 28 committed-asset-anti-drift scar], never file-bytes). | §71 §102 |

**29b-1 ✅ 2026-09-05 (the multi-stage TENSOR PIPELINE on a REAL CUDA device — the FOURTH backend for execute_tensor_pipeline; pays the 24z-ledgered CUDA-multi-stage debt; BIT-EXACT; Win RTX + WSL2 RTX)**: an expanded ml.mlp (gemm→relu→gemm, fuse=false) planned + executed as ONE CUDA submit, the intermediate device-resident, BIT-EXACT vs a CPU float MLP oracle. ⭐ CUDA VIABLE HERE (the census go/no-go, confirmed FIRST: the 5 existing [cuda] tests pass, 172 assns, real RTX). NEW `tests/ceir-gpu-cuda/test_ceir_pipeline_cuda.cpp` — file-local `resolve_stage` (Gemm→emit_contract_cuda push{M,K,N,1}/16B/ls256; VizDispatch(relu.ckir)→emit_compute_kernel_cuda, sentinel local_size bound to M·hidden=32; Elementwise→emit_elementwise_cuda push{n}/4B) + `run_mlp_module` (the portable dev/up/rb copy pattern, CUDA barrier a documented no-op) + the SHARED `execute_tensor_pipeline` — the MIRROR-INLINE house pattern (DX12:399-403), CudaComputeContext-typed. ⭐ BIT-EXACT not a tolerance (the GOLD gate): fmad=false on every pipeline + a CPU oracle that MIRRORS emit_contract_cuda's arithmetic (`prod=a*b; acc=acc+prod;` sequential-k, no MSVC cross-statement FMA) ⇒ device == oracle EXACTLY (the Vk 24b-4 leg tolerates GLSL fma; CUDA earns bit-exact — the 13z fmad=false discipline). GATE `"ceir 29b-1"`: device-free plan IDENTITY (3 stages [gemm,relu,gemm] — a silent stage drop can't pass); the bit-exact readback; n_alloc==non-alias buffer count ⛔ ~~(26f aliasing live on CUDA)~~ **[STRUCK → the assertions were STRENGTHENED post-advisor: this MLP has 8 buffers / 0 aliases; 26f aliasing does NOT engage — see the CORRECTION]**. ⛔⛔ TWO REAL BUGS FIXED (the CUDA multi-stage path was NEVER tested — the 24z debt surfacing): **(1) emit_contract_cuda push ABI** — it emitted FOUR scalar params `(…,M,K,N,nbatch)` but `CudaComputeContext::dispatch` passes the whole push_size blob as ONE cuLaunchKernel arg ⇒ K/N/nbatch UNBOUND ⇒ garbage dims ⇒ OOB ⇒ SIGSEGV. FIXED: emit ONE `uint4 dims` param (16B, matches the single-blob push). ⛔ ~~its ONLY consumer is this test (push_size was always 0 before → latent)~~ **[STRUCK — advisor caught: FALSE. `emit_contract_cuda` is a SHARED engine emitter with FOUR launch sites — see the POST-ADVISOR CORRECTION below.]** **(2) the CUDA test target lacked `CRD_REPO_DIR`** — every OTHER test target defines it; the CUDA target had never loaded an asset, so the `#ifndef` fallback "." was used ⇒ relu.ckir resolved only when CWD==repo root (Windows-run LUCK) and FAILED under WSL ctest (CWD==build dir) — the exact Win-green/WSL-red split (diagnosed by stderr-instrumenting resolve_stage: path=`./assets/ckir/relu.ckir` NOT GOOD). FIXED: `target_compile_definitions(... CRD_REPO_DIR="${CMAKE_SOURCE_DIR}")` (the house pattern). GREEN (run via the `[cuda]` Catch2 TAG, NOT `ctest -L` — no `cuda` label exists): `[cuda]` 6 cases / **191 assns** (188 + the 3 strengthened topology checks, see CORRECTION) on **Windows RTX (win-debug)** AND **WSL2 RTX (linux-gcc-debug, NON-ASan)** — both REAL devices; ⛔ the WSL Linux-ASan config self-SKIPS (cuInit fails under ASan, the CMakeLists scar), so "WSL2 RTX" means the non-ASan gcc config, NOT the ASan sweep. Tidy clean (test file). ⛔ DEFERRAL LEDGER (each w/ trigger): fused GemmRelu on CUDA (emit_contract_cuda does NOT unwrap the Max epilogue like emit_contract_hlsl → fuse=false here; a fused-CUDA consumer is the trigger) · attention on CUDA (no emit_permute_cuda for transpose) · Reduce/Broadcast CUDA pipeline stages (⭐ emit_reduce_cuda EXISTS [ckir_cuda.hpp:707 — the census-agent's "missing" was WRONG]; emit_broadcast_nd_cuda absent). ⛔ UNCOMMITTED-BATCH (adds/mods): `test_ceir_pipeline_cuda.cpp` (NEW + the strengthened topology asserts) + `tests/ceir-gpu-cuda/CMakeLists.txt` (the TU + CRD_REPO_DIR) + `engine/kir/include/crd/kir/ckir_cuda.hpp` (emit_contract_cuda uint4 push fix) + **`engine/kir-cuda/src/backend_cuda.cpp`** (POST-ADVISOR: 2 launch sites → the uint4 blob) + **`engine/kir-hip/src/backend_hip.cpp`** (POST-ADVISOR: the mirror site). ⛔⛔ **POST-ADVISOR CORRECTION (same tick, mandated post-impl review):** the advisor flagged that changing a SHARED engine emitter's kernel signature must audit EVERY launch site, not the ceir-gpu dispatch alone. GREP `engine/`: `emit_contract_cuda` has **FOUR** launch sites, not one. The uint4 change (correct for the single-blob ceir-gpu `CudaComputeContext::dispatch`) BROKE the three LEGACY sites that passed FOUR separate scalar `kernelParams` — matching the OLD `(…,M,K,N,nbatch)` signature: **(i)** `backend_cuda.cpp` `run()` non-tiled Contract (`&d0;&d1;&d2;&d3`), **(ii)** `backend_cuda.cpp` `time_contract_schedule()` naive (`&pm;&pk;&pn;&pb` — the AS-1b autotuner's baseline timer), **(iii)** `backend_hip.cpp` `run()` Contract. ⛔ EMPIRICAL PROOF: the FIRST rebuild+run of `crd-kir-cuda-tests` FAILED — `test_autotune_cuda.cpp:128 REQUIRE(nr.ok)` false (the naive baseline), and — the diagnostic tell — EVERY test AFTER it self-skipped "no CUDA device available" (the garbage-dims OOB launch put the CUDA context into a STICKY error state). FIXED all 3: pack `{M,K,N,nbatch}` into ONE 16-byte `crd::u32[4]` arg (align-4 is fine — `cuLaunchKernel`/`std::memcpy` byte-COPY the param, no aligned deref; the tiled/fused sites use `emit_contract_tiled[_fused]_cuda`'s 3-scalar `M,N,K` — UNCHANGED). ⭐ RE-RUN GREEN both devices: `crd-kir-cuda-tests [cuda]~[cublas]` **81504 assns (Win RTX) / 81488 (WSL2 RTX)** — the 16-assn delta is the autotuner sampling a device-dependent candidate count, all oracle-correct; the skips are gone (context no longer corrupted). ⛔ HIP (`backend_hip.cpp`) is the EXACT MIRROR fix but has **NEVER been COMPILED** by anything on this box (no ROCm toolkit on Windows OR WSL — tidy reports it UNGATED, `hip/hip_runtime.h` not found; not a "compiles-but-untested" — the ADR-0098 Part-C real-AMD validation is its FIRST compile, not its first run). The edit is syntactically trivial (a `crd::u32[4]` from four `crd::u32` locals, decaying to `void*` like the sibling scalar params) — low risk, but stated precisely so Part-C expects a first build. ⛔⛔ **ASSERTION CORRECTION (advisor D — the check was a tautology):** `n_alloc==non_alias` compared two counts BOTH derived from `alias_of<0` ⇒ near-vacuous. STRENGTHENED to a topology IDENTITY — measured actuals (Catch2 expansion): the unfused MLP has **8 buffers** (x,W1,W2 ExternalIn + a per-gemm C-accumulator + h1,a1 Intermediates + out Output) and **0 aliases**, so `CHECK(buffers==8U)`+`CHECK(aliases==0U)`+`CHECK(non_alias==8U)`+`n_alloc==non_alias`. ⛔⛔ MECHANISM READ, NOT INFERRED (advisor caught a 2nd-pass error: I first wrote "26f aliases only EQUAL-byte buffers" — that was itself an unverified guess replacing the struck ones). The ACTUAL `assign_shared_storage` (tensor_pipeline.cpp:176-259): sharing is ON (`share_intermediate_storage` DEFAULTS true; this test leaves it default) and the free-list pass RUNS, but a landlord needs only `bytes >= tenant.bytes` then GROWS to max (NOT equal-bytes), and only `Intermediate`-ROLE buffers share. It finds NO tenancy here: `out` is `Output` (func-return-pinned — never shares), and the two real Intermediates h1,a1 have NON-disjoint lifetimes (relu produces a1 in the SAME stage it last-reads h1 ⇒ the strict `free_at<produce` guard, the in-stage-hazard rule, forbids it). ⛔ the row's original "26f aliasing live on CUDA" was FALSE for this plan — struck above; the alias-materialization BRANCH of `run_mlp_module` (`bufs[i]=bufs[alias_of]`) is therefore **UNEXERCISED** on CUDA (CORRECT trigger: a DEEPER chain with a disjoint-lifetime Intermediate pair — e.g. a 2-hidden-layer MLP whose first hidden output frees strictly before a later Intermediate is produced — or the 29c two-class transfer; NOT `d1==d2`/equal-bytes). `backend_cuda.cpp` tidy-clean; `test_ceir_pipeline_cuda.cpp` tidy-clean. NEXT = **29b-2** (the CUDA-GRAPHS provider: CAPTURE this N-dispatch pipeline into ONE `cudaGraph` [stream capture around the submit], launch as one, BIT-EXACT vs this fallback BY CONSTRUCTION; + the boundary-transfer plan STAGE lands with its real producer; + the IExecutionProvider compile→plan APPEND forwarded from 29a). | §70 §102 §69 |

**29b-2a ✅ 2026-09-05 (the CUDA-GRAPHS capture MECHANISM — 29b-1's N-dispatch pipeline captured into ONE cudaGraph, instantiated ONCE, launched MANY; BIT-EXACT vs the fallback + deterministic replay; the runnable §70 native-graph provider's core)**: the first 29b-2 subslice (advisor: mechanism first, the IExecutionProvider seam second — a proven capture path gives 29b-2b a real implementor to append the pure-virtual against). **(1) API on `CudaComputeContext` (CUDA-ONLY, NOT `IComputeContext` — the 29-0 census "no cross-backend mirror; portability lives in the device-free partitioner, the runnable proof is CUDA-only"):** `begin_capture()` (cuStreamBeginCapture on the SAME stream, returns the SAME recorder ⇒ the SAME `execute_tensor_pipeline` RECORDS its dispatches instead of launching), `end_capture()` → an owned `CudaGraph` handle, `launch(const CudaGraph&)` (cuGraphLaunch + ev0/ev1 bracket ⇒ `last_gpu_ms()` reports graph time via the SAME accessor 28's harness reads). **(2) ⭐ SPLIT end+launch (advisor — the win the slice EXISTS to measure):** `end_capture()` INSTANTIATES ONCE (`cuGraphInstantiateWithFlags`); `launch()` reuses the exec — instantiate-once/launch-MANY. A `submit_captured()` that re-instantiated per call would make 29z's "N-dispatch vs 1 graph" bench measure capture+instantiate overhead and LOSE. **(3) `CudaGraph` = opaque RAII handle (house `ComputePipeline` pattern — the public header stays cuda.h-free; concrete `CudaGraphImpl` in the .cpp destroys `CUgraphExec`+`CUgraph`):** `valid()` + `node_count()`. **(4) ⛔ ROBUSTNESS (advisor):** capture the DISPATCHES ONLY — uploads run via `begin()/submit_and_wait()` BEFORE `begin_capture()` (a captured `cuMemcpyAsync` would re-run per launch); `CU_STREAM_CAPTURE_MODE_THREAD_LOCAL` (GLOBAL lets ANY thread's unsafe CUDA call invalidate the capture); `end_capture()` returns an INVALID handle (never a partial graph) on `cuStreamEndCapture` _INVALIDATED/_UNJOINED or an instantiate failure — the gate `REQUIRE(graph_ok)` before comparing (node-count alone passes if capture started fine then a late dispatch invalidated it); `cuGraphInstantiateWithFlags` called DIRECTLY (11.4+/12.x/13.x-stable), NOT the version-dependent `cuGraphInstantiate` macro (Win CUDA 13.3, WSL CUDA 12.0 — verified both). **(5) GATE `"ceir 29b-2a"`** (new TEST_CASE): the SAME plan as 29b-1 (via the EXTRACTED `build_mlp_plan` + `fill_mlp_data_and_oracle` — the 28b `build_mlp_payload` DRY precedent, both gates plan the IDENTICAL program) run THREE ways — FALLBACK (N-dispatch) / GRAPH (captured, launched once) / REPLAY (same exec, launched again). ⛔ `REQUIRE(graph_ok)` (capture valid) + `CHECK(node_count==3U)` — IDENTITY: one node per stage dispatch (gemm/relu/gemm; barriers are CUDA no-ops, no inter-stage copies) so an empty/partial capture can't pass; ⭐ BIT-EXACT BY CONSTRUCTION: `gr_out[i]==fb_out[i]` (graph == fallback) AND `gr_out[i]==oracle[i]` (both == the CPU MLP oracle) — same fmad=false kernels, same stream-capture dependency order, no FP reorder; REPLAY `gr_replay[i]==gr_out[i]` (launch-many is bit-identical — the property 29z benches). GREEN: **Windows RTX (win-debug, CUDA 13.3) AND WSL2 RTX (linux-gcc-debug, CUDA 12.0)** — `[cuda]` 7 cases / 224 assns BOTH (graph instantiation works on the WSL passthrough driver too, driver-version-sensitive per advisor); tidy clean (cuda_compute_context.cpp + test). ⛔ UNCOMMITTED-BATCH (adds/mods): `engine/gpu-context-cuda/include/crd/gpu/cuda_compute_context.hpp` (CudaGraph + the 3 capture methods) + `engine/gpu-context-cuda/src/cuda_compute_context.cpp` (CudaGraphImpl + impls) + `tests/ceir-gpu-cuda/test_ceir_pipeline_cuda.cpp` (build_mlp_plan/fill_mlp_data_and_oracle extraction + run_mlp_captured + 29b-2a). ⛔ POST-ADVISOR (mandated review): **(a) begin-capture guard** — `begin_capture()` now TRACKS `cuStreamBeginCapture`'s success in `m_capturing`; `end_capture()` refuses to call `cuStreamEndCapture` on a non-capturing stream. A failed begin (a prior invalidated capture never ended, or an already-capturing stream) would otherwise run the dispatches EAGERLY then hand back a bogus graph — the test still caught THAT via `graph_ok`, but the guard is the state a 29b-2b subgraph-capture bridge needs (re-verified Win 224 assns + retidy). **(b) WSL2 identity CONFIRMED explicitly** (not inferred from the matching aggregate count): the `29b-2a` case ALONE runs **33 assns / 1 case PASSED** on WSL2 RTX (a device skip returns early with far fewer). NEXT = **29b-2b** (⛔ ~~the `IExecutionProvider` compile→plan pure-virtual APPEND + a CUDA-Graphs bridge that implements it, wrapping this capture mechanism as an execution provider~~ **[STRUCK at 29b-2b — the seam-layering reconcile: `IExecutionProvider` is the SCALAR reference executor (`ExecResult`=i64), a tensor provider does not fit it; the tensor "compile→plan" IS `plan_tensor_pipeline` and a launch-graph provider is a plan LAUNCH-MODE, not an IExecutionProvider implementor — see the 29b-2b row]**; the ⛔ boundary-transfer plan STAGE stays forwarded to **29c** — 29b is ONE CUDA memory domain, no real cross-domain transfer to insert/assert yet). | §70 §102 §69 |

**29b-2b ✅ 2026-09-05 (the CUDA-Graphs provider DESCRIPTOR + the SEAM-LAYERING reconcile — a LAUNCH-GRAPH provider is a plan launch-mode, NOT an IExecutionProvider implementor; device-free)**: closes the 29b-2 subslices. ⛔⛔ THE RECONCILE (advisor, before any code — the forwarded "IExecutionProvider compile→plan APPEND" was based on an incomplete view; reading `exec.hpp` disproved it): **`IExecutionProvider` is the SCALAR reference executor** — `execute()` returns `ExecResult{Array<i64>}` (the §118 Interpreter: arith/core/func/async/task, integer-only). A TENSOR provider (float gemm/relu) does NOT fit that return. So there is **NO compile→plan pure-virtual on `IExecutionProvider`** at 29b: the "compile→plan" for a tensor provider IS **`plan_tensor_pipeline`** (crd-ceir-gpu — it already produces the reusable, provider-AGNOSTIC compiled form the 29b-2a `CudaGraph` handle wraps), and a **LAUNCH-GRAPH provider (CUDA Graphs) is a plan LAUNCH-MODE** (`begin_capture`/`end_capture`, 29b-2a), not a provider that claim-and-replaces. Inventing a second `CompiledPlan` type in crd-ceir would be the `is_memory_domain` drift the 29a-1 reconcile caught. ⭐⭐ TWO KINDS OF PROVIDER, one word (the finding that collapsed the design): **SEMANTIC-ENGINE** (coopvec — a native fused kernel that REPLACES the op; `apply_partition` leaves the claimed op in place un-expanded) vs **LAUNCH-GRAPH** (cuda_graphs — captures the CKIR-EXPANDED gemm/relu/gemm dispatches into ONE cudaGraph; its caller NEVER calls apply_partition — it EXPANDS then wraps). The partitioner modeled only the semantic-engine kind (per-op claim, leave-in-place); a launch-mode doesn't partition anything, it applies to whatever the plan produced. **(1) DESCRIPTOR** a `cuda_graphs` `MlProvider` row (test helper) + `cuda_graphs_can_claim` (partition_ml.{hpp,cpp}): op is ml.mlp, input + >=2 weights, activation==relu (⛔ the specialized-kernel scar — emit_contract_cuda has NO fused-GemmRelu unwrap, a gelu claim is a silent wrong-function), input/weights/output Float+rank-2; ⛔ NO coopvec uniform-hidden/[1,1024] clamp (the CKIR gemm is general-dims). `provider_class=Gpu`, `memory_domain="device_local"`, `determinism=`**`BitExact`** (STRONGER than coopvec's DeterministicWithinTarget — the correctly-rounded fmad=false CUDA pipeline bit-matches the CPU reference, 29b-1/29b-2a; a fused kernel may reassociate), `claims_subgraphs=true`. So the partition can NAME it (29c pins/compares against it). **(2) GATE** device-free `test_band24_gate.cpp "ceir 29b-2b"` (#878, MSVC + WSL gcc; `[partition]`): (0) IDENTITY determinism==BitExact + claims_subgraphs; (1) CLAIM+DISCRIMINATOR — build_band24 (relu mlp + attention): cuda_graphs claims ONLY the mlp as ONE subgraph (claimed_by==1, subgraphs_of==1, subgraph>=0), the attention FALLS BACK (no CUDA attention emitter — the real can't-claim); (2) MAXIMAL RUN — build_two_mlps: claims_subgraphs grows ONE run of 2 (subgraphs_of==1, subgraph_size==2 — the maximal-subgraph identity); (3) ⭐ LAUNCH-GRAPH != CLAIM-AND-REPLACE — FALSIFIABLE via a CONTRAST (advisor: expand_ml_ops never sees the partition, so its count alone is claim-independent — a tautology; the discriminating fact is that apply_partition HONORS the claim and expand_ml_ops does not). SAME module + SAME cuda_graphs claim, two paths: `apply_partition` expands ONLY the fallback attention → **expanded==1** (the CLAIMED mlp LEFT IN PLACE — the semantic-engine model), vs `expand_ml_ops` → **expanded==2** (the claimed mlp expands too — the launch-graph captures the EXPANDED dispatches). The 1-vs-2 gap IS the two-kinds distinction in code. The captured-graph BIT-EXACTNESS itself is the device gate **29b-2a** (already GREEN both RTX devices — this slice NAMES + claims what 29b-2a captures, no re-run). GREEN: MSVC #878 + WSL gcc #878 + `-R partition` **25/25 BOTH** compilers (the 25 includes unrelated NAME-substring matches — hesap/modwt/LayerStack; every ceir ml-partition case [24z + 29a-* + 29b-2b] is among them, all green — the 29a-2 substring-regex scar); tidy clean (partition_ml.cpp + test_band24_gate.cpp). ⛔ NO `PlanStage.provider` field this slice (26b-4 — a per-stage provider tag is 29c's need, when two classes share one plan with a transfer between; name-forward). ⛔ STRIKES-IN-PLACE (the reversed forwards): **provider.hpp:39** (the IExecutionProvider APPEND — struck to the scalar-seam reconcile), **partition_ml.hpp:160** (apply_partition's leave-in-place serves the semantic-engine kind only; the boundary transfer is 29c, not 29b), the **29b-2a NEXT** clause. ⛔ UNCOMMITTED-BATCH (adds/mods): `engine/ceir-gpu/include/crd/ceir/gpu/partition_ml.hpp` (cuda_graphs_can_claim decl + the :160 strike) + `engine/ceir-gpu/src/partition_ml.cpp` (the predicate def) + `tests/ceir-gpu/test_band24_gate.cpp` (cuda_graphs helper + the 29b-2b gate) + `engine/ceir/include/crd/ceir/provider.hpp` (the :39 strike). NEXT = **29c** (the TWO-CLASS boundary proof on ONE CUDA memory domain: cuda_graphs + the CKIR/stream fallback in ONE partition; ⛔ [29c-2 CORRECTED: the DEVICE fallback class is a `linalg.gemm`, NOT attention — no `emit_permute_cuda`, the 29b-1 DEFERRAL LEDGER; the (1) CLAIM+DISCRIMINATOR "attention falls back" above is the DEVICE-FREE partition sense, correct, but attention cannot RUN as the fallback on CUDA — see the 29c-2 row]; ⛔ ~~a REAL inserted boundary-transfer plan STAGE ASSERTED [`StageKind` gains a Copy/transfer + `PlanStage.provider`]~~ **[at 29c-1: `PlanStage.provider` DID land as the per-stage tag; the transfer STAGE is UNTRIGGERED on one domain — name-forwarded with the two-domain trigger, see 29c-1]**; PINNED + AUTO both proven; the CONSTRAINED provider-choice mode [struck-forward from 29a] — §102's own CKIR→engine→CKIR example). | §70 §102 §69 |

**29c-1 ✅ 2026-09-05 (the PARTITION-AWARE plan: `PlanStage.provider` + `plan_tensor_pipeline_partitioned` tags each stage by its ml op's provider; the boundary-transfer stage is UNTRIGGERED on one domain, name-forwarded; device-free)**: the first 29c subslice. ⛔⛔ THE ONE-DOMAIN RECONCILE (advisor, before code): the census 29c "a REAL inserted transfer/barrier ASSERTED" is **UNTRIGGERED, not degenerate** — the transfer's own trigger is "a provider whose memory_domain ≠ the fallback's"; cuda_graphs + the CKIR fallback are BOTH `device_local` on one CUDA domain, so it never fires. A `StageKind::Transfer` now = a value nothing produces (26b-4) ⇒ STRUCK from the census 29c line + the 29b-2b NEXT, name-forwarded WITH the two-domain trigger (the already-deferred cross-API interop). 29c's one-domain analogue of "the transfer landed" is the per-stage-capture BOUNDARY (`node_count == the claimed run's stages, not the total` — 29c-2). ⭐ THE 29b-2b TWO-KINDS INSIGHT PAYS OFF: a launch-graph provider does not partition OPS, it partitions the plan's STAGES into a captured run + a fallback remainder — so the load-bearing 29c question is "which stages get captured", and `PlanStage.provider` is that consumer. **(1) `PlanStage.provider`** (i32, APPENDED, default -1 = CKIR fallback). **(2) LINEAGE** — advisor's (a): `expand_ml_op` RAUWs + ERASES the source ml op (no breadcrumb; the partition's ml-op keys dangle post-expand). Added an `expand_ml_ops(ctx, m, lineage)` overload filling (each created op → the 0-based PRE-ORDER index of its source ml op) — an INDEX not a pointer (the source op is erased/could be address-reused); it aligns 1:1 with `MlPartition::assignments` (both walk ml ops in the same pre-order). ⛔ the created ops of one expansion are exactly the ops between `before=op->prev` and `after=op->next` (every `mk_*` does `insert_before(new, ml_op)`), captured by range-walk — no per-builder plumbing. **(3) `plan_tensor_pipeline_partitioned(ctx, m, alloc, partition, lineage, opts)`** (sibling of `_cached`): calls `plan_tensor_pipeline` then tags each stage `partition.assignments[lineage[stage.op]].provider` (a stage with NO lineage entry ⇒ -1 fallback). ⛔ POST-REVIEW HARDENING (advisor): a lineage index OUTSIDE the partition (`*idx >= assignments.size()`) is now a LOUD `PlanReject::PartitionLineageMismatch` (reject_op=nullptr — a KEY-property mismatch between two inputs, the TuneCacheLockedMiss mold), NEVER a silent -1 that would masquerade as "the fallback claimed more stages"; the new reject value carries a `plan_reject_name` case ("partition-lineage-mismatch") and PASSED the widen-enum audit (the sole switch consumer is `plan_reject_name`; gcc `-Werror=switch` clean, `.reject==` sites are equality compares). ⭐ METADATA-ONLY — stages/buffers byte-identical to the blind plan. Advisor's (b) CONFIRMED: `plan.stages` order == expansion order (insert_before ⇒ in place) ⇒ each ml op's stages are CONTIGUOUS. **(4) GATE** device-free `test_band24_gate.cpp "ceir 29c-1"` (#879, MSVC + WSL gcc; `[partition]`): build_band24 (relu mlp op0 + attention op1) → partition{cuda_graphs} (mlp→0, attn→-1) → `expand_ml_ops(m, lineage)` → `plan_tensor_pipeline_partitioned` → the mlp's stages are a contiguous provider-**0 PREFIX**, the attention's a contiguous **-1 SUFFIX** (split `k` found WITHOUT hardcoding; `0<k<total`; per-stage `provider==(s<k?0:-1)` — no interleave, no untagged stage); + METADATA-ONLY (`stages.size`/`buffers.size` == the partition-blind plan). ⛔ ADVISOR-HARDENED (post-review) into 3 SECTIONs off a shared `check_two_class` lambda: **(fused)** the original 0-prefix/-1-suffix on the DEFAULT plan (mlp collapses to GemmRelu stages); **(UNFUSED, `fuse_gemm_relu=false` — the 29b-1 config 29c-2 ACTUALLY captures)** each relu is a standalone `StageKind::VizDispatch` whose `compute.dispatch` op is created INSIDE the (before,after) expand range ⇒ IS in the lineage ⇒ still tags 0 ⇒ contiguity HOLDS — this proves the contiguous-run invariant on the captured config, not just the fused one, and is NON-VACUOUS (the mlp prefix carries ≥1 VizDispatch — else a relu reading -1 mid-mlp would split the prefix and fail the lambda); **(MISMATCH)** a short partition (1 assignment < 2 ml ops) ⇒ the attention's lineage index (1) is OOB ⇒ `PlanReject::PartitionLineageMismatch` + `reject_op==nullptr`. ⭐ FIRST attention to plan inside a PARTITION gate (26d-3c already planned an expanded attention device-resident — transpose/gemm/VizDispatch-softmax/gemm; here it rides the two-class tag as the -1 suffix, device-free). GREEN: MSVC #879 + WSL gcc + `-R partition` **26/26 BOTH** (25 + 29c-1; incl. unrelated substring matches); tidy clean (expand_ml.cpp + tensor_pipeline.cpp + test_band24_gate.cpp). ⛔ STRIKES-IN-PLACE: the census 29c "REAL transfer ASSERTED" (untriggered) + the 29b-2b NEXT transfer clause. ⛔ UNCOMMITTED-BATCH (adds/mods): `engine/ceir-gpu/include/crd/ceir/gpu/tensor_pipeline.hpp` (PlanStage.provider + plan_tensor_pipeline_partitioned decl + fwd-decl MlPartition + hash_map include) + `engine/ceir-gpu/src/tensor_pipeline.cpp` (the def + partition_ml include) + `engine/ceir-gpu/include/crd/ceir/gpu/expand_ml.hpp` (the lineage overload decl + hash_map include) + `engine/ceir-gpu/src/expand_ml.cpp` (the expand_ml_ops_impl refactor + the lineage overload) + `tests/ceir-gpu/test_band24_gate.cpp` (29c-1 gate + tensor_pipeline include). ⛔ the post-review `PlanReject::PartitionLineageMismatch` enum value + its `plan_reject_name` case ride the ALREADY-listed `tensor_pipeline.{hpp,cpp}` (no new file). ⛔ `partition_ml.hpp/.cpp` are UNCHANGED this tick (the batch reader should NOT look for a 29c-1 diff there — its edits are the pending 29b-2b `cuda_graphs_can_claim`/strike work). NEXT = **29c-2** (DEVICE): the test-side `run_mlp_captured` grows a per-stage predicate — capture ONLY `provider==cuda_graphs` stages, the fallback stages run before/after on the same stream; gate = the two-class plan executes BIT-EXACT vs the all-fallback run AND `node_count == the claimed run's stage count` (not the total — the one-domain analogue of "the transfer landed"). ⛔ do NOT touch `execute_tensor_pipeline`'s signature (per-stage capture is the test-side runner's job — the backend-agnostic run-half stays clean). ⛔ [29c-2 LANDED + CORRECTED THE FIXTURE: attention CANNOT be the CUDA fallback class — no `emit_permute_cuda` for transpose (the **29b-1 DEFERRAL LEDGER**, this doc), so the device fixture is a gemm→mlp→gemm SANDWICH, and "total" = 5 stages, NOT mlp+attention; see the 29c-2 row.] Then **29c-3** CONSTRAINED mode (a `provider_class` pin; two real classes now: cuda_graphs Gpu + the 29a-1 External stub). | §70 §102 §69 |

**29c-2 ✅ 2026-09-05 (the TWO-CLASS boundary on a REAL device — a partitioned plan CAPTURES ONLY the cuda_graphs mlp run into ONE cudaGraph while the flanking fallback gemms run eager on the same stream, crossing the capture edge BIT-EXACT; Win RTX 13.3 + WSL2 RTX 12.0)**: `PlanStage.provider` (29c-1) pays off on-device. ⛔⛔ SCOPE CORRECTION (advisor, before code — reading the file header disproved the forwarded framing): the earlier 29c-2 sketch (build_band24, attention as the fallback SUFFIX, `node_count==3 not the total-with-attention`) is **BLOCKED** — `resolve_stage` emits only Gemm/GemmRelu, Elementwise, VizDispatch(relu/viz .ckir); **attention/transpose CANNOT run on CUDA** (`emit_permute_cuda` absent — the **29b-1 DEFERRAL LEDGER** "attention on CUDA (no emit_permute_cuda for transpose)"; name-forwarded there, NOT re-ledgered). ⭐ THE FAITHFUL FIXTURE (advisor): a DEPENDENT SANDWICH `gemm(x,W0) → mlp_relu(·; W1,W2) → gemm(·,W3)` (dims 4/8/8/2/2, M·d1=32 = relu.ckir bound) — the two flanking `linalg.gemm`s are NOT ml ops ⇒ `partition_ml` never sees them (`assignments.size()==1`), `expand_ml_ops` never touches them, no lineage ⇒ provider **-1**; the mlp is claimed by cuda_graphs ⇒ its 3 expanded stages tag **0**. Stages come out `[gemm, gemm, relu, gemm, gemm]` tagged `[-1, 0, 0, 0, -1]`: a captured run FLANKED by fallback gemms with a REAL data dependency (x' feeds the mlp; y feeds z — the boundary this proves, vs an independent sidecar that proves only "two things ran"). fuse=false. **(1) `slice_plan(plan, lo, hi, alloc)`** (test-side) — copies ALL buffers verbatim (incl. `alias_of`) + stages `[lo,hi)`; the full buffer table keeps every bind index + `validate_tensor_pipeline`'s `n_buffers` honest. ⛔ `execute_tensor_pipeline`'s signature UNTOUCHED (the per-stage capture is the test runner's job). **(2) `run_two_class_captured`** — upload once → ALL-FALLBACK N-dispatch run of the whole plan (the bit-exact reference) → then TWO-CLASS: fallback PREFIX `[0,lo)` eager → `begin_capture` the claimed run `[lo,hi)` → `end_capture`/launch → fallback SUFFIX `[hi,end)` eager. The claimed run `[lo,hi)` is DERIVED by scanning `provider==0` (asserted `==[1,4)`), NOT hardcoded. ⛔ the inter-stage barrier `execute_tensor_pipeline` would place at the 3→4 split is DROPPED by the slice — and that is CORRECT: CUDA's `rec.barrier` is a documented no-op and same-stream order (eager prefix → cuGraphLaunch → eager suffix) carries the x'→mlp→z dependency (the load-bearing sentence). **(3) GATE** `test_ceir_pipeline_cuda.cpp "ceir 29c-2"` (#4822 Win / #4669 WSL): PLAN HALF (device-free, runs on CI without CUDA) — `stages.size()==5`, tags exactly `[-1,0,0,0,-1]`, the single 0-run scan `==[1,4)`, `assignments.size()==1`, `reject==None` (⭐ `PartitionLineageMismatch` UNTRIGGERED — every lineage index is 0 < 1). DEVICE HALF — `graph_ok`, `node_count==3` (⭐ IDENTITY: ONLY the claimed mlp run captured, NOT the total 5), runner's `[lo,hi)==[1,4)`, and ⭐⭐ two-class `z` **== all-fallback `z` == CPU oracle** ALL BIT-EXACT (fmad=false + sequential-k oracle chaining all 4 gemms + relu; a shared-alias bug can't hide — it would corrupt BOTH runs vs the oracle). ⭐ FALSIFIABILITY (advisor post-review): `node_count==3` alone can't tell "captured 3, flanking gemms ran" from "captured 3, flanking gemms silently skipped" (uninitialized device memory is often zero-ish and could accidentally match). The RESOLVE COUNT does — `run_two_class_captured` exposes `Resolver::n`: ALL-FALLBACK 5 + TWO-CLASS (prefix 1 + captured 3 + suffix 1) + REPLAY (prefix 1 + suffix 1) = **12** (`CHECK(n_pipes==12U)`): every fallback stage DISPATCHED through execute_tensor_pipeline, the captured run resolved ONCE, the two graph launches re-ran NOTHING (instantiate-once). ⭐⭐ **FIRST LIVE ALIAS MATERIALIZATION on the CUDA runner** — `plan_aliases==2`, honored by the runner (`n_aliases==plan_aliases`), the "DEEPER chain with a disjoint-lifetime Intermediate pair" the **29b-1 row's CORRECTION** named un-exercised. ⛔ READ, not guessed (the 29b-1 mechanism-read scar — the pre-code hypothesis "a1 aliases x'" was WRONG): the two tenancies are both Intermediate→Intermediate with tenant.bytes ≤ landlord.bytes (the 26f landlord-grows rule) — (a) **a1, the RELU OUTPUT** (a `resource.declare` the relu dispatch WRITES ⇒ plan-role Intermediate — ⛔ declares aren't always ExternalIn; a written-then-read declare gets Intermediate role, which is WHY it's eligible to tenant), [M,d1]=128B, reuses **x''s slot** (the leading `linalg.gemm` output [M,d0]=128B, dead after the mlp's first gemm reads it); (b) the mlp's output **y** [M,d2]=32B reuses **h1's slot** (the mlp's first-gemm output [M,d1]=128B, dead after the relu — the landlord grew). ⛔ verified by READING the plan: the plain gemm binds A,B,D only (C at beta=0 is UNBOUND, tensor_pipeline.cpp:477 — so buf[8] is NOT a gemm accumulator, it's the relu's write target a1). The gate LOCKS the topology (count==2 + the two (tenant_bytes,landlord_bytes) pairs + both Intermediate, the 29b-1 "8 buffers / 0 aliases" mold). ⛔⛔ REPLAY FINDING (advisor's replay design said "launch again + suffix, x' is stable" — EMPIRICALLY FALSE, the first run FAILED replay 8/8): with 26f storage-sharing LIVE the captured run DESTROYS its own input's slot (a1, the relu output, is WRITTEN into x''s slot during the captured mlp run — the (a) tenancy above), so x' is NOT persistent across launches — a faithful instantiate-once/launch-many must RE-RUN THE PREFIX (recompute x') before each relaunch. FIXED (re-run prefix + launch + suffix) → replay bit-identical. A real property: graph replay with aliased storage must re-establish the graph's inputs, not just its outputs' consumers. GREEN (post-hardening, all assertions): **Win RTX 13.3 #4822 + WSL2 RTX 12.0 #4669** (both real devices; the ASan config self-skips — cuInit-under-ASan scar), gcc compiled clean. Tidy clean (test_ceir_pipeline_cuda.cpp). ⛔ UNCOMMITTED-BATCH (adds/mods): `tests/ceir-gpu-cuda/test_ceir_pipeline_cuda.cpp` ONLY (the cuda_graphs_provider helper + build_two_class_plan + fill_two_class_data_and_oracle + slice_plan + run_two_class_captured + the 29c-2 gate + partition_ml/semantics/hash_map includes) — no engine change this tick. NEXT = **29c-3** (CONSTRAINED mode: a `provider_class` PIN discriminating TWO real classes in one span — cuda_graphs Gpu + the 29a-1 External stub; §102's CKIR→engine→CKIR example; device-free — the pin resolution, not a device run) [SPLIT at 29c-3a: the class-FILTER mechanism landed; the AUTHORED directive is 29c-3b]. Then **29z** (band-close: bench the N-dispatch fallback vs the 1-graph launch, ledger, scars, flip ✅). | §70 §102 §69 |

**29c-3a ✅ 2026-09-05 (CONSTRAINED provider-choice — a `provider_class` HARD FILTER composed with the 29a-3 pin; `provider_class` is now READ, no longer inert; device-free, MSVC + gcc)**: the mechanism half of 29c-3 (SPLIT per advisor — lock the filter-vs-preference decision in the tick whose gate can falsify it, BEFORE 29c-3b commits an authored surface to it). ⛔⛔ THE ONE DECISION (advisor): a class constraint is a **HARD FILTER, NOT a preference** — §102 "constrained provider-choice" is a PORTABILITY BOUNDARY ("run on a provider of THIS class or NOT AT ALL"), a different statement from the 29a-3 `pinned` (a preference that falls through). A filter that silently fell back to the wrong class is the graceful-wrong-answer trap `provider_from_transform` already warns against. So: the class restricts the ELIGIBLE-claimer set to `provider_class == constraint`; an op no eligible provider claims → **-1 fallback, NEVER another class**. It COMPOSES with `pinned` (filter OUTER): a pin to a provider the class excluded is IGNORED; an in-class pin is the preference within the filtered set. **(1) MECHANISM** `struct PartitionConstraint { bool has_class; ProviderClass provider_class; crd::i32 pinned; }` (partition_ml.hpp) folds the old trailing `crd::i32 pinned=-1` into ONE trailing `PartitionConstraint constraint={}` arg (24c's -1/`{}` invariant still reads as "no pin, no class"); `assign()` gains an `eligible()` predicate (available + advertise + `(!has_class || provider_class==constraint)`), pin-within-eligible then first-eligible. ⛔ `provider_class` is now READ — STRUCK the 29a-1 "INERT until 29a-2/3 read them" comment at partition_ml.hpp:41-42 in place (29c-3a reads it). `memory_domain`/`determinism` stay descriptor-only — ⛔ NO consumer reads them YET (the 29b-2b `BitExact` claim was an identity-check VALUE, not a consumer); a `DeterminismClass` constraint (e.g. "BitExact or fallback") is the natural NEXT filter axis, name-forward to 29z/30-0. **(2) THE EXTERNAL STUB** (new `external_stub(bool)` helper, NOT subgraph_stub with an override — its only delta is the class, a distinct name keeps the gate readable): `always_claims` + `ProviderClass::External` + `memory_domain="external"` (a REAL §24 vocab member, verified — context.cpp:2465) + `DeterminismClass::Unspecified` (an external bridge honestly does not know) + `claims_subgraphs=false`. **(3) GATE** device-free `test_band24_gate.cpp "ceir 29c-3a"` (#880 MSVC+gcc, `[partition]`) on build_band24 with `{cuda_graphs(Gpu)@0, external_stub(External)@1}` — BOTH claim the mlp; the attention (cuda_graphs REFUSES, external claims) is the asymmetry proving fallback-not-other-class. FIVE assertions on `assignments[i].provider` IDENTITY (not `claimed_by` counts): (1) UNCONSTRAINED → mlp→0, attn→1 (baseline + both stubs live); (2) `{Gpu}` → mlp→0, attn→**-1** (external filtered out, cuda_graphs refuses attn ⇒ FALLBACK — ⭐ the load-bearing assertion; a preference would have leaked to external); (3) `{External}` → mlp→1, attn→1 (cuda_graphs filtered out though FIRST); (4) `{Npu}` → both **-1** — ⛔ made NON-VACUOUS (advisor post-review) via a 3rd provider `npu_stub` (Npu-class, available, but `never_claims`): an in-class-but-REFUSING provider yields the fallback, NEVER a leak to the Gpu/External claimers that DO claim (without npu_stub this section passed under any impl — a count assertion in disguise); (5) COMPOSITION `{External,pin=0}` → mlp→**1** (the pin to the excluded Gpu provider is IGNORED — the filter is outer) and `{Gpu,pin=0}` → mlp→0 (in-class pin honoured) — the section that breaks if "pin-first-then-class" were built. GREEN: MSVC #880 + WSL gcc, `-R partition` **27/27 BOTH** (26 + 29c-3a; incl. unrelated substring matches). ⛔ CALL-SITE MIGRATION: 9 `pinned`-int call sites in test_band24_gate.cpp → `{.pinned = N}` (C++20 designated init, already house style — jobs::Config, register_op traits); the cuda/vulkan/expand_ml callers use the default `{}` (blast-radius COMPILE+LINK verified: `crd-ceir-gpu-cuda-tests` rebuilt clean on WSL against the new signature). Tidy clean (partition_ml.cpp + test_band24_gate.cpp). ⛔ UNCOMMITTED-BATCH (adds/mods): `engine/ceir-gpu/include/crd/ceir/gpu/partition_ml.hpp` (PartitionConstraint + sig change + the :41-42 INERT strike + docstrings) + `engine/ceir-gpu/src/partition_ml.cpp` (the class filter in assign + threaded through scan_region/partition_ml) + `tests/ceir-gpu/test_band24_gate.cpp` (external_stub helper + the 29c-3a gate + 9 pin-call-site migrations). NEXT = **29c-3b** (the AUTHORED directive: `transform.constrain_provider_class{class:string}` via opgen [the 29a-3b-1 mold, NOT Pure], a `constraint_from_transform` loader with an `UnknownProviderClass` typed reject [mirroring ProviderPinKind], `find_transform_misuse` gains it as a 2nd DuplicateDirective target, a committed `assets/ceir/constrain_provider_class_gpu.ceir` anti-drift-through-the-printer, and a composition test with the existing assign_provider_coopvec.ceir). ⛔ (advisor) the class-string vocab = the `ProviderClass` enum names LOWERCASED (`host`/`gpu`/`npu`/`media`/`external`); add a `provider_class_name()` mapper in `semantics.hpp` (grep first — reuse if one exists) so the directive parse + any future provider-descriptor print share ONE table. Then **29z** close. | §70 §102 §69 |

**29c-3b ✅ 2026-09-05 (the AUTHORED class constraint `transform.constrain_provider_class{class}` + the opgen C++-KEYWORD sanitizer it surfaced; TWO authored transform assets compose into ONE partition decision; device-free, MSVC + gcc)**: closes 29c-3 (the authored surface half of 29c-3a's mechanism). ⛔⛔ THE OPGEN DEFECT (surfaced first): `class` is the natural attr NAME (advisor: `provider_class` would collide semantically with the MlProvider field, reading as if the directive selected a *provider*), but opgen had NO C++-keyword sanitizer — it emitted `AttrId class` (builder param) + `AttrId class()` (accessor), UNCOMPILABLE. ⭐ FIX (advisor: fix the tool, not the name — a rename leaves the landmine armed for the next `default`/`template`/`register` attr): a `_cxx_ident(name)` helper (the C++20 keyword set incl. alternative-token operator words → append `_` iff reserved) applied at the **7 IDENTIFIER emission sites** (operand/result/attr accessor method names; operand+attr builder params; the `operands[]` initializer; the `set_attr` value arg) — the `attr("...")`/`set_attr(op,"...",…)` STRINGS keep the RAW name, so the authored surface stays `{class = "gpu"}` while the generated C++ is `class_`. A `-O`-proof drift lock (`if …: raise`, NOT a strippable `assert`) fails loudly if the keyword table is ever trimmed. ⛔ NO-OP PROOF (scoped precisely, advisor): pre/post-regen MD5 of the generated `*_ops.{cpp,hpp}` for ALL 22 non-transform dialects = BYTE-IDENTICAL (only `transform_ops.{cpp,hpp}` changed); the smoke/`.ops.json`/`.ops.md` emitters never consume `_cxx_ident`, so they're out of scope by construction. **(1) semantics.hpp** — `provider_class_name(ProviderClass)` (constexpr switch → host/gpu/npu/media/external) + `provider_class_from_name(StringView, ProviderClass&)` (constexpr decode-and-validate, out untouched on failure) + `provider_class_names_roundtrip()` `static_assert` drift lock (⭐ gcc compiled the constexpr `StringView::operator==` round-trip clean — the MSVC-constexpr-accepts-≠-gcc-accepts risk, retired). ONE table shared by the directive parse + any future provider-descriptor print. **(2) DIRECTIVE** `transform.constrain_provider_class{class:string,required}` in transform.ceirop.toml (the 29a-3b-1 mold — NO results/operands/effects, NOT Pure); `find_transform_misuse` gains `seen_constrain` (a 2nd DuplicateDirective target). **(3) LOADER** `constraint_from_transform(ctx, transform_mod)` (partition_ml.{hpp,cpp}) — resolves `class` REGION-RECURSIVELY (the `first_op_named` generalization of 29a-3b's `first_assign_provider`, now shared by both loaders) via `provider_class_from_name`; `ClassConstraintKind{None,Resolved,UnknownProviderClass}` — ⛔ NO `has_class` field on `ClassConstraint` (kind==Resolved IS has_class; a 2nd boolean is the drift trap). `compose_partition_constraint(cc, pin)` folds the two authored halves into ONE `PartitionConstraint` (filter OUTER, pin inner — the ONE place `has_class` and the loader `kind` can't disagree). **(4) COMMITTED ASSET** `assets/ceir/constrain_provider_class_gpu.ceir` (`module { ^bb0: transform.constrain_provider_class() {class = "gpu"} }`) — anti-drift-through-the-PRINTER vs the `build_class_module` oracle (never file-bytes; the 28 scar), roundtrip-stable. **(5) GATE** device-free `test_band24_gate.cpp "ceir 29c-3b"` (#881 MSVC + WSL gcc, `[partition]`), 5 sections: (a) `{gpu}`→Resolved/Gpu/op + ⭐ ALL FIVE ProviderClass names round-trip through the AUTHORED path (build directive w/ `provider_class_name(c)` → `constraint_from_transform` recovers exactly `c`, identity per class); (b) bogus/empty→UnknownProviderClass+op (never silent no-constraint — the graceful-wrong-answer trap), absent→None; (c) two directives→DuplicateDirective@second; (d) the committed asset — anti-drift + roundtrip + misuse-None + resolves→Gpu; (e) ⭐⭐ COMPOSITION — the FIRST time TWO authored transform assets (assign_provider_coopvec.ceir PIN + constrain_provider_class_gpu.ceir FILTER) parse-load, their loaders + `compose_partition_constraint` fold into ONE constraint partition_ml honours: on build_band24 w/ {coopvec(Gpu)@0, external_stub(External)@1} → `{Gpu,pin=0}` gives mlp→0 (in-class pin honoured) / attn→-1 (coopvec refuses, external FILTERED); the ⭐ DISCRIMINATOR — the SAME pin under an `{External}` filter → mlp→**1** (the Gpu pin is OUT of class ⇒ IGNORED, external claims both) — a pin-first-then-class impl fails this. §146 schedules-are-assets reaching the partitioner. GREEN: MSVC #881 (`-R partition` **28/28**) + WSL gcc #881 (opgen-drift + 3 transform-gen-smoke + 29c-3a + 29c-3b **6/6**; the gcc constexpr + `-Werror` switch + asset-slurp-cwd all clean). Tidy clean (test_band24_gate.cpp + partition_ml.cpp + transform.cpp + partition_ml.hpp + semantics.hpp). ⛔ STRIKE-IN-PLACE: partition_ml.hpp:138-139 (both the 29a-3b + 29c-3b "authored source lands in…" forward clauses — both LANDED). ⛔ UNCOMMITTED-BATCH (adds/mods): `tools/ceir_opgen/ceir_opgen.py` (`_cxx_ident` + the 7-site application + the raise lock) + `engine/ceir/generated/crd/ceir/gen/transform_ops.{cpp,hpp,ops.json,ops.md}` (regen — `class_`) + `engine/ceir/include/crd/ceir/semantics.hpp` (the mappers + static_assert) + `engine/ceir/ops/transform.ceirop.toml` (the 4th directive) + `engine/ceir/src/transform.cpp` (seen_constrain) + `engine/ceir-gpu/include/crd/ceir/gpu/partition_ml.hpp` (ClassConstraint/Kind + constraint_from_transform/compose decls + the :138-139 strike) + `engine/ceir-gpu/src/partition_ml.cpp` (first_op_named generalization + the 3 loaders) + `assets/ceir/constrain_provider_class_gpu.ceir` (NEW asset) + `tests/ceir-gpu/test_band24_gate.cpp` (build_class_module + the 29c-3b gate). ✅✅ **29c-3 COMPLETE** = 29c-3a (class-filter mechanism) + 29c-3b (authored directive + loader + asset + composition). NEXT = **29z** (band-close: bench the N-dispatch fallback vs the 1-graph launch → `docs/bench/`, the 29b-1 CUDA deferral ledger, scars [the opgen-keyword-sanitizer scar + the aliased-storage-graph-replay property], flip ✅). | §102 §69 §146 |

**29z-1 ✅ 2026-09-05 (the BAND-CLOSE BENCH — the CUDA-Graphs launch vs the N-dispatch fallback, BOTH RTX devices; + the `CudaComputeContext::enqueue` split it forced; SPLIT from 29z-2 close per advisor)**: the measured board the whole 29b/29c mechanism exists to justify. ⛔ SPLIT (advisor): 29z-1 = bench + board (this tick); 29z-2 = deferral ledger + scars + close doc + flip (next tick — the flip must come AFTER reading the numbers, not folded in). ⛔⛔ TWO ADVISOR-CAUGHT HARNESS ARTIFACTS (before any number was published): **(a) the sandwich CPU "loss"** — the first draft's graph arm was `run_range(prefix)`[submit+wait] + `launch(graph)`[submit+wait] + `run_range(suffix)`[submit+wait] = THREE CPU→GPU→CPU round-trips vs the fallback's ONE; it timed wait-latency ×3 (WSL 0.49×, Win 0.77× — the passthrough driver's higher per-wait latency confirmed the artifact). **(b) the GPU column** summed three event brackets for gr vs fb's one. ⭐ THE REAL DELIVERABLE (advisor: fix the tool, not the number): a `CudaComputeContext::enqueue(const CudaGraph&)` — a non-waiting `cuGraphLaunch` on the recording stream (the launch()-vs-enqueue split, the 29b-2a end_capture/launch split one level down), so a two-class pipeline records `begin → eager prefix → enqueue(graph) → eager suffix → submit_and_wait` as ONE submission / ONE wait / ONE event bracket (same-stream order carries x'→graph→z — the 29c-2 load-bearing property). With the split BOTH arms are one-bracket/one-wait, and the sandwich CPU FLIPPED 0.49× → **1.57× (WSL) / 2.24× (Win)** — the artifact was the whole "loss". ⛔ CI-GATED (advisor: enqueue is a public engine API; the bench is env-gated so it's CI-dead-code) — the **29c-2 gate now runs the enqueue single-submit path too** (`run_two_class_captured` gains an `enqueue_dst` out-param; asserts enqueue output == the waiting-launch two-class run == the CPU oracle), BIT-EXACT on Win #4824 + WSL #4671, so the two-class pipeline is PROVEN to run as one submit (not a bench-only convenience — the "migrated path runs on a real device" rule). **THE BENCH** `test_ceir_pipeline_cuda.cpp "ceir 29z"` (tag `[bench]`, env-gated on `CRD_CEIR_BENCH` so the default sweep WARN-skips — 26 assertions when set): a `BenchResolver` CACHING wrapper (⛔ the shared `resolve_stage` creates a new pipeline per call + CAPS at 16 — fine for the correctness runners' handful of calls, but a bench calls execute HUNDREDS of times and exhausted the cap at the 4th; the wrapper caches the whole ResolvedStage keyed by `st.op` — ⛔ assumes one op → one stage, TRUE under fuse=false; a future fused-GemmRelu bench must re-key); median-of-9, warmup 5, K ∈ {1,10,100} launches/sample; upload once, capture the provider==0 run once (whole plan for the unpartitioned mlp). TWO fixtures: **mlp3** (whole 3-stage plan captured, cap=[0,3)) + **sandwich5** (the 29c-2 5-stage two-class plan, cap=[1,4), 3-stage graph flanked by 2 eager gemms). ⭐ FINDINGS (board: `docs/bench/2026-09-05-ceir29-cuda-graphs-launch.md`): the win is the CPU SUBMIT PATH (one cuGraphLaunch vs N cuLaunchKernel), and it GROWS with dispatch count — the **sandwich WINS on CPU on BOTH devices** (Win 1.36–2.24×, WSL 1.13–1.57×), the 3-dispatch mlp is a smaller/noisier win (Win 1.20–1.70×, WSL ~parity incl a K100 **0.92× loss** — few launches saved, WSL jitter floor; recorded honestly). ⛔ GPU-time is a LOSS at these toy dims (gr `gpu_us` ~3–4.5× fb on both) — one bracket both arms, but the ~25µs delta is UNDISCRIMINATED (cuGraphLaunch fixed cost vs ev0→first-node latency; the per-bracket-floor control was not run — the Verdict says so, name-forward to a larger-fixture pass). GREEN: **Win RTX 13.3 + WSL2 RTX 12.0** bench exit 0 both; 29b-2a + 29c-2 correctness re-verified green both (the enqueue addition is additive to launch()); default sweep skips cleanly (WARN, 1 passed). Tidy clean (test_ceir_pipeline_cuda.cpp + cuda_compute_context.{hpp,cpp}). ⛔ CMake: `_CRT_SECURE_NO_WARNINGS` added to `crd-ceir-gpu-cuda-tests` (std::getenv for the CRD_CEIR_BENCH gate — the anim/ceridc precedent; WSL gcc ignores it; the reconfigure ran). ⛔ UNCOMMITTED-BATCH (adds/mods): `engine/gpu-context-cuda/include/crd/gpu/cuda_compute_context.hpp` + `engine/gpu-context-cuda/src/cuda_compute_context.cpp` (the `enqueue` decl+impl) + `tests/ceir-gpu-cuda/test_ceir_pipeline_cuda.cpp` (BenchResolver + median_of + bench_captured + the "ceir 29z" case + the 29c-2 enqueue coverage) + `tests/ceir-gpu-cuda/CMakeLists.txt` (_CRT_SECURE_NO_WARNINGS) + `docs/bench/2026-09-05-ceir29-cuda-graphs-launch.md` (NEW board) + `docs/bench/README.md` (index line). NEXT = **29z-2** (band CLOSE: the 29b-1 CUDA deferral ledger [emit_permute_cuda/attention; cross-domain Transfer stage; DeterminismClass filter axis; per-op §71 targeting; the non-CUDA graph APIs] each with a trigger; scars [aliased-storage-graph-replay — 29c-2, not yet a memory; + consider the launch-vs-enqueue-wait-structure lesson]; the close session doc [28z mold]; flip CEIR-29 ✅). | §70 §69 §102 |

**29z-2 ✅ 2026-09-05 (BAND-CLOSE COMPLETE — CEIR-29 ✅)**: the close tick (docs/memory only, no code). **(1) 29-0 CENSUS RECONCILED** (⛔ GATE re-verify — reconcile promises, don't just write rows, the 28-close scar): the `IExecutionProvider` compile→plan APPEND is **DISSOLVED** (struck at 29-0 with a pointer to the 29b-2b reconcile — a launch-graph provider is a plan LAUNCH-MODE, not a compile→plan implementor; the tensor compile→plan IS `plan_tensor_pipeline`; NO virtual was ever added, so it is NOT a deferral); the boundary-transfer STAGE + the stale 29z bench clause STRUCK at 29-0 (name-forward / column-corrected — `last_gpu_ms` [GPU-event] could NOT show the CPU-submit-path win, and there WAS a cudaEvent bracket-structure surprise, both contra the census's 29z sketch); constrained mode LANDED (29c-3). No census promise left unreconciled. **(2) DEFERRAL LEDGER** (each w/ trigger, in the close doc): emit_permute_cuda/attention-on-CUDA · fused-GemmRelu-on-CUDA (emit_contract_cuda no epilogue unwrap) · cross-domain `StageKind::Transfer` (memory_domain≠device_local; +`is_memory_domain` validate +run·CKIR·run double-transfer) · `DeterminismClass` filter axis · per-op §71 targeting (now 4 program-global directives would need it) · the per-bracket-floor GPU control + larger-fixture parity (29z-1, trigger: captured kernel time >~50µs) · `BenchResolver` re-key for fused GemmRelu · the 29-0 non-CUDA graph APIs (D3D CGC/MLIR, VK_ARM_data_graph, NPU, Work Graphs, device-graph launch, Metal ICB, cross-API interop, §103 sharding, autotuned provider choice) carried forward unchanged. **(3) SCARS — 2 NEW memory files:** `feedback_aliased_storage_graph_replay_must_reestablish_inputs` (a captured run overwrote its own input's aliased slot → replay re-runs the prefix; READ the alias table first) + `feedback_bench_arms_must_match_wait_and_bracket_structure` (A/B GPU bench arms must match submit/wait + event-bracket count or they measure sync not work; +the caching-resolver-for-repeated-execute clause) — MEMORY.md pointers added. **(4) CLOSE DOC** `docs/sessions/2026-09-05-ceir-29z-band-close.md` (the 28z mold: slices table + device/bench boards LINKING the bench doc + ledger + scars + uncommitted batch + proposed commit message NO trailer). **(5) FLIP** band row 🔄→✅ + MAP rewritten (the stale "29b-2 + boundary-transfer STAGE → 29c real-transfer-ASSERTED → 29z" struck for the actual 29b-2a/2b + 29c-1/2/3a/3b + 29z-1/2 chain); context.md header → CEIR-29 ✅✅✅ BAND CLOSED, the CEIR-28 focus block + the 29-progress narration REPLACED (replace-not-append) with the 29-closed summary, UNCOMMITTED note → 26/27/28/29 (~99 files), NEXT=CEIR-30. ✅✅✅ **CEIR-29 (native graph providers) COMPLETE** = 29-0 census + 29a (device-free core: descriptor + greedy-grow + authored pin/class) + 29b (CUDA tensor-pipeline gate + CUDA-Graphs capture + provider descriptor/reconcile) + 29c (partition-aware plan + on-device two-class boundary + class filter + authored directive) + 29z (bench + `enqueue` split + close). The runnable §70 native-graph provider (CUDA Graphs) is BIT-EXACT vs the CKIR fallback on two RTX devices, provider choice is authored (pin + class filter compose), and the CPU-submit-path win is measured. ⛔ UNCOMMITTED-BATCH (adds): the 2 scar files + `MEMORY.md` pointers (outside the repo) + `docs/sessions/2026-09-05-ceir-29z-band-close.md` + `docs/detours/D-007-ceir-tracker.md` (this row + the 29-0 strikes + the band-row flip) + `context.md`. NEXT = **CEIR-30** (`ceir.dist` multi-device, §68 §103; proof §140 — sharded reduction/training, placement is semantic; OPEN with an advisor CENSUS, no pre-design; ⛔ the census question is whether a 2-device box EXISTS to run it — if not, the band may be device-free + deferred-with-trigger, a legitimate census outcome, not a failure). | §70 §69 §102 |

**29a-3b-2 ✅ 2026-09-05 (the COMMITTED assign_provider .ceir ASSET — §146 "the schedule IS an authored asset"; CLOSES 29a-3 AND the whole 29a device-free CORE)**: authored `assets/ceir/assign_provider_coopvec.ceir` (`module { ^bb0: transform.assign_provider() {provider = "coopvec"} }`) — bootstrapped-via-THE-TEST (no throwaway compile: my best-guess canonical text, mirroring `schedule_fuse.ceir`, matched the printer on the FIRST anti-drift run — no fixup). GATE `test_band24_gate.cpp "ceir 29a-3b-2"` (#877 MSVC / gcc, device-free) mirrors the 27b committed-schedule-asset mold: (1) parse-load the committed file; (2) ⭐ ANTI-DRIFT through the PRINTER — `print(parse(file)) == print(build_pin_module oracle)`, NEVER file-bytes (the 28 committed-asset-anti-drift scar; ⛔ regenerate the file if build_pin_module changes, the 22c-3c rule); (3) ROUNDTRIP-STABLE (`print(parse(print(file))) == print(file)` — the committed file IS canonical); (4) find_transform_misuse None; (5) ⭐ END-TO-END — `provider_from_transform` resolves the LOADED module → Resolved index 1 (coopvec @1), then partition_ml pins build_band24's mlp→coopvec(1) / attention→stub(0). GREEN MSVC + gcc (`-R 29a` 5/5 both). Tidy clean (test_band24_gate.cpp; +local `slurp` + `<fstream>` + CRD_REPO_DIR guard, the 27b mold). ⛔ UNCOMMITTED-BATCH (adds/mods): **`assets/ceir/assign_provider_coopvec.ceir`** (NEW committed asset) + `test_band24_gate.cpp` (the 29a-3b-2 gate + the caps-blind-resolution-vs-caps-honouring block) + `provider.hpp`/`partition_ml.hpp` (deferral-comment updates — the advertise half landed 29a, compile→plan→29b). ✅✅ **29a-3 (authored pinning) COMPLETE** = 29a-3a (partitioner `pinned` mechanism) + 29a-3b-1 (directive + `provider_from_transform` loader) + 29a-3b-2 (committed asset). ✅✅ **the DEVICE-FREE 29a SCOPE is DONE** = 29a-1 (provider-class descriptor) + 29a-2 (subgraph greedy-grow) + 29a-3 (authored pinning): the §69 advertise/assign + §102 partition + authored-pin surface, device-free + gold-standard, MSVC + gcc. ⛔ 29-0 CENSUS RE-VERIFY (the 28-close scar — reconcile promises, don't just write rows): TWO 29a-census clauses are FORWARDED, struck in place at the 29-0 row — (1) CONSTRAINED provider-choice mode → **29c** (a `provider_class`-constrained pin needs TWO classes in one span to discriminate; pinned ✅ + auto ✅ landed here), (2) the `IExecutionProvider` compile→plan pure-virtual APPEND → **29b** (lands with its implementor, the CUDA-Graphs bridge; the DESCRIPTOR/advertise half of provider.hpp:36 landed 29a). No other census promise floats. NEXT = **29b** (the CUDA-GRAPHS native-graph provider — ⭐ PRE-STEP: the tensor-pipeline DEVICE GATE on `cuda_compute_context` [pays the 24z-ledgered CUDA-multi-stage-pipeline debt] so an N-dispatch fallback EXISTS to capture + BIT-EXACT-compare; THEN capture a claimed maximal subgraph → ONE `cudaGraph` launched as one; Win + WSL2 device gate; BIT-EXACT vs the fallback BY CONSTRUCTION; ⭐ the boundary-transfer plan STAGE [reconciled out of 29a-3a] lands HERE with a REAL producer). | §71 §102 §146 |

**30-0 CENSUS ✅ 2026-09-05 (band-OPEN advisor census — the `ceir.dist` multi-device band; facts VERIFIED by grep, NO pre-design)**: ⛔ CENSUS ONLY (the every-band rule): advisor-verified facts + runnable-proof decision + subslice MAP + deferral ledger; no op signatures, no code. **SCOPE (spec):** §68 `ceir.dist` dialect (device mesh · logical shards · placement · send/recv · peer copies · broadcast/reduce/all-reduce/all-gather/reduce-scatter · barrier · collective groups; providers multi-adapter local-GPU/NCCL/MPI/remote/cluster; "sharding annotations lower to communication automatically") · §103 multi-device sharding (tensors carry sharding annotations; the compiler PROPAGATES + MATERIALIZES partition/all-reduce/all-gather/reduce-scatter/reshard/peer-copies) · §140 proof (`tensor shard → per-device compute → all-reduce → result`; "prove device placement and transfer are SEMANTIC, not hard-coded application glue"). **HARDWARE (the census go/no-go):** ONE RTX 4070 Ti SUPER + WSL2 passthrough of the SAME GPU — NO 2nd physical GPU, NO NCCL/MPI/cluster. **⭐ RUNNABLE-PROOF DECISION (advisor, +1 reconcile): Host (CPU) + Gpu (CUDA) = TWO REAL provider classes** — two §24 memory domains (`host`/`device_local`), a REAL host↔device transfer that costs something, a REAL placement decision — §140's minimal FALSIFIABLE proof of "placement is semantic". ⛔ TWO-CUDA-STREAMS REJECTED: one device, one memory domain — a `cuMemcpyAsync` to self is not a transfer and a stream id is not a placement, so §140 has NOTHING to be semantic about (it's the 29c-2 same-stream capture relabelled). **VERIFIED FACTS (file:line):** (1) `resource.declare` ALREADY carries `memory_domain` as a CLOSED enforced vocab incl. `peer_visible` + `distributed` (resource.ceirop.toml:22) — ⛔ but this is the DOMAIN axis, NO device INDEX: a §103 sharding annotation (shard-axis + mesh-rank) is genuinely NEW (30a's), the domain merely COINCIDES with the device in Host+Gpu (the real-2-GPU trigger breaks the coincidence; so NOT "placement is not a new mechanism"). (2) the all-reduce COMBINE is a same-shape elementwise sum across shards → `emit_elementwise_cuda` RUNS it on CUDA; `tensor.reduce`-on-CUDA ~~absent~~ **not-wired-for-the-§140-axis-0-reduce** (~~`emit_reduce_cuda`/`emit_broadcast_nd_cuda` absent~~ **[STRUCK at 30b-3b: `emit_reduce_cuda` EXISTS (ckir_cuda.hpp:712) but (a) TRAILING-AXIS ONLY — an outer/strided axis0 reduce still can't run on CUDA, and (b) its kernel takes TWO scalar params (`nout`,`redsize`) — incompatible with `CudaComputeContext`'s single-blob push (cuda_compute_context.cpp:190; `backend_cuda.cpp:395`/`backend_hip.cpp:127` pass scalars), so wiring it is the cross-launch-site CONTRACT change of [[feedback_cuda_emitter_signature_is_a_contract_shared_by_every_launch_site]] — its OWN slice, trigger: a CUDA shard whose per-rank compute is a reduce. 30b-3b runs the reduces on Host, the all-reduce COMBINE on CUDA]**; CUDA `resolve_stage` handles Contract/Elementwise/compute-kernel only — test_ceir_pipeline_cuda.cpp:99-179) is IRRELEVANT to §140 (an axis-reduce, not the collective combine). (3) ⛔ NO pure-CPU tensor executor and NO llvmpipe tensor-execution runner exist — `execute_tensor_pipeline` is backend-agnostic ("ceir-gpu names NO backend: the caller's RESOLVER" tensor_pipeline_exec.hpp:6); the ONLY runnable device executor is CUDA; the non-CUDA tensor tests (test_tensor_pipeline.cpp, test_ckir_synth.cpp) are DEVICE-FREE planner/synth tests; hesap has no tensor engine. (4) ⭐ BUT a CKIR CPU reference interpreter EXISTS — `ckir_eval.hpp:3` "the CKIR **CPU reference interpreter** — the single oracle every GPU backend"; :82 interprets a graph `g` on the CPU over row-major float buffers (tensor-sized, NOT scalar-only) ⇒ **the Host executor = `ckir_synth` (backend-neutral, already the front half the CUDA resolver calls) + `ckir_eval` — a GENERAL 2nd CPU tensor executor via a thin resolver adapter, NOT per-fixture glue** (which §140 forbids). ~~⛔ arithmetic caveat (a FACT, not a blocker): eval is the F64 reference tier (ckir.hpp:31) vs the f32-CUDA path — a host-shard-vs-cuda-shard BIT-EXACT gate needs an F32-rounding eval mode OR a per-shard-vs-own-oracle/tolerance gate (30b decides).~~ **[RESOLVED at 30b-1, NOT deferred — the census FACT was imprecise: `eval_cpu` ROUNDS every elementary op to the node dtype (`round_dtype`, ckir_eval.hpp:48-54/240-242/275-278), so an F32 graph is BIT-EXACT vs a naive f32 GPU kernel — the SAME oracle every device backend proves against IS the Host executor. Neither a separate F32-rounding eval MODE nor a tolerance gate was needed; the §140 gate is `==`. So this is a RESOLVED census item, not a deferral.]** (5) plan seam (fact): `PlanStage.provider` + `plan_tensor_pipeline_partitioned` EXIST (29c-1); `execute_tensor_pipeline` binds a GPU recorder, so a HOST stage needs a NEW EXECUTION BRANCH — and that is exactly where 29c-1's cross-domain ~~`StageKind::Transfer`~~ trigger (`memory_domain ≠ device_local`) FIRES (the 29-0/29c-1 deferral pays out HERE). **[RECONCILED at 30b-2a/30b-2b/30b-3b — the TRIGGER fired but the MECHANISM DISSOLVED (the 29b-2b IExecutionProvider shape): NO `StageKind::Transfer` plan-stage member was ever added (the plan `StageKind` enum, tensor_pipeline.hpp:53, has no Transfer — `LoweredKind::Transfer` at lower.hpp:25 is the UNRELATED 13b command-movement kind). The cross-domain movement became `plan_transfers` (a DERIVED transfer LIST computed from the per-stage ProviderClass, 30b-2a) + `execute_two_class`'s upload/readback CALLBACKS (30b-2b), not a plan stage. Riders re-homed to the 30 ledger: `is_memory_domain` string validation + the run·CKIR·run double-transfer did NOT land — deferred with triggers below.]** **SUBSLICE MAP (a shape, not a design):** **30a** device-free `ceir.dist` CORE (mesh + shard-axis/mesh-rank placement annotation + the §140-MINIMUM collective vocab `shard`/`all_reduce`/`peer_copy`-transfer [broadcast/gather/scatter/barrier as each earns a consumer, 26b-4] + the sharding-annotation → collective LOWERING pass §103, proven by inspection: a 1-device mesh lowers to NO collectives [the IDENTITY gate] AND a 2-device mesh lowers to the transfers + all-reduce) → **30b** the Host+Gpu §140 PROOF (`shard → CPU shard [synth+eval] + CUDA shard → transfer → all-reduce [elementwise combine] → result`, bit-exact vs the single-device oracle, Win + WSL2; the `Transfer` stage + the Host execution branch + the f64/f32 gate LAND here) → **30c** AUTHORED placement (the transform-dialect device/shard-placement directive — reuse-vs-new-op decided at 30a; composes with 29's pin/class directives, §146 "placement is an asset") → **30z** close (bench sharded-2-device vs single-device — ⛔ an HONEST LOSS at toy dims [the transfer dominates]; STATED HERE so 30z isn't surprised as 29z was). **DEFERRAL LEDGER (each w/ trigger):** real 2-GPU mesh → a >1-GPU box · NCCL/MPI/remote/cluster providers → a cluster · §103 `reshard` → a consumer that changes sharding mid-program · the §68 long tail (broadcast/all-gather/reduce-scatter/barrier/collective groups) → each its first consumer · `emit_reduce_cuda`/`tensor.reduce`-on-CUDA → a CUDA shard needing a local axis-reduction (irrelevant to §140's combine) · ~~an F32-rounding CKIR eval mode → a host-vs-cuda BIT-EXACT gate (vs a per-shard/tolerance gate at 30b)~~ **[RESOLVED at 30b-1 — NOT a deferral: `eval_cpu`'s per-op `round_dtype` already makes an F32 graph f32-faithful, so the bit-exact gate is `==` with the EXISTING eval; see the fact-(4) strike above]** · the 29-0 non-CUDA-graph-API items carried forward unchanged. ⛔ UNCOMMITTED-BATCH (adds/mods): `docs/detours/D-007-ceir-tracker.md` (this row + the band-row flip) + `context.md` (the CEIR-30 focus). NEXT = **30a** (the device-free `ceir.dist` core: mesh + shard-annotation + the §140-minimum collective vocab + the sharding→collective lowering pass, with the 1-device IDENTITY gate — opgen the dialect from a new `dist.ceirop.toml`; ⛔ FIRST decide reuse-vs-new for the placement annotation vs `declare.memory_domain`, and whether the shard-axis/mesh-rank rides an attr or a new op). | §68 §103; proof §140

**30a-1 ✅ 2026-09-05 (the device-free `ceir.dist` DIALECT SKELETON — dist.mesh / dist.shard / dist.all_reduce + find_dist_misuse; the 27a mold; MSVC + WSL gcc)**: the first 30a subslice (advisor SPLIT 30a into 30a-1 dialect / 30a-2 sharding PROPAGATION / 30a-3 MATERIALIZATION — the full 30a is three ticks). **DIALECT** a NEW `engine/ceir/ops/dist.ceirop.toml` (24th dialect) opgen'd (⛔ ALL-dialects regen, no per-dialect flag — only `dist_ops.*` are new, the other 23 rewrote byte-identical; `crd-ceir-opgen-drift` GREEN both compilers). THREE ops (advisor-settled by house rule): **(1) dist.mesh** {name:symbol, shape:string} — a NAMED device-grid DECLARATION, NO results/operands/effects + NOT Pure (a zero-result Pure op is DCE-dead + two identical meshes would be CSE candidates — the transform-directive mold); **(2) dist.shard**(%t){mesh:symbol, axis:int, mesh_axis:int} — Pure, ⛔ result type == input type EXACTLY (the §70 don't-prematurely-lower invariant: a placement annotation PRESERVES the logical tensor; the per-device physical shape is 30a-3's); **(3) dist.all_reduce**(%t){mesh:symbol, fn:string} — the §140 collective, Pure, result type == input (combines partials, NOT an axis-reduce). **VERIFIER** `find_dist_misuse` (dist.{hpp,cpp}, const Context&, region-recursive, I6 op-NAME — the find_transform_misuse mold) with 9 typed kinds each pointing the op: MeshShapeInvalid (>=1 POSITIVE ints), DuplicateMesh (the 2nd, pre-order), UnknownMesh, OperandNotTensor, ResultTypeMismatch, ShardAxisInvalid (< input rank), MeshAxisInvalid (< mesh rank), FnInvalid. ⛔ MESH NAME resolved by THIS walk's own module scan (`find_mesh` — attr-only name-match, the frame.draw-list-name precedent; a SymbolTable EXISTS since CEIR-13 but dist.mesh is NOT registered into it this slice). ⛔⛔ TWO DECISIONS RECORDED (advisor — a reader will misread them as oversights): **(a) ResultTypeMismatch DELIBERATELY MERGES shard + all_reduce** (ONE property — result==input — for both; the op pointer discriminates; the tensor dialect's per-op-kind style [BroadcastResultMismatch/ReduceResultMismatch/…] exists only because those check DIFFERENT computed shapes, so it is NOT the precedent here). **(b) the collective `fn` = {sum,prod,max,min} = tensor.reduce's vocab MINUS mean** via a LOCAL `collective_fn_in`, NOT a share of tensor.cpp's `fn_in`: `fn_in` is ANON-NAMESPACE in tensor.cpp (sharing = a header hoist) AND the vocab INTENTIONALLY differs (mean needs a materialization post-scale the graph tier lacks — the tensor.reduce ckir ReduceFnUnsupported note; the 30a-3 named-forward). ⛔ next slice must NOT "fix" this by hoisting fn_in — the divergence is by design. **GATE** `tests/ceir/test_dist_gate.cpp` (3 TEST_CASEs, `[ceir][dist]`): (1) a well-formed mesh→shard→all_reduce VERIFIES clean + a TEXT round-trip (print→parse→re-walk None + print==reprint — the dist ops SERIALIZE) + ⭐ the collective vocab ADMITS all FOUR {sum,prod,max,min} (a 4-way identity so `collective_fn_in` can't pass by matching "sum" alone — the advisor-closed tautology); (2) each of the 8 misuses REJECTED with the EXACT kind AND the offending OP identity (⭐ `mean`→FnInvalid — the deliberate exclusion proven); (3) the COMMITTED `assets/ceir/dist_shard_reduce.ceir` — anti-drift-through-the-PRINTER vs `build_dist_module` (matched the printer on the FIRST authoring, no fixup) + ROUNDTRIP-canonical (`print(parse(print(file)))==print(file)`) + find_dist_misuse None. + the opgen `test_dist_gen_smoke.cpp` (auto-globbed; self-register + build-through-generated-builder + generated-verifier-rejects-malformed). GREEN: **MSVC win-debug** (30a-1 3/3 + dist gen smoke 3/3 + opgen-drift/validator 2/2) + **WSL gcc linux-gcc-debug** (identical 3/3 + 3/3 + 2/2 — the `cmake .` reconfigure picked up the globbed files; gcc `-Werror` clean, no switch/arith surprise). Tidy-20 clean (dist.cpp + dist.hpp + test_dist_gate.cpp; the generated files tidy-excluded). ⛔ LEDGER (confirm-you-know, advisor): `find_mesh` is O(ops×module) — FINE for a verifier, but 30a-2's propagation over a large module must gather meshes ONCE, not reuse this; mesh SCOPE = wherever the walk finds it this slice (a future SymbolTable registration of mesh names wants module scope — name-forward); the `peer_copy` op + the §68 collective long-tail (all_gather/reduce_scatter/broadcast/barrier/groups) + `reshard` + a mesh rank>1 + the `mean` post-scale = each its 30a-3/consumer trigger (the 30-0 ledger). ⛔ UNCOMMITTED-BATCH (adds/mods): `engine/ceir/ops/dist.ceirop.toml` (NEW) + `engine/ceir/generated/crd/ceir/gen/dist_ops.{hpp,cpp,ops.json,ops.md}` (NEW, opgen) + `engine/ceir/include/crd/ceir/dist.hpp` (NEW) + `engine/ceir/src/dist.cpp` (NEW) + `tests/ceir/test_dist_gate.cpp` (NEW) + `tests/ceir/generated/test_dist_gen_smoke.cpp` (NEW, opgen) + `tests/ceir/CMakeLists.txt` (the explicit test-list line) + `assets/ceir/dist_shard_reduce.ceir` (NEW asset). NEXT = **30a-2** (the sharding PROPAGATION analysis: a `Value→Sharding` map forward over the tensor ops — elementwise PRESERVES the sharding, a reduce over the SPLIT axis → **Partial**; the analysis that 30a-3 materializes an all_reduce from; ⛔ gather meshes ONCE, not via find_mesh-per-op). | §68 §103; proof §140

**30a-2 ✅ 2026-09-05 (the SHARDING PROPAGATION analysis — `propagate_sharding` computes a `Value→Sharding` map over dist.shard + tensor ops; device-free, ceir-gpu; MSVC + WSL gcc)**: the second 30a subslice (§103 "the compiler PROPAGATES sharding annotations"). HOME `engine/ceir-gpu/…/sharding.{hpp,cpp}` (the partition_ml precedent — a device-free compiler analysis over crd-ceir dialects lives in ceir-gpu). ⭐ THE LATTICE (advisor-settled, 4 values): **Replicated** (default — a Value ABSENT from the map; every device has the full tensor) / **Sharded**{mesh_op, axis, mesh_axis} / **Partial**{mesh_op, most-recent-reduced-axis (provenance), mesh_axis, fn} (a per-device partial a matching all_reduce completes) / **Conflict** (no single sharding without a reshard/all_gather — both ledgered). ⛔ `mesh` is the RESOLVED `const Operation*` (gathered ONCE up front — the 30a-1 ledger item; "same mesh?" = pointer equality). ⛔ TOTAL — always a map; an unresolvable state is `Conflict` (a lattice value the gate asserts + 30a-3 refuses to materialize past), NEVER a silent Replicated (the §70 pre-lowering lie). RULES (one forward pre-order pass): **dist.shard**→Sharded (⛔ a non-Replicated input → Conflict, the reshard guard); **dist.all_reduce**→Replicated IFF input is a Partial with matching mesh+fn, else Conflict (⛔ a Sharded input is an ALL_GATHER, a different collective — Conflict, not a silent gather); **tensor.elementwise**→the MEET (Replicated is bottom/absorbed; two equal → that value; else Conflict); **tensor.reduce**{axis,fn} on Sharded→ axis==sharded-axis ? **Partial**{fn} : Sharded with the axis RE-INDEXED (sharded>reduced ⇒ −1), on Partial→ Partial iff fn matches else Conflict; **every OTHER tensor-producing op** (matmul/gemm/transpose/reshape/broadcast/fft)→**Conflict** (the §70 no-lie — an unknown op's output sharding is unknown, not Replicated). ⛔ PRECONDITION (advisor): `propagate_sharding` assumes a `find_dist_misuse`/`find_tensor_misuse`-CLEAN module (run the two walks first) — 30a-3 must ENFORCE that order, not assume it. **GATE** `tests/ceir-gpu/test_sharding.cpp` (`[ceir][dist][sharding]`, IDENTITY on every field, 7 sections): (a) the §140 chain `declare→shard(axis0)→reduce(axis0,sum)→all_reduce(sum)` = Sharded / Partial{axis0,sum} / Replicated; (b) ⭐ reduce on the OTHER axis RE-INDEXES (shard axis1, reduce axis0 → Sharded axis0 — the discriminator); (c) all_reduce fn≠Partial fn → Conflict; (d) all_reduce on a Sharded (un-reduced) input → Conflict (the all_gather negative); (e) elementwise of two equal Partials → Partial + Sharded×Replicated → Sharded; (e′) ⭐ elementwise of two DIFFERENT-axis Shardeds → Conflict (the meet is not axis-blind — the tautology-closer, the 30a-1 4-fn mold); (f) matmul → Conflict (the unknown-op negative). GREEN: **MSVC win-debug + WSL gcc linux-gcc-debug** (30a-2 1/1 both; gcc `-Werror` clean). Tidy-20 clean (sharding.cpp + sharding.hpp + test_sharding.cpp). ⛔ LEDGER (each w/ trigger, advisor): `resource.import`→Replicated is a BINDING ASSUMPTION (an external is available where bound) → an `import{memory_domain=peer_visible}` may want Sharded; REGION-CARRIED sharding (a region op yielding a tensor → Conflict today, correct-by-the-unknown-op-rule; a real rule is 30a-3+, and def-before-use holds only on a structure-clean module — the precondition); the map is Value*-pointer-keyed + valid ONLY while the module is UNMUTATED → 30a-3 (which inserts ops) RE-RUNS this analysis, never patches the map. ⛔ UNCOMMITTED-BATCH (adds/mods): `engine/ceir-gpu/include/crd/ceir/gpu/sharding.hpp` (NEW) + `engine/ceir-gpu/src/sharding.cpp` (NEW) + `tests/ceir-gpu/test_sharding.cpp` (NEW) + `tests/ceir-gpu/CMakeLists.txt` (the explicit test-list line). NEXT = **30a-3** (the MATERIALIZATION pass: insert a `dist.all_reduce` where a Partial ESCAPES — reaches a consumer that is NOT itself an all_reduce/reduce/elementwise-of-Partials — completing it to Replicated; ⛔ RE-RUN propagate_sharding after each insert; the 1-device-mesh IDENTITY gate = a 1-device mesh lowers to NO collectives, byte-identical print through the printer; refuse to materialize past a Conflict). | §68 §103; proof §140

**30a-3 ✅ 2026-09-05 (the MATERIALIZATION pass — `materialize_sharding` auto-INSERTS dist.all_reduce where a Partial escapes; the 1-device IDENTITY; CLOSES the device-free 30a core; MSVC + WSL gcc)**: the third 30a subslice (§68/§103 "sharding annotations lower to communication AUTOMATICALLY"). `materialize_sharding(Context&, Module&, alloc) → {inserted, had_conflict}` (ceir-gpu/sharding.{hpp,cpp}). ⭐ THE ESCAPE RULE (advisor): a Partial value ESCAPES when it has a use whose owner is NOT dist.all_reduce / tensor.reduce / tensor.elementwise (those COMPOSE the partial — reduce/elementwise defer it, an all_reduce completes it); anything else (resource.export, matmul, a dispatch, …) needs the partial COMPLETE ⇒ insert a `dist.all_reduce{mesh,fn}` at the Partial's DEF (dominating every use), completing it to Replicated. ⛔ ITERATIVE: each insert MUTATES the module (invalidating the ShardingMap's Value* keys) so the pass RE-RUNS `propagate_sharding` after every insert (never patches the map) — a BOUNDED fixpoint (the monotone-watermark hang-guard: #inserts < #Partial-producing ops, capped at 1<<20). INSERT MECHANICS: build_all_reduce(V) → insert after V's def → `V.replace_all_uses_with(ar_result)` then `ar.set_operand(0, V)` (the cycle-free RAUW-all-then-restore trick). ⛔⛔ THE 1-DEVICE IDENTITY IS A 30a-2 PROPAGATION RULE, NOT a materialization skip (advisor — put the fact where it lives, every map consumer gets it): a `dist.shard` over a mesh axis of EXTENT 1 → **Replicated** (degenerate), and a `dist.all_reduce` over a 1-device (1-D) mesh → **identity** (pass the input's sharding through). ⭐⭐ THAT SECOND HALF WAS AN ADVISOR-CAUGHT DEFECT (post-impl review): without it, an AUTHORED `shard→reduce→all_reduce` on mesh "1" (the §140 fixture run degenerately on ONE device — exactly what 30b hits) was a FALSE **Conflict** (input Replicated, not Partial); fixed to pass-through → the whole chain stays Replicated, and materialization inserts NOTHING ⇒ the identity FALLS OUT (byte-identical print). **GATE** (`test_sharding.cpp`): the new `"ceir 30a-3"` TEST_CASE (6 sections): (a) auto-insert `shard→reduce→export` → inserted==1, the new dist.all_reduce{sum,@m2} reads the reduce + feeds the export; (b) ⭐ 1-device identity — mesh "1" → inserted==0 AND `print(before)==print(after)` BYTE-IDENTICAL; (c) an authored all_reduce is respected (no double-insert); (d) refuse past a Conflict (fn-mismatch → had_conflict, inserted==0); (e) chain `reduce→reduce→export` → ONE insert after the SECOND reduce (the first's partial composes); (f) ⭐ mixed-use policy — a partial with an escaping AND a composing use → RAUW-all completes it for BOTH (reduce2 reads the completed value; per-use repoint is the ledgered optimization). + the `"ceir 30a-2"` TEST_CASE GAINED 2 sections (advisor): shard-on-mesh-"1"→Replicated + the authored `shard→reduce→all_reduce`-on-mesh-"1"→Replicated-end-to-end (the defect regression). GREEN: **MSVC win-debug + WSL gcc linux-gcc-debug** (30a-1 + 30a-2 + 30a-3 all 5/5 both). Tidy-20 clean (sharding.cpp + sharding.hpp + test_sharding.cpp — ⛔ a function-LOCAL `constexpr` tripped `readability-identifier-naming` [local-constant case]; moved to a file-scope anon-namespace `kMaxMaterializeInserts`, the kMaxRank precedent — [[feedback_local_constexpr_is_localconstant_lowercase_under_gate]]). ⛔ LEDGER (each w/ trigger, advisor): a collective over a mesh RANK>1 needs a `mesh_axis` attr (30a-1 named it forward; the identity checks axis 0 — the only 1-D case with a consumer today); `elementwise(Partial, Sharded)`→Conflict emerges from TWO rules cooperating (the meet is Conflict + the partial doesn't escape) — correct, by design; region-carried sharding (a Partial escaping into an UNRELATED block is Conflict-safe only via unknown-op→Conflict — 30a-2's note, extended); per-use repoint (vs RAUW-all completing composing uses) is the optimization; `build_all_reduce` RE-INTERNS the mesh symbol from the mesh op's `name` — the ONE site to switch to a symbol-ref if a SymbolTable registration of mesh names lands. ⛔ UNCOMMITTED-BATCH (mods, all already in the 30a-2 batch — now carrying materialize + the 30a-2 amendment): `engine/ceir-gpu/include/crd/ceir/gpu/sharding.hpp` + `engine/ceir-gpu/src/sharding.cpp` + `tests/ceir-gpu/test_sharding.cpp` (NO new files). ✅✅ **the DEVICE-FREE 30a CORE is COMPLETE** = 30a-1 (dialect skeleton) + 30a-2 (sharding propagation) + 30a-3 (materialization + 1-device identity): the §68/§103 mesh/shard/collective vocab + the propagate→materialize passes, device-free + gold-standard, MSVC + gcc. NEXT = **30b** (the FIRST DEVICE tick: the Host executor = `ckir_synth`→`ckir_eval` as a resolver + a 2-class `PlanStage.provider` plan [Host + CUDA] + the cross-domain `StageKind::Transfer` stage 29c-1 named forward + the §140 chain `shard→CPU+CUDA→all_reduce→result` executed BIT-EXACT vs a single-device oracle, Win + WSL2; ⛔ OPEN with a census-sized orientation — re-read `execute_tensor_pipeline`'s recorder binding + `ckir_eval`'s entry signature before the advisor call). | §68 §103; proof §140

**30b-1 ✅ 2026-09-05 (the HOST EXECUTOR — `execute_tensor_pipeline_host`, the CPU MIRROR of execute_tensor_pipeline; device-free; MSVC + WSL gcc)**: advisor SPLIT 30b into three (30b-1 the Host runner mechanism / 30b-2 the two-class orchestrator + `Transfer` on CUDA / 30b-3 the §140 chain on the {Host,Gpu} mesh) — this tick = 30b-1, device-FREE. ⭐ THE MECHANISM (`engine/ceir-gpu/…/tensor_pipeline_exec.{hpp,cpp}`, beside the GPU executor it mirrors): run the SAME `TensorPipelinePlan` on the CPU by RE-SYNTHESIZING each stage's op (`synth_gemm/reduce/transpose/broadcast/elementwise` + GemmRelu via `GemmEpilogue::Relu` — the graph-tier CKIR) into a fresh `kir::KGraph` and interpreting it with **`kir::eval_cpu`** — ⛔ NO resolver + NO recorder (the CPU eval IS the backend; crd-kir is header-only + GPU-free, so ceir-gpu calls it directly — no backend named). ⭐⭐ THE F32-FAITHFUL KEY (the 30-0 f64/f32 caveat RESOLVED, not deferred): `eval_cpu` rounds EVERY elementary op to the node dtype (`round_dtype`, ckir_eval.hpp:48-54/240-242/275-278), so an F32 graph is BIT-EXACT vs a naive f32 GPU kernel — the SAME oracle every device backend proves against IS the Host executor, so the §140 gate is `==` (the oracle doctrine), NOT a tolerance. Per stage: f32→f64 each INPUT bind (first nbind−n_out, the 13a positional-slot order = synth Input iidx order), eval, f64→f32 the single trailing OUTPUT bind (LOSSLESS — already F32-rounded). ⛔ Fft (kernel-tier) + VizDispatch/Dequant/QuantGemm (authored .ckir) are NOT graph-tier ⇒ typed `ExecuteError::UnresolvedKernel` (never a silent skip; the switch is TOTAL over StageKind, no default, so a widened enum trips gcc -Wswitch). ⛔ classify FIRST (synth), THEN the `n_out==1` invariant guard — else a multi-output Fft (n_out=2) mis-reports BindingArity instead of UnresolvedKernel (a build-caught ordering bug this tick). **GATE** `tests/ceir-gpu/test_host_exec.cpp` (`[ceir][host-exec]`, 4 TEST_CASEs, device-free): (a) the canonical ml.mlp planned FUSED → [GemmRelu, Gemm] BIT-EXACT vs a CPU float oracle (arithmetic mirrors eval_cpu — the 29b-1 recipe; +the §137 profile rows asserted: kinds + trailing bytes_out + workgroups==0); (b) a standalone reduce(sum) BIT-EXACT vs a sequential f32 sum (the §140 building block); (c) an unsupported fft stage → UnresolvedKernel + the terminal keeps its sentinel (NO partial write past the abort); (d) ⭐ elementwise(add)/transpose/broadcast each BIT-EXACT (the advisor-caught "claimed-but-untested" gap — elementwise is 30b-3's per-shard op + the only other 2-input path). GREEN: **MSVC win-debug + WSL gcc linux-gcc-debug** (30b-1 4/4 both; bit-exact holds cross-platform — a real claim for the reduction order). Tidy-20 clean (tensor_pipeline_exec.cpp + .hpp + test_host_exec.cpp). ⛔ the `gpu::` ambiguity in the test is a TEST-FILE artifact (tensor_pipeline_exec.hpp transitively pulls crd::gpu/compute.hpp alongside crd::ceir::gpu) → the `ceg` alias + 3 `using`s, NOT an engine change. ⛔⛔ LEDGER FOR 30b-2 (advisor, each a fact not code): **(1) THE TWO-CLASS FUSE LANDMINE** — `fuse_gemm_relu=true` gives the HOST class a `GemmRelu` it runs bit-exact, but the CUDA class CANNOT unwrap it (`emit_contract_cuda`, test_ceir_pipeline_cuda.cpp:10-11); a two-class plan is planned ONCE ⇒ 30b-2 must plan `fuse=false` (the CUDA constraint wins for the shared plan) OR gate fusion PER-CLASS — else a mystery `UnresolvedKernel` at the class boundary [[feedback_authored_programs_load_via_resolve_program_text_engine_app_convention]]. **(2)** `eval_cpu` allocates+frees `buf` per call (ckir_eval.hpp:100/416) — a runner, not zero-alloc; the 30b-2 bench arm must account for it. **(3)** the Host class's `memory_domain="host"`, its `MlProvider` row, and the cross-domain `StageKind::Transfer` (29c-1 named-forward) are 30b-2's. **(4)** (c) can't distinguish "gemm ran, fft rejected" from "gemm rejected too" (fine — both ride synth_gemm); an optional probe `Value*` asserting the gemm intermediate ≠ sentinel is the airtight refinement (name-forward). ⛔⛔ BLAST-RADIUS CATCH (a LATENT CEIR-29 red the full `-R ceir` sweep surfaced, unrelated to 30b-1): `crd-ceir-invariants` (the source-scanning ADR-0109 I3 gate — crd-ceir is backend-AGNOSTIC) was RED on `provider.hpp:39/43` — the 29b-2b STRUCK deferral comment literally wrote "CUDA-Graphs"/"CUDA Graphs" (I3 bites comment prose naming a shading-lang/bytecode/COMPILER; API names Vulkan/DX12 are allowed — [[feedback_ceir_i6_grep_bites_comment_prose]]). FIXED by rewording the struck prose to "the launch-graph provider bridge" / "a native graph-API bridge" (meaning preserved, no literal backend name); `crd-ceir-invariants` GREEN (a source-scan test — no rebuild; WSL/CI identical). ⛔ UNCOMMITTED-BATCH (adds/mods): `engine/ceir-gpu/include/crd/ceir/gpu/tensor_pipeline_exec.hpp` (the `execute_tensor_pipeline_host` decl) + `engine/ceir-gpu/src/tensor_pipeline_exec.cpp` (synth_stage_host + the impl) + `tests/ceir-gpu/test_host_exec.cpp` (NEW) + `tests/ceir-gpu/CMakeLists.txt` (the explicit test-list line) + `engine/ceir/include/crd/ceir/provider.hpp` (the I3 comment reword — a 29-batch file). NEXT = **30b-2** (the two-class orchestrator: `run_two_class(plan, partition)` picks a runner by `PlanStage.provider` — Host stages via `execute_tensor_pipeline_host` over host buffers, Gpu via `execute_tensor_pipeline`; the `StageKind::Transfer` upload/readback at each class boundary [where `memory_domain≠device_local` FIRES]; the Host `MlProvider` row; CUDA gate = 29c-2's sandwich with the MIDDLE stage on Host, bit-exact all-CUDA vs all-Host vs oracle; ⛔ plan `fuse=false` — the landmine above). | §68 §103; proof §140

**30b-2a ✅ 2026-09-05 (the TWO-CLASS PLACEMENT→TRANSFER pass — `plan_transfers` + `stage_class_from_partition`; PURE device-free; MSVC + WSL gcc)**: advisor SPLIT 30b-2 into 30b-2a (this tick — the pure transfer-planning half) / 30b-2b (the engine runner + CUDA gate). ⭐ THE KEY DESIGN (advisor, 3 corrections to the obvious path): **(1) placement is an explicit `ConstSpan<ProviderClass> stage_class` (one per stage), NOT `partition_ml`** — the sandwich hand-writes it, 30b-3's sharding placement PRODUCES it; `PlanStage.provider` stays a 29c concept + a `stage_class_from_partition` ADAPTER derives the vector from it (two producers, one consumer). **(2) `Transfer` is NOT a StageKind** — the planner is partition-BLIND (partition_ml.hpp:147); a transfer is a PLACEMENT consequence derived from `stage_class` + def-use, so `plan_transfers(plan, stage_class, alloc) → Array<Transfer{buffer, before_stage, direction}>` is a PURE pass (walk each input to its last WRITER; writer-class ≠ reader-class ⇒ a §24 crossing before the reader). **(3) the runner is engine-side but device-free** (function-pointer Gpu half + TransferFn) — 30b-2b's. ⭐ MODEL DECISION (advisor-delegated, RECORDED): ExternalIn buffers are **Host-BORN** (the caller holds the f32) + the Output-must-be-Host-visible readback IS a planned transfer (`before_stage == num_stages`) ⇒ the runner hand-copies NOTHING, ALL host↔device movement is planned (the "placement is semantic, not glue" §140 property). Deduped on (landlord, last-writer-stage, direction); a new write invalidates prior transfers; aliases resolve to the LANDLORD (a tenant's bytes live there). ⛔ PRECONDITION (advisor): `plan_transfers` assumes a VALIDATED plan (its `n_out > nbind` skip is silent — `validate_tensor_pipeline` runs first in every runner; the propagate_sharding-misuse-clean-precondition mold). **GATE** `tests/ceir-gpu/test_two_class.cpp` (`[ceir][two-class]`, 6 TEST_CASEs, device-free): (a) all-Host → ZERO transfers, all-Gpu → exactly ONE readback + the ExternalIn uploads; (b) the sandwich [Gpu,Host,Host,Host,Gpu] → the KEY inter-class transfers by IDENTITY (xp D→H@1, y H→D@4, z D→H@end) + NO transfer inside the Host middle run; (c) the REVERSE [Host,Gpu,Gpu,Gpu,Host] → transfers FLIP + NO final readback (Output ends Host); (d) an aliased (reshape) buffer crossing → names the LANDLORD not the alias; (e) `stage_class_from_partition` on the 29c-2 partition → all-Gpu ⇒ zero INTER-STAGE cross-class (the uploads + 1 readback are the I/O 29c-2 did by hand, now planned — advisor: "don't soften that, (e) proves zero inter-stage cross, not zero transfers"); (f) ⭐ the DEDUPE + resident-skip branches (advisor-CAUGHT dead code — the sandwich never reads one external twice): `gemm(a,W)→p·gemm(a,V)→q·elementwise(p,q)` with `a` read by BOTH gemms → all-Gpu uploads `a` ONCE + [Gpu,Host,Gpu] skips the resident `a`/`p`. ⛔ the KEY buffers are found by STAGE POSITION (stage_out), NOT pre-expansion SSA values — `expand_ml_ops` REPLACES the ml.mlp result value (a build-caught bug: `bland(mo->result(0))` couldn't find `y`). GREEN: **MSVC win-debug + WSL gcc linux-gcc-debug** (30b-2a 6/6 both). Tidy-20 clean (tensor_pipeline_exec.cpp + .hpp + test_two_class.cpp — split multi-decls; `M`/`D` locals → `rows`/`dim` [local-constant lowercase, and `m` is the Module param]). ⛔⛔ 30b-2b FACTS (advisor, decisions RECORDED): **(i) THE VizDispatch-on-Host EXIT (the fuse-landmine resolution) IS the gold path + CONFIRMED viable:** the 29c-2 sandwich's middle relu is a VizDispatch(relu) stage — Host `eval_cpu` can't run it AND `fuse=true` blocks CUDA, so NEITHER class runs the plan under one fuse setting; the fix = the Host executor GAINS VizDispatch via `ckir_read(relu.ckir) → KEntry → wrap f32 host arrays as `KernelBuffer` (the `tests/gpu-shared/ckir_kernel_dispatch.hpp:159` mold — device-free) → bind_authored_local_size → `eval_cpu_kernel` (ckir_kernel_eval.hpp:54; KernelBuffer byte-views, NOT eval_cpu's f64 ins[])` — the CUDA resolver's VizDispatch branch (test_ceir_pipeline_cuda.cpp:136-168) with eval_cpu_kernel where emit_compute_kernel_cuda is; this RETIRES the 30b-1 "authored-kernel CPU eval is name-forward" deferral + unblocks fuse=false on both classes. **(ii) execute_two_class signature (sketch):** `execute_two_class(ctx, plan, stage_class, transfers, host_bufs:ConstSpan<f32*>, gpu_user:void*, GpuStageFn, TransferFn, alloc)` — Gpu half a fn-ptr (ceir-gpu names no backend), Host stages run `execute_tensor_pipeline_host` over host_bufs (the ONE runner the engine owns); the CUDA test supplies execute_tensor_pipeline-over-a-recorder as GpuStageFn + a staging `cuMemcpy` as TransferFn. ⛔ UNCOMMITTED-BATCH (adds/mods): `engine/ceir-gpu/include/crd/ceir/gpu/tensor_pipeline_exec.hpp` (Transfer/TransferDir/TransferPlan/plan_transfers/stage_class_from_partition decls + the partition_ml/semantics includes) + `engine/ceir-gpu/src/tensor_pipeline_exec.cpp` (impl + landlord_of + transfer_dir_name) + `tests/ceir-gpu/test_two_class.cpp` (NEW) + `tests/ceir-gpu/CMakeLists.txt` (the explicit test-list line). NEXT = **30b-2b** (the engine `execute_two_class` runner + the Host VizDispatch exit (i) + the CUDA sandwich gate: [Gpu,Host,Host,Host,Gpu] bit-exact vs all-Gpu vs all-Host vs oracle, Win + WSL2). | §68 §103; proof §140

**30b-2b-1 ✅ 2026-09-05 (the DEVICE-FREE Host VizDispatch — exit (i): `execute_tensor_pipeline_host` GAINS authored-kernel eval via a caller resolver; retires the 30b-1 authored-kernel-CPU-eval deferral + unblocks fuse=false on BOTH classes; MSVC + WSL gcc)**: advisor SPLIT 30b-2b into 30b-2b-1 (this tick — the pure device-free Host VizDispatch, the fuse-landmine resolution proven all-Host) / 30b-2b-2 (the engine `execute_two_class` runner + the CUDA three-way gate). ⭐ THE MECHANISM (`tensor_pipeline_exec.{hpp,cpp}`): a NEW trailing `HostRunOptions{ HostKernelResolveFn kernel=nullptr; void* user=nullptr; }` param on `execute_tensor_pipeline_host` (⛔ DEFAULT `{}` ⇒ the 30b-1 signature + all its callers stay valid — additive), where `HostKernelResolveFn = bool(*)(StringView symbol, kir::KGraph&, kir::KEntry&, void* user)`. ⛔ the ENGINE names NO asset path (crd-ceir-gpu is GPU-free AND path-free — the "ceir-gpu names no backend" rule extends to assets): the VizDispatch branch reads the stage op's `kernel` attr SYMBOL and hands it to `opts.kernel` — the TEST owns the load. Per VizDispatch stage (`eval_viz_stage_host`): guard `opts.kernel==nullptr || st.op==nullptr` → `UnresolvedKernel`; `opts.kernel(symbol, g, entry, user)` false → `UnresolvedKernel` (NO partial write — the terminal keeps its sentinel); `out_elem = plan.buffers[out_bi].bytes/sizeof(f32)` (guard >0x7fffffff → UnresolvedKernel); `bind_authored_local_size(entry.local_size[0], out_elem, kMaxAuthoredLocalSize)`; wrap ALL binds f32→f64 into `KernelBuffer{data, len, set=0, binding=(u8)i}` (the `ckir_kernel_dispatch.hpp:159` mold — f64 byte-views, read+written in place, NOT eval_cpu's f64 ins[]); `groups = const_grid_dim(ctx, st.op->operand(0))`; `eval_cpu_kernel(g, entry, kb, nbind, entry.local_size[0], alloc, groups)`; f64→f32 back-convert OUTPUTS ONLY (i>=n_in). ⭐ this is the CUDA resolver's VizDispatch branch (test_ceir_pipeline_cuda.cpp:136-168) with `eval_cpu_kernel` where `emit_compute_kernel_cuda` is — the SAME authored .ckir, the SAME `bind_authored_local_size`, so the Host relu is bit-exact vs the CUDA relu by construction. **SHARED HELPER** `tests/gpu-shared/ckir_asset_resolve.hpp` (NEW): `crd::tests::resolve_ckir_asset(symbol, g, entry, user=IAllocator*)` maps relu/viz_magnitude/viz_normalize → CRD_REPO_DIR literal paths (⛔ compile-time literals, no runtime path build) + `ifstream` + `ckir_read` — the CUDA resolver's mold, hoisted so 30b-2b-2 CONSOLIDATES the CUDA test's two inline copies onto it. **GATE** `test_host_exec.cpp` (+2 TEST_CASEs): (1) the ml.mlp planned `fuse=false` → [Gemm, VizDispatch(relu), Gemm] — SECTION bit-exact via the resolver (the relu RAN on Host), SECTION `opts.kernel=nullptr` → UnresolvedKernel, SECTION reject_all → UnresolvedKernel + ⛔ the terminal UNTOUCHED across ALL `mrows*d2` elements (advisor fix: the terminal is a [4,3] tensor, not a scalar — a loop over 12, not `term[0]`, so a partial write of elements 1..11 can't hide); (2) ⭐ the two-class sandwich [gemm→mlp(relu)→gemm] fuse=false share=false → 5 stages run ALL-Host bit-exact vs a CPU oracle (xp=x@W0, h1=relu(xp@W1), yy=h1@W2, z=yy@W3, sequential-k) — ⛔ the seeds are BYTE-IDENTICAL to test_ceir_pipeline_cuda.cpp's `fill_two_class_data_and_oracle` (dd=M=d0=d1=d2=d3=4; advisor fix: aligned the x seed 0.1·(i−8)→0.1·(i−12)) so 30b-2b-2's three-way gate re-seeds the EXACT data ⇒ 30b-2b-2 debugs ONLY transfer + the Gpu arm, not the Host arm. GREEN: **MSVC win-debug** (all 30b 13/13 incl the 2 new #900/#901) + **WSL gcc linux-gcc-debug** (30b-2b-1 2/2). Tidy clean (tensor_pipeline_exec.cpp + .hpp + test_host_exec.cpp + ckir_asset_resolve.hpp — the CRD_REPO_DIR `#define "."` fallback carries the ckir_light_cull_test.hpp NOLINT). ⛔ ADVISOR AFTER-REVIEW (2 findings applied, above; +1 recorded non-hole): `push_stage_profile` still stamps gx=gy=gz=1/workgroups=0 on the VizDispatch row though `const_grid_dim`+`entry.local_size[0]` now give the REAL grid — DELIBERATELY left; 30b-2b-2's profile consumer decides whether a Host workgroup count is "0=interpreter" or the authored grid (a row note, not this tick's code). ⛔⛔ 30b-2b-2 FACTS (advisor, decisions FINAL): **(1) the CUDA resolver refactor** — test_ceir_pipeline_cuda.cpp:136-152's inline ifstream+ckir_read → `resolve_ckir_asset(kv.s, g, ve, res.alloc)` + the include (two copies→one; 30b-2b-2 rebuilds CUDA anyway). **(2) `execute_two_class` computes `transfers` INTERNALLY** from `stage_class` via `plan_transfers` (the 30b-2a correction stands); sig `execute_two_class(ctx, plan, stage_class, host_bufs, gpu_user, GpuStageFn, TransferFn, alloc, HostRunOptions host_opts={}, TensorPipelineProfile* profile=nullptr)` — ⛔ `host_opts` THREADS the relu resolver to the Host stages else the sandwich middle is UnresolvedKernel on Host + the gate can't run. **(3)** HostToDevice → `TransferFn(Transfer, host_bufs[b], gpu_user)` uploads; DeviceToHost → readback into `host_bufs[b]`; the device buffer table is the gpu_user's (the runner never sees a `ComputeBuffer*`); the Gpu stage runner gets the plan slice `[s,s+1)` (the cuda:585 `slice_plan` mold) so `execute_tensor_pipeline` records exactly that stage, the caller wraps begin/submit_and_wait PER STAGE (a transfer sits between class changes + needs the prior submit complete; batching contiguous Gpu runs is 30b-2b-2's optional optimization). ⛔ UNCOMMITTED-BATCH (adds/mods): `engine/ceir-gpu/include/crd/ceir/gpu/tensor_pipeline_exec.hpp` (HostRunOptions + HostKernelResolveFn + the kir::KGraph/KEntry fwd-decl + the trailing opts param) + `engine/ceir-gpu/src/tensor_pipeline_exec.cpp` (eval_viz_stage_host + const_grid_dim + push_stage_profile + the VizDispatch branch at the loop top) + `tests/ceir-gpu/test_host_exec.cpp` (+2 TEST_CASEs, HostRunOptions threaded through run_host, reject_all_kernels) + `tests/gpu-shared/ckir_asset_resolve.hpp` (NEW). NEXT = **30b-2b-2** (the engine `execute_two_class` runner [transfers computed internally] + the CUDA sandwich three-way gate [Gpu,Host,Host,Host,Gpu] bit-exact vs all-Gpu vs all-Host vs oracle + the CUDA resolver refactor onto ckir_asset_resolve.hpp, Win + WSL2). | §68 §103; proof §140

**30b-2b-2a ✅ 2026-09-05 (the engine `execute_two_class` RUNNER + `slice_plan` hoist — proven DEVICE-FREE with a mock device; the MECHANISM proof, the §140 real-domain proof is 30b-2b-2b; MSVC + WSL gcc + the ADL cross-check on both RTX binaries)**: advisor SPLIT 30b-2b-2 into 30b-2b-2a (this tick — the runner + a device-free mock gate) / 30b-2b-2b (the CUDA harness + the three-way + the resolver refactor + the fixture hoist). ⭐ THE RUNNER (`tensor_pipeline_exec.{hpp,cpp}`, beside plan_transfers): `execute_two_class(ctx, plan, stage_class:ConstSpan<ProviderClass>, host_bufs:ConstSpan<f32*>, gpu_user:void*, GpuStageFn gpu, TransferFn xfer, alloc, HostRunOptions host_opts={}, TensorPipelineProfile* profile=nullptr)` — placement AS DATA (a `ProviderClass` per stage) drives a mixed run: a Host stage runs on the CPU via `execute_tensor_pipeline_host` over `host_bufs`, a device stage via `gpu` over the caller's OWN buffer table (through `gpu_user`, never seen here — "ceir-gpu names no backend"). ⛔ the host↔device crossings are DERIVED INTERNALLY (`plan_transfers(plan, stage_class)`, the 30b-2a correction) and applied via `xfer` at each stage boundary — the runner hand-copies NOTHING, the Output ends Host-visible in `host_bufs` (the §140 "placement is semantic, not glue" property). ONE code path: a 1-stage `slice_plan(plan, s, s+1)` for BOTH classes; `apply_before(s)` filters `tp` by `before_stage==s` (a `before_stage==ns` final-readback after the loop). ⛔ ARITY GUARD (`stage_class.size() != plan.stages.size()` → BindingArity) BEFORE plan_transfers (which silently yields empty on a mismatch — a runner proceeding would move nothing + eval on stale/sentinel memory). ⛔ `GpuStageFn = ExecuteError(*)(const TensorPipelinePlan& stage_slice, void*)` (the 1-stage slice; per-stage begin/submit — a transfer sits between class changes + needs the prior submit complete); `TransferFn = ExecuteError(*)(const Transfer&, f32* host_ptr, u64 bytes, void*)` (the runner owns the byte count from the plan). ⭐ PROFILE COMPLETE (advisor): a Host stage profiles itself inside `execute_tensor_pipeline_host`; a device stage → the anon `push_stage_profile(profile, plan, stages[s])` AFTER `gpu` succeeds ⇒ N rows in stage order (Host+device alike, gx/gy/gz=1/workgroups=0). ⛔⛔ `slice_plan` HOISTED from the CUDA test to the engine header (execute_two_class needs it) — EXACT signature match so the CUDA test's local was DELETED (⭐ THE ADL LANDMINE advisor caught: a same-signature local + the engine one = an AMBIGUOUS call for every unqualified `slice_plan(plan,…)` since `TensorPipelinePlan ∈ crd::ceir::gpu` makes ADL find the engine one ALONGSIDE the local — a compile error in a TU I wasn't editing; deleting the local makes the unqualified calls resolve to the engine's, ZERO call-site edits). **GATE** `test_two_class.cpp` (+2 TEST_CASEs, DEVICE-FREE): a **mock device** = a SECOND f32 table (SENTINEL until a transfer populates it — a missing/late upload leaves garbage ⇒ the bit-exact FAILS) run by a 2nd Host executor (`mock_gpu_stage`) + LOGS (`xfer_log`, `gpu_ops`). (1) the sandwich under [Gpu,Host,Host,Host,Gpu] / reverse [Host,Gpu,Gpu,Gpu,Host] / all-Host / all-Gpu → ALL bit-exact vs the CPU oracle (the placement changed nothing) + ⭐ `xfer_log == plan_transfers(plan, sc)` by IDENTITY & IN ORDER + `gpu_ops ==` the device-class stages + the FULL profile kind-sequence [Gemm,Gemm,VizDispatch,Gemm,Gemm]; negatives — arity → BindingArity/0-ran/Output-sentinel; `host_opts.kernel=nullptr` → the Host relu UnresolvedKernel + `gpu_calls==1` (⭐ the resolver-THREADING proof — the Gpu prefix ran, the Host relu couldn't); a `gpu`/`xfer` error → propagated, nothing after. (2) ⭐ `share_intermediate_storage=true` → aliased intermediates: bit-exact + EVERY transfer names a LANDLORD (`alias_of<0`) + ≥1 landlord-of-an-alias CROSSES the boundary (`landlord_crossings≥1` HELD on the 4-wide sandwich — the landlord plumbing runs THROUGH a transfer, not only inside the Host run). GREEN: **MSVC win-debug + WSL gcc linux-gcc-debug** (30b-2b-2a 2/2 both) + ⭐ the ADL CROSS-CHECK: `crd-ceir-gpu-cuda-tests` BUILDS on both compilers (the local-slice_plan deletion resolves by ADL under MSVC + gcc) and **29b-1 + 29c-2 bit-exact on Win RTX + WSL2 RTX** (the fresh CUDA binary — the deletion is correct at runtime, blast-radius mandated). Tidy clean (tensor_pipeline_exec.cpp + .hpp + test_two_class.cpp + test_ceir_pipeline_cuda.cpp). ⛔ ADVISOR AFTER-REVIEW (2 findings applied): **(1)** the reverse section asserted a PLANNER property (`before_stage != ns` — 30b-2a (c) already proves it, can't fail through the RUNNER) → REPLACED with the `xfer_log == plan_transfers` IDENTITY under the FLIPPED placement (the runner-side assertion that catches a wrong-transfer-set) + `count_before(tp, ns)==0`; **(2)** share=true only checked `aliases≥1` (couldn't tell if an alias CROSSED) → added the every-transfer-names-a-landlord contract + `landlord_crossings≥1` (ran it — it HELD, so no row-note softening needed). +profile FULL kind-sequence (not just size + stages[2]) + a tp-order==log-order CONTRACT comment on `apply_before`. ⛔ ROW NOTES (advisor, name-forward): `else`=device in execute_two_class is Host-vs-not-Host EXHAUSTIVE for the 2-class world — a 3rd `ProviderClass` needs a TOTAL switch here (correct default today: an unknown class is "not Host"); the tp-order==log-order the gate asserts is a CONTRACT (a future re-sort of plan_transfers breaks the gate or must fix it). ⛔ UNCOMMITTED-BATCH (adds/mods): `engine/ceir-gpu/include/crd/ceir/gpu/tensor_pipeline_exec.hpp` (slice_plan + GpuStageFn/TransferFn + execute_two_class decls) + `engine/ceir-gpu/src/tensor_pipeline_exec.cpp` (slice_plan + execute_two_class impl) + `tests/ceir-gpu/test_two_class.cpp` (+2 mock TEST_CASEs; MockDevice/mock_gpu_stage/mock_transfer/fill_sandwich/SandwichData/SandwichIO/Seed/run_two_class_mock; build_sandwich gained `io`+`share`) + `tests/ceir-gpu-cuda/test_ceir_pipeline_cuda.cpp` (the local `slice_plan` DELETED — the ADL note). NEXT = **30b-2b-2b** (the CUDA harness: `gpu_user`={Resolver, dev[40] alias→landlord NO-seed, compute*}; `GpuStageFn` = begin → `execute_tensor_pipeline(slice, rec, resolve_stage, &res, dev_span)` → submit_and_wait; `TransferFn` = staging CpuToGpu/GpuToCpu + `rec.copy` + submit_and_wait; the THREE-WAY [Gpu,Host,Host,Host,Gpu] vs all-Gpu vs all-Host vs `fill_two_class_data_and_oracle`, every element ==, Win RTX + WSL2 RTX; + the resolver refactor cuda:138-152 → `resolve_ckir_asset`; + HOIST fill_sandwich/SandwichData → `tests/gpu-shared/two_class_fixture.hpp`, one oracle body). | §68 §103; proof §140

**30b-2b-2b ✅ 2026-09-05 (THE §140 REAL-DOMAIN PROOF — `execute_two_class` runs the sandwich Host(CPU)+CUDA, BIT-EXACT vs all-Host vs all-Gpu vs the oracle on BOTH RTX; CLOSES 30b-2b AND 30b-2)**: the CUDA harness for the 30b-2b-2a runner + the three-way gate. ⭐ THE GATE (`test_ceir_pipeline_cuda.cpp`, `[ceir][ml][gpu][cuda]`): the SAME 5-stage sandwich plan (`build_two_class_plan`, mrows=4/d0=8/d1=8/d2=2/d3=2 — 29c-2's dims) run THREE ways + the CPU oracle: **ARM 1 all-Host** (`execute_tensor_pipeline_host`, device-FREE — runs in CI with no GPU) → bit-exact vs oracle; **ARM 2 all-CUDA** (`execute_tensor_pipeline` whole plan); **ARM 3 the Host+CUDA SPLIT** [Gpu,Host,Host,Host,Gpu] via `execute_two_class` — ⛔ the placement DELIBERATELY CROSS-CUTS the partition tags (the mlp+relu that cuda_graphs claimed run on the CPU, the flanking fallback gemms on CUDA) proving the split is driven by the placement vector, NOT the plan. split == all-Gpu == all-Host == oracle, EVERY element (placement changed the WHERE, never the WHAT — "placement is semantic, not glue"). ⭐ THE CUDA HARNESS: `TwoClassGpu` = {Resolver, `dev[40]` alias→landlord NO-seed, `bufs[40]` raw span, compute*, identity counters}; `cuda_gpu_stage(slice)` = begin → `execute_tensor_pipeline(slice, rec, resolve_stage, &res, dev_span)` → submit_and_wait; `cuda_transfer(t, host_ptr, bytes)` = per-transfer staging (CpuToGpu upload / GpuToCpu readback, min-16 buffer + `rec.copy` `bytes` + submit_and_wait). The ExternalIn uploads are PLANNED transfers (NO device seeding — the Host-born model) ⇒ a fresh device table filled ONLY by `plan_transfers`. ⭐⭐ FALSIFIABILITY (3 identities, advisor): `n_pipes==2` (exactly the 2 device stages resolved on the CUDA Resolver — none of the Host stages touched it) + `n_xfer == plan_transfers(plan,sc).size()` (the runner applied EXACTLY the planned crossings, hand-copied nothing) + ⛔ (advisor AFTER, the blocking find) **a §137 PROFILE row per stage** asserted `[Gemm,Gemm,VizDispatch,Gemm,Gemm]` in order — proving the HOST stages 1-3 actually RAN (without it, a silent Host-stage skip zero-propagates through the beta=0 tail and only the seed pattern would catch it — not an assertion). ⛔ SENTINEL (advisor AFTER): both host-table helpers fill `-777.0F` (a skipped Host stage leaves a loud marker, not a clean 0; the beta=0 biases stay safe: 0·−777=0). ⭐ THE FIXTURE HOIST (advisor: "one oracle body"): `tests/gpu-shared/two_class_fixture.hpp` (NEW) = `crd::tests::fill_two_class_sandwich(mrows,d0,d1,d2,d3, x,w0,w1,w2,w3,oracle)` — the general-dims body from the CUDA test, now the SINGLE oracle shared by ALL THREE gates (the CUDA three-way, test_two_class's mock at dd=4, test_host_exec's all-Host at dd=4 — all inline copies DELETED). ⭐ THE RESOLVER REFACTOR: the CUDA VizDispatch loader (was inline ifstream+ckir_read) → `crd::tests::resolve_ckir_asset(kv.s, g, ve, res.alloc)` — the CUDA relu and the Host relu now load the IDENTICAL authored kernel via one loader. GREEN: **Win RTX #4847 + WSL2 RTX #4694** (the three-way, both devices) + 29b-1/29c-2 re-green both (blast radius: the resolver refactor + fixture hoist) + the device-free 30b suite green both (test_host_exec + test_two_class re-seeded through the shared fill). Tidy clean (two_class_fixture.hpp + test_two_class.cpp + test_host_exec.cpp + test_ceir_pipeline_cuda.cpp). ⛔ ORDER (advisor, the ADL scar): hoist+refactor landed + rebuilt+green FIRST, THEN the harness+gate — two independent CUDA-TU changes at once is a bisect. ⛔ ROW NOTES (advisor, name-forward): the all-Gpu arm is the ONLY place a device buffer is read before any write reaches it (the unseeded beta=0 biases are garbage there, safe only because beta=0 — an argument FOR the planned-transfer model: the mixed arm uploads them, always correct); `execute_two_class`'s per-stage submit = N submits + T transfer submits (correct FIRST form, measurably slower — the batched-contiguous + single-submit-enqueue form is 30z's bench arm, the census-predicted honest toy-dim loss; do NOT optimize here). ✅✅ **30b-2b COMPLETE** = 30b-2b-1 (Host VizDispatch) + 30b-2b-2a (the device-free runner + mock gate) + 30b-2b-2b (this — the CUDA real-domain proof). ✅✅ **30b-2 COMPLETE** = 30b-2a (plan_transfers) + 30b-2b. ⛔ UNCOMMITTED-BATCH (adds/mods): `tests/gpu-shared/two_class_fixture.hpp` (NEW) + `tests/ceir-gpu-cuda/test_ceir_pipeline_cuda.cpp` (the resolver refactor + the local fill DELETED + the harness `TwoClassGpu`/`cuda_gpu_stage`/`cuda_transfer`/`run_all_host`/`run_all_gpu`/`run_two_class_cuda` + the three-way TEST_CASE) + `tests/ceir-gpu/test_two_class.cpp` (fill_sandwich → shared fill; cmath dropped) + `tests/ceir-gpu/test_host_exec.cpp` (the inline oracle → shared fill). NEXT = **30b-3** (the §140 CHAIN on the {Host,Gpu} mesh: `declare→shard(axis0,mesh "2")→reduce(axis0,sum)→all_reduce(sum)→export` LOWERED to a two-class plan — rank0's shard→Host, rank1's→Gpu, per-shard reduce, all_reduce = elementwise(add) of the two partials; ⛔ the placement vector is PRODUCED from the shard annotation's mesh rank [the new fn beside stage_class_from_partition], NOT hand-written; ⛔ the oracle is the TWO-STAGE oracle [reduce(shard0)+reduce(shard1), NOT a flat sum — f32 sequential-k is non-associative]; run through execute_two_class on the real device Win+WSL2, bit-exact; NO all-Gpu arm [emit_reduce_cuda ledgered-absent] — the honest shape). | §68 §103; proof §140

**30b-3a ✅ 2026-09-05 (the sec-140 reduction LOWERING + placement PRODUCER — device-free; `lower_sharded_reduction` beside materialize_sharding; MSVC + WSL gcc)**: advisor SPLIT 30b-3 into 30b-3a (this tick — the lowering + placement producer, gated device-free) / 30b-3b (the same plan through `execute_two_class` on real CUDA). ⭐ THE PASS (`sharding.{hpp,cpp}`): `lower_sharded_reduction(ctx, m, alloc, rank_lineage:HashMap<const Operation*,i32>&) -> LowerResult{u32 ranks, bool had_conflict}`. Precondition: materialize_sharding has RUN (the dist.all_reduce completing the escaping Partial exists). IN-PLACE (the materialize model reversed): (1) ⛔ CONFLICT SCAN FIRST — propagate_sharding + `region_has_conflict`; ANY Conflict → refuse `{0,true}`, module BYTE-IDENTICAL (a partial rewrite corrupts). (2) find the sec-140 chain `declare/import -> dist.shard(mesh,axis) -> tensor.reduce(axis,fn) -> dist.all_reduce(fn)` (verified via the analysis: shard=Sharded/single-use, reduce=Partial/single-use, all_reduce=Replicated). (3) E>=2 (`extent = mesh_extent(mesh, mesh_axis)`) → build E per-rank `resource.declare` shard tensors `[N/E,…]` (`shard_tensor_type` divides dim[axis]) + E `tensor.reduce` ops EACH TAGGED `rank_lineage[rd]=rank` (the placement producer `plan_tensor_pipeline_partitioned` reads → PlanStage.provider) + an (E-1)-op `tensor.elementwise` combine tree (`combine_op_for`: sum→add/prod→mul/max→max/min→min — all four wired in synth_elementwise; else refuse), inserted before the original declare; RAUW `all_reduce.result -> combine`, erase the chain CONSUMER-FIRST (ar→r→ts→t, each legal — the prior erase cleared the last use), `strip_dead_meshes`. (4) E==1 the 1-DEVICE IDENTITY: strip every degenerate dist op (`first_degenerate_dist_op` — shard/all_reduce over a size-1 axis) + dead meshes → the unsharded reduce (`ranks==1`, rank_lineage empty). ⛔ the emitted module carries NO dist ops. ⛔⛔ ADVISOR RECONCILE (a design fork surfaced before coding — the print-identity contradiction): my earlier gate said mesh-1 → `print(before)==print(after)`, but the lowering MUST change the module (removing dist ops is its job) → the correct identity is `print(lowered_mesh1) == print(a SEPARATELY-authored plain declare->reduce->export)`, byte-identical; `print(before)==print(after)` belongs ONLY to the refuse path. ⛔⛔ THE PLANNER FIX (Option B, advisor-reconciled — my "no planner code touched" design note was WRONG, empirically: the planner rejected the lowered module `UnsupportedOp`): `resource.export` ADDED to the planner's skip list (tensor_pipeline.cpp:425, the `func.return`/`func.func`/`arith.const` category — a RESULTLESS OUTPUT BOUNDARY; Output is still the final stage's trailing write, which the export reads). NOT stripped by the lowering (export is not a dist op; stripping it in the bridge would be a lossy translator erasing the output marker). ⛔ `resource.export` is a REGISTERED op (build_export/export_kind/verifier in resource_ops.hpp) — not a phantom; only test_sharding.cpp used it, so the skip is behavior-inert for every existing plannable module (proven fresh below). **GATE** `test_sharding.cpp` (+1 TEST_CASE, 3 SECTIONs, DEVICE-FREE): (split) mesh-2 `declare[8,4]->shard(axis0)->reduce(axis0,sum)->export`, materialize (inserts 1 all_reduce), lower → `ranks==2`, 2 reduces tagged EXACTLY {0,1} (creation order deterministic), NO dist ops; `plan_tensor_pipeline_partitioned` (assignments provider 0/1) → `[Reduce,Reduce,Elementwise]` providers `[0,1,-1]`; `stage_class_from_partition({Host,Gpu}, Host)` → `[Host,Gpu,Host]`; `plan_transfers` by ⭐ IDENTITY (not category) — the ONE H2D upload's `.buffer` == rank-1's shard plan-buffer index, the ONE D2H readback's `.buffer` == `stages[1]` trailing bind (rank-1's partial), NO final readback (combine on Host); all-Host run (`execute_tensor_pipeline_host`) == a TWO-STAGE f32 oracle `fold(rows0-3)+fold(rows4-7)` EVERY element AND `two_stage != flat` (one 8-row fold) on ≥1 element (f32 non-associativity proven LIVE, seed `0.1*(i-12)` — exposed it, no seed change). (identity) mesh-1 → `ranks==1`, untagged, plan `[Reduce]`, byte-identical print vs a plain `declare->reduce->export`. (refuse) a Sharded value into a matmul → Conflict → `had_conflict`+`ranks==0`+module byte-identical. GREEN: **MSVC 30b-3a #896 + band-30 29/29 + 22c 12/12 + 26e-4** ; **WSL gcc 30b-3a + band-30 29/29 + 22c 11/11** (gcc -Werror switch-check clean). Tidy clean (sharding.cpp + tensor_pipeline.cpp + test_sharding.cpp; the local const `E`→`extent` for the localConstant gate). ⛔ ADVISOR AFTER-REVIEW (2 gaps applied): **(1) THE STALE-SIBLING-EXE SCAR** (`feedback_stale_exe_sibling_false_green`) — `crd-ceir-gpu.lib` changed but the CUDA/DX12/Vulkan test exes are SIBLINGS not deps, so ninja didn't relink them; #4848 (30b-2b-2b CUDA), 22c-3e + 26e-4 (DX12) had run with the OLD planner baked in → RELINKED all three device exes + reran FRESH: **30b-2b-2b #4848 + 22c-3e #4893 + 26e-4 #4901 green** (the planner skip is provably inert for export-free modules, now VERIFIED not inherited). **(2)** the plan_transfers gate was CATEGORY (counted `(dir,before_stage)` pairs) → rewritten to IDENTITY (buffer-index match, above); + the lineage check `(*g0==0&&*g1==1)||(*g0==1&&*g1==0)` had a DEAD second branch contradicting `stages[0].provider==0` → tightened to `*g0==0 && *g1==1`. ⛔ LEDGER for 30c (advisor): rank→device is `mesh_classes[r]` (caller-supplied here) → 30c makes it an authored asset; the SEAM CONFLICT — materialize's escape model needs a USE (the export IS the escape), the pipeline's Output model is a DEAD last-stage result; export bridges them today, 30c picks the authored terminal deliberately; an export reading an INTERMEDIATE (not the terminal) has NO liveness effect under `share_intermediate_storage` (its buffer could be aliased away) — not reachable in -3a's single chain, ledgered. ⛔ UNCOMMITTED-BATCH (adds/mods): `engine/ceir-gpu/include/crd/ceir/gpu/sharding.hpp` (LowerResult + lower_sharded_reduction decl) + `engine/ceir-gpu/src/sharding.cpp` (lower_sharded_reduction + region_has_conflict/first_op_named/first_degenerate_dist_op/strip_dead_meshes/combine_op_for/shard_tensor_type helpers) + `engine/ceir-gpu/src/tensor_pipeline.cpp` (the resource.export skip) + `tests/ceir-gpu/test_sharding.cpp` (the 30b-3a gate + collect_named/fold_sum/Seed/run_all_host helpers). NEXT = **30b-3b** (the SAME lowered plan through `execute_two_class` on real CUDA — ~~rank0→Host, rank1→Gpu~~ **[STRUCK at 30b-3b: rank1's reduce CANNOT run on CUDA (30-0 strike — trailing-axis + push-ABI); the honest placement is `[Host, Host, Gpu]` — BOTH reduces on Host, the all-reduce COMBINE on CUDA]**, Win RTX + WSL2 RTX, bit-exact vs the two-stage oracle; ⛔ NO all-Gpu arm — the honest shape). | §68 §103; proof §140

**30b-3b ✅ 2026-09-05 (THE §140 SHARDED-REDUCTION PROOF on Host+CUDA — `lower_sharded_reduction`'s output runs through `execute_two_class` `[Host,Host,Gpu]`, bit-exact vs all-Host + the two-stage oracle on BOTH RTX; CLOSES 30b-3 AND 30b)**: the real-device §140 proof for the reduction lowering. ⛔⛔ ADVISOR-DRIVEN SCOPE CORRECTION (the census sketch was wrong): the census said "rank0→Host, rank1→Gpu" + "emit_reduce_cuda absent". Orientation found `emit_reduce_cuda` EXISTS (ckir_cuda.hpp:712) but is (a) TRAILING-AXIS ONLY (the §140 axis0 reduce is an outer/strided fold it returns false on) and (b) its kernel takes TWO scalar params, incompatible with `CudaComputeContext`'s single-blob push (cuda_compute_context.cpp:190) while `backend_cuda.cpp:395`/`backend_hip.cpp:127` pass scalars — the cross-launch-site CONTRACT of [[feedback_cuda_emitter_signature_is_a_contract_shared_by_every_launch_site]] (which the recall SAVED me from a bad 3-file surgery on). ⭐ ADVISOR RULING (reconcile): scope discipline wins — the reduce-on-CUDA contract is its OWN slice (touching kir-cuda/kir-hip inside a 30b tick, on hardware where HIP can't run, is the dangerous seam the scar warns of); 30b-3b runs the per-rank reduces on HOST (eval_cpu handles the axis0 strided fold) and the all-reduce COMBINE (elementwise — `emit_elementwise_cuda`, already reconciled + proven in 29b-1) on CUDA. This is the more HONEST §140 proof: the COLLECTIVE (all-reduce) is what §140 is about, and it runs cross-domain with real transfers. ⛔ NO ENGINE CODE TOUCHED — the honest proof needed none (the combine is a `StageKind::Elementwise` the CUDA resolver already handles). **THE GATE** `test_ceir_pipeline_cuda.cpp "ceir 30b-3b"` (`[ceir][ml][gpu][cuda][dist]`): build the §140 axis0 chain `declare[8,4]→shard(mesh "2",axis0)→reduce(axis0,sum)→export`, `materialize_sharding` (inserts 1 all_reduce), `lower_sharded_reduction` → ranks==2 + rank_lineage; `plan_tensor_pipeline_partitioned` → `[Reduce,Reduce,Elementwise]`; ⭐ the placement `[Host,Host,Gpu]` PRODUCED (not hand-written) via `stage_class_from_partition(plan, providers=[{Host},{Host}], fallback_class=Gpu)` — both tagged reduces→Host, the untagged combine→the Gpu fallback (the "two producers, one consumer" promise). TWO arms: **ARM 1 all-Host** (`execute_tensor_pipeline_host`, device-FREE — CI-runnable) == the two-stage oracle `fold(rows0-3)+fold(rows4-7)` per col (≠ a flat 8-row fold on ≥1 col — f32 non-assoc, `any_diff` asserted); **ARM 2 the split** `[Host,Host,Gpu]` via `execute_two_class` == all-Host == oracle EVERY element. ⭐⭐ FALSIFIABILITY: `n_pipes==1` (EXACTLY the ONE combine resolved on the CUDA Resolver — both reduces ran on Host, else n_pipes would count them) + `n_xfer == plan_transfers(plan,sc).size()` == 3 (2 partial uploads @stage 2 + ⭐ the ONE Output readback at `before_stage==ns` — the identity that fires ONLY because the combine ran on the DEVICE; a combine-ran-on-Host bug would drop it) + a §137 profile `[Reduce,Reduce,Elementwise]` (the two Host reduces + the Gpu combine all RAN, no silent skip). GREEN: **Win RTX #4849 + WSL2 RTX #4696** + blast radius fresh on BOTH devices (29b 3/3, 29c-2 2/2, 30b-2b-2b 1/1 — the CUDA exe rebuilt, header-only additions). ⛔ NO STALE-SIBLING HAZARD this tick: no engine `.lib` was touched (sharding.cpp/tensor_pipeline.cpp unchanged), so the DX12/Vulkan exes stay fresh from 30b-3a's relink. Tidy clean (test_ceir_pipeline_cuda.cpp + test_sharding.cpp). ⭐ the axis1 (INNERMOST) device-free twin ADDED to `test_sharding.cpp`'s 30b-3a case (advisor: prove the geometry a FUTURE reduce-on-CUDA contract slice needs, device-free first; the reduce runs on Host in 30b-3b). ⛔ ROW NOTE (advisor): the two-stage oracle differs from a flat fold on ≥1 column, not all four (`any_diff`) — the bit-exact claim is "vs the two-stage oracle", the transfers prove the combine ran on-device. ✅✅ **30b-3 COMPLETE** = 30b-3a (the lowering + placement producer, device-free) + 30b-3b (the Host+CUDA §140 proof). ✅✅ **30b COMPLETE** = 30b-1 (Host exec) + 30b-2 (two-class) + 30b-3 (sharded reduction). ⛔ UNCOMMITTED-BATCH (adds/mods): `tests/ceir-gpu-cuda/test_ceir_pipeline_cuda.cpp` (+`sharding.hpp`/`dist.hpp` includes + `collect_named_cuda`/`fold_sum_c` helpers + the 30b-3b TEST_CASE) + `tests/ceir-gpu/test_sharding.cpp` (the axis1 device-free SECTION). NEXT = ~~**30c**~~ **30c-1 ✅ (below)** (AUTHORED placement — the `mesh_classes[r]` / `fallback_class` the caller supplied became an AUTHORED transform-dialect directive, §146 "placement is an asset"; composes with 29's pin/class directives). | §68 §103; proof §140

**30c-1 ✅ 2026-09-05 (AUTHORED PLACEMENT — `transform.place_mesh` directive → `placement_from_transform` loader → `stage_class_from_placement` (the THIRD per-stage ProviderClass producer); device-free; MSVC + WSL gcc)**: advisor SPLIT 30c into 30c-1 (this tick — op + `find_transform_misuse` guard + loader + third producer + a device-free gate on a BUILT transform module) / 30c-2 (the committed `.ceir` asset via bootstrap-via-print, next tick). §146 "placement is an asset": the `mesh_classes[r]`/`fallback_class` the 30b-3b caller HAND-WROTE become an AUTHORED transform-dialect directive. **THE OP** `transform.place_mesh {mesh(symbol), classes(string comma-list), fallback(string)}` added to `transform.ceirop.toml` + regenerated ALL dialects (`ceir_opgen.py` — `mesh`/`classes`/`fallback` are clean C++ idents, no keyword sanitize) → `build_place_mesh`/`verify_place_mesh`/register. **THE GUARD** `find_transform_misuse` gains a `seen_place` bool → a second `transform.place_mesh` is `DuplicateDirective` (the at-most-one contract, region-recursive I6-name scan). **THE LOADER** `placement_from_transform(ctx, transform_mod, payload, alloc) → Placement{Array<ProviderClass> rank_classes, ProviderClass fallback, const Operation* op, PlacementKind kind}`: resolves `mesh` (a Symbol) against the PAYLOAD's `dist.mesh` ops (so the rank-count validation has the extent) via the hoisted-public `gather_meshes`/`resolve_mesh`/`mesh_extent` (MOVED from anon in sharding.cpp — the placement loader's mesh-query helpers); comma-splits `classes` via `provider_class_from_name`; validates count == mesh axis-0 extent. `PlacementKind{None, Resolved, UnknownMesh, MultiAxisMesh, UnknownProviderClass, RankCountMismatch}`. **THE THIRD PRODUCER** `stage_class_from_placement(plan, ConstSpan<ProviderClass> rank_classes, fallback, alloc)`: `provider<0 || >=size → fallback else rank_classes[provider]` — ⭐ body byte-identical to `stage_class_from_partition` MINUS the `.provider_class` deref (INTENTIONAL — the identity gate depends on it, NOT duplication to refactor: a future tidy that merges them would break the two-producer claim). **THE GATE** `test_sharding.cpp "ceir 30c-1"` (`[ceir][dist][sharding][transform]`, device-free): a built `transform.place_mesh(mesh="m2", classes="host,host", fallback="gpu")` resolves→`{Resolved, [Host,Host], Gpu}`; ⭐ the typed rejects each asserted by IDENTITY (kind AND `op != nullptr` — the loader's contract is "op points at the offending directive so the caller can SURFACE the reject at the right site"; a category-only check would false-green a dropped op): `m9`→UnknownMesh, `host,bogus`→UnknownProviderClass (classes segment), `.../bogus` fallback→UnknownProviderClass (the SAME kind covers BOTH attrs), 3-class→RankCountMismatch, ⛔ a 2-D mesh `"2,2"`→MultiAxisMesh (⭐ advisor-AFTER hardening: a 2-D mesh is verify-CLEAN — `find_dist_misuse` allows it — so axis-0 validation would SILENTLY place 2 classes onto 4 ranks; refuse a >1-axis mesh TYPED via the `mesh_extent(...,1)!=0` probe, a `mesh_axis` attr is the sec-71 named-forward — the code refuses what it names forward, doesn't accept it silently); + DuplicateDirective on two directives; then LOWER (`lower_sharded_reduction` ranks==2) + `plan_tensor_pipeline_partitioned` → `stage_class_from_placement == [Host,Host,Gpu] == stage_class_from_partition` element-wise (⭐ IDENTITY — the authored placement produces the SAME per-stage vector as the hand-written partition producer: "two producers, one consumer", now THREE). ⛔ extent<=0 (a malformed axis-0 shape) is unreachable past `find_dist_misuse`'s MeshShapeInvalid guard in the intended flow (30c-2 wires dist-verify-first) — folded into RankCountMismatch as belt-and-braces, commented. GREEN: **Win #897 + WSL gcc #897** (both RECOMPILED+relinked THIS tick — ⛔ a wrong target name `crd-tests-ceir-gpu` (unknown, EXIT=1) silently no-op'd the first Win build; CAUGHT by verifying the real `add_executable` name `crd-ceir-gpu-tests`, rebuilt correct + reran — the stale-exe-false-green doctrine, a passing #897 on the prior-tick exe would have validated OLD 30c-1, not the new MultiAxisMesh code); regression Win transform 58/58 + opgen-drift 1/1 (drift-clean, NO toml regen needed this tick) + band-30 (`ceir 30`) 23/23 + 29a-3b 2/2 + 29c 5/5. ⛔ STALE-SIBLING scar honored — `partition_ml.cpp` lives in the `crd-ceir-gpu` LIB, so the cuda/dx12/vulkan test exes RELINKED fresh (EXIT=0) + reran (cuda 30b-3b 1/1 + `13z` 29/29 cross-backend green). ⭐ LEDGER: `ceir_opgen.py` regen is idempotent across toolchains (the shared tree stays drift-clean both — no regen this tick). Tidy clean (partition_ml.cpp + test_sharding.cpp). ⛔ UNCOMMITTED-BATCH (adds/mods): `engine/ceir/ops/transform.ceirop.toml` + regenerated `engine/ceir/generated/crd/ceir/gen/transform_ops.{hpp,cpp}` + `engine/ceir/src/transform.cpp` (seen_place) + `engine/ceir-gpu/include/crd/ceir/gpu/{sharding.hpp,partition_ml.hpp,tensor_pipeline_exec.hpp}` + `engine/ceir-gpu/src/{sharding.cpp,partition_ml.cpp,tensor_pipeline_exec.cpp}` + `tests/ceir-gpu/test_sharding.cpp`. NEXT = ~~**30c-2**~~ **30c-2a ✅ (below)** (the committed `assets/ceir/place_mesh_host_host_gpu.ceir` — renamed per the `<directive>_<value>` mold — via bootstrap-via-print; the CUDA end-to-end is 30c-2b). | §68 §103 §146; proof §140

**30c-2a ✅ 2026-09-05 (the COMMITTED placement .ceir ASSET + its canonical reading gate — `assets/ceir/place_mesh_host_host_gpu.ceir` via bootstrap-via-print; device-free; MSVC + WSL gcc)**: advisor SPLIT 30c-2 into 30c-2a (this tick — the committed asset + the device-free reading gate + the dist-verify-first discharge + the shared-slurp hoist) / 30c-2b (the CUDA end-to-end, next tick). §146 "the placement IS an authored asset": the `transform.place_mesh` directive 30c-1 built in memory becomes a COMMITTED `.ceir`. **THE ASSET** `assets/ceir/place_mesh_host_host_gpu.ceir` = `module {^bb0: transform.place_mesh() {classes = "host,host", fallback = "gpu", mesh = @m2}}` — bootstrap-via-print (a throwaway `[.][bootstrap30c2]` case WARN'd `print(mk_place_module("m2","host,host","gpu"))`, copied verbatim, DELETED). ⭐ named `<directive>_<value>` per the mold (`assign_provider_coopvec`/`constrain_provider_class_gpu`), NOT the consumer. **THE GATE** `test_sharding.cpp "ceir 30c-2"` (`[ceir][dist][sharding][transform]`, device-free), mirrors 29a-3b-2: build the §140 payload (mesh "m2" + declare→shard→reduce→export, materialize) → ⭐ `dist::find_dist_misuse(payload)==None` BEFORE the loader (dist-verify-FIRST) → slurp + `parse().ok` → ⭐ ANTI-DRIFT `print(parse(file)) == print(mk_place_module)` (through the PRINTER, NEVER file-bytes — the CEIR-28 committed-asset scar) → roundtrip-stable → `find_transform_misuse==None` → `placement_from_transform → {Resolved,[Host,Host],Gpu}` → LOWER + plan → the LOADED placement drives `stage_class_from_placement == [Host,Host,Gpu]`. ⛔ KEEP `mk_place_module` as the anti-drift ORACLE (do NOT delete — a `.ceir` module has no author-then-delete path; regenerate the file if the builder changes). ⭐ the **Resolved path** is proven through the ASSET here; the **rejects** (UnknownMesh/MultiAxisMesh/UnknownProviderClass/RankCountMismatch) are proven on BUILT modules (30c-1) — no false "rejects via the asset path" inheritance. **THE HOIST** band24's anon-namespace `slurp` → `tests/gpu-shared/ceir_asset_slurp.hpp` `test_support::slurp_asset` (ONE reader for band24 + sharding + the coming CUDA arm, no drift — search-engine-before-building); **4 call sites / 2 cases** in band24 rewired. ⛔ HONESTY (advisor): the 30c-1 `partition_ml.cpp` "extent<=0 unreachable past find_dist_misuse (30c-2 wires dist-verify-first)" fold is asserted at the DEVICE-FREE site THIS tick; the OTHER real call site (the CUDA 30b-3b arm) does NOT yet walk find_dist_misuse — that lands in 30c-2b, where the fold comment closes FULLY. GREEN: **Win #898 + WSL gcc #898**; band24 blast radius 29a-3b-2 #883 + 29c-3b #887 both toolchains (the hoist, complete). ⛔ NO engine code touched (asset + 2 test files + 1 new shared header); NO `.lib` touched → NO stale-sibling relink for cuda/dx12/vulkan this tick (they stay fresh from 30c-1's relink). `opgen-drift` untouched (no toml change). Tidy clean (test_sharding.cpp + test_band24_gate.cpp + ceir_asset_slurp.hpp). ⛔ UNCOMMITTED-BATCH (adds): `assets/ceir/place_mesh_host_host_gpu.ceir` (NEW) + `tests/gpu-shared/ceir_asset_slurp.hpp` (NEW) + `tests/ceir-gpu/test_sharding.cpp` (the 30c-2 gate + parse/slurp includes) + `tests/ceir-gpu/test_band24_gate.cpp` (the slurp hoist). NEXT = **30c-2b** (the CUDA end-to-end): AUGMENT 30b-3b's CUDA harness (NOT a new case — duplicating the oracle body violates the one-oracle-body rule from 30b-2b-2b) — add `dist::find_dist_misuse(payload)==None` before the loader (closes the fold FULLY), slurp + parse the asset via `slurp_asset`, `placement_from_transform → stage_class_from_placement` → assert element-wise `== scv` (the hand-written `stage_class_from_partition` STAYS as the PARITY reference — deleting it degrades the arm to can't-fail), then the LOADED vector DRIVES `execute_two_class`; tag `[30c-2b]`; Win RTX + WSL2 RTX; CUDA exe relink only. **[30c-2b ✅ below]** | §68 §103 §146; proof §140

**30c-2b ✅ 2026-09-05 (the CUDA END-TO-END — the LOADED `place_mesh_host_host_gpu.ceir` placement DRIVES `execute_two_class` on real CUDA; CLOSES 30c-2 AND 30c; Win RTX + WSL2 RTX)**: the committed asset 30c-2a proved device-free now drives a real-device run. AUGMENTS the 30b-3b CUDA test (NOT a new case — duplicating the oracle body violates the one-oracle-body rule from 30b-2b-2b; renamed `ceir 30b-3b/30c-2b`, `[30c-2b]` tag added — ⛔ the ctest NUMBER moved #4850→**#4851 Win**, #4696→**#4698 WSL**, and the tag is COSMETIC: `ctest -R` matches the NAME, the name change is what makes `-R 30c-2b` work). **THE FLOW** (after materialize, BEFORE lower — `lower_sharded_reduction` STRIPS the mesh, the loader needs mesh "m2" present): ⭐ `dist::find_dist_misuse(payload)==None` (dist-verify-FIRST — this is the OTHER real call site the 30c-1 `partition_ml.cpp` fold comment named; with BOTH sites now walking find_dist_misuse the "extent<=0 unreachable past find_dist_misuse" fold is HONEST END-TO-END with NO code edit — I chose NOT to touch the `.lib` for a comment-only change [it forces the full sibling relink]; the comment's forward-ref to "30c-2" is now a completed fact, so it stands as-written) → `slurp_asset` + `parse().ok` → `placement_from_transform → {Resolved,[Host,Host],Gpu}` → LOWER → `stage_class_from_placement` = `scv_asset`. ⭐ PARITY: the hand-written `stage_class_from_partition` = `scv` STAYS as the reference (deleting it degrades the arm to can't-fail, `[[feedback_deleting_reference_in_ab_parity_test_degrades_to_can_t_fail]]`); `scv_asset == scv` asserted by **REQUIRE** (⛔ advisor-AFTER: NOT CHECK — the LOADED vector drives execution below, so plan_transfers/n_xfer/oracle all self-agree with WHATEVER the asset resolved to; a DRIFTED asset must ABORT here, the ONE place it meets the independent hand-written reference, not bury one failure among ~15 self-consistent passes). Then the LOADED `scv_asset` DRIVES `run_two_class_cuda`; all 30b-3b falsifiability holds (n_pipes==1, n_xfer==3, profile [Reduce,Reduce,Elementwise], bit-exact vs the two-stage oracle + all-Host). GREEN **Win RTX #4851 + WSL2 RTX #4698**; CUDA-exe siblings 29b-1/29c-2/30b-2b-2b 4/4 BOTH toolchains (the recompiled TU is healthy). ⛔ NO engine code / NO `.lib` touched → NO stale-sibling relink for dx12/vulkan; ONLY the CUDA exe relinked. ⛔ `test_ceir_add_cuda.cpp` recompiled once on WSL via the target's shared PCH (`crd_enable_default_pch`, tests/ceir-gpu-cuda/CMakeLists:35) invalidation on the glob re-check — BENIGN, no source change (the 2nd WSL build recompiled ONLY test_ceir_pipeline_cuda.cpp, confirming it settled). Tidy clean (test_ceir_pipeline_cuda.cpp). ✅✅ **30c-2 COMPLETE** = 30c-2a (committed asset + device-free reading gate) + 30c-2b (the CUDA end-to-end). ✅✅ **30c COMPLETE** = 30c-1 (directive + loader + 3rd producer) + 30c-2 (the committed asset, device-free + CUDA). ⛔ UNCOMMITTED-BATCH (mods): `tests/ceir-gpu-cuda/test_ceir_pipeline_cuda.cpp` (+parse/transform_ops/slurp includes + register_transform_ops + the 30c-2b augmentation + the rename/tag). NEXT = **30z** (BAND CLOSE — docs + bench, NOT code): the census-predicted HONEST toy-dim bench loss (the host↔device transfer dominates at [8,4]) → `docs/bench/`; the 30-0 census RECONCILE (⛔ GATE re-verify — strike every "absent"/"deferred" the band RESOLVED: the `emit_reduce_cuda` census error [struck at 30b-3b], the F32-eval-mode caveat [RESOLVED at 30b-1], etc.); the deferral ledger w/ triggers; the session log; the proposed Conventional-Commits message (NO AI co-author trailer — the user commits). | §68 §103 §146; proof §140

**30z-1 ✅ 2026-09-05 (the CEIR-30 BAND-CLOSE BENCH — the §140 sharded-reduction TRANSFER-COST decomposition on BOTH RTX; the census-predicted HONEST toy-dim loss made QUANTITATIVE; SPLIT from 30z-2 close per advisor)**: the measured board the whole 30b/30c mechanism exists to justify. ⛔ SPLIT (advisor): 30z-1 = bench + board (this tick, CODE + a board); 30z-2 = census reconcile + deferral ledger + session log + flip (next tick, DOCS-only — the flip comes AFTER the numbers). **WHAT IS MEASURED** the SAME lowered `[Reduce,Reduce,Elementwise]` §140 plan (`[rows,cols]` sharded axis-0 → 2 per-rank reduces → an all-reduce elementwise combine) run THREE ways to quantify the cross-domain PLACEMENT tax (a Cerid-INTERNAL decomposition, NOT a peer crush): **A = all-Host** (`execute_tensor_pipeline_host`, device-FREE baseline), **B = sharded `[Host,Host,Gpu]`** (`execute_two_class`: the 2 reduces on Host, the all-reduce combine on CUDA, 2 partial uploads + 1 readback), **C = the transfer FLOOR** (arm B's 3 planned transfers via `cuda_transfer`, NO compute — the discriminator: is the tax the crossing or the CUDA combine?). **THE HARNESS** `test_ceir_pipeline_cuda.cpp "ceir 30z"` (tag `[bench]`, env-gated on `CRD_CEIR_BENCH` so the default ctest sweep WARN-skips — 21 assertions when set): `struct BenchTwoClass{TwoClassGpu tg; BenchResolver bres; double combine_gpu_ms}` + `bench_gpu_stage` (⛔ the 29z CACHING `resolve_stage_cached` keyed by `st.op` — 90 executes would else re-create the combine pipeline every call + exhaust the 16-cap; ⭐ SNAPSHOTS `last_gpu_ms()` into `combine_gpu_ms` IMMEDIATELY after the combine's `submit_and_wait`, BEFORE the trailing readback submit overwrites `last_gpu_ms` — the board number is the combine bracket, not the readback) + `bench_transfer` (forwards to `cuda_transfer`); `bench_one_dim` builds the payload once, lowers, plans, seeds heap Arrays, sets up all 3 arms ONCE (no alloc/re-seed in the timed loop), warmup 5 / median of 9 / K=10 per sample (`std::chrono::steady_clock`). ⭐⭐ FALSIFIABILITY (ON during the timed run): **bit-exact** vs the two-stage f32 oracle `fold(shard0 col c)+fold(shard1 col c)` — ⛔ advisor-AFTER: BOTH arms looped ×10 back-to-back before the compare (was arm-B×10 + arm-A×1 — asymmetric vs the board's "10 back-to-back" claim; now symmetric, the aliased-storage-replay clobber catch on both) + `n_xfer==3` (`== plan_transfers(plan,sc).size()`, 2 uploads + 1 readback, CONSTANT in rows) + `combine_gpu>0` (the 28b median==0⇒measuring-nothing hard-fail). **THE FINDINGS** (board `docs/bench/2026-09-05-ceir30-sharded-reduction-transfer-cost.md`): the cross-domain tax is a FIXED per-crossing cost amortized by shard compute — **`C_xferfloor` ~CONSTANT across the whole sweep** (Win ~1640–1730µs, WSL2 ~3130–3580µs — it does NOT grow with rows because only the `[cols]` partials + result cross, never the `[rows/2,cols]` shards [Host-consumed]; WSL2's passthrough driver ~doubles the per-submit latency, the only cross-device difference), **`tax(B−A) ≈ C_xferfloor` at every dim** and both DWARF `combine_gpu` (20–105µs) ⇒ the tax is the CROSSING not the combine (arm C is the discriminator the 29z board LACKED — its GPU-overhead gap was left undiscriminated). `A_host` grows O(rows×cols) so `B/A` AMORTIZES: **Win 345.7× → 70.4× → 4.05× → 1.095× (`[8,4]`/`[64,32]`/`[512,128]`/`[4096,512]`)**, **WSL2 1020.8× → 146.2× → 6.68× → 1.118×** — at `[4096,512]` the sharded run is only ~5–12% slower than all-Host, the tax nearly hidden by the 28–34ms reduce. ⭐ this IS §140 "placement is semantic, not glue": the SAME plan runs all-Host and `[Host,Host,Gpu]` BIT-EXACT, the only difference is WHERE each stage ran — a free authored choice (30c) with a measurable honest cost. **N/A-with-check (SANITY #9):** no all-Gpu arm (`emit_reduce_cuda` trailing-axis-only + 2-scalar push — the reduce-on-CUDA contract ledgered; check: a CUDA resolve of a Reduce stage → UnresolvedKernel) · no real 2-GPU arm (single RTX + WSL2 passthrough — check: the 30-0 census recorded the hardware go/no-go). ⛔ ROW NOTES (advisor-AFTER, a reader will misread these as bugs): **(a)** gcc `-Werror=shadow` caught arm C's loop var `t` (Transfer) SHADOWING the seed tensor `t` — MSVC compiled it silently, the shadow flavor of [[feedback_msvc_hides_switch_gaps_gcc_werror_switch_catches_real_bugs]] (renamed `xf`). **(b)** `collect_named_cuda`/`fold_sum_c` are FORWARD-DECLARED before `bench_one_dim` (~L1122) but DEFINED later (~L1703/1717) — the SAME unnamed namespace across the ONE TU, so decl+def link; NOT a "forward decl into an anon namespace" bug. **(c)** `test_ceir_add_cuda.cpp` did NOT recompile this tick on WSL ([2/2] both builds) — the 30c-2b one-time PCH-invalidation recompile CONFIRMED settled. **(d)** the `[4096,512]` device table shows ~8MB VRAM (two `[2048,512]` shard buffers CREATED but never written — Host-consumed) — the `run_two_class_cuda` setup pattern (a device buffer per plan buffer), NOT a bench leak; devalloc stayed at its 64MB pool. **(e)** run-to-run jitter: Win `B_sharded` at `[8,4]` was 1621/1676/1580/1884µs across the FOUR captured Win runs — the amortization shape + `C≈tax≫combine` reproduce every run + both devices. ⛔ LEDGER (the 2-GPU deferral, quantitative for 30z-2): at `[4096,512]` the Host reduce is ~28ms serialized; two devices IN PARALLEL would halve `A` while the ~1.7ms tax stays fixed → predicted `B/A ≈ 0.55–0.6`, a WIN — trigger: a second PHYSICAL GPU ~~(the census go/no-go)~~ **AND the reduce-on-CUDA contract (halving `A` means the rank-1 per-rank reduce runs on ITS device — corrected at 30z-2)**. GREEN: **Win RTX #4853 + WSL2 RTX #4700** (both env-gated `CRD_CEIR_BENCH=1`, 21 assertions; the symmetric-gate change RE-VERIFIED green both after the arm-A×10 edit — a gate change, not a rename); default sweep skips cleanly (WARN). Tidy-20 clean (test_ceir_pipeline_cuda.cpp). ⛔ NO engine code / NO `.lib` touched → NO stale-sibling relink (dx12/vulkan stay fresh); ONLY the CUDA exe relinked. ⛔ UNCOMMITTED-BATCH (adds/mods): `tests/ceir-gpu-cuda/test_ceir_pipeline_cuda.cpp` (the BenchTwoClass/bench_gpu_stage/bench_transfer/bench_one_dim harness + the "ceir 30z" case) + `docs/bench/2026-09-05-ceir30-sharded-reduction-transfer-cost.md` (NEW board) + `docs/bench/README.md` (index line). NEXT = **30z-2** (band CLOSE — DOCS ONLY, no code): the 30-0 census RECONCILE (⛔ GATE re-verify — strike every "absent"/"deferred" the band RESOLVED: the `emit_reduce_cuda` census error [struck 30b-3b], the F32-eval-mode caveat [RESOLVED 30b-1], the two-CUDA-streams rejection [confirmed], the boundary-transfer STAGE trigger [FIRED at 30b-2a/30b-3b]); the deferral ledger w/ triggers (2-GPU quantitative above; NCCL/MPI/cluster; §103 reshard; the §68 long-tail; reduce-on-CUDA); the close session doc (29z mold); the proposed Conventional-Commits message (NO AI co-author trailer — the user commits); flip CEIR-30 ✅. | §68 §103; proof §140

**30z-2 ✅ 2026-09-05 (BAND-CLOSE COMPLETE — CEIR-30 ✅)**: the close tick (docs + memory only, NO code). **(1) 30-0 CENSUS RECONCILED** (⛔ GATE re-verify — reconcile promises, don't just write rows, the 28-close scar; ⭐ advisor-caught THREE traps a straight "write the rows" pass would miss): **(a) the F32-eval-mode caveat lived in THREE places** (the 30-0 verified-fact (4) "arithmetic caveat" + the 30-0 deferral-ledger item + the CEIR-30 band-row DEFERRED list) — all struck in place (SUPERSEDED rule) as **RESOLVED at 30b-1, NOT deferred**: the census FACT was imprecise — `eval_cpu` ROUNDS every elementary op to the node dtype (`round_dtype`, ckir_eval.hpp:48-54), so an F32 graph is bit-exact vs a naive f32 GPU kernel; neither a separate F32-rounding eval MODE nor a tolerance was needed, the §140 gate is `==`. **(b) the boundary-transfer `StageKind::Transfer` STAGE did NOT "fire and land" — it DISSOLVED** (verified by grep, not memory: the plan `StageKind` enum at tensor_pipeline.hpp:53 has NO Transfer member; `LoweredKind::Transfer` at lower.hpp:25 is the UNRELATED 13b command-movement kind) into `plan_transfers` (a DERIVED transfer list, 30b-2a) + `execute_two_class` callbacks (30b-2b) — the 29b-2b IExecutionProvider shape (trigger fired, mechanism reshaped). Struck at the 30-0 fact-(5) site AND the 29z close doc's ledger bullet; the riders (`is_memory_domain` validation + run·CKIR·run double-transfer) did NOT land → re-homed to the 30 ledger with triggers. **(c) the `emit_reduce_cuda` "absent" census error** (already struck at 30b-3b — it exists at ckir_cuda.hpp:712, just trailing-axis-only + a 2-scalar-push contract, so not WIRED for the axis-0 reduce ≠ absent) confirmed; the census had grepped the CONSUMER (`resolve_stage`) to infer the PRODUCER's absence — the INVERSE of grep-the-consumer, now a scar clause. **(d)** the 29z carried-forward list's "§103 multi-device sharding (a >1-GPU box)" STRUCK in the 29z close doc — CEIR-30 IS §103; the sharding MACHINERY landed, only the >1-GPU HARDWARE remains deferred. **(2) DEFERRAL LEDGER** (each w/ trigger, in the close doc): a real 2-GPU mesh → a 2nd PHYSICAL GPU **AND** the reduce-on-CUDA contract (the 30z-1 quantitative payoff `B/A ≈ 0.55–0.6` at [4096,512], conservative) · NCCL/MPI/cluster → a cluster · §103 reshard → a mid-program sharding change · the §68 collective long-tail → each its first consumer · reduce-on-CUDA (emit_reduce_cuda outer-axis + the cross-launch-site 2-scalar-push contract) → a CUDA shard reducing · the re-homed Transfer riders · the `mean` collective-fn (⛔ do NOT hoist tensor.cpp's fn_in — divergence by design) · peer_copy + mesh rank>1 · the 29-0 non-CUDA-graph items UNCHANGED but WITHOUT §103. **(3) SCARS — 3 CLAUSES into EXISTING homes (no new files, advisor):** `scars_ceir_ir.md` (a lowering that STRIPS an asset ends its consumers' window — `lower_sharded_reduction`→`strip_dead_meshes`, so mesh consumers run after materialize BEFORE lower) + `feedback_bench_arms_must_match_wait_and_bracket_structure` (the correctness GATE guarding a bench must loop every compared arm the SAME N — 30z-1's arm-B×10/arm-A×1 asymmetry) + `feedback_coverage_inventory_grep_the_consumer_not_the_node` (the INVERSE: an "X is ABSENT" claim needs X's DEFINITION-site grep, not the consumer's — the emit_reduce_cuda census miss). **(4) CLOSE DOC** `docs/sessions/2026-09-05-ceir-30z-band-close.md` (the 29z mold: slices table w/ ctest #s + device/bench boards LINKING the bench + a RESOLVED-not-deferred section + the deferral ledger + the 3 scar clauses + uncommitted batch + proposed commit message NO trailer). **(5) FLIP** band row 🔄→✅ + the MAP 30z entry marked 30z-2 ✅ + the band-row CLOSED annotation. ⭐ POSITIVE reconcile: the 30-0 census's 30z sketch ("honest loss at toy dims, transfer dominates") HELD — 30z-1 measured exactly it, unlike 29z where the census bench column was wrong. ✅✅✅ **CEIR-30 (`ceir.dist` multi-device) COMPLETE** = 30-0 census + 30a (device-free core: dialect + propagate + materialize) + 30b (Host+Gpu §140 proof: host executor + two-class orchestrator + sharded reduction, bit-exact both RTX) + 30c (authored placement: directive + loader + 3rd producer + committed asset, device-free + CUDA) + 30z (bench + close). The §140 "placement is semantic, not glue" is PROVEN: the same lowered plan runs all-Host and `[Host,Host,Gpu]` bit-exact on two RTX runners, the only difference being WHERE each stage ran. ⛔ UNCOMMITTED-BATCH (adds): `docs/sessions/2026-09-05-ceir-30z-band-close.md` (NEW) + the 3 memory-file clause edits + `docs/detours/D-007-ceir-tracker.md` (this row + the census strikes + the band-row flip) + `docs/sessions/2026-09-05-ceir-29z-band-close.md` (the 2 in-place strikes: Transfer STAGE + §103) + `context.md`. NEXT = **CEIR-31** (Media/UI/audio bridges — absorbs I2D effect graphs / MED workflows / audio graphs, routed through CEIR with no new schedulers; ⛔ OPEN with an advisor CENSUS, no pre-design — the census question: which existing I2D/MED/audio consumers exist to route through CEIR, and whether any is RUNNABLE on this box, or the band is device-free + deferred-with-trigger). | §68 §103; proof §140

**31-0 CENSUS ✅ 2026-09-05 (band-OPEN advisor census — the Media/UI/audio bridges band; facts VERIFIED by grep, NO pre-design)**: ⛔ CENSUS ONLY (the every-band rule): advisor-verified facts + runnable-proof decision + subslice MAP + deferral ledger; no op signatures, no code. **SCOPE (spec):** roadmap "CEIR-28 Media/UI/audio bridges" (=tracker CEIR-31 — [[reference_ceir_band_numbering_tracker_is_authoritative]]): route media / UiEffectGraph / audio-DSP workflows through CEIR, ⛔ NO new schedulers · §141 UI EFFECT proof (frosted glass: `backdrop→downsample→blur→tint/noise→composite`, asset-authored CEIR) · §142 AUDIO proof (`input→EQ→delay→compressor→output`, RT-safe / no-alloc-or-block / graph-hot-reload-at-safe-boundary) · §177 (codecs / hardware-codec / external-lib / job-system stay NATIVE — the *algorithmic* use is CEIR) · §178 (renderer/post/GI/RT/compute/tensor/ML/**media-processing-chains**/**UI-effect-graphs**/geometry-pipelines must NOT remain native-only if expressible). **⭐ RUNNABLE-PROOF DECISION (advisor, +1 reconcile that FLIPPED the initial image-first guess): AUDIO first, then IMAGE** — two real proofs, not a menu (§141 + §142 are separate proof sections; this box runs both). **31a = §142 audio (OFFLINE)**: `render_graph` (audio_graph.hpp:29) is a DEVICE-FREE, deterministic, BIT-EXACT CPU-numeric renderer with a native oracle ALREADY in the tree — a 30b-shaped proof with the oracle pre-built, reusing the CEIR-30 Host-executor ORCHESTRATION (the 30b-2b-1 `HostKernelResolveFn` per-stage CPU-kernel exit + `execute_two_class`/`slice_plan` — ⛔ NOT `synth_*→eval_cpu`, the tensor-plan path: a biquad is a STATEFUL RECURRENCE [y[n]←y[n-1],y[n-2]; f64 state per channel], NOT a `tensor.elementwise`/`reduce`, and `eval_cpu` has no recurrence/scan node — so SOME new per-node kernel surface is required, NOT "zero"); and "no new schedulers" is FALSIFIABLE here (there is a bespoke node-walk scheduler ~~to DELETE~~ **[⛔ 31z ruling (4): KEPT as the live product path + the differential oracle; the falsifiable claim is "no scheduler ADDED to CEIR", proven]** + a bit-exact oracle to prove the deletion changed nothing). **31b = §141 UI effect (RAF/GPU)**: reuses RAF (the mandate) but "no new schedulers" is merely ASSERTED (RAF already IS the scheduler; the effect graph authors passes into it — nothing deleted), and it needs a GPU device gate + RAF lowering of a multi-pass CHAIN + effect-kernels-as-assets. ⛔ roadmap §-order (141 before 142) is a CATALOG order, not a dependency — do the falsifiable/most-complete one first. **VERIFIED FACTS (file:line):** (1) `render_graph` (engine/audio/include/crd/audio/audio_graph.hpp:3-31) = the GEO-10 OFFLINE graph renderer: Sources pull bound buffers / Gains+Sends apply automated dB / Biquads run hesap-dsp RBJ closed forms (f64 state) / Mixes sum, on a stereo f32 bus; ⭐ "identical inputs render identical bytes (the gate)"; the REALTIME layer (audio_device.hpp) is SEPARATE (voices+commands, not this renderer) ⇒ the offline path is the device-free ctest-runnable proof, realtime is the deferred half. (2) the §142 node-set GAP: `render_graph` has Sources/Gains/Sends/Biquads/Mixes; §142 wants input→EQ→**delay**→**compressor**→output — EQ = biquad (EXISTS, rbj.hpp/iir.hpp), but a delay-LINE + a dynamics-COMPRESSOR are NEITHER render_graph nodes NOR hesap-dsp closed forms (hesap-dsp is filter DESIGN/analysis — its "delay" = group-delay, "envelope" = Hilbert analytic, NOT effect nodes; [[feedback_coverage_inventory_grep_the_consumer_not_the_node]] definition-site check) ⇒ split (b)-then-(a): **31a-1** proves the EXISTING node set (Source→Gain→Biquad→Mix) bit-exact vs render_graph THROUGH CEIR, **31a-2** authors delay+compressor as new hesap-dsp/Host-executor kernels to complete the §142 chain. (3) IMAGE infra (31b): RAF frame-graph (gpu-context/frame_graph.hpp) + Vk/DX12 backends EXIST; the UI-effect→RAF contract is capability toml:734 ("multi-pass UI effect graph … compiling to RAF frame-graph work — reuses RAF, no mini scheduler"); the post kernels (ckir_bloom/post/screen/svgf/cinematic/fft) exist ONLY as `build_*(KGraph&)` HEADERS called from TEST FIXTURES (test_ckir_svgf/test_dx12_compute/test_vulkan_context), NOT production scene_renderer.cpp (cleaned at task #34) ⇒ 31b is NEW effect-CHAIN authoring (CEIR-18 authored scene RENDERERS; NO multi-pass image EFFECT CHAIN exists as a committed asset), test-fixture-builder conversion OPTIONAL under the standing-order fixture carve-out; `rt_composite.ckir` is a committed composite pass already. (4) the CEIR-9b DAW proof (tests/ceir/test_daw.cpp) was a MOCK `audio` dialect (a U-§97 UNIVERSALITY validation — zero new machinery), NOT a real audio bridge; 31a is the FIRST real audio-DSP-through-CEIR execution. (5) I2D: NO engine/i2d module (Track B PAUSED, ADR-0107 review pending); MED PAUSED (Track D); codecs native (§177 — engine/audio has wav/aiff/flac). (6) ⛔ `AudioGraphResource` is an **'AGRF' BINARY cooked resource** (audio_resources.hpp:152 — `audio_graph_build` cooks it, `AudioGraphLoader` loads it via `resources::ILoader`; ONE validator gates build+load: DAG/cycles-refused/indices-in-bounds) — a COMPETING authored form to a `.ceir` text asset. So 31a-1's "asset-authored CEIR" has a FORK the census NAMES: IMPORT the AGRF → lower to a CEIR program (reuse the cooked resource as source) vs AUTHOR the `.ceir` DIRECTLY and diff `render_graph(AGRF)` as the oracle (the "everything is an authorable asset" precedent favors author-direct; AGRF is binary, not text). **⛔ 31a FALSIFIABLE GATE (advisor, so 31a doesn't re-derive):** the proof is NOT "CEIR calls render_graph" — it is: an AUTHORED audio-DSP CEIR program (the §142 chain) → CEIR's def-use/TOPOLOGICAL order decides the process order (⛔ a pure DAG Source→Gain→Biquad→Mix has NO hazards — 4d hazard-ordering is trivially the data-flow order) → the Host-executor ORCHESTRATION runs each node's CPU kernel (via `HostKernelResolveFn`, NOT eval_cpu — the recurrence caveat above) → output BIT-EXACT vs `render_graph` on the SAME AudioGraphResource+bindings. ⛔ ARITHMETIC CAVEAT (the 30-0 f64/f32 caveat in AUDIO form — READ audio_graph.cpp's biquad loop BEFORE 31a-1, not during): `render_graph` runs biquads with f64 STATE on an f32 BUS, so bit-exact REQUIRES 31a mirroring its exact rounding sites (a per-sample f64 accumulate + f32 bus store is the likely contract); if 31a cannot match them the gate degrades to a ULP-tolerance, which MUST be stated as a LOSS, not disguised as bit-exact. `render_graph`'s node-walk stays as the legacy REFERENCE arm ([[feedback_deleting_reference_in_ab_parity_test_degrades_to_can_t_fail]] — keep-as-oracle, parity via REQUIRE not CHECK since the CEIR path DRIVES the output) until parity proven; ⛔ 31a's advisor decides at slice time whether render_graph is DELETED (deletion-is-done) or KEPT-as-oracle. ⛔ 31a earns only ONE THIRD of §142 — "audio time domain"; "RT-safe (no-alloc/no-block)" + "hot-reload-at-safe-boundary" are RT-SCHEDULER properties (no safe boundary exists in an offline render) → the realtime half, deferred. **SUBSLICE MAP (a shape, not a design):** **31a** §142 audio-offline [**31a-1** existing-node-set bit-exact-vs-render_graph-through-CEIR → **31a-2** delay+compressor kernels to complete the §142 chain; ⛔ FIRST decide a `ceir.audio` dialect vs reuse of the ceir tensor/host ops for the DSP nodes] → **31b** §141 UI effect graph (frosted-glass chain on RAF/GPU, Vk+DX12; new effect-graph authoring vs reuse of the render dialects, decided at 31b slice time) → **31z** close (⛔ NO obvious bench crush — the proof is CORRECTNESS/DELETION, not speed; a CEIR-vs-native timing bench only if honest). **DEFERRAL LEDGER (each w/ trigger):** I2D effect graphs → the ADR-0107 review + an engine/i2d module (Track B) · MED workflow chains → the Track D resume · RT-safe realtime audio (no-alloc/no-block) + hot-reload-at-safe-boundary → the audio_device.hpp realtime callback path (offline render_graph IS the runnable proof; realtime is the trigger — the CEIR-30 "offline exists, realtime deferred" analog) · delay/compressor kernels → the §142 full chain (31a-2) · CLAP hosting (tracker PLG-3 row) → a DAW-project phase · UiWorld/Canvas widget integration ("reuse the UiWorld/Canvas plan" — a DESIGN in ADR-0107 + sandbox usage, PAUSED) → the ADR-0107 review · geometry pipelines (§178) → their own consumer · the render_graph DELETION decision → 31a slice-time. ⛔ UNCOMMITTED-BATCH (adds/mods): `docs/detours/D-007-ceir-tracker.md` (this row + the band-row flip) + `context.md` (the CEIR-31 focus). NEXT = **31a-1** (the §142 audio-offline bit-exact-vs-render_graph-through-CEIR proof over the EXISTING node set — Source→Gain→Biquad→Mix; ⛔ FIRST decide `ceir.audio` dialect vs reuse of the ceir tensor/host ops for the DSP nodes, and whether render_graph is deleted or kept-as-oracle — advisor BEFORE). | build §61 §62 §63; corpus §95 §96 §97; proof §141 §142

| ✅ | CEIR-27 | `ceir.transform`/`ceir.rewrite` programs → schedules + rewrites are AUTHORABLE assets a loader/driver applies · CLOSED 2026-09-05: 27-0 census + 27a (transform dialect+loader) + 27b (§146 two-schedule BIT-EXACT, Vk+DX12+llvmpipe) + 27c (§72 rewrite-rule identity_reshape, plugin-boundary) + 27d (reshape_of_reshape build_op + order-priority + guards) + 27z (close) · 27e named-forward (pass-time rule wiring, own census) | §71 §72 §146 |
| ✅ | CEIR-28 | Autotune/PGO/cost model → per-device config cache + deterministic locked mode · CLOSED 2026-09-05: 28-0 census + 28a (`ceir.tune` dialect + module-wide `find_tune_misuse` + full-key loader, #834) + 28b COMPLETE [28b-1 `program_hash`=fnv1a of canonical print (u64) + 28b-2a portable Vk MEASURER (median-time 4 configs RTX+lavapipe, plan-sig winner-collapse + identity gate, fuse ~12% win) + 28b-2b COMMITTED device-keyed cache asset `tune_cache_vk_rtx4070ti_super.ceir` + device-gated anti-drift, RTX+lavapipe 84 assns gcc-clean] done + 28c (deterministic locked mode: `plan_tensor_pipeline_cached` + `TunePolicy{Fallback,Locked}`, key=program_hash(m) internal, miss→base/no-measure or typed `TuneCacheLockedMiss`; device-free gate MSVC+gcc) done + 28d (DX12 measurer mirror: fuse wins on DX12 too ~6%, program_hash + plan_sig IDENTICAL to Vk ⇒ cross-backend planner determinism; ~9µs DX12 readback floor) done + 28z-1 (warmup unify; Require deferred) + 28z-2 (committed 3-row cross-backend cache `tune_cache.ceir` vk-RTX/dx12-RTX/vk-lavapipe + per-row field anti-drift, one test per backend TU, all rows addressable; 92 assns lavapipe) + 28z-3 (close: bench→docs, cross-config win+linux, deferral ledger, 2 scars, flip ✅) done — fusion the measured winner Vk+DX12 by ~1µs, program_hash+plan_sig identical across all 3 backends; NEXT band CEIR-29 · reuse: the v17 autotuner (checked-in tuning DB) | §80 §81 §82 |
| ✅ | CEIR-29 | Native graph providers → the §70 partitioner across provider CLASSES (fulfills the `provider.hpp:36` / `partition_ml.hpp:5` NAMED deferral) · **CLOSED 2026-09-05** (CUDA-Graphs launch-graph provider: device-free partitioner core + on-device two-class proof + authored class/pin directives + the launch-vs-fallback bench). **29-0 census ✅ 2026-09-05:** runnable §70 provider = **CUDA GRAPHS** (launch-graph ⇒ BIT-EXACT vs the CKIR/stream fallback by construction; reuses the existing `execute_lowered` CUDA path, ADR-0126). ~~D3D MLIR Programs / VK data-graph / NPU research~~ → DEFERRED (DirectX Compute Graph Compiler is PRIVATE preview summer-2026; VK_ARM_data_graph ARM-Mali-only; NPU research) — SUPERSEDED in place. MAP: 29a device-free CORE [**29a-1 ✅** ProviderClass enum + `MlProvider` descriptor fields {provider_class, memory_domain(§24 string), determinism} + extracted `is_memory_domain`, #873] → **29a-2 ✅** subgraph greedy-grow (`claims_subgraphs` + monotonic `subgraph` id + `subgraphs_of`/`subgraph_size`, #818) → **29a-3a ✅** authored provider pin (partitioner honours a `pinned` index — preference-not-force, #819) → **29a-3b-1 ✅** `transform.assign_provider` directive + `provider_from_transform` loader (name→index, typed rejects, #876) → **29a-3b-2 ✅** committed `assign_provider_coopvec.ceir` asset (canonical + anti-drift + end-to-end pin, #877) ⇒ **✅ 29a-3 + the WHOLE 29a device-free CORE DONE** → **29b-1 ✅** the CUDA tensor-pipeline device gate (expanded ml.mlp bit-exact on Win RTX + WSL2 RTX; the N-dispatch fallback + 2 CUDA-backend bug fixes: emit_contract_cuda uint4 push + the CUDA test target's CRD_REPO_DIR) → **29b-2a ✅** CUDA-Graphs capture MECHANISM (begin_capture/end_capture[instantiate-once]/launch; bit-exact + deterministic replay, Win RTX 13.3 + WSL2 RTX 12.0) → **29b-2b ✅** the `cuda_graphs` provider descriptor + the SEAM reconcile (a launch-graph provider is a plan LAUNCH-MODE, ~~an IExecutionProvider compile→plan implementor~~ **[that census APPEND DISSOLVED — see 29-0 strike]**) → **29c-1 ✅** the partition-aware plan (`PlanStage.provider` + `plan_tensor_pipeline_partitioned` + expand→ml lineage; ⛔ the boundary-transfer STAGE is UNTRIGGERED on one CUDA domain — name-forwarded WITH the two-domain trigger, NOT asserted) → **29c-2 ✅** the two-class boundary on a REAL device (capture ONLY the cuda_graphs run, flanking fallback gemms eager on the same stream, bit-exact Win+WSL2; first live 26f alias materialization on the CUDA runner) → **29c-3a ✅** the `provider_class` HARD-FILTER composed with the pin → **29c-3b ✅** the authored `transform.constrain_provider_class` directive + the opgen C++-keyword sanitizer → **29z-1 ✅** the launch-vs-N-dispatch BENCH (CPU submit-path win 1.36–2.24× on the two-class sandwich, both devices; the `CudaComputeContext::enqueue` split it forced) → **29z-2 ✅** close (deferral ledger + 2 scars + this flip). ⛔ 29-0 CENSUS RECONCILED: the IExecutionProvider APPEND DISSOLVED (29b-2b), the boundary-transfer STAGE + the 29z bench clause STRUCK at 29-0 (name-forward / column-corrected), constrained mode LANDED (29c-3). | §70 §102 §69 |
| ✅ | CEIR-30 | `ceir.dist` multi-device → sharded reduction/training slice; placement is semantic · **CLOSED 2026-09-05** (the Host(CPU) + Gpu(CUDA) §140 proof: the SAME lowered `[Reduce,Reduce,Elementwise]` plan runs all-Host AND `[Host,Host,Gpu]` bit-exact on two RTX, differing only in placement; = device-free dist dialect + propagate/materialize + `lower_sharded_reduction` + authored `place_mesh` + the transfer-cost bench. RESOLVED-not-deferred: the F32-eval caveat [30b-1 eval_cpu is f32-faithful], the boundary-transfer STAGE [DISSOLVED into plan_transfers], the emit_reduce_cuda "absent" census error [30b-3b: it exists, just not wired]) · **30-0 census ✅ 2026-09-05:** runnable proof = **Host (CPU, `ckir_synth`+`ckir_eval`) + Gpu (CUDA)** = two real provider classes / two §24 domains (two-CUDA-streams REJECTED — one domain, nothing to be semantic about); the Host executor is the EXISTING CKIR CPU interpreter (a GENERAL 2nd executor, NOT per-fixture glue); all-reduce combine = elementwise-add (runs on CUDA); shard-axis/mesh-rank annotation is NEW (the domain axis rides `declare.memory_domain`, which exists). MAP: **30a ✅** device-free core [**30a-1 ✅** the dialect skeleton — dist.mesh/shard/all_reduce + find_dist_misuse (9 kinds, mesh name-match by module walk), the committed dist_shard_reduce.ceir anti-drift, MSVC+WSL gcc → **30a-2 ✅** sharding PROPAGATION (`propagate_sharding`: the Replicated/Sharded/Partial/Conflict lattice, elementwise meet, reduce-over-split→Partial, all_reduce completes, unknown-op→Conflict per §70; MSVC+WSL gcc) → **30a-3 ✅** MATERIALIZATION (`materialize_sharding`: auto-insert all_reduce where a Partial escapes, RAUW-all-then-restore; the 1-device identity byte-identical; MSVC+WSL gcc)] → **30b** Host+Gpu §140 proof [**30b-1 ✅** the HOST EXECUTOR `execute_tensor_pipeline_host` — the CPU mirror (synth_*→`eval_cpu`, f32-FAITHFUL so bit-exact vs the same oracle every GPU backend proves against — the f64/f32 caveat RESOLVED not deferred); a fused MLP + reduce + elementwise/transpose/broadcast bit-exact + unsupported→UnresolvedKernel-no-partial-write; device-free MSVC+WSL gcc → **30b-2** the two-class orchestrator [**30b-2a ✅** `plan_transfers` + `stage_class_from_partition` — the PURE placement→transfer pass (a `ProviderClass` per stage → the host↔device transfers the def-use edges imply; Host-born externals + planned Output readback; aliases→landlord; device-free MSVC+WSL gcc) → **30b-2b** [**30b-2b-1 ✅** the device-free Host VizDispatch(relu) exit — a caller `HostKernelResolveFn` loads the authored .ckir → `eval_cpu_kernel` (bit-exact vs the CUDA relu by construction); fuse=false MLP + the two-class sandwich all-Host bit-exact, seeds = `fill_two_class_data_and_oracle` so 30b-2b-2 re-seeds the exact data; the shared `ckir_asset_resolve.hpp` helper; MSVC+WSL gcc → **30b-2b-2** [**30b-2b-2a ✅** the engine `execute_two_class` runner (transfers computed internally from stage_class; per-stage slice; profile complete) + `slice_plan` hoisted from the CUDA test (ADL) — proven DEVICE-FREE with a mock device (bit-exact under [G,H,H,H,G]/reverse/all-Host/all-Gpu, log==plan_transfers by identity, aliased-storage landlord path), MSVC+WSL gcc + 29b-1/29c-2 re-green on both RTX → **30b-2b-2b ✅** the §140 REAL-DOMAIN proof — the CUDA harness + the three-way gate (the sandwich Host+CUDA bit-exact vs all-Gpu vs all-Host vs oracle, Win RTX + WSL2 RTX; n_pipes/n_xfer/profile falsifiability) + the resolver refactor + the fixture hoist (one oracle body) — **30b-2b + 30b-2 COMPLETE**]] → **30b-3** the §140 chain on the {Host,Gpu} mesh [**30b-3a ✅** `lower_sharded_reduction` — the reduction→per-rank-plan LOWERING + placement producer (`rank_lineage`), device-free: E shard declares + E tagged reduces + an elementwise combine tree, RAUW+erase in-place (consumer-first), the 1-device byte-identical identity, refuse-on-Conflict (module untouched); the placed plan → `[Reduce,Reduce,Elementwise]`/`[0,1,-1]`, `stage_class` `[Host,Gpu,Host]`, `plan_transfers` by IDENTITY, all-Host bit-exact vs the two-stage f32 oracle (≠ a flat fold, non-assoc live); +the planner now skips `resource.export` (a resultless boundary); MSVC + WSL gcc, #896 → **30b-3b ✅** the SAME plan through `execute_two_class` on real CUDA — placement **`[Host,Host,Gpu]`** (⛔ NOT rank1→Gpu: `emit_reduce_cuda` is trailing-axis-only + its 2-scalar push is the cross-launch-site contract, its own slice; so BOTH reduces on Host, the all-reduce COMBINE on CUDA) PRODUCED from rank_lineage; ARM-all-Host + ARM-split bit-exact vs the two-stage oracle, `n_pipes==1`/`n_xfer==3` (2 uploads + the Output readback@ns = the combine-ran-on-device identity)/profile `[Reduce,Reduce,Elementwise]`; NO all-Gpu arm THIS SLICE (the reduce-on-CUDA contract is ledgered); NO engine code touched; Win RTX #4849 + WSL2 RTX #4696 — **30b-3 + 30b COMPLETE**] → **30c** authored placement [**30c-1 ✅** the `transform.place_mesh` directive (op via opgen) + `find_transform_misuse` `seen_place` DuplicateDirective guard + the `placement_from_transform` loader (mesh-keyed against the PAYLOAD's dist.mesh, axis-0 count-validated, typed rejects `UnknownMesh`/`MultiAxisMesh` [a >1-axis mesh refused, not silently placed onto axis-0]/`UnknownProviderClass`/`RankCountMismatch`, each asserted by IDENTITY incl. `op != nullptr`) + `stage_class_from_placement` (the THIRD per-stage ProviderClass producer, byte-≡ `stage_class_from_partition` on the same plan — three producers, one consumer) + the hoisted-public `gather_meshes`/`resolve_mesh`/`mesh_extent`; device-free gate #897 MSVC + WSL gcc, siblings relinked → **30c-2** [**30c-2a ✅** the committed `assets/ceir/place_mesh_host_host_gpu.ceir` + its device-free reading gate (bootstrap-via-print, anti-drift through the PRINTER, roundtrip-stable, `dist::find_dist_misuse` dist-verify-FIRST, `placement_from_transform → stage_class_from_placement == [Host,Host,Gpu]`) + the shared-slurp hoist (`tests/gpu-shared/ceir_asset_slurp.hpp`, band24 rewired 4-sites/2-cases); #898 MSVC + WSL gcc, no engine/`.lib` touched → **30c-2b ✅** the CUDA end-to-end: AUGMENTED 30b-3b's harness (one-oracle-body, renamed `30b-3b/30c-2b` #4851 Win/#4698 WSL) so the LOADED placement DRIVES `execute_two_class` while the hand-written `stage_class_from_partition` stays as the PARITY reference (REQUIRE-aborts on drift); closes the 30c-1 fold comment at the CUDA site (both call sites now walk find_dist_misuse, no code edit); Win RTX + WSL2 RTX] ⇒ **30c COMPLETE**] → **30z** close [**30z-1 ✅** the band-close BENCH — the §140 sharded-reduction TRANSFER-COST decomposition (A all-Host / B `[Host,Host,Gpu]` sharded / C the transfer FLOOR, the SAME lowered plan bit-exact all three ways); the census-predicted honest toy-dim loss made QUANTITATIVE: the cross-domain tax is a FIXED per-crossing cost (arm C ~constant in rows, `tax≈C≫combine`) amortized by O(rows×cols) shard compute → `B/A` collapses 345×→1.10× (Win) / 1021×→1.12× (WSL2) across `[8,4]`→`[4096,512]`; env-gated `CRD_CEIR_BENCH`, Win RTX #4853 + WSL2 RTX #4700, board `docs/bench/2026-09-05-ceir30-sharded-reduction-transfer-cost.md` → **30z-2 ✅** the CLOSE (docs-only): census RECONCILED (F32-eval caveat + boundary-transfer STAGE + emit_reduce_cuda "absent" all struck/resolved in place; §103 struck from the 29z carried-forward list) + deferral ledger w/ triggers + 3 scar clauses into existing homes + the close doc `docs/sessions/2026-09-05-ceir-30z-band-close.md` + flip ✅]. ✅✅✅ **CEIR-30 COMPLETE**. DEFERRED w/ trigger: real 2-GPU mesh · NCCL/MPI/cluster · §103 reshard · the §68 collective long-tail · ~~an F32-rounding eval mode~~ **[RESOLVED 30b-1: eval_cpu is f32-faithful per-op]** · reduce-on-CUDA (emit_reduce_cuda outer-axis + push-contract — trigger: a CUDA shard whose per-rank compute is a reduce). | §68 §103; proof §140 |
| ✅ | CEIR-31 | Media/UI/audio bridges (absorbs I2D effect graphs, MED workflows, audio graphs) → routed through CEIR, no new schedulers · reuse: MED-1 codecs (stay native, §177), the UiWorld/Canvas plan · **31-0 census ✅ 2026-09-05:** runnable proof = TWO real proofs (advisor, reconcile FLIPPED image-first→audio-first). **31a = §142 AUDIO (offline)** FIRST — `render_graph` (audio_graph.hpp) is a device-free, deterministic, BIT-EXACT CPU-numeric renderer with a native oracle already in-tree, reuses the CEIR-30 Host-executor ORCHESTRATION (the 30b-2b-1 `HostKernelResolveFn` per-stage CPU-kernel exit — NOT `synth_*→eval_cpu`: biquads are stateful recurrences needing a new per-node kernel form, 31a-1's fork [CKIR kernel vs a `ceir.audio` dialect]; bit-exact needs mirroring render_graph's f64-state/f32-bus rounding), and "no new schedulers" is FALSIFIABLE (a bespoke node-walk ~~to DELETE~~ **[⛔ 31z ruling (4), 2026-09-06: render_graph is KEPT — it is the LIVE offline product path AND the bit-exact differential oracle; deletion-is-done does NOT apply to code the CEIR asset has not replaced (only a proof exists, realtime is deferred). The proven falsifiable claim is "no scheduler ADDED to CEIR": `execute_audio_graph_ceir` walks CEIR's block/def-use order, ZERO Kahn/queue code]** + a bit-exact oracle); the §142 node-set gap (delay/compressor absent from render_graph AND hesap-dsp) → split 31a-1 (existing Source→Gain→Biquad→Mix bit-exact-through-CEIR) then 31a-2 (delay+compressor kernels). **31b = §141 UI effect (RAF/GPU)** — the frosted-glass chain (backdrop→downsample→blur→tint/noise→composite) authored as CEIR on RAF (reuses RAF, the mandate; "no new schedulers" merely ASSERTED — nothing deleted); NEW effect-chain authoring (CEIR-18 did scene renderers; no committed image-effect chain exists; post kernels are test-fixture `build_*` headers only). **31z** close (no obvious bench — correctness/deletion, not speed). DEFERRED w/ trigger: I2D effect graphs (ADR-0107/Track B) · MED chains (Track D) · RT-safe realtime audio + hot-reload (audio_device.hpp — offline is the runnable proof, realtime is the trigger) · CLAP hosting (PLG-3) · UiWorld/Canvas widgets (ADR-0107) · geometry pipelines §178. MAP: **31a** §142 audio-offline [**31a-1** existing-node bit-exact-vs-render_graph [**31a-1a** the `ceir.audio` dialect [**31a-1a-i ✅ 2026-09-05** the FIVE structural nodes (`audio.source`/`gain`/`biquad`/`mix`/`send` via opgen, each `determinism=BitExact` + `domain=HostAudioTime`; source=NOT-Pure/no-operands, gain/mix/send=Pure, biquad=StateEdge, mix=variadic) + `find_audio_misuse` (7 semantic kinds mirroring the AGRF `graph_validate`: OperandNotTensor/ResultTypeMismatch[shape-preserving]/BiquadFilter[0,3]/Cutoff(0,1)/Q>0/SourceStartFrame≥0; I6 op-NAME switch, const Context, ⛔ `.valid()`-guarded BEFORE every attr read so the absent case is the generated `verify_*`'s job not a dead wrong-kind branch — [[feedback_ceir_attr_reader_must_check_valid_absent_reads_as_zero]]) + the device-free gate (well-formed source→gain→biquad + 2-input mix + TEXT round-trip + a 4-filter ACCEPTANCE sweep {0,1,2,3} + 7 misuse-by-op-IDENTITY incl. BOTH filter bounds `4` and `-1`); MSVC #645/#646 + gen-smoke #653/#654/#655 + opgen-drift #725, WSL gcc 2/2 + drift; advisor-AFTER fixed 2 (the absent-attr dead branch — the gate had gone green on the mix section's `create_operation` source reading absent `start_frame` as Int 0, a quirk; the fix makes it green on the RULE — and the untested `fl.i<0` lower bound); `find_structure_error==None` (the DAG/StateEdge-feedback rule) DEFERRED → 31a-1a-ii's reading gate; opgen `resource_ops` showed ` M` vs HEAD = a prior-slice uncommitted edit made consistent, drift-check confirms] → **31a-1a-ii ✅ 2026-09-05** the committed `assets/ceir/audio_source_gain_biquad_mix.ceir` (Source@a→Gain{-6dB}→Biquad{Lowpass,0.25,0.7071}; Source@b; Mix(%2,%3) — static params, ≥2 edges into Mix so ~~operand=edge order is testable~~ [31a-1b-ii STRIKE: VACUOUS on a 2-input mix — IEEE f32 add is COMMUTATIVE (a+b==b+a bit-exact); the order contract binds only at ≥3 inputs (associativity), untested until a 3-input fixture exists] at 31a-1b) BOOTSTRAPPED-via-print (`build_audio_module` KEPT as the oracle; binary-mode LF-only write, then stripped) + a device-free reading gate (test #647): anti-drift-THROUGH-THE-PRINTER (`print(parse(file))==print(oracle)`, never file-bytes) + roundtrip-stable + `find_audio_misuse==None` + ⛔ `find_structure_error==None` (the DAG/StateEdge rule — the func body is a Graph region [`ir.hpp:312` default, no terminator required], the graph a pure DAG; CLOSES the 31a-1a-i structure-verifier gap the advisor flagged, AND the SAME CHECK added to the dist 30a-1 gate #644 so BOTH committed-asset reading gates on `crd-ceir-tests` structure-verify). ROW NOTES: (a) the printer emits SHORTEST-ROUNDTRIP DECIMAL (`0.7071` survives print→parse→print) so ANY authored f64 attr roundtrips exactly — the `%g`-precision-cap risk does NOT apply, 31a-1b's kernel reads the PARSED f64; (b) the mix result stays DANGLING by design (no `func.return` — `create_func(…,0U)`=zero results, a return would be `YieldCountMismatch`; how 31a-1b names the graph output is 31a-1b's call, the anti-drift test makes a regen safe); (c) ⛔ struck the `#ifndef CRD_REPO_DIR "."` fallback in BOTH TUs (inherited from the 30a-1 mold — it's the cwd-luck scar PRE-ARMED, a missing define must fail LOUD; CMakeLists:96 always defines it) — [[feedback_cuda_test_target_missing_crd_repo_dir_is_cwd_luck]]; (d) WSL `CRD_REPO_DIR`=the shared `/mnt/d` tree (the dist gate's precedent), the audio gate's `REQUIRE(f.good())` on WSL is the direct proof. MSVC #645/#646/#647 + dist #642/#643/#644, WSL gcc 3/3+3/3, tidy-clean] → **31a-1b** host lowering + BIT-EXACT-vs-render_graph proof [**31a-1b-i ✅ 2026-09-05** the KERNEL EXTRACTION (the 30b-2b-1 identical-kernel precedent applied to audio): `engine/audio/include/crd/audio/audio_kernels.hpp` = the three per-node-per-block stages `apply_source`/`apply_gain`/`apply_biquad` (+ `db_to_linear` moved here as the ONE gain-law site), ⛔ f32 PARAMS (cutoff/q/gain_db) so a caller reading an f64 CEIR attr is FORCED by the signature to narrow through f32 (the coeff-divergence guard) + biquad f64 state PASSED IN (so a single-pass [start=0,len=frames] call and render_graph's per-block call are the SAME function — single-pass==chunked made structural); `render_graph` REFACTORED onto them BYTE-IDENTICAL (trace: 3 case bodies, diff = `mine→bus`/`node.*→param`/`bq_z1[node_index]→z1` pointer-decay/`q→qq` rename; the automated per-sample-dB branch stays INLINE — the dialect has no automation node); the 3 direct includes filter/rbj/cmath moved into the kernels header (correct coupling, tidy confirms no unused-include). ⛔ REGRESSION GATE = the 4-TRACK bit-STABILITY gate (`render(x)==render(x)` + properties) + the CODE TRACE — NOT a frozen-reference-buffer gate (none exists in-tree; 31a-1b-ii's CEIR-vs-render_graph IS that frozen ref, superseding). DEBT: the kernels header includes the RENDERER header (audio_graph.hpp) for `GraphSourceBinding`+`BiquadType` (2 PODs) → invert the arrow (an audio_types.hpp) IF a 2nd consumer outside engine/audio appears. `crd-audio-tests` #6496/#6497 MSVC + WSL gcc `-Werror` 3/3, tidy 2/2 → **31a-1b-ii ✅ 2026-09-06** `execute_audio_graph_ceir` in engine/audio (`ceir_audio_render.{hpp,cpp}`, engine/audio links crd-ceir — a consumer dep, ADR-0109-clean): walk the func body in authoring order, per op alloc f32[frames*2] + sum operands in order + I6 op-NAME dispatch to the shared kernels with attrs narrowed `(f32)attr.as_float()`; sink = unique unused-result op (convention; `audio.output` the eventual contract); bindings = (name,GraphSourceBinding) list keyed by the source `name` attr; the FROZEN-REFERENCE gate = load the committed `audio_source_gain_biquad_mix.ceir` + hand-build the matching AGRF (5 nodes/4 edges/out=4) + 2 distinct non-trivial stereo buffers (decaying sine + noise burst) + N=1000 (≥4 blocks) both paths byte-identical; ⛔ add `CRD_REPO_DIR` to crd-audio-tests CMakeLists (no asset reads today → absent; NEVER a TU fallback — the cwd-luck scar). RESULT: MSVC #6501 + WSL gcc `-Werror` both byte-identical (memcmp). ⭐ PROVEN (bit-exact, both toolchains independently): the ORCHESTRATION — CEIR's authoring/topological walk + per-node operand sum + unique-sink + `(f32)` narrowing == render_graph's Kahn walk + edge-order sum + out_node; PLUS by-NAME source routing (the negative control: rebind @a↔@b → output DIFFERS, so `name` is READ not positional). SHARED (proven elsewhere, not here): the kernel MATH — `apply_*` are the SAME function both sides (identical-kernel precedent); hesap-dsp rbj gate + 4-TRACK prove them. VACUOUS (correctly): mix operand-order on a 2-input mix ([[scars_ckir_emitter_eval]] commutative≠associative — an edge-swap negative control returned `0!=0`, byte-identical; the order contract binds only at ≥3 inputs). UNTESTED this fixture: source `start_frame`/`loop` non-zero (trivial values), multi-func modules (first `func.func` taken). `.valid()`-guarded 7 attr reads (the attr-reader scar); CMake crd-audio→crd-ceir PUBLIC + CRD_REPO_DIR on crd-audio-tests; tidy 3/3+1] ⇒ **✅✅ 31a-1 CLOSED** — the §142 EXISTING-NODE-SET proof (route the offline audio graph through CEIR, NO new scheduler, BIT-EXACT vs render_graph; render_graph STAYS as the reference — deletion is a 31z/CEIR-34 call once 31a-2 lands the full §142 chain). DEFERRED w/ trigger: a **3-input mix fixture** (an order-sensitivity gate WITH teeth — trigger: the first authored graph with a 3+-input mix, where associativity makes operand order bind) → **31a-2** the §142 chain's remaining nodes (NEW kernels, NO oracle → closed-form gates) [**31a-2a ✅ 2026-09-06** `audio.delay` {delay_frames:int} (StateEdge — the classic feedback echo; NOT Pure) via opgen (drift-clean) + `AudioMisuseKind::DelayFramesInvalid` (delay_frames≥0, NO upper bound) + `apply_delay` in audio_kernels.hpp (in-place BACKWARD single-pass, `out[n]=in[n-D]` n≥D else +0.0F, one caller — render_graph has no delay) + the executor dispatch. GATES: device-free misuse-by-identity (delay_frames −1 → kind+op, 0/5 → None) #648 + a DIRECT-kernel bit-exact (4 D cases: 0=identity/17/n−1=last-frame boundary/n+10=silence, vs a hand-shifted reference) + an executor-DISPATCH wiring gate (`expect = apply_delay(apply_source(zeroed))` — proves order/routing; numerics proven by the direct gate + 31a-1b-ii) #6503, MSVC + WSL gcc `-Werror`, tidy 6+2. Two bugs the gates caught: (1) the PASS-1 allow-list missed `audio.delay` (dispatch had it → `0==64` refusal) → collapsed the TWO op-set enumerations into ONE `is_audio_op` + a PASS-2 final-else assert ([[feedback_widening_a_closed_enum_audit_every_consumer_not_just_total_switches]] string-vocab form); (2) a copy-based reference preserved an input `-0.0F` while the executor's `+=`-into-zero source render normalizes `-0.0F→+0.0F` (render_graph's model) → reference rebuilt via `apply_source` ([[scars_ckir_emitter_eval]] sign-of-zero). DEFERRED: a feedback-echo fixture (delay's StateEdge exemption declared-not-exercised, w/ the 3-input mix) → **31a-2b** `audio.compressor` (the §142 chain's LAST node — genuinely-new numerics; split -i kernel / -ii executor) [**31a-2b-i ✅ 2026-09-06** `audio.compressor` {threshold_db,ratio,attack_ms,release_ms:float} (StateEdge; NOT Pure) via opgen (drift-clean) + 3 `AudioMisuseKind`s (ratio≥1 / attack_ms>0 / release_ms>0 by identity; threshold_db UNrestricted; `.valid()`-guarded) + `apply_compressor` in audio_kernels.hpp — a FEED-FORWARD peak compressor: stereo-LINKED peak detector `d=max(|L|,|R|)`, linear one-pole envelope `env=c·env+(1−c)·d` (`c=exp(−1/(ms·1e-3·sr))`, attack when d>env else release), hard-knee static curve on `20·log10(env)` (`gain_db=(env_db>threshold_db)?(env_db−threshold_db)·(1/ratio−1):0`), the SAME gain to both channels, ⛔ UPDATE-then-gain + gain narrowed ONCE (`(f32)(x·gain)`), `f64& env` scalar state, NO makeup (a following audio.gain); f32 params (the narrowing discipline), sr a KERNEL param at -i. GATES: device-free misuse-by-identity ×3 + well-formed incl. ratio==1 boundary + negative threshold #649 + a DIRECT-kernel TOLERANCE gate vs an INDEPENDENT `std::exp/log10/pow` closed-form on a TWO-SEGMENT step (attack rising THROUGH the threshold + attack→release switch at a NONZERO B below threshold + release CROSSING back below threshold — env closed forms `A·(1−a^(n+1))` / `B+(env_bd−B)·r^(k+1)`), the stereo-LINKED-peak-with-abs detector PINNED by an asymmetric-NEGATIVE-channel fixture (L=+0.5v ch0 / R=−v ch1 so d=v ONLY for linked-peak-with-abs — VERIFIED to fail by a temporary `d=|L|` hack) + `above_thr>0`/`unity_release>0` regime counters #6505, MSVC + WSL gcc `-Werror`, tidy 6+2. ⛔ TOLERANCE = 1e-5 RELATIVE contract + a 2e-7 tripwire: the observed max deviation ~6e-8 = the f32 OUTPUT-quantization floor (1 ULP of the f32 bus), NOT the f64 libm diff (~1e-16, below the floor + not gated) — the advisor's 1e-9 estimate assumed an f64 output; MEASURED-then-set ([[scars_ckir_emitter_eval]] f32-storage-floor, the 3rd fp-semantics entry). `is_audio_op` ADMITS `audio.compressor` but the executor has NO dispatch branch yet → a compressor graph hits the PASS-2 assert (debug) / silent `return 0` (release) — the -ii reminder BY DESIGN → **31a-2b-ii ✅ 2026-09-06** the compressor EXECUTOR path + graph `sample_rate` — RESOLVED as a `func.func` int attr (⛔ NOT an `audio.graph_meta` op: a 0-result meta op would false-read as a 2nd unconsumed SINK → ambiguous-output; the `AudioGraphResource.sample_rate` graph-global-field + the recursion-policy func-attr precedent, advisor BEFORE): the executor reads it ONCE (`fn->attr("sample_rate")`, ABSENT→48000, present-but-non-Int-or-≤0→graceful `return 0`) → the PASS-2 `audio.compressor` dispatch calling `apply_compressor(mine, frames, …, sr, env)` (`env` a local per-op f64, single-pass, passed-in like biquad z1/z2) + `find_audio_misuse::SampleRateInvalid` on the func op (`.valid()`-guarded; ⛔ NO generated verifier owns `func.func`'s attr so this is the ONLY guard before the executor's silent reject — a `48000.0` Float slip is a REACHABLE kind-arm, gated). GATES: the `sample_rate` func-attr TEXT round-trip (print→parse→print stable + the attr SURVIVES + reads 48000 — proves authorable-in-TEXT, not just in-C++, the printer emits ALL op attrs generically) + SampleRateInvalid by identity on BOTH a 0 (`≤0`) and a Float (non-Int) rate #650 + the executor-PATH gate #6507/#6250: source@s→compressor rendered == `apply_compressor(apply_source(zeroed), …, 48000)` BIT-EXACT (memcmp — ⛔ this proves ROUTING + sr PLUMBING, NOT numerics [those are -i's tolerance gate]; both sides call the IDENTICAL kernel, a hardcoded 48000 would false-green it) with the asymmetric-NEGATIVE-channel source inheriting the linked-detector teeth THROUGH `apply_source`, PLUS the sr-SWAP teeth (the SAME module at 96000 MUST differ — the ONLY arm that SEES the executor read the rate) + a graceful-reject arm (`sample_rate=0`→`execute==0`). MSVC + WSL gcc `-Werror`, tidy 5+2; the stale `is_audio_op` "NO dispatch yet" comment struck. NO opgen (a func attr, not a new op)] ⇒ **✅✅ 31a-2b CLOSED ⇒ ✅✅ 31a-2 CLOSED** — the §142 chain source→gain→biquad→**delay**→**compressor**→mix is EXECUTABLE through CEIR, both toolchains → **31a-3 ✅ 2026-09-06** the COMMITTED §142 full-chain asset `assets/ceir/audio_full_chain.ceir` (NEW, LF-only): `main{sample_rate=48000}{ source@a→gain→biquad→delay→compressor; source@b; mix(%4,%5) }` — ALL SIX node types + the sample_rate func attr in ONE committed asset, bootstrapped-via-print (the 31a-1a-ii mold; `build_audio_full_chain_module` KEPT as the anti-drift oracle, the write STRIPPED) + a THREE-arm gate: (a) READING #651 — anti-drift-THROUGH-THE-PRINTER (`print(parse(file))==print(oracle)`) + roundtrip-stable + `find_audio_misuse==None` + `find_structure_error==None` + sample_rate read FROM THE FILE (not the in-memory #650) + ⛔ a COMPLETENESS arm (EXACTLY 7 ops, all 6 node names, source×2 — the mold's BLIND SPOT: a node-DELETED asset also parses+verifies clean, the reading arms can't see a missing node); (b) EXECUTION #6509 — `execute_audio_graph_ceir` over the committed asset == the composed kernels BIT-EXACT (memcmp), the reference mirroring the executor's per-node "zeroed bus, += inputs, apply op" EXACTLY (the sign-of-zero normalize at EVERY node — not in-place) single-pass, ⛔ its PARAMS DERIVED FROM THE PARSED ASSET not literals (an asset param edit + oracle-regen can't leave the reference testing stale values — anti-drift-through-the-printer applied to the execution reference; proves the 6-node TOPOLOGY walk [sums/order/sink/sr], NOT numerics); (c) slurp+parse extracted to a `load_committed_ceir` helper per TU (the cwd-luck `REQUIRE(f.good())` in ONE place; caller-owned buffer). MSVC + WSL gcc `-Werror`, tidy. ⇒ **✅ 31a IMPLEMENTATION COMPLETE** — every 31a subslice done (-1 existing nodes bit-exact-vs-render_graph, -2 new delay+compressor kernels, -3 committed complete chain authored+verified+executed). ⛔ **31a is NOT CLOSED** — it closes at **31z** on three RULINGS (not missing work): (1) a 3-input mix order-sensitivity fixture (associativity teeth — [[scars_ckir_emitter_eval]] commutative≠associative), (2) a feedback-echo fixture (the delay/biquad/compressor StateEdge exemption declared-not-exercised), (4) the `render_graph` delete-vs-keep-as-oracle call, (5) ⛔ the Tensor-shape-vs-`frames` NON-CHECK (the asset's `!tensor<…[256,2]>` shape decl is NOT validated against the executor's `frames` call param — a declared-words-not-validated gap, [[feedback_declared_header_words_must_be_validated_at_cook_time]] family); ~~(3) the COMMITTED full-chain asset~~ ✅ DONE at 31a-3] **✅✅✅ 31z BAND CLOSE 2026-09-06 — CEIR-31 CLOSED (2 Win + 2 Linux + tidy 5/5; crd-ceir 159 + crd-audio 109721 Win / 109718 Linux [the −3 = the [soak] realtime headless skip]):** the four 31a rulings RESOLVED — **(1)** committed `assets/ceir/audio_mix3.ceir` (3-source→3-input mix, `[dyn,2]`) + reading gate (anti-drift + completeness: 4 ops, mix.num_operands==3) + EXECUTION gate (CEIR==render_graph BIT-EXACT in authored order, PERMUTED `mix(a,c,b)` DIFFERS; ⛔ the tooth guarded by a REQUIRE f32-non-assoc precondition va=1e8/vb=1/vc=-1e8 → authored 0.0/permuted 1.0, no sign-of-zero); **(2)** the delay StateEdge exemption EXERCISED (delay-headed `delay(mix(src,delay))` back-edge → find_structure_error==None; the SAME cycle Pure-headed → FeedbackWithoutState — the TRAIT admits it) + ⛔ the EXECUTOR fix `ceir_audio_render.cpp` `*s>=my`→return 0 (REFUSE a back-edge, mirroring render_graph's Kahn cycle-refusal — closed a CAN'T-FAIL: the verifier BLESSED [StateEdge] a graph the single-pass executor would SILENTLY mis-run as an echo-less passthrough; graph-feedback EXECUTION = a future SCC-eval capability, no bit-exact oracle, LEDGERED); **(4)** render_graph KEPT (live product path + differential oracle; the "no scheduler ADDED to CEIR" claim proven, the census "node-walk to DELETE" clause SUPERSEDED-in-place above); **(5)** the audio buffer type CORRECTED `!dim<256>`→`!dim<dyn>` (dim0 = the runtime block size — a static 256 was a latent contradiction the 1000-frame block-invariance test drove through) + cook-time `find_audio_misuse` SHAPE validation (TensorRankInvalid / ChannelCountInvalid [dim1 Static==2] / FrameDimInvalid [dim0 Static≥1-or-Dynamic, reject Symbolic/0]) + execute-time `frames`==PINNED-Static-dim0 EQUALITY (Dynamic accepts any block). RECLASSIFY sweep: every `build_*` is a printer/differential ORACLE or a loader, ZERO runtime C++ algorithm builders (details → the band-close doc). §141+§142 band-row FLIPPED ✅. Advisor-BEFORE (the four forks + the [dyn,2]-vs-static reconcile) + advisor-AFTER. Full detail + reclassify + deferral ledger + proposed commit → `docs/sessions/2026-09-06-ceir-31z-band-close.md`. → **31b-0 CENSUS ✅ 2026-09-06** (advisor census-grade BEFORE, facts grep-VERIFIED not guessed): §141 UI EFFECT = the frosted-glass chain (backdrop→downsample→blur→tint/noise→composite) as an AUTHORED RAF frame. ⭐ VEHICLE = EXISTING machinery, ZERO new ops/executors/dialect (the "new UI-effect dialect" census question CLOSED-**no**): a `ceir.frame` graph (frame.pass nodes) authored as a `.frame.toml` (the SOLE frame source — converts to ceir.frame via the 15a-3 round-trip-gated converter; NOT direct ceir.frame text) whose passes run committed `.ckir` fullscreen kernels via `kind="raster.fullscreen"` + `shader="engine://ui/..."` (the CEIR-18p `resolve_program` disk-first `.ckir` path, the tonemap_agx post-pass precedent). VERIFIED FACTS: (1) forward_agx's `post` pass (`kind=raster.fullscreen`, `reads=[scene_hdr]`, `writes=[@output]`) IS the "read scene, write ELSEWHERE" precedent → backdrop capture is NOT the RMW scar (composite reads a backdrop COPY, writes @output); (2) `transfer.copy` is a WIRED `.frame.toml` kind (`{"copy",kExecTransferCopy}` frame_asset.cpp:1752) DISPATCHED in frame_runtime.cpp:1034 + converted frame_ceir.cpp:28 → backdrop capture = a `kind="copy"` pass (scene_color→backdrop transient), ZERO new code (the §141 toml:753 "needs backdrop capture" limitation PREDATED transfer.copy-as-a-pass); (3) a fullscreen pass names a `.ckir` by MOUNT id (CEIR-18p `ckir_read` disk-first — the visbuffer/velocity/cluster `.ckir` precedent) → NO kernel-by-path plumbing; (4) RAF transient aliasing EXISTS (frame_graph.hpp/raster_context.hpp) but the effect chain is its FIRST ping-pong-blur consumer (blur_h→blur_a, blur_v→blur_b, iter2 reads blur_b writes blur_a = a cross-pass WAR on blur_a, the [[feedback_frame_graph_war_needs_resource_lifetime]] scar) → the device gate MUST render a 2-iteration blur CORRECT, don't assume the aliaser handles it. ⛔ DEVICE GATE = NUMERIC (not golden-image): a synthetic hard-EDGE backdrop (left=A / right=B) → the chain → readback, assert (a) FAR-from-edge output==tint(A)/tint(B) ±1 LSB (blur of a constant IS the constant — pins tint+composite), (b) AT-edge strictly-BETWEEN + monotone (pins blur happened + its direction), (c) transition WIDTH scales with blur radius (radius 1 vs 4, width₄>width₁ — pins the kernel reads its radius); both Vk+DX12 each pass (a-c), cross-backend memcmp NOT required (blur precision differs). SPLIT (advisor; the 2 checks came back EXISTS ⇒ **31b-2 STRUCK**; a 3rd check reshaped 31b-1 — see below): ⛔ **the FIVE effect `.ckir` kernels are all STRAIGHT-LINE HAND-AUTHORED (no bootstrap-via-write, no builder)**: `eval_cpu_kernel` is SCALAR + REFUSES Vec nodes ([[scars_ckir_emitter_eval]] — a Vec4 color kernel has NO device-free NUMERIC gate), and the blur is FIXED unrolled taps + a spec-const for direction/spacing (SpecConst EXISTS, ckir.hpp:1693 names "blur direction"; `moment_blur.ckir` = 9 TexSamples / 0 For loops = the unrolled-blur precedent) — NOT a For-loop, so NOT complex, so NO `ckir_write` bootstrap; the loop micro-gate ~~31b-1c~~ is STRUCK. Per-kernel device-free gate = STRUCTURAL (⛔ NOT CPU-eval): `ckir_read`→`ckir_write` byte-exact roundtrip + `emit_fragment_glsl`/`_hlsl` smoke + op-vocabulary/tap-count + `find_*_misuse==None`; NUMERICS are 31b-4's on-device gate. **31b-1a** the 3 STRAIGHT-LINE non-blur kernels (`ui_backdrop_fetch` ✅ 2026-09-06 = one TexSample of the backdrop [RENAMED from ~~ui_downsample~~; advisor-AFTER reconcile: a 1-tap fetch is NOT a downsample kernel — the 2×2 box average is a FRAME-GRAPH contract (a LINEAR sampler + a 2:1 dest transient), NOT this kernel; a resolution-INDEPENDENT 4-tap box = ~36 nodes of Vec math NO device-free gate can verify (NOT worth the risk on the chain's FIRST kernel + NOT a fallback), and a ~16-node version BAKES the resolution = a WORSE coupling than the sampler]; STRUCTURAL gate WITH TEETH #4136/#4134: parses + byte-exact roundtrip + EXACTLY 1 TexSample + 0 StorageLoad (the discriminator vs rt_composite's storage read) + emits GLSL `texture(` AND HLSL `.Sample(` (the sampler is USED, not a dead binding); ⛔ 31b-3 LEDGER — grep-VERIFIED `SamplerFilter::Nearest=0`=the SamplerDesc default so a pass with NO declared filter is a POINT sample = NO averaging → the backdrop_fetch pass MUST declare `sampler_min_filter`/`sampler_mag_filter="linear"` + the half-res dest transient, BOTH asserted by the 31b-3 round-trip gate ([[feedback_declared_header_words_must_be_validated_at_cook_time]]); MSVC + WSL gcc `-Werror`, tidy 1; **`ui_tint_noise` ✅ 2026-09-06 (2/3)** = `TexSample(backdrop)*tint + splat(noise·amp)` [tint = 4 SCALAR spec-consts (ids 0-3) via Vec2/VecConcat→vec4, amp = spec-const 4 (default 0.03, 31b-4 sets 0 for the ±1LSB arm); noise = a backend-EXACT integer hash of `(u32)FragCoord.xy` (`x*0x9E3779B1 ^ y*0x85EBCA77; ^=>>15; *0x27D4EB2F; ^=>>13; (f32)h·(1/2^32)`) — ⛔ all hash nodes dtype=U32 so `>>` lowers UNSIGNED (logical, not sar) + the u32-wrap oracle matches GPU; noise→float is `Cast<F32>` NOT IntBitsToFloat] hand-authored (Python-hex `cval`s = a text generator, NOT a C++ kgraph builder), committed `assets/ckir/ui_tint_noise.ckir` (37 nodes). Gate #4137/#4135 STRUCTURAL WITH TEETH — ⛔ THREE-TIER honesty: the roundtrip pins the file is CANONICAL (`ckir_roundtrip_diff` is a serialize(g)==serialize(parse(write(g))) FIXED-POINT test, never file-bytes), the teeth (1 TexSample, 5 spec-consts, BitXor≥2, Shr≥2, 0 StorageLoad, GLSL `texture(`+`>>`, both emits) pin the OP-VOCAB, NEITHER proves the cvals/wiring are the INTENDED hash (a wrong magic const = a valid f64 passes every tooth; advisor conceded a mistaken "roundtrip pins cvals" claim). MSVC #4137 + WSL gcc `-Werror` #4135 BOTH green true-by-run, tidy 1. ⛔ the hash-INTENT proof LANDED #4138/#4136 (part 2): a `build_hash_scalar` COMPUTE oracle (x,y from a BUFFER — eval_cpu_kernel is a scalar COMPUTE evaluator that refuses a Fragment FragCoord builtin) eval-verified BIT-EXACT vs a C++ `u32` reference (8 pairs, ≥2 with bit 31 set at BOTH shift stages = the logical-`>>` probe, one per shift) + a NODE+WIRING match asserting the committed `g.node(17..32)` == a run of the SAME helpers on op/dtype/cval AND operand WIRING (constant-offset map for intra-run operands; x↔n15, y↔n16 — catches a swapped-input / A·A mis-wire that survives emit). ⛔ SCOPE: eval==C++ proven HERE; GPU==eval rests on the emitter uint lowering (U32→`uint` ctype grep) → on-device 31b-4 arm (e). ⭐ the helpers are KEPT as permanent VERIFICATION FIXTURES = a THIRD `.ckir`-oracle pattern (NOT relu_vjp's keep-as-regen, NOT softmax's bootstrap-then-DELETE): **oracle-for-a-hand-authored-asset** (no kernel builder ever existed — the committed `.ckir` is the source; a scalar COMPUTE oracle proves the Vec4 asset's scalar sub-math the fullscreen kernel can't eval). Both toolchains true-by-run, tidy. `ui_composite` **DEFERRED to 31b-3** (author the kernel ALONGSIDE the frame so kernel `iidx` + frame slot can't drift, device-tested immediately): a `Mix(scene,effect,mask)` 3-texture fullscreen — ⛔⛔ **31b-3-b STRIKE (this census claim was WRONG, [[feedback_superseded_adr_clause_must_be_struck_in_place]]):** ~~FIRST-OF-KIND — NO fullscreen `.ckir` samples 2+ textures, the only multi-read precedent (`taa_resolve`) reads its extras via `fs_constants` StorageLoad NOT multiple samplers, the `input<N>`→(set,binding) contract isn't located → provable only on-device~~ — `deferred_lighting.ckir` IS a device-proven multi-texture fullscreen (n1 arrayed `Texture` count=3 @ the bindless heap binding 16 + n4/n6/n8 `SampleIndexed` at layers 0/1/2); the census missed it (credited only taa_resolve's StorageLoad). The contract IS located: a MULTI-read fullscreen binds its `reads` into the descriptor-array heap @ binding 16 in DECLARATION ORDER + `SampleIndexed(layer)`, a 1-read fullscreen binds a single texture @ binding 1 ([[feedback_one_read_fullscreen_binds_single_texture_not_bindless]]). ui_composite COPIES deferred's idiom (proven at 31b-3-b); the REMAINING device risk is narrow — reads→heap-layer ORDER + the mask CHANNEL (`.r`), a 31b-3-c authoring contract + a 31b-4 arm (a) tooth, NOT the binding mechanism. ⛔ ONE-Sampler-per-pass is a CONTRACT (`ap.sampler=pass_sampler(d)` — a pass has exactly ONE SamplerDesc; a multi-texture kernel feeds ONE Sampler node to every TexSample). rt_composite = `scene*shadow_mask` (a StorageLoad-mask MULTIPLY, NOT a lerp) so `ui_composite` is genuinely new, not a duplicate) + structural gates → **31b-1b** `ui_blur_h`/`ui_blur_v` (fixed unrolled taps, Gaussian σ-from-radius weights, spec-const direction/spacing — its own advisor-BEFORE on the tap weights) + structural gates → **~~31b-1c~~ STRUCK** (no For-loop) → **~~31b-2~~ STRUCK** (the `.ckir`-by-path + transfer.copy plumbing both EXIST, checks (2)(3)) → **31b-3** `assets/frame/ui_frosted_glass.frame.toml` (scene.raster synthetic edge → `copy` backdrop → downsample → blur_h → blur_v ×2 → tint_noise → composite → present) + the `.frame.toml`↔ceir.frame round-trip gate (the 15a-3 mold, device-free) + `find_frame_misuse==None` → **31b-4** the DEVICE gate (render on Vk+DX12, the (a)(b)(c) numeric asserts, radius 1 vs 4) → **31b-z** close (§141 row flip; capability toml:753 limitations updated from "not started"; glow/bloom/distortion — the OTHER §141 effects — ledgered w/ the SAME recipe, frosted-glass is the ONE proof). **31b-z ✅ 2026-09-06 — §141 CLOSED**: capability `ui_effect_graph` flipped target→**gate-only/L4** (renders a COMPLETE correct composite on Vk+DX12+llvmpipe device gates, but NO sandbox/app consumer ⇒ honest `gate-only` per the scene_rt L3/gate-only precedent, NOT shipped-basic which implies the app renders it — Fable INITIALLY advised shipped-basic, then retracted to gate-only when the scene_rt gate-only row came into view [level=maturity, status=evidence-source]); every field corrected off "not started" — deps=`forward_basic` (the shipped RAF frame graph it reuses, NOT the unshipped canvas_compositor/ui_material a shipped* row can't depend on), runtime_systems=`[]` (the "UiEffectGraph→compiler" abstraction was NOT needed — 31b-0 census, ZERO new ops, the frame IS the authored form), ckir=`fs`/exec=`fullscreen.raster` (no compute in the real chain), the 6 real asset ids + the real `[ceir31b][gpu][vulkan|dx12]` gate names; glow/bloom/distortion LEDGERED same-recipe (each a `.frame.toml` of committed `.ckir` fullscreen kernels on the existing RAF frame graph — NOT authored, frosted-glass is the ONE §141 proof); ADR-0107 §UiEffectGraph bullet + the lifecycle mermaid annotated w/ dated evidence notes (ANNOTATE not strike — the ADR describes the OPEN I2D-7 full per-panel graph, which the proof does not contradict). Gate: win-debug `[ceir31b]` **763 assert / 21 case** green THIS tick (21>13 ⇒ DX12 device gates ran; re-verified, NOT inherited) — docs-only close ⇒ tidy N/A (no C++ changed), the 4-config Win+Linux board is inherited from the g-2 code close. **31b-1b ✅ 2026-09-06 — `ui_blur.ckir`** (ONE separable-Gaussian asset; H/V = two frame passes setting the dir spec-const — the census's "ui_blur_h/ui_blur_v" was PASSES not files, the moment_blur precedent). **σ = R/3** (R=4, 9 taps → the ±4 window at ±3σ, ~0.27% truncated); weights `exp(-i²/2σ²)/Σ` computed f64 → each ROUNDED TO f32 → residual `1−Σf32` folded into the CENTER tap so the stored f32 set is symmetric and its f32 left-to-right sum is within 1 ulp of 1.0 (measured 0.99999994 — exact 1.0f unreachable for a symmetric f32 set, the f32-storage-floor). spec-consts dir.x@0 / dir.y@1 / step@2. Bootstrapped-via-`ckir_write` → committed (~104 nodes) → reading gate → the graph builder + `[.emit-ui-blur]` generator DELETED, the `blur_weights_r4_sigma_r3` weight ORACLE KEPT — ⭐ the **COMPOUND THIRD `.ckir`-oracle pattern**: softmax's bootstrap-then-DELETE + the tint_noise one-function-two-consumers oracle (the builder CALLED the oracle; the reading gate matches committed cvals to it) — the next multi-node Vec4 kernel with a derivable constant table copies THIS. Gate #4139/#4137 STRUCTURAL WITH TEETH: 9 TexSample, 3 spec-consts, 1 Texture, 1 Sampler, 0 StorageLoad, ⛔ **0 Vec3** (the 2D discriminator — a moment_blur copy-paste has 9 Vec3 array coords + fails) + a NUMERIC WEIGHT check (9 weights = the σ=R/3 oracle BIT-EXACT + symmetric + monotone-from-center + Σ within 1 ulp of 1.0) + a header carrying the σ-law/f32-fold/spec-ids/support-width + roundtrip + both emits. ⚠ the weight filter "non-spec F32 Const with cval∈(0,1)" is a property of THIS asset (the tap-index consts −4..4 fall outside the OPEN interval; it would break for a future blur with FRACTIONAL tap spacing e.g. i·0.5). MSVC + WSL gcc `-Werror` both green true-by-run, tidy. ⇒ **✅✅ 31b-1 COMPLETE** (backdrop_fetch ✅ + tint_noise ✅ + blur ✅; composite → 31b-3). ⛔ **31b-3 LEDGERS surfaced this tick:** (a) the blur `step` spec-const is PER-PASS ASYMMETRIC — 1/half-res-W for the H pass, 1/half-res-H for V (the first spec-const carrying a per-pass DIMENSION) → the `.frame.toml` must express a spec-const as a function of a transient's extent, OR bake the half-res extent as a literal and assert it matches the transient in the round-trip gate (whether that expression exists is a 31b-3 grep); (b) `step` default 1/1024 is a PLACEHOLDER bake — 31b-3 MUST set it per pass, else 31b-4 arm (c) width-scales-with-radius SILENTLY passes at the wrong step. **31b-3 MAP (advisor-scoped 2026-09-06):** **31b-3-a** the `spec_N` PASS PARAM (authoring + runtime TOGETHER) — SEAT = **A′ (advisor-locked): a SECOND ENTRY KIND in `ProgramRegistry`** (Spec beside Plain), NOT widening the `RasterFn`/`KernelFn` fn-ptr (⛔ REJECTED: 40 providers get an ignored param AND the `tonemap_agx`/bespoke providers SILENTLY DROP a spec → untinted-no-error = the asset-OK-but-BLACK class), NOT a spec-present discriminator (⛔ B REJECTED: "pass has spec_N → new path else registry" resolves the SAME kernel two ways; "registry-first fallback" softens the LOUD `nullptr→UnresolvedProgram` into a file-not-found deep in ckir_read — either is TWO resolution paths). MECH: `SpecRasterFn=IRasterProgram*(*)(void* user, SpecSet)` beside `RasterFn` + a kind tag + 2nd fn field on `RasterEntry`; `register_raster(id,RasterFn,user)` UNCHANGED→Plain, new `register_raster_spec`→Spec (ZERO edits to the 40 providers, public `register_raster_program` type kept); `raster(id,specs)`: Plain+count0→`fn(user)`, Plain+count>0→SpecOnOpaque LOUD-by-construction, Spec→`fn(user,specs)`. `SpecSet={const SpecConst* items;u32 count}` `SpecConst{u32 id;f64 value}` SORTED-by-id = a NEUTRAL POD (⛔ NOT `const FramePassDesc*` — scene-render's PUBLIC header must not know frame-cook's asset type). `SceneHost::program(id)`→`program(id,specs,&err)` (:5455 is the IMPL, `frame_runtime.cpp:832` the ONE raster caller) + the matching `host.kernel(`/`kernel(id,specs,&err)` (frame_runtime:302 consumes `p.kernel_program` — spec_N applies to compute.dispatch too). `err` out-param names THREE RECORD-TIME `FrameExecError`s (⛔ NOT FrameCookError — resolution is record-time): unregistered (today's) / SpecOnOpaqueProgram / SpecConstNotInProgram (provider `set_spec_const`==0). SPLIT: **31b-3-a-i** the AUTHORING + record-seat threading, further SUB-SPLIT (the seat is a PURE-VIRTUAL interface with ~13 implementors, a two-module change — too big for one tick): **31b-3-a-i-1 ✅ 2026-09-06** the `spec_N` PASS PARAM authoring (device-free, frame-cook only). ⭐ TWO discoveries collapsed the scope: (1) spec_N rides the EXISTING generic `[pass.params]` seam (frame_asset.cpp:943 `(*t)["params"].as_table()` reads it; frame_emit.cpp:571-584 emits every non-folded param; the ceir `p:`/`pt:` loop round-trips it) → parse/emit are FREE, ZERO new plumbing (the advisor's "add parse/emit" was already done by the generic machinery); (2) ⛔ CORRECTION to the advisor's "find_frame_misuse kind": the ceir.frame dialect doc is explicit — executor-specific OPEN attrs are validated at COOK against the executor schema, NOT the op verifier — so the check is a **cook-time `FrameCookError::SpecConstBadPassKind`** in the shared `pass_contract_diag` (run by BOTH `validate_frame_graph` desc-side AND the ceir cross-op sweep frame_ceir.cpp:1249), NOT core's `FrameMisuseKind` (which correctly stays ignorant of executor attrs). DELIVERED: `parse_spec_param(name,&id)` shared helper (name=="spec_"+≥1 digits, u32-overflow→reject; ONE home for the cook check + the 31b-3-a-i-2 SpecSet builder) + the check in pass_contract_diag (spec on a pass NEITHER fullscreen NOR compute → rejected BY NAME, placed BEFORE MissingDrawList so a spec-on-geometry verdict is the SPECIFIC one, not swallowed) + `SpecConstBadPassKind` enum + message. GATES (test_frame_asset.cpp): the malformed-BY-NAME section — spec_0 on raster.geometry → SpecConstBadPassKind + offending pass named "geo"; spec on raster.fullscreen → Ok (no false positive); compute admitted (≠SpecConstBadPassKind); `spec_x` (non-digit tail) does NOT trip (parse_spec_param reject path) — PLUS a round-trip TEST_CASE: spec_0/1/2={1.0,0.0,0.5} parse → find_pass_param==exact → emit_frame_toml → re-parse → values survive EXACTLY + `!is_folded_pass_param("spec_2")`. MSVC + WSL gcc `-Werror` both green true-by-run (56/56), tidy 2 (fixed: `name.data()[i]`→`name[i]` simplify-subscript; local `kSrc`→`src` naming). ⭐ advisor-AFTER (APPROVED as built; pass_contract_diag home + the `!=SpecConstBadPassKind` compute tooth both CONFIRMED right): +`SpecConstDuplicateId` (spec_1 + spec_01 = distinct names the generic seam preserves → the SAME constant_id → a silent last/first-wins; a pairwise cook check in the fullscreen/compute else-branch, same file/test) + a raster.composite ACCEPT gate (both kinds → kExecFullscreenRaster; ui_composite is the spec-carrying pass, the tooth that matters most). ⛔ -a-i-2 LEDGER (own, don't inherit): the APPENDED default virtual's LOUD branch (specs.count>0 on a host with no spec support) needs a DISTINCT err value (`SpecConstUnsupportedByHost`, or reuse SpecOnOpaqueProgram — name the reason in the enum comment) + a TEST HOST to EXERCISE it, else it's unreached `return nullptr` reading as the unregistered case. **✅ 31b-3-a-i-2 DONE 2026-09-06 (MSVC + WSL gcc -Werror BOTH green true-by-run, tidy 5)** the RUNTIME SEAT (A′, advisor-plan-confirmed): `ProgramRegistry` Spec entry KIND (SpecRasterFn beside RasterFn + a kind tag on RasterEntry; `register_raster`→Plain unchanged, new `register_raster_spec`→Spec; `raster(id,specs,&err)`: id NOT found → nullptr+err untouched [the seat maps →UnresolvedProgram, so UNREGISTERED WINS over spec-on-opaque]; found Plain+count0→fn(user); found Plain+count>0→err=SpecOnOpaqueProgram+nullptr LOUD; found Spec→fn(user,specs,err)) — ⛔ the err out-param + the `SpecRasterFn(user,specs,FrameExecError* err)` provider sig are designed NOW (no Spec provider yet, but -a-ii's `set_spec_const==0`→SpecConstNotInProgram reports through the SAME channel; retrofitting err at -a-ii = a sig change to a just-introduced type). `SpecConst{u32 id;f64 value}` + `SpecSet{const SpecConst* items;u32 count}` + `kMaxSpecConsts=16` (⛔ ONE shared header both cook + record read, else drift; 16 ≫ tint_noise 5 / blur 3, impostor ~18 is bespoke-C++ never on this path) — home decided by the frame_asset.hpp↔frame_runtime.hpp include direction (SpecSet must reach the IFrameGraphHost virtual). ⛔ registry naming FrameExecError = fine (scene-render already includes frame_runtime.hpp for IFrameGraphHost) but ADD a one-line include-dependency comment so nobody "fixes" it. APPENDED virtuals `program(id,specs,&err)`/`kernel(id,specs,&err)` (⛔ NOT a sig change to the pure-virtual `program(id)` — ~13 implementors incl. a dozen test hosts; the "append to the vtable" idiom; DEFAULT: specs.count>0 → err=`SpecConstUnsupportedByHost`+nullptr [comment: a host predating spec support was handed a spec'd pass; a frame whose specs would be dropped FAILS not renders], else delegate to `program(id)` = ONE resolution path) + SceneHost override + record-seat threading at BOTH seats (frame_runtime:832 raster / **:943** compute kernel [NOT :302 — that's the payload assign; RT-stage host.kernel at :902-923 are raytrace passes, cook already rejects specs there]) building the SpecSet from `d.params` via parse_spec_param into a `kMaxSpecConsts` stack buf + `CRD_ASSERT` on overflow (cook-guaranteed ≤16) + the seat maps null: `perr!=Ok?perr:UnresolvedProgram`. COOK: +`SpecConstTooMany` in the pass_contract_diag fullscreen/compute else-branch (count >kMaxSpecConsts). GATES (provider-free, via SceneHost + a throwaway test host): spec_0 on a Plain-registered id → SpecOnOpaqueProgram+null; same id empty specs → the Plain program (unchanged path works); UNREGISTERED id + spec_0 → still UnresolvedProgram (order check); a minimal IFrameGraphHost overriding nothing spec-related + a fullscreen pass w/ spec_0 → err==SpecConstUnsupportedByHost (put where a throwaway host already exists — test_frame_ceir/test_frame_asset, NOT the dx12 device hosts); +17-spec fullscreen → SpecConstTooMany cook gate. ⭐ DELIVERED + DISCOVERIES: (1) SpecConst/SpecSet/kMaxSpecConsts landed in **frame_asset.hpp** (NOT frame_runtime.hpp) — the ONLY header both the cook [frame_asset.cpp] and the IFrameGraphHost virtual [frame_runtime.hpp includes frame_asset.hpp] can read; frame_asset.cpp does NOT include frame_runtime.hpp; (2) FrameExecError needed a FWD-DECL before IFrameGraphHost (enum defined after the class) → the two spec virtuals' default bodies are inline-defined AFTER the enum; (3) ⛔⛔ OVERLOADED-VIRTUAL (MSVC-silent, gcc-FATAL): appending `program(id,specs,err)` as an OVERLOAD of `program(id)` made every subclass overriding only `program(id)` HIDE it → gcc `-Werror=overloaded-virtual` broke ALL ~13 test hosts (MSVC gave NO warning — the WSL build caught it, the combined build's `| tail` had masked ninja's failure so a stale exe false-greened first). FIX: the two spec virtuals got DISTINCT names `program_spec`/`kernel_spec` (the house pattern for appended vtable virtuals — instance_program/overlay_pass/for_each_load never overload) → zero hiding, zero `using` on any host, the dozen hosts truly untouched. ⭐ scar: an appended virtual must NOT reuse an existing virtual's name under -Woverloaded-virtual; (4) ⛔ WIDEN-ENUM SCAR fired: gcc/tidy `-Werror=switch` caught the `frame_exec_error_text` switch missing the 3 new values — audited, ONE exhaustive switch (the message fn), the vulkan enum-message test loops only to UnsupportedCapability so it never reached the new codes (no break; the no-default switch guarantees a message by construction); (5) the registry SpecOnOpaqueProgram gate is a WHITE-BOX ProgramRegistry unit test (scene-render tests + a one-line src include-dir) using a `bool*`-via-`user` sentinel (no reinterpret_cast). GATES GREEN (MSVC): registry KIND logic (#6401), default-host SpecConstUnsupportedByHost (#4187), SpecConstTooMany cook (in the malformed case). ⛔ the record-seat threading itself (build_pass_spec_set + err-map at :832/:943) is proven at INTEGRATION when the frosted-glass frame records (31b-3-c/31b-4) — device-free here it's the registry + default-host + cook. NEXT = **31b-3-a-ii** = the GENERIC ui `.ckir` thunk (4 ui ids over ONE thunk = the post "two-providers-over-one-builder" precedent :1833; `user`=a per-registration struct {mount string, cache}) body = `resolve_program_text→ckir_read→each spec: set_spec_const-or-fail→lower_entry→create_program→create_raster_program(fullscreen_vs,fs)`, cached by (id, spec-set hash) linear-Array + a RELOAD hook (the prog_taa/`slot` reset precedent — a .ckir edit must not render STALE, RAF-11 family) + the KGraph READ-BACK gate (spec_0=0,spec_1=1 → cval 1.0 at id 1 in the LOWERED graph; swapped specs → TWO DISTINCT cached programs; spec_7 → SpecConstNotInProgram). ⛔ the read-back tests the PROVIDER (its (id,spec-set) cache + set_spec_const patching), NOT the record SEAT — the SEAT proof (build_pass_spec_set + the err-map threading each pass's specs at :832/:943, unit-untested in -a-i-2 by design) is a **31b-3-c INTEGRATION tooth**: a frame with dir spec_0=1/spec_1=0 on the ui_blur H pass + spec_0=0/spec_1=1 on the V pass records → the two passes resolve to DIFFERENT programs (H≠V blur direction). Don't conflate the two. ⛔ READ-BEFORE-BODY (gates -a-ii, not -a-i's start): the fullscreen-VS pairing (`ensure_post_program` pvs+pfs at :1874 — where does `pvs` come from) + `reload.hpp` reset. FORK (2) baked ui_blur_h/ui_blur_v assets REJECTED (baked step = a hardcoded proof resolution = the coupling rejected on backdrop_fetch). → **31b-3-b** `ui_composite` via the bindless idiom (read deferred_lighting.ckir end-to-end — the multi-texture fullscreen binding-layout) → **31b-3-c** `ui_frosted_glass.frame.toml` + panel-quad mask pass + the spec-const-from-extent question (ledger (a) above) → **31b-3-z** close. **✅ 31b-3-a-ii DONE 2026-09-06 (MSVC #6402/#6403 + WSL gcc -Werror BOTH green true-by-run 2/2 each, tidy 1)** the GENERIC ui `.ckir` provider + its DEVICE-FREE specialize core. DELIVERED: (1) `engine/scene-render/src/spec_apply.hpp` (NEW, header-only) = the ONE spec-set→KGraph seat — `apply_spec_set(fg,SpecSet,err*)` ALL-OR-NOTHING (pass 1 validates EVERY id is a spec-const BEFORE any mutation → a spec_N absent from the kernel FAILS `SpecConstNotInProgram` with the graph UNTOUCHED, never a silent half-specialization; pass 2 set_spec_const each — the emitted default IS the pipeline value both backends, no VkSpecializationInfo) + `spec_set_hash` (stack-sorted-by-id FNV-1a over (id,f64-bits) = ORDER-independent [set_spec_const ignores order] + VALUE-sensitive [a different blur dir = a different program], a BUCKET key) + `spec_set_equal` (order-independent collision backstop); (2) `ensure_ui_program(UiKind,SpecSet,err)` in scene_renderer.cpp = the ensure_moment_program recipe (cook_stage_named post_fullscreen VS → resolve_program_text disk-first → ckir_read → apply_spec_set → create_program → create_raster_program) CACHED per (kind,spec-set) in an `Array<UiVariant>{kind,hash,SpecConst[16],count,prog}` Impl member; (3) 3 `register_raster_spec` ids `engine://ui/{backdrop_fetch,tint_noise,blur}` (one captureless thunk per kind, `user=Impl*`) in register_default_programs; (4) the ui_variants sweep in retire_all_programs (rr each `prog`; `pfs` stages already ride adv_stages) — the fs_programs/fs_hashes precedent. GATES (device-free, test_scene_render): the cache-KEY order/value gate (#6402, asset-free/always-runs) + apply_spec_set on the COMMITTED ui_blur.ckir (#6403, CRD_ASSETS_DIR-gated) — a valid set patches ids 0/1 to SENTINEL values (0.25/0.75, teeth hold regardless of the authored default) + leaves step (id 2) UNTOUCHED, and a `{0→42, 7→1}` set (id 0 BEFORE the absent id 7) → SpecConstNotInProgram + id 0 STILL at default (the no-partial-apply teeth); the REQUIRE(spec_cval(2)) also PINS ui_blur's ids = dir.x@0/dir.y@1/step@2. ⭐ advisor-AFTER DISCOVERIES: **(1) RELOAD reconcile (struck-in-place, [[feedback_superseded_adr_clause_must_be_struck_in_place]]):** advisor-BEFORE ruled cache-once + a RAF-11 follow on the premise NO engine `.ckir` reload path exists; the reconcile FOUND `init_programs`(:3392)→`prepare_reinit`(:3400)→`retire_all_programs`(:1178) RETIRES moment_prog(:1223)+adv_stages(:1242) on EVERY RAF-11 commit (src_commit→rebuild_programs→init_programs) → the premise was WRONG, RESET is covered TODAY (ui_variants joins the sweep, per the :1172 "any program added later MUST be retired here too" mandate); TRIGGER — a `.ckir` `SourceReload` adapter (:1262-1300 covers .crdm/.crdv/.crdl only) — is the engine-wide RAF-11 follow SHARED with moment/post/taa, NOT a -a-ii gap. **(2)** register_default_programs is GUARD-ONCE (`default_programs_registered` :2648) → register_raster_spec's append-no-dedupe is SAFE (registry never grows on reload; raster(id) returns the FIRST match). **(3) lower_entry = a 31b-4 tooth, NOT settled:** NO lower (moment/hzb precedent — no Step/Floor/ternary); but tint_noise's Shr/BitXor/Cast<F32>-on-U32 through create_program's SPIR-V/DXIL path ≠ the GLSL emitter I inferred from — **31b-4 arm (e) [the hash-on-GPU arm] ALSO decides lower-vs-not for tint_noise; if create_program rejects/mis-lowers, the fix is ONE lower_entry call in ensure_ui_program, not an asset change.** **(4) dup-id ASSERT:** cook guarantees unique ids (SpecConstDuplicateId); a dup reaching apply_spec_set = a caller bypassing cook → `CRD_ASSERT_MSG` in pass 1 (the build_pass_spec_set overflow-assert idiom) ENFORCES the uniqueness premise spec_set_hash/equal rely on (a dup makes the sorted hash order-DEPENDENT + the by-id compare order-SENSITIVE). **(5)** tidy: UiKind enum base u32→u8 (performance-enum-size). ⛔ SCOPE (don't conflate): the gates test the PROVIDER's device-free core (apply_spec_set patching + the cache KEY), NOT (a) the record SEAT (build_pass_spec_set + the err-map threading at :832/:943 — a **31b-3-c** integration tooth: dir spec_0=1/spec_1=0 on H vs 0/1 on V → DIFFERENT programs), NOR (b) create_program / the cache-HIT path (a 2nd equal-spec call returns the same pointer, no recook — device-gated 31b-4, the H==H twin of the H≠V proof; the miss path calls create_program first, so it can't be tested device-free). composite stays DEFERRED to 31b-3-b (no committed asset → a provider whose resolve_program_text always fails = a nullptr indistinguishable from a typo). ⇒ **✅ 31b-3-a DONE** (the spec_N pass param: -a-i-1 authoring + -a-i-2 runtime seat + -a-ii provider — all device-free/record parts proven; on-device deferred to 31b-4). **✅ 31b-3-b DONE 2026-09-06 (MSVC #4140 + WSL gcc -Werror #4138 both green true-by-run, tidy 2)** `ui_composite` via the BINDLESS idiom. DELIVERED: the committed `assets/ckir/ui_composite.ckir` (NEW, 16 nodes, hand-authored straight-line — the backdrop_fetch/tint_noise mold) = `out.rgb = mix(scene.rgb, effect.rgb, splat(mask.r)); out.a = 1` over THREE textures via ONE descriptor-array `Texture` @ the bindless heap (set 0, binding 16, **count 3**) + ONE `Sampler` @ binding 2 (the ui-family slot, INHERITED from ui_blur — not copied from deferred blind) + three `SampleIndexed(tex,samp,uv,layer)` at layers 0/1/2 (the deferred_lighting n1/n4/n6/n8 precedent) + Swizzle rgb ×2 + Swizzle `.r` + Splat + Mix + VecConcat(·,1.0); registered `engine://ui/composite` via register_raster_spec + UiKind::Composite (0 spec-consts — a pure Mix — but Spec-registered so all 4 ui kernels share ONE provider; empty SpecSet = apply_spec_set no-op, a stray spec_N fails SpecConstNotInProgram at the SEAT). GATE #4140/#4138 STRUCTURAL WITH TEETH (device-free, the ui_blur mold; ⛔ NO CPU-eval — a Vec4 kernel, [[scars_ckir_emitter_eval]] Vec-refusal): parses + fixed-point roundtrip + `Texture==1` with **type.count==3** (the descriptor-array tooth — a count==1 single-texture copy is the WRONG idiom) + `Sampler==1` + `SampleIndexed==3` with the 3 index operands = U32 Consts **0/1/2 each exactly once** (the layer-order tooth — a `0,0,1` typo reads scene twice) + `Mix==1` + `TexSample==0` (the discriminator vs the other 3 ui kernels — they sample via TexSample, composite via SampleIndexed) + `StorageLoad==0` (vs rt_composite's storage-mask multiply) + **out-node type.rows==4** (a VecConcat that dropped the alpha fails HERE, not as black-alpha on device — advisor-AFTER) + both emits (GLSL `texture(` present = the bindless sample is live, not dead). ⭐ advisor DISCOVERIES: **(1) CENSUS STRIKE (in-place above):** FIRST-OF-KIND / no-fullscreen-samples-2+ / taa_resolve-only-precedent / contract-unlocated were ALL WRONG — deferred_lighting IS the device-proven multi-texture-fullscreen precedent, the contract IS located (multi-read → heap@16 in reads order + SampleIndexed(layer); 1-read → binding 1). **(2)** the asset-header mask clause fixed LOAD→CLEAR (LOAD preserves garbage; the mask transient must CLEAR to 0 so outside-the-quad reads 0 — a two-word fix that would have mis-authored the -c mask pass). ⛔ -c LEDGER (authoring contracts, in the asset header + here): declare `reads=[scene,effect,mask]` in THAT order (heap layers 0/1/2) · the mask pass writes a solid `vec4(1,1,1,1)` quad into a transient the frame CLEARS to 0, coverage in `.r` · Sampler@2 is the ui-family slot · the composite pass declares NO `spec_N`; the reads→heap-layer ORDER + the `.r` channel are provable only ON-DEVICE (31b-4 arm (a)), NOT the binding MECHANISM (proven by deferred + this gate). **31b-3-c SPLIT (advisor 2026-09-06):** **-c-i** author `ui_frosted_glass.frame.toml` (full chain: scene→copy backdrop→backdrop_fetch→blur h1/v1/h2/v2→tint_noise→mask→composite→@output) + the device-free ROUND-TRIP gate (the 15a mold: parse_frame_toml→to_ceir_frame→print fixpoint + find_frame_misuse==None + validate_frame_graph clean) + the AUTHORING H≠V tooth (find_pass_param spec_0/spec_1 on blur_h1=1/0 vs blur_v1=0/1) + the step-literal-matches-scale assert; **-c-ii** the RECORD-SEAT H≠V tooth (build_pass_spec_set yields DIFFERENT SpecSets for H vs V — expose the static helper or a recording test host). ⛔ **EXTENT RULING (ledger (a) RESOLVED, advisor):** BAKE the literal step for the proof frame (correct at ONE fixed resolution; the round-trip gate asserts step==1/(proof_res×scale)) — the extent-DERIVED step is a **31b-4 tooth** (a 2nd-resolution arm makes the literal fail visibly → the derivation lands THEN = a seat-side per-read-extent arg to build_pass_spec_set + a KERNEL-declared 'derive spec N from read M's extent axis A' mapping, the moment inv_size precedent generalized). ⛔ 3 SHORTCUTS STRUCK: a magic-float sentinel (§128-in-disguise), a String FrameParamType (widen-enum on a POD every consumer switches), a seat-hardcoded 'id 2 is step' (the generic seat must stay kernel-agnostic). **✅ 31b-3-c-i DONE 2026-09-06 (MSVC #4285 + WSL gcc -Werror both green true-by-run; full ceir sweep 936/936 MSVC + 50/50 gcc blast-radius; tidy 6/6)** — the committed `assets/frame/ui_frosted_glass.frame.toml` (7 transients: scene_color RGBA16F depth_buffer / backdrop / blur_src/a/b/effect scale 0.5 / mask R8; 2 draw_lists visible_geometry + ui_panels; 10 passes: scene [raster.geometry standard_forward] → backdrop_copy [copy] → backdrop_fetch [fullscreen, filter=linear] → blur_h1/v1/h2/v2 [fullscreen, spec_0/spec_1 dir + spec_2=1/512 step baked] → tint_noise → mask [raster.geometry, ui_panels, engine://ui/panel_fill placeholder] → composite [fullscreen, reads=scene_color/effect/mask→@output]) + the device-free ROUND-TRIP gate (the 15a mold: parse_frame_toml==Ok + validate_frame_graph==Ok + resources==7/passes==10/composite reads==3 + the AUTHORING H≠V tooth via find_pass_param [blur_h1 spec_0=1/spec_1=0 vs blur_v1 spec_0=0/spec_1=1] + step==1/(1024×0.5) + to_ceir_frame→print→re-parse→print FIXPOINT + find_frame_misuse==None on BOTH the source AND re-parsed module + the desc↔ceir emit-identity). ⭐ THIS FRAME EXPOSED + FIXED TWO ceir-CORE print/parse asymmetries (the FIRST engine://-id'd, param-carrying frame to round-trip through ceir TEXT): (1) a bare `@sym` with `://` (an asset id) and (2) a bare op-attr name with `:` (the frame `p:clear_color:0`/`pt:` param-bag key encoding) — BOTH broke re-parse (is_ident_char stops at ':'); FIXED with MLIR's QUOTED forms `@"..."`/`"..." = ...` via ONE shared `name_needs_quote` helper (print.cpp emit both, parse.cpp accept both), ZERO change to existing bare text (ceir roundtrip #230 pins both bare-and-quoted; 936/936 no regression). **⛔ SWEEP FINDING — a 26c/26z REGRESSION fixed here (caught by a `-R ceir` sweep WIDER than the change's DECLARED radius [text round-trips], NOT a -c-i defect — the lesson the scar encodes):** the 4 `ceir 15d-1` frame-hazard tests (#4252-4255) were RED (hz 1→4 on build_scene). ROOT: CEIR-26c/26z gave `resource.declare`/`resource.import` an `Allocate` effect (to block CSE — CSE keys on the `OpTrait::Pure` trait, and an effect makes the op non-Pure) but the hazard-access side was never taught what Allocate MEANS — `effect_access(Allocate)` was `{write,Memory}` (grouped with Dealloc/Residency for use-after-free), so every declare/import now WROTE its resource → spurious `declare→use`/`import→use` hazard edges polluting every frame's set (build_scene: declare(scene)→geometry + declare(scene)→forward + import(@output)→forward + the real geometry→forward RAW = 4). FIX (advisor-locked option C, the `Nondeterministic` mold): `effect_access(Allocate)` → `{false,false,None}` INERT — alloc-before-use is ALREADY an SSA def-use edge (the declare's RESULT is the consumer's OPERAND), so a memory-hazard edge is REDUNDANT; Dealloc/Residency STAY `{write,Memory}` (use-after-free is NOT SSA-implied). The Allocate effect still EXISTS on the op (non-Pure ⇒ CSE-blocked [26c-2 Vk+DX12 bit-exact #4839/#4932 still green]; `realtime_safe` still flags may-allocate [ADR-0116 unaffected]) — only its hazard access is inert. §4d judgment doc STRUCK-in-place (2026-08-09-ceir-4-effects.md ×2) + hazard.hpp case-split + comment + test_hazard.cpp assertions updated (Allocate inert / Dealloc still writes) + 15d-3 lifetimes re-verified (declare's `first` shift is benign). DEBT (ledgered, no opgen touch — the ALL-dialects-diff-lies scar): `resource.ceirop.toml`'s declare doc "the hazard walk orders allocations without treating declare as a memory access" is now approximately-true (SSA orders them, not the walk) — fix at the next opgen touch. ⛔ the quoted-name fix is TEXT-only: `binary.cpp` was NEVER affected (attr names + symbols are interned string-TABLE indices, so `p:clear_color:0`/`@engine://` already round-tripped through binary pre-tick). ⛔ **-c-ii PRE-WORK LEDGER (advisor-AFTER carry-forwards):** (1) `parse_attrs` + the `@` symbol path must `fail("empty ... name")` on a zero-length quoted name (`""`)/`@""` — MLIR-quoting ADMITS a nameless attr the builder can't construct + a fuzz-reachable `set_attr(op,"",…)` path; + a `rejects("… {\"\" = 1}")` malformed-inputs case. **✅ 31b-3-c-ii DONE 2026-09-06 (MSVC #6407 + WSL gcc -Werror #6175 both green true-by-run; ceir sweep 936/936 MSVC + 33/33 gcc blast-radius; tidy 6/6)** — the RECORD-SEAT H≠V spec-set tooth + the empty-name reject carry-forward. ⛔ SEAT-EXPOSURE RULING (advisor reconcile): a recording test host driving the REAL seat needs `IFrameGraph`+`IRasterContext` = device objects (FrameRecorder::record's params) → NOT device-free = the 31b-4 integration tooth (proves build→host.program_spec threading on-device); the DEVICE-FREE -c-ii proves the EXACT builder the seat calls (:832/:943) + the cache key it feeds. DELIVERED: (1) `build_pass_spec_set` MOVED out of frame_runtime.cpp's anon-ns into `crd::framecook::detail::` (declared in frame_runtime.hpp after IFrameGraphHost, one-line "not a public API" comment; the 2 seat call-sites gain `detail::`) — a test-reachable internal, the honest label; (2) the tooth in tests/scene-render (next to #6402/#6403, the seat→provider seam where spec_apply.hpp is already imported) #6407/#6175: parse the SHIPPED ui_frosted_glass.frame.toml → `detail::build_pass_spec_set` on blur_h1/v1/h2 → THREE arms — (a) EXACT sets (count==3 + every (id,value): h1={0→1,1→0,2→1/512}, v1={0→0,1→1,2→1/512}, NOT a mere inequality that a `spec_3` typo survives), (b) the PROVIDER cache key discriminates H≠V (`spec_set_hash(h1)!=hash(v1)` + `!spec_set_equal`) = two DIFFERENT specialized programs (the frame's whole point), (c) the twin H passes COLLAPSE (`hash(h1)==hash(h2)` + `spec_set_equal(h1,h2)`) = the device-free half of the 31b-4 cache-hit H==H tooth. ⭐ the exact-set arm reads the REAL shipped toml → an authoring drift (a mis-typed dir/step) fails HERE, device-free, not on-device. (3) the -c-i carry-forward: `parse_attrs` + the `@`-symbol path REJECT a zero-length quoted name (`""`/`@""`) with an offset (MLIR-quoting admitted a nameless attr the builder can't make + a fuzz crash path) + a `CRD_ASSERT_MSG(name.size()>0)` in `Context::set_attr` (the SOURCE guard — `Transaction::set_attr` DELEGATES to it [transaction.cpp:293], so BOTH build paths are covered; builder & text grammar now AGREE on what's constructible) + 2 `rejects(…)` cases in the malformed-inputs test #234 (`{"" = 1}` and `{s = @""}`; roundtrip suite 15/15). ⇒ **✅ 31b-3-c DONE** (-c-i frame + round-trip + ceir-core quoted-name/regression fixes; -c-ii record-seat H≠V + empty-name reject). **✅✅ 31b-3 CLOSED 2026-09-06 (31b-3-z sub-band close, GATE-RE-VERIFIED not inherited):** the full 31b DEVICE-FREE surface — 12/12 tests re-run THIS tick on BOTH toolchains (MSVC + WSL gcc -Werror): the 5 ckir kernel gates (#4137 backdrop_fetch / #4138+#4139 tint_noise struct+hash-intent / #4140 blur / #4141 composite-bindless) + the 7 spec/frame gates (#4188 spec_N round-trip / #4189+#6404 registry KIND+no-spec-host / #4285 frame -c-i round-trip / #6405 cache-key / #6406 apply_spec_set / #6407 -c-ii record-seat H≠V). SOURCE CLAIMS confirmed against scene_renderer.cpp: 4 `register_raster_spec` ui ids (backdrop_fetch:2785/tint_noise:2788/blur:2791/composite:2794) + the `ui_variants` retire sweep (:1249-1250) + the spec_set_equal cache lookup (:2053-2059). CENSUS re-verified: `{"copy",kExecTransferCopy}` frame_asset.cpp:1798 = the frame's `backdrop_copy kind="copy"` (31b-2-struck confirmed, NOT a stale strike). ⇒ **31b-3 = the §141 frosted-glass chain is AUTHORED + spec-wired + round-trip-proven DEVICE-FREE** (frame.toml → ceir.frame → back; ONE ui_blur.ckir specializes H≠V; composite bindless-mixes 3 textures; the seat builds distinct spec-sets per pass). SWEEP FINDINGS carried in (fixed this arc, NOT 31b-3 scope-creep): 2 ceir-core print/parse asymmetries (quoted `@sym`/attr-name) + the 26c/26z Allocate-hazard regression. **✅ 31b-4-a DONE 2026-09-06 (panel_fill pre-work, device-free — MSVC #4142 + WSL gcc -Werror both green; 31b sweep 13/13 both toolchains; tidy 3/3):** RULED **opt-B** (advisor-BEFORE — opt-A's `UiPanel` is a PARKED Track-B/ADR-0107 component, so a geometry mask drags I2D into this proof; opt-B touches nothing parked). DELIVERED: the committed `assets/ckir/ui_mask_rect.ckir` (NEW, 15 nodes, hand-authored straight-line — the backdrop_fetch mold) = `cov = step(x0,uv.x)·step(uv.x,x1)·step(y0,uv.y)·step(uv.y,y1)` → Splat→Vec4 (R8 mask.r); rect bounds = 4 SPEC-CONSTS x0@0/y0@1/x1@2/y1@3 defaults 0.25/0.25/0.75/0.75 (NOT baked → 31b-4 arm (a) MOVES the rect via a `[pass.params]` edit + watches composite move; the FIRST Spec-registered ui kernel with real specs on a NON-blur kernel + the FIRST fullscreen ui kernel with 0 texture reads); symmetric [0.25,0.75] so the NDC±Y flip is invisible ([[feedback_clip_space_y_convention_mirrors_every_rtt_sampled_by_uv]]); Step emits inline `(v<edge)?0:1` on both backends (ckir_glsl:282/hlsl:616) so NO `lower_entry` for EMISSION (the device create_program lower-vs-not is a 31b-4-b arm, the tint_noise precedent). Registered `engine://ui/mask_rect` (UiKind::MaskRect + register_raster_spec + ui_asset_path arm). REWIRED the frame: mask pass raster.geometry+ui_panels+panel_fill → **raster.fullscreen + reads=[] + engine://ui/mask_rect + [pass.params] spec_0-3**, DELETED the `ui_panels` draw_list (frame now has ONE draw_list). Structural gate #4142/#4140 WITH TEETH: 4 spec-consts with the EXACT rect defaults (a swapped x0/x1 fails HERE) + 4 Step + 0 Texture/Sampler/TexSample/SampleIndexed/StorageLoad (the discriminator — the ONE ui kernel reading NO texture) + Splat==1 + out Vec4 + the Step ternary EMITTED + NO `texture(` in the GLSL + roundtrip. -c-i gate #4285 updated (draw_lists==1 pin + fullscreen-mask comment) — still green. ⭐ advisor-AFTER: the -c-ii record-seat tooth #6407 EXTENDED with the mask pass (`build_pass_spec_set(mask)` → count==4 + the exact 0.25/0.25/0.75/0.75 — the device-free record-seat proof for the 4th spec-carrying kernel; a `spec_3` omitted in the toml fails HERE, nothing else proved the FRAME authored the rect). ⛔ VERIFIED before authoring: a `reads=[]` fullscreen pass cooks (no fullscreen-needs-reads rule in pass_contract_diag; only the `{"raster.fullscreen",kExecFullscreenRaster}` table row); the fullscreen VS hands a Vec2 uv (ui_backdrop_fetch StageIn precedent). NEXT = **31b-4-b** the DEVICE arms (a)-(h) — advisor-BEFORE's FIRST question = can device tests RUN here (Vk+DX12 present?); if not, the row is "harness authored, device-gated". **⛔ 31b-4 DEVICE ARMS (consolidated from the -1/-3/-b ledgers into ONE list — Vk+DX12 each):** (a) composite reads→heap-layer ORDER [scene=0/effect=1/mask=2] + the mask `.r` channel; (b) blur HAPPENED — at-edge output strictly-between + monotone; (c) transition WIDTH scales with blur radius (radius 1 vs 4, width₄>width₁ — kernel reads its radius); (d) the cache-hit H==H on device (the twin-H device half; -c-ii proved the device-free half); (e) the tint_noise integer hash on GPU + the lower-vs-not decision (create_program's SPIR-V/DXIL path on Shr/BitXor/Cast<F32>-on-U32 → if it mis-lowers, ONE `lower_entry` in ensure_ui_program, not an asset change) — ⛔ RUN (e) BEFORE (h): (h) sets amp=0 to zero the noise term, so a mis-lowered hash SURVIVES (h) silently; (e) is the ONLY arm that sees it; (f) the 2-iteration ping-pong WAR on blur_a (the transient aliaser's FIRST ping-pong-blur consumer — [[feedback_frame_graph_war_needs_resource_lifetime]]); (g) the EXTENT-derived step (a 2nd-resolution arm makes the baked 1/512 literal fail visibly → the derivation lands: a seat per-read-extent arg + a kernel-declared 'derive spec N from read M extent axis A' map); (h) the NUMERIC gate — synthetic hard-EDGE backdrop → far-from-edge==tint(A)/tint(B) ±1 LSB (amp spec-const=0 for this arm) + at-edge monotone + width-scales; (i) ⛔ a 0-READ fullscreen pass records + binds NOTHING (the mask pass is `reads=[]` — the ONLY such pass engine-wide, grep-confirmed; 31b-4-b is the FIRST time the record path hits `reads.size()==0` on a fullscreen pass → the bind-reads branch's THIRD case beside 1-read-single-texture / multi-read-bindless, [[feedback_one_read_fullscreen_binds_single_texture_not_bindless]] — verify no implicit `>=1` assumption). DEFERRED to **31b-z** (the WHOLE-31b close, AFTER 31b-4): the §141 row flip + capability toml:753 limitations + glow/bloom/distortion same-recipe ledger. DEBT (next opgen touch — the ALL-dialects-diff scar): `resource.ceirop.toml` declare-doc "orders allocations without treating declare as a memory access" now approximately-true (SSA orders, walk doesn't) — verified the ONLY drift (import doc still true; `docs/decisions/0124-memory-planner.md` grep-clean of Allocate/hazard-order claims). **31b-4-b-i DONE 2026-09-06** (arms (a) the composite READS the mask + (i) the reads=[] mask pass records clean; Vk+DX12 both green; own TU test_ui_frosted_glass_gpu.cpp, shared arm + per-backend wrappers): the GEOMETRY-FREE device frame (a §141 ui panel needs NO scene geometry) exposed THREE renderer defects in ONE empty-world family — nobody had rendered an authored frame over a 0-instance scene end-to-end. (1) render() short-circuited the WHOLE frame on an empty draw list (scene_renderer.cpp:6799 early-return DELETED — the scene clear + the fullscreen chain now record). (2) the recorder rejected a clear-only geometry pass as UnresolvedProgram (rec.program = the FIRST draw's, absent when empty; frame_runtime.cpp:874 now exempts an empty RESOLVED draw list — a non-empty binding with a null program STILL fails, the REN-38 llvmpipe guard intact; frame-cook 105/105 confirms). (3) ⛔⛔ the command-lowering encoder folds a scope's LoadOp::Clear into its FIRST draw verb, so a 0-draw pass emitted NO vkCmdBeginRendering and left scene_color UNDEFINED (an empty viewport rendered garbage) → NEW **IRasterContext::clear_scope** (Vulkan vkCmdBeginRendering+End with each attachment's authored LoadOp/clear; DX12 OMSetRenderTargets + ClearRTV/ClearDSV; colour float+uint + depth + MRT) issued by CommandEncoder::end_rendering when m_first && wants_clear (no draw consumed the clear); the render_materialize.cpp:504 "count==0 still clears" comment was the FALSE invariant that hid it — struck in place. The gate's `record_ok==1`/`build_ok==1` REQUIREs (empty world RECORDS) + the uniform-backdrop CHECK (corner==centre ⇒ the 0-draw clear landed everywhere) pin all three. Blast radius all green both backends: scene-render 97, frame-cook 105, render-graph 15, gpu-context 6, gpu-context-vulkan 269, gpu-context-dx12 150 + both b-i gates; 9 touched files tidy-clean (LLVM-20). ⛔ SANDBOX SMOKE DEFERRED to CI — a RULING not an omission: the three fixes are INERT on a populated draw list (it drew ⇒ end_rendering's m_first is false; draw_list non-empty ⇒ neither :6799 nor :874 fire), so the sandbox's live path never exercises them; the ONLY path they change is the empty world, which the b-i device gates prove on Vk+DX12 — per the per-slice grant, whole-app is CI's. ⭐ arm (i) as originally written ("0-read fullscreen pass records + binds nothing") UNDER-claimed — the gate proves the STRONGER fact: the reads=[] pass records clean AND the whole authored chain records+builds+executes over an EMPTY world (record_ok==1/build_ok==1). ⭐ NEW arm (j) = the 0-draw-clear (a clear-only geometry pass CLEARS on device; corner==centre uniform-backdrop CHECK) — not in the original (a)-(i) list; it is the clear_scope regression arm. **31b-4-b-ii-1 DONE** (arm (d) cache-hit H==H on device, both backends): render A twice with an IDENTICAL graph/spec-set → a BYTE-IDENTICAL framebuffer (FNV-1a over all 64×64 px) AND an EXACT ensure_ui_program (kind,spec-set) cache count of 6 (backdrop_fetch + blur-H + blur-V [h1≡h2, v1≡v2 dedup ⇒ 2] + tint_noise + mask_rect + composite). ⛔ IDENTITY not category (advisor 2026-09-06): a `vc0>0` check would MISS a mis-keyed cache that fails to dedup h2→h1 (cooks a 7th variant, still passes H==H); pinning `==6` catches it AND prints the real count on mismatch. New non-virtual `SceneRenderer::ui_variant_count()` diag accessor (no layout change → no six-binary sweep; module blast radius sufficed). Rides the SAME shared arm inside the two `CEIR-31b-4-b-i GATE (Vulkan)`/`(DX12)` test cases (name them literally for re-verify). Full crd-scene-render-tests module green (2111 assertions/97 cases); scene_renderer.cpp + test TU tidy-clean. **31b-4-b-ii-2 DONE** (arm (e) tint_noise integer hash on GPU, Vk+DX12 — GPU==eval; 31b-1a-iii proved eval==C++): a DEDICATED probe frame `assets/frame/ui_tint_noise_probe.frame.toml` (`kind="clear"` backdrop → ONE `raster.fullscreen` engine://ui/tint_noise → @output, spec_0..3=0 tint + spec_4=1 amp ⇒ @output = splat(noise(hash((u32)FragCoord.xy))) raw), NOT through the frosted-glass chain (blur/composite/mask would smear the bit pattern). Scalar oracle (ref_hash/_stage1/_stage3 + NEW noise_from_hash_f32) LIFTED test_ckir_asset.cpp→`tests/gpu-shared/ui_tint_noise_oracle.hpp` (inline; the KGraph emitters STAY — eval fixtures; #4138/#4139 re-verified inert post-lift). Gate names: `CEIR-31b-4-b-ii-2 GATE (Vulkan)`/`(DX12)` + the device-free round-trip **#4287** (probe parses/round-trips/authors tint=0/amp=1; 0 draw_lists is valid). ⭐ tint=0 zeroes the UV-dependent backdrop term + FragCoord is window-space top-origin on BOTH backends ⇒ the noise is Y-FLIP-INVARIANT and BACKEND-AGNOSTIC (ONE oracle serves both; both matching it transitively proves cross-backend agreement — the advisor RETRACTED both the per-backend-oracle and the "(e) exposes the flip" claims on this evidence). ⛔ UNORM8 FINDING → [[feedback_unorm8_store_tolerance_gate_with_spec_band_not_exact]]: the f32→UNORM8 store has a 0.6-ULP conversion tolerance (D3D11.3 §3.2.3.1), so 3971/4096 exact (IDENTICAL both backends), every one of the 125 disagreements a noise·255 value just above a .5 boundary the converter rounds DOWN — the hash is bit-exact, only the quantization ties. Gate teeth = within1==4096 (adjacency to the ideal byte) + out_of_spec==0 (`|noise·255−readback|<1.1` = 0.5 half-quantum + 0.6 ULP, the SPEC constant, not a tuned fraction band) + exact≥3072 (sane-converter regression floor) + splat r==g==b; ⛔ NEVER count-exact==N (the estimate ≥4000 failed at 3971). ⛔ (e) lower-vs-not DECIDED: **NO lower_entry** — the emitted shader computes the authored hash bit-exact on both backends (create_program accepted AND the device numerics match within tolerance). ⛔ Y-FLIP RULED — **b-iii-1 DONE 2026-09-06** (`CEIR-31b-4-b-iii-1 GATE (Vulkan/DX12)` green). ~~grep ruling, reads-based: `ui_backdrop_fetch` reads scene_color = GEOMETRY-pass output = shape of the FLIP group (taa/velocity/deferred/rt_composite), so the ui chain's FIRST hop MAY need `!ndc_y_points_down()`.~~ **<- STRUCK (advisor 2026-09-06): the reads-based grouping was WRONG.** The flip group reads *pixel-space* buffers written by compute/MRT passes with a DIFFERENT origin (velocity vectors; gbuffer normals/positions decoded from FragCoord; RT results indexed by pixel) — a mirrored sample changes the VALUE's meaning; the no-flip group (tonemap/moment/hzb/**ui**) reads plain-COLOR images where mirroring only mirrors the picture. RULING: the ui chain cooks `vertex/post_fullscreen.crdv` via the SAME no-flip `cook_stage_named` call as the shipped tonemap (scene_renderer.cpp:1856 vs :2065 — BOTH verified by reading: no viewport negation, no sampler-V flip between cook and create_raster_program), so it INHERITS tonemap's visually-verified no-flip: **NO `!ndc_y_points_down()` needed.** GATE (run_hard_edge_orientation_arm): a geometry hard-edge quad (world +Y ⇒ TOP half via look_at(up=+Y)) read through the ui chain — the 1-hop scene read is upright (`scn>20`, a PREDICTED top-brighter value not merely a sign) AND the 7-hop effect chain agrees in sign (`(eff>0)==(scn>0)`) ⇒ scene==effect==@output orientation AND absolute-upright, BOTH backends. (e) was flip-invariant (tint=0) so it neither exposed nor needed this. **31b-4-b-iii-2 DONE 2026-09-06** (`CEIR-31b-4-b-iii-2 GATE (Vulkan/DX12)` green): arms (h) far==scene + (b) blur monotone, on the geometry hard-edge scene. ⭐ PER-ROW design (NOT a single ref): the quad is NOT flat — a ~5-LSB view-dependent Fresnel gradient (140→146) — so (h) asserts `|effect[y]-scene[y]|<=1` on the clean rows [0,20] ∪ [44,63] (INCLUDING the border tooth rows 0/63) and (b) bounds every row by the scene's LOCAL ±8px [min,max] envelope (the honest anti-ringing tooth on a non-flat input) + monotone across the ramp (rows 24-40) + strictly-between at the edge. ⛔⛔ TWO DEFECTS the arm FOUND + SOLVED (never test-around, per [[feedback_solve_losses_never_document_and_accept]]): (1) BORDER-BLEED — the shipped frame's 6 sampling passes inherited `SamplerAddress::Repeat` (raster_context.hpp:111) though :100 names ClampToEdge the post-process default, so the blur/downsample/composite WRAPPED opposite-edge content across the screen edge (a UI backdrop's VISIBLE edge); fixed with 6 `address = "clamp"` frame lines. (2) DX12 BINDLESS-SAMPLER — `record_bindless`/`record_bindless_storage` (dx12_raster_context.cpp) bound the sampler table to heap-start (s0 WRAP), IGNORING the pass sampler that every non-bindless draw honors (REN-38-B8); so a DX12 bindless fullscreen pass could NOT clamp (Vulkan already honored it); fixed with the active_sampler_slot offset (backward-compat: default slot 0). ⭐ FALSIFIED by the fixture's real shape (why the per-row design exists): the ±16px blur-support ESTIMATE (measured ±8: two σ=R/3 V-passes at 1/32 step on 32 half-res texels + the 2:1 resample deviate from scene only on rows 24-40), the uniform-quad assumption (Fresnel gradient), the far-read rows 4-8/56-60 (border-corrupted; the clean plateaus are rows 12-24 / 40-52). Blast radius: all 9 device gates + 31b-3-c-i round-trip + 7 REN-41 TAA-family gates green; tidy clean. ⛔ COVERAGE (honest scoreboard): the composite bindless-clamp IS gated — the (h) border tooth (rows 0/63) FAILS on the pre-fix heap-start code (r0 was 125 vs 140); the `record_bindless_storage` (TAA resolve) fix is **UNGATED** — no test sets TAA's `address`, so REN-41 passes on the old code too — a TAA clamp gate lands when TAA declares `address` (REN-41 close / 31b-z). **31b-4-b-iii-3 DONE 2026-09-06** (`CEIR-31b-4-b-iii-3 GATE (Vulkan/DX12)` green): arm (c) width∝step — the blur's ramp width SCALES with its step spec-const ⇒ the kernel READS spec_2 (a baked-spacing kernel gives IDENTICAL widths ⇒ fail). `render_frosted_frame` now takes a `step_repl` string; width = rows in the edge neighbourhood **[12,52]** (NOT all 64 — excludes the borders so a resample border-smear can't leak in; a border regression fails the b-iii-2 tooth INDEPENDENTLY) where |effect[y]-scene[y]|>1. MEASURED (identical Vk & DX12, bit-deterministic): step 1/32 → width **16**; step 1/128 (¼ texel/tap, near-no-op) → width **6** = the RESAMPLE FLOOR (the b-iv (g) baseline). Gate: width₃₂ > width₁₂₈+8 (measured +10 blur contribution over the floor) ∧ width₁₂₈≥2 ∧ width₁₂₈<width₃₂. The 4× step ratio (1/32:1/128) is the census's "radius 4 vs 1" on the STEP knob (R=4 is baked in the 9 taps). ⭐ MEASURE-FIRST caught the estimate: I inferred width_narrow~4, MEASURED 6 (the [[feedback_unorm8_store_tolerance_gate_with_spec_band_not_exact]] discipline — a force-fail dump to read it, not arithmetic on a threshold). All 11 frosted device gates + tidy green. **31b-4-b-iv-f DONE** (2026-09-06): the 2-iter ping-pong CHAINS — blur_src→[h1]→a→[v1]→b→[h2]→a→[v2]→b, reusing blur_a/blur_b. Gate: width(2-iter) > width(1-iter)+2, iter-2 DISABLED (h2/v2 `spec_2 = 0.000000000` = 0-step identity) via a new `blur_step_iter2` per-pass patch seam in `render_frosted_frame`. MEASURED (identical Vk & DX12, bit-deterministic): width_2iter **16**, width_1iter **12** (+4; over the b-iii-3 6-row resample floor that is 10 vs 6 blur contribution — the b-iv-g targets). ⛔ CLAIM CORRECTED (advisor): this is a CHAINING gate (iter-2 consumes iter-1, `reads=` honored), NOT an aliaser-WAR gate — the blur_a reuse is WAR-ordered by the DATA DEPENDENCY (h2 reads b←v1, so h2 cannot run before v1's read of a completes), not by the transient aliaser; the [[feedback_frame_graph_war_needs_resource_lifetime]] scar is the NO-data-edge case and is out of this gate's discrimination. All 13 frosted device gates + tidy green. **⭐ 31b-4-b-iv-g-1 DONE** (2026-09-06, `CEIR-31b-4-b-iv-g-1 GATE` Vk+DX12 green): the FALSIFIER — a baked step LITERAL does not travel across resolution. `run_step_travels_arm` renders the hard-edge chain at **128²** (vs every other arm's 64²) three ways: `width_shipped` (shipped step line untouched = baked 1/512), `width_literal` (1/512 patched explicitly), `width_correct` (1/64 = 1 texel/tap on the 64-texel half-res). MEASURED (identical Vk & DX12, bit-deterministic): **6 / 6 / 16**. ⭐ correct==16 = the SAME full-res width as the 64² arm's 1/32 (b-iii-3 width_wide=16) — resolution-INVARIANT in full-res rows (1 texel/tap = the same 4-half-res-texel radius × the same 2× upsample at BOTH resolutions); the baked literal collapses to the 6-row resample floor (== the 64² floor). Gate: `width_correct > width_literal+8` (16>14, the falsifier) ∧ `width_literal >= 2` (floor exists) ∧ **⛔ THE PIN `width_shipped == width_literal`** (6==6 today; byte-identical TOML since patching 0.001953125→itself is a no-op). NO engine work (only target size + step_repl). g-2 hand-off (from g-1; ✅✅ DONE — see the DONE block below) — **31b-4-b-iv-g-2** (the derivation; ⛔ FOUR constraints from the g-1 advisor-AFTER): (1) ⛔ CONFLICT-IS-ERROR not precedence — a pass with BOTH `spec_2 = X` and `derive_spec_2 = {...}` on the same id is a COOK ERROR (extend `SpecConstDuplicateId` to literal-vs-derive), and the four shipped literals are **DELETED not shadowed** (the pin only flips if the blur passes carry the derivation ALONE — a left-behind override keeps `shipped==literal` green ⇒ no red ⇒ derivation unproven); (2) ⛔ the helper's four-identical-needle assumption BREAKS — H derives axis x, V axis y (the 31b-1b per-pass-asymmetric ledger), so the shipped lines become two shapes ×2 in file order h1(x)/v1(y)/h2(x)/v2(y), and `derive_spec_2 = {...}` ≠ 20 chars ⇒ `patch_same_width` can't ⇒ g-2 day-one adds a variable-width `patch_replace` (containers::String erase+insert) in the TU + retargets the four patch calls to the two axis needles (one place, all four step-patching arms fix together); (3) ⛔ the red→green ORDER g-2 MUST follow to SEE the red: (a) frame→derive + retarget helper → build → run g-1 → the PIN goes **RED at 16≠6** (shipped now derives to 1/64) → (b) THEN flip the pin to `width_shipped == width_correct` → green (do (b) before running and the red is never observed ⇒ derivation unproven); (4) ⛔ BLIND SPOT — g-2's gate MUST include a NON-SQUARE render (128² has x==y, so a derivation that ignores `axis` and always reads x passes every square gate = category not identity): e.g. 128×64 where H step derives 1/64 and V step 1/32, and the row-blur width vs an H-edge column-scan width differ by the predicted ratio. Design shape: `derive_spec_2 = {read="blur_src", axis="x", op="inv"}` (less-magic than a `"1/read[..].extent.x"` string); `build_pass_spec_set` resolves it from the seat's per-read extents (the inv_size precedent generalized); the frame's PROOF_RES 1024 comment (:21-28) is struck. **✅✅ 31b-4-b-iv-g-2 DONE 2026-09-06 (CEIR-31b-4-b-iv-g-2 GATE Vk+DX12 + device-free non-square resolver unit + WSL linux-gcc debug/asan + win-asan + tidy 5/5 — full per-file detail in context.md):** ⛔ a PC crash interrupted this tick mid-g-2; the cook-side (`derive_spec_<n>_{read,axis,op}` parse+validate + `test_frame_asset.cpp:420-490`) was already in-tree — verified compiling + 106-case-green, THEN g-2-b completed. `build_pass_spec_set` resolves the derived id from the read's pixel extent via the ONE `image_dim_px` sizing formula (the transient/persistent creation routed through it — no drift; signature grew `desc`+`out_w`/`out_h`, both seats + the -c-ii tooth updated). The 4 blur passes converted literal `spec_2`→`derive_spec_2_{read,axis,op}` (H off X, V off Y). GATES: (a) device-free NON-SQUARE 128×64 resolver unit test (H→1/64, V→1/32; catches axis-blind + resolution-blind); (b) -c-i literal→derive-triple AUTHORING + field-survival via the emit-identity round-trip; (c) g-1 pin FLIPPED `width_shipped==width_correct` (the planted red→green: 6→16); (d) NEW `run_derive_step_arm` NON-SQUARE 128×64 `fb_hash(shipped DERIVE)==fb_hash(H=1/64+V=1/32 literals)` BYTE-IDENTICAL ∧ `!=fb_hash(swapped axes)`, Vk+DX12 both RAN. render_frosted_frame step override → variable-width `patch_replace` (derive block ⇄ `spec_2=X`) + per-axis blur_step_h/v; `nullptr`=DERIVED (all prior arms survive). ⛔ REPO-POLICY LEDGER (advisor-AFTER): `.gitattributes` has NO `*.toml eol=lf` rule (only `* text=auto`), so a Windows checkout with core.autocrlf would rewrite committed `.toml`/`.frame.toml`/`.ckir`/`.ceir` assets CRLF — `read_shipped_asset` now normalizes CRLF→LF test-side (immune), but a repo-wide `*.{toml,frame.toml,ckir,ceir} eol=lf` rule is the ROOT fix (deferred — broad line-ending churn across many existing assets; flag for the user). → 31b-z (§141 flip) → 31z. | build §61 §62 §63; corpus §95 §96 §97; proof §141 §142 | build §61 §62 §63; corpus §95 §96 §97; proof §141 §142 |
| ✅ | CEIR-32 | **CHIR + language prototype** — implements the CEIR-0e design CORRECTED by the CEIR corpus → same program authored visually AND textually, hot reload. **✅ 32-0 BAND-OPEN CENSUS 2026-09-06 (advisor-BEFORE, facts grep-VERIFIED, docs-only no code):** ⭐ RUNNABLE PROOF (§143) = the event handler `on event: query entities / parallel update / await async task / update state` authored TWO ways — CHIR **text** AND a **graph-schema document** (the CR-D007 inspection/authoring schema: nodes + typed pins + edges, layout in a side-table keyed by stable id) — BOTH lowering through the **SAME CHIR→CEIR path** to the **SAME CEIR module** (printer-canonical equality, the anti-drift mold), + **hot reload** via the EXISTING CEIR-10a state-migration core (`collect_state_schema`/`contract_hash` → Reject/Migrate, StateEdge-cell-keyed). ⛔ RULING 1 (advisor): the "visual" half is honest as a **SCHEMA, not an editor** — §180's items (stable ids, typed pins, layout-separated, semantic diff, no visual-only runtime) are properties of the source MODEL, not a renderer; CEIR-32 DEFINES the CR-D007 schema (unblocking CEIR-33/D7E, which RENDERS it — the capability-toml:19 L6 blocker), and the graph form lowers through the SAME path as text (that IS §180 #13). ⛔ RULING 2 (advisor): the **ADR DECIDES, the prototype IMPLEMENTS §143 ONLY** — §181 says "defined" not "implemented", 0e §3 says v1 need not ship §98; CHIR-0 v1 surface = exactly the 5 §143 verbs (event-handler func, ECS query, `parallel for`, `await`, state update) + real diagnostics + source spans; generics/traits/ADTs/pattern-semantics/full-ownership/modules/reflection/stdlib → the deferral ledger. GREP-VERIFIED facts: (a) NO `event` dialect among the 26 → the handler is a `func.func` (+ domain/`async.scope`), zero-new-ops-if-possible (the 31b-0 discipline, confirmed at 32b/32d); (b) CHIR seams are REAL forward refs, NOT prose — `core.match` docs "PATTERN semantics are CHIR's" (structural arms only), `type.hpp` alpha-scoping + SparseTensor-kind = "CHIR headroom", and **`TypeKind::Qualified` + `OwnershipKind::BorrowedView` (type.hpp:64-79) = the CEIR-3f light-borrow target, BUILT** → CHIR surfaces borrows onto an EXISTING encoding; (c) ECS = `register_ecs(ctx)→Systems` (test_ecs.cpp, a registration bridge); (d) hot reload = CEIR-10a (`collect_state_schema`/`contract_hash`/`interface_hash` → Reject/Migrate/reload), NOT RAF-11; (e) ADR pattern = ACCEPTED + advisor-approved under the autonomous cadence (the ADR-0114 mold, the user's commit ratifies); (f) ADR-0109 has the CHIR clause (design-only→32; lowers/erases; structured source model = semantic nodes + stable ids + source spans + layout) but the **crd-chir module placement is UNSITED → the 32a ADR decides it** (host-only, deps crd-ceir). CORPUS-EVIDENCE MAP for the ADR's "corrected by the corpus" (cite the proof, don't re-derive): explicit state = StateEdge cells (audio delay/biquad §142, TAA history) CONFIRMS 0e §5's state-store leg; audio `[dyn,2]` (31z) = runtime dims not generics; 31b spec-consts = compile-time specialization (const-generics?); 18p VART = 0e Q5 (generic→CKIR-variant landmine); `core.match` structural arms = the pattern-match lowering target. STALE 0e markers FIXED in-place (a dated banner): CEIR-29→32 (×15), CEIR-1c (stable ids) + CEIR-3f (ownership qualifiers) both BUILT not "⬜". SLICE LADDER: **32-0 census** (this) → **32a CHIR ADR** (bind the 0e §8 deferred decisions against the corpus + site crd-chir) → **32b** source model + graph-schema (the shared semantic model + the two projections' data forms) → **32c** text parser (`parse.cpp` is the mold; ⛔ NO std containers) → **32d** CHIR→CEIR lowering + the text/graph PARITY gate (both → same CEIR, printer-canonical) → **32e** hot reload w/ state migration (CEIR-10a) → **32z** close. DEFERRAL LEDGER (each w/ a trigger): generics/traits/ADTs/pattern-match-SEMANTICS (core.match is structural)/modules/reflection/serialization-metadata/extension-methods/stdlib-boundary → each its first corpus consumer or the CEIR-29-class future work; full borrow-checker → REJECTED by 0e §5 (fights visual+agent authorability); the visual EDITOR (rendering the schema) → CEIR-33/D7E. **✅ 32a CHIR ADR DONE 2026-09-06 (docs-only, advisor-BEFORE + facts re-verified) — [ADR-0128](../decisions/0128-chir-0-language-binding-decisions.md) ACCEPTED:** binds the CEIR-0e-deferred decisions against the corpus (cite-not-derive). **D1 scope** = CHIR-0 v1 is the §143 five constructs ONLY + diagnostics/spans (table of semantic-requirement→CEIR-target); **D2** = the `parallel`-body STATE-FREE + SELF-CONTAINED rules (both verifier-enforced: `ParallelBodyStateful`; captures NOT seeded, task.toml:19), so `parallel update` (pure) + `update state` (cell write) are SEQUENTIAL statements — AND ⛔ **done-check correction (advisor):** the parallel body is self-contained so §143's `parallel update` over the ECS query result (an outer value — ECS storages are block args, test_ecs build_world) needs the entity-view to REACH the body → a NAMED 32d prerequisite (capture-seeding or an index-keyed self-contained storage read), NOT a free lowering; **⛔ D3 the reload landmine** = `assign_stable_ids` assigns SEQUENTIAL pre-order ids with a MONOTONE watermark (`next=max(existing,watermark)+1`), so a re-lowered module would re-number → CEIR-10a Reject; CHIR binds each STATE CELL a DETERMINISTIC source-derived id (name+scope, not position) so the state schema is edit-stable → contract_hash Migrate. ⛔ **done-check correction (advisor):** `set_stable_id` is DESERIALIZATION-ONLY (the STID loader, context.cpp:365) NOT a lowering-time pin, and the id space is a flat u64 with NO tag/range — so a raw-hash pin would explode the watermark ([[feedback_monotone_id_needs_watermark_not_live_max_scan]] scar); the ADR pins the REQUIREMENT (deterministic-from-source + watermark-safe) and defers the exact pin scheme to 32e, not the convenient set_stable_id call; **D4 ownership** = the 0e §5 COMPOSITE confirmed, each leg citing its corpus consumer (values=Tensor assets, handles=resource.declare/AssetRef, arenas=31b blur_a/b transients, state stores=StateEdge cells §142/TAA, light borrow=BUILT `TypeKind::Qualified`/`BorrowedView`), full checker REJECTED (visual+agent authorability); **D5** = text+graph are two projections of ONE source model, the CR-D007 graph is a SCHEMA lowering through the SAME path (§180 #13), CEIR-32 defines / 33 renders; **D6 module** = new `crd-chir` (engine/chir) host-only one-way → crd-ceir (the ADR-0109 gap filled; the graph schema lives in crd-chir, editor→chir→ceir). Grep re-verified for the ADR: NO `event` op anywhere → handler = `func.func`+time-domain (ADR decides, zero-new-ops); `parallel`=`task.parallel_for` state-free; stable-id SEQUENTIAL (the D3 landmine). NO grammar in the ADR (semantic requirements only; 32c's parser + docs/systems/chir.md own syntax). Supporting: decisions-README row 0128 + the 0e note pointer + ADR-0109 line-28 strike-in-place (`design-only until CEIR-29`→ADR-0128, crd-chir sited). **✅ 32b SOURCE MODEL + CR-D007 GRAPH SCHEMA DONE 2026-09-06 (first CODE slice, advisor-BEFORE, 2 Win + 2 Linux + tidy + 8 guards + ctest):** NEW module `engine/chir` (`crd-chir`) — host-only, deps crd-ceir + core/log/memory/containers/units ONLY (ADR-0128 D6), one-way→crd-ceir. `node.hpp/cpp` = the ONE canonical source model: a `ChirNode` (kind + StableId + SourceLoc + typed pins + attrs + child regions) — Operation-SHAPED but source-language, NOT ceir::Operation (ADR-0128 ruling 1); arena-pooled `Array<ChirNode>` + u32 handles + a string pool, allocator-param; NodeKind = the §143 5 constructs + Program/StateDecl scaffolding. ⛔ stable ids DERIVED (ADR-0128 ruling 3/D3): `derive_ids` = `fnv1a(parent.id ‖ kind ‖ name)` for NAMED (POSITION-INDEPENDENT — reorder siblings → id unchanged, the reload-stable state-schema property) / ordinal for anon; `semantic_hash` folds kind+name+pins+attrs+child-ids+edges IGNORING layout+SourceLoc (§180 #10). `schema.hpp/cpp` = the CR-D007 graph-schema projection: a canonical LF-only text serialization (nodes+pins+edges+layout-side-table) that is a SERIALIZE FIXED-POINT (print(read(x))==x); the reader is a whitespace tokenizer (parse.cpp style), graceful-reject on malformed. Committed `assets/chir/event_handler.chirgraph` (NEW, the §143 program) bootstrapped-via-print from the KEPT `build_event_handler_model` oracle (write STRIPPED). GATE `[chir]` 21 assert/3 cases: anti-drift-through-printer + roundtrip fixed-point + id-anti-drift (re-derive==file) + completeness (7 nodes exact, all kinds, 3 edges) + id determinism (build twice→same) + POSITION-INDEPENDENCE (swap 2 named decls→ids unchanged) + layout separability (move coords→semantic_hash unchanged; delete node→changes). Proves §180 items 1/2/3/4/6/9/10 with ZERO parser/lowering. DoD #7: `docs/systems/chir.md` + README index row. ⛔ done-check facts: MSVC lacks `__builtin_memcpy`→used `std::bit_cast` (the crd::math::float_convert idiom); the § in a test name tripped BOTH the ASCII guard AND ctest by-name invocation (UTF-8 mangling scar)→`sec-180`. ⛔ advisor done-check fixes (2026-09-06): (1) the ANONYMOUS-node ordinal keyed on the GLOBAL construction index → re-keyed on the PARENT'S children order (the SEMANTIC sibling order, ADR-0128 ruling 3 position-independence); §143 is fully NAMED so the anon path was untested → added the anon-ordinal case (two anonymous `Await` siblings get distinct + deterministic ids). (2) `read_schema` accepted a graph with a wrong-direction edge or an orphaned layout row → now VALIDATES (every layout id resolves to a node; every edge wires an OUT pin→IN pin) + graceful-rejects (the In→In / orphan-layout negative case); the two validation loops use the sanctioned `std::ranges::any_of`/`all_of` idiom (readability-use-anyofallof). Re-gated: 2 Win (win-debug 5/5 chir cases + all guards; win-asan 27 assert/5) + 2 Linux (gcc-debug+asan 27/5) + tidy clean. **✅ 32c CHIR TEXT PROJECTION DONE 2026-09-06 (advisor-BEFORE + done-check; 2 Win + 2 Linux + tidy + guards):** NEW `engine/chir/{include/crd/chir/text.hpp, src/text.cpp}` — `print_chir` (SourceModel→CHIR text) + `parse_chir` (text→SourceModel, `ChirParseResult{ok,err_off,err_line,err_col,msg}`), the recursive-descent `fail`-latches-first-error mold from ceir/parse.cpp. SYNTAX (docs/systems/chir.md): one uniform node form `KIND [name] [attrs] (dir name: type [= node.pin]) { children }`; ⛔ IDENTs allow `.` (dotted attr value bare, NO quoting machinery), NODE NAMES dot-free (a PINREF splits at its last dot); a name spelling a KIND keyword begins the next sibling (`await await` = two anon awaits); pre-order emit/parse reproduces the oracle node indices → committed ids. ⛔ THE ANCHOR is SEMANTIC, not byte-print_schema (advisor corrected: print_schema carries loc+layout; the oracle has loc0+layout, a text parse has real spans+no layout → strict byte-equality is impossible): 4 teeth — print_chir(oracle)==.chir bytes (anti-drift), print_chir(parse_chir(t))==t (text FIXED-POINT), semantic_hash(parse)==semantic_hash(oracle), **semantic_hash(parse_chir(.chir))==semantic_hash(read_schema(.chirgraph))** (the two committed projections AGREE — the literal 32d precondition). ⛔ MODEL CORRECTION surfaced by 32c (a 32b bug): `semantic_hash` folded edges in INSERTION order but edges are a SET — a consumer-order text parse reordered them → parity would fail; `SourceModel::add_edge` now inserts CANONICALLY by the (to_node,to_pin) single-writer consumer key (linear, no std), so both projections' edge order + semantic_hash match regardless of authoring order; `read_schema` gained a single-writer REJECT (two edges into one in-pin) + negative case; the committed `.chirgraph` edge section REGENERATED canonical via the kept oracle. Spans SURVIVE (node loc = text line:col + file_id, self-checked vs the asset); malformed text = graceful reject w/ EXACT line:col (unknown-kind @2:3, unresolved ref, out-pin-bound, non-program-root). Committed `assets/chir/event_handler.chir` (NEW, bootstrap-via-print→verify→STRIP, oracle KEPT). GATE `[chir]` 51/9. Files: text.hpp/cpp (NEW), node.cpp (add_edge canonical), schema.cpp (single-writer reject), test_source_model.cpp (+bootstrap[stripped]+5 cases+single-writer neg), assets/chir/event_handler.chir (NEW) + .chirgraph (regen), docs/systems/chir.md (the syntax + status). NO opgen. 1 MEMORY scar (`scars_ceir_ir.md` — a set-valued field folded into a hash is authoring-order-sensitive; canonicalize the STORAGE at insert, never patch the hash). ⛔ advisor done-check fixes: (1) `resolve_edges` was first-match-wins → a duplicate source name SILENTLY mis-wired; now scans ALL nodes + REJECTS ambiguity (+ negative case + the "uniquely-named sources" restriction stated in chir.md); (2) the anonymous-node TEXT path (`await await` = two anon awaits) was documented but untested + a mislabeled reject-case comment → added the anon-through-text case (two anon awaits, distinct ids, fixed-point) + fixed the comment; (3) struck the superseded "NO text parser (32c)" clauses in CMakeLists + node.hpp in place. **✅ 32d CHIR→CEIR LOWERING + TEXT/GRAPH PARITY DONE 2026-09-06 (advisor-BEFORE ×2 + done-check; 2 Win + 2 Linux + tidy + guards):** NEW `engine/chir/{include/crd/chir/lower.hpp, src/lower.cpp}` — `lower_chir(SourceModel, ceir::Context&)→ceir::Module*`, a DETERMINISTIC one-way structural walk (ADR-0128 D1). D1 map (ZERO new ops, built via ctx.create_operation / func::create_func — the corpus construction idiom, NOT a rendering KGraph builder): EventHandler→func.func(params=the query view)+a `domain` attr; Query→the view = func block-args ("existing registration bridge"); ParallelFor→task.parallel_for(0,1,1){^(%iv):core.yield <view>}; Await→async.launch{}→tok / async.await(tok); StateUpdate→core.state(init,next)→cur. ⛔ CENSUS CORRECTIONS advisor surfaced BEFORE any code: (a) `register_ecs` is a TEST-ONLY mock (test_ecs.cpp inline-registers a mock `ecs` dialect of SYSTEM ops) — there is NO engine ecs.query op/bridge; the ADR's "existing registration bridge" = the pattern (storages as block-args), so Query lowers to func params, NO op, NO mock dialect in production. (b) THE D2 READ PATH: the parallel body references the query-VIEW func param (an OUTER value) directly — proven STRUCTURE-VERIFIER-CLEAN because `task.parallel_for` is NOT IsolatedFromAbove (only func.func is), so the param DOMINATES the body (no CaptureThroughIsolation/UseBeforeDef); self-containment ("captures=UndefinedValue at exec", task.toml:19) is an EXECUTION caveat (`check_parallel_region` is an exec pre-flight checking arity+yield+state-freedom, never operand ORIGIN — read the code, the advisor's decisive question), so seeding is the ADR-named 32e/runtime refinement — 32d lands the structurally-correct read that VERIFIES + round-trips + parity-gates, NO capture-seeding, NO mock. GATE `[chir]` 87/12: PARITY (parse_chir(.chir)→A, read_schema(.chirgraph)→B, lower both [separate Contexts — same ctx clashes on the func symbol], print(A)==print(B) BYTE-EXACT) + find_structure_error==None + CEIR text round-trip + D1 op-presence (func.func/domain/task.parallel_for/async.launch/async.await/core.state, NO event.) + ⭐ D2 read-path IDENTITY (the parallel yield operand IS func.arg(0), num_args==2 the Position.Velocity view — gate=identity not category). Files: lower.hpp/cpp (NEW), test_chir_lower.cpp (NEW), tests/chir/CMakeLists.txt (+the TU +crd-ceir link), docs/systems/chir.md (the lowering section). ⛔ advisor done-check fixes (BOTH the [gate=identity-not-category] failure): (1) `lower_chir` first FABRICATED inputs (konst 0/1), ignoring `m.edges()` → the parity gate passed even with EVERY wire lost (oracle-minus-edges lowered byte-identical) → rewrote to THREAD the dataflow (record each out-pin's SSA Value, resolve each in-pin THROUGH the edges: pf.entities←q.entities=the view, su.%init←st.cell=%current), const-fallback only unconnected pins, + a NEGATIVE test (oracle-minus-edges lowers DIFFERENTLY — the tooth); (2) the StateDecl was DROPPED → it now emits `core.state` carrying a `chir_decl` source-name attr (the D3 stable-id seam 32e consumes), so the cell has source identity; (3) non-blocking: parallel bounds typed `!index` to match `%iv` (were i64). Documented mismatches (chir.md, not hidden): task.parallel_for has NO CEIR result so pf.updated is unbacked; the state %next is a placeholder (the D3 feedback wiring is 32e). GATE re-run `[chir]` 90/13 (2 Win + 2 Linux + tidy + guards). 1 MEMORY scar (`scars_ceir_ir.md` — a lowering that fabricates its inputs passes a parity gate trivially; parity is necessary not sufficient; the falsifier is source-minus-edges != source). ****✅ 32e HOT RELOAD + STATE MIGRATION DONE 2026-09-06 (advisor-BEFORE ×3 + done-check; 2 Win + 2 Linux + tidy + guards):** NEW lowering-time Context APIs `pin_stable_id`/`reserve_stable_id_floor` (DISTINCT from deserialization-only set_stable_id/set_stable_id_watermark, ADR-0128 D3 constraint a); each `state` cell's CEIR id = 1+(chir_node_id % kChirStateIdReserve=1<<24) — source-derived (fnv scope‖name via the CHIR StableId) + position-independent + watermark-FLOORED so every sequential/reload-invisible id sits ABOVE the reserved band (constraint b: no raw-hash watermark jump); COLLAPSED the 32d two-cell placeholder → ONE core.state per StateDecl (the `update state` verb folds into the cell's %next), so the reload schema is one cell per DECLARATION (D1/D3). 6 [chir] reload cases prove the CEIR-10a decision table on the §143 program built with edits: body-edit + decl-reorder → CompatibleReuse; add-cell (core.state has ZERO effects → contract stays equal) + rename → Migrate, existing cell keeps its id even when a new cell is declared BEFORE it; signature (new query component → new func param) → Reject; text+graph pin the SAME cell ids; band-membership falsifier (pinned cell ∈ [1,reserve], func.func > reserve). SCAR: a NON-ASCII (em-dash) TEST NAME passes a tag-filtered run but FAILS ctest-by-name — Win codepage mangles the name→filter round-trip → "No tests ran" → fail ([ascii-only-test-names], via ctest). **✅ 32z BAND CLOSE DONE 2026-09-06 — CEIR-32 (CHIR + language prototype) CLOSED** (source model+graph schema 32b, text projection 32c, CHIR→CEIR lowering+parity 32d, hot reload+state migration 32e; session docs/sessions/2026-09-06-ceir-32z-band-close.md). NEXT = CEIR-33 (D7E: render the CR-D007 graph schema). | §4 §98; proof §143; DoD §180 §181 |
| ✅ | CEIR-33 | CR-D007 universal program editor / D7E. ⛔ **UN-PARKED 2026-09-11 (user direction b) — driven in the CEIR loop.** ✅ **CLOSED on C2 2026-09-11 (user-ruled):** CEIR-33's deliverable = the 33a domain contracts + the L6 bar (§2: 4 DONE / 1 PARTIAL / 2 GAP-until-consumed); the flagship widgets are re-scoped to **I2D-9** (ride ADR-0107 there) — D7E's UI half is now an I2D-band obligation, not a CEIR-loop item. CEIR-L7 authoring ceiling tracks I2D-9. **✅ 33-0 ORIENTATION CENSUS DONE 2026-09-11** (`docs/design/ceir-33-d7e-orientation-census.md`, advisor-reconciled). VERDICT: D7E = **TWO HALVES.** (33a) the **domain CONTRACTS** the editors satisfy — substrate-side, DRIVABLE, largely DONE: per **ADR-0107 §D8** *"D7E therefore = the domain contracts those widgets satisfy, and it defines the L6 bar"* — the user's "D7E RIDES the finished substrate" model maps HERE. Inventory (authoring + round-trip/cook gate + hot-reload): frame-graph `.frame.toml` ×23 (`validate_ceir_frame`, test_frame_ceir) **DONE** · material `.crdm` ×6 **DONE** · CKIR `.ckir` ×42 (35b reject-hardened) **DONE** · CEIR/CHIR graph `.chirgraph`/`.ceir` ×17 (CEIR-32b schema, printer-canonical) **DONE** · inspectors **PARTIAL** (hot-reload=CEIR-10a done; rest=runtime introspection, not asset contracts) · geometry-graph editor + capture/regression/profiling contracts **GAP-until-consumed** (D8 lists them as I2D-9 widget domains → defining a `ceir.geom`/capture contract asset with NO consumer = ⛔ speculative invention). (33b) the **WIDGETS themselves = the I2D programme's I2D-9**, NOT drivable: ADR-0107 is **"Proposed"** (the I2D-0 gate, pending user review) + I2D-1…4 (Canvas→Text→UiWorld→CR-D007) have **ZERO code** in the tree; nobody authorized building a UI framework autonomously. ⚠ **The §174-manifest L6 residual and 33b are ONE blocker** — `gpu-platform-capabilities.toml`: *"L6 requires a CR-D007 inspection/authoring schema (band D7E, unstarted)."* 🔴 **DECISION C SURFACED (user's call, NOT picked):** **C1** accept ADR-0107 → I2D band → D7E closes at I2D-9; **C2** rule CEIR-33's deliverable = the domain contracts + L6 bar, re-scope widgets to I2D-9 (**lean C2**, D8's own text). C rides the **already-pending ADR-0107 review** — not a new ask. · reuse: the I2D-9 widget plan | §163 §164 §165 §145 |
| ✅ | CEIR-34 | Legacy deletion → **one execution-program architecture**; every 0h row executed. **✅ CLOSED 2026-09-11 (re-verification band; E4+R2 driven to completion, not parked) — 34-0 census DONE** (`docs/design/ceir-34-legacy-deletion-census.md`, GATE-re-verifies). VERDICT: every 0h row is **DONE/SATISFIED** (F1 frame_runtime=thin cook→CEIR frontend · F2/F3 @ 15f · E1/E2/E5 @ 16d §128 · E3a SATISFIED=authored `forward_csm` graph + sanctioned camera-fit host resolver · E3b @ 15 §88 [verify] · R1 @ 16z · M1 @ 0f · §7 manifest clean [verify] · §8-1 resolved) **EXCEPT two USER DECISIONS**, both leaning PARK-with-I2D/RAH: **E4** (`register_default_programs`/`init_programs` hand-list, `scene_renderer.cpp:2747` — its RAH-7 replacement was COLLAPSED at re-baseline, no landing slice) + **R2** (`draw_overlay` device verb, `command_lowering.hpp:311/315` — ImGui composites through it; ADR-0107 §36 keeps ImGui until I2D). ⇒ CEIR-34 is a RE-VERIFICATION band: the user RULED 2026-09-11 to PULL E4+R2 into the loop as REAL deletions (no park): **E4 DONE** (imperative `register_default_programs` hand-list → the `assets/scene_programs.manifest` + `register_from_manifest` loader; adding a program = a manifest edit, zero C++) + **R2 DONE** (the `draw_overlay`/`draw_overlay_range`/`record_overlay` device verb RETIRED on both backends — the overlay rides generic `draw_storage`/`draw_storage_depth_load`; a driven-out PRE-EXISTING DX12 `first_vertex`→`SV_VertexID` defect [StartVertexLocation not reaching SV_VertexID on the tested NVIDIA adapter] fixed via a backend-internal identity index buffer + a both-backend offset-contract gate; 4 configs + LLVM-20 tidy — log: `docs/sessions/2026-09-11-ceir-34-r2-overlay-verb-retirement.md`), then 34z CLOSED 2026-09-11: 34a re-verified the DONE-row parity gates GREEN on one-Win+one-Linux/both backends (win-debug ceir-1x 79/79 · raf7 7/7 Vk+DX12 · RAF-8 6/6; linux-gcc-debug gcc-Werror clean, ceir-1x 265/265 · raf7 6/6 · RAF-8 6/6) + E3b §88 corpus committed + §7 manifest clean — no latent CEIR-replay drop. Session: `docs/sessions/2026-09-11-ceir-34z-band-close.md`. **NEXT = whole-engine PQP (CEIR-35) → CEIR-33.** | §175-31 §178 |
| ✅ | CEIR-35 | Production qualification → cross-backend/ASan/fuzz/deterministic-cook/hot-reload-stress/large-graph/perf boards/docs; the DoD lists answered item by item. **35-0 ORIENTATION CENSUS DONE 2026-09-11** ✅ **35z CLOSED 2026-09-11** — every DoD row Q1–Q10 GREEN; **35d-Q7 = 3 CEIR-system perf boards shipped** (compile 0.05% of glslang · executor GPU-identical to hand-rolled +0.1µs record · reuse 87×; win-release + RTX 4070 Ti SUPER; `docs/bench/2026-09-11-ceir35-{compile-time-decomposition,executor-overhead,plan-reuse-amortization}.md`); **Decision A RESOLVED** (close on the DoD table; feature-quality PSNR/SSIM → feature bands, not a CEIR gap) + **Decision C = C2**. Close log: `docs/sessions/2026-09-11-ceir-35z-band-close.md`. **The CEIR detour spine (bands 1→35) is COMPLETE.** (`docs/design/ceir-35-production-qualification-census.md`). ⛔ SCOPE (confirm w/ user): qualifies the CEIR **spine** (IR/compiler/execution/hot-reload/cook, cross-backend, perf) — NOT the whole-engine PQP (feature quality → Track A/MAT/VGE/ARG/I2D-PQ post-CEIR; whole-repo sweep = CI's job). VERDICT: mostly a CONSOLIDATION/re-verify band (Q1 round-trip · Q3 det-cook · Q4 cross-backend · Q8 ASan all ESTABLISHED per-band); genuine NEW work = **35b fuzz harness+corpus** · **35c hot-reload soak/stress** · **35d large-graph + CEIR-system perf boards** · 35f docs. ⛔ 35a (cross-backend/hot-reload consolidation matrix, Q4/Q5) gated on 34z; **35b (fuzz) / 35d (large-graph) are INDEPENDENT of E4/R2**. **35b IN PROGRESS 2026-09-11** (deterministic mutation-robustness gate over the 4 parsers `ceir::parse`/`deserialize`/`ckir_read`/`parse_chir`, extending the CEIR-1h/18q hand-picked malformed corpuses; seeds built in-test, fresh Context/mutant, 4 invariants [well-formed · accepted-mutant fixed-point · determinism · non-vacuity], reuses test_fuzz.cpp's Rng, ceir→kir→chir one module/tick). ✅ **ceir arm (text `parse` + binary `deserialize`) 4/4 CONFIGS GREEN — win-debug + win-asan + linux-gcc-debug + linux-gcc-asan** (all ASan-clean where applicable; scoped WSL build --target + ctest -R on native ext4) — in `tests/ceir/test_fuzz.cpp` (the §167 fuzz home, not a new file); no parser defect found; binary random capped 64 (count-field flip → ~1M-elem alloc; cap stress-tested by the count-corruption section). ✅ **+ tidy CLEAN (LLVM-20)** — caught a nested-ternary + `kMutants`→`mutant_count` (local-constexpr case scar); rebuilt+retested win-debug 2/2 IDENTICAL (behavior-preserving). **ceir arm DONE.** Remaining 35b: kir (`ckir_read`, CkirReadResult mirrors ParseResult; seed=ckir_write(build_scale)) + chir (`parse_chir`) arms — same pattern, one per tick. ⚠ ENV: VS auto-updated 14.50.35717→14.51.36231 — win-debug-scalar + win-asan wiped+reconfigured; run-ctest.bat line 5 hardcode is HARMLESS (vcvars provides the current ASan DLL). Slices 35-0→35a(consolidate)→35b(fuzz)→35c(soak)→35d(large-graph+perf)→35f(docs)→35z. **⭐ 35b IN PROGRESS — the mutation fuzz EARNED ITS KEEP:** the ceir arm (`parse`+`deserialize`, `tests/ceir/test_fuzz.cpp`) is 4/4 configs + tidy clean (no parser defect); the **kir arm (`ckir_read`, `tests/kir/test_ckir_asset.cpp`) FOUND + FIXED a real SIGSEGV** — a bare `[[node]]` (a truncated file) left `KNode.op` INDETERMINATE (no default member init) → `ckir_write`'s `kKOpNames[op]` indexed OOB. FIX (fix-forward, `ckir_asset.hpp`): value-init `KNode n{}` + REQUIRE `op`/`kind`/`stage` on read + a post-parse INDEX-RANGE validation pass (every node/stmt/ext/struct ref bounds-checked against final sizes, forward-refs permitted) → a corrupt/hostile asset is REPORTED (ok=false), never accepted-then-crashed (a §PR-7 PQP-2 gap closed); 2 hand-written corpus regressions pin the exact reasons. Fuzz GREEN on win-debug; re-gate all 4 configs GREEN (win-debug full suite · win-asan 142/142 ASan-clean · linux-gcc-debug 218/218 · linux-gcc-asan 153/153 CPU-blast-radius ASan-clean) + LLVM-20 tidy clean; the lone linux-asan SEGV is a PRE-EXISTING llvmpipe+ASan device-dispatch artifact (`<unknown module>`/JIT worker thread; linux-debug + win-asan-real-GPU both clean; orthogonal to a text-parser change) — surfaced as a separate PQP-3 item, NOT a 35b blocker. **chir arm (`parse_chir`, test_chir_lower.cpp): 4 configs GREEN (win-debug · win-asan 21/21 · linux-gcc-debug 23/23 · linux-gcc-asan 23/23, all ASan-clean) + tidy; NO defect** (parse_chir already uses the fail-latches-first-error / graceful-reject mold; the CANONICAL-IDEMPOTENCE tooth = print_chir(parse_chir(m)) re-parses+re-prints byte-identical). ✅✅✅ **CEIR-35b COMPLETE — all 3 parser/loader entry points (parse · deserialize · ckir_read · parse_chir) systematically mutation-fuzzed × 4 configs + tidy; the §167 fuzz now guards every CEIR-spine loader; the kir SIGSEGV fix is the headline (a real §PR-7 PQP-2 gap closed).** ✅ **35a Q1 ASSET-LOAD SWEEP** (`[kir][asset][ceir35a]`): a CMake-glob-generated manifest (`gen/ckir_asset_list.hpp`, CONFIGURE_DEPENDS, NO new link — ADR-0098 smoke intact) drives a `ckir_read` over EVERY committed `assets/ckir/*.ckir` (43 files) — GREEN on all 4 configs (win-debug · win-asan · linux-gcc-debug · linux-gcc-asan, ASan-clean) + engine-consumer compile-check (scene_renderer.cpp) + tidy ⇒ the 35b over-rejection claim is HONESTLY closed (the ~20 hand-picked gates covered only 1/3; the fix over-rejects none of the 43). 35a cont.: name the pre-existing llvmpipe+ASan device-dispatch SEGV (PQP-3) + the Q1/Q3/Q4 cross-config re-verify table. ✅✅ **35c HOT-RELOAD SOAK (Q5, §172)** — `[ceir][reload][ceir35c]` in test_hot_reload.cpp: 272 reload cycles across the FULL decision table (HotSwap/NoChange/NeedsMigration/ContractChange) prove the reload LIFECYCLE stays generation-safe (handle staleness + drain-to-size-1 every cycle) + leak-free (ASan-clean over 272 generations on win-asan + linux-gcc-asan; coarse num_chunks growth bound) under rapid churn; GREEN 4 configs + tidy. The pre-existing linux-asan+llvmpipe device-dispatch SEGV was RE-CHARACTERIZED as a NON-DETERMINISTIC lavapipe-ORC-JIT×ASan flake (214/214 on re-run) — documented `ceir-35-production-qualification-census.md` §6 as a PQP-3 known-flake (not our code, not CEIR-35b). ✅✅ **35d Q6 LARGE-GRAPH scalability CORRECTNESS** — `[ceir][cook][ceir35d]` in `tests/ceir-cook/test_large_graph.cpp`: a synthetic ~8k-op arith graph (4096 const + 4096 addi chain) COOKS (compiler: lower/verify/optimize/serialize at scale) + LOADS + EXECUTES (reference interpreter at scale) to the EXACT sum 8390656; GREEN 4 configs (win-debug/win-asan/linux-gcc-debug/linux-gcc-asan, ASan-clean) + tidy — no O(N²)/fixed-buffer/recursion blowup an order of magnitude past the authored graphs. **35d Q7 perf boards (compile-time · plan-cache · exec-overhead-vs-native) = HELD for the user's bench-design steer** (all-peers/no-cherry-pick/docs-bench convention). ✅ **35a-matrix-finalize DONE 2026-09-11** (34z unblocked it — census §7 spine consolidation Q1/Q3/Q4/Q8). ✅ **35e whole-engine BREADTH (B) DONE 2026-09-11** (census §8 — every CEIR-riding family re-verifies GREEN both backends + config matrix). ✅ **35f docs DONE 2026-09-11** — `docs/systems/ceir.md` written+indexed + ✅ **§174 manifest schema=2 two-axis migration LANDED** (`level`→`raf_level`; `ceir_level`/`providers`/`determinism_tier` on all 39 rows; rule-driven per ceir-0g §4 + advisor-reconciled [bands 1→35 fired the step-5 raise, flat 0 would under-claim]; ceiling CEIR-L6; anti-drift-verified 39×4 + diff-clean + providers-invariant; census §10). ⭐ advisor done-check caught+fixed 2 honesty bugs pre-stick: L6 basis re-cited F1→CEIR-16d §128 E1/E2/E5 (scene-raster), + FOUR STALE pre-CEIR-19 RAF rows corrected (CEIR-19b `rt_shadow` + 19c §134 wavefront, Vk+DX12(DXR)+lavapipe ⇒ hybrid_rt→L6, raytrace.pipeline→L5, full_rt_lit→L3 [wavefront control-flow, not a lit renderer]; raytrace_inline stays L2 = SBT not inline) + providers invariant enforced. ✅ **CEIR-native rows 9 of 9 major families ADDED 2026-09-11** (manifest 48 rows): tensor_ml_mlp(23c Q8-layer L5; 24 pending)·ceir_autodiff(25 L5)·ceir_optimizer(26 L2 device-free)·ceir_transform_rewrite(27 L5 BitExact)·ceir_autotune(28 L5)·cuda_graphs_provider(29 L3 submit-path)·ceir_dist_sharding(30 L5)·ceir_audio_dsp(31 L3)·chir_language(32 L2), all raf_level="n/a", determinism_tier=BitExact where a band gate proves it (⛔ no cross-band proof borrowing — the optimizer's device exercise via 25c credited to CEIR-25, not the optimizer row). + provider-vocab bug fixed (providers=ProviderClass host/gpu/npu, NOT backend — 20 render rows ["vulkan","d3d12"]→["gpu"]). ✅ **matrix generator BUILT 2026-09-11** (ceir-0g §4 step-4): `tools/ceir_capability_matrix/gen_matrix.py`→`docs/generated/gpu-platform-capability-matrix.md` + 3-tier CHECKS (HARD load-bearing/vocab/range/providers-invariant/stale-path); committed matrix 48 features 0 HARD/0 review; gated `crd-ceir-capability-matrix-{drift,validator}` ctests (win-debug PASS) + 12 unit tests (advisor caught a check-design bug: don't force the generator's field-model onto the manifest — tier by cost). ✅ **finer rows DONE 2026-09-11**: ceir_sparse (23e CSR SpMV L5 Vk+DX12) + ceir_ml (24 full-MLP+SDPA L5 Vk+DX12==oracle tol). **Manifest COMPLETE at 50 rows** (11 CEIR-native families + render/UI seed; 0 HARD/0 review; generator+drift ctests green). **The ENTIRE autonomous CEIR-35/§174 track is DONE — no ungated CEIR work remains.** 🔴 GATED (user): Decision A (whole-engine QUALITY, census §9) + Decision C (CEIR-33 closure) + Q7 perf-board bench steer. **NEXT: the autonomous-drivable CEIR-35/§174 work is DONE; 35z awaits the user-gated A/C/Q7 (the ungated §174 residuals can fill anytime).** | §179 §180 §181 §182; tests §167–§172 |

---

## Paused (explicitly parked, NOT dropped — resumes per row after CEIR-16z unless noted)

| Was | State at pause (2026-08-07) | Resumes as |
|---|---|---|
| Track A: RPL→VGE bands | ⬜ (nothing beyond RAH started) | CEIR-18/19/20 proof assets + post-CEIR bands |
| Track B: I2D/SPR (+ ADR-0107 review) | Moved to the renderer/UI programme | Future scope, gates and status: [renderer/UI master plan](../ROADMAP.md#master-table). CEIR-31 is the effect proof; C2 routes widgets to I2D-9. |
| Track C: CGP selector + HGP/MLR | CGP-0 ◧ (CUDA backend ✅ landed) | selector when a CEIR provider needs it; HGP→CEIR-22; MLR→CEIR-24 |
| Track D: MED codecs, D7E, PQP, EYL | MED-1 ◧ (GIF+LZW landed; external-oracle corpus follow-up owed) | codecs stay native capabilities (§177); workflows via CEIR-31; D7E→CEIR-33 |
| Main roadmap (hesap v18, eylem v1c+) | unchanged (pre-CEIR posture) | after the detour as before; eylem orchestration lands on `ceir.physics` (§60) when v1c+ resumes |
