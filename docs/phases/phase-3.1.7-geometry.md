# Phase 3.1.7 — `crd-geometry`: computational-geometry substrate

**Status:** 🚧 **active** — 7th of 11 sub-modules COMPLETE (v6 `-polygon`
CLOSED 2026-05-16) / 51 of 49 renewed-scope slices shipped (104% — v6
expanded 4→6 slices per v6a substrate separation; nothing cut).
**v0 + v1 (`-bvh`) + v1h+v1i+v1j + v2 (`-convex`) + v3 (Shewchuk + 2D hull
+ Quickhull + simplify) + v4 (`-mesh`) + v5 (`-spatial`)** all closed.
**v6 `-polygon` IN PROGRESS — 3 of 6 slices shipped 2026-05-16**: new
`engine/geometry-polygon/` module (7th `crd-geometry-*` sub-module).
v6a substrate ✅ (`Polygon2<T>` / `PolygonView2<T>` / `Ring2<T>` non-owning
+ owning types with CCW-outer / CW-holes winding convention; prefix-sum
ring offsets; layout-pinned via static_assert; signed_area / centroid /
aabb / is_ccw / is_simple O(n²) / ensure_orientation / point_in_ring /
point_in_polygon Hormann-Agathos 2001 crossing-number + Shewchuk orient2d
adverse; typed `Vec2<Length32>` strip-compute-retag wrappers per ADR-0078
§5 D34; 39 cases / 151 assertions / 4-config DoD PASS). v6b ear-clipping
w/ holes ✅ (classical Meisters 1975 O(n²) + Eberly 1999 cut-and-join
hole bridging; lex-tuple deterministic ear pick; Shewchuk orient2d
adverse; 14 cases / 50 assertions / 4-config DoD PASS). v6c CDT ✅
(Bowyer-Watson incremental Delaunay with Shewchuk `incircle` adaptive +
lex-sorted insertion order + jump-walk point location + Domiter-Zalik
2008-style carve-and-retriangulate constraint recovery for arbitrary
multi-flip including non-convex-quad chains + Lawson 1977 post-flip
Delaunay restoration + polygon-with-holes variant with even-odd centroid
in/out filter; 14 cases / 410 assertions / 4-config DoD PASS).
**v6 `-polygon` cluster CLOSED 2026-05-16** — all 6 slices shipped
(v6a substrate + v6b ear-clipping with holes + v6c CDT + v6d Vatti
polygon Boolean + v6e Bentley-Ottmann + v6-close: ADR-0076 §21 amendment
+ `docs/systems/geometry-polygon.md` + 18-config full sweep PASS).
Cluster totals: 6 slices · ~4350 LOC engine + ~2750 LOC tests ·
92 cases / 695 assertions · 20 locked substrate decisions.
**🎯 v7 `-mesh-processing` NEXT** (Garland-Heckbert QEM + Loop subdivision
+ isotropic remesh + Liepa hole-fill + manifoldness repair + Taubin
smoothing). Per-slice DoD = 4 configs (win-debug + win-asan + win-shipping
+ win-tidy); cluster close = full 18-config sweep (11 Win + 7 Linux WSL).

(Historical pre-ship status preserved for archive — research dossier
closed 2026-05-11 + addendum §13 added 2026-05-11; ADR-0076 Accepted +
Amended 2026-05-11 §12 + §13; slice list locked at **30 slices**, expanded
to **49 slices** per the renewed-scope review 2026-05-14, plus the v4d
SIMD raycast slice [originally planned v4g] and the v4-validate slice;
**first-light kickoff IMMEDIATELY after Phase 3.1 v1b cluster close
(sweep-PASS gates the pivot)** — not after Phase 3.1.6 `crd-hesap`
as originally locked. Sequence pivot per ADR-0076 §12: full phase
executes BEFORE Phase 3.1 v1c so eylem v1c+v1d+v1d-mesh and sdf v2
consume geometry from day 1, dissolving the deferred-refactor debt
the original sequence required. ADR-0076 §13 (2026-05-11): the
pre-existing `crd::math::geometry` (404 LOC — `Ray`/`Plane`/`Sphere`/
`AABB`/`Triangle`/`Frustum` + ~16 closest-point/intersect helpers,
~9 consumers) is **moved-and-deleted** into `crd-geometry-primitives`
v0a — `crd-math` keeps only Vec/Mat/Quat/Transform/SIMD/deterministic;
and a new **v0f cutting-edge / branchless / SIMD intersection corpus**
sub-slice lands after v0e — watertight ray-tri (Woop 2013),
Baldwin-Weber 2016, branchless NaN-safe slab ray-AABB, Williams/Ize
robust-traversal ray precompute, Plücker edge classification, Ericson
Voronoi-region closest-point-on-triangle, + `Vec4f`/`Vec8f` batch
kernels + ULP-conformance tests.)

**ADR amendments since:** §15 (checklist additions 2026-05-13: primitives
hardening + `signed_distance.hpp` + ConvexHullView + queries.hpp facade);
§16 (witness-index tiebreak rule for EPA & hull support — pin #12 added
2026-05-14); §17 (v2 `-convex` cluster CLOSED 2026-05-14); §18 / §18.5
(v3 cluster substrate-foundation Q1–Q4 decisions + CLOSED 2026-05-15);
§19 (v4 `-mesh` cluster CLOSED 2026-05-16 — 9 locked decisions); §20
(v5 `-spatial` cluster CLOSED 2026-05-16 — 20 locked substrate
decisions); **§21 (v6 `-polygon` cluster CLOSED 2026-05-16 — 20 locked
substrate decisions across v6a–v6e + v6-close)**.

**Shipped systems documentation:** `docs/systems/geometry-primitives.md`,
`docs/systems/geometry-bvh.md`, `docs/systems/geometry-convex.md`,
`docs/systems/geometry-viz.md`, `docs/systems/geometry-mesh.md`,
`docs/systems/geometry-spatial.md`, `docs/systems/geometry-polygon.md`.

**Research dossier:** `docs/research/cerid-geometry.md` (11,523 words —
industry survey, algorithmic scope, determinism contract, module split,
slice list, consumer integration plan, slot decision)

**ADR:** `docs/decisions/0076-geometry-substrate-architecture.md` (Accepted 2026-05-11)

**Cornerstones:** ADR-0063 (determinism contract — Cerid-internal
predicates, deterministic-sort SAH BVH split, deterministic GJK simplex
update) · PRINCIPLES.md ("modular by default", "single-path", "no STL
in hot paths" — algorithms work over `crd::containers::Array` /
`ConstSpan` not `std::vector`)

**Supersedes:** none — net-new module slot.

**Strategic Execution Plan 2026-05-15** (`docs/ROADMAP.md` § Strategic
Execution Plan; memory [[feedback_strategic_execution_plan_2026_05_15]]):
this phase ships in FULL — the renewed-scope 49-slice plan stays
intact (no Pathway-B consumer-driven cutting). The Phase 3.1.7.5
`crd-units` substrate ships FIRST (immediate next phase, before v4
mesh resumes) so v4–v11 + v4-validate land with dimensional types
from day 1. Per-sub-module eylem-stub integration smoke runs as
hygiene during this phase to mitigate the ~7-month eylem cold-storage
gap (see `phase-3.1-eylem.md` § Eylem cold-storage mitigation for the
smoke matrix).

## Why this exists

Computational geometry is genuinely cross-cutting in Cerid. The
following modules ALL need spatial-acceleration structures + convex
shape ops + triangle mesh queries + polygon/mesh processing:

- **eylem broadphase** (v1c) — dynamic AABB tree (BVH refit + insert + query + raycast)
- **eylem narrow phase** (v1d) — GJK distance + EPA penetration depth + MPR alternative
- **eylem mesh collider** (v1d-mesh) — triangle mesh closest-point + raycast (BVH-accelerated)
- **eylem convex hull collider** — convex hull build + conditioning (V-HACD-class decomposition for editor)
- **crd-sdf v2 mesh-bake** — winding-number test (Jacobson 2013 — robust on non-watertight meshes) + BVH closest-point
- **crd-renderer** (Phase 3.5+) — frustum cull + occlusion BVH
- **crd-scene `SpatialBVHIndex`** (ADR-0053 reserved IComponentIndex shell) — the actual BVH implementation
- **crd-audio** (Phase 3.4) — acoustic ray-casts on level geometry
- **crd-eylem-aero** (ADR-0073 reserved) — aerodynamic surface evaluation on triangle mesh
- **crd-eylem-cine** (ADR-0074 reserved) — animated mesh queries for cinematic-bridge
- **navmesh** (Phase 7) — Delaunay tetrahedralisation + connectivity graphs
- **editor** (Phase 7) — V-HACD pipeline + selection geometry + picking + manipulator gizmos

Without a shared substrate, EACH consumer ships its own BVH +
GJK/EPA + closest-point — divergent perf, divergent determinism, 3-5×
maintenance burden. The Cerid pattern (`crd-sdf`, `crd-hesap`) for
cross-cutting domain substrates is to ship as a peer module to
`crd-math`, NOT bloat `crd-math` itself.

## Why NOT bloat `crd-math`

Same reasoning that motivated `crd-sdf` (ADR-0064 §2) and `crd-hesap`
(ADR-0065 §2):

1. **Compile-time tax** — every module that links `crd-math` would
   recompile every header that includes a triangulation algorithm or
   BVH builder template. Currently `crd-math` is ~30 headers; bloated
   it'd be ~150.
2. **Scope conflation** — `crd-math` is "vectors / matrices / quats /
   determinism wrappers / SIMD substrate". Computational geometry is
   "spatial reasoning over arbitrary shape representations". Different
   tier of abstraction.
3. **Tooling tier mismatch** — many crd-geometry algorithms (V-HACD,
   QEM simplification, Delaunay tetrahedralisation) are cooker/editor
   tier — they shouldn't live in the runtime engine's leaf math module.
4. **Independent evolution** — `crd-geometry-bvh`'s GPU mirror needs
   `crd-rhi` (for VkBuffer staging); `crd-math` must NEVER depend on
   `crd-rhi` (cycle).

## Multi-domain consumer matrix

| Consumer | Sub-module(s) needed | When |
|---|---|---|
| eylem v1c broadphase | `crd-geometry-bvh` | when 3.1.7 lands; ships own narrow BVH first |
| eylem v1d narrow phase | `crd-geometry-convex` (GJK + EPA) | when 3.1.7 lands; ships own GJK first |
| eylem v1d-mesh TriangleMesh collider | `crd-geometry-mesh` (closest-point, raycast) + `crd-geometry-bvh` | when 3.1.7 lands |
| crd-sdf v2 mesh-bake | `crd-geometry-mesh` (winding number) + `crd-geometry-bvh` | when 3.1.7 lands; ships own mesh-bake first |
| crd-renderer 3.5+ frustum cull | `crd-geometry-bvh` | natural at 3.5 |
| crd-scene SpatialBVHIndex | `crd-geometry-bvh` | reserved shell; lights up when 3.1.7 lands |
| crd-audio 3.4 acoustic raycasts | `crd-geometry-bvh` + `crd-geometry-mesh` | natural at 3.4 |
| crd-eylem-aero (ADR-0073) | `crd-geometry-mesh` (surface eval) | natural at v6f |
| crd-eylem-cine (ADR-0074) | `crd-geometry-mesh` (animated queries) | natural at v4d |
| editor V-HACD pipeline | `crd-geometry-decomposition` | Phase 7 |
| navmesh | `crd-geometry-delaunay` + `crd-geometry-mesh` | Phase 7 |

## Refactor reservation pattern (eylem v1c + v1d)

Same precedent already in the codebase:

- **eylem v7 FEM** ships own narrow internal PCG → consumes
  `crd-hesap-iterative` later (per Phase 3.1.6 doc).
- **eylem v9 differentiable** ships its own narrow autodiff → consumes
  `crd-hesap-autodiff` later.

For v1c + v1d the same applies:

- **eylem v1c (broadphase)** ships its own dynamic AABB tree (Catto
  GDC 2019) inside `crd-eylem-rigid3d`. When `crd-geometry-bvh`
  lands, refactor to consume it. Refactor is non-API-breaking — the
  broadphase System's public surface stays.
- **eylem v1d (narrow phase)** ships its own GJK + EPA inside
  `crd-eylem-rigid3d`. Refactors to consume `crd-geometry-convex`
  when 3.1.7 lands.

This lets eylem v1 ship without blocking on a 4-6 month substrate
slip; the refactor is bounded scope + delivers measurable perf +
maintenance wins when it happens.

## Slot in the broader Cerid roadmap

**Recommended:** Phase 3.1.7 (after `crd-hesap` 3.1.6, before Phase
3.2 animation). The ordering is:

1. `crd-sdf` (3.1.5) — implicit-surface representation, narrow scope
2. `crd-hesap` (3.1.6) — numerical computing, narrow scope
3. `crd-geometry` (3.1.7) — computational geometry, narrow scope

All three are substrate modules that **don't depend on each other**
internally (modulo trivial imports of `crd-math` types). The slot
ordering is determined by *consumer-side dependency* — Phase 3.5+
renderer consumes BVH; Phase 3.4 audio consumes BVH; eylem v1c+v1d
consume convex/BVH but ship own first → 3.1.7 lands BEFORE Phase 3.2
animation so renderer/audio/eylem-refactor can light up cleanly.

**Alternative considered:** Phase 3.1.55 (between sdf and hesap). The
case for this ordering is that geometry is closer to sdf in
dependency direction (both are spatial). The case AGAINST is that
hesap's matrix solvers are needed by some geometry ops (sparse
Cholesky for parameterisation, eigendecomposition for QEM
simplification's quadric error ellipsoids); hesap-first lets
crd-geometry consume crd-hesap-direct for the matrix-heavy parts.

**Decision:** Phase 3.1.7 (after hesap), driven by the matrix-solver
dependency.

## Slice list — locked (research dossier §8 + ADR-0076 §7)

| Slice | Scope | LOC | Calendar |
|---|---|---|---|
| **v0a** ✅ 2026-05-12 | `crd-geometry-primitives` module skeleton + primitive types (`Point3` alias, `Line`/`Segment`/`Ray`, `Plane`, `AABB`, `OBB`, `Sphere`, `Capsule`, `Triangle3`, `Frustum`). **ADR-0076 §13 move-and-delete:** absorbed `crd::math::geometry` (`Ray`/`Plane`/`Sphere`/`AABB`/`Triangle`→`Triangle3`/`Frustum` + ~16 helpers + their `std::formatter`s) → `crd::geometry::primitives::*` in `crd/geometry/primitives/{primitives,format}.hpp`; `crd/math/geometry.hpp` **deleted**; 9 consumers repointed (`crd-scene` `query.hpp`/`world.hpp` — new PUBLIC edge `crd-scene → crd-geometry-primitives`; `crd-math` umbrella + `format.hpp`; `tests/math` geometry tests moved to new `tests/geometry-primitives/test_primitives.cpp`; `tests/bench`; `tests/scene`; `runtime/examples/smoke_math.cpp`). `crd-math` now ships only Vec/Mat/Quat/Transform/SIMD/`deterministic`; `/wd4723` MSVC suppression moved to `crd-geometry-primitives`; `crd-no-std-math-check` CI guard now scopes `engine/geometry-primitives`. Built + run green on win-debug/win-asan/win-clang-cl/linux-gcc-debug. System doc: `docs/systems/geometry-primitives.md`. | ~700 + refactor | done |
| **v0b** ✅ 2026-05-13 | **2D peer set + closest-point catalogue (2D + 3D) + segment↔segment** (ADR-0076 §14 amendment). Added 2D types `Line2`/`Segment2`/`Ray2`/`AABB2`/`OBB2`/`Circle`/`Capsule2`/`Triangle2`/`Point2` + their `operator==`/helpers/`std::formatter`s; **renamed** the v0a 3D types to their `…3` forms (`Line→Line3` … `Capsule→Capsule3`; `Plane`/`Sphere`/`Frustum`/`Triangle3` unchanged) per the new dimension-suffix naming rule; ~8 consumer files repointed (`crd-scene` query/world incl. the ADR-0053 `in_aabb(AABB3<f32>)` shape, `tests/geometry-primitives`, `tests/bench`, `tests/scene`, `runtime/examples/smoke_math.cpp`, `crd-math` umbrella+format comments). New `crd/geometry/primitives/closest_point.hpp`: `closest_point` + `closest_param(Segment)` + `distance` + `distance_squared` for every primitive (2D & 3D), and `closest_points(seg1,seg2,out_c1,out_c2)` + `distance(seg1,seg2)` — the mutually-closest pair. Dimension-agnostic cores written once over a vector concept: **Ericson §5.1.5** Voronoi-region closest-point-on-triangle (7-region branch-light cascade, with a collinear-triangle 3-edge fallback), **Ericson §5.1.9** segment↔segment (robust on parallel/degenerate), the linear-projection param. Sphere/Circle/Capsule return the closest *surface/boundary* point (defined for interior `p`); degenerate query (on center/spine) → fixed `+x` deterministic tiebreak; sub-mm scales preserved (`numeric_limits<T>::min()` degenerate guards). New `tests/geometry-primitives/test_closest_point.cpp` (TEMPLATE_TEST_CASE float+double, ~14 cases / property tests vs dense barycentric sample). clang-format/tidy clean; `crd-no-std-math-check` guard green. Built + run green on **win-debug + win-shipping** (geometry-primitives-tests 20 cases / 39 726 assertions; crd-scene-tests 271; crd-math-tests 140; smoke_math) — full 14-config sweep deferred to v0 close. System doc updated. | ~1.6 KLOC + ~450 LOC tests + rename | done |
| **v0c** ✅ 2026-05-13 | **intersection corpus (2D + 3D)** — new `crd/geometry/primitives/intersect.hpp`. Ray casts: `intersect_ray_{aabb,obb,cylinder,capsule}` (3D, Tavianator branch-light slab for AABB/OBB; the robust precomputed-`RayPacket` is v0f) + `intersect_ray2_{aabb,obb,line,segment,circle,capsule,cylinder}` (2D). Boolean overlap: `intersects(AABB3,Triangle3)` Akenine-Möller 2001 13-axis SAT, `intersects(OBB3,OBB3)` 15-axis SAT, `intersects(OBB2,OBB2)` 4-axis, `intersects(Triangle2,Triangle2)` 6-axis SAT, `intersects(Triangle3,Triangle3)` Möller 1997 (coplanar case → 2D test), `segments_intersect(Segment2,Segment2)` (+ out-point), sphere/circle/capsule↔{AABB,OBB,Triangle,Segment,Plane,Capsule} reductions (exact, via v0b `distance_squared`), `intersects(Plane,{AABB3,OBB3,Sphere})`, `intersects(Frustum,OBB3)`, `intersects(Triangle2,{AABB2,Segment2,Circle})`, `intersects(Line2,{AABB2,OBB2})`. `intersects(Capsule3,{AABB3,OBB3})` conservative (segment-vs-box-grown-by-r — no false negatives; exact SAT-box-pair deferred to eylem v2). Determinism: SAT axes in a fixed enumeration order; near-zero cross-product axes skipped (never a false "separated"). **New types `Cylinder3`/`Cylinder2`** (flat-cap segment+radius — distinct from `Capsule`'s hemispherical caps; the v0c plan's user-confirmed addition) + `operator==`/`std::formatter`/`contains`. New `tests/geometry-primitives/test_intersect.cpp` (TEMPLATE_TEST_CASE float+double; ray hit/miss/grazing/inside-start, SAT per-axis-class + brute-force cross-checks, tri-tri piercing/coplanar/disjoint, sphere/capsule reductions vs `distance_squared`, 2D segment crossing/parallel/collinear/T-junction). Caught + fixed a real bug in the first AABB-tri SAT (the `(0,0,1)×e` block's f0/f2 vertex-pair selection was swapped) — rewrote the 9 edge-cross axes as a clean lambda over all 3 vertex projections. clang-format/tidy clean; `crd-no-std-math-check` green. Built + run green on **win-debug + win-shipping** (geometry-primitives-tests 38 cases / 40 988 assertions) — full 14-config sweep deferred to v0 close. System doc updated. | ~1.6 KLOC + ~600 LOC tests | done |
| **v0d** ✅ 2026-05-13 | **`Tetrahedron` type + barycentric / tetrahedron utilities** (Ericson §3.4). New type `Tetrahedron<T>` in `primitives.hpp` (3D-only → no suffix per the naming rule, like `Plane`/`Frustum`) + `operator==`/`std::formatter`/`f`-`d` aliases + `centroid`/`signed_volume`(= `(1/6)·det[b−a|c−a|d−a]`)/`volume`. New `crd/geometry/primitives/barycentric.hpp`: `barycentric(Tetrahedron,p)→Vec4` (the 4 weights as signed-volume ratios with `p` substituted for each vertex — orientation-stable; `CRD_ASSERT` on a flat tetra), `contains(Tetrahedron,p)` (all weights ≥ −ε), `from_barycentric` (the inverse — `Triangle3`/`Triangle2`/`Tetrahedron`), `decompose_prism_to_tets(Triangle3 bottom, Triangle3 top)→StaticArray<Tetrahedron,3>` (the canonical 3-tet split of a triangular wedge through `a₀` — fixed diagonal so neighbouring wedges share matching tets; the Marching-Tetrahedra / FEM hex-to-tet form). The triangle barycentric/contains forms already live in `primitives.hpp` (v0a–v0b) — unchanged. New `tests/geometry-primitives/test_barycentric.cpp` (TEMPLATE_TEST_CASE float+double; tetra volume/centroid/orientation, vertices→basis & centroid→(¼,¼,¼,¼), `contains` inside/face/edge/vertex/outside, orientation-independence on a negatively-oriented tetra, `barycentric∘from_barycentric` round-trip on random interior points, prism-split non-degeneracy + volume-sum + coverage). clang-format/tidy clean; `crd-no-std-math-check` green. Built + run green on **win-debug + win-shipping** (geometry-primitives-tests 46 cases / 42 182 assertions) — full 14-config sweep deferred to v0 close. System doc updated. | ~250 LOC + ~250 LOC tests | done |
| **v0e** ✅ 2026-05-13 | **iq formulary + `reduce_argmax_with_lex_tiebreak` + the shader-helpers module skeleton.** New `crd/geometry/primitives/formulary.hpp`: smooth-min/max — `smin_poly`/`smin_cubic` (iq quadratic & cubic; exact `min` outside the ±k blend band, dip = k at the crossover, collapse to `min` as k→0) + `smin_exp` (associative; via `crd::math::deterministic::exp2/log2`) + `smax_*` counterparts; value-domain `op_round`/`op_onion`/`extrude_2d`; position-domain `domain_repeat`/`domain_mirror` (2D/3D/scalar, exact `std::floor`-modulo, no trig) + `domain_elongate` + `domain_twist`/`domain_bend` (via `crd::math::deterministic::sin/cos`). New `crd::math::simd::reduce_argmax_with_lex_tiebreak` (`engine/math/include/crd/math/simd/reduce.hpp`, also wired into `simd.hpp`): scalar-deterministic horizontal argmax over a `Vec8f`/`Vec4f` chunk — `ArgmaxLex{index,score,x,y,z}` running-best + `argmax_lex_beats` (score-desc → (x,y,z)-asc-lex → earliest-index; NaN-scores never win; partition-independent), the ADR-0076 §4 #10 determinism pin v3 Quickhull needs. New module `engine/geometry-shader-helpers/` (target `crd-geometry-shader-helpers`, namespace `crd::geometry::shader_helpers`) — **skeleton only** (force-link stub + reserved header; the formula-IR cooker + GLSL/HLSL backends + ULP-conformance test land in v9e); added to root `CMakeLists.txt` and to the `crd-no-std-math-check` guard's scoped dirs. New tests `tests/math/test_reduce.cpp` (comparator: score/lex/index/NaN/invalid; chunk folds; partial lanes; partition-independence vs a scalar reference) and `tests/geometry-primitives/test_formulary.cpp` (smin exact-outside-band / crossover-dip = k / k→0-collapse / smax-duality / ≤min; `op_*`; `extrude_2d` cylinder hand-cases; `domain_repeat`/`mirror` periodicity + cell bounds; `domain_elongate` zero-inside-±h; `domain_twist`/`bend` k=0-identity + rigidity). clang-format/tidy clean (my files); `crd-no-std-math-check` green. Built + run green on **win-debug + win-shipping** (geometry-primitives-tests 56 cases / 49 224 assertions; crd-math-tests 143 / 2 956) — full 14-config sweep deferred to v0 close. System doc updated. | ~1.0 KLOC + ~450 LOC tests | done |
| **v0f** ✅ 2026-05-13 | **cutting-edge / branchless / SIMD intersection corpus** (ADR-0076 §13; research dossier §13) — **closes the v0 sub-phase**. New scalar headers: `watertight_ray_tri.hpp` — `precompute_ray_tri(Ray3)→RayTriShear` + `intersect_ray_triangle_watertight` (Woop/Benthin/Wald 2013: per-ray shear+scale to the dominant-axis frame, edge-function form, sign consistent across shared edges → no cracks; on an exact-zero edge function it recomputes that test in `double` when `T==float`; the default ray-tri for `-mesh` v4d leaves + `crd-sdf` v2 mesh-bake) + `TriAffine` / `precompute_triangle_affine` / `intersect_ray_triangle_precomputed` (Baldwin-Weber 2016: per-triangle 3x4 affine = inverse of `[e1|e2|n]`, ~9 mul/ray, branchless — opt-in for cooked static meshes); `robust_ray_aabb.hpp` — `RayAABBPrecompute{inv_dir, sign[3]}` + `precompute_ray_aabb(Ray3)` + `intersect_ray_aabb_robust` (Williams 2005 precomputed slab + Ize 2013 conservative `tmax` widening `x(1+2γ₃)`; NaN-safe min/max so a zero-direction axis drops out rather than poisoning); `plucker.hpp` — `PluckerLine{d,m}` + `plucker_from(Segment3|Ray3|Line3|p,q)` + `plucker_side(a,b)=dot(a.d,b.m)+dot(b.d,a.m)` (fixed sum order; sign-zero = on-the-line) + `intersect_ray_triangle_plucker` (sign-only edge classification, fully branchless). **Single-ray vs multi-ray** is an explicit split: the scalar precompute structs are *per-ray* (leaf-batch — one ray vs the N AoSoA primitive columns of a BVH leaf); the `Vec8f` `RayPacket8` is *per-packet* (Wald-style — 8 coherent rays, the box scalar/broadcast). New SIMD module piece: `simd_batch.hpp` + out-of-line `src/simd_batch.cpp` (so `crd-simd-emission-check` has a SIMD `.obj`) — `Vec8f` AoSoA bundles (`Aabb8`/`Sphere8`/`Triangle38`/`Segment38Pair`) + kernels `ray_vs_8_aabb` (one ray, 8 child boxes — Williams slab x8 + Ize pad), `ray_packet8_vs_aabb` (8 rays, 1 box), `ray_vs_8_triangle` (Möller-Trumbore x8, `cull_back` flag), `aabb8_vs_aabb`, `sphere8_vs_sphere`, `segment8_vs_segment_distsq` (Ericson §5.1.9 in SIMD via masked selects — robust on parallel/degenerate); all comparisons return all-bits-set masks, `1/x` is `_mm256_div_ps` (ADR-0076 §4 #8-#11). New tests `tests/geometry-primitives/test_v0f_corpus.cpp` (Woop basic+back-cull, shared-edge no-crack property, Woop vs v0c-MT agreement on a random non-degenerate corpus, Baldwin-Weber vs Woop, robust ray-AABB vs v0c slab + conservatism, Plücker side-values + ray-tri boolean vs Woop), `tests/geometry-primitives/test_v0f_simd.cpp` (each kernel lane-by-lane vs its scalar reference; mask convention hit!=0/miss==0). Also fixed two cross-config issues found by the v0-close sweep: (a) GCC `-Werror=shadow` — `intersects(Triangle3,Triangle3)`'s `interval` lambda params `d0/d1/d2` shadowed outer `d1`/`d2`; renamed to `e0/e1/e2`; (b) **non-ASCII characters in `TEST_CASE` names** (em-dash etc.) broke Windows `ctest` name-matching → all the new v0b–v0f test cases reported `(Failed)` — replaced with ASCII in every new test file; `scripts/check_no_non_ascii_test_names` guard green. clang-format/tidy clean; `crd-no-std-math-check` + `crd-simd-emission-check` green. **Full 14-config `scripts/full-sweep.ps1` PASS** (geometry-primitives-tests 74 cases; crd-math-tests with `test_reduce`). System doc updated. | ~1.4 KLOC + ~700 LOC tests | done |
| — | **v0 sub-phase ✅ CLOSED 2026-05-13** — `crd-geometry-primitives` ships the full primitive type set (2D + 3D + `Tetrahedron`), closest-point catalogue, intersection corpus (scalar + branchless cutting-edge + `Vec8f` SIMD), iq formulary, and `reduce_argmax_with_lex_tiebreak`; `crd-geometry-shader-helpers` skeleton in place. Next = **v1a** (`-bvh`: binned-SAH BVH). | | |
| **v1a** ✅ 2026-05-13 | New `crd-geometry-bvh` module (`engine/geometry-bvh/`, target `crd-geometry-bvh`, ns `crd::geometry::bvh`, deps `crd-core`+`crd-math`+`crd-containers`+`crd-geometry-primitives`; added to root `CMakeLists.txt` and the `crd-no-std-math-check` scoped dirs). `BvhTree` container — flat `Array<BvhNode>` (32-byte node: `AABB3<f32> bounds` + `u32 left_first` + `u16 prim_count` + `u8 split_axis` + pad; `prim_count==0` ⇒ interior children at `left_first`/`+1`) + `Array<u32>` leaf-order permutation; the tree does **not** own the AABBs (the builder records leaf union bounds + reorders the index array; query helpers take the `ConstSpan<AABB3<f32>>` back — ADR-0076 §11 functional form). `bvh_build(ConstSpan<AABB3<f32>>, IAllocator*, BvhBuildOptions{sah_bins=16, max_leaf_prims=4, topology=Binary}) → BvhTree` — Wald 2007 binned SAH (centroid-bucket per axis, sweep bin boundaries for the min-cost split `Nₗ·halfArea(boxₗ)+Nᵣ·halfArea(boxᵣ)`, recurse; `≤max_leaf_prims` ⇒ leaf; coincident-centroid node ⇒ deterministic median-by-index fallback so leaves stay capped; empty span ⇒ empty tree; single-threaded — jobs-parallel build is v1f); explicit-stack DFS, asserts depth `< k_max_bvh_depth=64`. Determinism (ADR-0076 §5.2 — pinned): SAH split axis evaluated X→Y→Z, lower bin index wins a cost tie (running best replaced only on *strictly* lower cost); leaf-order via a stable two-pass partition into scratch — no `std::sort`; verified by a deterministic-replay test (`memcmp` of node + index arrays across two builds of identical input). `bvh_sah_cost(tree)` quality metric. `bvh_raycast(tree, prims, ray, tmax=∞) → optional<BvhRayHit{t, payload}>` (`BvhRayHit = RayHit<u32>` after v1i-a — payload is the prim index) — ordered traversal (near child first per `split_axis`), per-node + per-prim test via the v0f precomputed Williams 2005 + Ize 2013 robust slab (`precompute_ray_aabb` / `intersect_ray_aabb_robust` — hole-free, NaN/∞-safe, conservative `tmax` pad; entry `t` exact); returns the primitive AABB hit (per-triangle ray-tri is `-mesh` v4). `bvh_overlap(tree, prims, box, on_prim)` callback + `Array<u32>&` convenience. New `tests/geometry-bvh/{test_bvh_build,test_bvh_raycast}.cpp` (14 cases / TlsfAllocator fixtures: empty/single, structural invariants on random corpora [permutation, child-enclosure, leaf-cap, tight leaf bounds], coincident-centroid stack/cap, long-thin row + SAH quality, deterministic replay, SAH-beats-single-leaf, `sah_bins<2` clamp; raycast vs brute force [parameter bit-match + named prim genuinely hit], empty/miss/`tmax`-clamp/origin-inside/grazing; overlap vs brute force, callback↔Array agree). clang-format/tidy clean; `crd-no-std-math-check` green. Built + run green on **win-debug + win-asan + win-shipping** + win-tidy build (per the in-flight `-bvh` verification directive — full 17-config sweep at `-bvh` close). System doc `docs/systems/geometry-bvh.md`. | ~1.0 KLOC + ~450 LOC tests | done |
| **v1b** ✅ 2026-05-13 | O(n) bottom-up refit — new public header `crd/geometry/bvh/bvh_update.hpp` + impl `src/bvh_update.cpp` (the `aabb_*` accumulation helpers extracted from `bvh_build.cpp` into an internal `src/aabb_ops.hpp` shared by both). `bvh_refit(BvhTree& tree, ConstSpan<AABB3<f32>> prims)` — single reverse pass over the node array (works because `bvh_build` pushes a node before its children → children have a higher index; a debug assert checks it): leaf ⇒ bounds = union of its prims; interior ⇒ bounds = union of the (already-refit) children. Topology — node array, leaf-order permutation, `split_axis` — left untouched; only `bounds` changes. No-op on an empty tree; `CRD_ASSERT` on a prim-count mismatch (rebuild instead). Query correctness is unaffected by how far prims moved (ancestors' bounds grow to cover them — only efficiency degrades). Catto GDC 2019. New `tests/geometry-bvh/test_bvh_refit.cpp` (4 cases: same-prims refit ⇒ tree byte-identical to a fresh build [`memcmp`]; after moving every box [some far enough to leave its leaf] ⇒ every node's bounds match the exact union it should hold + root encloses all + `bvh_raycast`/`bvh_overlap` vs brute force over the moved set; topology untouched [node/index/`left_first`/`prim_count`/`split_axis` all unchanged]; empty no-op + single-leaf recompute). clang-format/tidy clean; `crd-no-std-math-check` green. Built + run green on **win-debug + win-asan + win-shipping** (+ win-tidy build); `crd-geometry-bvh-tests` 18 cases. System doc updated. | ~150 LOC + ~250 LOC tests | done |
| **v1c** ✅ 2026-05-13 | **`DynamicBvh`** — the incrementally-updatable AABB tree (a *different structure* from the static `BvhTree` packed array; new public header `crd/geometry/bvh/dynamic_bvh.hpp` + impl `src/dynamic_bvh.cpp`, in the umbrella). A binary tree of fat AABBs with parent pointers + a free list of recycled 48-byte nodes (`aabb`/`parent`/`child1`/`child2`/`user_data`/`next_free`/`height`; `height==-1`⇒free, `0`⇒leaf, `≥1`⇒interior); height-balanced single tree rotations (`balance` — Box2D `b2DynamicTree::Balance`: rotate the deeper child up when its height exceeds its sibling's by >1) applied at every ancestor on insert/remove; a fat-AABB margin (`DynamicBvhConfig::fat_margin`, default 0.1) so a small move is a no-op. `insert(tight, user_data)→DynamicBvhNodeId` (stable handle): store `inflate(tight, margin)`; greedy best-sibling descent (Box2D-style cost: `2·halfArea(union)` to make a parent here vs. `halfArea(union(child))−halfArea(child)+inherit` to descend; deterministic c1-on-tie); new internal parent; refit + rebalance to the root. `remove(id)`: splice out the leaf, promote its sibling into the grandparent, free the old parent, refit + rebalance up. `update(id, new_tight)`: if the stored fat AABB still encloses `new_tight` ⇒ no-op, return false; else remove + reinsert (handle stays valid), return true. `query(box, on_leaf)` / `query(box, Array<u32>&)` / `raycast(ray, on_leaf)` — visit leaves by *fat* AABB (broadphase; raycast uses the v0f Williams/Ize precomputed slab; fixed `u32 stack[k_max_dynamic_bvh_stack=256]`). Access: `is_empty`/`leaf_count`/`user_data`/`fat_aabb`/`bounds`. Diagnostics: `sah_cost` (Σ interior half-areas / root half-area — Catto's metric), `max_depth`, `validate()` (debug — parent/child links, height invariant, AABB enclosure, leaf count; `[[maybe_unused]]` on the assert-only locals so Release stays `/WX`-clean). The internal `aabb_*` helpers (now incl. `aabb_union`/`aabb_inflate`/`aabb_contains` in `dynamic_bvh.cpp`'s anon ns) reuse `src/aabb_ops.hpp` from v1b. `bvh_update.hpp`'s v1c forward-pointer reworded to point at `dynamic_bvh.hpp`. New `tests/geometry-bvh/test_dynamic_bvh.cpp` (7 cases: empty/single-leaf [bounds, depth, validate]; query-overlap vs brute force over fat AABBs [+ `Array` overload]; raycast vs brute force over fat AABBs; update no-op-inside-margin / reinsert-on-escape + found-at-new-not-old; structural validity + query correctness across a 4000-op random insert/remove/update sequence [`validate()` every 250 steps]; depth ≤ ~3·log₂n + slack and `sah_cost` finite after 2000 inserts; handle stability after ops on other leaves). clang-format/tidy clean; `crd-no-std-math-check` + `crd-no-non-ascii-test-names` green. Built + run green on **win-debug + win-asan + win-shipping** (+ win-tidy build); `crd-geometry-bvh-tests` 25 cases. System doc updated. | ~520 LOC + ~310 LOC tests | done |
| **v1d** ✅ 2026-05-13 | **`Bvh4Tree` — the 4-wide topology variant** (new public header `crd/geometry/bvh/bvh4.hpp` + impl `src/bvh4.cpp`, in the umbrella). `bvh4_collapse(const BvhTree& binary, IAllocator*) → Bvh4Tree` — widen a built binary tree top-down: for each interior node start with its two binary children, then repeatedly "open" the interior member with the largest `aabb_half_area` (tiebreak: lower source-node index — deterministic) by replacing it with its two children, until 4 children or no openable member (so `child_count` ends at 2–4; `wc < 4` before each open ⇒ `wc + 1 ≤ 4` after, never overshoots); recurse via an explicit job stack. Self-contained re: leaf order (copies `binary.prim_indices()` in); `reserve(binary.node_count())` ⇒ no realloc during the collapse (asserted), and the result has ≤ `binary.node_count()` nodes (typically ~half a balanced source). Empty source ⇒ empty `Bvh4Tree`; single-leaf source ⇒ one node, `child_count == 1`. `Bvh4Node{AABB3<f32> bounds; Bvh4Child children[4]; u8 child_count;}` + `Bvh4Child{AABB3<f32> bounds; u32 first; u16 count;}` (`count>0`⇒leaf slot, `first`=prim-index start; `count==0`⇒node slot, `first`=child node index); accessors mirror `BvhTree`. `bvh4_raycast(tree, prims, ray, tmax=∞)→optional<BvhRayHit>` — four sequential v0f Williams/Ize robust slab tests per node (the *scalar* "ray-vs-4-AABB"; v1g adds the `Vec4f` kernel); `bvh4_overlap(tree, prims, box, on_prim)` callback + `Array<u32>&` — both give the **same results as `bvh_raycast`/`bvh_overlap` over the source binary tree** (the collapse only changes fan-out). `inline constexpr usize k_max_bvh4_stack = 256` (a 4-ary DFS pushing all children at once needs ~3·depth). `bvh4.cpp`'s anon ns uses `detail::aabb_empty`/`aabb_merge`/`aabb_half_area` from `src/aabb_ops.hpp`. `bvh.hpp` umbrella + roadmap comment updated. New `tests/geometry-bvh/test_bvh4.cpp` (5 cases: structural invariants on random corpora [`child_count`∈{2,3,4} or {1} for the single-leaf root; node-child refs point forward, `child.bounds == nodes[child.first].bounds` exactly; node bounds = exact union of children; every prim referenced once; `node_count ≤ binary.node_count()`; root encloses all prims]; `bvh4_raycast` == `bvh_raycast` [parameter bit-match + named prim genuinely hit, incl. `tmax` clamps]; `bvh4_overlap` == `bvh_overlap` [sorted prim-index sets, callback + `Array` forms]; deterministic collapse [`memcmp` of node + index arrays across two collapses]; empty / single-leaf / two-prim edge cases). clang-format/tidy clean; `crd-no-std-math-check` + `crd-no-non-ascii-test-names` green. Built + run green on **win-debug + win-asan + win-shipping** (+ win-tidy build); `crd-geometry-bvh-tests` 30 cases. System doc updated. | ~310 LOC + ~290 LOC tests | done |
| **v1e** ✅ 2026-05-13 | **BVH-accelerated closest-point query** — `bvh_closest_point(const BvhTree&, ConstSpan<AABB3<f32>> prims, const Vec3<f32>& query, f32 max_dist=∞) → optional<BvhClosestPoint{Vec3<f32> point; f32 distance_squared; u32 payload}>` in `bvh_query.hpp` (impl in `bvh_query.cpp`). Branch-and-bound stack DFS: the per-node AABB squared distance to `query` is a lower bound on every leaf below it, so a node whose lower bound ≥ the current `best_d2` is pruned — *re-checked on pop* (the best may have tightened since this node was pushed); at an interior node both children's AABB distances are computed and the *nearer* child is pushed last (popped first) so the best tightens before the far subtree is reached, and a child already known not to beat the best is skipped at push. All comparisons in squared space (`max_dist²` cutoff stored, no `sqrt` on the hot path); a small anon-ns `aabb_dist2(box,q) = dot(closest_point(box,q)−q, …)` keeps this TU independent of `closest_point.hpp` (`closest_point(AABB3,Vec3)` is in `primitives.hpp`). `nullopt` for an empty tree or nothing within `max_dist`. (Closest point on the primitive *AABB* — per-triangle closest-point inside a leaf is `-mesh` v4; the `Bvh4Tree`/`DynamicBvh` closest-point land in the v1i query facade.) New `tests/geometry-bvh/test_bvh_closest.cpp` (5 cases: empty tree → nullopt; matches a brute-force-over-all-AABBs reference on random corpora [`distance_squared` bit-match — both use the same `closest_point` call — + the named prim genuinely realizes that distance + `point` is on its AABB; on a tie, prims may differ but each is valid]; query inside a box → distance 0 at the query point; `max_dist` cutoff [in → hit at the exact face distance, out → nullopt]; single primitive). clang-format/tidy clean; `crd-no-std-math-check` + `crd-no-non-ascii-test-names` green. Built + run green on **win-debug + win-asan + win-shipping** (+ win-tidy build); `crd-geometry-bvh-tests` 35 cases. System doc updated. | ~120 LOC + ~190 LOC tests | done |
| **v1f** ✅ 2026-05-13 | **`bvh_build_parallel` (jobs-parallel binned-SAH build) + the Embree-comparison benchmark.** New public header `crd/geometry/bvh/bvh_build_parallel.hpp` + impl `src/bvh_build_parallel.cpp` (in the umbrella; `crd-geometry-bvh` gains `crd-jobs` PRIVATE); the serial builder's reusable kernels extracted to `src/bvh_build_internal.hpp` (`BinHistogram` + `clear_histogram`/`merge_histogram`/`accumulate_histogram`/`bin_index`, `sweep_for_split`, `centroid_bounds_of`, `stable_partition_by_bin`, `SplitChoice`, `k_max_bins`, `k_sah_cost_epsilon`) — `bvh_build.cpp` refactored onto them, no behaviour change. `bvh_build_parallel(prims, alloc, opts, num_jobs=0, parallel_threshold=8192) → BvhTree`: same explicit-stack DFS as `bvh_build`, but for a node with ≥ `parallel_threshold` prims it fans the two O(count) reductions (centroid bounds; the 3-axis bin histograms) out over `crd::jobs::parallel_for(num_jobs, num_jobs, …)` — `fn(i, i+1)` ⇒ `begin` is the chunk index, each worker reduces `idx[first + i·total/nj … first + (i+1)·total/nj)` into its own slot of a per-job partial array (on the tree's allocator, reused every node — *not* the frame arena, so a big build doesn't grow it), the main thread folds the partials in fixed job order. Min/max (componentwise box union) + integer adds are exact + commutative ⇒ the histograms (hence the SAH split, the partition, the node layout) are **byte-for-byte equal to `bvh_build`** regardless of `num_jobs`. `num_jobs == 0` ⇒ `jobs::num_workers()`; `num_jobs ≤ 1` or sub-threshold input ⇒ falls back to `bvh_build`. Requires `jobs::init()`. **Fixed a latent bug in `aabb_ops.hpp`'s `aabb_merge`** — it did `aabb_include_point(a, b.min); aabb_include_point(a, b.max);`, which is correct for two well-formed boxes but corrupts `a` to the *universe* box when `b` is the empty sentinel `{min=+∞, max=−∞}` (including `+∞` as a point pushes `a.max` to `+∞`). That misbehaved on every merge of an empty bin — the serial `sweep_for_split` prefix bounds when a middle bin is empty (masked: produced valid-but-suboptimally-split trees, the tests check invariants not splits) and the parallel histogram fold when a chunk has no prims in a bin. Fixed to a proper componentwise corner-min/max union (robust to the empty sentinel; bit-identical for well-formed boxes). Side effect: `bvh_build`'s trees are now correctly SAH-split — slightly different node arrays than before; all invariant/equivalence/deterministic-replay tests still pass. The Embree-comparison benchmark is `tests/bench/test_bench_bvh.cpp` (`[!benchmark]`, not in ctest; `crd-bench` gains `crd-geometry-bvh` + `crd-jobs`): build time on 100k/1M random AABBs (serial vs parallel), raycast throughput (binary vs BVH4) vs a 100k-AABB tree, `bvh4_collapse` time, closest-point throughput — measures Cerid's numbers; the §4.1 reference targets ("within 2× of Embree", ~2 ns/ray-AABB, ~8 ns/node, …) are inline; no Embree dependency (a direct head-to-head can later build against a local Embree behind an opt-in flag). New `tests/geometry-bvh/test_bvh_parallel.cpp` (hosts the binary's `BvhJobsListener` — Catch2 listener: `jobs::init()` in `testRunStarting`, `frame_reset()` per case, `jobs::shutdown()` in `testRunEnded`) — 4 cases: bit-identical-to-serial on random corpora for `num_jobs ∈ {1,2,4,8}` with `parallel_threshold=64` (exercises the parallel path on small inputs); 20k-AABB corpus above the default threshold matches serial; degenerate inputs (empty / single / coincident centroids); `num_jobs=1` and sub-threshold both take the serial fast path. clang-format/tidy clean; `crd-no-std-math-check` + `crd-no-non-ascii-test-names` green. Built + run green on **win-debug + win-asan + win-shipping** (+ win-tidy build; `crd-bench` builds in win-debug); `crd-geometry-bvh-tests` 39 cases. System doc updated. | ~530 LOC + ~340 LOC tests/bench | done |
| **v1g** ✅ 2026-05-13 | **`Vec4f` ray-vs-4-AABB kernel + BVH4 promoted to the recommended traversal form** (Option A — keep `bvh4_collapse(BvhTree)` as the BVH4 path, don't add a second 4-way builder; the elite-quality route per Embree/Jolt/PBRT). New public header `crd/geometry/bvh/bvh4_simd.hpp` (in the umbrella) + impl `src/bvh4_simd.cpp`: `ray_vs_4_aabb(ray, RayAABBPrecompute, bmin_x/y/z + bmax_x/y/z [SoA `Vec4f` over the four children], t0, t1) → {Vec4f t_enter, Vec4f hit_mask}` — one `Vec4f` Tavianator min/max slab chain (`tx0 = (bmin_x − ox)·ix; tx1 = (bmax_x − ox)·ix; …; tmin = max(min(tx0,tx1), min(ty…), min(tz…)); tmax = min(max…); tmax ·= (1+2γ₃)` per Ize 2013; `lo = max(tmin, t0); hi = min(tmax, t1); hit = cmp_le(lo, hi)`), NaN/∞-direction-safe via the min/max ordering; out-of-line in a `.cpp` (a real TU — but `Vec4f` is 128-bit `xmm` at every SIMD level, so the AVX2-`ymm`-expecting `crd-simd-emission-check` doesn't scan it; a 128-bit-aware variant is a follow-up, same as the `simd_batch.cpp` one from v0f). `bvh4_raycast` (in `bvh4.cpp`) now uses it: per popped node, transpose the ≤4 children's `.bounds` into 6 `f32[4]` → `Vec4f::load` → one `ray_vs_4_aabb`, then `store` `hit_mask`/`t_enter` to `f32[4]` and scan lanes `[0, child_count)` (skip a missed lane or one entered ≥ the current `best_t`; leaf-prim tests stay scalar `intersect_ray_aabb_robust` — per-leaf SIMD over the prims is `-mesh` v4g). **Bit-identical** (for finite/well-formed inputs) to the four scalar `intersect_ray_aabb_robust` calls it replaced (the Tavianator `min`/`max` form picks the same near/far plane the Williams `sign`-bit form does) — so the v1d "`bvh4_raycast` == `bvh_raycast`" test (and the new lane-by-lane kernel test) still pass. **`BvhBuildOptions::topology` + the `BvhTopology` enum removed** — `bvh_build` is always binary, `bvh4_collapse` is the BVH4 path (the `topology` field was speculative from v1a); the two builders' `CRD_ASSERT(... topology ...)` removed; `bvh_tree.hpp` doc updated. Docs flip: BVH4 is the recommended traversal form for static, query-heavy data (renderer cull, eylem broadphase static side, mesh-raycast leaves) — consumers call `bvh4_collapse`. New `tests/geometry-bvh/test_bvh4.cpp` case: `ray_vs_4_aabb` lane-by-lane vs four scalar `intersect_ray_aabb_robust` (2000 random ray×4-box trials, finite + ∞ `tmax`; hit-mask + exact `t_enter` match). The `~8 ns/node` target is in the v1f bench's comments + measured by `bvh4_raycast` benchmark there. clang-format/tidy clean; `crd-no-std-math-check` + `crd-no-non-ascii-test-names` + `crd-simd-emission-check` green. Built + run green on **win-debug + win-asan + win-shipping** (+ win-tidy build; `crd-bench` builds in win-debug); `crd-geometry-bvh-tests` 40 cases. System doc updated. **`-bvh` sub-module is now complete (v1a–v1g)**; next is v1h. | ~200 LOC + ~50 LOC tests | done |
| **v1h** ✅ 2026-05-13 | **Primitives-substrate hardening** (ADR-0076 §15). **`crd/geometry/primitives/constants.hpp`** — the geometry-wide epsilon/tolerance policy, named by intent (templated `constexpr T k_*()`, the `k_` snake form `bvh_build_internal.hpp` already used): `k_distance_epsilon` / `k_area_epsilon` / `k_parallel_epsilon` / `k_degenerate_extent_epsilon` (`1e-6F` for `f32`, `1e-12` for `f64` — the `default_epsilon` step), `k_sah_cost_epsilon` (`1e-6F` — `bvh_build_internal.hpp` now `using`s this; one source of truth), `k_default_fat_margin` (`0.1` — `DynamicBvhConfig`'s default, a *world unit* not an ε), `k_robust_aabb_pad_ulps` (`3` — `robust_ray_aabb.hpp`'s `ray_aabb_robust_pad<T>()` now derives `γ_n` from it; same value). **Value-preserving rename** — `crd-geometry-bvh-tests` stays 40/40; the existing function-signature defaults stay on `crd::math::default_epsilon<T>()` (migrating those onto the named constants is a later ergonomic pass, not v1h). **`crd/geometry/primitives/is_finite.hpp`** — `is_finite(x)` for `Vec2/Vec3` + every primitive (3D Line3/Segment3/Ray3/Plane/Sphere/AABB3/OBB3/Capsule3/Cylinder3/Triangle3/Tetrahedron/Frustum/ConvexHullView; 2D peers) + `all_finite(ConstSpan<Primitive>)`. The **NaN/Inf contract** (ADR-0076 §15): *queries tolerate* (a NaN/∞ primitive is silently never-hit / never-closest, never UB — the v0c/v0f IEEE-comparison corpus already gives this; the finiteness predicates are *not* on the query path), *builders reject in debug* — `bvh_build` / `bvh_build_parallel` (`CRD_ASSERT(all_finite(prims))`), `DynamicBvh::insert`/`update` (`CRD_ASSERT(is_finite(aabb))`), `bvh4_collapse` (`CRD_ASSERT(is_finite(binary.bounds()))`) on the *caller-supplied* data; release compiles the assert away → valid-but-possibly-degenerate structure, still no UB. Internal sentinels are NOT subject to it: `aabb_empty() = {+∞,−∞}` is a deliberate non-finite union identity (`is_finite` `false` by design — pinned by a test). **`crd/geometry/primitives/signed_distance.hpp`** — 20 Inigo Quilez analytic SDFs in C++ (`sqrt`/`abs`/`min`/`max`/`clamp`/`dot`/`length` only — no transcendental libm, so `crd-no-std-math-check` is clean; `sqrt(3)` / the regular-polygon vertex cosines are pre-evaluated literals): 3D `sd_sphere`/`sd_box`/`sd_round_box`/`sd_box_frame`/`sd_plane`/`sd_capsule`/`sd_cylinder`/`sd_cone`/`sd_torus`/`sd_triangle`(unsigned — a triangle has no interior)/`sd_ellipsoid`(iq's well-behaved bound)/`sd_octahedron`; 2D `sd_circle`/`sd_box_2d`/`sd_round_box_2d`/`sd_segment_2d`(unsigned)/`sd_triangle_2d`(signed)/`sd_equilateral_triangle_2d`/`sd_pentagon_2d`/`sd_hexagon_2d` (component-wise vector helpers `vabs`/`vmax0`/`max3`/`sgn` live in a `sd_detail` sub-namespace — `crd-math` stays lean). The C++ scalar reference `crd-geometry-shader-helpers` (v9e) emits GLSL/HLSL twins of and `crd-sdf` v0 reuses (`closest_point.hpp` gives the unsigned distance + closest point on the primitive structs in world space; this gives the signed, negative-inside value in shape-local space). **`ConvexHullView<T>`** in `primitives.hpp` (non-owning: `ConstSpan<Vec3> vertices` + `ConstSpan<Plane> faces` [outward-facing] + `ConstSpan<u32> face_vertex_indices` + `ConstSpan<u32> face_vertex_offsets` [prefix-sum, size `faces+1`]) + the two trivially-correct queries `support(hull, dir)` (extreme vertex, lowest-index tiebreak) and `contains(hull, point)` (inside iff `signed_distance ≤ ε` for every face) — `crd-convex` v3 *produces* one, `crd-eylem`'s `Collider::ConvexHull` references one; ray-vs-hull / closest-point-on-hull / GJK-contains land in v2a–v2f. New tests `test_constants.cpp` / `test_is_finite.cpp` / `test_signed_distance.cpp` (strong cross-checks vs `closest_point.hpp`'s `distance()` where a matching primitive exists + sign vs `contains` + closed-form spot values + 1-Lipschitz on random pairs, ellipsoid exempted) / `test_convex_hull_view.cpp`. clang-format/tidy clean; `crd-no-std-math-check` + `crd-no-non-ascii-test-names` + `crd-simd-emission-check` + `crd-no-std-sort-check` green. Built + run green on **win-debug + win-asan + win-shipping** (+ win-tidy build); `crd-geometry-primitives-tests` 103 cases, `crd-geometry-bvh-tests` 40 cases. System doc updated; session log `docs/sessions/2026-05-13-geometry-v1h-primitives-hardening.md`. | ~700 + ~600 tests | done |
| **v1i-a** ✅ 2026-05-13 | **Unified query facade + templated result types + missing `closest_point` variants.** New `crd/geometry/result_types.hpp` (substrate — `template <typename Payload> struct RayHit{ f32 t; Payload payload; }` + `struct ClosestPointResult{ Vec3 point; f32 distance_squared; Payload payload; }`; field order pinned by ADR-0076 §16 pin #2). New `crd/geometry/queries.hpp` — compile-time-overload-polymorphic `raycast` / `overlap` / `closest_point` over `{BvhTree, Bvh4Tree, DynamicBvh}` (inline forwards to the existing per-backend functions — zero overhead); primitive overloads (`closest_point` / `distance` / `intersects` / `signed_distance` / iq `sd_*`) flow through transitively from `closest_point.hpp` / `intersect.hpp` / `signed_distance.hpp` without re-definition (same `crd::geometry::primitives` namespace, picked up by ADL). `BvhRayHit` and `BvhClosestPoint` migrated from hand-defined structs to `using BvhRayHit = RayHit<u32>;` / `using BvhClosestPoint = ClosestPointResult<u32>;` aliases — field rename `.prim_index` → `.payload`, field order `{t, payload}` / `{point, distance_squared, payload}`; impl side (`bvh_query.cpp` / `bvh4.cpp` / `dynamic_bvh.cpp`) and 4 test files updated to the new order. New free function `bvh4_closest_point(Bvh4Tree, prims, query, max_dist)` — branch-and-bound DFS, insertion-sort the ≤4 children near-first (push descending, pop ascending; deterministic on tiebreak); same algorithmic shape as v1e `bvh_closest_point`. New member `DynamicBvh::closest_point(query, max_dist)` — broadphase closest leaf by *fat* AABB, reports `user_data` payload (member-fn because it accesses the private `Node` array; mirrors `query`/`raycast` members). DynamicBvh's `raycast` deliberately stays callback-only (broadphase semantics — no nearest-hit form here; v1c's general nearest-hit extends). New `tests/geometry-bvh/test_queries.cpp` — 10 cases: layout pins (`static_assert` on the alias relations + field order); facade `raycast`/`overlap`/`closest_point` for {BvhTree, Bvh4Tree, DynamicBvh} each cross-checked against the direct backend call AND brute force on a random corpus (300+ trials with finite/∞ `max_dist`/`tmax`); the new `bvh4_closest_point` and `DynamicBvh::closest_point` get dedicated edge cases (empty / single-prim / inside-fat-AABB / `max_dist` cutoff). clang-format/tidy clean; `crd-no-std-math-check` + `crd-no-non-ascii-test-names` + `crd-no-std-sort-check` green; `crd-simd-emission-check` green when run under vcvars (the full `ctest --preset win-debug`). **`crd-geometry-bvh-tests` 50 cases / 65 549 assertions** (was 40 / 13 197 after v1g). Built + run green on **win-debug + win-asan + win-shipping** (+ win-tidy build); full project ctest **1232/1232** on win-debug + win-asan, **1227/1227** on win-shipping. Session log `docs/sessions/2026-05-13-geometry-v1i-a-queries-facade.md`. | ~300 + ~340 tests | done |
| **v1i-b** ✅ 2026-05-13 | **Shapecast (closed-form TOI).** New `crd/geometry/bvh/bvh_shapecast.hpp` + `src/bvh_shapecast.cpp` — `bvh_shapecast_sphere(BvhTree, prims, Sphere moving, Vec3 dir, f32 tmax) → optional<BvhRayHit>` and `bvh_shapecast_box(BvhTree, prims, AABB3 moving, Vec3 dir, f32 tmax)` + their `bvh4_*` counterparts. Traversal = same near-child-first ordered DFS as `bvh_raycast` but each node + leaf-prim AABB is inflated by the moving shape's pad (per-axis `radius` for sphere; `half_extents` for box) before the v0f Williams/Ize robust slab test. Sphere-vs-AABB is **conservative** (square corners of the inflated AABB approximate the rounded Minkowski form — broadphase precondition every shipped engine accepts); box-vs-AABB is **exact** (Minkowski sum of two AABBs is an AABB); the general convex-cast / GJK-cast is `-convex` v2. `Vec4f` ray-vs-4-AABB SIMD kernel reuse for `bvh4_shapecast_*` deferred (~40 LOC follow-up; mirror of v1g pattern). `queries.hpp` facade extended: `cast_ray(Ray3, {AABB3 / OBB3 / Sphere / Plane}, tmax) → optional<T>` (thin `optional`-wraps the existing `intersect_ray_*` family); `cast_sphere(Sphere moving, dir, tmax, target)` for target ∈ {AABB3 (conservative), Sphere (exact quadratic), Plane (exact `signed_distance ± r`)}; `cast_box(AABB3 moving, dir, tmax, AABB3 target)` (exact Minkowski-AABB); BVH overloads forward to the new `bvh_shapecast_*` functions. **Argument convention pinned (propagates to v2 GJK-cast and v6 CCD):** primitive overloads use `(moving, dir, tmax, target)` (moving first; matches PhysX/Jolt); BVH overloads use `(tree, prims, moving, dir, tmax)` (structure first; matches v1a `raycast` / v1i-a `closest_point` shape). Overlap-at-start: `t=0` for AABB targets (slab returns 0 when ray origin is inside); explicit early-return added for sphere-vs-sphere and sphere-vs-plane (`intersect_ray_sphere` would otherwise return the *exit* time when origin is inside). Capsule-vs-triangle is deferred to v2 (GJK-based) / v4 (mesh-collider). New `tests/geometry-bvh/test_shapecast.cpp` — 15 cases: degenerate-recovery (zero-radius `cast_sphere` ≡ `cast_ray`; zero-extent `cast_box` ≡ `cast_ray`); overlap-at-start gives `t=0` across sphere×AABB, sphere×sphere, sphere×plane, box×AABB; exact-numeric closed-form spot checks (r=1 sphere at -3 → AABB at +2 gives t=4 against the inflated face; sphere×sphere t=8; sphere×plane t=4.5; box×AABB t=8); **bisection cross-check** (advisor recommendation — `distance²(target, swept_center(TOI))` is `≤ r²` at TOI for 20 random sphere-vs-AABB trials, and `> r²·0.95` one ε before TOI; catches a Minkowski-sum sign bug in the closed-form derivation that the per-algo brute force cannot); BVH shapecast matches brute force (200 prims × 300 random sphere sweeps + 300 random box sweeps, BvhTree and Bvh4Tree agree byte-for-byte); BVH degenerate-recovery (`cast_sphere(r=0)` ≡ `raycast`; `cast_box(half=0)` ≡ `raycast`); tmax cutoff; empty-tree → `nullopt`. Drive-by: v0f-style em-dash fix in one TEST_CASE name (`crd-no-non-ascii-test-names` guard would have caught it pre-ctest, but I didn't run the guard before the first sweep — same lesson, kept). clang-format/tidy clean on the new files; the pre-existing `bugprone-unchecked-optional-access` Catch2 `REQUIRE.has_value()` pattern in `test_queries.cpp` is unchanged. **`crd-geometry-bvh-tests` 65 cases / 68 383 assertions** (was 50 / 66 091 after v1i-a). Built + run green on **win-debug + win-asan + win-shipping** (+ win-tidy build); full project ctest **1247/1247** on win-debug + win-asan, **1242/1242** on win-shipping. Session log `docs/sessions/2026-05-13-geometry-v1i-b-shapecast.md`. | ~370 + ~340 tests | done |
| **v1i-c+** ✅ 2026-05-13 | **v1i-c debt payment (same-day).** Per user request, the three debt items v1i-c deferred to `docs/debt.md` paid this session rather than carried to v1j/v2: (1) **caller-owned scratch for `find_overlapping_pairs`** — new public POD `DynamicBvhPairWork{a,b}` (promoted out of function scope) + `DynamicBvhPairScratch{Array<u32> walk; Array<DynamicBvhPairWork> cross}` (move-only, allocator-constructed, `clear()` retains capacity); new scratch-taking overload `find_overlapping_pairs(Fn&&, DynamicBvhPairScratch&)` + Array-append + facade mirror; existing alloc-per-call overload now one-line forward. Test: 150-leaf tree, 5 mutation iterations with `update(random leaf)` between, scratch-path produces bit-identical sorted pair sets to alloc-per-call. Eylem v1c hot-path no longer pays per-tick `Array` alloc churn. (2) **`Vec4f ray_vs_4_aabb_inflated` SIMD kernel** in `bvh4_simd.hpp/cpp` — splats per-axis pad to Vec4f, inflates input columns, forwards to existing `ray_vs_4_aabb`; `bvh4_inflated_raycast` (the shapecast traversal in `bvh_shapecast.cpp`) now transposes ≤4 children into SoA Vec4f and does one kernel call per node, mirrors v1g's `bvh4_raycast` pattern; leaf-prim path stays scalar (per-leaf SIMD is `-mesh` v4g). 2000-trial lane-by-lane test (varying pad: zero / small isotropic / larger anisotropic) confirms bit-identical results to the scalar form for finite/well-formed inputs. Per-instruction comparison suggests a ~2-3× per-node ratio for the AABB test itself; per-call speedup on `bvh4_shapecast_*` depends on the leaf-scalar/interior-SIMD work mix (sparse trees see less benefit than dense ones) and is **unmeasured this slice** — dedicated shapecast micro-bench is future work when a real consumer needs the number. (3) **AABB-equivalence shift-invariance assertion** in `test_validation.cpp::BVH raycast at +1e6 origin` — now asserts the named hit AABB at the far origin (shifted back) matches the named hit AABB at origin within local ULP tolerance instead of comparing payload indices; decouples from builder permutation. Drive-by: removed two unused `using` decls flagged by clang-tidy. **`crd-geometry-bvh-tests` 82 cases / 76 581 assertions** (was 80 / 68 563; +2 cases, +8018 assertions). Built+run green win-debug/asan/shipping/tidy; full project ctest 1275/1275 win-debug+asan, 1270/1270 win-shipping. **All three debts struck from `docs/debt.md`.** Zero new debt. Session log `docs/sessions/2026-05-13-geometry-v1i-c-debt-payment.md`. | ~150 + ~150 tests | done |
| **v1i-c** ✅ 2026-05-13 | **Broadphase pairs + validation discipline + `crd-no-non-ascii-test-names` guard correctness fix.** New `DynamicBvhPair{u32 a; u32 b}` POD (a < b invariant + operator==/<) and new member template `DynamicBvh::find_overlapping_pairs(Fn&& on_pair)` — Catto GDC 2019 dual-descent: outer DFS over interior nodes calling cross-walk on (child1, child2) at each interior; cross-walk emits `on_pair(min, max)` for leaf-pair overlaps, expanding 1→2 or 1→4 children at mixed/interior nodes; both walks use `crd::containers::Array<...>` work stacks allocated from the tree's own allocator (unbounded cross-stack growth on pathological dense corpora is safe — Array form, not fixed bound). Array form `find_overlapping_pairs(Array<DynamicBvhPair>&)` is one-line forwarding from `dynamic_bvh.cpp`. Facade overloads in `queries.hpp`: `find_overlapping_pairs(const DynamicBvh&, Fn&&)` + `Array<DynamicBvhPair>&` form. Static-tree variants (BvhTree/Bvh4Tree) deliberately NOT shipped — static trees don't store user_data; if needed later they have a different signature shape. New shared header `tests/geometry-primitives/include/test_corpus.hpp` (header-only, both test binaries add as include dir): degenerate generators `degenerate_aabbs()` (point box / empty sentinel / NaN min/max / half-infinite) + `degenerate_triangles()` (coincident / collinear / needle / NaN / ∞ vertex) + `degenerate_spheres()` (zero / negative / NaN / ∞ radius / center) + `degenerate_rays()` (zero dir / NaN / ∞); large-coordinate `shift()` for each primitive + `k_far_origin_modest=1e6F`/`k_far_origin_stress=1e7F` + `ulp_tolerance_for(magnitude)` (≈ `m/2²²` for `m>1`, `1e-6` floor) — comparison slack callers use for shifted-scene queries (`1e-6` is wrong at +1e7 where local ULP is O(1.0)). New `tests/geometry-bvh/test_overlapping_pairs.cpp` (8 cases): empty/single-leaf no pairs; hand-built 3-box scene; brute-force agreement on 5 varying-density random corpora (sorted-vector equality + `a < b` invariant); callback form == Array form; deterministic across re-invocations; **n=10000 dense soak** (advisor recommendation, eylem-v1c blocker — 64 MB allocator, no TLSF OOM, brute-force count matches); tracks insert/remove/update. New `test_validation.cpp` in both test binaries (11 cases primitives + 7 cases BVH): NaN/Inf contract (§16 pin #3 — queries tolerate, never UB) exercised over `intersect_ray_*`/`closest_point`/`distance²`/SDFs/BVH raycast/overlap/closest_point/shapecast/find_overlapping_pairs; large-coordinate sweep ray-vs-AABB / ray-vs-sphere at +1e6 agrees with origin within local ULP, AABB-vs-AABB intersects shift-invariant at +1e7, BVH raycast at +1e6 returns same prim payload within local ULP, BVH overlap at +1e7 returns same prim set; `is_finite` correctly accepts finite-but-extreme corpus and rejects non-finite. **`crd-no-non-ascii-test-names` guard correctness fix** — the third consecutive slice hit by Windows ctest non-ASCII argv mojibake (v0f em-dashes, v1i-b `²`+em-dash, v1i-c `×`) made the recurrence undeniable; investigation found the guard had been a silent no-op since v0e (~3 days). Two compounding bugs in `scripts/check_no_non_ascii_test_names.ps1`: (a) param default `[string] $RepoRoot = (Resolve-Path "$PSScriptRoot/..").Path` — PowerShell evaluates param defaults in a scope where `$PSScriptRoot` is empty → `Resolve-Path "/.."` silently became `D:\`, `$testsDir = "D:\tests"` didn't exist, early-return PASS; (b) regex `[^ -]` — `-` between space and class-close `]` parses as a range op, equivalent to `[^\x20-\x2D]`, matched every letter/digit so even ASCII-clean lines would have been flagged if (a) hadn't masked it. Fix: initialize `$RepoRoot` in the body via `if ([string]::IsNullOrEmpty)`; replace regex with per-character codepoint loop (`foreach ($ch in $line.ToCharArray()) { if ([int]$ch -gt 0x7F) ... }`) that can't be misparsed. Linux `.sh` was correct (`LC_ALL=C grep $'[\x80-\xff]'`). Verified the fix by seeding `tests/_corruption_test/test_x.cpp` with `×` in a TEST_CASE name (guard FAILed with exit 1 + named the offending line) then removing the seed (guard PASSed on the real tree). Inline postmortem comments in the .ps1 name the bug + dates + slices bit. Drive-by em-dash/non-ASCII fixes in two TEST_CASE names in `test_validation.cpp` (the bug that revealed the guard regression — both replaced with ASCII). `crd-geometry-bvh-tests` 80 cases / 68 563 assertions; `crd-geometry-primitives-tests` 114 cases / 64 467 assertions. Built + run green on **win-debug + win-asan + win-shipping** (+ win-tidy build); full project ctest **1273/1273** on win-debug + win-asan, **1268/1268** on win-shipping. clang-format/tidy clean. Session log `docs/sessions/2026-05-13-geometry-v1i-c-broadphase-pairs-and-validation.md`. **Closes v1i (a/b/c).** | ~250 + ~600 tests | done |
| ~~**v1i**~~ (split into a/b/c on 2026-05-13 — user opted to land the three deliverables as separate slices since v1i sized at ~700 LOC was bigger than typical) | **Unified query facade + shapecast + broadphase-pair API + validation sweep** (checklist; ADR-0076 §15) — `crd/geometry/queries.hpp`: compile-time-overload-polymorphic (NOT virtual) `raycast(...)` / `overlap(...)` / `closest_point(...)` / `contains(...)` / `distance(...)` over `{primitive, BvhTree, Bvh4Tree, DynamicBvh}` (and extended over the `-spatial` structures at v5) + the shared result types `RayHit{t, payload}` / `ClosestPointResult{point, distance², payload}` / overlap-callback convention — a single "give me hits" surface that forwards to the right backend. **Shapecast (sweep tests):** `cast_ray` / `cast_sphere` / `cast_box` against a primitive *and* a BVH — time-of-impact for the closed-form shapes (sphere-cast vs AABB = ray-vs-AABB-grown-by-r; box-cast vs AABB = ray-vs-Minkowski-AABB; capsule-cast vs triangle; …), no GJK needed (the *general* convex shapecast = GJK-cast extends this in v2; eylem v6 CCD's two-moving-convex case stays in eylem). **Broadphase pairs:** `find_overlapping_pairs(const DynamicBvh&, OutFn)` — the dual-descent self-overlap (all `(i<j)` fat-AABB-overlapping leaf pairs in one traversal, not n separate queries) — the all-pairs primitive every physics broadphase wants (eylem v1c wraps it). **Validation discipline:** a systematic **degenerate-geometry corpus** (zero-volume AABBs / collinear or zero-area triangles / coincident points / NaN-Inf inputs) and a **large-coordinate** sweep (all geometry shifted to a +1e6 / +1e7 far origin — queries still correct within an f32-precision tolerance; flags where f64 staging is needed) added across the whole `crd-geometry` test suite + as reusable test helpers. | ~700 + ~500 tests | ~3 days |
| **v1j-a** ✅ 2026-05-13 | **`crd-geometry-viz` companion module substrate.** New module `engine/geometry-viz/` (target `crd-geometry-viz`, ns `crd::geometry::viz`, deps `crd-core` + `crd-math` + `crd-containers` + `crd-geometry-primitives` + `crd-geometry-bvh` + `crd-draw`; mirrors `crd-eylem-viz` companion pattern per ADR-0066 §13). Three layered headers + impls: `primitives.hpp` (overloaded `draw(RenderBuffer&, Shape, ...)` for AABB3 / OBB3 / Sphere / Capsule3 / Cylinder3 / Plane[finite patch] / Triangle3 / Tetrahedron / Frustum[8-corner reconstruction via closed-form 3-plane intersection] / Ray3 / Segment3 / Line3 — forwards to existing `crd::draw::*_to`), `queries.hpp` (`draw_ray_hit` ray + cross + optional normal arrow; `draw_closest_point` segment + endpoint markers; `draw_normals` per-vertex hairs), `bvh.hpp` (`depth_color` 8-entry palette; `draw_bvh(BvhTree | Bvh4Tree | DynamicBvh, depth_limit)` recursive walks emitting depth-coloured AABBs; `draw_overlap_pairs_with(DynamicBvh, ud→pos)` template + convenience `draw_overlap_pairs`; `draw_frustum_cull` per-prim kept-vs-culled). 26 cases / 44 assertions on `crd-geometry-viz-tests` (exact line-count pins per primitive, depth-palette cycle, BVH node-count match, overlap-pair count, kept-vs-culled cull test). Built + run green on **win-debug + win-asan + win-shipping** (+ win-tidy build); full project ctest **1301/1301** on win-debug + win-asan, **1296/1296** on win-shipping. clang-format/tidy clean. All 4 CI guards green (`crd-no-non-ascii-test-names` actually verifies now post-v1i-c fix). System doc `docs/systems/geometry-viz.md`. Session log `docs/sessions/2026-05-13-geometry-v1j-a-viz-substrate.md`. **Debt added:** `DynamicBvh::for_each_leaf(Fn)` public walker (so `draw_bvh(DynamicBvh)` can show per-leaf bounds, not just root; editor Phase 7 will need it; not blocking today). | ~500 + ~200 tests | done |
| **v1j-b** ✅ 2026-05-13 — **closes v1** | **Sandbox integration — scene-selector dropdown + 4-mode geometry showcase.** New `sandbox/src/geometry_showcase.{hpp,cpp}` (~870 LOC, pure data + pure functions, kept out of the 1500-LOC `sandbox_layer.cpp`). `enum class SandboxScene{Physics, GeometryViz}` + `GeometryShowcaseState` POD + public `render_geometry_showcase(state, RenderBuffer&, IAllocator&)` + `draw_geometry_showcase_imgui(state)`. **Primitive viewer** — dropdown selects one of 11 primitives (Sphere/AABB/OBB/Capsule3/Cylinder3/Plane/Triangle3/Tetrahedron/Frustum/Ray3/Segment3); ImGui sliders for params; `viz::draw(buf, shape)` emits wireframe; draggable query point + `viz::draw_closest_point` overlay for the 7 primitives with a `gprim::closest_point` overload (Cylinder/Tetrahedron skip — closest_point doesn't exist for those in v0b). **Query showcase** — N random AABBs from deterministic seed (slider 2..64, default 8) → `bvh_build` each frame; ImGui radio picks raycast / overlap / closest-point / sphere-cast; calls the v1i-a/b unified facade (`crd::geometry::raycast`/`overlap`/`closest_point`/`cast_sphere`); on hit highlights the prim + emits result via `viz::draw_ray_hit` / `viz::draw_closest_point`. **BVH viewer** — N random AABBs (slider capped at 800 per advisor — per-frame rebuild + walk budget; rebuild-caching debt filed); tree kind radio toggles BvhTree/Bvh4Tree/DynamicBvh — `viz::draw_bvh` emits depth-coloured AABBs for static trees; `viz::draw_bvh_bounds` + per-leaf query walk for DynamicBvh; depth-limit slider; for DynamicBvh: optional `viz::draw_overlap_pairs_with(…, ud→centroid)` overlay; optional frustum-cull overlay via local `build_view_frustum` helper (FOV/aspect/near/far/pos sliders) + `viz::draw_frustum_cull`. **SDF heatmap** — SDF kind selector (Sphere/Box/RoundBox/Torus/Octahedron/Capsule/Cone/BoxFrame/Cylinder); per-type parameter sliders; grid resolution 4..48; each sample evaluates v1h `gprim::sd_*`; coloured via HSV ramp (blue inside / green surface / red outside); `sdf_max_distance` cutoff. `SandboxLayer` patched with two members (`m_scene` + `m_showcase`) + conditional eylem `step_fixed` skip in GeometryViz mode + new "Scene" ImGui panel + showcase emit into shared `m_draw_buffer`; eylem TLSF (16 MB, named) reused as showcase scratch. Default scene = Physics — existing v1b-e boot behaviour preserved. Built+run green **win-debug + win-asan + win-shipping** (+ win-tidy build); full project ctest **1302/1302** on win-debug + win-asan, **1297/1297** on win-shipping. Sandbox smoke `--smoke-test 3` PASSES on all three (528-530 frames @ ~176 fps, zero Vulkan validation, clean exit). **Coverage caveat (advisor):** the smoke runs only the Physics-default boot path; the four showcase sub-modes' code paths are NOT automated — user-side validation = manually click through every sub-mode + every primitive type + every SDF kind (documented as the "Validation responsibility" section in the session log). New `tests/geometry-viz/test_showcase.cpp` smoke automation filed as debt. **Debt added:** rebuild-caching for BVH viewer; closest_point overloads for Cylinder3 + Tetrahedron in `crd-geometry-primitives`; the test_showcase.cpp smoke. **v1 cluster CLOSED 2026-05-13** — Phase 3.1.7 v1a (BvhTree) → v1b (refit) → v1c (DynamicBvh) → v1d (Bvh4Tree) → v1e (closest-point) → v1f (parallel build) → v1g (Vec4f BVH4 SIMD) → v1h (primitives hardening) → v1i-a/b/c (unified queries + shapecast + broadphase pairs) → v1i-c+ (debt payment: scratch + Vec4f shapecast SIMD + AABB-equiv test) → v1j-a (`crd-geometry-viz` substrate) → **v1j-b (closes v1)**. 14 sub-slices total. **Full 17-config `scripts/full-sweep.ps1` is now DUE** (deferred since v1c per user; CI surprisingly green so sweep amortises once at v1 close). Session log `docs/sessions/2026-05-13-geometry-v1j-b-sandbox-showcase.md`. | ~870 sandbox + ~50 layer patch | done |
| ~~**v1j-b**~~ (original line; absorbed into the ✅ row above) | **Sandbox integration.** ImGui dropdown selecting between the existing eylem physics demo (preserved as-is) and a new geometry-showcase scene. Showcase has four ImGui-selectable sub-modes: (1) Primitive viewer — dropdown of primitive types with sliders, renders wireframe + closest-point line from a draggable query point. (2) Query showcase — fixed scene of ~5 primitives, ImGui picks a query (raycast / overlap / closest-point / shapecast), renders input + result. (3) BVH viewer — N random AABBs (slider), depth-coloured nodes, toggle BvhTree / Bvh4Tree / DynamicBvh, insert/remove for DynamicBvh + overlap-pair lines, frustum-cull toggle. (4) SDF heatmap — sample v1h `sd_*` analytic SDFs at a grid, render points tinted by distance. | ~1500 sandbox/UI + ~0 tests | ~3 days |
| ~~**v1j**~~ (split into a/b on 2026-05-13 — user expanded scope to include the full sandbox showcase) | **`crd-geometry-viz` companion module** (NEW MODULE — debug-only; depends `crd-geometry-*` + `crd-draw`; `crd-geometry` itself never depends on `crd-draw` — the lean-substrate / `crd-eylem`+`crd-eylem-viz` precedent) — pure functions emitting `crd::draw::RenderBuffer` primitives: `draw_aabb` / `draw_obb` / `draw_sphere` / `draw_capsule` / `draw_frustum` / `draw_triangle` / `draw_ray` (primitive wireframes); `draw_ray_hit` (hit point + normal arrow + the `t`-segment); `draw_closest_point` (segment query-point→closest-point); `draw_normals` (per-face/per-vertex normal hairs on a mesh view); `draw_bvh(BvhTree | Bvh4Tree | DynamicBvh, depth_limit)` (node AABBs colour-keyed by depth); `draw_frustum_cull(Frustum, BvhTree)` (kept vs culled node AABBs in two colours); `draw_overlap_pairs(DynamicBvh)` (lines between overlapping leaf centroids). Extended incrementally — v4 adds mesh-query draws, v5 adds octree/grid-cell draws, v6 adds polygon-clip draws. | ~500 + ~200 tests | ~3 days |
| **v2a** ✅ 2026-05-14 | **GJK distance kernel — substrate decisions locked here.** Module `engine/geometry-convex/` (target `crd-geometry-convex`, ns `crd::geometry::convex`, PUBLIC dep on `crd-geometry-primitives`). Three locked API decisions: (1) **C++20 `ConvexShape` concept + ADL `support()` free-fn overloads** — compile-time overload-polymorphic, matches ADR-0076 §16 pin #1; (2) **local-frame shapes + two explicit transforms** in `gjk_distance(a, xform_a, b, xform_b)` — GJK driver computes `T_BA = xform_a^{-1} * xform_b` once and works in shape A's local frame (~40% faster than world-frame for OBB pairs); support functions take `dir` in shape's local frame, pure axis-aligned math, ~6–10 ops each, inline-perfect; (3) **`SupportPoint{Vec3f point, u32 vertex_idx}`** — `k_invalid_vertex = ~u32{0}` sentinel for analytic shapes; hull support returns argmax vertex index with lowest-index tiebreak on coincident extrema (ADR-0076 §4 pin #11 → pin #12 added). GJK driver: Ericson §9.5 Voronoi-region sub-distance routines (1/2/3/4-simplex), termination on `k_distance_epsilon` convergence OR origin-containment fast-out (sets up v2b). Returns `ClosestPointResult` with world-space witness points + Minkowski-difference vector + the terminating simplex (preserved for v2c EPA). Deterministic-by-construction: Ericson order, NOT van den Bergen (ADR-0076 §4 pin #1). Four `support()` overloads: Sphere (~10 ops), OBB3 (~6 ops + copysign), Capsule3 (endpoint argmax + radius), ConvexHullView (linear scan + lowest-idx tiebreak). | ~700 + ~400 | 3 days |
| **v2b** ✅ 2026-05-14 | **GJK boolean overlap fast-out.** Same simplex machinery as v2a with early-termination when simplex contains origin (already wired in v2a's driver; this slice exposes the public entry point + tests). One-line forward into `queries.hpp::overlap(convex_a, xform_a, convex_b, xform_b)`. Separate slice only to keep PR-sized commits — 90% of the work is shared with v2a. | ~150 + ~150 | 1 day |
| **v2c** ✅ 2026-05-14 | **EPA penetration depth + contact normal.** Expanding Polytope Algorithm continuing from v2b's overlap simplex. Half-edge-style growing polytope (Catto 2010 GDC); face-priority queue keyed on origin distance; termination on epsilon-flat new vertex. Emits penetration vector + closest face normal + the two world-space witness points + per-witness `vertex_idx` (consumed by eylem v1d-manifold for feature identification). **Deterministic tiebreak: lowest face-index on coincident-distance faces** (ADR-0076 §4 pin #12 — to add). The `vertex_idx` carried since v2a makes EPA's polytope duplicate-vertex check O(1) integer-compare instead of fuzzy Vec3-compare → cross-platform reproducible convergence. | ~600 + ~400 | 3 days |
| **v2d** ✅ 2026-05-14 | **SAT box-pair fast path.** Specialised `sat_obb_obb(OBB3, Transform, OBB3, Transform)` and `sat_aabb_obb(AABB3, OBB3, Transform)` — Gottschalk 1996 15-axis test (3 + 3 + 9), returns separating axis + penetration depth (matches EPA semantics for box-vs-box, ~2–3× faster on box-heavy scenes). Wired into the convex-pair dispatch at the eylem boundary so OBB-OBB pairs skip GJK/EPA. Cross-check tests vs v2c for randomised box-pair corpus (1000 trials, EPA-vs-SAT agree within `k_distance_epsilon`). | ~300 + ~250 | 2 days |
| **v2e** ✅ 2026-05-14 | **`ConvexHullView` queries.** `ray_vs_hull` (Cyrus-Beck parametric face-plane clipping; deterministic on coincident edge entries), `closest_point(hull, p)` (GJK against degenerate point shape — reuses v2a kernel), `contains(hull, p)` (signed-plane test against outward face planes — promoted from v1h to here with deterministic tiebreak). Hooked into the unified `queries.hpp` facade overload set. | ~400 + ~350 | 2 days |
| **v2f** ✅ 2026-05-14 | **GJK-based convex shapecast.** Catto 2013 — iterate GJK with the moving shape's transform advanced by the current TOI estimate; secant root-finding to converge on time-of-impact (NOT bisection — Catto's secant is the elite pattern); emits `RayHit<u32>{t, payload}` + witness points at contact. Extends `cast_*` in `queries.hpp` for the general moving-convex-vs-static-convex case (v1i shipped only closed-form sphere/box-cast). Cross-checked vs bisection of v2b boolean across the sweep parameter. | ~700 + ~450 | 3 days |
| **v2g** ✅ 2026-05-14 | **Hill-climbing hull support with cached vertex (PhysX/Havok pattern; perf, no API change).** Extends `ConvexHullView` with optional per-vertex adjacency arrays (`ConstSpan<u32> vertex_adjacency_indices` + `ConstSpan<u32> vertex_adjacency_offsets` — prefix-sum form). New `support(ConvexHullView, dir, hint_vertex_idx)` overload that warm-starts the search from `hint_vertex_idx` (the previous GJK iteration's argmax for this hull) and walks neighbors to a higher-dot neighbor until local-max — O(1) amortized per support eval on convex hulls (provably converges in ≤diameter steps). GJK driver caches `vertex_idx` per shape across iterations and feeds it back as the hint. Dispatch: `if N≤32 → SIMD-linear-scan (v2h); else → hill-climbing`. 3–5× win for N>32 hulls. **Bench:** GJK hull-vs-hull on N=64 should drop from ~150 ns/pair to ~50 ns/pair on Zen 4 reference. Cerid V-HACD hulls are 8–32 verts (where SIMD-linear-scan dominates); hill-climbing wins on mesh-collider derived hulls (N=100s) and editor-tier raw hulls. | ~250 + ~200 | 2 days |
| **v2h** ✅ 2026-05-14 | **SIMD-batched hull support — `Vec4f` / `Vec8f` over vertex dots (perf, no API change).** Add SoA-pre-transposed vertex layout option on `ConvexHullView` (`ConstSpan<f32> vx_soa, vy_soa, vz_soa` co-existing with the AoS form — cooker emits both when SIMD path is opt-in). Per support eval: `Vec8f vx = load(vx_soa)`, similarly y/z, compute 8 dots in parallel, lane-wise max with `reduce_argmax_with_lex_tiebreak` (v0e infrastructure, already deterministic). On AVX2, the per-vertex cost drops from ~6 ns to ~1 ns. Combined with v2g warm-start: GJK hull-vs-hull on N=32 lands in <30 ns/pair. `crd-simd-emission-check` validates AVX2 `ymm` emission on `crd-geometry-convex/src/hull_support_simd.cpp` (out-of-line per the same pattern as `simd_batch.cpp` v0f). | ~300 + ~250 | 2 days |
| **v2i** ✅ 2026-05-14 | **f64 GJK instantiation + aerospace orbital corpus (precision, falls out of templates for free).** Ensure every `support()` overload has an f64 form (Sphere/OBB/Capsule already do; ConvexHullView gets a `ConvexHullView<f64>` view). Instantiate `gjk_distance<f64>` against orbital-scale poses (10⁷ m). Test corpus: f32 vs f64 ULP cross-check at increasing distances (origin / 10³ / 10⁶ / 10⁹ m); the slice surfaces the distance at which f32 GJK falls out of acceptable error and at which f64 is required. Aerospace consumers (orbital rendezvous, asteroid contact) get `gjk_distance<f64>` for free — no parallel API. | ~150 + ~300 | 2 days |
| **v2j** ✅ 2026-05-14 | **Sutherland-Hodgman convex polygon clipping + feature enumeration (eylem v1d-manifold gate).** Move Sutherland-Hodgman *from v6 to here* — it's fundamentally a convex-polygon algorithm; Vatti in v6 handles general polygons. Ships `clip_convex_polygon(ConstSpan<Vec3> poly, Plane against, IAllocator*) → Array<Vec3>` + the multi-plane convenience `clip_against_convex_volume(poly, ConstSpan<Plane> side_planes, alloc)`. Plus **feature enumeration helpers** consumed by manifold builders: `enumerate_faces(OBB3) → StaticArray<FaceFeature, 6>` (each face = plane + 4 vertex indices), `enumerate_faces(ConvexHullView)` (uses existing face arrays), `enumerate_edges(OBB3, Capsule3, ConvexHullView)` (edge = vertex-pair + adjacent-face-pair). Sphere has no face/edge features; reported via `is_smooth(Shape)`. This slice is the gate for eylem v1d-manifold (PhysX/Jolt-grade 4-point contact manifolds). | ~300 + ~250 | 3 days |
| **v2-close** ✅ 2026-05-14 | **Tiebreak conformance + degenerate corpus + perf bench + full 17-config sweep.** ADR-0076 §4 determinism pins re-verified across all v2 entry points (Ericson simplex order, EPA face tiebreak [pin #12], SAT axis order, hull-support lowest-index, Sutherland-Hodgman vertex-on-edge convention). Degenerate corpus reused from v1i-c (zero-thickness boxes, coincident hulls, NaN/Inf support inputs — queries tolerate, builders reject). New `tests/bench/test_bench_gjk.cpp` (`[!benchmark]`) hits every performance budget in §"Performance budgets" (GJK 50–200 ns/pair, Sphere/Box closed-form <20 ns/pair, hull-vs-hull warm-start <30 ns on N≤32). Session log + v2 close-out. Full sweep required. | ~50 + ~200 | 1 day |
| **v3a** ✅ 2026-05-14 | **Shewchuk 1997 adaptive-precision predicates** (NEW substrate foundation, 2026-05-14 user-approved). `orient2d` / `orient3d` / `incircle` / `insphere` with adaptive expansion arithmetic per Shewchuk "Adaptive Precision Floating-Point Arithmetic and Fast Robust Geometric Predicates" (1997). Lives in `engine/geometry-primitives/include/crd/geometry/primitives/predicates.hpp` (the leaf substrate — every higher-tier module consumes without depending on `-convex`). Resolves ADR-0076 §164 / §397 open question; consumers: v3b 2D hull, v3c Quickhull, future v6 Vatti polygon Boolean, v8 Bowyer-Watson Delaunay, v9 V-HACD, CFD AMR, FEA contact, CAD boolean (Phase 3.1.8 `crd-brep`). ULP-conformance test against Shewchuk's published reference results. | ~600 + ~400 tests | 3-5 days |
| **v3b** ✅ 2026-05-14 | **2D convex hull (Andrew's monotone chain).** `convex_hull_2d_indices(points, out_hull_indices)` primitive + `convex_hull_2d_points(points, out_hull_points)` convenience. Lex-sort with `crd::containers::stable_sort` + exact `orient2d` from v3a + lowest-input-index tiebreak. Output: CCW polygon (vertices + indices into input). Degenerate corpus: 0/1/2/3 points, all-colinear, all-coincident, very-large coord. Lives in `engine/geometry-convex/include/crd/geometry/convex/convex_hull_2d.hpp`. | ~200 + ~250 tests | 1-2 days |
| **v3c** ✅ 2026-05-14 (a+b+c all done) | **3D Quickhull (Barber-Dobkin-Huhdanpaa 1996, robust).** **Split 2026-05-14 into a/b/c per advisor's per-seam discipline**: v3c-a (skeleton + types + initial tetrahedron + degenerate fallbacks) ✅; v3c-b (main iteration loop — eye selection + visible-face DFS using full Stage D `orient3d` + horizon walk + face replacement + neighbor relink + conflict-list redistribution, 14 test cases passing first-try) ✅; v3c-c (`enrich_for_gjk` populates v2g vertex adjacency + v2h SoA SIMD f32-only arrays + flat-3D-hull coplanar reconstruction via v3b on dominant plane, 8 new test cases passing first-try). Total ~1020 LOC engine + ~660 tests / 22 cases / 401 assertions across win-debug + win-asan + win-shipping + win-tidy clean. Promoted `compute_vertex_adjacency_from_faces` to `crd-geometry-primitives::hull_adjacency.hpp` for multi-consumer use. `QuickhullResult<T>{vertices, faces (outward Planes), face_vertex_indices, face_vertex_offsets}` + `convex_hull_view_of(QuickhullResult) → ConvexHullView` helper + `enrich_for_gjk(QuickhullResult&)` mutator appending v2g vertex-adjacency + v2h SoA SIMD arrays. Outer-set partition + horizon walk. Determinism: ADR-0076 §4 pin #11 Quickhull lex order (lowest-input-index on furthest-point ties; deterministic horizon-walk order; coplanar-points-to-face assignment uses lowest input index). Robust: exact `orient3d` from v3a; coplanar input falls back to v3b 2D hull on dominant plane + flat-3D-hull marker; near-degenerate input snaps below `k_distance_epsilon`. Builder-reject contract (ADR-0076 §15): `CRD_ASSERT(all_finite(points))` at entry. Honest LOC sizing per Q3 recommendation (~1500 vs original ~900). Lives in `engine/geometry-convex/include/crd/geometry/convex/quickhull.hpp` + `engine/geometry-convex/src/quickhull.cpp`. | ~1500 + ~800 tests | 6-7 days |
| **v3d** ✅ 2026-05-15 | **Hull simplification — greedy vertex removal with shrinkage-distance cost + locked-vertex preservation + convexity guard.** Greedy vertex-removal re-triangulation targeting `target_vertex_count` (caller-specified; default 0 = no target) OR `max_error_threshold` (caller-specified shrinkage distance, length units; default 0 = no threshold; with both 0 → identity copy). Per-vertex cost = max signed distance from removed vertex to any fan-triangle plane that would replace it; pivot for fan = lowest-index ring vertex (ADR-0076 §4 pin #11 determinism). **Convexity guard**: every candidate fan triangle's outward plane must have all OTHER live hull vertices below within `k_distance_epsilon`; failures tombstone the candidate, try next-cheapest. `HullSimplifyOptions::keep_vertex_indices: ConstSpan<u32>` (default empty) excludes locked vertices from candidate selection — multi-domain pin per Q4 (V-HACD uses empty, CAD/FEA/robotics/eylem-contact use populated to preserve feature corners / attachment points / boundary nodes / contact-critical IDs). Degenerate sources (empty / 1-/2-/3-vertex / coplanar / colinear / coincident) return identity copies. Output: fresh `QuickhullResult<T>` with vertices in increasing-original-input-index order (matches v3c convention); output vertices are a strict subset of input vertices (output ⊆ input AABB trivially). API: `simplify_hull(source, alloc, opts) → QuickhullResult<T>`. Lives in `engine/geometry-convex/include/crd/geometry/convex/hull_simplify.hpp` + `engine/geometry-convex/src/hull_simplify.cpp`. Templated `T ∈ {f32, f64}` with explicit instantiations. **16 test cases / 191 assertions** (identity / tetrahedron-no-op / cube→4 / octahedron→4 / locked-vertex-preservation / all-locked-no-op / tiny-threshold-no-removal / huge-threshold-full-reduction / determinism-replay / random-cloud / f32-cube / AABB-containment / random-cloud-locked). **Built+run green on win-debug + win-asan + win-shipping + win-tidy** (1546/1546 on debug/asan/tidy, 1541/1541 on shipping). Full convex suite 198 cases / 21270 assertions. Drive-by: fixed pre-existing non-ASCII characters (`→`, `—`) in v3b + v3c TEST_CASE names that were Windows-ctest-mojibaking; `crd-no-non-ascii-test-names` guard now clean across `tests/geometry-convex/`. Session log `docs/sessions/2026-05-15-geometry-v3d-hull-simplify.md`. | ~440 + ~440 tests | done |
| **v3-close** ✅ 2026-05-15 | **Tiebreak conformance + cross-check + large-coord stability + perf bench + full 17-config sweep.** New `tests/geometry-convex/test_v3_close.cpp` (9 cases / 243 assertions): (1) Quickhull shuffled-input preserves hull vertex SET (5 perms × 80-point cloud) — same-SET-different-input-indices invariant per ADR-0076 §4 pin #11; (2) 2D monotone-chain shuffled-input preserves hull vertex SET; (3) Large-coord 1e6 origin shift preserves Quickhull topology; (4) Large-coord 1e7 origin shift preserves Quickhull topology; (5) 3D Quickhull coplanar fallback matches v3b 2D monotone-chain on Z=0 plane (geometric set equality); (6) v3d threshold-respect against measured ground-truth shrinkage (tiny eps → no removal; generous threshold → reduces); (7) v3d all-locked + huge threshold = no removal; (8) v3d large-coord 1e6 simplification stays convex + locked preserved; (9) v3d shuffled-input simplification stays valid (weak invariants — greedy trajectory depends on hull vertex order). New `tests/bench/test_bench_quickhull.cpp` (8 benchmarks: Quickhull build 100/1k/10k; monotone-chain 1k/10k; simplify 200→8, 500→16). Full convex suite **207 cases / 21513 assertions** (was 198 / 21270 at v3d close, +9 / +243). **Full 17-config `scripts/full-sweep.ps1` PASS**: 10 Win (debug / relwithdebinfo / release / asan / clang-cl / debug-scalar / debug-sse2 / shipping / clang-cl-shipping / tidy) + 7 Linux (debug / relwithdebinfo / release / asan / debug-scalar / debug-sse2 / shipping) all green. **Pre-existing v3a debt paid en route**: `engine/geometry-primitives/src/predicates.cpp`'s `two_two_sum` Shewchuk-primitive helper was unused on the live code paths (only `two_two_diff` is reached); clang-cl `-Werror=unused-function` flagged it; marked `[[maybe_unused]]` with comment that it stays as a documented Shewchuk-expansion helper for the future Stage D `insphere` consumer. **Drive-by**: also paid the pre-existing non-ASCII test-name debt in v3b/v3c (19 TEST_CASE names with `→` / `—` mojibaking through Windows ctest argv — `crd-no-non-ascii-test-names` guard now green across `tests/geometry-convex/`). Session log `docs/sessions/2026-05-15-geometry-v3-close.md`. ADR-0076 §18 amendment locks Shewchuk-predicates + Quickhull + hull-simplify substrate decisions per the Q1–Q4 user-approved choices on 2026-05-14. | ~360 + ~280 tests + ~250 bench | done |

**v3 sub-phase total: ~2750 LOC engine + ~2000 LOC tests, ~13–19 days calendar (~2.5 weeks).** (Was ~2000 / ~1 wk in the original phase-doc plan; expanded per the 2026-05-14 user-approved recommendations on the 4 substrate-foundation questions — Shewchuk predicates added as v3a; honest Quickhull LOC sizing; no owning-view type; `keep_vertex_indices` from day 1.)

| ~~Original v3a–v3c row~~ | (Replaced 2026-05-14 by the four-slice v3a/v3b/v3c/v3d catalog above + v3-close.) | — | — |
| **v4a** ✅ 2026-05-16 | **`mesh_closest_point` (Ericson §5.1.5 + BVH branch-bound).** New module `engine/geometry-mesh/`: `TriangleMeshView<T>{ConstSpan<Vec3<T>> vertices; ConstSpan<u32> indices}` (non-owning), `TriangleMeshBvh{Array<AABB3<f32>> triangle_aabbs; BvhTree tree}` (per-tri AABB array + Wald-2007 binned-SAH binary builder via existing `bvh_build`), `mesh_closest_point(view, bvh, query, max_dist=inf) → optional<MeshClosestPoint>` (Ericson Voronoi-region cascade at BVH leaves + branch-and-bound traversal with `aabb_dist_sq` lower-bound pruning + lowest-tri-index tiebreak per ADR-0076 §4 pin #11). Two-layer typing per ADR-0078 §5: raw `<MathScalar T>` algorithm + `TriangleMeshViewT<D, T>` / `MeshClosestPointT<D, T>` typed-Quantity wrappers in `mesh_queries_typed.hpp`. 125-query brute-force cross-validation on unit cube. 6 cases / 145 assertions. Session log `docs/sessions/2026-05-16-geometry-v4a-mesh-closest-point.md`. | ~600 + ~200 tests | done |
| **v4b** ✅ 2026-05-16 | **`mesh_raycast` — Woop 2013 watertight + Williams/Ize slab.** **Switched from originally-planned Möller-Trumbore to Woop after probing v0f corpus** — watertight contract eliminates edge-leak failure mode, exact-zero edge predicates promote to double, bit-exact across f32/f64 rays. Per-ray `precompute_ray_aabb` + `precompute_ray_tri` shear amortise across traversal. BVH ordered descent (far-pushed-first → nearer-popped-first tightens best_t before far subtree). Leaves: `intersect_ray_triangle_watertight` per triangle; lowest-tri-index tiebreak on equal t. `MeshHitPayload{tri, bary}` returned; bary = Woop's `(1-u-v, u, v)` saves callers from re-running interpolation. `cull_back=false` default. Typed `Ray3T<D, T>` / `MeshRayHitT<D, T>` wrappers (origin = `Vec3<Quantity<D,T>>`, direction = dimensionless `Vec3<T>`, t = `Quantity<D, T>`). 8 cases / 16 assertions. Session log `docs/sessions/2026-05-16-geometry-v4b-mesh-raycast.md`. | ~500 + ~250 tests | done |
| **v4c** ✅ 2026-05-16 | **`mesh_winding_number` — Jacobson 2013 robust inside/outside on non-watertight meshes.** Direct O(N) sum of Van Oosterom-Strackee 1983 per-triangle solid angles, `w(p) = (1/4π) Σ 2·atan2(a·(b×c), |a||b||c| + (a·b)|c| + (b·c)|a| + (c·a)|b|)`. `mesh_is_inside(view, query, threshold=0.5)` convenience. Degenerate vertex-coincident contributions short-circuit to 0. **Jacobson robustness verified**: +X face-removed cube interior point classifies inside via the 0.5 threshold (w ≈ 0.833, lost 4π/6 sr of solid angle from missing face). Determinism caveat: atan2/sqrt libm drift 1-2 ULP per contribution; 0.5 threshold absorbs it. Typed wrapper signature: dimensionless `f32` return (winding = rotations/4π — no Quantity wrap on return); typed `Vec3<Quantity<D,T>>` query only. **v4c-fast (Jacobson §4 hierarchical treecode with per-BVH-node dipole moments)** deferred until consumer surfaces. 8 cases / 281 assertions. Session log `docs/sessions/2026-05-16-geometry-v4c-mesh-winding-number.md`. | ~400 + ~300 tests | done |
| **v4d** ✅ 2026-05-16 (was v4g) | **`mesh_raycast_simd` — AVX2 8-wide Möller-Trumbore at BVH leaves.** Same BVH traversal as v4b (Williams/Ize precomputed slab + ordered nearer-first descent + best_t pruning), differs only at the leaf inner loop: chunk leaf's triangles into groups of ≤8 + gather AoS Vec3f vertex positions + transpose into 9 × Vec8f SoA registers + SIMD MT pipeline (edge1, edge2, pvec=rd×e2, det, inv_det, tvec, u, qvec, v, t) → 4 Vec8f result (det, u, v, t) + scalar lane scan applies masking (cull_back / `\|det\|>ε=1e-7` / `0≤u≤1` / `0≤v` / `u+v≤1` / `0≤t≤best_t`). **Mid-implementation lesson**: first-draft SIMD-mask AND via `min(mask_gt, mask_lt)` is implementation-defined for NaN-encoded `_CMP_*` result registers — silently lost the cull bit; switched to scalar lane scan after SIMD ALU. MT-vs-Woop precision divergence on tessellated curved-surface near-edge rays — bounded cross-validation against v4b on 36-ray sphere corpus tolerates ≤6 divergent triangle-picks. Pad-replicate last valid tri for partial chunks (lane scan ignores via lane<valid_count; deterministic; no NaN-poisoning). v4d-fast follow-on (per-leaf SoA repack at BVH build time, saves gather/transpose cost) deferred until benchmarks justify. 7 cases / 15 assertions. Session log `docs/sessions/2026-05-16-geometry-v4d-mesh-raycast-simd.md`. | ~400 + ~150 tests | done |
| **v4-validate** ✅ 2026-05-16 | **`validate_triangle_mesh` — formal mesh validation pipeline stage.** Six defect kinds: OutOfBoundsIndex (a=tri, b=bad vertex), DegenerateTriangle (i0==i1 etc; a=tri), ZeroAreaTriangle (`\|edge1×edge2\|² < (2·ε)²`; a=tri), NonManifoldEdge (edge shared by ≥3 tris; a=v_lo, b=v_hi), BoundaryEdge (edge shared by 1 tri; a=v_lo, b=v_hi), InconsistentOrientation (two adjacent tris use shared edge in same direction; a=tri, b=tri). Three-pass deterministic algorithm: triangle-level scan → build sorted edge table with canonical (min(va,vb), max(...)) keys + dir preserved → run-classify by count. `MeshValidationReport{defects, triangle_count, vertex_count, manifold/boundary/non_manifold_edge_count, well_formed, watertight}`. `well_formed` excludes critical defects (OutOfBounds/Degenerate/NonManifold/InconsistentOrientation); zero-area + boundary are informational. `watertight = well_formed && boundary_edge_count == 0 && triangle_count > 0` (empty mesh is well-formed but not closed surface). **`Area32` typed at options surface; raw f32 inside the algorithm per ADR-0078 §5 D34.** Sorted-edge classification beats hashmap for cross-compiler determinism. Use cases: cooker pipeline gate, editor mesh-import diagnostics, optional runtime gate before TriangleMeshCollider construction. 10 cases / 46 assertions. Session log `docs/sessions/2026-05-16-geometry-v4-validate.md`. | ~400 + ~250 tests | done |
| **v4-close** ✅ 2026-05-16 | **`-mesh` cluster CLOSED.** ADR-0076 §19 amendment locking 9 substrate decisions (watertight reference vs SIMD fast path; non-owning view + separate BVH; MeshHitPayload carries bary; O(N) winding at base + hierarchical treecode deferred; libm drift accepted for winding; Area32 typed at boundary; sorted-edge classification beats hashmap; zero-area not critical; watertight requires triangle_count>0) + `docs/systems/geometry-mesh.md` system documentation + 18-config full sweep PASS (11 Win + 7 Linux WSL, 11:49 elapsed, 0 failed). Drive-by: fixed `unused variable 'inds'` in `test_mesh_validate.cpp:163` flagged by clang-cl `-Werror=unused-variable` (MSVC was lenient). **Cluster totals: 5 slices · 39 cases / 503 assertions · `engine/geometry-mesh/` module = 1 umbrella + 6 logical headers + 5 .cpp files + typed wrapper layer for closest_point + raycast + winding.** Session log `docs/sessions/2026-05-16-geometry-v4-validate.md` includes the cluster close. | docs + sweep | done |
| **v5a** ✅ 2026-05-16 | **KD-tree (Friedman/Bentley/Finkel 1977 + nanoflann/PCL refinements).** New module `engine/geometry-spatial/`. Static balanced KD-tree over a point set: `KdTree<T>` (flat 12 B / 16 B `KdNode<f32>` / `<f64>` array + `Array<u32>` payload permutation; non-owning view over caller's points; pinned via `static_assert`) + `kd_build(points, alloc, opts)` deterministic builder. **Elite-tier choices over canonical FBF77**: (1) widest-extent split axis NOT round-robin (X<Y<Z tiebreak on equal extent — better depth on skewed clouds, canonical topology); (2) leaf bucket size 8 default per `KdBuildOptions::leaf_threshold` (nanoflann ≈ 10, PCL ≈ 15; ~8× fewer nodes vs leaf=1, kills per-node prune overhead); (3) median pick via `crd::containers::nth_element` + lex-tuple `(coord, original_index)` comparator (no two elements compare equal — partition fully determined; bit-identical tree topology across MSVC / GCC / clang, the determinism trap `std::nth_element`'s implementation-defined equal-key ordering closes); (4) pre-allocate both children before subtree DFS (the `bvh_build` trick — right child sits at left+1 regardless of subtree depth). Queries: `kd_radius(tree, points, q, r, out)` (squared-distance prune at interior + linear leaf scan), `kd_nearest_n(tree, points, q, k, out)` (branch-and-bound k-NN; **caller-allocated max-heap** via `crd::containers::push_heap` / `pop_heap` deterministic across compilers; `MaxByDistance` orders `(d², payload)` lex so tied-distance higher-payload sits at top → §4 pin #11 tiebreak by eviction; pruning is strict `>` not `>=` because tied lower-bound subtree may hold tied-distance lower-payload winner; final ascending sort via `crd::containers::sort` for reproducibility), `kd_range_aabb(tree, points, box, out)` (interval-prune DFS, inclusive bounds). **k-NN API takes `k` explicitly NOT via `out.capacity()`** — `Array::reserve` rounds up, capacity-as-k contract is unreliable (caught at first run with 1-vs-8 mismatch). Builder REJECTS non-finite input (debug `CRD_ASSERT`); queries TOLERATE non-finite (every finite-vs-NaN compare false → AABB-prune kills every subtree at root; symmetric with ADR-0076 §15 BVH pin). Two-layer typing per ADR-0078 §5 D34: raw `<MathScalar T>` algorithm bodies + `kd_queries_typed.hpp` strip-compute-retag wrappers (`Vec3<Length32>` / `Length32` in, distance squared `Quantity<DimMul<D,D>, T>` out). 24 cases / brute-force cross-validated on 500-1000 pt random clouds × 5 trial seeds × 5 radii / 4 k values; permutation determinism (5 shuffles → set-equal positions); large-coord stability (origin + 1e6 → set-equal payloads, NOT byte-identical topology — f32 ULP at 1e6 ≈ 0.0625 validly reorders sub-ULP separations). 5-config DoD PASS: win-debug 1977/1977 + win-asan + win-shipping + win-shipping-profile 1972/1972 + win-tidy. Session log `docs/sessions/2026-05-16-geometry-v5a-kd-tree.md`. | ~700 engine + ~750 tests | done |
| **v5b** ✅ 2026-05-16 | **Loose octree (Thatcher Ulrich 2000) — the workhorse for scene spatial culling.** New `loose_octree.hpp` + `loose_octree.cpp` in `engine/geometry-spatial/`. Dynamic AABB index: 64-byte `OctreeNode<T>` (`static_assert`-pinned: AABB(24) + 8×u32 children(32) + parent(4) + depth(1) + flags(1) + pad(2)) + free-list-managed object pool with stable `OctreeObjectId` handle (DynamicBvh precedent — invalid after remove, by API contract). Per-cell `Array<u32 obj_idx>` allocated on first insert. **Loosening factor 2.0** (Ulrich's pin) — loose AABB = 2× tight cell, gives single-cell residency property. **Locked elite-tier choices** (advisor-validated 2026-05-16): (1) update fast-path predicate = AABB-fit only (NOT center-in-cell — Ulrich's correctness invariant; ~90%+ of small motions fast-path); (2) raycast = t-near-first child descent + best_t pruning, emission order NOT part of API contract — only nearest result is (BVH pattern, ADR-0076 §16 pin #2); (3) overlap = Morton-order child traversal (000…111) deterministic emission; (4) octant tiebreak `>=` (lower-octant-wins on midplane); (5) lowest-payload-index tiebreak on equal raycast `t` (§4 pin #11); (6) builder REJECTS non-finite + out-of-root + oversized (debug `CRD_ASSERT`); queries TOLERATE non-finite (defensive `is_finite(ray.origin) && is_finite(ray.direction)` at raycast entry); (7) `leaf_object_threshold = 8` is split TRIGGER not hard cap (max-depth cells retain all assigned objects); (8) lazy root allocation (empty trees cost nothing). Two-layer typing per ADR-0078 §5 D34: `octree_queries_typed.hpp` strip-compute-retag wrappers (`AABB3<Length32>`, `OctreeRay3T<D, T>`, `OctreeRayHitT<D, T>` typed surface). 17 cases / **fast-path 100% test** (the elite-quality discriminator — sized-by-construction object_half=0.1 + max_depth=8 ⇒ fast-path window 0.68 > cell radius 0.39 ⇒ any cell-resident motion hits fast path) + slow-path triggers when needed + brute-force overlap cross-validation × 20 query boxes + raycast nearest + equal-`t` lowest-payload tiebreak + loosening 1.5/2.0/3.0 result-count invariance + permuted-insert determinism + insert/remove cycle stability + NaN tolerance + all-coincident + sizing pin. 5-config DoD PASS: win-debug 1996/1996 + win-asan + win-shipping + win-shipping-profile 1991/1991 + win-tidy clean. Session log `docs/sessions/2026-05-16-geometry-v5b-loose-octree.md`. **Deferred** (not blockers): f64 raycast precompute (f32-only today); `compact()` empty-subtree collapse (cheap in steady-state churn); f64 raycast t-near ordering. | ~750 engine + ~530 tests | done |
| **v5c** ✅ 2026-05-16 | **R-tree — full Beckmann 1990 R*-tree (NOT quadratic-split simplification).** New `rtree.hpp` + `rtree.cpp` in `engine/geometry-spatial/`. M=16 + m=5 (Beckmann §6 + Boost.Geometry default). Two-field `RTreeEntry { aabb, payload, handle }` (32 B per entry; inline `overlap` template emits user payload at zero cost). Stable handle via indirection table `m_locations[handle] → {node, entry}` updated on every split/reinsert/condense (the canonical R-tree handle stability technique). **Full R*-tree algorithm**: (1) **choose-subtree** (Beckmann §4.1) — leaf-parent uses overlap-enlargement (lex tiebreak: area-enlargement → area → child idx), higher levels use area-enlargement; (2) **split** (§4.2) — for each axis, sort by lower-bound + upper-bound, sum perimeters across all (M-2m+2)=8 distributions per sort, pick min-perimeter axis (lex tiebreak x<y<z), within axis pick min-overlap distribution (lex tiebreak min-area); (3) **forced reinsertion** (§4.3) — on first overflow per level per insert, remove `floor(M × 0.3) = 4` entries farthest from node centre + reinsert from root in distance-ASCENDING order (`m_treated_levels` bitmask, one-shot); (4) **delete + condense-tree** (Guttman §3.4) — collect orphans on underflow, reinsert at original level, root single-child collapses. **STR (Sort-Tile-Recursive) bulk-load** (Leutenegger 1997) first-class — `bulk_load(aabbs, payloads, out_handles)` packs L=⌈N/M⌉ leaves into S=⌈√L⌉ vertical slabs sorted by x-midpoint, each slab into M-leaves sorted by y-midpoint, recursively pack levels above. Optimal packing for cooked-level data (~7× faster than sequential insert). **k-NN — Hjaltason-Samet 1999 incremental** — priority queue ordered by min-distance from query to AABB; pop closest, expand or emit; stops when k results ready. PQ uses `crd::containers::push_heap`/`pop_heap` (deterministic). **Determinism**: every sort uses `crd::containers::sort` with explicit lex-tuple comparator; lex tiebreak everywhere (split, choose-subtree, reinsert-distance, STR sort). Builder REJECTS non-finite (debug `CRD_ASSERT`); queries TOLERATE non-finite (defensive `is_finite` short-circuit at API surface for overlap + raycast + k-NN). Lowest-payload tiebreak on equal raycast `t` + equal k-NN distance (§4 pin #11). `validate()` whole-tree structural check asserts parent links + AABB enclosure + leaf level + size-in-[m,M] + handle-location consistency. Two-layer typing per ADR-0078 §5 D34: `rtree_queries_typed.hpp` strip-compute-retag wrappers (`AABB3<Length32>`, `RTreeRay3T<D, T>`, `RTreeRayHitT<D, T>`, `RTreeNeighborT<D, T>`). 17 cases / **first-overflow-fires-reinsert + second-overflow-fires-split** + brute-force overlap × 20 query boxes + raycast nearest + equal-`t` lowest-payload tiebreak + brute-force k-NN k∈{1,5,20} + insert/remove cycle handle stability + STR bulk-load brute-force xval × 10 query boxes + STR depth ≤ sequential + permutation determinism + NaN tolerance + sizing pin. 5-config DoD PASS: win-debug 2012/2012 + win-asan + win-shipping + win-shipping-profile 2007/2007 + win-tidy clean. Session log `docs/sessions/2026-05-16-geometry-v5c-rtree.md`. | ~1400 engine + ~570 tests | done |
| **v5d** ✅ 2026-05-16 | **Spatial hash (Teschner 2003) — particle/swarm broadphase + soft-body neighbour search.** New `spatial_hash.hpp` + `spatial_hash.cpp` in `engine/geometry-spatial/`. Hash function: `H(ix,iy,iz) = (ix·73856093) ^ (iy·19349663) ^ (iz·83492791)` mod POW2 bucket count (bit-mask `& (bucket_count-1)`); cell coords via signed-safe `static_cast<i32>(std::floor(v / cell_size))`. AABB stored to ALL overlapping cells (multi-cell membership = cost of O(1) cell lookup). Free-list-managed object pool with stable `SpatialHashObjectId` handle = pool slot. Each bucket = `Array<u32 obj_idx>` allocated lazily on first insert into that cell. **Elite-tier widening per user mandate** (phase doc spec was conservative — radius+overlap callbacks): (1) **Per-query generation-counter dedup** — `mutable u64 m_query_generation` bumped once per query; `obj.last_query_gen` compared on visit; **zero allocation per query** (no `seen` set); preserves `noexcept` query interface; (2) **Amanatides-Woo 1987 voxel raycast** (NOT recursive descent) — per-axis `step ∈ {-1,0,+1}` + `tDelta = |cell_size/dir|` + `tMax = parametric t at next cell boundary`; walk: scan cell, prune by `best_t`, advance to axis with smallest `tMax`; cap iterations at 2^20 cells; lowest-payload tiebreak on equal `t` (§4 pin #11); (3) **`find_overlapping_pairs(out)`** broadphase pair query (eylem v3 XPBD soft-body target) — visit each bucket, emit `(min,max)` AABB-overlapping pairs, cross-cell duplicates removed via `crd::containers::sort` + manual unique; (4) **Update fast-path** — same-cell-range detection, no rebucketing (returns `false`; ~95%+ small motions). **Determinism**: cells visited in canonical (z,y,x) loop order; pairs canonical `(a < b)`. Builder REJECTS non-finite + inverted (debug `CRD_ASSERT`); queries TOLERATE non-finite + zero-direction-ray (defensive `is_finite` short-circuit at API surface). Two-layer typing per ADR-0078 §5 D34: `hash_queries_typed.hpp` strip-compute-retag wrappers (`AABB3<Length32>`, `SpatialHashRay3T<D, T>`, `SpatialHashRayHitT<D, T>`). 21 cases / brute-force overlap × 20 + brute-force radius × 5×3 + multi-cell findable + Amanatides-Woo voxel raycast (nearest + diagonal + negative-direction + lowest-payload tiebreak + miss-nullopt + corner-grazing) + update fast-path (same-cell ⇒ no rebucketing) + slow-path (cell-change ⇒ rebucketed) + insert/remove cycle handle stability + `find_overlapping_pairs` brute-force xval + negative coords + NaN tolerance + dedup (8000-cell object emitted once per query). **v5d-fast caller-scratch overload shipped same session** (per user "elite, no deferring" mandate): `SpatialHashScratch` POD with per-scratch generation counter (`Array<u64> per_object_gen` + `current_gen` + overflow-protect); convenience and scratch overloads share `*_traverse_` template helpers parameterised over `WasVisited`/`MarkVisited` policy lambdas (provably equivalent — same dedup logic, same emission order); `find_overlapping_pairs` already const-safe, no scratch needed. **Concurrent-query test via `crd::jobs::parallel_for`** (`SpatialHashJobsListener` Catch2 listener inits 4-worker fiber pool, same pattern as `test_bvh_parallel.cpp`'s `BvhJobsListener`): 16 distinct queries × 25 iterations = 400 fan-out tasks across 16 jobs / 4 worker fibers, per-task fresh `TlsfAllocator` + `SpatialHashScratch`, `std::atomic<u32> mismatches == 0` under win-asan race detection. +6 scratch test cases. 5-config DoD PASS: win-debug 2040/2040 + win-asan + win-shipping + win-shipping-profile 2035/2035 + win-tidy clean. Session log `docs/sessions/2026-05-16-geometry-v5d-spatial-hash.md`. | ~850 engine + ~750 tests | done |
| **v5e** ✅ 2026-05-16 | **Dense `UniformGrid` — bounded-domain dense AABB index.** New `uniform_grid.hpp` + `uniform_grid.cpp` in `engine/geometry-spatial/`. Distinct from v5d SpatialHash: dense flat `Array<Array<u32>>` sized `nx·ny·nz` (no hash, no collision penalty, O(cell_count) memory). Construction: `(nx, ny, nz) = ceil((bounds.max-bounds.min)/cell_size)`; flat `cell_idx = (z·ny+y)·nx+x`. **Out-of-bounds AABBs CLAMP to grid** (insertion silently restricted to overlapping cells; handle remains valid). 256M-cell sanity cap (debug `CRD_ASSERT`) — past that, doc directs to `SpatialHash` for sparse domains. Per-cell `Array<u32>` lazily allocated; ~32 B overhead per empty cell from the Array itself. **Update fast-path** = same-cell-range detection. **Per-query generation-counter dedup** + **`UniformGridScratch` POD with per-scratch counter for thread-safe path SHIPPED FROM DAY 1** (no v5e-fast follow-on per "elite, no deferring" mandate); convenience and scratch overloads share `*_traverse_` template helpers parameterised over `WasVisited`/`MarkVisited` policy lambdas (provably equivalent). **Raycast = grid-bounds-clipped Amanatides-Woo voxel traversal**: slab-test ray vs grid AABB → entry-clip; clamp `t_entry ≥ 0`; compute start cell; per-axis step+tDelta+tMax; walk ALL-tied-axes-advance (corner-grazing safe) until cell idx leaves grid OR best_t pruning OR step cap (2²² cells). Lowest-payload tiebreak on equal `t` (§4 pin #11). **`find_overlapping_pairs(out)`** — dense linear cell scan, sort+unique cross-cell dedup. Builder REJECTS non-finite + inverted; queries TOLERATE non-finite + zero-direction (defensive `is_finite` short-circuit). Two-layer typing per ADR-0078 §5 D34: `grid_queries_typed.hpp`. **Concurrent test via `crd::jobs::parallel_for`** (shared `SpatialHashJobsListener` from v5d): 16 distinct queries × 25 iterations = 400 fan-out tasks across 16 jobs / 4 worker fibers, per-task fresh `TlsfAllocator` + `UniformGridScratch`, atomic mismatches == 0 under win-asan race detection. 26 cases — empty + cell-counts + out-of-bounds clamping + brute-force overlap × 20 + brute-force radius × 5×3 + 7 Amanatides-Woo raycast cases (nearest, lowest-payload tiebreak, negative direction, corner-grazing diagonal, miss-no-objects, ray-starts-outside-grid, slab-rejects-grid-miss) + update fast/slow paths + insert/remove cycle + `find_overlapping_pairs` brute-force xval + NaN tolerance + dedup-on-many-cell-object + scratch parity (overlap+radius+raycast) + concurrent-via-jobs. **One sizing pass en route**: first DoD pass surfaced 16 OOM failures on the test fixture's 4 MB allocator — diagnosis: `default_cfg(half=50, cs=1)` creates a 100³ = 1M-cell grid (~32 MB cells overhead). Fix: bump fixture to 64 MB + shrink default_cfg to 20³ + bump concurrency-corpus alloc to 32 MB + correct stale `nx==100` expectation + use larger explicit configs for tests requiring ±25 reach. Documented user contract: bounds matter, choose them sized for your data. 5-config DoD PASS: win-debug 2066/2066 + win-asan + win-shipping + win-shipping-profile 2061/2061 + win-tidy clean. Session log `docs/sessions/2026-05-16-geometry-v5e-uniform-grid.md`. | ~700 engine + ~700 tests | done |
| **v5-index-bringup** ✅ 2026-05-16 | **`SpatialBVHIndex` promoted from Phase 3.0 v1i no-op shell → real LooseOctree-backed component index in `crd-scene` — FIRST non-reserved spatial index.** New `engine/scene/spatial_bvh_index.{hpp,cpp}` + `IAabbExtractor` interface. **ADR-0053 day-one promise PRESERVED**: two-state design (unconfigured = identical no-op behaviour to the shell; configured via `world.find_index<SpatialBVHIndex>()->configure(extractor, octree_opts)` = real work). Existing Phase-3.0 user code with `register_component<X>(SpatialBVH{})` continues to run with no changes; opting in is one line. `crd-scene` gains PUBLIC link `crd-geometry-spatial`. **Canonical IAabbExtractor pattern** `extract(EntityId, ComponentId, const void* data)` (NOT just `(EntityId)`) — avoids the storage-migration-mid-commit `get_component` null-return hazard; `data` IS the freshly-installed bytes per IStorageEventSink contract. Storage event hooks: on_insert/on_update → extractor → LooseOctree insert/update with same-AABB-fit fast-path absorption (zero-cost no-op moves); on_remove per-component conservative no-op (entity may still carry other watched components); on_entity_destroyed definitively removes. **Update via UPSERT** (`world.add_component(e, new_value)`) is the ONLY way to refresh bounds — `get_component_mut` only bumps chunk-version, doesn't fire per-entity on_update; documented prominently. Storage handle = `u32 entity.index()` in octree payload + side `HashMap<u32, EntityId>` for O(1) query-result reconstruction (LooseOctree payload is u32-only). Query API: `overlap(AABB3<f32>, Array<EntityId>&)` + `raycast(Ray3<f32>) → optional<RayHit<EntityId>>`. Queries naturally const-safe (LooseOctree const-safe by construction + read-only HashMap iteration; no mutable state). Three debugging passes en route: (1) HashMap iterator uses `.key()`/`.value()` methods not `.first`/`.second`/`operator*` — refactored to use `find()` + reverse-lookup map for O(1) queries; (2) World API uses `spawn()`/`destroy_immediate()`/`get_component<T>(e)` not `create_entity`/etc; (3) extractor-mid-migration null fixed via data-pointer signature; (4) update-via-UPSERT not get_component_mut. 8 cases — auto-no-op-when-unconfigured + configure wires LooseOctree + overlap returns matching entities + UPSERT-update propagates + destroy drops entry + raycast nearest + raycast-unconfigured-nullopt + **concurrent overlap queries via `crd::jobs::parallel_for`** (16 queries × 25 iterations = 400 fan-out tasks across 16 jobs / 4 worker fibers, per-task isolated `TlsfAllocator` + output Array, atomic mismatches == 0 under win-asan race detection — proves naturally-const-safe claim empirically). 5-config DoD PASS: win-debug 2077/2077 + win-asan + win-shipping + win-shipping-profile 2072/2072 + win-tidy clean. Pattern locked: `(EntityId, ComponentId, void* data)` extractor signature + two-state index + LooseOctree backing + concurrent-jobs test becomes the template for future spatial extensions (`LightInfluenceIndex` Phase 3.5, `OcclusionIndex` Phase 3.5+, `AudioOcclusionIndex` Phase 3.4). Session log `docs/sessions/2026-05-16-geometry-v5-index-bringup.md`. | ~250 engine + ~430 tests | done |
| **v5-queries-extension** ✅ 2026-05-16 | **Extend the unified `crd::geometry::{raycast/overlap/radius/nearest_n/find_overlapping_pairs}` facade to dispatch over the 5 v5 backends.** New header `engine/geometry-spatial/include/crd/geometry/spatial/queries.hpp` (header-only, ~250 LOC). Sits ALONGSIDE `crd-geometry-bvh`'s `crd/geometry/queries.hpp` — both facades in `crd::geometry` namespace; ADL unifies them when both are included. Avoids reverse-dep (`crd-geometry-bvh` doesn't depend on `crd-geometry-spatial`). **Compile-time overload polymorphism** per ADR-0076 §16 pin #1 + v1i-a precedent — no virtual, no IAcceleration vtable, zero overhead. **Support matrix** (don't fake what doesn't fit): KdTree (k-NN + radius), LooseOctree (overlap + raycast), RTree (overlap + raycast + k-NN), SpatialHash (overlap + radius + raycast + find_overlapping_pairs), UniformGrid (overlap + radius + raycast + find_overlapping_pairs). Result types pass through unchanged (heterogeneous shapes are honest API surface) — first-arg-type overload resolution picks per-backend signature. Both callback (`template<typename Fn>`) AND Array-sink forms. **CONVENIENCE overloads only** — no scratch overloads (scratch is backend-specific knob that doesn't generalise; consumers needing parallel-query thread-safety call native scratch-taking overload per `feedback_spatial_substrate_thread_safety.md`). Forwards-only impl preserves each backend's documented determinism + thread-safety contract verbatim. 16 cases / facade-parity tests vs direct backend calls (LooseOctree overlap+raycast, RTree overlap+raycast+nearest_n, SpatialHash overlap+radius+raycast+pairs, UniformGrid overlap+radius+raycast+pairs, KdTree radius+nearest_n) + cross-backend uniformity demo (same `crd::geometry::overlap` name across 4 AABB backends returns same set on same content). 5-config DoD PASS: win-debug 2093/2093 + win-asan + win-shipping + win-shipping-profile 2088/2088 + win-tidy clean. Session log `docs/sessions/2026-05-16-geometry-v5-queries-extension.md`. | ~250 engine + ~450 tests | done |
| **v5-close** ✅ 2026-05-16 | **`-spatial` cluster CLOSED.** ADR-0076 §20 amendment (20 locked substrate decisions) + `docs/systems/geometry-spatial.md` system documentation + 18-config full sweep PASS + ROADMAP/context/MEMORY sync. Sandbox SpatialQueries viz scene deferred to focused phase-segment session. Cluster totals: 8 slices · ~4900 LOC engine + ~4330 LOC tests · 5 spatial backends · cross-backend ASan race coverage 2000 fan-out tasks · unified `crd::geometry::*` facade now dispatches over 8 backend types (5 spatial + 3 BVH). Session log `docs/sessions/2026-05-16-geometry-v5-close.md`. | docs + sweep | done |
| **v6a** ✅ 2026-05-16 | **`crd-geometry-polygon` substrate types + predicates.** New `engine/geometry-polygon/` module (7th `crd-geometry-*` sub-module). `Ring2<T>` (non-owning view; closed-ring `next`/`prev` wraparound), `PolygonView2<T>` (multi-ring view; ring_offsets prefix-sum, size `ring_count+1` matching `ConvexHullView::face_vertex_offsets` v1h pin), `Polygon2<T>` (owning, debug-assert finite-input at `add_ring`). **Winding convention LOCKED**: outer ring CCW + hole rings CW (matches Clipper2 + Vatti 1992 + Eberly 1999). Predicates: `signed_area` (shoelace via f64 accumulator regardless of T), `centroid` (Bourke 1988 area-weighted + arithmetic-mean fallback for zero area), `aabb` (with +∞/−∞ empty sentinel matching §15 convention), `is_ccw` / `is_cw`, `ensure_orientation` (auto-reverse mismatched rings — explicit helper for raw input, builders never silently re-orient), `point_in_ring` / `point_in_polygon` (Hormann-Agathos 2001 crossing-number + Shewchuk `orient2d` adverse for boundary cases), `is_simple` (O(n²) brute-force; v6e Bentley-Ottmann is the O(n log n) replacement when n ≥ ~64). Two-layer typing per ADR-0078 §5 D34: `polygon_predicates_typed.hpp` strip-compute-retag wrappers (`Vec2<Length32>` boundary, raw f32/f64 inside; `signed_area` returns typed Area `Quantity<DimMul<D,D>, T>`). `polygon_substrate.cpp` static_assert pins the bit-identical-layout invariant between `Vec2<Quantity<D,T>>` and `Vec2<T>` (ADR-0078 §2 D2). 39 cases / 151 assertions. 4-config DoD PASS. | ~700 + ~600 tests | done |
| **v6b** ✅ 2026-05-16 | **Ear clipping triangulation with hole support — Meisters 1975 classical O(n²) + Eberly 1999 cut-and-join hole bridging.** `triangulate_ear_clip(Ring2)` for simple polygons + `triangulate_ear_clip(PolygonView2)` for polygons-with-holes. **Algorithm**: doubly-linked vertex list via twin `Array<u32>` next/prev (no STL list per PRINCIPLES.md); convex/reflex classification per vertex via Shewchuk `orient2d` (collinear → reflex; never ears); ear pick = smallest current vertex index (lex-tuple tiebreak → byte-identical output across compilers / SIMD widths / OSes). **Hole bridging (Eberly)**: holes processed in DESCENDING max-x order. For each hole: find rightmost vertex M (max-x; max-y tie; min-idx tie); cast +X ray to find closest outer edge; pick Eberly candidate (larger-x endpoint, refined by any reflex vertex inside the search triangle); insert doubled-bridge `[..., V, M, hole-rotated-from-M, M, V, ...]` — the two coincident (V, M) edges become zero-width slivers that ear-clipping eats naturally. 7-state diagnostic enum (EmptyPolygon / NonSimpleOuter / NonSimpleHole / HoleBridgingFailed / BridgedSelfIntersect / NonFiniteInput / Ok). 14 cases / 50 assertions: unit square / triangle / L-shape / 3-toothed comb / 5-point star / 16-gon convex / square-with-hole / square-with-2-holes / f64-large-coord / vertex-rotation determinism / 4 diagnostic statuses / area-conservation invariant. 4-config DoD PASS. | ~900 + ~700 tests | done |
| **v6c** ✅ 2026-05-16 | **Constrained Delaunay Triangulation — Bowyer-Watson + Domiter-Zalik carve-and-retriangulate constraint recovery.** `constrained_delaunay(points, constraints)` generic PSLG entry + `constrained_delaunay(PolygonView2)` convenience (with even-odd centroid in/out filter to strip in-hole triangles). **Phase 1** (Bowyer-Watson, Bowyer 1981 / Watson 1981): Shewchuk `incircle` adaptive for bad-triangle detection + lex-sorted (x, y, original-index) insertion order for determinism + super-triangle at 1000× bbox scale (matches Triangle/CGAL empirically-stable scaling) + point location via jump-walk from `hint_tri` (O(√n) per insertion in practice) + cavity expansion BFS in monotonic triangle-ID order + per-cell re-triangulation by fanning from new vertex. **Phase 2 (constraint recovery)**: fast-path `find_edge(va, vb)` → mark constrained both sides; else trace chain via apex-side `orient2d` (segment-to-vertex side test determines exit edge per chain triangle); **Domiter-Zalik 2008 carve-and-retriangulate** for arbitrary multi-flip including non-convex-quad chains — partition chain polygon into upper/lower sub-polygons split by constraint, free chain triangles, ear-clip each sub-polygon, re-link outer neighbours via canonical-edge match. Anglada 1997 single-flip is the fast path; carve-and-retriangulate the robust fallback. **Phase 3** (Lawson 1977 post-flip Delaunay restoration): scan every non-constrained edge; if `incircle > 0` for the neighbour's apex, flip; re-queue 4 surrounding edges. 7-state diagnostic enum (TooFewPoints / NonFiniteInput / DuplicatePoint / ConstraintOutOfBounds / ConstraintsCrossing / InternalInvariant / Ok). **Mid-implementation bug-fix**: flip's convexity check originally used wrong orient2d args (silently rejected valid flips). Correct test: `orient(pa, pd, pc) > 0` AND `orient(pd, pb, pc) > 0` for new T'=(va,vd,vc) + U'=(vd,vb,vc) to be CCW. 14 cases / 410 assertions: 3-pt / 4-pt square / 32-pt random + every-tri-CCW + empty-circumcircle invariant + constraint-already-edge fast path + interior-cut single-flip + long-constraint-crossing-multiple-edges multi-flip carve-retriangulate + polygon-with-hole in/out filter + 4 diagnostic statuses + f64 1e6-coord stability + insertion-order determinism. 4-config DoD PASS. | ~1400 + ~700 tests | done |
| **v6d** ✅ 2026-05-16 | **Vatti polygon Boolean — 4 ops via planar-subdivision + winding-number face classification.** `polygon_boolean(subject, clip, op)` + 4 convenience overloads (`polygon_union` / `polygon_intersect` / `polygon_difference` / `polygon_xor`). Multipath + holes + 4 Boolean ops + EvenOdd + NonZero fill rules. **Algorithm strategy** (per ADR-0076 §21 pin #10 — chose planar-subdivision over Vatti scanbeam): (1) collect every directed edge of subject + clip with winding-contribution sign (+1 for CCW outer, -1 for CW hole, 0 for OTHER polygon); (2) find pairwise intersections (brute-force O(n×m) — v6e Bentley-Ottmann is the natural drop-in upgrade for >10k vertices); (3) build vertex table via lex-sort + coalesce on exact coordinate equality; (4) build half-edge DCEL (twin pairs); at each vertex sort outgoing half-edges CCW by angle (quadrant + orient2d comparator); wire `next_in_face` per DCEL convention (`twin(o_k).next = o_{(k+n-1) % n}` — CW-predecessor); (5) walk each face boundary loop via `.next_in_face`; (6) for each loop pick sample point on the LEFT of each directed edge (midpoint + perpendicular-LEFT offset — inside the loop's face for ANY orientation); (7) ray-cast +X from sample, count signed crossings of subject + clip input edges; (8) apply Boolean predicate per fill rule; (9) for each kept loop emit vertex sequence as output ring — CCW loops → outer rings, CW loops → hole rings (natural v6 winding-convention mapping); (10) optional `clean_output` (consecutive collinear vertex removal + duplicate dedup) gated by opts. **Mid-implementation bug-fix**: face-walking originally treated each LOOP as distinct face and SKIPPED CW loops (assumed unbounded-face boundaries) — broke polygon-with-hole output. Fixed by emitting every kept LOOP independently. **Determinism**: lex-tuple ordering throughout, output sorted before return. **Robustness**: Shewchuk `orient2d` everywhere, vertex-on-vertex + edge-coincident + shared-corner all flow through adaptive path. 13 cases / 47 assertions: 4 ops on disjoint / overlapping (with classic Boolean area invariants A(U)+A(I)=A(S)+A(C)) / subject-contains-clip / clip-contains-subject / polygon-with-hole vs covering clip / shared-corner / shared-edge / empty-operand diagnostic / f64-1e3-coord. 4-config DoD PASS. | ~750 + ~350 tests | done |
| **v6e** ✅ 2026-05-16 | **Bentley-Ottmann 1979 line-segment intersection — full O((n+k) log n) sweep-line.** `bentley_ottmann(segments)` + short-circuit `bentley_ottmann_any` variant for `is_simple`-style usage. **Sweep-line over Y** (bottom-to-top): (1) **event queue** = min-heap keyed by lex (y, x, kind, seg-a, seg-b); uses `crd::containers::push_heap` / `pop_heap` for bit-exact ordering across compilers; (2) **status structure** = sorted `Array<u32>` of active segments by X-at-sweep-Y; O(log n) binary-search insertion + linear-shift insert/erase (acceptable for typical n < 1k); (3) **3 event kinds** with canonical 1979 ordering Start < Intersection < End at coincident (y, x); (4) **horizontal segments** extracted into secondary brute-force pass (clean separation from X-sorted status); (5) **deduplication on canonical pair-key** (`(min(a,b), max(a,b))`) prevents re-reporting after status swaps. **Robustness**: Shewchuk `orient2d` for the segment-segment intersection helper — transverse + endpoint-on-interior + vertex-on-vertex + collinear-overlap-endpoint all detected via adaptive sign + on-segment-closed check. **Determinism**: output sorted by lex (point.y, point.x, seg_a, seg_b) before return. 3-state diagnostic enum (Ok, NonFiniteInput, DegenerateSegment). 13 cases / 37 assertions: empty / disjoint / two-crossing-segments / 4-segment # pattern (4 intersections) / T-junction / parallel non-intersecting / **5×5 grid of 25 intersections** (dense stress) / short-circuit `_any` (true on first hit, false on no intersection) / degenerate / non-finite / f64-1e6-coord / lex-sort output determinism. 4-config DoD PASS. | ~600 + ~400 tests | done |
| **v6-close** ✅ 2026-05-16 | **`-polygon` cluster CLOSED.** ADR-0076 §21 amendment (20 locked substrate decisions + slice ledger + cluster cross-validation + complexity table) + `docs/systems/geometry-polygon.md` system documentation (when-to-use matrix, architecture diagram, API stencils, determinism + robustness contracts, performance pins, two-layer typed architecture, integration touch-points across crd-font / crd-eda / crd-eylem navmesh / crd-resources cooker / crd-renderer decals / editor sketch tool, comprehensive references) + 18-config full sweep PASS (13:52 elapsed). Cluster totals: 6 slices · ~4350 LOC engine + ~2750 LOC tests · 92 cases / 695 assertions · 20 locked substrate decisions. Session log `docs/sessions/2026-05-16-geometry-v6-close.md`. | docs + sweep | done |
| **v7a** ✅ 2026-05-16 | **`HalfEdgeMesh<T>` substrate** — the mutable mesh data structure every v7 algorithm consumes. New module `engine/geometry-mesh-processing/` (8th `crd-geometry-*` sub-module, mutable side of geometry; v4 `-mesh` is the read-only side). Per-half-edge `{origin, twin, next, face}` (16 B; `static_assert`-pinned, no flag byte — `origin == k_null_vertex` is the dead sentinel) + per-vertex `{position, outgoing, flags}` + per-face `{first_he, flags}`. Free-list-managed pools with stable handles (alive-bit per slot; LIFO pop). `build_from(positions, indices)` constructs from indexed triangle mesh — **lex-tuple twin pairing** `(min(va,vb), max(va,vb), he_id)` sort + adjacent-pair-sweep (bit-identical across MSVC / GCC / clang per ADR-0063); **materialised boundary half-edges** (one per unpaired edge, `face == k_null_face`) so boundary loop traversal is the same `.next` walk as a face loop, branchless on the twin pointer; detects non-manifold edges (`BuildStatus::NonManifoldEdge`); rejects degenerate triangles + non-finite input + out-of-bounds index + odd index count. `to_indexed(out_positions, out_indices, out_old_to_new=nullptr)` extracts back. Atomic edit ops: **`collapse_edge(he, new_pos)`** (Edelsbrunner 2001 link-condition gate — 1-rings of `a` and `b` share EXACTLY the two opposite apex vertices; rejects boundary-side collapse for v7a, deferred to v7f manifoldness repair), **`split_edge(he, new_pos)`** (1 new vertex + 4 new HEs + 2 new faces; atomic — partial state never visible), **`flip_edge(he)`** (diagonal swap with face/HE rewiring; rejects boundary + duplicate-edge cases). Topology queries: `vertex_count` / `face_count` / `edge_count` / `is_manifold` (per-vertex fan-walk count vs slot-scan count) / `is_closed` / `euler_characteristic` / `boundary_loop_count`. Walk helpers: `for_each_outgoing_he(v, fn)` (**uniform CW rotation** `cur.twin.next` — works the same for interior and boundary; boundary vertices start at the boundary HE so the walk visits the full half-fan before wrapping back) + `for_each_face_he(f, fn)`. Two-layer typing per ADR-0078 §5 D34: raw `<MathScalar T>` algorithm bodies + typed `Vec3<Length32>` consumers via strip-compute-retag wrappers (deferred to first typed consumer). Explicit instantiations for `f32` + `f64`. 14 Catch2 cases / 67 assertions. 4-config DoD PASS (`scripts/per-slice-check.ps1`, elapsed 03:51). Session log `docs/sessions/2026-05-16-geometry-v7a-half-edge-mesh.md`. Pinned design decisions D1-D8 carried for ADR-0076 §22 amendment at v7-close. | ~700 + ~500 tests | done |
| **v7b** ✅ 2026-05-17 | **Quadric Edge Collapse Decimation (Garland-Heckbert 1997 + Garland 1998 boundary preservation).** New `Quadric<T>` (symmetric 4x4 stored as 10 unique floats; layout pinned; outer-product `from_plane` ctor + `evaluate(q, v)` expanded form + `optimal_position(q)` 3x3 Cramer inverse with midpoint fallback). New `qem_decimate(const HalfEdgeMesh<T>&, options) -> HalfEdgeMesh<T>` — input untouched; constructs fresh output via `to_indexed → build_from` on requested allocator. **Boundary preservation** (Garland 1998 §3.1: heavy `n_b = edge × n_f` wall plane on every boundary HE — silhouette can't implode; default weight 1000). **Inversion prevention** (predict-then-collapse: substitute `v_opt` for the merged + other endpoints; check no incident face flips orientation; rejects self-intersection — Hoppe 1993 / standard production practice). **Locked vertices** (cooker LOD anchor primitive; 1-locked constrains `v_opt` to locked endpoint; both-locked rejected; positions bit-identical). Lazy-invalidation min-heap via `crd::containers::push_heap`/`pop_heap` keyed lex `(cost, canonical_he_id)` for byte-identical determinism across compilers (per-canonical-HE generation counter, bump-before-push, pop checks alive + generation match + non-boundary). Stop conditions: `target_face_count` OR `max_error_threshold` (at least one required, else status = `NoStopCondition`). Reports `Ok` / `NoStopCondition` / `NonManifoldInput` / `EmptyMesh` / `TargetUnreachable` + telemetry (`collapses_applied` / `collapses_rejected_link` / `collapses_rejected_flip` / `singular_fallbacks`). Two-layer typing per ADR-0078 §5 D34: raw `<MathScalar T>` algorithm body; typed `Vec3<Length32>` wrappers deferred to first typed consumer. Explicit instantiations for `f32` + `f64`. 15 cases / 92 assertions (7 Quadric + 8 qem_decimate). 4-config DoD PASS (`scripts/per-slice-check.ps1`, elapsed 03:24). Session log `docs/sessions/2026-05-17-geometry-v7b-qem-decimation.md`. Pinned design decisions D9-D16 carried for ADR-0076 §22 amendment at v7-close. | ~800 + ~600 tests | done |
| **v7c** ✅ 2026-05-17 | **Loop subdivision (Loop 1987).** Each triangle splits 1→4 via edge-midpoint insertion + smoothing-mask position update. C² limit surface almost everywhere (C¹ at extraordinary vertices). New `loop_subdivide(const HalfEdgeMesh<T>&, opts) -> HalfEdgeMesh<T>` — input untouched. **Interior edge mask**: `3/8·A + 3/8·B + 1/8·C + 1/8·D` (Loop 1987 §2.2; C, D = apex of incident faces). **Boundary edge mask**: `(A + B)/2` (cubic-B-spline limit position). **Interior vertex mask**: `(1 - n·β)·V + β·Σ neighbours` with `β = (1/n)·(5/8 - (3/8 + 1/4·cos(2π/n))²)` — uses **`crd::math::deterministic::cos`** for bit-identical FP across compilers/architectures per ADR-0063 (Cephes-style polynomial — same result on MSVC/clang-cl/GCC/x64/ARM64). **Boundary vertex mask**: `3/4·V + 1/8·u_left + 1/8·u_right` (cubic-B-spline boundary curve refinement). **Sub-triangle CCW emission** (4 per face): corner@v₀ (v₀, m₀₁, m₂₀), corner@v₁ (v₁, m₁₂, m₀₁), corner@v₂ (v₂, m₂₀, m₁₂), central (m₀₁, m₁₂, m₂₀). **Indexed-form pipeline** per-level (extract → temp HE mesh build → emit new positions+indices → repeat), final `build_from` creates output. Multi-level via `n_levels` option; `n_levels = 0` returns clone-of-input. **Boundary-neighbour detection** uses v7a's CW fan walk: for each outgoing HE at v, edge is boundary iff `he_is_boundary(ho) OR he_is_boundary(twin(ho))` — guarantees exactly 2 hits per 2-manifold boundary vertex. Reports `Ok` / `EmptyMesh` / `NonManifoldInput` + telemetry. Two-layer typing per ADR-0078 §5 D34: raw `<MathScalar T>` body. Explicit instantiations for `f32` + `f64`. 9 cases / ~70 assertions. 4-config DoD PASS (`scripts/per-slice-check.ps1`, elapsed 03:47). Session log `docs/sessions/2026-05-17-geometry-v7c-loop-subdivision.md`. Pinned design decisions D17-D25 carried for ADR-0076 §22 amendment at v7-close. | ~500 + ~400 tests | done |
| **v7d** ✅ 2026-05-17 | **Isotropic remeshing (Botsch-Kobbelt 2004 §4).** New `isotropic_remesh(const HalfEdgeMesh<T>&, opts) -> HalfEdgeMesh<T>` — input untouched. **4-pass-per-iteration loop**: (1) **split** edges > `(4/3)·L` at midpoint; (2) **collapse** edges < `(4/5)·L` gated by link condition + **post-collapse safety pre-check** (no resulting edge to merged-vertex neighbours may exceed `(4/3)·L`, prevents oscillation); (3) **flip** interior edges toward target valence (6 interior / 4 boundary), gated by total-deviation reduction AND **`vertices_connected(c, d)` duplicate-edge pre-check** (v7a's `flip_edge` doesn't gate on c-d-already-connected — would create non-manifold; D36); (4) **tangential smoothing** (Botsch-Kobbelt §4 actual spec): displacement decomposed via area-weighted vertex normal, only tangential component applied (`d_t = d - (d·n)·n`), then projected back to ORIGINAL input surface via f32 BVH closest-point query (precision-cast at boundary; D37). **Inversion-rejection safeguard** — if move would flip any incident face's orientation, keep v fixed (Hoppe-style; D38). **Per-pass canonical-HE snapshot** (Botsch-Kobbelt iteration discipline: new HEs created within a pass NOT re-processed in same pass). Jacobi-style smoothing via new `HalfEdgeMesh::set_vertex_position` setter. Multi-iteration via `n_iterations` option; `n_iterations = 0` returns clone-of-input. Module deps EXTENDED: `crd-geometry-mesh-processing` now PUBLIC-links `crd-geometry-bvh` + `crd-geometry-mesh`. Reports `Ok` / `EmptyMesh` / `NonManifoldInput` / `InvalidTargetLength` + telemetry. Two-layer typing per ADR-0078 §5 D34: raw `<MathScalar T>` body. Explicit instantiations for `f32` + `f64` (BVH stays f32; f64 path casts at query boundary, precision loss bounded by f32 ulp × surface scale). 9 cases / ~70 assertions. **2 mid-slice bugs found by bisection** (D36 flip-creates-duplicate-edge + D37/D38 naïve-Laplacian-smoothing-degenerates-at-projection) — root-caused via disable-projection → disable-smoothing → bisect. 4-config DoD PASS on retry (`scripts/per-slice-check.ps1`, elapsed 03:13); first attempt hit transient `bugprone-reserved-identifier` clang-tidy access violation (upstream LLVM bug; `feedback_transient_clang_tidy_crash.md` policy filed, same class as MSVC LTCG ICE). Session log `docs/sessions/2026-05-17-geometry-v7d-isotropic-remesh.md`. Pinned design decisions D26-D38 carried for ADR-0076 §22 amendment at v7-close. | ~600 + ~500 tests | done |
| **v7e** ✅ 2026-05-17 | **Liepa 2003 hole filling.** New `fill_holes(const HalfEdgeMesh<T>&, opts) -> HalfEdgeMesh<T>` — input untouched. **Algorithm**: (1) boundary loop detection via boundary-HE `.next` traversal with visited bitmap (D39); (2) per-loop outside-mesh face normal precomputation (D40); (3) **Liepa O(N³) DP min-weight triangulation** with `ω(T) = area(T) + λ · sum_of_dihedral_penalties_over_T's_edges` (D41) — phantom-edge dihedral DEFERRED at sub-DP, resolved at parent merge or against closing-edge outside normal at top level (D42); (4) recursive triangulation reconstruction from optimal-split table; (5) patch attachment via `to_indexed → append → build_from` single rebuild (D45). **Patch triangle vertex order** `(b_i, b_m, b_k)` with `i < m < k` is CCW from OUTSIDE the surrounding mesh — outward normals consistent with rest of mesh (verified via signed-volume divergence-theorem check on filled cubes; D44). Reports `Ok` / `EmptyMesh` / `NonManifoldInput` / `NoHolesToFill` + telemetry (holes_detected / holes_filled / holes_skipped_too_large for loops > `max_hole_size` / triangles_added). Determinism via lex-tiebreak on smaller split index (D43). Two-layer typing per ADR-0078 §5 D34: raw `<MathScalar T>` body. Explicit instantiations for `f32` + `f64`. **Liepa §4 Steiner refinement + §5 Laplacian fairing SHIPPED same-session in v7e-refine re-open** (after user scope-check — "optional" phase-doc language overridden by "elite" user mandate). §4: per-loop σ from input-mesh edges (D47); too-coarse `α·dist > σ AND > σ_avg` test (D48, α=√2 default); centroid Steiner insertion at index level + Delaunay flip pass on interior edges using `∠acb + ∠adb > π` via `crd::math::deterministic::acos` (D49) + v7d's D36 duplicate-edge guard; convergence loop (D51). §5: Jacobi Laplacian smoothing on Steiner vertices only, loop CLAMPED (D50). 3 extra tests for refine/fairing. 12 cases / ~90 assertions: empty / no-holes / 3-vertex-hole (cube-minus-1-tri, refine=off) / 4-vertex-hole (cube-minus-1-face, refine=off) / 4-vertex-hole refine=on convergence / 4×4-grid-hole refine triggers Steiner / fairing iters match on refined patch / 2-separate-holes / max_hole_size guard / determinism / f64 / signed-volume orientation. One MSVC `C4267 (size_t → u32)` fixed mid-pass. 4-config DoD PASS first try TWICE (v7e initial §3-only elapsed 03:41 + v7e-refine same-session §3+§4+§5 elapsed 03:22). Session log `docs/sessions/2026-05-17-geometry-v7e-fill-holes.md` (with v7e-refine same-session update). Pinned design decisions D39-D51 carried for ADR-0076 §22 amendment at v7-close. | ~500 + ~400 tests | done |
| **v7f** ✅ 2026-05-17 | **Manifoldness repair.** New `repair_manifoldness(const HalfEdgeMesh<T>&, opts) -> HalfEdgeMesh<T>` — input untouched. **Phase A (D52-D54)**: non-manifold edge detection + repair via index-level operation with `crd::containers::HashMap<u64, Array<u32>>` keyed on packed `(min(u,v), max(u,v))` u64 → pair triangles by orientation (forwards: origin < dest; backwards: origin > dest) → first pair keeps original endpoints, subsequent pairs share a single duplicated `min(u,v)` vertex, singletons (orientation imbalance) get individual duplicates. **Phase B (D55-D57)**: bowtie vertex detection via TEMP HalfEdgeMesh + v7a CW fan-walk per vertex (walk_count != slot_count → bowtie) + union-find on triangle-share-edge-through-v adjacency BFS → lowest-min-triangle-id fan keeps `v`, each other fan gets duplicated vertex. **Phase A → Phase B sequence** (D58 — Phase B requires manifold-edge precondition); **two phases CAN CASCADE** (Phase A vertex duplication can disconnect a previously-connected fan structure → Phase B detects + repairs; tests verify this with explicit Phase-A-only counts vs full-A+B counts). Reports `Ok` / `EmptyMesh` / `AlreadyManifold` (D59) + telemetry. Two-layer typing per ADR-0078 §5 D34: raw `<MathScalar T>` body. Explicit instantiations for `f32` + `f64`. 9 cases / ~70 assertions. 4-config DoD PASS first try (`scripts/per-slice-check.ps1`, elapsed 03:45). Session log `docs/sessions/2026-05-17-geometry-v7f-repair-manifoldness.md`. Pinned design decisions D52-D59 carried for ADR-0076 §22 amendment at v7-close. | ~400 + ~350 tests | done |
| **v7g** ✅ 2026-05-17 | **Self-intersection removal.** New `remove_self_intersections(const HalfEdgeMesh<T>&, opts) -> HalfEdgeMesh<T>` — input untouched. **3 phases**: (1) Broadphase = brute-force O(n²) AABB-overlap + share-vertex filter (D60 — phase-doc BVH broadphase deferred to v7g-followon since static BvhTree lacks find_overlapping_pairs). (2) Narrowphase = Möller 1997 triangle-triangle test with `crd::geometry::primitives::orient3d` exact-sign gating (D61); touch-only cases (≥1 orient3d zero + same-sign rest, or ≥2 zeros) explicitly rejected as not transversal cuts. (3) Per-triangle CDT retriangulation = drop-largest-normal-axis 2D projection + flip-winding-if-normal-negative (D63) + epsilon-dedup for 3-triangle-common-edge case (D64) + `crd::geometry::polygon::constrained_delaunay` + centroid-inside-T sanity filter (D66, strips super-triangle artifacts) + 3D lift via plane equation. **Cross-triangle vertex stitching** (D62): per-pair endpoints created ONCE and shared between T_i and T_j → output 2-manifold along the cut. **Graceful CDT-failure degradation** (D65): keep original tessellation on ConstraintsCrossing. Reports `Ok` / `EmptyMesh` / `NoSelfIntersections` + telemetry. Module dep EXTENDED: now PUBLIC-links `crd-geometry-polygon`. Two-layer typing per ADR-0078 §5 D34: raw `<MathScalar T>` body. Explicit instantiations for `f32` + `f64`. 5 cases / ~25 assertions. **Mid-slice bug fix**: cube test initially reported false-positive intersections (axis-aligned faces produce many `orient3d==0` cases that naïve sign comparison mishandled); fix = explicit touch-only rejection in orient3d gate. 4-config DoD PASS on retry (transient clang-tidy crash on first attempt, per `feedback_transient_clang_tidy_crash.md` policy; retry elapsed 03:16). Session log `docs/sessions/2026-05-17-geometry-v7g-remove-self-intersections.md`. Pinned design decisions D60-D66 carried for ADR-0076 §22 amendment at v7-close. | ~700 + ~500 tests | done |
| **v7h** ✅ 2026-05-17 | **Taubin smoothing (Taubin 1995) — volume-preserving Laplacian.** New `taubin_smooth(const HalfEdgeMesh<T>&, opts) -> HalfEdgeMesh<T>` — input untouched. **2-pass alternation per iteration**: Pass 1 shrink with λ > 0 (`v ← v + λ·L(v)`), Pass 2 un-shrink with μ < 0 (`v ← v + μ·L(v)`); L = uniform umbrella operator `mean(neighbours) - v` (D71). Default (λ=0.5, μ=-0.53) targets Taubin's wide-pass-band filter (k_PB ≈ 0.113). Boundary clamp by default (D70); when disabled, boundary vertices smooth via boundary-only 1-ring (matches v7c cubic-B-spline-mask intent). Boundary flag cached ONCE at entry (D68 — Taubin doesn't change topology). Jacobi-style atomic update per pass via `set_vertex_position` (D69). `InvalidParameters` rejects λ ≤ 0 + μ ≥ 0 but allows `|μ| ≤ λ` (D72). Clone-and-mutate pattern (D67). Reports `Ok` / `EmptyMesh` / `NonManifoldInput` / `InvalidParameters` + telemetry. Two-layer typing per ADR-0078 §5 D34: raw `<MathScalar T>` body. Explicit instantiations for `f32` + `f64`. 8 cases / ~80 assertions. **Mid-slice test fix**: initial volume-drift assertion was too tight on raw 8-vertex cube (Taubin's frequency analysis assumes smooth manifolds, not sharp-corner cubes — drift was 93%); revised to subdivide first via Loop then assert Taubin drift STRICTLY less than pure-Laplacian drift (the algorithm's hallmark). 4-config DoD PASS first try (`scripts/per-slice-check.ps1`, elapsed 03:41). Session log `docs/sessions/2026-05-17-geometry-v7h-taubin-smooth.md`. Pinned design decisions D67-D72 carried for ADR-0076 §22 amendment at v7-close. | ~300 + ~300 tests | done |
| **v7-close** ✅ 2026-05-17 | **`-mesh-processing` cluster CLOSED.** ADR-0076 §22 amendment locking all 72 design decisions D1-D72 indexed by slice + `docs/systems/geometry-mesh-processing.md` system documentation + **18-config full sweep PASS** (`scripts/full-sweep.ps1`, elapsed 15:17): 11 Windows + 7 Linux configs all green. ROADMAP / context.md / phase doc / MEMORY all synced. **12 real bugs fixed mid-close** caught by wider corpus the per-slice 4-config DoD missed: 8 `T{double_literal}` narrowings (`-Wfloat-conversion -Werror` on gcc-linux; fixed via `static_cast<T>(literal)`); 1 multi-line-comment ASCII art (`-Wcomment -Werror` on gcc); 1 unused variable (`-Wunused-but-set-variable -Werror` on clang-cl). 2 transient compiler crashes (MSVC LTCG ICE + clang-tidy AV) auto-cleared on retry per existing policies. **Carryover lesson**: gcc-linux warnings-as-errors fire on `T{double}` patterns that MSVC silently accepts; use `static_cast<T>(literal)` for non-exact-representable defaults + constexpr in `<MathScalar T>` template code from now on. Cluster totals: 8 algorithm slices + 1 substrate + 1 close · ~4200 LOC engine + ~2830 LOC tests · 80 cases / 1180 assertions · 72 locked design decisions in ADR-0076 §22. Session log `docs/sessions/2026-05-17-geometry-v7-close.md`. | docs + sweep | done |
| ~~Original v7a–v7g row~~ | (Replaced 2026-05-16 by the 9-slice substrate-first plan above — v7a renamed to `HalfEdgeMesh` substrate; original algorithms shifted to v7b–v7h; same pattern as v6's 4→6 substrate separation.) | — | — |
| **v8a** ✅ 2026-05-17 | **New `engine/geometry-delaunay/` module + 2D Bowyer-Watson pure Delaunay.** New `delaunay_2d(ConstSpan<Vec2<T>>, IAllocator*) -> DelaunayResult2<T>` entry. **Algorithm**: super-triangle at 1000× bbox (D74) + lex-sort `(x, y, original_index)` (D75) + jump-walk point location via apex-side `orient2d` w/ deterministic edge-cross tiebreak (D76) + cavity BFS via Shewchuk adaptive `incircle` (already full Stage D, v8a paydown done) collecting bad-circumcircle triangles (D77) + cavity boundary edge collection (D78) + re-triangulation by fanning from inserted vertex w/ neighbour rewiring O(K²) second pass (D79) + strip-time emission filtering super-tri vertices in slot order (D80). Internal `Tri` slot 12 bytes + alive flag w/ LIFO free-list pool (D73). Reports `Ok` / `TooFewPoints` / `NonFiniteInput` / `DuplicatePoint` / `InternalInvariant`. Two-layer typing per ADR-0078 §5 D34. Explicit `f32` + `f64` instantiations. v6c CDT in polygon module unchanged (optional v8-close follow-on can refactor it to consume v8a). 10 cases / 29 assertions: 3 diagnostics + single-triangle + square + pentagon + 32-pt random cloud (CCW + empty-circumcircle invariant verified per-triangle) + insertion-order determinism + large-coord 1e6 f32 + f64. One mid-build error fixed (`<array>` missing). 4-config DoD PASS first try (`scripts/per-slice-check.ps1`, elapsed 03:44). Session log `docs/sessions/2026-05-17-geometry-v8a-delaunay-2d.md`. Pinned design decisions D73-D80 carried for ADR-0076 §23 amendment at v8-close. | ~600 + ~400 | ~3-5 days |
| **v8b** ✅ 2026-05-17 | **2D Bowyer-Watson with Hilbert-curve insertion order (delaunator-style).** Original "v8b divide-and-conquer Guibas-Stolfi 1985" plan re-targeted at user-choice scope-check (option B) — the production-industry-standard path (Mapbox delaunator, Sandia, libigl, CGAL `spatial_sort`). Hilbert space-filling curve (Hilbert 1891 + Skilling 2004 iterative `xy2d`) gives spatial locality so jump-walks from the hint converge in O(1) average steps; total cost O(n log n) dominated by Hilbert sort (D82). **Shared Bowyer-Watson core extracted** to `engine/geometry-delaunay/src/delaunay_2d_internal.hpp` (namespace `crd::geometry::delaunay::detail`): `Tri` + `TriPool` + `build_super_triangle<T>` + `locate_triangle<T>` + `insert_point<T>`. v8a's `delaunay_2d.cpp` refactored to consume this header; v8b's new `delaunay_2d_hilbert.cpp` adds Hilbert sort + driver. **Hilbert grid resolution 2^16 cells/axis** (D81 — CGAL/delaunator standard; 16-bit fits `u32` codes; sub-cell res irrelevant since `original_index` tiebreaks at D83). **Degenerate-bbox fallback** = unit extent → Hilbert sort degenerates to original-index tiebreak (D84). Duplicate-coord rejection uses separate lex-sort pass (Hilbert sort doesn't guarantee adjacent dups). Same `DelaunayResult2<T>` + `DelaunayStatus` types as v8a; output STRUCTURE invariant (equivalence test on 48-pt random cloud canonicalises triangle sets byte-for-byte). New `delaunay_2d_hilbert(ConstSpan<Vec2<T>>, IAllocator*) -> DelaunayResult2<T>` entry. Umbrella `delaunay.hpp` re-exports v8b alongside v8a. Two-layer typing per ADR-0078 §5 D34: raw `<MathScalar T>` body. Explicit `f32` + `f64` instantiations. 12 cases / 552 assertions on the v8b suite: 3 diagnostics + single-tri + square + pentagon + 64-pt random cloud + insertion-order determinism + large-coord 1e6 f32 + f64 + **v8a equivalence** (48-pt cloud, canonical sets byte-match) + **1024-point scale** (32×32 jittered grid completes cleanly, ≤2N tris, Delaunay invariants hold). v8a+v8b combined: 22 cases / 581 assertions all green. **Parallel-mode upgrade to `scripts/per-slice-check.ps1`** shipped alongside (`-Parallel` flag, `Start-Job`-per-config, `ninja -j NumProc/NumJobs`, logs to `scripts\.per-slice-logs\`). 4-config DoD PASS first try (`scripts/per-slice-check.ps1`, elapsed 03:25). Session log `docs/sessions/2026-05-17-geometry-v8b-delaunay-hilbert.md`. Pinned design decisions D81-D84 carried for ADR-0076 §23 amendment at v8-close. | ~550 + ~350 | done |
| **v8c-pre** ✅ 2026-05-17 | **MANDATORY `insphere_exact` Stage D paydown.** Replaced the Stage-A-equivalent re-expression in `engine/geometry-primitives/src/predicates.cpp` with a literal port of Shewchuk's `insphereexact` (predicates.c v4.0.0 lines 3346-3601): 10 pairwise (x,y) 2D minors (4 elts each via existing `two_minor`) → 10 trio cofactor expansions (24 elts each via existing `triangle_cofactor(m1, s1, m2, s2, m3, s3)`) → 5 quad 96-element expansions (`a's quad = cde + bce - deb - bcd` pattern, cycled) → 5 lifted dets (1152 elts each; double-scale each quad by point's x/y/z, sum x/y/z parts) → cascaded final sum (5760 elts max). Sign returned via `deter[deterlen - 1]`. **Stack vs thread_local split**: small intermediates (4/8/16/48/96/192-elt arrays) stay on stack (~15 KB); the large 384/768/1152/2304/3456/5760-element buffers live in `thread_local static` storage (~170 KB per thread that ever calls — one-time TLS cost, no stack pressure, no allocator dependency). **Adversarial corpus shipped** in `tests/geometry-primitives/test_predicates.cpp` Section (11): 7 cospherical-zero tests (r²=14 / 10000 / 1e6 / 1e10 / non-axis-aligned r²=1e6 / non-symmetric r²=5e7 / **non-symmetric r²=5e9 (the discriminating one — Stage A returned `-16777216`, Stage D returns 0**) + 1 inside-vs-outside sign test + 1 orientation-symmetry test. Build-first regression: full predicates suite still 170 cases / 65630 assertions. Debt entry `docs/debt.md::Shewchuk adaptive predicates` marked CLOSED. Session log `docs/sessions/2026-05-17-geometry-v8c-pre-insphere-stage-d.md`. **API surface unchanged** — `insphere_exact(Vec3<f64>, ..., Vec3<f64>) -> f64` signature is identical; the upgrade is a body-only replacement. | ~150 + ~150 | done |
| **v8c** ✅ 2026-05-17 | **3D Bowyer-Watson tetrahedralisation.** New `delaunay_3d(ConstSpan<Vec3<T>>, IAllocator*) -> DelaunayResult3<T>` entry. Shared internal `engine/geometry-delaunay/src/delaunay_3d_internal.hpp` (namespace `crd::geometry::delaunay::detail3d`) houses `Tet { u32 v[4]; u32 nbr[4]; u8 alive }` ≤ 40-byte slot (D90, static_assert pinned), free-list `TetPool`, `face_vertices[4][3]` permutation table giving canonical outward-oriented faces (D92 — sign-verified via transposition parity such that `orient3d(face, v[i]) > 0` for every positively-oriented tet), `build_super_tet<T>` (D94 — base vertices in z=cz-scale plane CW-from-+z so that Shewchuk's "orient3d > 0 iff d below" convention gives `orient3d(s0,s1,s2,s3) > 0` for s3 at +z; `CRD_ASSERT` at init), `locate_tet<T>` jump-walk (apex-side `orient3d` per face, lowest-face-index tiebreak for determinism), `insert_point_3d<T>` (cavity BFS via Shewchuk Stage D `insphere` — v8c-pre prerequisite — + cavity boundary face collection + **D91 star-shape defensive check** verifying `orient3d(face, q) > 0` before constructing new tet + Phase 3 free + Phase 4 fan + Phase 5 O(K²) edge-match neighbour wiring). Public `delaunay_3d.cpp` owns input validation (TooFewPoints/NonFiniteInput/DuplicatePoint/**Coplanar** new in 3D D93 — every-4-tuple orient3d-zero detection), lex-sort `(x, y, z, original_index)` insertion order, augmented-points construction, per-point insert loop, strip-time tet emission filtering super-tet vertices. `DelaunayResult3<T>` extends v8a's structure with `cavity_max_size` + `super_tet_stripped` telemetry; `DelaunayStatus3` enum adds `Coplanar` value. Reports `Ok` / `TooFewPoints` (< 4) / `NonFiniteInput` / `DuplicatePoint` / `Coplanar` / `InternalInvariant`. Two-layer typing per ADR-0078 §5 D34. Explicit `f32` + `f64` instantiations. **Mid-slice bug fix**: initial super-tet ordering gave `orient3d < 0` because I used CCW-from-+z base + apex above, but Shewchuk's convention is `orient3d > 0 iff d below plane`; swapped s1↔s2 to flip base to CW-from-+z (= CCW-from-back), matching the convention. Faces table verified separately. 12 cases / 33 assertions on v8c suite (4 diagnostics + single-tet + 5-point center-split + cube + 24-pt random cloud verified via `verify_delaunay_3d` helper [orient3d > 0 + empty circumsphere on EVERY output tet] + **cospherical-pathology 9-pt mixture** with 5-point r²=5e9 cospherical cluster + 4 non-cospherical points — the v8c-pre Stage D validator that proves Stage D insphere actually enables v8c without inverted tets + insertion-order determinism + large-coord f32 stability + f64). Combined delaunay suite 34 cases / 614 assertions all green. 4-config DoD via `scripts/per-slice-check.ps1 -Parallel`. Session log `docs/sessions/2026-05-17-geometry-v8c-delaunay-3d.md`. Pinned design decisions D90-D94 carried for ADR-0076 §23 amendment at v8-close. | ~720 + ~340 | done |
| **v8d-2d** ✅ 2026-05-17 | **2D Voronoi diagram extraction** (geometric dual of v8a/v8b). New `voronoi_2d(ConstSpan<Vec2<T>>, IAllocator*) -> VoronoiResult2<T>` entry. **Algorithm**: (1) run `delaunay_2d` to get triangle indices; (2) reconstruct Delaunay tri adjacency via **sort-and-scan over 3T half-edges** `(min(u,v), max(u,v), tri_id, opposite_local_index)` per ADR-0076 §4 pin #11 lex-tuple determinism (NOT HashMap — matches v4-validate / v6e Bentley-Ottmann pattern); (3) compute circumcentre per Delaunay tri via new `crd::geometry::primitives::circumcenter_2d` primitive (D95 — lifted to f64 regardless of T to avoid coord² overflow on large f32 inputs); (4) build per-site cell by walking around the site via Delaunay neighbour info (NOT angular sort — robust under cocircular configs); (5) unbounded cells walk BOTH CCW and CW from start tri, splice, compute ray dirs (D96 — perpendicular to Delaunay hull edge, sign-checked via dot vs opposite vertex to point AWAY from cell interior). **Output (D97)**: `voronoi_vertices` indexed by Delaunay tri id; `cells[i].site_index == i`; CCW orientation pinned for bounded cells. `VoronoiResult2<T>` contains `voronoi_vertices` + `cells` + status. `VoronoiCell<T>` carries `site_index` + `vertex_indices` + `is_bounded` + `first_ray_dir` + `last_ray_dir` (valid iff unbounded). `VoronoiStatus2` enum: `Ok` / `TooFewPoints` / `NonFiniteInput` / `DuplicatePoint` / `InternalInvariant` (Delaunay diagnostics propagated). Two-layer typing per ADR-0078 §5 D34. Explicit `f32` + `f64` instantiations. Umbrella `delaunay.hpp` re-exports v8d-2d. New primitive `engine/geometry-primitives/include/crd/geometry/primitives/circumcenter.hpp` reusable by future Ruppert v8g + tet meshers per advisor guidance. **Adversarial corpus shipped FIRST per advisor's TDD recommendation** (test_voronoi_2d.cpp 11 cases / 48 assertions): 3 diagnostics + 3-site triangle (1 Voronoi vertex / 3 unbounded cells) + 4-site square (all 4 unbounded) + 5-site square+center (1 bounded / 4 unbounded + CCW signed area > 0 assertion) + **16-site exact-integer 4×4 grid (cocircular every-unit-square; Stage D incircle resolves; 4 bounded inner / 12 unbounded perimeter)** + cospherical-pathology 9-site mixture (5 cocircular r²=5e9 + 4 outsiders — Stage D incircle validator) + insertion-order determinism + large-coord f32 stability + f64 precision tier. **Defining-property validator** = brute-force nearest-input-site check (for each cell, verify sites[cell.site_index] is closest to itself among all sites) — the Voronoi DEFINITION; appears in every non-trivial test. **2 mid-slice bug fixes**: (1) initial walk used `nbr[si]` for CCW direction but that's actually CW in CCW-oriented triangulation; swapped to `nbr[(si+2)%3]` (incoming edge to s) for CCW + `nbr[si]` (outgoing edge from s) for CW; ray-direction lookup also corrected. (2) initial 16-grid test assumed jittered grid would have 12 hull vertices but small jitter pushed perimeter points off the hull non-deterministically; switched to exact integer grid (lets Stage D incircle handle the cocircular configs deterministically). Combined delaunay suite **45 cases / 662 assertions all green**. 4-config DoD via `scripts/per-slice-check.ps1 -Parallel`. Session log `docs/sessions/2026-05-17-geometry-v8d-2d-voronoi.md`. Pinned design decisions D95-D97 carried for ADR-0076 §23 amendment at v8-close. | ~450 + ~370 | done |
| **v8d-3d** ✅ 2026-05-17 | **3D Voronoi cells extraction** (geometric dual of v8c). New `voronoi_3d(ConstSpan<Vec3<T>>, IAllocator*) -> VoronoiResult3<T>` entry + DCEL-form output (`VoronoiResult3<T>` w/ `voronoi_vertices` = tet circumcentres + `cells` = per-site `Array<VoronoiFace3<T>>` w/ `neighbor_site_index` + `vertex_indices` (CCW from neighbor side, D99) + `is_unbounded`) + **ConvexHullView helper** `convex_hull_for_cell(result, sites, cell_index, alloc) -> VoronoiCellHull3<T>` returning vertices + outward face Planes + face-vertex-offsets prefix-sum (returns empty for unbounded cells, D101). New `VoronoiStatus3` enum w/ `Coplanar` propagated from delaunay_3d. **Algorithm**: (1) run `delaunay_3d`; (2) rebuild tet face adjacency via sort-and-scan over 4T half-faces keyed `(sorted v0/v1/v2, tet_id, opp_local_idx)` per ADR-0076 §4 lex-tuple determinism (NOT HashMap); (3) compute circumcentre per tet via new `crd::geometry::primitives::circumcenter_3d` primitive (D100 — lifted to f64 regardless of T, Cramer's rule on 3x3 system for tet circumcentre); (4) sort-and-scan 6T half-edges keyed `(vmin, vmax, tet_id, local_edge_idx)` to find each Delaunay edge's tet fan; (5) per Delaunay edge, walk the tet fan via face-adjacency (D98 — pick 2 fan-faces = `{0,1,2,3} \ {si, ni}` in start tet, walk through one then splice the other if unbounded) collecting circumcentres; (6) **dot-product sign check against axis** `(neighbor - site)` to determine if natural walk order is CCW from vmin or vmax side, reverse for the wrong-side cell so BOTH sides get correct outward-normal CCW orientation in a single fan walk. Reports `Ok` / `TooFewPoints` (< 4) / `NonFiniteInput` / `DuplicatePoint` / `Coplanar` / `InternalInvariant`. Two-layer typing per ADR-0078 §5 D34. Explicit `f32` + `f64` instantiations. Umbrella `delaunay.hpp` re-exports v8d-3d. **Adversarial corpus shipped FIRST per advisor TDD** (test_voronoi_3d.cpp 14 cases / 87 assertions): 4 diagnostics + 4-site tet (1 Voronoi vertex / 4 unbounded cells with 3 faces each) + 5-site tet+interior (1 bounded cell) + 8-site cube (all unbounded, `[cocircular]` Stage D incircle validator) + 9-site cube+interior (1 bounded + 8 unbounded + outward-normal check) + 24-site random cloud + **cospherical-pathology 9-site mixture** `[cospherical]` (Stage D insphere validator carried from v8c) + **ConvexHullView helper test** validating face planes pass through their vertices (signed distance < 1e-9) + site is on NEGATIVE side of every face plane (cell interior convention) + empty hull for unbounded cells + determinism + f64. **Defining-property validator** = brute-force nearest-input-site check + **face_normals_outward validator** = triangle-fan cross-product sum dotted against (neighbor - site) > 0 on every face. **Two mid-slice bug fixes** = (1) `EdgeFanResult res{};` default-construction of `Array<u32>` member required explicit `(alloc)` ctor on the struct; (2) `build_edge_fans` had unused `alloc` parameter (Cerid uses `/W4 /WX` so unused params are errors). Combined delaunay suite **59 cases / 749 assertions all green**. 4-config DoD via `scripts/per-slice-check.ps1 -Parallel`. Session log `docs/sessions/2026-05-17-geometry-v8d-3d-voronoi.md`. Pinned design decisions D98-D101 carried for ADR-0076 §23 amendment at v8-close. | ~600 + ~360 | done |
| **v8e** 📋 planned | **Lloyd's CVT iteration** (Lloyd 1982 Centroidal Voronoi Tessellation relaxation). Take a point set, run Voronoi, move each point to centroid of its cell, repeat until convergence. Converges to "evenly-distributed point set" optimum. Powers stippling, blue-noise sampling, isotropic remeshing alternative, particle initialisation. | ~400 + ~250 | ~3-4 days |
| **v8f** 📋 planned | **Sibson 1981 Natural Neighbour Interpolation.** Scattered-data interpolation via "stolen Voronoi area" weighting. Crushes inverse-distance-weighted for terrain reconstruction, scientific-field interpolation. C¹ continuous. Builds on v8d-2d Voronoi. | ~400 + ~300 | ~4-5 days |
| **v8g** 📋 planned | **Ruppert 1995 2D Delaunay refinement** — quality bound on min angle for FEA 2D mesh prep. Takes initial Delaunay (or v6c CDT) and refines by inserting Steiner points until every triangle's minimum angle exceeds α (typical 25-30°). Theoretical termination for α ≤ 20.7°. The standard production primitive for FEA 2D meshing. | ~600 + ~400 | ~5-7 days |
| **v8h** 📋 planned | **Shewchuk-style 3D Delaunay refinement (sliver removal)** — tetmesh quality for FEA 3D. Slivers (almost-flat tetrahedra) tank solver conditioning. Removal via small-perturbation + edge-collapse + face-flip. Powers eylem v7 FEM + future crd-fea. | ~700 + ~400 | ~6-8 days |
| **v8-close** 📋 planned | **`-delaunay` cluster CLOSED.** ADR-0076 §23 amendment locking all D73-D~95 design decisions + `docs/systems/geometry-delaunay.md` system overview + 18-config full sweep PASS + roadmap/context/MEMORY sync + cluster session log. Note v6c CDT consolidation option (refactor v6c to consume v8a internally) as optional follow-on. | docs + sweep | ~2 days |
| **v8 TOTAL** | 11 slices, Full elite scope locked 2026-05-17 (Phase 3.1.7 sub-module 9 of 11). | **~6200 + ~3450 tests** | **~4-6 weeks** |
| **v9a–v9d** | GPU LBVH builder (Karras 2012+2013) + GPU BVH refit + V-HACD convex decomposition (Mamou 2014, editor-tier) + REPL bindings | ~8000 | ~3 wk |
| **v9e** | `crd-geometry-shader-helpers` GLSL/HLSL output side — cooker emits GLSL/HLSL twins of the v1h `signed_distance.hpp` analytic SDFs + the v0e iq smin/domain-ops, from a formula-IR manifest seeded at v0e; ULP-conformance test against those C++ references; first-light consumer = `crd-renderer` Phase 3.5+ DFAO/DF-soft-shadow | ~4000 | ~2 wk |
| **v4-validate** ✅ 2026-05-16 | **`validate_triangle_mesh` — formal mesh validation pipeline stage** (shipped INSIDE the v4 `-mesh` cluster — see slice table above for full closure detail). Six defect kinds: OutOfBoundsIndex / DegenerateTriangle / ZeroAreaTriangle / NonManifoldEdge / BoundaryEdge / InconsistentOrientation. Three-pass deterministic algorithm (triangle scan → sorted edge table → run-classify by count). `MeshValidationReport{defects, triangle_count, vertex_count, manifold/boundary/non_manifold_edge_count, well_formed, watertight}`. `Area32` typed at options surface; raw f32 inside per ADR-0078 §5 D34. 10 cases / 46 assertions. Session log `docs/sessions/2026-05-16-geometry-v4-validate.md`. | ~400 + ~250 tests | done |
| **v10a** 📋 planned | **`crd-geometry-curves` module substrate.** New module `engine/geometry-curves/` (target `crd-geometry-curves`, ns `crd::geometry::curves`, deps `crd-core` + `crd-math` + `crd-containers` + `crd-geometry-primitives`). Curve primitive types: `Polyline3` (non-owning view + owning variant), `QuadBezier3`, `CubicBezier3`, `CubicHermite3` (position + tangent at each end), `CatmullRom3` (uniform + centripetal parameterization — the cinematic camera default), `BSpline3` (degree-3, owns knot vector `Array<f32>`), `CircularArc3` (center + plane + start_angle + sweep), `EllipseArc3`. 2D peers (`Polyline2`, `QuadBezier2`, `CubicBezier2`, `CircularArc2`, `EllipseArc2`) shipped same slice (mirrors the v0a/v0b 2D-peer-set discipline). Closed-curve flag on each primitive (closed B-spline / closed Catmull-Rom for loop paths). Templated on `T ∈ {f32, f64}` (matches v3 Quickhull + v2i f64 GJK precedent). System doc `docs/systems/geometry-curves.md`. | ~700 + ~400 tests | ~3 days |
| **v10b** 📋 planned | **Sampling + flattening.** `sample_uniform(curve, n_samples) → Array<Vec3>` (parameter-uniform — fastest, lowest quality at high curvature); `sample_adaptive(curve, tolerance) → Array<Vec3>` (chord-error subdivision: de Casteljau midpoint test for Bezier, recursive split for splines until segment chord-error < tolerance; the LOD default); `sample_by_curvature(curve, max_angle_step) → Array<Vec3>` (camera-banking / banking-quality default); `to_polyline(curve, tolerance) → Polyline3` convenience. Tolerance ties to `k_distance_epsilon` from v1h `constants.hpp`; per-call overrides allowed. Closed-curve sampling produces a closed polyline (first == last, marked). Determinism: no `std::sort`, no transcendental outside `crd::math::deterministic`. | ~500 + ~400 tests | ~3 days |
| **v10c** 📋 planned | **Arc-length system.** `build_arclength_table(curve, n_samples=64) → ArclengthTable` (cumulative-distance over a sufficiently-dense uniform sample; `Array<{t, distance}>`); `length_of(curve) → f32` total; `t_at_distance(table, distance) → f32` (binary search + linear interpolation) for constant-speed traversal; `distance_at_t(table, t) → f32` (inverse). Closed-curve modular distance (wraps mod length). Consumer: cinematic camera spline traversal (constant-speed dollying), robot trajectory constant-speed sampling, animation keyframe timing remap. Validation: `t_at_distance ∘ distance_at_t == identity` within `1e-6`; arc-length monotone in `t`. | ~300 + ~250 tests | ~2 days |
| **v10d** 📋 planned | **Curve queries.** `aabb_of(curve) → AABB3` (subdivision-bounded for Bezier/spline — recursive split until chord-AABB stable; exact for circular/elliptic arcs); `closest_point(curve, p, tolerance) → CurveClosestPoint{t, point, distance²}` (Newton-Raphson on `d/dt|curve(t) - p|² = 0`, initialized via subdivision rejection — global minimum, not local); `distance(curve, p)` convenience; `intersect_ray(curve, ray, tolerance) → optional<{t_curve, t_ray, point}>` against flattened polyline (curve-curve intersection deferred to v10 follow-up). Hooked into the unified `crd/geometry/queries.hpp` facade overload set. Consumer: editor curve-edit gizmo (closest-point), cinematic camera trigger volumes (ray-vs-spline-path), debug-draw cursor-pick on smoothed polyline. | ~400 + ~300 tests | ~2 days |
| **v10e** 📋 planned | **Frames + viz + sandbox showcase.** `tangent(curve, t) → Vec3` (first derivative); `normal(curve, t) → Vec3` (Frenet via second-derivative + Gram-Schmidt; defaults to a deterministic up-vector when the curvature is zero); `binormal(curve, t) → Vec3`; **rotation-minimizing frame** (`compute_rmf(curve, samples) → Array<{tangent, normal, binormal}>` via double-reflection method, Wang 2008 — the smooth-banked camera / smooth-banked trajectory default). `crd-geometry-viz` companion (already shipped v1j) extended with `draw_curve(buf, curve, samples)` / `draw_polyline(buf, polyline)` / `draw_tangent_frame(buf, curve, n_samples)` / `draw_rmf(buf, frames)`. Sandbox showcase scene added (`SandboxScene::CurvesShowcase`) — pickable curve type + control-point dragging + sample slider + frame mode toggle. **Curve-curve intersection deferred** to follow-up (advisor: ships when a real consumer asks; v10 ships sampling + AABB + closest-point + frames, the cinematic+robotics MVP). | ~400 + ~350 tests | ~2 days |
| **v11** 📋 planned | **Transform-aware query helpers in `crd-geometry-primitives`.** New `crd/geometry/primitives/transform.hpp` (in the existing `crd-geometry-primitives` module — NO new module per the 2026-05-14 scope decision; `crd-geometry-runtime` is a reserved-not-created slot, see ADR candidate 2). `TransformedShape<T>{ T shape; Mat4 to_world }` lightweight value-type wrapper; free functions `transform_aabb(Mat4, AABB3) → AABB3` (8-corner method — conservative AABB-of-transformed-corners, the standard pattern), `transform_obb(Mat4, OBB3) → OBB3` (rigid-exact for orthogonal + uniform-scale `M`; conservative-via-AABB for general affine — detected by checking `M` for orthogonality + uniform-scale at construction), `transform_capsule3` / `transform_cylinder3` (axis-line transform + radius scale by `|M|` average), `transform_ray3_to_local(world_ray, world_to_local) → Ray3` (the inverse-direction pattern that lets a raycast happen in local space then transform the hit back). Cross-domain decision pin (already in §11.3 above): **`crd-geometry` stays local-space pure**; world-space dispatch is a *consumer* concern (`crd-scene-spatial` / `crd-eylem` already adapt). v11 ships only the transform-shape helpers — the dispatch layer (`world_raycast(scene, ray)` / `world_overlap(scene, box)`) lives at consumer time, NOT here. Tests: `transform_aabb(M, box) ≈ aabb_of_8_corners(M·box.corners())` within `k_distance_epsilon` on rigid + scale + skew; identity transform is a value-preserving no-op (`transform_aabb(Mat4::identity(), b) == b` bit-exact); degenerate scale (zero / negative axis) does not produce NaN; OBB rigid-transform is exact (orthogonal `M`); 2D peers (`transform_aabb2` / `transform_obb2`) shipped same slice. | ~400 + ~300 tests | ~2 days |

**Total: ~42 slices, ~20.0 KLOC engine + ~5 KLOC editor-tier + ~4 KLOC
cooker-emitted GLSL/HLSL, ~7–9 months calendar** (was 25 / 14 + 5 /
4–6 months prior to supplement-dossier additions; +v0e/v1g/v4g/v9e
from the supplement; +v0f from the ADR-0076 §13 amendment; +v1h/v1i/v1j
from the ADR-0076 §15 checklist additions 2026-05-13 — primitives
hardening [constants/epsilon-policy + is_finite/NaN-Inf contract +
`signed_distance.hpp` iq analytic SDFs in C++ + `ConvexHullView`] +
the unified query facade [`queries.hpp` + shapecast + broadphase-pair
API + degenerate/large-coordinate validation discipline] + the new
`crd-geometry-viz` debug-draw companion module; the §13 move-and-delete
also nets ~−0.4 KLOC out of `crd-math` into v0a; **v2 expanded 2026-05-13
from ~6 → 11 slices for elite-tier completeness: v2a–v2f (the GJK/EPA/SAT
core + hull queries + GJK-cast) + v2g hill-climbing hull support
(PhysX/Havok pattern, 3–5× win for N>32 hulls) + v2h Vec4f/Vec8f SIMD-
batched hull support (~6× per-vertex on AVX2) + v2i f64 GJK instantiation
for orbital-scale aerospace + v2j Sutherland-Hodgman convex polygon
clipping & feature enumeration [migrated from v6, gates eylem v1d-manifold
PhysX/Jolt-grade 4-point manifold generation] + v2-close. v6 amended:
loses Sutherland-Hodgman to v2j, becomes v6a–v6d. ADR-0076 §16 pin #12
added: witness-index tiebreak rule for EPA & hull support.** Geometry
phase grows by 9 slices net (10 added in v2, 1 removed from v6) and
~1.5 KLOC engine.).

**Renewed total: 51 slices, ~22 KLOC engine + ~5 KLOC editor-tier + ~4
KLOC cooker-emitted GLSL/HLSL, ~7.5–9.5 months calendar** (was 42 / 20.0
KLOC / 7–9 months prior to renewed-scope additions 2026-05-14; +1
v4-validate + 5 v10a–e + 1 v11 = +7 slices via renewed scope; +2 slices
2026-05-16 via v6 substrate separation (v6a–v6d planned → v6a/b/c/d/e
+ v6-close shipped; nothing cut). The earlier "Total" paragraph (42 /
20.0 KLOC / 7–9 months) is the pre-renewal historical sizing.

## Renewed scope coverage (2026-05-14)

A comprehensive scope review against the multi-domain mandate (gaming +
simulation + manufacturing + CAD + CFD + aerospace + robotics + medical
viz + DAW + cinematic) was run 2026-05-14 against the existing 42-slice
plan. **Most of the missing-work inventory is already covered** by the
v0–v9e structure — the table below maps inventory items to the slice
that owns them. **Four genuine gaps surfaced**, captured as new in-phase
slice clusters (v4-validate, v10a–e curves, v11 transform-aware
helpers); the renewed total is **49 slices / ~7.5–9.5 months / ~22 KLOC
engine + ~5 KLOC editor-tier + ~4 KLOC cooker-emitted GLSL/HLSL**.

**Cerid is becoming an engineering-first real-time application /
simulation platform**, and `crd-geometry` is its shared computational-
geometry substrate for simulation, physics, robotics, rendering, CAD/CAE
preparation, manufacturing geometry, and editor tooling. The renewed
scope below makes that explicit — captures missing/future work, classifies
it, maps consumers, lists risks. It does **not** widen the active phase
to solve the full CAD/CAE/EDA/manufacturing problem; those are separate
future domain phases (3.1.8 `crd-brep`, 3.1.10 `crd-cfd`, 3.1.12
`crd-fea`, 3.1.13 `crd-cam`, the newly-added `crd-eda` ROADMAP stub).

### Priority classification

**Must-have-before-3.1.7-close** (in-phase slices — see "New in-phase
slices" table below for v4-validate / v10a–e / v11):

1. **v10 `-curves` sub-module** — curve primitives + sampling + arc-length + closest-point + tangent/normal/binormal frames. Cinematic camera paths, robot trajectories, debug-draw smoothed polylines all need this.
2. **v11 transform-aware query helpers** in `crd-geometry-primitives` — `TransformedShape<T>` + `transform_aabb` / `transform_obb` / `transform_capsule3` / `transform_cylinder3` + `transform_ray3_to_local`. Pin: `crd-geometry` stays local-space pure; world-space *dispatch* is a consumer concern.
3. **v4-validate** — formal mesh-validation pass extracted from v7's scattered manifoldness-fix / self-intersection-removal. Cooker pipeline + editor mesh-import gate.
4. **Units / scale / precision policy ADR candidate** — see "ADR candidates" section below; brief inline policy in this doc, ADR minted at v10 or v11 close.
5. **Hull simplification** (v3d, in flight) — locked-vertex `keep_vertex_indices` constraint already designed in 2026-05-14 scoping. **Untouched by this renewal — the user directive 2026-05-14 was: "we don't need to touch the current slice we are working on we can finish them."**
6. **Debug visualization** (v1j ✅) — primitives, ray hits + normals, closest points, BVH nodes by depth, frustum-cull, overlap pairs. Curve viz lands as part of v10e.
7. **Benchmark / validation infrastructure** — Embree-comparison bench (v1f ✅), per-cluster perf budgets (v2-close ✅, v3-close, v4-close, …), `crd-no-non-ascii-test-names` + `crd-no-std-math-check` + `crd-no-std-sort-check` + `crd-simd-emission-check` guards (v1i-c ✅).
8. **Consumer mapping** — see expanded table below.

**Should-have-soon-after-3.1.7-close** (reserved follow-up under existing sub-modules, NOT in 3.1.7 close):

- **Curve self-intersection + curve-curve intersection** — reserved under `-curves` follow-up; ships when a real consumer asks (cinematic doesn't need it).
- **Polygon offsetting** (miter / bevel / round joins, stroke expansion, PCB clearance zones, CNC toolpath compensation) — reserved under `-polygon` follow-up; primary consumer is future `crd-eda` + `crd-cam`.
- **KD-tree k-nearest + radius search** — `-spatial` v5 ships unconstrained KD-tree; k-nearest / radius are a follow-up if a point-cloud consumer surfaces (lidar batch, particle nearest-neighbor).
- **SDF mesh sampling + gradient/normal + raymarch helper** — `crd-sdf` (3.1.5) consumes 3.1.7 mesh-bake + closest-point; the SDF-side query facade lives in `crd-sdf`, not here.
- **Curve-along-mesh / tube-along-curve / ribbon-along-curve** — procedural shape helpers; ship in `crd-procgen` (3.1.15) or `crd-animation` (3.2) when those land.
- **Quality triangulation** (minimum-angle improvement, Steiner-point insertion) — `-delaunay` v8 ships unconstrained + constrained Delaunay; quality-driven refinement is a follow-up for FEA mesh prep.

**Future-major (separate sub-phases or domain phases, NOT 3.1.7 slices):**

- **BRep / NURBS / CAD kernel** → Phase 3.1.8 `crd-brep` (parametric surfaces, B-rep solid topology, exact booleans, fillet/chamfer/sweep/loft, STEP/IGES/Parasolid import).
- **Parametric feature trees + GD&T** → Phase 3.1.9 `crd-cad-feature`.
- **CFD geometry** (unstructured grid topology, FVM/FEM, AMR, multiphase VOF) → Phase 3.1.10 `crd-cfd`.
- **Estimation / control / path planning** (Kalman / EKF / SLAM, PID / LQR / MPC, RRT / RRT*, PRM, A*) → Phase 3.1.11 `crd-estimation` + `crd-control`.
- **Engineering FEA** (static structural / modal / buckling / fatigue; thermal-structural coupling) → Phase 3.1.12 `crd-fea` (distinct from eylem v7 dynamic FEM).
- **Manufacturing toolpaths / CAM** (3- / 5-axis milling, turning, additive slicing, G-code post-processors) → Phase 3.1.13 `crd-cam`.
- **PCB / EDA geometry** (board outline, traces, vias, pads, copper zones, DRC, Gerber export, routing helpers) → NEW Phase `crd-eda` future-major stub in ROADMAP. Added 2026-05-14 to surface the EDA ambition without committing scope.
- **Scientific visualization** (isosurfaces, streamlines, slice planes, vector-field LIC, perceptual color maps, in-engine plots) → Phase 3.1.16 `crd-sciviz`.
- **Procedural geometry** (Wave Function Collapse, L-systems, terrain erosion, city/building generation) → Phase 3.1.15 `crd-procgen`.
- **ML-inference geometry** (denoising / SR over geometry-derived G-buffers, content-generation mesh decode) → Phase 3.1.14 `crd-ml-inference`.

### Consumer mapping (expanded)

| Geometry feature | Primary consumer | Secondary consumers | Phase priority |
|---|---|---|---|
| Dynamic BVH (v1c ✅) | eylem v1c broadphase | crd-scene `SpatialBVHIndex`, robotics sensors | current ✅ |
| BVH4 + Vec4f SIMD (v1g ✅) | crd-renderer 3.5+ frustum cull | crd-audio acoustic raycasts, editor picking | current ✅ |
| Unified query facade (v1i ✅) | every consumer | n/a | current ✅ |
| Shapecast / sweep TOI (v1i-b ✅) | eylem v6 CCD | character controller, editor manipulator | current ✅ |
| GJK / EPA / SAT (v2a–v2d ✅) | eylem v1d narrowphase | robotics collision, CAD validation | current ✅ |
| Hill-climb hull support (v2g ✅) | eylem PhysX-class hull contact | CAD/robotics convex queries | current ✅ |
| `Vec4f/Vec8f` SIMD support (v2h ✅) | eylem N≤32 hull pairs | offline batch (cooker convex prep) | current ✅ |
| Sutherland-Hodgman + feature enumeration (v2j ✅) | eylem v1d-manifold | CAD boolean prep, polygon clip in UI | current ✅ |
| Shewchuk adaptive predicates (v3a ✅) | v3b/v3c Quickhull | v6 Vatti, v8 Bowyer-Watson, future CAD boolean, future FEA contact | current ✅ |
| 2D convex hull (v3b ✅) | future UI/editor hull tools | future PCB outline simplification | current ✅ |
| 3D Quickhull (v3c ✅) | eylem convex collider cooker | CAD conditioning, future V-HACD pipeline | current ✅ |
| Hull simplification + `keep_vertex_indices` (v3d ✅ 2026-05-15) | eylem stable contact (vertex budget cap) | CAD remap, FEA attachment-point preservation, robotics gripper-finger hull | current ✅ |
| Mesh raycast + closest-point + winding (v4a–f) | editor picking | lidar/depth sensor, SDF bake, eylem mesh collider | current |
| Mesh BVH leaf SIMD (v4g) | mesh-vs-ray batched queries | renderer cull, acoustic raycasts | current |
| **Mesh validation pass (v4-validate) — NEW** | editor cooker mesh import | eylem mesh collider import gate, SDF-bake input gate, manufacturing pre-flight | current — NEW |
| Spatial structures beyond BVH (v5a–e) | scene `IComponentIndex` (KD/octree/R-tree/hash/grid) | UI/editor selection, particle systems | current |
| Polygon ops (v6a–d) | UI / editor 2D | navmesh, sketch tool, future PCB outline | current |
| Mesh processing (v7a–g) | cooker LOD / editor remesh | CAD/CAE preparation, manufacturing prep | current |
| Delaunay 2D/3D + Voronoi (v8a–d) | navmesh, future FEA mesh prep | scientific viz (Voronoi cells) | current |
| GPU LBVH + V-HACD + REPL (v9a–d) | crd-renderer Phase 3.5+ | offline batch raycast, eylem cooker, REPL scripting | current |
| Shader-helpers GLSL/HLSL (v9e) | crd-renderer 3.5+ DFAO / DF-soft-shadow | crd-sdf shader twins | current |
| **Curves (v10a–e) — NEW** | cinematic camera spline paths | robot trajectories, debug-draw smoothed polylines, road/path authoring, animation curve viz | current — NEW |
| **TransformedShape helpers (v11) — NEW** | crd-scene world-space queries | crd-eylem world API, editor picking, renderer cull | current — NEW |
| Debug viz (v1j ✅) | sandbox + editor | every algorithm with a visual artifact | current ✅ |
| BRep / NURBS surface eval | CAD substrate | manufacturing toolpath base, CAE meshing input | Phase 3.1.8 |
| Polygon offset + clearance | future `crd-eda` DRC + `crd-cam` toolpath | UI/editor, CAD sketch offset | Phase 3.1.13 / future `crd-eda` |
| Unstructured-grid topology + FVM/FEM | CFD substrate | scientific viz CFD overlays | Phase 3.1.10 |
| Volumetric meshing (tet, hex) | FEA substrate | CFD inlet/outlet patch tagging | Phase 3.1.10 / 3.1.12 |
| Sensor-ray geometry (lidar pattern, depth sensor) | robotics simulation | aerospace radar/lidar | Phase 3.1.11 + Phase 8 robotics |
| Robot footprint + swept volume | robotics navigation | manufacturing AGV planning | Phase 3.1.11 + Phase 8 |
| Toolpath geometry (pocket / contour / drill) | manufacturing substrate | CAD/CAM editor | Phase 3.1.13 |
| Board outline + trace + DRC + Gerber | PCB substrate | manufacturing fab export | future `crd-eda` |

### Risk register

- **CAD topology naming problem** (re-evaluation of features through edits, fillet/Boolean robustness across topology rebuilds) — out of 3.1.7 scope; lives in Phase 3.1.8 `crd-brep` where it's a substantial sub-problem. v3a Shewchuk predicates form the foundation but do not solve the topology-graph identification problem.
- **Large-coordinate f32 precision** — aerospace consumers (orbital scales, 1e7 m) need f64 staging. v2i (f64 GJK ✅), v3a (Shewchuk f64 predicates ✅), v3c (f64 Quickhull ✅). Mesh-side f64 staging (v4 `-mesh`) deferred until a real consumer surfaces (eylem-aero or eylem-cine reservation).
- **Robust polygon Boolean** — Vatti (v6c) handles the common case; pathological self-intersections + coincident-coplanar cases need v3a `orient2d_exact` + custom handling. Risk: edge cases not covered by reference implementations (Clipper2, GPC). Mitigation: degenerate corpus + adversarial test sweep at v6c close.
- **Mesh validation as a pipeline-stage concern** — v4-validate is a *cooker + editor-import* stage. Runtime never re-validates. Risk: developer authors a non-manifold mesh and only discovers it at integration time. Mitigation: v4-validate emits a validation report that the cooker + sandbox surface; CRDR pack format carries a `'MVAL'` chunk (or equivalent) noting which validation rules were verified at cook time.
- **Transform-aware queries crossing modules** — `crd-geometry-primitives` stays local-space pure; world-space *dispatch* (the `world_raycast(scene, ray)` adapter) belongs to `crd-scene` / `crd-eylem`. Risk: an inattentive consumer adds world-space helpers to `crd-geometry`. Mitigation: v11 ships only the wrapper types + transform helpers, and the consumer-side adapter pattern is documented as a Pin in §11 below + ADR candidate.
- **Curve sampling tolerance** — adaptive flattening's tolerance value flows through to render LOD + physics step + manufacturing toolpath. Risk: per-consumer epsilon drift causes visual seams, physics-mesh divergence, or manufacturing tolerance violations. Mitigation: v10b ties tolerance to the v1h `k_distance_epsilon` policy by default; per-call overrides allowed but the default is anchored.
- **`-curves` sub-module sizing risk** — v10 is ~2.5 KLOC across 5 sub-slices. If the GLSL/HLSL twin emit becomes a real consumer requirement, v9e expands. Mitigation: ship v10a–e against C++ scalar consumers first; the GLSL/HLSL curve emit is a follow-up.
- **Mesh validation false-positives at editor-import** — coplanar near-degenerate triangles may trip "degenerate" rule when the artist intent was deliberate (e.g., flat decals). Mitigation: validation reports a *severity* (info / warning / error) per rule; the editor surfaces warnings without blocking import.
- **Robust ray-AABB Williams/Ize widening overconservatism** — for very-far-origin scenes (1e7+ m, aerospace large-world) the conservative widening may admit false positives at the BVH-traversal step. Mitigation: f64 BVH refit + traversal for those domains lives behind a future `BvhTreeF64` opt-in. v2i precedent (f64 GJK) shows the pattern.

### ADR candidates (mint at slice time, NOT this session)

1. **`crd-geometry` precision + epsilon policy** (mint at v10 or v11 close, whichever lands first). **The unit-system question is RESOLVED at engine-wide scope by Phase 3.1.7.5 `crd-units`** (locked 2026-05-14, `docs/phases/phase-3.1.7.5-units.md`) — *NOT* a geometry-local ADR. Geometry's local ADR covers only the residual *precision-tier* and *epsilon* questions. Pin:
   - **Unit system: deferred to `crd-units` substrate (Phase 3.1.7.5).** SI everywhere, compile-time-dimensional types at API surface, `crd-geometry-primitives` AABB3 / OBB / Sphere / Triangle3 / Plane / Capsule / Cylinder / Tetrahedron / Frustum / ConvexHullView all become `Vec3<Length<T>>`-parametrised at the API surface in Phase 3.1.7.5 v0c adoption pass. The per-domain mm/m/μm split is **OBSOLETED** — every domain (games / robotics / CAD / PCB / aerospace / CFD / FEA / CAM / medical) reads `Length<T>` from `crd-units`, no domain-local unit choice.
   - **Precision tier:** geometry hot paths are `T ∈ {f32, f64}` templated already (Quickhull v3c shipped both); aerospace large-world consumers stage to `Length<f64>`, games stay on `Length<f32>`. The dimensional type is the same.
   - **Epsilon policy: per-query, not global.** Geometry-wide intent-named constants live in v1h `constants.hpp`; specific algorithms (GJK termination, predicate Stage A/B/C/D thresholds, SAH cost tie) carry their own; consumers call with overrides for adversarial inputs.
   - **Large-coordinate test corpus is mandatory** in `tests/geometry-*/test_validation.cpp` (v1i-c ✅ shipped the +1e6/+1e7 origin sweep precedent). Once `crd-units` adoption lands, the large-coord sweep uses `Length<f64>` explicitly.
   - **Geometry's API-surface adoption is sized in Phase 3.1.7.5 v0c, NOT inside Phase 3.1.7.** Phase 3.1.7 ships pre-`crd-units`; the v0c adoption pass re-tags AABB / OBB / etc. mechanically (the SIMD kernels are unchanged — layout is bit-equal).

2. **Transform-aware adapter pattern** (mint at v11 close). Pin:
   - `crd-geometry-primitives` stays local-space pure. World-space dispatch is a *consumer* concern.
   - `TransformedShape<T>` is a value-type wrapper that pairs `T` (local-space primitive) with `Mat4 to_world`; it lives in `crd-geometry-primitives` but carries no world-space queries — those are owned by `crd-scene-spatial`, `crd-eylem` world API, or `crd-renderer` cull.
   - `transform_aabb` (8-corner) / `transform_obb` (rigid-exact, affine-conservative-via-AABB) / `transform_capsule3` / `transform_cylinder3` / `transform_ray3_to_local` ship as free functions.
   - Future opt-in `crd-geometry-runtime` sub-module remains a possibility if a consumer-side facade duplication problem surfaces (Cerid's pattern: do not pre-create modules). Tracked as a reserved slot, not a slice.

3. **Curve mathematics ownership** (mint at v10 close). Pin:
   - `crd-geometry-curves` owns curve math (sampling, flattening, arc-length, closest-point, frames).
   - `crd-draw` consumes sampled points and debug primitives; it never owns curve math. Existing `crd-draw` is unchanged by v10.
   - Renderer / animation / cinematic / robotics never reach into curve internals; they consume `to_polyline(curve, tolerance) → Polyline3` or the streaming sampler.
   - Curve self-intersection + curve-curve intersection are reserved follow-ups; v10 ships sampling + AABB + closest-point only.

4. **Mesh validation pipeline stage** (mint at v4-validate close). Pin:
   - Manifoldness / duplicate-vertex / non-manifold-edge / inverted-normal / self-intersection / open-boundary / disconnected-component checks run at **cook time + editor import**.
   - Runtime never re-validates. The CRDR mesh chunk carries a `validated_at: timestamp` + rule list at cook time.
   - Severity: info / warning / error per rule (artist intent may admit warnings).
   - Self-intersection check is BVH-accelerated (v4-validate consumes v1a–g + v4a–f).

5. **ADR-0076 §19 amendment formalizing the renewed-scope review** (mint at next ADR session; structurally identical to the §15 precedent). Captures: the 7 new slices (v4-validate + v10a–e + v11), the slice-count revision (42 → 49), the 4 candidate ADRs above as deferred follow-ups, the architectural rules pinned in §11 above, the cross-reference to Phase 3.1.17 `crd-eda` for the PCB/EDA elevation. Justifies-by-precedent: §12 amendment 2026-05-11 (pivot before eylem v1c), §13 amendment 2026-05-11 (`crd::math::geometry` move-and-delete + v0f corpus), §15 amendment 2026-05-13 (v1h/v1i/v1j checklist additions), §16 (architecture pins), §17 (v2 substrate locks), §18 (v3 Shewchuk predicates) all set the pattern of treating substantive scope amendments as in-place numbered sections appended to ADR-0076 rather than minting a new ADR per amendment. §19 follows that pattern.

### Validation + benchmark expectations

For each new in-phase slice cluster:

- **v4-validate** — Golden-mesh corpus: known-watertight, known-non-manifold, known-degenerate-triangle, known-self-intersecting, known-inverted-normal. v4-validate reports each issue with exact triangle index + edge incident pair. Cross-platform / cross-config: `scripts/full-sweep.ps1` 17-config sweep at v4-validate close.
- **v10 curves** — Scalar vs adaptive-flattening parity (tolerance honored to within `1.1·tolerance` chord error); arc-length table monotone in `t`; `t_at_distance ∘ distance_at_t` is identity within `1e-6` for `t ∈ [0, 1]`; curvature continuity at segment joins for C2 splines; closed-curve roundtrip identity (`sample_uniform(closed_curve, N)[0] == [N-1]`). Determinism: bit-exact builds across compilers / SIMD widths (no `std::sort`, no transcendental libm outside `crd::math::deterministic`).
- **v11 transform-aware helpers** — `transform_aabb(M, box)` matches `aabb_of_8_corners(M·box.corners())` within `k_distance_epsilon`; rigid-transform OBB is exact (rotation + translation, det(M)=1); identity transform is a value-preserving no-op; degenerate scale (zero / negative axis) does not produce NaN — reports zero-volume AABB instead.
- **Existing pre-renewal expectations** — every shipped slice already meets the §4.1 performance budgets (Embree-comparison bench v1f, per-cluster perf suite at each `-close`). The renewed scope inherits these.

### Architectural rules (pinned, do not relitigate)

1. **`crd-geometry` does NOT depend on domain modules** (robotics, CAD, EDA, manufacturing, eylem internals). Domain modules may depend on `crd-geometry`.
2. **`crd-draw` consumes sampled points and debug primitives**; it does not own curve mathematics, mesh processing, or BVH traversal. v10e curves ship via `crd-geometry-viz` (which already depends on `crd-draw`).
3. **`crd-geometry-primitives` stays mostly pure / local-space.** World-space integration goes through consumer-side adapters (v11 helpers are *primitives* of the adapter, not the adapter itself).
4. **Heavy / offline geometry** (mesh repair, Boolean, remeshing, CAD topology, CAM toolpath, EDA DRC) is **separated from hot runtime query paths**. v7 mesh-processing is cooker/editor-tier; runtime never re-runs QEM.
5. **Every optimized path has a scalar / reference path** (v0f branchless + SIMD, v1g BVH4 SIMD vs scalar slab, v2h SIMD support vs scalar) — equivalence tested.
6. **Determinism is a substrate concern**: stable IDs, stable sorting (no `std::sort`), stable tie-breaks (lexicographic vertex tiebreak, lowest-index witness, X→Y→Z SAH axis), reproducible outputs across compilers / SIMD widths / OSes.
7. **Large-coordinate / degenerate / near-coplanar / near-collinear cases are part of the validation culture** (v1i-c degenerate corpus + large-coord sweep is the template).
8. **The active phase stays consumer-driven.** Algorithms are added because a Cerid consumer needs them, not because they are interesting. (V-HACD is in v9c because the editor needs it; B-spline curves are in v10a because cinematic + robotics need them; PCB DRC is NOT in 3.1.7 because `crd-eda` is its consumer and is a separate future phase.)

## New in-phase slices (renewed scope, 2026-05-14)

The four genuine gaps surfaced by the 2026-05-14 scope review land as
new in-phase slice clusters. **In-flight wording unchanged: v3d hull
simplification remains the next immediate slice** (per user directive
"we don't need to touch the current slice we are working on we can
finish them"). The renewed-scope additions ship *after* the v3 cluster
closes and the v4 / v5 / v6 / v7 / v8 / v9 clusters proceed —
v4-validate is inside the v4 cluster (after v4a–f), v10 lands as its
own cluster between v9 and v9e (or alongside v9e — schedule TBD when v9
lands), v11 lands at the v0 hardening tail or alongside v4-validate.

| Slice | Scope | LOC | Calendar |
|---|---|---|---|
| **v4-validate** ✅ 2026-05-16 | **`validate_triangle_mesh` — formal mesh validation pipeline stage** (shipped INSIDE the v4 `-mesh` cluster — see slice table above for full closure detail). Six defect kinds: OutOfBoundsIndex / DegenerateTriangle / ZeroAreaTriangle / NonManifoldEdge / BoundaryEdge / InconsistentOrientation. Three-pass deterministic algorithm (triangle scan → sorted edge table → run-classify by count). `MeshValidationReport{defects, triangle_count, vertex_count, manifold/boundary/non_manifold_edge_count, well_formed, watertight}`. `Area32` typed at options surface; raw f32 inside per ADR-0078 §5 D34. 10 cases / 46 assertions. Session log `docs/sessions/2026-05-16-geometry-v4-validate.md`. | ~400 + ~250 tests | done |
| **v10a** 📋 planned | **`crd-geometry-curves` module substrate.** New module `engine/geometry-curves/` (target `crd-geometry-curves`, ns `crd::geometry::curves`, deps `crd-core` + `crd-math` + `crd-containers` + `crd-geometry-primitives`). Curve primitive types: `Polyline3` (non-owning view + owning variant), `QuadBezier3`, `CubicBezier3`, `CubicHermite3` (position + tangent at each end), `CatmullRom3` (uniform + centripetal parameterization — the cinematic camera default), `BSpline3` (degree-3, owns knot vector `Array<f32>`), `CircularArc3` (center + plane + start_angle + sweep), `EllipseArc3`. 2D peers (`Polyline2`, `QuadBezier2`, `CubicBezier2`, `CircularArc2`, `EllipseArc2`) shipped same slice (mirrors the v0a/v0b 2D-peer-set discipline). Closed-curve flag on each primitive (closed B-spline / closed Catmull-Rom for loop paths). Templated on `T ∈ {f32, f64}` (matches v3 Quickhull + v2i f64 GJK precedent). System doc `docs/systems/geometry-curves.md`. | ~700 + ~400 tests | ~3 days |
| **v10b** 📋 planned | **Sampling + flattening.** `sample_uniform(curve, n_samples) → Array<Vec3>` (parameter-uniform — fastest, lowest quality at high curvature); `sample_adaptive(curve, tolerance) → Array<Vec3>` (chord-error subdivision: de Casteljau midpoint test for Bezier, recursive split for splines until segment chord-error < tolerance; the LOD default); `sample_by_curvature(curve, max_angle_step) → Array<Vec3>` (camera-banking / banking-quality default); `to_polyline(curve, tolerance) → Polyline3` convenience. Tolerance ties to `k_distance_epsilon` from v1h `constants.hpp`; per-call overrides allowed. Closed-curve sampling produces a closed polyline (first == last, marked). Determinism: no `std::sort`, no transcendental outside `crd::math::deterministic`. | ~500 + ~400 tests | ~3 days |
| **v10c** 📋 planned | **Arc-length system.** `build_arclength_table(curve, n_samples=64) → ArclengthTable` (cumulative-distance over a sufficiently-dense uniform sample; `Array<{t, distance}>`); `length_of(curve) → f32` total; `t_at_distance(table, distance) → f32` (binary search + linear interpolation) for constant-speed traversal; `distance_at_t(table, t) → f32` (inverse). Closed-curve modular distance (wraps mod length). Consumer: cinematic camera spline traversal (constant-speed dollying), robot trajectory constant-speed sampling, animation keyframe timing remap. Validation: `t_at_distance ∘ distance_at_t == identity` within `1e-6`; arc-length monotone in `t`. | ~300 + ~250 tests | ~2 days |
| **v10d** 📋 planned | **Curve queries.** `aabb_of(curve) → AABB3` (subdivision-bounded for Bezier/spline — recursive split until chord-AABB stable; exact for circular/elliptic arcs); `closest_point(curve, p, tolerance) → CurveClosestPoint{t, point, distance²}` (Newton-Raphson on `d/dt|curve(t) - p|² = 0`, initialized via subdivision rejection — global minimum, not local); `distance(curve, p)` convenience; `intersect_ray(curve, ray, tolerance) → optional<{t_curve, t_ray, point}>` against flattened polyline (curve-curve intersection deferred to v10 follow-up). Hooked into the unified `crd/geometry/queries.hpp` facade overload set. Consumer: editor curve-edit gizmo (closest-point), cinematic camera trigger volumes (ray-vs-spline-path), debug-draw cursor-pick on smoothed polyline. | ~400 + ~300 tests | ~2 days |
| **v10e** 📋 planned | **Frames + viz + sandbox showcase.** `tangent(curve, t) → Vec3` (first derivative); `normal(curve, t) → Vec3` (Frenet via second-derivative + Gram-Schmidt; defaults to a deterministic up-vector when the curvature is zero); `binormal(curve, t) → Vec3`; **rotation-minimizing frame** (`compute_rmf(curve, samples) → Array<{tangent, normal, binormal}>` via double-reflection method, Wang 2008 — the smooth-banked camera / smooth-banked trajectory default). `crd-geometry-viz` companion (already shipped v1j) extended with `draw_curve(buf, curve, samples)` / `draw_polyline(buf, polyline)` / `draw_tangent_frame(buf, curve, n_samples)` / `draw_rmf(buf, frames)`. Sandbox showcase scene added (`SandboxScene::CurvesShowcase`) — pickable curve type + control-point dragging + sample slider + frame mode toggle. **Curve-curve intersection deferred** to follow-up (advisor: ships when a real consumer asks; v10 ships sampling + AABB + closest-point + frames, the cinematic+robotics MVP). | ~400 + ~350 tests | ~2 days |
| **v11** 📋 planned | **Transform-aware query helpers in `crd-geometry-primitives`.** New `crd/geometry/primitives/transform.hpp` (in the existing `crd-geometry-primitives` module — NO new module per the 2026-05-14 scope decision; `crd-geometry-runtime` is a reserved-not-created slot, see ADR candidate 2). `TransformedShape<T>{ T shape; Mat4 to_world }` lightweight value-type wrapper; free functions `transform_aabb(Mat4, AABB3) → AABB3` (8-corner method — conservative AABB-of-transformed-corners, the standard pattern), `transform_obb(Mat4, OBB3) → OBB3` (rigid-exact for orthogonal + uniform-scale `M`; conservative-via-AABB for general affine — detected by checking `M` for orthogonality + uniform-scale at construction), `transform_capsule3` / `transform_cylinder3` (axis-line transform + radius scale by `|M|` average), `transform_ray3_to_local(world_ray, world_to_local) → Ray3` (the inverse-direction pattern that lets a raycast happen in local space then transform the hit back). Cross-domain decision pin (already in §11.3 above): **`crd-geometry` stays local-space pure**; world-space dispatch is a *consumer* concern (`crd-scene-spatial` / `crd-eylem` already adapt). v11 ships only the transform-shape helpers — the dispatch layer (`world_raycast(scene, ray)` / `world_overlap(scene, box)`) lives at consumer time, NOT here. Tests: `transform_aabb(M, box) ≈ aabb_of_8_corners(M·box.corners())` within `k_distance_epsilon` on rigid + scale + skew; identity transform is a value-preserving no-op (`transform_aabb(Mat4::identity(), b) == b` bit-exact); degenerate scale (zero / negative axis) does not produce NaN; OBB rigid-transform is exact (orthogonal `M`); 2D peers (`transform_aabb2` / `transform_obb2`) shipped same slice. | ~400 + ~300 tests | ~2 days |

**Renewed total: 51 slices, ~22 KLOC engine + ~5 KLOC editor-tier + ~4
KLOC cooker-emitted GLSL/HLSL, ~7.5–9.5 months calendar** (was 42 / 20.0
KLOC / 7–9 months prior to renewed-scope additions 2026-05-14; +1
v4-validate + 5 v10a–e + 1 v11 = +7 slices via renewed scope; +2 slices
2026-05-16 via v6 substrate separation (v6a–v6d planned → v6a/b/c/d/e
+ v6-close shipped; nothing cut). The earlier "Total" paragraph (42 /
20.0 KLOC / 7–9 months) is the pre-renewal historical sizing.

**Progress as of 2026-05-16:** 7 of 11 sub-modules COMPLETE
(`-primitives` ✅ + `-bvh` ✅ + `-convex` ✅ + v3 convex-hull extension ✅
+ `-mesh` ✅ + `-spatial` ✅ + `-polygon` ✅). Closed slice clusters by
date: v0 (2026-05-13) + v1 -bvh (2026-05-13) + v2 -convex (2026-05-14)
+ v3 Shewchuk + hull extension (2026-05-15) + v4 -mesh (2026-05-16) +
v5 -spatial (2026-05-16) + v6 -polygon (2026-05-16). **Next**: v7
`-mesh-processing` (Garland-Heckbert QEM + Loop subd + remesh + Liepa
hole-fill + repair + Taubin smoothing) — consumes v6e Bentley-Ottmann
for self-intersection scan.

## Performance budgets (per supplement dossier §4.1)

Targets per sub-module on a Zen 4 reference CPU (Win-shipping config,
full LTO + AVX2). Per-slice DoD measures against these:

| Sub-module / op | Target | Reference |
|---|---|---|
| Ray-vs-AABB — branchless NaN-safe slab (scalar) | ~2 ns/test | Tavianator / Williams 2005; no hot-path branches |
| Ray-vs-4-AABB — `Vec4f` batch slab | ~3–4 ns/4-test | one `Vec4f` min/max chain |
| Ray-vs-triangle — Möller-Trumbore (scalar) | baseline (~10 ns) | Möller-Trumbore 1997 |
| Ray-vs-triangle — watertight Woop 2013 (scalar) | ≤ 1.3× Möller-Trumbore | shear+scale overhead bought back by shared-edge consistency |
| Ray-vs-triangle — Baldwin-Weber precomputed | ≤ 0.8× Möller-Trumbore on read-mostly meshes | 9 mul + 6 add/ray; 48 B/tri storage |
| Ray-vs-8-triangle — `Vec8f` Woop / M-T batch | <10 ns/8-tri batch on AVX2 | matches the v4g per-leaf SIMD budget shape |
| BVH4 traversal — ray-vs-4-AABB node test | ~8 ns/node on AVX2 | Embree publishes ~6 ns; ~30% gap accepted for determinism |
| GJK — convex pair distance | 50–200 ns/pair, 2–6 iterations typical | Box2D v3 published envelope |
| Quickhull — 100k points to closed 3D hull | <30 ms | qhull reference: ~25 ms on identical input |
| Mesh closest-point — 100k-tri BVH-accelerated, `Vec8f` lane batch | <1 µs / 8-point batch | Embree per-leaf SIMD pattern |
| QEM simplification — 1M-tri to 100k-tri | <3 s | Garland-Heckbert ~2 s; ~50% headroom for determinism + container overhead |
| LBVH GPU build (v9a) — 1M primitives on RTX 3060 | <8 ms | Karras 2012 reports 4–6 ms on Fermi |

SIMD posture per sub-module (full table in supplement §4.1):
`-primitives` SIMD-natural; `-bvh` SIMD-critical; `-convex` partial;
`-mesh` SIMD batch queries; `-spatial` mostly scalar; `-polygon`
minimal SIMD; `-mesh-processing` SIMD for QEM only; `-delaunay` mostly
scalar; `-gpu` already SIMT.

## Reference reading

- **Base research dossier:** `docs/research/cerid-geometry.md`
  (11,523 words — industry survey + algorithmic scope + module split
  + slice plan + Phase 3.1.7 slot decision)
- **Supplement dossier:** `docs/research/cerid-geometry-supplement.md`
  (6,116 words — Inigo Quilez body of work + computational-geometry
  textbook foundation + SIMD performance contract + `crd::containers`
  integration patterns + GLSL/HLSL shader-helper proposed sub-module)
- **ADR:** `docs/decisions/0076-geometry-substrate-architecture.md`
  (Accepted 2026-05-11; supplement additions integrated 2026-05-11)
- **Foundational textbooks** (per supplement §3): Ericson "Real-Time
  Collision Detection" (v0/v1/v2 anchor); Eberly "Geometric Tools"
  (v0/v3 anchor); de Berg et al. "Computational Geometry" (v6/v7/v8
  anchor); Botsch et al. "Polygon Mesh Processing" (v7 anchor);
  Akenine-Möller et al. "Real-Time Rendering 4e" (broad).
- **Foundational web reference:** [iquilezles.org](https://iquilezles.org/articles/)
  — Inigo Quilez's article catalog (analytic distance functions 2D + 3D,
  smooth-blending operators, domain ops, raymarching, soft shadows, AO).
- **v0f cutting-edge intersection corpus** (research dossier §13):
  - Woop, Benthin, Wald (2013) — *Watertight Ray/Triangle Intersection*.
    JCGT 2(1). (Embree's default ray-tri; consistent sign across shared
    edges — the v4d `-mesh` leaf default + `crd-sdf` v2 mesh-bake.)
  - Baldwin, Weber (2016) — *Fast Ray-Triangle Intersections by
    Coordinate Transformation*. JCGT 5(3). (Precomputed unit-triangle
    transform; the cooked-static-mesh default.)
  - Williams, Barrus, Morley, Shirley (2005) — *An Efficient and Robust
    Ray-Box Intersection Algorithm*. JGT 10(1). (Precomputed
    `inv_dir` + sign-mask slab test.)
  - Ize (2013) — *Robust BVH Ray Traversal*. JCGT 2(2). (Boundary-
    consistent traversal — partner to Woop watertight tri; the v1g
    `RayPacket` precompute.)
  - "Tavianator" branchless slab form — Tavian Barnes (2011, 2015, 2022
    revisions), *Fast, Branchless Ray/Bounding Box Intersections* — the
    de-facto NaN-safe `tmin/tmax` formulation; mathematically equivalent
    to Williams 2005 with IEEE min/max ordering.
  - Plücker-coordinate edge tests — see Erickson (1997) *Plücker
    Coordinates Tutorial* + Ericson *RTCD* §5.3 / Eberly GTE
    line-triangle classification (sign-only, branchless edge functions).
  - Ericson *Real-Time Collision Detection* §5.1.5 — the Voronoi-region
    `closest_point(Triangle, p)` (v0b).

## Open questions (resolved by dossier)

1. Robust geometric predicates: Shewchuk 1997 adaptive precision vs
   fixed-precision-with-epsilon — pick one, document the contract.
2. Half-edge mesh: ship our own (~2K LOC) vs wrap CGAL (GPL — reject)
   vs wrap libigl (Eigen-based — reject for STL-discipline reasons) vs
   wrap OpenMesh (LGPL, large) — likely answer: ship our own, modeled
   on libigl's API but using `crd::containers`.
3. GPU BVH algorithm: LBVH (Lauterbach 2009 — Morton-code based) vs
   PLOC vs HLBVH — start with LBVH (simplest), reserve PLOC for v9b.
4. Convex decomposition: V-HACD is offline-only; whether a runtime
   decomposer is even useful in Cerid (probably not — editor tier).
5. Determinism strategy: how the cross-platform replay-hash CI
   exercises crd-geometry primitives (the dossier proposes a
   `crd-geometry-bench` config matrix mirroring eylem's v9b CI).
6. **Shader-helper library ownership** (added 2026-05-11 from
   supplement dossier §6.3): `crd-geometry-shader-helpers` (Cerid
   owns it from `crd-geometry`'s side) vs `crd-sdf-shader-helpers`
   (Cerid owns it from `crd-sdf`'s side). The math is geometric (shape
   distance + closest-point); the primary GPU-side consumer is
   sdf rendering (DFAO, DF-soft-shadow). **Decision deferred to
   Phase 3.1.5 close** — when crd-sdf's shader requirements are
   concrete, the consumer-weighted answer becomes clear. Until then,
   treat the library as Cerid-owned + name-agnostic; the cooker-side
   generator implementation is identical regardless of which module
   owns the symbol.

## References

Industry survey (in research dossier, ~10-12K words):
- **Game/physics**: Bullet btDbvt, PhysX PxBVH + Pxg GJK, Jolt, Havok HKBV, Box2D v3 b2DynamicTree, Unity DOTS, Unreal FBVHTree, Godot
- **Geometric processing**: CGAL (academic reference), Geogram (Inria), libigl (Eigen-based), Geometric Tools / Eberly (GTE), Open3D, OpenMesh, MeshLab core, PMP
- **Spatial acceleration**: Embree (Intel — gold standard), nanoRT, Boost.Geometry, S2 (Google geo)
- **Robotics**: FCL (Flexible Collision Library — exact analogue of what eylem needs), HPP-FCL, OMPL primitives
- **Polygon/mesh**: Vatti, Greiner-Hormann, Sutherland-Hodgman, Boost.Polygon, Clipper2 (Angus Johnson)
- **Convex decomposition**: V-HACD, HACD, CoACD
- **Tetrahedralisation**: TetGen, fTetWild
- **Mesh repair**: MeshFix, libigl::is_edge_manifold, OpenMesh mesh-doctor

Foundational papers cited in the dossier (preliminary):
- Shewchuk 1997 — Adaptive precision floating-point arithmetic + fast
  robust geometric predicates
- Jacobson 2013 — Robust inside-outside segmentation using
  generalised winding numbers (the standard for non-watertight mesh)
- Garland & Heckbert 1997 — Surface simplification using quadric
  error metrics (QEM)
- Lauterbach 2009 — Fast BVH construction on GPUs (LBVH)
- Karras 2012 — Maximizing parallelism in the construction of BVHs,
  octrees, and k-d trees
- Catto GDC 2019 — Dynamic AABB Tree (the eylem v1c reference)
- Bowyer 1981 + Watson 1981 — Computing Dirichlet tessellations (2D
  Delaunay)

(Full citation list lands in the research dossier.)
