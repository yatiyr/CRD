# D-007 + D-008 (MERGED) — the GPU program system: one IR for every shader, one device

> **This is the single master doc for the shader/kernel detour.** It merges the two former detours — **D-007** (CKIR
> becomes the universal shader IR) and **D-008** (the gpu-context convergence: one device, one program surface) — into ONE
> ordered subslice table. The old two-doc split made the sequence unreadable (two different "C3"s, interleaved B/C slices);
> this replaces both. `D-008-gpu-context-convergence.md` is now a stub pointing here.

- **North-star ADRs:** [0101](../decisions/0101-ir-is-source-of-truth-for-all-shaders.md) (the IR is the single source of
  truth) · [0102](../decisions/0102-render-data-lighting-pass-architecture.md) (how the IR feeds the renderer) ·
  [0103](../decisions/0103-gpu-context-owns-every-gpu-program.md) (gpu-context owns every GPU program; supersedes 0099 §6).
  Corpus: `docs/systems/shader-ir-corpus-and-stages.md`.

## The mission (why the IR must be frontier-complete)

Compute the best + most interesting things end to end — **ML/AI · FFT · simulations · skinning · particles · light
systems · rendering that out-beauties Unreal · ray tracing · beautiful + stylized (toon/NPR) effects · anything a GPU can
do** — portably across ALL backends, correct + performant + bit-exact. One IR (CKIR) is the single source; GLSL / HLSL /
WGSL / MSL / CUDA / HIP are derived OUTPUTS, never authored or stored. This detour hardens that IR + the device surface it
runs on; the actual render pipelines / physics / editor are built ON it in their own phases. **No compromises — the
frontier version of every capability, by construction.**

## The two invariants (grep-gated, not prose)

> **No module outside a backend names a shading language or a bytecode.**
> **I1** — GLSL/HLSL/WGSL/MSL/CUDA source lives only inside one backend TU, between our emitter and the vendor compiler.
> **I2** — SPIR-V/DXIL/PTX bytes never appear in a public header; a consumer holds an opaque `IGpuProgram`, nothing else.

Currency **in** = the IR (`KGraph` + `KEntry`). Currency **out** = `IGpuProgram`. **Both are CLOSED** (C0…C2): the
`crd-no-shader-language-leak` gate is green with an empty allowlist, and `ShaderModule`/raw-SPIR-V is gone from every public
header. One `VkDevice` — the `VulkanGpuContext` owns it; `rhi-vulkan` adopts, never creates.

## ⭐⭐ Legacy path vs CKIR path — where we are, where we LAND (user 2026-07-12, "very important")

The engine is **mid-migration**, and there are TWO shader/render paths in the tree right now. This is deliberate and temporary:

- **LEGACY path (the shipped runtime today):** the runtime renderer + the `runtime/examples/smoke_*.cpp` render through
  **rhi** pipelines with the **hand-authored GLSL Effect system** — `reflect_triangle.vert`/`.frag` source files → `crd::shader::EffectDesc`
  → `runtime->create_effect` → `device->create_program(cooked SPIR-V)`. GLSL is written BY HAND here.
- **CKIR path (this detour, built in `tests/gpu-context-*` first):** everything is authored in **backend-neutral CKIR**
  (`KGraph`+`KEntry`); GLSL/HLSL/WGSL/MSL/PTX appear ONLY as **emitter output inside a gpu-context backend** — never
  hand-written, never stored. `create_program(KGraph,KEntry)` → `IRasterContext`/`IComputeContext` → the GPU.

What is **already unified**: the **device seam** (C2/D-008). The runtime bootstraps a `create_vulkan_gpu_context(...)` and rhi
**adopts** its `VkDevice` — ONE device, gpu-context-owned. What is **not yet migrated**: the runtime's actual **rendering** still
runs on rhi pipelines + the GLSL Effect system; it does not yet route draws/dispatches through the gpu-context contexts + CKIR.

Two more "cook"-named things that are NOT the B8-k cook: **D1** serializes the CKIR/material graph into the versioned **`.crdr`**
resource (replacing stored GLSL/HLSL text); **D2** is the offline **cook** (IR → SPIR-V/DXIL/MSL/WGSL/PTX bytecode, content-hash
cached). The **B8-k "cook seam"** is the in-memory material→per-pass-program assembly that FEEDS D1/D2 — it touches no `.crdr` yet.

> **⛔ THE ENDGAME (non-negotiable): after this detour is FULLY over, the engine is 100% CKIR.** The runtime renderer migrates
> ENTIRELY onto CKIR → gpu-context (`IRasterContext`/`IComputeContext`) + the `.crdr` cook (D1–D5). The legacy GLSL Effect
> system (`crd::shader::EffectDesc`, `create_effect`, the hand-authored `.vert`/`.frag` files, the SPIR-V-source path) is
> **DELETED** — no dual path survives. GLSL/HLSL/etc. remain ONLY as machine-emitted backend outputs behind I1/I2. The
> transitional coexistence above exists solely so the shipped engine keeps rendering while the CKIR path is hardened; it is a
> bridge, not a destination. **Full CKIR, no exceptions, once the detour lands.**

## ▶ The locked order (user 2026-07-11): FULL visual frontier FIRST, then hesap-GPU

The compute substrate hesap-GPU needs is **already proven** (GEMM at cuBLAS parity, 6 backends, bit-exact) — so hesap is a
dependency-light return whenever wanted. The user chose to complete the **entire** visual frontier first anyway. hesap-GPU
is the LAST stop of this detour, not an early exit. Two side tracks per slice: **IR** = the shader/emitter side (was
D-007); **DEV** = the device/context side (was D-008).

> **⭐ Render→compute dependencies are built DURING this detour (user 2026-07-12).** CKIR *is* the GPU compute compiler, so
> the compute PRIMITIVES the frontier rendering needs — **FFT** (convolution/physically-accurate bloom + glare · FFT ocean),
> GPU **sort** (Gaussian-splat depth · OIT · light lists), **scan / stream-compaction** (clustered light-cull · Nanite
> cluster-cull · particle compaction), **reduction** (auto-exposure histogram · SDSM / Hi-Z depth min-max · luminance), small
> **GEMM / MLP** (neural radiance cache · neural materials, w/ B10 coop-vectors) — are implemented here as CKIR compute kernels
> (bit-exact CPU oracle + both backends) as part of the slice that needs them. This serves rendering now AND hesap-GPU later
> from one source. The BROADER hesap-GPU (dense/sparse LA, eigensolvers, ODE, stats — the parts rendering never calls) stays
> post-detour. Each gap slice below flags its compute dependency where one exists.

## ══════ THE MASTER SUBSLICE TABLE (execution order) ══════

> **The first column is the tick.** `✅` = done · `◧` = in progress (some sub-slices done) · `⬜` = todo. `◀ NEXT` marks the current front. Original slice IDs are kept
> in the **id** column for traceability (sessions/commits/ADRs reference them). Everything above the `── frontier ──` line
> is DONE.

| ✓ | # | id | side | slice — one-line contract |
|---|---|----|------|---------------------------|
| ✅ | 1 | A1–A4 | IR | **CKIR core** — algebra + bit ops · transcendental/intrinsics · value types (vec/mat/quat + swizzle + geometric + interp) · control flow (`unroll_for` + dynamic `for_loop`/`while_loop`/`switch`). Bit-exact CPU oracle + Vulkan + DX12. |
| ✅ | 2 | fan-out | IR | **all backends get the corpus + type layer** — Vulkan(GLSL) · DX12(HLSL) · WebGPU(WGSL, real hw) · CUDA(PTX, scalarized, bit-exact) · Metal(MSL, emit-only). (HIP ⬜ = later host, v17-o.) |
| ✅ | 3 | B0 | IR | **type system** — `KType{scalar,kind,rows,cols}` · `mat2`/non-square R×C · `bool`/`bvec`/`ivec`/`uvec` · fixed-size **arrays** + **structs** (SROA). Type-aware emitters, all 5 backends. |
| ✅ | 4 | B3-a / B3-a′ | IR | **IR stage model** — `KStage` = the **14 SPIR-V execution models** (compute→mesh→RT) · `StageIn`/`Builtin`/`UniformBlock` leaves · **32 builtins** (type + legal-stage mask in one table) · `entry_valid()`. |
| ✅ | 5 | C0 | DEV | **the program seam** — `crd/gpu/program.hpp` (`ShaderStage`×14 + opaque `IGpuProgram`) · `create_program(cooked SPIR-V)` · gpu-context-vulkan absorbs shaderc/dxc PRIVATE · `crd/shader/compile.hpp` DELETED · **I1 gate live**. |
| ✅ | 6 | C1 | DEV | **`IRasterContext`** — shader-object draw (`VK_EXT_shader_object`) on **dynamic rendering**, bindless-first · `create_program(KGraph,KEntry)` compute on-ramp. (C1-a/b/c ✅; C1-d folded into C2.) |
| ✅ | 7 | C2 | DEV | **device unification — ONE `VkDevice`** — gpu-context render-capable → rhi ADOPTS it → sandbox on one device → **I2 closed** (ShaderModule retired) → **I1 closed** (Effect frontend off shaderc, `crd-shader-vulkan` bridge) → rhi creates no device. (C2-a…f ✅.) |
| | | | | ── **frontier** (everything below is ⬜; do in this order) ── |
| ✅ | 8 | B3-c | IR | **GLSL VS+FS emitters** — hoisted the ~60-case value switch into a shared `emit_value_stmt` (compute byte-exact, kir-vulkan 33010 unchanged); `emit_stage_glsl` adds the raster prologue (`layout(location)` in/out, `layout(set,binding,std140)` UBO, per-stage builtins) + epilogue (`gl_Position`, colour attachments, `gl_FragDepth`); `create_program(KGraph,KEntry)` routes Vertex/Fragment through the seam. **Gate green: a KIR VS + FS entry each compile to a valid SPIR-V program (gpu-context-vulkan 66/10); a vertex with no clip position is refused.** |
| ✅ | 9 | B3-d | IR | **HLSL VS+FS mirror** — extracted the HLSL value switch into a shared `emit_value_stmt_hlsl` (compute byte-exact, kir-dx12 30821 unchanged); `emit_stage_hlsl` emits struct-based VS/FS I/O (`[[vk::location(N)]]` pins the SPIR-V location · `SV_Position`/`SV_VertexID`/`SV_IsFrontFace` builtins · `cbuffer register(bN,spaceN)`). **Gate green: a KIR VS + FS entry each emit HLSL that DXC lowers to valid SPIR-V (gpu-context-vulkan 73/11).** (The DX12 gpu-context is compute-only today, so the gate compiles via DXC; the DX12 raster *program* seam lands with C4.) |
| ✅ | 10 | C4 | DEV | **DX12 `IRasterContext`** — D3D12 mirror of C1: a graphics (DIRECT) queue + offscreen RGBA8 render targets + a clear-with-readback (**C4-a**), then a `Dx12GpuContext : IGpuContext` that mints `Dx12GpuProgram`s (cooked DXIL, or CKIR→HLSL→DXIL via `emit_stage_hlsl`+dxc — the promised **real DX12 create-program seam**) and a graphics-PSO `create_raster_program` + `DrawInstanced` (**C4-b**). **Gate green (real D3D12, MSVC + clang-cl): clear+readback exact incl. 256-byte padded row-pitch · KIR VS+FS → DXIL programs (a no-clip-position vertex refused) · a red triangle over a blue clear reads back centre=red / corner=blue (gpu-context-dx12 3/3).** |
| ✅ | 11 | B3-e | IR | **the actual draw — IR-authored, both backends** — ONE shared backend-neutral CKIR triangle (`tests/gpu-shared/ckir_raster_triangle.hpp`: `VertexIndex` → 3 clip positions via a select-on-index chain, SROA-safe; constant-red FS) drives `create_program(KGraph,KEntry)` → `create_raster_program` → `draw` → pixel readback on **both** Vulkan (GLSL→SPIR-V→shader objects) and DX12 (HLSL→DXIL→graphics PSO). **Gate green (real GPUs, MSVC + clang-cl): the identical IR draws centre=red / corner=blue on both backends (gpu-context-vulkan 12/83, gpu-context-dx12 5/51).** Surfaced + fixed a latent emitter bug: the shared `emit_value_stmt`/`emit_value_stmt_hlsl` emitted an **integer** `Const` with a float literal (`int t = 0.0`) — type-strict GLSL rejects it (HLSL coerced, so DX12 masked it); both now emit int literals; compute byte-exact intact (kir-vulkan 33010, kir-dx12 30821 unchanged). |
| ✅ | 12 | B1 | IR | **fragment-stage foundation** — sub-slices: **[✅ B1-a] derivatives** `dFdx`/`dFdy`/`fwidth` (fragment-only KOps + `is_fragment_only_op` gate in `entry_valid`; GLSL `dFdx`/HLSL `ddx`; CPU oracle = 0; **gate green on real GPUs MSVC+clang-cl: `dFdx(FragCoord.x)`=1 / `dFdy`=0 read back on Vulkan + DX12, compute byte-exact kir 33010/30821 unchanged**) · **[✅ B1-b] `discard`/alpha-test** (`KEntry::discard_cond` bool node + `entry_valid` fragment-only gate; GLSL/HLSL `if (cond) { discard; }` before outputs; **gate green Vulkan+DX12 MSVC+clang-cl: a cutout on `FragCoord.x<16` keeps the right half red / clears the left; compute byte-exact unchanged**) · **[✅ B1-c] interpolation qualifiers** (`Interp`{Smooth·Flat·NoPerspective·Centroid·Sample} on `stage_in`/`KStageOutput`; `entry_valid` forces int interpolants to `flat` both sides; GLSL `flat/noperspective/centroid/sample` + HLSL `nointerpolation/…`. **All four OBSERVABLY tested on Vulkan + DX12**: flat int payload · noperspective screen-linear vs perspective-correct on a `w={1,4,1}` triangle · **centroid + sample on NEW MSAA targets** (`create_color_target_ms`: Vulkan AVERAGE-resolve attachment, DX12 `ResolveSubresource` + per-sample-count PSO) — centroid diverges at edges, a `sample`-qualified step antialiases the threshold (32 greys) vs `smooth`'s hard edge. **⭐ Fixed 3 latent NVIDIA-masked bugs**: HLSL `SV_Position` emitted FIRST stole VS output register 0 → every interpolant `E_INVALIDARG`'d the DX12 PSO (now emitted LAST); `sample`→`SampleRateShading` and `discard`→`DemoteToHelperInvocation` needed their device features enabled. **Gate green MSVC+clang-cl+ASan+shipping: gpu-context-vulkan 134/18 · dx12 97/11 · Vulkan validation SILENT across all 18 · compute byte-exact unchanged · C2 adoption + rhi-vulkan intact**) · **[✅ B1-d] frag-depth write + early-Z flag + conservative depth** (`KEntry::early_fragment_tests` + `DepthMode`{Any·Greater·Less}; `entry_valid` forbids early-tests+depth-write and requires a write for a conservative mode. GLSL `layout(early_fragment_tests) in;`/`layout(depth_greater/less) out gl_FragDepth`; HLSL `[earlydepthstencil]`/`SV_Depth{,GreaterEqual,LessEqual}`. **NEW depth-buffer infra both backends** — `create_color_depth_target` + `draw_depth(clear_depth, DepthCompare)`: Vulkan D32 image + dynamic depth state + dynamic-rendering depth attachment; DX12 D32 texture + DSV + depth-compare BAKED into the PSO (`Dx12RasterProgram` cache now keys on (samples, depth-func)). **Observable frag-depth**: fullscreen tri at primitive depth 0 + FS writing `gl_FragDepth=FragCoord.x/32`, 0.5 clear/LessEqual ⇒ left passes red / right fails to clear — appears ONLY because the shader wrote depth. **⭐ Fixed a DXIL rule**: `SV_DepthGreaterEqual`/`LessEqual` + reading `SV_Position` needs the position input `noperspective centroid`. **Gate green MSVC+clang-cl+ASan+shipping: gpu-context-vulkan 157/21 · dx12 117/14 · Vulkan validation silent on the depth path · compute byte-exact kir 33010/30821 unchanged**) · **[✅ B1-e] per-fragment/draw VRS out** (`VK_KHR_fragment_shading_rate`) — VARIABLE-RATE SHADING, ALL THREE rate sources both backends: per-draw PIPELINE rate (`draw_vrs`), per-PRIMITIVE shader output (`KEntry::shading_rate` → GLSL `gl_PrimitiveShadingRateEXT` / HLSL `SV_ShadingRate`; packed `(Yshift<<2)|Xshift`, 2×2=5, identical encoding), and ATTACHMENT per-tile image (`create_color_vrs_target`: Vulkan R8_UINT clear + `VkRenderingFragmentShadingRateAttachmentInfoKHR`, DX12 upload + `RSSetShadingRateImage`). Device infra: Vulkan enables the ext + pipeline/primitive/attachment features + tile-size query; DX12 `D3D12_OPTIONS6` Tier-2 + `ID3D12GraphicsCommandList5`. ⚠ enabling VRS makes `vkCmdSetFragmentShadingRateKHR` MANDATORY for shader-object draws ⇒ a 1×1 default in `set_draw_state`. **Observable via 2×2 blockiness** (a ramp FS ⇒ each 2×2 block shares one invocation): per-draw 0→512, per-primitive 512, attachment 512 on both backends. **Gate green MSVC+clang-cl+ASan+shipping: gpu-context-vulkan 177/24 · dx12 130/17 · Vulkan validation silent on the VRS path · compute byte-exact unchanged**) · **[✅ B1-f] conservative raster + inner coverage + fragment interlock/ROV + FS storage buffers** — four capabilities behind append-only interface additions (`ConservativeMode`{Off·Overestimate·Underestimate}; `supports_conservative_raster/inner_coverage/fragment_interlock`; `draw_conservative(mode)`; `IStorageBuffer`+`create_storage_buffer`/`draw_storage`). IR: `KOp::StorageLoad`+`g.storage_load(idx)`, `KEntry{storage_write_index,storage_write_value,interlock}`, `KBuiltin::InnerCoverage` (both appended at END; `entry_valid` gates uint index/value + fragment-only interlock). Emitters: GLSL `coherent buffer` SSBO + `GL_ARB_fragment_shader_interlock` `layout(pixel_interlock_ordered) in;` + begin/endInvocationInterlockARB + `gl_FragFullyCoveredNV`; HLSL `RasterizerOrderedStructuredBuffer`/`RWStructuredBuffer` u0 + `SV_InnerCoverage`. Device: Vulkan `VK_EXT_conservative_rasterization`+EDS3 (conservative-mode + extra-overestimation-size) + `VK_EXT_fragment_shader_interlock` (pixel-ordered) + `fragmentStoresAndAtomics`; DX12 `D3D12_OPTIONS` ConservativeRasterizationTier + ROVsSupported. Binding parity: every program carries the FS storage slot (Vulkan set-0 SSBO / DX12 u0 UAV table, unaccessed by non-storage draws). Conservative is PSO-baked on DX12 (cache-keyed) vs EDS3 dynamic on Vulkan. **Observables IDENTICAL both backends**: OVERESTIMATE covers 242→312 red pixels; inner coverage 220 white(interior)/92 black(edge) under overestimate; interlock RMW = EXACTLY 2 at the two-triangle overlap, 0 at background. ⚠ **Validation caught 4 real bugs silent with layers off**: overestimate+shader-objects also needs `vkCmdSetExtraPrimitiveOverestimationSizeEXT` (VUID-07632); FS storage write needs `fragmentStoresAndAtomics` (NonWritable-06340); an FS reading `SV_InnerCoverage` links ONLY into a conservative PSO ⇒ the program carries a `wants_conservative_raster()` bit (from the graph at IR→program time) to prebuild it conservative (DX12 debug-layer ERROR); and `KOp::StorageLoad` had been mid-inserted → moved to END. **Gate green MSVC+clang-cl+ASan+shipping: gpu-context-vulkan 201/27 · dx12 151/20 · both backends validation/debug-layer CLEAN · compute byte-exact kir-vulkan 33010 unchanged · 13 files tidy-clean.** **▶ B1 COMPLETE.** |
| ✅ | 13 | B2 | IR | **texture & sampler system** — SEPARABLE texture+sampler IR types (`TKind::Texture`/`Sampler`, `TexDim` + arrayed/ms/shadow/array-count in `KType`; the portable model HLSL/WGSL/MSL use). Sub-slices: **[✅ B2-a] 2D sampling foundation** — `texture`/`sampler` binding leaves + `tex_sample`; GLSL `texture(sampler2D(t,s),uv)` / HLSL `tex.Sample(s,uv)`; NEW device infra (create+upload a 2D RGBA8 image + default sampler; Vulkan set-0 layout gains sampled-image(1)+sampler(2), DX12 root sig gains SRV(t1)+sampler(s2) + a sampler heap). Observable: left-red/right-green texture → screen L=red/R=green, pixel-exact both backends. · **[✅ B2-b] sample-op family** — `sampleLod`/`sampleGrad`/`sampleCmp`(shadow, + comparison sampler + depth texture)/`texelFetch`/`gather`(literal channel)/`textureSize`; texelFetch reads exact texels, textureSize=exact 16×16, gather L=255/R=0, shadow L=white/R=black — identical both backends. · **[✅ B2-c] dimensions** — 1D/3D(volume)/Cube(IBL, direction-sampled)/2DArray(CSM, layer-indexed)/CubeArray(`vec4(dir,cube)`), all observable both backends (Vulkan one generalized image creator; DX12 per-subresource footprint upload + per-kind SRV ViewDimension). **2DMS**: emitter wired (`sampler2DMS` / `Texture2DMS.Load(coord,sample)`) but its observable needs a render-to-sampled-MSAA path (render-to-texture) — MSAA data can't be uploaded; rides B8. · **[✅ B2-d] BINDLESS** — a `texture(...,N)` descriptor array + `tex_sample_at(index)` with a DYNAMIC per-fragment index; GLSL `texture2D tex[N]` + `nonuniformEXT` / HLSL `Texture2D tex[N] : register(t3)` + `NonUniformResourceIndex`; Vulkan enables descriptor-indexing (set-0 binding 3 = image array[8]), DX12 a t3[8] SRV table (Tier-2). Observable: 2-texture array, index=screen-side → L=red/R=green both backends. · **[◧ B2-e] sampler feedback** — DX12-native `SV_FeedbackTexture*` / `WriteSamplerFeedback`; documented as a DX12-only virtual-texturing capability (Vulkan has NO standard equivalent — a real divergence) that also needs mipped textures + feedback transcode → lands with the texture-streaming system, not core sampling. **⭐ Validation caught 2 real VUIDs** (green ≠ clean): a CubeArray view + its `SampledCubeArray` SPIR-V capability need `imageCubeArray` (VUID-viewType-01004 / pCode-08740). **Gate green MSVC+clang-cl+ASan+shipping: gpu-context-vulkan 309/32 · dx12 246/25 · both backends validation/debug-layer CLEAN · compute byte-exact kir-vulkan 33010 unchanged · 11 files tidy-clean.** **▶ B2 core (a–d) COMPLETE.** |
| ✅ | 14 | B5 | IR | **PBR surface output — lighting-agnostic** (ADR-0102 D3: material = surface response; render path = lighting). New `ckir_material.hpp` material-profile layer. **[✅ B5-a] surface contract + deferred G-buffer (MRT)** — the canonical OpenPBR surface struct + `pack_gbuffer` (surface → 4 RGBA8 MRT: base+metallic · normal_enc+roughness · emissive+occlusion · opacity), and the **NEW MRT device capability** (Vulkan dynamic-rendering N colour attachments + N-attachment blend/write dyn-state; DX12 N RTVs + PSO `NumRenderTargets=N`, cache-keyed) via `IGBufferTarget`/`create_gbuffer_target`/`draw_gbuffer` w/ per-attachment readback. **[✅ B5-b] the FULL OpenPBR 1.1 slab** — ~37-field append-only struct (base extras · specular w/color/ior/aniso · transmission · subsurface · coat · fuzz/sheen · thin-film · geometry thin_walled/tangent/coat_normal · emission) with spec defaults (`surface_defaults`); an EXTENDED 8-attachment G-buffer (`pack_gbuffer_ext`) carries every layer for the observable (IORs/aniso stay in the struct for B8's BRDF). **[✅ B5-c] shading-model + alpha taxonomy** — fresh `ShadingModel`{Standard·Unlit·Toon·Cel·Gooch·Outline·Hatching} + `AlphaMode`{Opaque·Masked·Translucent·Additive} tags in the surface (packed into gbuf3), + `set_masked` (alpha-test discard via the B1-b mechanism). **Observables pixel-IDENTICAL Vulkan vs DX12**: a material writes its surface → G-buffer, every core+slab channel + the shading-model tag (Gooch=4) read back exact; masked opacity ramp discards the sub-cutoff half. ⚠ reconciled with the shipped renderer `SurfaceData` append-only stability rule (added `opacity` + slab layers, never reorder). **Gate green MSVC+clang-cl+ASan+shipping: gpu-context-vulkan 356/35 · dx12 290/25 · both backends validation/debug-layer CLEAN · compute byte-exact kir-vulkan 33010 unchanged · tidy-clean.** **▶ B5 COMPLETE — the surface-material contract is fixed; B6 (MaterialX node library) → B7 (lowering) → B8 (renderer + lighting library) build on it.** |
| ✅ | 15 | B6 | IR | **material node library (MaterialX-parity + NPR)** — SOURCE (noise perlin/simplex/worley/fBm · shapes · geometric · texture) · OPERATOR (math/logical/adjustment/compositing/conditional/channel/convolution) · UV (panner/rotator/triplanar) · NPR nodes. Each bit-exact vs the MaterialX reference. **[✅ B6-a] the OPERATOR set** — new `ckir_nodes.hpp` (namespace `crd::kir::nodes`), ~60 MaterialX-stdlib nodes: math (add..power/trig/geometric) · logical (and/or/not/xor) · conditional (ifgreater/ifgreatereq/ifequal/switch) · channel (combine/separate/extract) · adjustment (remap/smoothstep/luminance ACEScg/rgbtohsv/hsvtorgb/contrast/range/saturate/hsvadjust) · compositing (mix + plus/minus/difference/burn/dodge/screen/overlay/disjointover + Porter-Duff over/in/out/mask/matte + inside/outside/premult/unpremult) · convolution (heighttonormal). Every node transcribed from the MaterialX genglsl/nodegraph reference (constants fetched from MaterialX source: floor-`mod`, ACEScg luma `0.2722287,0.6740818,0.0536895`, overlay bg-pivot, M_FLOAT_EPS=1e-8) + **bit-exact on the CPU oracle** (F64, `tests/kir/test_ckir_nodes.cpp`, 5 cases) + **GPU observable both backends** (overlay per-channel branch flips at screen-centre → 57,36,14 / 244,227,210 pixel-IDENTICAL Vulkan vs DX12). Per-channel-branched blends (burn/dodge/overlay) decompose to scalar-per-channel (core `Select` takes a scalar cond). · **[✅ B6-b] SOURCE (noise/shapes/geometric)** — new `ckir_noise.hpp` (namespace `crd::kir::nodes::noise`): a faithful transcription of MaterialX `mx_noise.glsl` (the OSL oslnoise subset). **NOISE: perlin 2D/3D · cell 2D/3D · fractal(fBm) 2D · worley(cellular) 2D/3D** (all 4 distance metrics euclid/dist²/manhattan/chebyshev + both styles) — **bit-exact vs the mx_noise reference on the CPU oracle** (`tests/kir/test_ckir_noise.cpp`, C++ port of the reference incl. the 4-arg bjmix hash) AND **rendering pixel-identically on both backends** (perlin 139/139 · worley 154/154 Vulkan vs DX12). **SHAPES** (in `ckir_nodes.hpp`): ramplr/ramptb/checkerboard (CPU-oracle bit-exact) + splitlr/splittb (fragment-only, `mx_aastep`). **GEOMETRIC**: position/normal/tangent/bitangent/texcoord/geomcolor (thin `stage_in` wrappers; texture-source nodes ride the B2 sampling infra). The 32-bit Bob-Jenkins hash reproduces on the f64/i64 IR via split-rotl (no >2^53 intermediate) + `&0xFFFFFFFF` wraparound; U32-typed nodes → `uint` emit (logical `>>`). **⭐ Surfaced + fixed 2 real emitter bugs the GPU caught but the CPU oracle couldn't**: the raster value emitters (`emit_value_stmt`/`emit_value_stmt_hlsl`) lagged the compute emitter — missing the bitwise ops + `Trunc/Ceil/Round/Sign/Tan/Asin/Acos/Atan2/Smoothstep` (needed by B6-a too); and U32 constants > INT_MAX emitted without a `u` suffix (GLSL int-overflow + `uint`-vs-`int`-literal). **Compute byte-exact HELD (kir-vulkan 33010 / kir-dx12 30821 unchanged).** · **[✅ B6-c] UV / vector transforms** (in `ckir_nodes.hpp`): `rotate2d` (mx_rotate_vector2) · `rotate3d` (Rodrigues, mx_rotate_vector3) · `place2d` (the panner/rotator UV transform, both SRT + TRS operation orders, NG_place2d) · `triplanar_weights`+`triplanar` (the NG_triplanarprojection blend: normalize-abs-normal → pow(1/clamp(blend,.03,1)) → re-normalize → weighted sum; the full 3-image projection composes this with B2 sampling at B8). **All CPU-oracle bit-exact** (`[uv]`) + **place2d observable pixel-identical both backends** (98/98) — validating Radians/Sin/Cos on the raster path. **No regressions (gpu-context-vulkan 388/39 · dx12 318/32; kir CPU 399/49).** · **[✅ B6-d] NPR** (`ckir_nodes.hpp`, MaterialX nprlib): `viewdirection` (stage input) · `facingratio` (faceforward/invert-aware `dot(v,n)`, NG_facingratio) · `gooch_shade` (warm/cool `mix((1+N·L)/2)` diffuse + Phong `pow(max(dot(-L,reflect(V,N)),0),shininess)·spec` specular, NG_gooch_shade). CPU-oracle bit-exact (`[npr]`) + **gooch observable pixel-identical both backends** (193,193,181) — validating `reflect` on the raster path. **▶ B6 COMPLETE (2026-07-12).** Full cross-config close: **win-debug + clang-cl (both backends) + asan (clean) + shipping all GREEN**; kir CPU 400/50 · gpu-context-vulkan 395/40 · dx12 324/33; **compute byte-exact HELD (kir-vulkan 33010 / kir-dx12 30821 unchanged)**; validation-clean inherited (B6 adds NO device features — pure value-graph logic on the already-clean raster path); all files tidy-clean (LLVM-20). ⭐ Fixed 2 real emitter bugs the GPU caught but the CPU oracle couldn't (raster value emitters lagged compute on the bitwise + trig/round ops; U32 consts needed a `u` suffix). **The full MaterialX-parity node library is in — B7 (graph→IR lowering + stage split) → B8 (renderer + lighting library) build on it.** |
| ✅ | 16 | B7 | IR | **material lowering + stage split** — graph → IR compiler: per-vertex/per-fragment "cheapest correct stage" classification · const-fold + DCE · round-trip bit-stable · static switches → `ShaderOption` variants. New `engine/kir/include/crd/kir/ckir_lower.hpp` (namespace `crd::kir::lower`). **[B7-a] frequency classification** — `Frequency`{Constant·Uniform·Vertex·Fragment} + `classify(g,entry,out[])`: each node's cheapest correct stage (a Const is compile-time; a `UniformBlock`/texture is per-draw; a vertex builtin/VS attribute is per-vertex; an interpolated varying/FragCoord/derivative/implicit-LOD sample/storage read is per-fragment; interior = MAX of inputs, pinned to Fragment by the fragment-only ops) + `entry_frequency`. **`lower(g,roots,n)`** wraps the existing semantics-preserving `KGraph::optimize` (const-fold → DCE → CSE hash-cons) + `lower_entry(g,e)` (gathers an entry's roots, lowers, writes them back — the one call a cook/renderer makes). **[B7-b] stage split + variants** — `uniform_boundary` finds the MAXIMAL uniform subexpressions consumed on the per-fragment path (the value-neutral hoist sites; physical VS-varying/uniform-buffer materialization lands with B8); `specialize(g,option,value,roots,n)` pins a `ShaderOption` selector to a compile-time constant, const-folds it into the switch conditions, then `fold_static_branches` collapses each static-cond `Select` to its chosen branch (new `KGraph::pin_const`/`alias` primitives) and DCE reclaims the dead branch → a smaller VARIANT bit-identical to the runtime switch (the full permutation matrix + dedup is D3). **ROUND-TRIP BIT-STABLE proven**: `lower`/`specialize` eval bit-identical on the CPU oracle (`tests/kir/test_ckir_lower.cpp`, 86 asserts/5 cases: classification correctness · fold/DCE/CSE bit-stable + idempotent · uniform boundary · variant == runtime branch) AND **[B7-c] a LOWERED B6 material renders PIXEL-IDENTICAL on both backends** (overlay 57,36,14 / 244,227,210 Vulkan == DX12 == un-lowered). **▶ B7 COMPLETE (2026-07-12).** Cross-config close: win-debug + clang-cl (both backends) + asan (clean) + shipping GREEN; kir CPU 486/55 · gpu-context-vulkan 406/41 · dx12 334/34; **compute byte-exact HELD (kir-vulkan 33010 / kir-dx12 30821 unchanged)**; tidy-clean. **The IR compiler that feeds B8 the cook path is in — B8 (renderer integration + lighting library) is next.** |
| ✅ | 17 | B8 | IR | **renderer integration — the FRONTIER lighting + shadow system (GOLD STANDARD)** — the material RENDERS lit, shadowed, skinned, through the frame graph on Vulkan + DX12. **⚠ SCOPE EXPANDED (user 2026-07-12: "full lit gold standard shadows and cascaded shadow maps… everything gold standard"): B8 now BUILDS the full frontier lighting/shadow pipelines, not just the seam — amends ADR-0102 D7** (whose "defer the full Forward+/Deferred pipelines + light-culling to the post-hesap rendering phase" is superseded by the locked "full visual frontier BEFORE hesap" order; struck in place there). Thirteen sub-slices — each **bit-exact vs its published reference on the CPU oracle + observable pixel-identical on both backends** (the B5–B7 bar). **LIGHTING LIBRARY** (`ckir_lighting.hpp`, CKIR shader-side): **[✅ B8-a]** Cook-Torrance core — `d_ggx` (Trowbridge-Reitz, `k·(k/π)`) · `v_smith_ggx_correlated` (height-correlated) · `f_schlick` (`pow5=x²·x²·x`) · `fd_lambert` (1/π) + `fd_burley` (Disney) · `env_brdf_approx` (Karis analytic DFG) · `energy_compensation` (**Kulla-Conty multiscatter** `1+f0·(1/dfg.y−1)`) · `brdf_direct` combiner (`(Fd+Fr·E)·NoL·light`). Faithful transcription of Filament `surface_brdf.fs` (`PI=3.14159265359`). **CPU-oracle bit-exact vs the Filament reference** (`tests/kir/test_ckir_lighting.cpp`) + **observable pixel-identical both backends** (a PBR surface + directional light under a FragCoord roughness ramp → `239,83,58` Vulkan == DX12). Cross-config GREEN (win-debug+clang-cl+asan+shipping); byte-exact HELD (33010/30821); no device features (rides the clean raster path). **[✅ B8-b]** the OpenPBR lobe stack over the B5 slab — ALL lobes bit-exact vs their published reference on the CPU oracle + observable pixel-identical on both backends: `aniso_specular_lobe` (`d_ggx_anisotropic`+`v_smith_ggx_correlated_anisotropic`, tangent frame, at/ab from anisotropy) · `sheen_lobe` (`d_charlie`+`v_neubelt`) · `clearcoat_lobe`+`clearcoat_fresnel` (`d_ggx`+`v_kelemen`, IOR 1.5, base attenuated by 1−Fcc) · `subsurface_term` (forward/back-scatter wrap BTDF) · **thin-film iridescence** `eval_iridescence` (Belcour-Barla spectral Fresnel — `fresnel0_to_ior`/`ior_to_fresnel0`/`eval_sensitivity` XYZ→sRGB, per-channel φ23, m=1..2 series; glTF `KHR_materials_iridescence`) · **transmission/refraction** `transmission_btdf` (glTF `KHR_materials_transmission` BTDF `(1−F)·base·D·Vis` on the mirrored light) + `volume_attenuation` (Beer-Lambert) + `refraction_ray` (Snell `refract`). The transmission's analytic core is fully HERE — only the scene-colour SAMPLE that the refraction ray reads is renderer-side (bound at B8-l). `[lobes]`+`[thinfilm]`+`[transmission]` bit-exact + a layered clearcoat+sheen observable (`235,81,57`) AND a thin-film+transmission "glass" observable (Cos/Exp/Log/Refract/Smoothstep end-to-end → `14,26,13`) pixel-identical Vulkan==DX12. ⭐ caught two bit-faithfulness bugs: `a2·b2·b2` is LEFT-associative in Filament; φ23 is a PER-CHANNEL select (core `Select` is scalar-cond). Cross-config GREEN (win-debug + clang-cl/tidy + asan + shipping/LTO); byte-exact HELD (33010/30821); no device features. **⛔ NO DEFERRALS — the earlier "defer the spectral/renderer-coupled two" was WRONG and struck (user 2026-07-12): both are analytic shader math and were implemented in full here.** **[✅ B8-c]** light types — `directional_light` (L=−normalize(dir), no attenuation) · `point_light` (`distance_attenuation`: `square_falloff_attenuation` inverse-square smooth-window, falloff=1/radius²) · `spot_light` (× `spot_angle_attenuation` cone, scale/offset from the inner/outer cone half-angles) · `punctual_radiance` (`brdf_direct`·attenuation) + the forward loop (Σ over lights). Faithful transcription of Filament `surface_light_punctual.fs` (`saturate`=clamp01; Filament negates the spot axis for the cone). `[lights]` bit-exact (directional+point+spot summed) + a 3-light observable (a lit surface with a specular highlight, sweeping world-x varies the point distance + spot cone → `255,174,94`) pixel-identical Vulkan==DX12. The set-1 light-array STRUCTURED BUFFER binding is the renderer leaf (B8-k/l); the per-light + loop MATH is complete here. Cross-config GREEN (win-debug + clang-cl/tidy + asan + shipping/LTO); byte-exact HELD (33010/30821); no device features. **[✅ B8-d]** **area lights (LTC)** — Heitz Linearly-Transformed-Cosines. **✅ RECT (the polygon core):** `integrate_edge_vec` (the acos rational edge form factor) · `ltc_evaluate_rect` (clipless — orthonormal basis → `Minv·transpose(mat3(T1,T2,N))` → project + normalize the 4 corners → Σ edge form factors → `len·scale`, back-face cull) · `ltc_lut_uv` (the ltc_2 sample coord). Faithful transcription of Heitz selfshadow/ltc_code (`LUT_SIZE` 64). `[area]` bit-exact — `integrate_edge_vec` + rect with BOTH the identity/diffuse Minv AND a fitted-like off-diagonal Minv — + a diffuse rect area-light observable (`52,33,20`) pixel-identical Vulkan==DX12. Cross-config GREEN (debug + clang-cl/tidy + asan + shipping); byte-exact HELD; no device features. ⭐ scar: V∥N ⇒ `T1 = normalize(0)` = NaN (test uses V≠N). **✅ LINE/TUBE:** `Fpo`/`Fwt` (the closed-form line-integral primitives) · `i_diffuse_line` (clamped-cosine line integral + branchless z=0 horizon clipping) · `i_ltc_line` (Minv transform + the `1/|inverse(transpose(Minv))·ortho|` reweight) · `ltc_evaluate_line` (endpoints→shading frame, × tube radius). `[area]` bit-exact — BOTH the identity/diffuse AND a fitted-like Minv (the latter exercises the full `mat_inverse`, transcribed against the oracle's cofactor/minor arithmetic) — + a diffuse tube observable (`91,121,192`) pixel-identical Vulkan==DX12; 4-config CPU GREEN. **✅ DISK/SPHERE:** `solve_cubic` (Blinn's cubic root solver — `Atan2`/`Cos`/`Sqrt` + the branch selects) bit-exact in isolation · `ltc_evaluate_disk` (the ellipse eigendecomposition — general-vs-axis-aligned + `d11>d22` + the V3 orientation flip, all branchless-select) + `ltc_disk_lut_uv`; sphere = the disk facing P. `[area]` bit-exact (identity + fitted Minv) + a diffuse disk observable (`48,29,19`) Vulkan==DX12. **✅ LTC LUT + ANISO:** `ltc_lut_coord` (the (√(1−NoV), roughness) sample coord) · `ltc_matrix` (isotropic 4-coeff → sparse Minv) · `ltc_matrix_aniso` (6-coeff → the off-diagonal aniso fit); reconstruction bit-exact + a SPECULAR rect observable (`53,48,43`) AND an ANISOTROPIC-GGX observable (`59,52,28`) pixel-identical Vulkan==DX12 — proving sample→reconstruct→evaluate. The 64×64 fitted `ltc_1`/`ltc_2` tables are a renderer TEXTURE asset (uploaded at runtime via the B2 sampler path), not embedded. ⭐ scar: a `g.binary(Mul, vec3, scalar)` shape mismatch renders fine on GPU (GLSL broadcasts `vec·float`) but the same-shape oracle reads OOB → G/B channels zero; the observable caught it (use `nodes::detail::bin` / splat). **▶ B8-d COMPLETE — rect · line/tube · disk/sphere · SolveCubic · the LTC LUT (iso + aniso reconstruction + specular) — all bit-exact on the CPU oracle + observable pixel-identical on both backends; 4-config CPU GREEN; tidy-clean; byte-exact HELD.** **[✅ B8-e]** **IBL** — **runtime evals:** `sh_irradiance` (Filament's L2 spherical-harmonic irradiance — the 9-coeff pre-convolved form; band grouping + `(3·n.z)·n.z` order pinned) + `ibl_diffuse`; `ibl_specular` (Karis split-sum — prefiltered env radiance × `(F0·dfg.scale + dfg.bias)` × Kulla-Conty multiscatter energy-comp, dfg from the analytic `env_brdf_approx`). **Generation (precompute):** `importance_sample_ggx` (Filament GGX-NDF tangent-space sampler) + `dfg_integrand` (the split-sum DFG (A,B) contribution — IBL Smith `k=roughness²/2`, `NoL>0` guarded) — averaging over a Hammersley set yields the DFG LUT / prefiltered mips. `[ibl]` bit-exact vs Filament/Karis (runtime AND generation) + an IBL observable (SH diffuse + split-sum specular, sweeping normal + roughness → `190,162,160`) pixel-identical Vulkan==DX12; 4-config CPU GREEN; tidy-clean. The prefiltered env cubemap (roughness mips), DFG LUT, and SH coefficients are precomputed RENDERER assets (B2 cube/texture path + the generation math above); the per-pixel evals are here. **▶ B8-e COMPLETE.** **SHADOWS (gold standard, the emphasis):** **[✅ B8-f]** shadow-map foundation + the **bias stack** — shader-side: `shadow_project` (light_vp·(wp,1) → perspective-divide → NDC → uv/depth) · `normal_offset_bias` (push along N by scale·sin(incidence)) · `slope_scaled_bias` (base·tan, clamped) · `receiver_plane_bias` (Isidoro 2006, from ddx/ddy of the shadow coord — feeds B8-g PCF) · `shadow_factor` (normal-offset → project → slope bias → hardware-PCF `tex_sample_cmp` on the B2-b comparison-sampler + depth-texture path). Faithful to MJP/Holbert/Isidoro; kills acne + peter-panning. `[shadow]` bit-exact (projection + all 3 bias terms) + a `draw_shadow` observable (receiver plane → biased compare vs an uploaded depth map → screen-left lit `229`/right shadowed `0`) pixel-identical Vulkan==DX12; 4-config CPU GREEN; tidy-clean. The `PassType::Shadow` depth RENDER (rendering the map from the light) is the renderer/frame-graph leaf (B8-k/l); this consumes the map. **[✅ B8-g]** filtered soft shadows — **PCF** `pcf_shadow` (8-tap Poisson rotated per-pixel by interleaved-gradient noise, hardware-PCF `tex_sample_cmp`, averaged) · **PCSS** `pcss_penumbra` (blocker-depth → penumbra ratio → variable-radius PCF, contact-hardening; the blocker search samples the map) · filterable **EVSM** `evsm_warp` + `chebyshev_bound` + `reduce_light_bleed` + `evsm_shadow` (Lauritzen pos+neg exp warps, one-tailed Chebyshev, light-bleed linstep) · **4-moment Moment Shadow Maps** `msm_hamburger` (Peters-Klein I3D 2015 — bias → Cholesky-factor the Hankel matrix → quadratic roots → 3-case form factor). `[softshadow]` bit-exact (IGN/PCSS/EVSM/MSM) + a PCF `draw_shadow` observable (`229`/`0`) pixel-identical Vulkan==DX12; 4-config CPU GREEN; tidy-clean. ⭐ scar: MSM needs a ≥3-depth (well-conditioned) filtered distribution — a 2-point one makes the Hankel rank-deficient → NaN; and the `Mix(vec4, vec4, scalar)` factor must be splatted (same broadcast scar, ternary flavour — elements 0..k match, rest OOB). EVSM/MSM store MOMENTS (a filterable float target the shadow pass writes — the resolve is HERE; the float moment-map storage rides B8-k/l, the device has no float texture upload yet). **[✅ B8-h]** **CASCADED SHADOW MAPS** — the gold-standard directional system, the selection/stabilization MATH: `csm_split_practical` (the practical split — log-uniform `near·(far/near)^(i/n)` blended with the uniform split by λ, Zhang PSSM / MS "CSM") · `csm_select_cascade` (the branchless Step-sum cascade index over the three split planes) · `csm_texel_snap` (**Valient** stabilization — round the shadow-space UV to the shadow-map texel grid → no shimmer under camera motion) · `csm_blend_factor` (the smooth cross-cascade blend band, `clamp01((d−(split−w))/w)`). `[csm]` bit-exact (split/select/snap/blend, transcribing the exact op grouping — `Step(edge,v)=v<edge?0:1`, `Round=nearbyint` ties-to-even) + a **cascade-selection observable** (a view-depth ramp crossing the three splits → R=cascade-index/3 staircase · G=split blend · B=texel-snapped sweep → `255,255,223`) pixel-identical Vulkan==DX12; 4-config CPU GREEN (win-debug + clang-cl/tidy + asan + shipping); tidy-clean; byte-exact HELD (33010/30821). ⭐ a fresh tidy build over the accumulated uncommitted work peeled one onion-layer — a nested ternary in `vulkan_raster_context.cpp` (`readability-avoid-nested-conditional-operator`, newly-enabled) — fixed in place (two flat ternaries). The **SDSM** tight fit (depth min/max reduction), the 2DArray shadow ATLAS storage + per-cascade PCF/PCSS render, and the cascade-select matrices (set 1, ADR-0102 D4) are the renderer/frame-graph leaf (B8-k/l — the device has no 2DArray-depth upload yet); the split/select/stabilize/blend math is complete here. **[✅ B8-i]** the screen-space + translucent shadow frontier — the analytic cores buildable on the current infra, each bit-exact + observable pixel-identical both backends: **contact shadows** `contact_shadow` (Bend/UE screen-space ray-march — a 4-tap windowed-occlusion march, `bias ≤ ray−scene ≤ thickness` per tap via scalar `step`, hardest-tap max × distance/edge fade; the real per-tap depth-buffer SAMPLE binds at B8-l) · **translucent / deep shadows** `fourier_opacity_transmittance` (**Fourier Opacity Maps**, Jansen-Bavoil HPG 2010 — the n=2 series reconstructs the optical depth → `exp(−τ)` fractional transmittance for hair/foliage/smoke, the order-independent soft shadow hard maps can't express) · **Virtual Shadow Map addressing** `vsm_clipmap_level` (UE5 VSM — `clamp(floor(log2(view_z/base)),0,max)`) + `vsm_page_coord` (virtual `floor(uv·pages)`; the page-table + GPU-feedback allocation + virtual→physical remap is the renderer subsystem at B8-l). `[ssshadow]` bit-exact (contact/FOM/VSM) + THREE observables both backends (contact `242,191,140` · FOM `131,131`≈want`132` 1-ULP Sin/Cos/Exp within ±4 · VSM `170,207,143`) pixel-identical Vulkan==DX12; 4-config CPU GREEN; tidy-clean; byte-exact HELD (33010/30821). ⭐ scar avoided: a vec4 `Step` has no per-component raster emitter (`create_program→nullptr`) — the 4 taps are INDEPENDENT scalar samples, so scalar `step` per tap (a horizontal reduction) is the idiomatic + wired form. **INFRA-SEQUENCED leaves (NOT deferrals — their analytic cores build where their infra lands): RT shadows + SIGMA/ReBLUR denoise ride B9** (they need the GPU ray-scene traversal) **· MegaLights / ReSTIR-sampled many-light stochastic shadows ride B14** (the reservoir substrate — Epic 2025 / Zhang EGSR 2025, thousands of shadowed area lights at fixed cost). **SKINNING (set 3):** **[✅ B8-j]** the deformation math both techniques: **linear-blend skinning** `lbs_skin_position` (`p' = Σ wᵢ·(Mᵢ·[p,1])` — the affine-equivalent transformed-position blend, matvec + weighted sum) + `lbs_skin_normal` (the same blend on the direction `[n,0]`, re-normalized) · **dual-quaternion skinning** `dquat_skin_position` (Kavan et al. 2007 — antipodality-corrected 2-bone blend of the real/dual quats → normalize by `|qr|` → the rigid point transform via `cross`; the volume-preserving arc LBS collapses on twist). `[skin]` bit-exact (LBS pos/normal + DQS, incl. the antipodality sign-flip crossing `dot(r0,r1)<0`) + TWO observables both backends (LBS `156,114,51` blend-slide · DQS `94,170,140` rotation arc) pixel-identical Vulkan==DX12; 4-config CPU GREEN; tidy-clean; byte-exact HELD (33010/30821). The bone PALETTE + per-vertex indices/weights (set-3 structured buffer) + the optional compute PRE-SKIN pass are the renderer leaf (B8-l); the blend math is complete here. **RENDERER (rhi frame graph, ADR-0102 D2/D4):** **[✅ B8-k]** the cook seam — a material authored ONCE, lowered into its per-pass programs. New `ckir_cook.hpp` (`crd::kir::cook`): `PassType`{Shadow·DepthPrepass·GBuffer·Forward} · `MaterialTemplate` (the authored surface-graph callback + params) · `SurfaceInputs`/`VariantOptions` · `shade_forward` (surface → the B8 Cook-Torrance lit colour) · **`build_fs_for_pass`** (routes the one surface to the pass's outputs — Shadow/Depth = depth-only + alpha-test, GBuffer = B5 MRT pack, Forward = shaded — then runs B7 `lower_entry`) · **`specialize_variant`** (B7 `ShaderOption` bake). `[cook]` proves it: STRUCTURAL (each pass's n_out/discard/stage correct across opaque+masked) + **lowering ROUND-TRIP BIT-STABLE** (the Forward variant evals identically before/after `lower_entry`) + **ShaderOption specialize** (a pinned selector folds to its branch, bit-identical to the runtime uber-shader). GPU: the **Forward variant renders LIT** (`230,73,52`) pixel-identical Vulkan==DX12 + oracle-exact, AND the **GBuffer variant compiles** on both backends. **⭐⭐ Surfaced + fixed a REAL B8-a correctness bug the bit-exact tests were BLIND to:** `energy_compensation` divides by the analytic-DFG bias (`env_brdf_approx.y`), which undershoots NEGATIVE at roughness ≳0.8 → `ecmp` large-negative → the surface renders BLACK for all rough materials. The `[brdf]`/`[ibl]` bit-exact + observable tests all PASSED because the oracle and GPU reproduce the same buggy value symmetrically (bit-exactness proves PORTABILITY, not physical CORRECTNESS). Fixed by flooring the DFG bias to a small positive (the physical constraint; matches the codebase `PREVENT_DIV0` idiom) — roughness→1.0 now renders (0.8: black→`1,1,0.80`); refs updated; full lighting suite re-verified bit-exact. 4-config CPU GREEN; tidy-clean; byte-exact HELD (33010/30821 — the fix is in the lighting library, not the golden compute emissions). The material VS (vertex-attribute transform + B8-j skinning + varying emission) + the physical bindings (params→set-2 · scene lights→set-1 · bone palette→set-3 · reflection/descriptor plumbing into crd-shader) need vertex-buffer + descriptor binding and ride B8-l; the FS pass-differentiation + lowering + variant machinery is complete here. **[✅ B8-l]** the render paths — the analytic cores of ADR-0102's hybrid (deferred opaque + forward transparent), new `ckir_render.hpp` (`crd::kir::render`), each bit-exact + observable both backends: **FORWARD+ clustered light culling** — `cluster_z_slice` (the exponential Doom-2016 depth slice `floor(log(z/near)/log(far/near)·N)`) · `cluster_coord` (the froxel tile×slice) · `sphere_aabb_sq_dist` + `light_cluster_cull` (the light-SPHERE-vs-cluster-AABB test the cull compute pass runs per (light,cluster)). **DEFERRED lighting** — `deferred_shade` (DECODE the B5 G-buffer MRT — g0=(base,metallic)·g1=(normal_enc,roughness)·g2=(emissive,occlusion) — back into a surface, then B8 Cook-Torrance shade it; renders `230,73,52`, PIXEL-IDENTICAL to the FORWARD path since pack→decode is lossless for the direct channels). **CLUSTERED DECALS** — `decal_project` (world point → decal-box via the inverse transform → decal uv + inside test; forward+ AND deferred, no prepass). `[render]` bit-exact (cluster/cull/decal/G-buffer-decode) + THREE observables both backends (deferred `230,73,52` · cluster staircase+cull `191,76,255` · decal uv+inside `255,166,0`) pixel-identical Vulkan==DX12; 4-config CPU GREEN; tidy-clean; byte-exact HELD (33010/30821). **⭐ the vec·scalar broadcast scar bit a THIRD time** (`deferred_shade` normal decode `nenc·2−1`: `konst`/`kf` shaped-like-a-node are STILL scalars — Shape ≠ comps — so `binary(Mul, vec3, konst)` mismatches; GPU rendered `230` while the same-shape oracle read OOB → `33`) — fixed with `nodes::detail::bin`; memory updated. **RENDERER LEAVES (B8-m + the post-detour render phase):** the clustered-cull COMPUTE dispatch (atomics building each cluster's light list into a set-1 buffer), the G-buffer render-to-texture + sampling in the deferred pass, the decal-cull list build, and the frame-graph orchestration (shadow(N cascades) → depth-prepass → light/decal-cull → G-buffer/forward → lighting → composite) — those need the compute context + structured buffers + render-to-sampled-texture; the shading/culling/projection math they invoke is complete here. **[✅ B8-m] THE CULMINATION — B8 CLOSED.** ONE master material shader composing the WHOLE B8 stack: **skinning** (B8-j `lbs_skin_normal` tilts the normal) → **surface** (B5 base_color sweep) → **direct** Cook-Torrance (B8-a/c `directional_light`) → **IBL ambient** (B8-e, dimmed) → **real PCF shadow** (B8-f/g `pcf_shadow` sampling an uploaded depth map via the comparison sampler). Composed PHYSICALLY CORRECTLY — the shadow modulates the DIRECT term only; the IBL ambient SURVIVES in shadow (`clamp01(direct·shadow + ambient)`). Rendered via `draw_shadow` on both backends: the **LIT region is oracle-EXACT** (`118,121,101` == `build_master_lit_expected` = `direct+ambient`, ±4, pixel-identical Vulkan==DX12) and the **SHADOWED region keeps only the ambient floor** (col27 R=`89`, darker by 29 — the direct light occluded — yet non-black, ambient preserved). Cross-config: win-debug both backends + tidy-clean (clang) + shipping-LTO + asan all compile GREEN; byte-exact HELD (33010/30821); lighting suite 154/17 (VK) · 131/17 (DX) no regression. The full multi-cascade CSM 2DArray atlas + PCSS + the multi-pass frame-graph (compute light-cull → gbuffer → lighting → composite) + validation on the new compute/structured-buffer features are the renderer subsystem (B8-k/l leaves + the post-detour render phase); the SHADING composition — every B8 lighting/shadow/skinning system feeding one coherent lit+shadowed+skinned pixel — is complete + proven here. **▶ B8 COMPLETE — the frontier lighting + shadow system is IN, bit-exact + both-backends across B8-a…m. Detail + references: see "B8 — the frontier lighting + shadow system" below.** |
| ✅ | 18 | B12 | IR | **screen-space lighting frontier** — the indirect + volumetric contributions the raster path can't get from IBL alone; each a CKIR pass (bit-exact CPU oracle + observable pixel-identical both backends, the B8 bar). **[✅ B12-a] ambient occlusion** — new `ckir_screen.hpp` (`crd::kir::screen`), the AO math bit-exact + observable both backends: **Visibility-Bitmask AO (SSILVB, Therrien 2023 — the GTAO successor)** `ssilvb_sector_mask` (the u32 sector mask one depth sample occludes — clamped shifts so start+count ≤ 32, GPU-u32 == oracle-i64) + `ssilvb_ao` (`1 − popcount(bitfield)/N`) · **GTAO** (Jimenez ground-truth) `gtao_slice` (the cosine-weighted horizon arc-integral) + `gtao_multibounce` (the albedo re-lighting cubic) · **specular occlusion** `spec_occlusion` (Frostbite `saturate(pow(NoV+ao, exp2(−16·rough−1)) − 1 + ao)`). `[ssao]` bit-exact (GTAO/multibounce/spec-occ/SSILVB bitmask+popcount) + an observable composing all four (`48,34,27`) pixel-identical Vulkan==DX12, oracle-exact; 4-config CPU GREEN; tidy-clean; byte-exact HELD (33010/30821). **⭐ wired `BitCount`/`BitNot`/`FindLSB`/`FindMSB` into BOTH raster value emitters** (GLSL `bitCount`/HLSL `countbits`) — they were in the compute emitter but the raster/fragment path lagged (`create_program→nullptr`); the compute golden emissions are untouched so the canary held. Bent normals = the march-accumulated unoccluded direction, and the depth-buffer march that finds the horizon angles + accumulates the bitmask are the renderer leaf (post-detour screen-space pass, needs the depth+normal G-buffer as textures); the integrands/bitmask/occlusion math is complete here. **[B12-b] screen-space reflections (SSR)** — Hi-Z (hierarchical-depth) ray-march · **stochastic rough** SSR (GGX-importance, Stachowiak ratio-estimator) · temporal + spatial resolve · on-miss fallback to a **radiance/surface cache** (Lumen/SHaRC-style, B14) then **RT reflections** (B9) · reflection **denoise** (ReBLUR-class) + screen-edge fade — the hybrid SS→cache→RT pipeline. **[B12-c] screen-space GI (SSGI)** — the **visibility-bitmask** indirect-diffuse bounce (SSILVB gives GI for free) — the contact-GI layer ON TOP of the B14 world-space cache, never the sole GI. **[B12-d] volumetric lighting + fog** — froxel (3D clip-space) scattering + temporal reprojection (Wroński/Hillaire) · **light-shafts / god-rays** (froxel + epipolar sampling through the CSM) · height fog · the shared **phase-function family** (Henyey-Greenstein · Cornette-Shanks · **Draine approximate-Mie**, Jendersie & d'Eon 2023) + Beer-Lambert absorption → the sci-fi light beams. (Full sky/atmosphere + clouds = **B15**; path-traced media / volumetric-ReSTIR = B14/B9.) **[B12-e] screen-space SSS** — separable subsurface scattering (Jimenez / Burley diffusion) for skin (the screen-space partner of B8-b `subsurface_term`). (RT counterparts — RTAO · RT reflections · RT GI · path tracing — arrive with **B9**; these are the wide-HW screen-space frontier.) **▶▶ B12 COMPLETE (2026-07-12)** — new `ckir_screen.hpp` (`crd::kir::screen`), all five sub-slices bit-exact + observable pixel-identical both backends: **[✅ B12-a]** AO — `ssilvb_sector_mask`+`ssilvb_ao` (Visibility-Bitmask/SSILVB, u32 sector bitmask + popcount, clamped shifts for GPU-u32==oracle-i64) · `gtao_slice`+`gtao_multibounce` (GTAO ground-truth) · `spec_occlusion` (Frostbite). **[✅ B12-b]** SSR — `ssr_reflect` (Hi-Z reflection ray) · `ssr_hiz_hit` (thickness intersection) · `ssr_edge_fade` · `ssr_confidence` (+ GGX-importance stochastic reuse from B8-e). **[✅ B12-c]** SSGI — `ssgi_bounce` (the visibility-bitmask indirect diffuse `sample_mask & ~bitfield`, rides B12-a). **[✅ B12-d]** volumetrics — the **phase-function family** `henyey_greenstein` · `cornette_shanks` · **`draine_mie`** (Jendersie & d'Eon 2023 approximate-Mie) + `beer_lambert` + `froxel_scatter`. **[✅ B12-e]** SSS — `burley_diffusion` (per-channel diffusion profile) + `sss_gaussian` (separable kernel). `[ssao]`/`[ssr]`/`[ssgi]`/`[volsss]` bit-exact 4-config + FIVE observables both backends (ssao `48,34,27` · ssr `70,75,55` · ssgi `27,19,31` · volumetric `12,10,7` · sss `4,5,4`) oracle-exact; tidy-clean; byte-exact HELD (33010/30821); kir 536/78. **⭐ wired `BitCount`/`BitNot`/`FindLSB`/`FindMSB` into BOTH raster value emitters** (compute had them, the fragment path lagged → `create_program→nullptr`) — the compute golden emissions untouched so the canary held. ⭐ the vec·scalar broadcast scar bit a 4th time (`burley_diffusion` `rd·⅓`: `kf` is a scalar → `nodes::detail::bin`). **The DEPTH/COLOUR-buffer MARCHES + froxel/temporal reprojection + separable blur passes are the renderer leaf (post-detour screen-space passes, need the G-buffer bound as textures); every integrand / bitmask / phase / profile is complete + proven here.** RT counterparts (RTAO · RT reflections · RT GI · path tracing) ride B9. |
| ✅ | 19 | B13 | IR | **post-processing frontier — HDR → display, the beautiful finish** — each a CKIR compute / full-screen pass (bit-exact CPU oracle + both backends). **[✅ B13-a] temporal AA + upscale + frame-gen** — new `ckir_taa.hpp` (`crd::kir::taa`), the TAAU resolve core bit-exact + observable both backends: **YCoCg** clamp space `rgb_to_ycocg`/`ycocg_to_rgb` · the history rectification **`clip_aabb`** (Karis/Playdead branchless line-box clip) + **`variance_clip`** (Salvi moments `mean ± γ·σ`) · **`catmull_rom_weights`** (sharp bicubic history) · **`luma_feedback`** (Karis anti-flicker alpha) + **`taa_resolve`** (exponential blend) · **`disocclusion`** (depth-mismatch reject) · **`ign_temporal`** (Jimenez interleaved-gradient noise, per-frame scrambled) + **`dither_apply`** (deband) · **`frame_gen_blend`** (frame-generation interpolation core) · **`smaa_luma_edge`** (SMAA edge pass). `[taa]` bit-exact **4-config** (all 12 transforms) + a temporal-resolve observable both backends (`169,152,137` oracle-exact, Vulkan==DX12); tidy-clean; kir 538/80; canary HELD (no emitter touched — all KOps pre-wired). ⭐ scar-clean by construction: nodes for per-pixel/frame inputs + doubles for tuning constants + `nd::detail::bin` for every vec·scalar (broadcast-safe). **RENDERER LEAVES** (need bound resources / external SDKs, not new IR): the 3×3 neighborhood GATHER + MV-reprojection SAMPLE (texture taps); the **ML-upscaler seam** backing DLSS 4 / FSR 4 / XeSS (external SDK device path) + our OWN learned upscaler via **B10 coop-vectors**; frame-gen's REQUIRED Reflex-class low-latency partner; the blue-noise + SMAA area/search textures (B2 binds); history ping-pong. Halton jitter = a host-side camera sequence feeding the shader. Every transform those leaves invoke is complete here. **[✅ B13-b] bloom + light emission** — new `ckir_bloom.hpp` (`crd::kir::bloom`), the bloom + glare core bit-exact + observable both backends: the **physically-based dual-filter pyramid** — `downsample_13tap` (Jimenez 13-tap 5-block combine, ½ centre + ⅛×4 outer) + **`downsample_karis`** (first-mip per-block `1/(1+luma)` weighting → firefly-free, threshold-free, energy-conserving) + `upsample_tent` (3×3 (1,2,1) tent) + `soft_knee` (optional Jimenez quadratic knee) + `combine` (energy-bounded lerp composite) → the HDR glow for **emissive / neon / LED / sci-fi** · **FFT-convolution glare**: the frequency-domain **`complex_mul`** (image spectrum ⊗ aperture-PSF spectrum — kernel-shaped diffraction/anamorphic glare) is built here; the forward/inverse **FFT is the B-compute kernel** it feeds · **lens flare** `lens_halo` (radial ring) + `starburst` (bladed-aperture diffraction spikes) + `spectral_tint` (chromatic ghost dispersion). `[bloom]` bit-exact **4-config** (9 transforms) + a bloom observable both backends (`91,73,49` oracle-exact, Vulkan==DX12); tidy-clean; kir 539/81; canary HELD. **RENDERER LEAVES**: the pyramid TAP GATHER (13/9 bilinear samples per mip) + mip-chain render-target ping-pong; the **FFT forward/inverse GPU transform** (B-compute — `complex_mul` is ready for it); the lens-dirt + aperture textures (B2 binds); the ghost SAMPLE along the flare vector. Every per-pixel transform is complete here. **[✅ B13-c] exposure + tonemap + HDR output** — new `ckir_post.hpp` (`crd::kir::post`), the HDR core bit-exact + observable both backends: **auto-exposure → EV100** `ev100_from_luminance` + `exposure_from_ev100` (Frostbite) · the tonemap/"look" stage **decoupled from the output encode** — **AgX** `agx` (Troy Sobotka/Filament fit: inset matrix → log2 → 6th-order sigmoid, hue-preserving) + **Khronos PBR-Neutral** `pbr_neutral` (base-colour accuracy, highlight-only compression) as the built-in ANALYTIC looks; **Tony McMapface + ACES 2.0 are baked 3D-LUTs** sampled via the B2 Tex3D path (the LUT bake is an asset, the sampling mechanism is B2) — explicitly NOT per-channel Narkowicz-"ACES" hue-skew · the OUTPUT encodes **sRGB** `srgb_encode` (SDR OETF) + **PQ/ST.2084** `pq_encode` (HDR10 / BT.2408) · `gamut_compress` (perceptual, toward-luma). `[hdr]` bit-exact 4-config (ev100/exposure/AgX/PBR-Neutral/sRGB/PQ/gamut) + THREE observables both backends (AgX `197,188,174` filmic roll-off · PBR-Neutral `250,231,222` · PQ `250,240,231`) oracle-exact; tidy-clean; byte-exact HELD (33010/30821). **⭐ scars: the vec·scalar broadcast bit a 5th time (AgX poly `coef·xⁿ` — `kf` is scalar → `nodes::detail::bin`); and `Select` reads a SCALAR cond, so `srgb_encode` had to go per-channel (a vec3 cond misreads/misaligns).** The histogram REDUCTION for the average luminance (a parallel reduce — B-compute), the eye-adaptation temporal blend, and the 3D-LUT texture bind (Tony/ACES2/OCIO) are the renderer leaf; every exposure/tonemap/encode transform is complete here. · **3D-LUT colour grading** · **gamut compression** · **HDR output** (PQ/scRGB, BT.2408 203-nit paper white). **[✅ B13-d] cinematic** — new `ckir_cinematic.hpp` (`crd::kir::cinematic`), the lens-effect core bit-exact + observable both backends: **depth of field** — the Garcia (Frostbite 2017) SEPARABLE complex-phasor circular bokeh: `circle_of_confusion` (thin-lens signed CoC `(f²/N)(z−zf)/(z(zf−f))`) + `complex_gaussian` (the phasor component `exp(a·r²)·(cos,sin)(b·r²)` — a disk = Σ complex Gaussians, so 2-D bokeh factors into H+V passes) + `bokeh_realize` (the real reconstruction `re·cre−im·cim`) + `coc_coverage` (scatter-as-gather) + `dof_composite` (near/far sharp↔blur blend) · **motion blur** — the McGuire (I3D 2012) velocity-tile: `velocity_scale` (shutter scale + max-len clamp) + `mb_cone` + `mb_cylinder` (the two reconstruction weights) + `mb_soft_depth` (foreground classification). `[cine]` bit-exact **4-config** (9 transforms) + a DoF/motion-blur observable both backends (`100,95,67` oracle-exact, Vulkan==DX12); tidy-clean; kir 540/82; canary HELD. **RENDERER LEAVES**: the separable H/V complex GATHER passes (per-component accumulation over the CoC-scaled tap line + ping-pong target); the velocity-field TILE-MAX + NEIGHBORMAX dilation gather; the half-res DoF/MB scatter targets. Every per-sample transform is complete here. **[✅ B13-e] finish** — new `ckir_finish.hpp` (`crd::kir::finish`), the finishing chain bit-exact + observable both backends: **geometric specular AA** `specular_aa` (Tokuyoshi-Kaplanyan screen-space NDF filtering — widen α² by the normal's sub-pixel variance `√(α²+min(2κ(|dNdx|²+|dNdy|²),σ²max))`, kills specular shimmer) · **chromatic aberration** `ca_offset` (radial dispersion UV offset) · **vignette** `vignette` (optical cos⁴ = att², att=1/(1+r²/f²)) · **film grain** `film_grain` (Lottes luma·(1−luma) midtone-weighted) · **CAS** `cas_sharpen` (AMD FidelityFX contrast-adaptive sharpen — per-channel min/max-ring weight `√clamp01(min(mn,1−mx)/mx)`, no ringing). `[finish]` bit-exact **4-config** (5 transforms) + a finishing-chain observable both backends (`97,88,68` oracle-exact, Vulkan==DX12); tidy-clean; kir 541/83; canary HELD. **⭐ the vec·scalar broadcast bit a 6th time** — CAS's `1−mx` (scalar−vec3) + `mx+ε` (vec3+scalar) were bare `g.binary` → GPU broadcasts, oracle OOB in channels 1,2 (40/60 = 2ch×20); fixed with `g.splat`/`nd::detail::bin`. **RENDERER LEAVES**: the normal-derivative source (dFdx/dFdy fragment builtins the caller feeds); the CA per-channel SAMPLE at the offset UVs; the grain/blue-noise texture (B2); the CAS 3×3 neighborhood GATHER (caller supplies the 5 cross taps). Every transform is complete here. |
| ✅ | R1 | B-hdr | RES | **HDR image I/O — OUR OWN codec, zero 3rd-party** (user 2026-07-12: "I don't want to use 3rd party library for just reading an hdr image at all! We must do it ourselves"; built AFTER B13). New `crd-resources` codec `hdr_image.hpp`/`.cpp` (`crd::resources::HdrImage` — flat row-major channel-interleaved f32) — decode/encode float pixels + round-trip through **`.crdr`** (`hdr_to_crdr`/`hdr_from_crdr`, type 'HDRI' = HEAD + PIXF chunks, lossless). Scope = **RGBE + EXR + PFM**: **[✅ B-hdr-a] Radiance RGBE** (`.hdr`/`.pic`) — read+write, Ward `rgbe↔float` (`f=m·2^(e−136)`), new+old-format RLE (adaptive run/literal); `B→float→B` is bit-exact **idempotent** (anchors the round-trip). **[✅ B-hdr-b] Portable FloatMap** (`.pfm`) — read+write, lossless raw f32 (RGB `PF`/gray `Pf`, LE+BE, bottom-to-top row flip). **[✅ B-hdr-c] OpenEXR** (`.exr` — scanline, half/float) — `hdr_exr.cpp`, three steps ALL done. **step 1 ✅**: the full container (magic · version · header attributes · chlist B,G,R/Y · offset table · scanline blocks) + **our own IEEE half↔float** (round-to-nearest-even) + **NONE** + **RLE** (EXR reorder+predictor + signed-count byte-RLE). **step 2 ✅ = ZIP** — **our own DEFLATE** (reusable `deflate.hpp`/`.cpp`: RFC-1951 inflate ALL block types [stored·fixed·dynamic Huffman] + LZ77, fixed-Huffman+hash-chain deflate, RFC-1950 zlib + Adler-32) → 16-line-block ZIP (+ ZIPS). **step 3 ✅ = PIZ** — **our own wavelet + Huffman** (`hdr_piz.hpp`/`.cpp`: the OpenEXR 2-D lifting wavelet `wav2Encode/Decode`, the canonical-Huffman+run-length codec `huf_compress/uncompress`, the value bitmap/LUT) → 32-line-block PIZ. **⭐⭐ Validated BOTH readers against REAL OpenEXR files** (OpenCV-written 7×5 — a ZIP FLOAT and a PIZ HALF — each decoded bit-exact) + a foreign CPython-zlib dynamic-Huffman vector. So the codec reads/writes real-world RGBE, PFM, and NONE/RLE/ZIP/ZIPS/PIZ `.exr` — **entirely our own code, zero 3rd-party.** `[hdr]`+`[deflate]` **4-config** (23 cases, 197 assertions incl. PIZ FLOAT/HALF round-trip + REAL PIZ .exr decode); tidy-clean. **⛔ deflate scar: `push_back(out[k])` self-references the container — a realloc mid-overlap-copy reads a dangling ref (UAF); copy to a local first.** To get the wavelet + Huffman byte-exact we referenced the published OpenEXR algorithm (as DEFLATE used RFC 1951) — the implementation is ours. The GPU upload as a float env-map texture is the renderer/rhi consumer leaf (B8-e IBL / B15 sky) — needs a float TextureFormat wired through the texture path. |
| ✅ | 20 | B14 | IR | **real-time GLOBAL ILLUMINATION + DENOISING (the RT-tier GI stack; pairs C3/B9)** — the 2026 frontier is a THREE-LAYER stack, each a CKIR pass. **[✅ B14-a ESTIMATOR COMPLETE] ReSTIR** — reservoir spatiotemporal importance resampling, the full RIS + temporal + spatial ESTIMATOR in CKIR (`ckir_restir.hpp`, statement-tier; candidate GENERATION = which light, its shaded contribution, the shadow-ray visibility = the B9 RT leaf; the reservoir/RIS/reuse MATH is done): reservoir = [f(y), p̂(y), W, M]; `build_restir_ris` (Weighted-Reservoir-Sampling stream of M candidates, W = Σw/(M·p̂), estimate f(y)·W) + `build_restir_temporal` (two-reservoir merge, prev M clamped to m_cap·M_cur) + **`build_restir_spatial`** (K+1-reservoir merge over K screen neighbours — streaming WRS, one random per neighbour). Verified: CPU oracle — RIS is **UNBIASED** (pixel-mean of f(y)·W == the true integral even under an imperfect p̂=1) + temporal AND spatial reuse each **DROP VARIANCE** ≥2× (canonical p̂=f ⇒ merged estimate = running mean over the accumulated candidate stream, var ~ Var(f)/M_eff) while staying unbiased + M grows; Vulkan (RIS 1.11e-7, temporal 1.19e-7, spatial 1.29e-7 == oracle, add/mul/div/max/cmp/select + index-cast only ⇒ bit-exact). GPU perf (RTX 4070 Ti SUPER, `[.gi-bench]` → `docs/bench/2026-07-15-gi-atmosphere-vulkan.md`): RIS M=32 0.527 ms / 262 k px. Scope onward: **ReSTIR DI** (Bitterli 2020) → **ReSTIR GI** (Ouyang 2021) → **ReSTIR PT / GRIS** (Lin 2022). The dominant real-time sampler; also feeds many-light shadows (B8) + reflections (B12-b). **[✅ B14-b] world-space radiance cache** — **[✅ DDGI built + polished]** Majercik 2019 visibility-moment irradiance probes in CKIR (`ckir_ddgi.hpp`, statement-tier; RT ray-gen = the B9 leaf, analytic core done): octahedral encode/decode (Cigolle, no singularity) + Chebyshev depth-moment visibility (no leak) + probe SAMPLE (8-probe trilinear × normal-wrap × Chebyshev × BILINEAR oct-irradiance → leak-free indirect diffuse) + probe UPDATE (cosine irradiance + cos^k depth moments + temporal hysteresis). ✅ POLISHED: bilinear octahedral sampling (4-tap) + full ARBITRARY probe grid (cell = floor((pos−origin)/spacing) clamped, 8 corner probes via flat index — 4×4×4 indexing verified). CPU 57 asserts + Vulkan (sample 1.3e-7, update 1.8e-6). — original scope: a portable **spatial-hash cache (SHaRC-style)** and/or **surfels (EA GIBS)** and/or **DDGI irradiance probes** (Majercik 2019, visibility-moment probes = the no-RT-HW fallback tier) — the cheap multi-bounce + stabilization layer. **[◧ B14-c] spatiotemporal DENOISER (we own it) — GOLD-STANDARD SVGF-2017 built** — **[✅ B14-c-1 GOLD] the edge-stopping À-TROUS wavelet** (depth-GRADIENT weight + 3×3-gaussian variance pre-blur + Σ(hw)²Var/(Σhw)² — the faithful Schied formula, not a proxy) · **[✅ B14-c-2] TEMPORAL integration** (`build_svgf_temporal`: reproject+disocclusion-reject+EMA α=max(1/hist,α_min)+luminance moments→Var) · **[✅ B14-c-3] the 5-iteration PIPELINE** (temporal→à-trous×5, crushes noise ≥10× end-to-end). Verified: invariants + Vulkan (à-trous 3.64e-7, temporal bit-exact 0.0) + DX12. [✅ B14-c-4] A-SVGF variance-aware adaptive-α (outlier ⇒ reset, no lag; noise ⇒ no reset; Vulkan bit-exact). **B14-c DENOISER COMPLETE.** — earlier note: **[✅ B14-c-1] the edge-stopping À-TROUS wavelet** (`ckir_svgf.hpp` `build_svgf_atrous`: 5×5 increasing-stride blur, depth/normal/luminance edge-stopping weights, variance-guided; a statement-tier COMPUTE pass — gathers its own stencil, not a deferred renderer leaf). Verified: CPU-oracle INVARIANTS (uniform preserved · noisy variance drops >2× · a hard depth edge STOPS the bleed) + Vulkan **and** DX12 == oracle at maxrel 3.58e-7 (arithmetic bit-exact, exp/pow ULP). **⛔ scar: the compute-kernel value emitter LACKED `Exp`/`Pow` (the fragment path had them) — the mirror of "raster lags compute"; added to all 5 backends (glsl/hlsl/cuda/msl/wgsl).** NEXT B14-c-2 = temporal accumulation + moment/variance estimation, B14-c-3 = the 5-iteration pipeline, B14-c-4 = A-SVGF gradient reset. Original scope: **SVGF/A-SVGF** (Schied 2017/18: variance-guided à-trous + temporal-gradient reset) → **ReBLUR/ReLAX/SIGMA**-class (diffuse+specular+shadow), reservoir-correlation-aware + firefly clamp; optional hooks to **DLSS-RR / OIDN**. **[✅ B14-d FUNCTIONAL CORE] NEURAL radiance cache** — an online-trained MLP GI cache (Müller 2021), the PORTABLE vendor-neutral NRC (no shipping engine has a differentiable-IR NRC — a moat). **Built in CKIR (`ckir_nrc.hpp`, `crd::kir::nrc`, statement-tier compute; all Vulkan BIT-EXACT vs oracle = 0.0):** [✅ B14-d-1] `build_nrc_hashgrid_encode` — the Instant-NGP MULTIRESOLUTION HASH-GRID encoder (L levels × trilinear-blended hashed features; verified zero-table→0, trilinear PARTITION-OF-UNITY, determinism); [✅ B14-d-2] `build_nrc_infer` — the cache query (encoded → ReLU hidden → RGB radiance, == a hand-computed forward); [✅ B14-d-3] `build_nrc_train_grad` — the online-step BACKPROP (L2-loss gradient → dW1/dW2, verified == FINITE DIFFERENCES). **⛔ scar found+fixed: the statement-tier compute Const emitter mangled a u32 const > INT_MAX (MSVC `static_cast<int>`(2654435761)→INT_MIN); the hash exposed it — fixed to `app_int_const`/`%lld` across ALL 5 backends (golden emissions held).** The fused/coopmat MLP PERF tier rides B10 (the FP32 MLP already crushes cuBLAS). **▶▶ B14 COMPLETE — a/b/c/d all done: ReSTIR + DDGI + SVGF + NRC, the full real-time GI stack in CKIR.** Original scope: built on **CKIR autodiff (v15/v16)** + **B10 coop-vectors**. Compute deps: spatial-hash grid (atomics) · à-trous filter · online-MLP GEMM+backprop (**B-cmp**). (Radiance Cascades — Sannikov — prototype for 2D/stylized.) |
| ✅ | 21 | B15 | IR | **SKY / ATMOSPHERE + VOLUMETRIC CLOUDS** — **[✅ B15-a] physically-based atmosphere** — **Hillaire 2020** (transmittance + multiple-scattering + sky-view + **aerial-perspective** froxel LUTs; dynamic time-of-day; the UE5 Sky-Atmosphere method) whose aerial-perspective froxel composites straight into B12-d fog. Built in CKIR (`ckir_atmosphere.hpp`, `crd::kir::atmos`, statement-tier compute; scalarized RGB): **[✅ B15-a-1] TRANSMITTANCE LUT** — the Rayleigh(∝exp(−h/8km),wavelength-dep)+Mie(∝exp(−h/1.2km))+ozone(tent@25km) extinction integral T=exp(−τ) ray-marched (40 steps) over the Bruneton (r,μ) parameterisation, reformulated CANCELLATION-FREE (H²−ρ²=H²(1−v²), no difference of km-scale squares). CPU physics: T∈(0,1], Rayleigh blue>green>red (why the sky is blue/sunsets red, T_r≈0.94 T_b≈0.76 up), horizon darker, T→1 at altitude; Vulkan maxabs 6.25e-5. **[✅ B15-a-2] MULTIPLE-SCATTERING LUT** (`build_atmos_multiscatter`) — Hillaire's isotropic 2nd-order fill: per texel (μ_sun,alt) integrate single-scatter over a Fibonacci sphere (each ray bilinear-samples the transmittance LUT for the sun transmittance), form L₂ + f_ms, sum the closed-form series Ψ=L₂/(1−f_ms). CPU physics: Ψ≥0/finite, grows toward the ground, bluish (Rayleigh-dominated), brighter sun-up; Vulkan 1.46e-5. **[✅ B15-a-3] SKY-VIEW LUT** (`build_atmos_skyview`) — the renderable per-direction sky radiance: march the view ray adding single scattering (Rayleigh + Cornette-Shanks Mie phase, sun-transmittance-weighted) + isotropic multiple scattering (from the multiscatter LUT), attenuated by the view transmittance. CPU physics: blue zenith, sun-side Mie glow, bright horizon; Vulkan 1.43e-5. **[✅ B15-a-4] AERIAL-PERSPECTIVE froxels** (`build_atmos_aerial`) — a 3D camera-frustum volume: one thread per (x,y) column marches through depth slices, writing each cell's running (RGB inscatter + mean transmittance) using the same single+multiple-scatter integrand; composites into B12-d fog as `surface·T + inscatter`. CPU physics: T falls with depth, inscatter accumulates, bluish; Vulkan 1.79e-5. **▶ B15-a COMPLETE — the full Hillaire atmosphere (transmittance + multiscatter + sky-view + aerial).** Hot shared nodes (r_x, μ_sun_x, bilinear coords) `stmt_materialize`d — register reuse on GPU + collapses the recursive-oracle re-walk. Portability metric is ABSOLUTE (a [0,1]/small-radiance LUT); the ~1e-5 floor is hardware exp/sqrt-vs-libm, all add/sub/mul/div bit-exact. **NEXT: B15-b volumetric clouds.** — original scope: **Bruneton-Neyret** precompute + **Hosek-Wilkie / Prague (Wilkie 2021, twilight/polarisation)** analytic sky as the offline env/IBL bake. **[✅ B15-b] volumetric CLOUDS** — **Nubis** (Schneider 2015/2023) built in CKIR (`ckir_clouds.hpp`, `crd::kir::clouds`, statement-tier; both Vulkan BIT-EXACT vs oracle): **[✅ density field]** `build_cloud_density` — the industry-standard **PERLIN-WORLEY** base (a Perlin-FBM DILATED by an inverted Worley-FBM ⇒ Schneider's billowy cauliflower cumulus; reuse **B6 `ckir_noise.hpp`**) × cumulus height gradient × weather COVERAGE carve × high-freq **WORLEY** erosion; **fully procedural + portable**, CPU physics (density∈[0,1], vanishes at slab floor/ceiling, grows with coverage, deterministic) + Vulkan **1.51e-7** (compiled `optimize=true`). **[✅ ray-march]** `build_cloud_march` — the Nubis pipeline: BAKE the density into a 3D VOLUME (density kernel over a grid) then the march SAMPLES it trilinearly (sampling a baked volume per step is the standard Nubis perf tier); accumulates **BEER-POWDER** transmittance + a light-march toward the sun + **Cornette-Shanks phase** + Schneider's **multi-octave multiple-scatter** series; CPU physics (T∈(0,1], opaque + scatters, denser→more opaque) + Vulkan **4.77e-7**. **⛔ SIX portability scars found+fixed (all general CKIR wins):** (1) the recursive oracle had NO memo → deep noise graphs intractable → added generation-keyed MEMOIZATION (guarded off for subgroup ops), full suite 200s→29s; (2) the statement-tier compute Const emitter mangled a u32 const > INT_MAX (`static_cast<int>`→INT_MIN) → `app_int_const`/`%lld` all 5 backends; (3) `BitOr/And/Xor` of BOOL operands (Perlin gradient3's `(h==12)|(h==14)`) emitted illegal `bool | bool` GLSL → emit `||`/`&&`/`!=` for bool operands; (4) procedural 27-cell Worley (`nz::worley3`) UNROLLED blows shaderc+driver super-linearly (6× in a Perlin-Worley density ≈ 8000 nodes, killed at 500–800 CPU-s) → **`clouds::worley3_loop` emits a RUNTIME LOOP over the 27 cells** (running-min is order-INDEPENDENT ⇒ bit-exact; per-thread shared min accumulator — the ReSTIR pattern), ~30 nodes each ⇒ the full gold Perlin-Worley compiles in seconds; (5) the compute-kernel GLSL emitter's statement path is **SCALAR-ONLY** (vec3/dot/swizzle live only in the raster value emitter) ⇒ compute-path noise must SCALARIZE; (6) a value shared across sibling For-bodies — or a top-level value that strands inside a loop — must be `stmt_materialize`d at top level (else GLSL "undeclared identifier": the temp is scoped to the first loop it appears in); (7) a NEW cross-backend emit gate (`test_ckir_kernel_emit.cpp`, all 5 backends) then exposed that the **CUDA/MSL/WGSL** compute emitters INLINE-EXPANDED value expressions with NO CSE (unlike the GLSL/HLSL `temped[]` path), so the deep Perlin-FBM+hash DAG expanded EXPONENTIALLY (128 MB OOM) — gave all three the same node-id `is_inline_op`/`decl`/`matd` materialization pass ⇒ every backend emits the gold cloud density compactly (debt `ckir-offhost-emitter-cse`, FIXED same session; existing off-host structural tests unchanged; determinism unaffected — CUDA compiles `--fmad=false`). **The gold Perlin-Worley is fully procedural, portable across ALL 5 backends, and bit-exact — no baked-texture crutch for the shape.** **▶▶ B15 COMPLETE — atmosphere (a) + clouds (b).** — original scope: Perlin-Worley/Worley density field (reuse **B6 `ckir_noise.hpp`**) + weather/coverage/height remap, ray-march with **Beer-Powder + multi-octave multiple-scatter** + the **approx-Mie phase**, temporal 1/16-step amortization; Nubis³ = voxel/NanoVDB authorable fly-through cloudscapes. Shared phase-function family with fog/water. |
| ◧ | 22 | B16 | IR | **WATER / OCEAN + CAUSTICS** — **[✅ DoD-CLOSED 2026-07-18 — displaced-ocean geometry core + B4 mesh path verified GREEN all configs (win-debug full 5061 + focused shipping/asan/release/tidy on the 7 touched targets); the close-out sweep peeled a pre-existing onion — 12 non-ASCII test names, 3 `std::pow`→`crd::math::pow`, the win-shipping `#deps 0` stale-obj landmine, and a FULL gold-standard units typing of the CKIR sim/GI configs (atmos/ocean/ddgi/svgf → Length/Velocity/Acceleration/Angle + custom InverseLength, bit-exact) — all root-caused + fixed, no debt. Session: `docs/sessions/2026-07-18-b16-close-units-typing.md`. DEFERRED (per the move to B4→B17): B16-a-1 SSR + planar reflection + screen-space refraction + underwater Beer/god-rays (Arc Blanc) + wave-particle↔FFT coupling; B16-b caustic maps + DXR caustics.]** **[✅ B16-a-0] transcendental compute substrate** — the ocean spectrum needs `log` (Box-Muller Gaussian), `tanh` (dispersion depth), `atan2` (wave direction); the compute-kernel emitters only wired `sqrt/sin/cos/exp/pow/floor` (the full transcendental set lived only in the raster value emitter). Wired **`Log/Log2/Tanh/Atan2/Atan/Asin/Acos/Sinh/Cosh`** into ALL FIVE compute-kernel emitters (`ckir_{glsl,hlsl,cuda,msl,wgsl}.hpp`; the oracle's `apply_unary`/`apply_binary` already had them); 5-backend emit gate + CPU-oracle test (`[ocean]` in `test_ckir_kernel_emit.cpp`) + **Vulkan dispatch == oracle to ULP (4.77e-7)**; crd-kir 30569/154 green, tidy-clean. **[✅ B16-a-1..a-4 SIMULATION + fragment render, 2026-07-16]** the full **Horvath** ocean runs bit-exact in CKIR: `build_ocean_spectrum` = **TMA (JONSWAP × Kitaigorodskii depth Φ) + Mitsuyasu directional spread + swell + capillary suppression** (⛔ scar: the TMA mid-branch `1−½(2−ωh)²` goes NEGATIVE for deep water ωh≥2 → clamp ωh≤2, else the whole spectrum ZEROES — caught by an `hmax=0.001,normal=0` diagnostic); `build_ocean_evolve` (exact-Hermitian h̃(k,t)); the Tessendorf 8-field→4-complex pack on ONE **batched inverse 2-D FFT** (`build_fft2d_c2c_batched`, batch=4·C) + the **fusion crush** (fold evolve into the row-IFFT); `build_ocean_assemble` (displacement + slope-corrected normal + **Jacobian J<0 foam**); `build_ocean_foam_accumulate` (temporal `max(prev·decay,inject)` — whitecaps linger then fade); `OceanCascadeConfig`. Pipeline GPU==oracle to ULP (1.19e-7). **Render observable** (`[.ocean-frame]` → BMP): the GPU pipeline BAKES the field to a texture, **2 real cascades** (swell+chop) via `draw_bindless` + `tex_sample_at`, spectrum-driven amplitude (sea state = wind/fetch/swell, no artistic waveH), temporal foam, **[✅ B16-b started] Worley/Voronoi CAUSTICS** (`nz::worley2` compiles in the FRAGMENT path — a real Voronoi win) + thick partial clouds + a god-ray radial-scatter pass. **gpu-context wins**: **mipmapped textures** (`create_texture_mipped`, vkCmdBlit chain) + **bindless multi-texture** render path. **▶ PIVOT → DISPLACED GEOMETRY (2026-07-16, user-directed):** a fragment normal-map render shows the FFT's full spectrum as busy 'white-noise'; the smooth directional swells (gasgiant Ocean-URP / FFT-Ocean refs) come from rendering **displaced GEOMETRY**, so B16-a-1 completes on the vertex-displaced grid. Portable-first (all 5 backends incl. WebGPU — mesh/tess don't exist on WGSL) then a mesh-shader fast path. **[✅ gpu-context enablers landed + verified, 603 raster asserts green]** textures now **VERTEX+FRAGMENT visible** (a VS samples the FFT displacement) + **`draw_bindless_depth`** (depth-occluded displaced grid). **[✅ DISPLACED GEOMETRY LANDED + RENDERED, 2026-07-16]** new CKIR op **`SampleIndexedLod`** (bindless ARRAY sample at an EXPLICIT LOD — the combo CKIR lacked: SampleIndexed is implicit-LOD/FS-only, SampleLod is non-bindless; a VS has no derivatives so it needs both) — appended at END of `KOp` (cook-stable); builder `tex_sample_at_lod` (lod in the ext pool, mirroring `tex_sample_grad`); **GLSL** (`textureLod(sampler(tex[nonuniformEXT(i)],samp),uv,lod)`) + **HLSL** (`tex[NonUniformResourceIndex(i)].SampleLevel(...)`) emitters + the nonuniform-qualifier gate; MSL/WGSL emit gracefully returns false (the same portability status as the existing bindless SampleIndexed — WebGPU has no descriptor-array bindless; the portable WGSL form = a `texture_2d_array` layer + LOD, a promote step). Then the **Johanson PROJECTED-GRID** water: `build_ocean_displaced_vs` (a screen-space lattice tessellated from `VertexIndex` — cell=vid/6, corner=vid%6, integer div/mod — raycast onto the y=0 plane with the EXACT FS camera, displaced by the SampleIndexedLod-fetched SWELL height with a distance-ramped LOD + a far-taper so the horizon meets the sky cleanly, then projected back to clip with the inverse-camera closed form, **no mat4**) + `build_ocean_water_geo_fs` (shades from the interpolated world position; the high-freq CHOP lives here as a **per-pixel mip-filtered normal map** — the FS has derivatives ⇒ NO minification aliasing, the fix for the "white-noise" look — combined with the swell slope + teal body + turquoise SSS + HDR-white foam + grazing aerial haze). Rendered `[.ocean-frame]` → **`ocean_geo_*.bmp`**: real 3-D swell silhouettes, crisp fine detail, calm directional rollers — the gasgiant/AC4 look, geometry-driven. The AAA split = **geometry carries the smooth swell, the FS normal-map carries the fine chop**. Composited over the fragment SKY pass by coverage-α (no new context load-op). **[✅ 4-CASCADE + JOINT-JACOBIAN FOAM, 2026-07-16 user-directed]** upgraded to the production **4-spectrum** ocean (`OceanCascadeRender` — 4 non-harmonic world scales ⇒ tiling period = LCM ⇒ effectively non-repeating): the BIG cascades carry HIGH-amplitude rolling-swell geometry, the FINE cascades are LOW-amplitude (mostly in the per-pixel normal — "small detail, low amplitude"). Each cascade bakes `[nx, nz, height, ½(1−J)]`; the FS SUMS the per-cascade folds → the **JOINT Jacobian foam** (folding of the combined 4-spectrum surface, the user's explicit ask — first-order exact since `det(I+ΣG) ≈ 1+Σtr(G_c)`), generous whitecaps across the crests. Teal-green body + stronger sun-glitter path (ref: a Sea-of-Thieves-class confused sea). Matches/beats the reference. **[✅ PROMOTED to the ENGINE 2026-07-16]** the whole displaced-geometry ocean render pass is lifted out of the test harness into `engine/kir/include/crd/kir/ckir_water_render.hpp` (`crd::kir::water`): `OceanCascadeRender` config + `ocean_grid` camera + the shared `ocean_projected_vertex` + `build_ocean_displaced_vs` + `build_ocean_displaced_mesh` + `build_ocean_water_geo_fs` — reusable by the renderer + the node editor (the tests alias it into `crd::gputest`, call sites unchanged; render byte-identical). The portable displacement path is **B4-tess** (line 110); the mesh-shader fast path is **B4** task/mesh. Research: `docs/research/2026-07-16-aaa-ocean-water.md`. **⚠ B16-close visual polish still owed (gold-standard, user-flagged): the horizon composite-seam outline · streakier foam · softer sun · wispier clouds — see the ref (`build/ref_ocean_2.png`).** **[B16-a-1 remaining scope]** SSR + planar reflection + screen-space **refraction** + **underwater** Beer absorption/god-rays (ref: **Arc Blanc**, JCGT 2025); interactive wave-particle↔FFT coupling (arXiv 2511.02852). **Compute dep: GPU FFT kernel (B-cmp) ✅** — the spectrum→displacement IFFT. **[B16-b] caustics** — **caustic maps / cascaded caustic maps** (image-space raster, Shah 2007 / Kougianos 2021) as the default + **DXR ray-traced caustics** (RTG-II 2021, dynamic glass/metal/water) as the RT tier (B9). |
| ✅ | 23 | B17 | IR | **ORDER-INDEPENDENT TRANSPARENCY** — ALL THREE tiers LANDED + device-verified BOTH backends (2026-07-19, session `docs/sessions/2026-07-19-b17-oit.md`; accuracy board `docs/bench/2026-07-19-oit-tier-scoreboard.md`). **[✅ B17-b] MBOIT** (Münstermann 2018, the hero glass/foliage tier): absorbance-weighted 4-POWER-MOMENT generation + the Peters-Klein HAMBURGER reconstruction (`oit::msm_hamburger_scalar` — a scalar transcription of the moment-shadow-map `lighting::msm_hamburger`, since the compute-kernel emitter is scalar-only) → per-fragment transmittance `T(zᵢ)=exp(-b₀·G(wᵢ))` → composite, over the SHARED deferred store. Lowers to-ULP (GPU==`eval_cpu_kernel`, both backends). 4 power moments resolve 2 depth masses **EXACTLY** ⇒ bit-exact at 2-layer complexity (glass), beating WBOIT (30 LSB off). **LIFTED to high depth complexity: the 6-POWER-MOMENT extension** (`oit::msm_hamburger6_scalar` — the Hamburger solve generalized to a 4×4 Hankel Cholesky + a CUBIC root-solve + a Gauss-Radau/Lagrange-weight form factor) resolves **3 masses**, landing **~1 LSB from exact** at 3-layer complexity where WBOIT is 18 LSB off — an **18× accuracy win over WBOIT**, both backends. Exact-capacity = ⌊moments/2⌋ masses (8/10-moment extends further via a larger Cholesky + quartic/quintic). MBOIT beats WBOIT at every depth complexity within its moment budget — no recorded loss. **[✅ B17-a] WBOIT** (McGuire-Bavoil 2013, the cheap single-pass tier): a NEW gpu-context capability — **render-to-texture float MRT + per-attachment blend equations** (Vulkan `vkCmdSetColorBlendEquationEXT` dynamic state · DX12 `IndependentBlendEnable` static PSOs). accum (RGBA16F additive) + revealage (R16F multiplicative) accumulate in ONE order-independent pass; a full-screen composite resolves over the background (`IRasterContext::draw_wboit`). CKIR shaders (`tests/gpu-shared/ckir_oit_test.hpp`) + f16-aware CPU oracle; observable RGBA8 matches **≤2 LSB** (f16 accum + f32 divide ⇒ to-ULP tier) VK==DX12. **[✅ B17-c] A-BUFFER exact reference** (Carpenter 1984 / GPU LL): two portable CKIR **compute** kernels (`engine/kir/include/crd/kir/ckir_oit.hpp` — `build_abuffer_store` + `build_abuffer_resolve`) — deferred per-fragment store (static slot = fragment id for the fixed-coverage reference scene; the runtime **atomic** append is the scalable variant, needing value-returning atomics in CKIR — a separable IR extension) + a per-pixel depth **SORT network** (fully unrolled compare-exchange) + the exact front-to-back `over` composite. Pure f32 mul/add/sub on a deterministic order ⇒ **BIT-EXACT** vs `eval_cpu_kernel` and VK==DX12 (worst \|Δ\|=0). The GROUND TRUTH the approximate tiers score against. **[✅ SCOREBOARD]** WBOIT vs exact = **14 LSB** max error (bounded, measurable). No regression (VK raster 703 · DX12 725). Tidy-clean. **⌊moments/2⌋ = 3-mass is the MBOIT reconstruction-complexity ceiling** — exhaustively mapped (session log): 8/10-moment power = shader-COMPILE wall (the quartic body is too complex even emitted once in a runtime loop — proven via a trivial-body diagnostic), trigonometric = QUALITY wall (Fourier/Fejér too blurry, 80 LSB; exact-recovery Szegő needs root-finding → back to the compile wall); beyond 3 masses the EXACT A-buffer is the tool. **[✅ B17-c SCALABLE — the three carry-forwards ALL SHIPPED 2026-07-19, both backends]:** (1) **atomic linked-list A-buffer** via NEW **value-returning atomics** in CKIR (`atomic_add_fetch` node allocator + `atomic_exchange` list push — `AtomicResult` impure leaf, all 5 emitters) — race-built lists, sort-resolved, matches the static-slot exact reference **BIT-FOR-BIT** (worst \|Δ\|=0) VK==DX12; gold interleaved node-pool layout (≤8-binding descriptor cap); (2) **stochastic transparency** (Enderton 2010) — one kernel, S deterministic-hash-covered sub-samples, mean = the UNBIASED `over` (converges ~1/√S; per-frame TAA = 1 sample/frame); the triple32 hash is GPU==oracle **bit-exact** both backends, which required making the **CKIR oracle's u32 arithmetic wrap mod-2^32** (`apply_binary_typed` — it did integer ops in f64 with no wrap, a mission-critical portability hole; full kir suite stayed green 34759); (3) **GPU perf board** (`docs/bench/2026-07-19-oit-tier-perf.md`, `[.oit-bench]` `last_gpu_ms` min-of-30, 1024²×4-layer): static store-based tiers cheapest (~0.28 ms) and **MBOIT's accuracy is FREE** (memory-bound), atomic list ~10× (unbounded-depth price), stochastic cheapest unbounded tier per-frame (~0.061 ms @ S=1). Tidy-clean all 3 targets; VK full 84c · DX12 746a. **B17 = 5 tiers, both backends, accuracy + perf boards, no loss recorded.** The substrate 3DGS (B19) also composites through. |
| ◧ | 24 | B18 | IR | **HAIR / FUR shading** (geometry = B4 curves / B9 LSS) — **Marschner** fiber scattering (R/TT/TRT lobes, 2003) + **dual-scattering** multiple-scatter (Zinke 2008 — makes light/blonde hair read right) + **Chiang (Disney) hair/fur BSDF** (2016, energy-conserving, melanin — the production path-traced standard) + **deep opacity maps** (Yuksel 2008, real-time self-shadow); **LSS ray-traced strands** (B9) for the RT tier. **◀ NEXT (2026-07-19)** — the RT triangle tier + C3/B9 (minus LSS) are in place; B18 now builds the hair/fur BCSDF stack on top and lands the LSS curve/sphere AS that flips C3/B9 to ✅. Sub-slices: **B18-a** Chiang energy-conserving BCSDF core (R/TT/TRT/TRRT) → **B18-b** fur medulla + Huang microfacet lobe → **B18-c** dual scattering (Zinke) + deep opacity self-shadow → **B18-d** strand geometry emission + hair G-buffer → **B18-e** composited frame (stochastic-transparency OIT). **DELIVERED (2026-07-20) — B18-a/b/c ✅, B18-d core ✅, B18-e ✅:** Chiang BCSDF + fur medulla + Huang R/TT/TRT; Zinke dual scattering + moment LUT + Yuksel DOM + Hu volumetric MS; strand-gen + 64-bit atomicMin G-buffer; and now the **Lipp 2026 tangent-oriented elliptical bilateral compositing filter** (`build_hair_filter_kernel`, `ckir_hair_geom.hpp`) with an explicit tail-lane bounds guard — gated on CPU (anisotropy ratio exact to exp(k²(1/σ⊥²−1/σ∥²)), hard depth rejection, convexity, gap-fill vs cross-strand smear) and DISPATCH-verified on **both** backends (Vulkan == DX12 == oracle, maxabs 1.43e-6, 38 deliberate out-of-range tail lanes). Beauty frame adds true **sub-pixel coverage** (bilinear 2×2 splat + order-independent 1−∏(1−a)) — 18.7% of covered pixels are partial, which is what resolves the silhouette; the depth-guarded filter by design cannot. **REMAINING in B18-d:** stochastic LOD + Δ=−log(ρ). ⭐ RESEARCH BASE: `docs/research/2026-07-19-hair-fur-frontier-collection.md` (9-paper dossier: Chiang16 · Huang22 · Yan17 · Zinke08 DS · **Hu TOG-07/2026 volumetric MS** · Yuksel08 DOM · EGSR25 aggregated-BCSDF LOD · Lipp26 deferred SW raster · Yuksel24 hair meshes · Curly-Cue 24) + `2026-07-19-huang-microfacet-hair.md`; papers in `docs/research/papers/hair_fur/`. → **B18-f RT STRAND TIER (the C3/B9 LSS closeout):** LSS curve/sphere AS build (C3) + curve-hit attributes in the RT closest-hit (B9) + path-traced hair reference (the BCSDF as CHIT + dual scattering) — **this is the sole remaining item in C3 (#28) + B9 (#29); when it lands both flip ◧ → ✅.** |
| ⬜ | 25 | B19 | IR | **3D GAUSSIAN SPLATTING (radiance fields as a first-class primitive)** — **3DGS** (Kerbl 2023 — millions of anisotropic Gaussians, tile-based differentiable rasterization = an OIT/**sort** problem) + **Mip-Splatting** (Yu 2024, alias-free — mandatory) + **2DGS/SuGaR** (surfel/mesh extraction bridging into the B1 material pipeline) + **compression** (Self-Organizing Gaussians, 10-50× — mandatory to ship) + **relightable / ray-traced / PBR Gaussians** (3DGRT, GaussianShader — reflect/relight captured content). Differentiable training rides **CKIR autodiff (v15/v16)**. Compute deps: **GPU radix/bitonic sort** (per-tile depth) + the OIT composite (**B-cmp** / B17). |
| ⬜ | 26 | B-cmp | IR | **hesap-GPU COMPUTE PRIMITIVES (built DURING the detour — user 2026-07-12; each lands with its first render consumer)** — the CKIR compute kernels the frontier rendering needs, bit-exact CPU oracle + both backends: **FFT** (B13-b convolution bloom · B16 FFT ocean) · **GPU sort** (radix/bitonic — B19 Gaussian depth · B17 A-buffer · light lists) · **scan / stream-compaction** (B8-l clustered light-cull · B4 visibility-buffer cluster-cull · particle compaction) · **reduction** (B13-c auto-exposure histogram · SDSM + Hi-Z depth min/max · luminance) · **small GEMM / MLP + backprop** (B14-d NRC · B10 neural materials, on coop-vectors). These SERVE hesap-GPU too (one source); the BROADER hesap-GPU (dense/sparse LA · eigensolvers · ODE · stats — rendering never calls) stays post-detour. |
| 🚧 | 27 | B4 | IR | **mesh-shading pipeline** — **[✅ B4 MESH CORE LANDED + GPU-PROVEN 2026-07-16]** the modern amplification path runs end-to-end in CKIR: a **`Mesh` KEntry** (`mesh_vertices`/`mesh_primitives` + a `mesh_prim` uvec3 of local indices; per-vertex `position`+`out[]` read the workgroup builtins — a global vertex id from `WorkgroupIndex`·V + `LocalInvocationIndex`; validated in `entry_valid`) → **`emit_mesh_glsl`** (`GL_EXT_mesh_shader`: `SetMeshOutputsEXT`, guarded per-vertex writes to `gl_MeshVerticesEXT[tid]` + per-vertex out arrays + `gl_PrimitiveTriangleIndicesEXT[tid]`; reuses the raster value machinery) → shaderc `shaderc_mesh_shader` @ **SPIR-V 1.4** (literal `LocalSize`, not `LocalSizeId`) → the device: **`VK_EXT_mesh_shader` + `meshShader` + `maintenance4`** enabled (gated so the non-mesh device is byte-identical), a **mesh shader object** (`VK_SHADER_STAGE_MESH_BIT_EXT`, **`NO_TASK_SHADER` flag** — the missing bit that caused a device-lost until found) via `create_mesh_program`, and **`vkCmdDrawMeshTasksEXT`** via `draw_mesh` (skips the vertex-input/topology dynamic state — faults some drivers with a mesh shader bound). **The `[gpu-context][vulkan][gpu][mesh]` triangle proof RENDERS** (a mesh-emitted triangle, red-center/blue-corner pixel-identical to the vertex-pull triangle) — CKIR mesh entry → shader object → `drawMeshTasks` → pixels. ⭐ scar: the raster `Vec2`/`Vec3` emitter hardcoded the `vec2(`/`vec3(` constructor → a `uvec3` of indices emitted `vec3(...)` (compile fail); fixed to `vtype(nd.type)` (no-op for float, so canary held). **[✅ OCEAN MESHLETS RENDERED 2026-07-16]** the 4-cascade ocean now renders as MESHLETS: `ocean_projected_vertex` factored as the ONE shared raycast/displace/project (both the vertex-pull VS and the mesh path call it ⇒ pixel-identical), + `build_ocean_displaced_mesh` (each workgroup emits an 8×8-vertex patch → 98 tris ≤ glslang's 128 cap; 32×32 = 1024 meshlets tile the grid; `WorkgroupIndex`→patch, `LocalInvocationIndex`→local vertex/primitive, integer div/mod). Renders `ocean_mesh_*.bmp` == `ocean_geo_*.bmp` (teal swells + whitecaps), via `draw_mesh_bindless_depth` (bindless cascades + depth). **⭐ THREE more device scars found+fixed:** (1) `local_size` too large — glslang caps mesh workgroups at 128, so the patch is 8×8 (98 threads), not 12×12 (242); (2) VUID-08690 — once `meshShader` is enabled EVERY plain `vkCmdDraw` must bind the MESH stage to null (else device-lost), so `set_draw_state` binds `MESH=null` for vertex draws; (3) **`nonuniformEXT` on a UNIFORM (constant) bindless index returns ZERO in a mesh shader on this NVIDIA driver** — the emitter now omits the qualifier for a constant index (correct anyway; the ocean cascade index is a constant), which is what made the mesh displacement finally read the FFT. Descriptor bindings gained the MESH stage flag. **[✅ HLSL MESH EMITTER 2026-07-16]** `emit_mesh_hlsl` (DX12 / Shader-Model-6.5): `[outputtopology("triangle")]` + `[numthreads]` + `SetMeshOutputCounts` with `out vertices`/`out indices` array params (SV_GroupIndex=tid, SV_GroupID=meshlet), SV_Position LAST (the DXIL register-packing scar). DXC profile `ms_6_5`/`as_6_5` wired. **The `[hlsl][mesh]` gate COMPILES both the triangle AND the real bindless ocean meshlet HLSL → SPIR-V via DXC** — the mesh IR lowers to DX12 too (the device-side mesh PSO + `DispatchMesh` is the DX12 render follow-up; Vulkan already renders). **PORTABILITY:** Vulkan = renders · DX12 = emit+DXC-validated · WGSL/MSL = the vertex-pull fallback (WebGPU has no mesh shaders; the caller guards on `mesh_shader()`). Raster suite 620 green. **[✅ DX12 DEVICE MESH RENDER LANDED 2026-07-18]** the CKIR mesh IR now RENDERS on the DX12 device, not just DXC-validated: IR→DXIL mesh on-ramp (`ms_6_5` profile + `KStage::Mesh`→`ShaderStage::Mesh` + a `create_program` mesh branch calling `emit_mesh_hlsl`), the device gate (`ID3D12Device2`+`ID3D12GraphicsCommandList6` via QI + `D3D12_FEATURE_D3D12_OPTIONS7` `MeshShaderTier`), a **hand-rolled mesh PSO subobject STREAM** (`build_mesh_pso` via `ID3D12Device2::CreatePipelineState` — the backend has no CD3DX12; ⛔ scar: `alignas(void*)` on each stream subobject trips `C4324` under `/WX` → suppressed by design, the padding IS the layout), a `Dx12RasterProgram` mesh variant (`pso_for` branches to the stream PSO), `create_mesh_program` (4-table root sig, SRV/sampler/bindless `ALL`-visible so the mesh stage can sample cascades) + `draw_mesh` (`DispatchMesh`, no IA topology). **The `[dx12][mesh]` triangle RENDERS** (red-center/blue-corner == the vertex-pull triangle, 8 asserts) — CKIR mesh entry → DXIL → stream PSO → `DispatchMesh` → pixels; tidy-clean. ⭐ the mesh IR now fans out to BOTH devices (Vulkan + DX12 render; WGSL/MSL = vertex-pull fallback). **[✅ DX12 OCEAN MESHLETS RENDER 2026-07-18]** the displaced ocean now renders on DX12 via mesh shaders: added `draw_mesh_bindless_depth` (combined the DX12 `draw_bindless` SRV-heap machinery + the `draw_depth` colour+DEPTH target + `DispatchMesh`; the mesh root sig makes the bindless cascades ALL-visible so the mesh stage samples the FFT displacement). The `[dx12][mesh][ocean]` test renders `build_ocean_displaced_mesh` (16² meshlet patches) with 4 bindless cascade textures into a depth target — **~60% ocean coverage (5566/9216 px)**, tidy-clean. Device parity with the Vulkan ocean meshlets. **[NEXT]** the WGSL-portable bindless-LOD form (texture_2d_array layer, not descriptor-array bindless — WebGPU has no bindless). — **[✅ TASK / AMPLIFICATION STAGE LANDED + RENDERS BOTH BACKENDS 2026-07-18]** the GPU-driven amplification path runs end-to-end in CKIR: a **`Task` KEntry** (`task_emit` = the mesh-workgroup count + an optional single-uint `task_payload`) + a new **`KBuiltin::TaskPayload`** (the mesh reads the payload); **`emit_task_glsl`** (`EmitMeshTasksEXT` + `taskPayloadSharedEXT`) + **`emit_task_hlsl`** (SM6.5 `as_6_5`: `DispatchMesh` + groupshared payload), and the mesh emitters gained the payload declaration/read (`in payload` on HLSL, `taskPayloadSharedEXT` on GLSL). DEVICE: **Vulkan** `create_task_mesh_program` (task+mesh+frag shader objects, mesh WITHOUT `NO_TASK_SHADER` since a task precedes it, draw binds the TASK stage) · **DX12** the AS subobject added to the mesh PSO stream + `create_task_mesh_program` (AS+MS+PS). The `[mesh][task]` test RENDERS on BOTH: **1 task workgroup `EmitMeshTasksEXT/DispatchMesh`-es 4 mesh workgroups ⇒ 4 triangles, each coloured by the payload** (proves amplification AND the task→mesh payload channel); tidy-clean, both suites green (vk 26 asserts/3 cases · dx12 22/3). **[✅ MULTI-FIELD PAYLOAD 2026-07-18]** the task→mesh payload extends beyond 1 uint to a FIXED 4-field struct (`KBuiltin::TaskPayload{,1,2,3}` — distinct builtins ⇒ distinct CSE ⇒ no field aliasing; the task's + mesh's `taskPayloadSharedEXT`/`groupshared MeshPayload` are both 4 uints so their layouts always match); `KEntry.task_payload[4]` + `n_task_payload`; a real meshlet pass hands the mesh bounds/LOD/material, not one uint. The `[mesh][task]` test passes a 3-uint payload (v0,v1,v2 → R,G,B) read all-three by the mesh — both backends (vk 12 asserts, dx12 8); single-field back-compat held. **[✅ GPU-DRIVEN COMPUTE-CULL → INDIRECT MESHLET DISPATCH 2026-07-18 — BOTH BACKENDS ON THE DEVICE]** the Nanite scale loop with NO CPU round-trip: `build_meshlet_cull` (a compute pass — one thread per meshlet — `atomicAdd`s the survivor count straight into an INDIRECT-dispatch args buffer `{groupCountX,1,1}`), then the raster context's new **`draw_mesh_indirect`** consumes it via **`vkCmdDrawMeshTasksIndirectEXT`** (Vulkan) / **`ExecuteIndirect` + a `D3D12_INDIRECT_ARGUMENT_TYPE_DISPATCH_MESH` command signature** (DX12) — the mesh-workgroup count decided ENTIRELY on the GPU. Cross-seam plumbing: `compute_usage::indirect` (Vulkan `VK_BUFFER_USAGE_INDIRECT_BUFFER_BIT` + **CONCURRENT queue-family sharing** so the dedicated compute queue's write is read by the graphics queue without an ownership transfer; the compute submit fences first), `ComputeBuffer::native_handle()` (the `VkBuffer`/`ID3D12Resource*` escape hatch feeding `draw_mesh_indirect`). The `[mesh][indirect]` test: 8 meshlets, 5 visible → the compute cull writes count=5 → the indirect dispatch launches exactly 5 mesh workgroups → **5 triangles render, the 3 culled never dispatch** — Vulkan (14 asserts) + DX12 (9), device-verified, no validation errors. `IRasterContext` gained `draw_mesh_indirect` (appended at END). **[✅ PER-PRIMITIVE VRS FROM THE MESH 2026-07-18 — BOTH BACKENDS ON THE DEVICE]** the mesh shader emits a per-primitive shading rate (`KEntry::shading_rate` → GLSL `gl_MeshPrimitivesEXT[].gl_PrimitiveShadingRateEXT` / HLSL `out primitives MPrim { uint sr : SV_ShadingRate }`) so a distant/low-detail meshlet shades itself at a coarser fragment rate — a real perf lever. New `draw_mesh_vrs` sets the REPLACE/OVERRIDE combiner (the primitive rate wins over the 1×1 base). The `[mesh][vrs]` test: a fullscreen-triangle mesh with a 2×2 primitive rate over the ramp FS → each 2×2 block shades once (n_equal = 512 = full coarsening) on Vulkan + DX12. **[⏭ WGSL `texture_2d_array` cascade — CORRECTLY SCOPED TO THE WebGPU MILESTONE, NOT D-007]** WGSL is COMPUTE-ONLY in this codebase (no `emit_stage_wgsl`/fragment/mesh emitter; the raster frontier is GLSL+HLSL by design for the Vulkan+DX12 milestone), so the bindless-cascade→`texture_2d_array` port is a RASTER-emitter change with no WGSL raster emitter to host it — building one IS the WebGPU deployment path ([[project_browser_wasm_deployment_goal]]), a separate later effort. The `texture_2d_array` layer+LOD form stays the documented portable approach for when that lands. remaining original scope: **cluster/meshlet emission feeding cluster-AS** (RTX Mega Geometry mesh↔RT bridge, → B9/C3 RT tier). Geometry legacy-emulated. **+ B4-vis: NANITE-CLASS VIRTUALIZED GEOMETRY — the VISIBILITY BUFFER** (Burns-Hunt 2013: store triangleID/instanceID/barycentrics, deferred-texture + shade → kills overdraw, scales to micropoly; the modern replacement for the fat G-buffer) **+ a compute SOFTWARE RASTERIZER for sub-pixel triangles** (Karis Nanite 2021, ~3× HW throughput on micropoly) + **GPU-driven** per-cluster HZB two-pass occlusion cull + 128KB streaming pages (compute dep: scan/stream-compaction, **B-cmp**; ref: RE ENGINE REAC 2025). **[✅ B4-vis-1 COMPUTE SOFTWARE RASTERIZER LANDED + BIT-EXACT BOTH BACKENDS 2026-07-18]** the Karis software rasterizer runs end-to-end in CKIR: a new `KStmtKind::BufferAtomicMin` (atomic-min on a u32 buffer — appended at END of the cook-serialized stmt enum; wired into ALL FIVE compute-kernel emitters `atomicMin`/`InterlockedMin`/`atomic_fetch_min_explicit` + the CPU oracle, where MIN is order-independent ⇒ bit-exact) + the completion of `Ceil`/`Clamp` in all five compute-kernel value emitters (the elementwise path had them; the imperative-kernel `rhs` did not — the wire-ALL-backends scar) → **`crd::kir::visbuffer::build_sw_raster_visbuffer`** (`ckir_visbuffer.hpp`): one thread per triangle, perspective-divide → screen, an edge-function coverage test over the clamped bbox (front-facing `min(edges) >= 0`), perspective-correct barycentric depth (z/w is affine in screen space), packed into a per-pixel `(depth << id_bits) | triangleId` u32 written by **atomicMin** — the nearest triangle wins, ties broken by lowest id. NO 64-bit atomics (a single u32 key ⇒ portable) and the compute shader does its OWN perspective divide, so the depth is **backend-independent** (the HW clip-space NDC-z convention never applies). The `[visbuffer]` test: a 3-triangle scene (a quad at depth 0.6 + a nearer centre triangle at 0.2) rasterizes **bit-exact GPU == CPU oracle** on **Vulkan** (9 asserts, GLSL→SPIR-V) AND **DX12** (8 asserts, HLSL→DXIL) — the two backends agree with each other (shared scene+oracle) — the centre pixels resolve to the near triangle (atomicMin depth), the quad halves to ids 0/1, outside stays the empty key; + a 5-backend emit gate (12 asserts, `test_ckir_kernel_emit.cpp`). Regression-clean (DX12 [compute] 111 · Vulkan [kernel] 311 · full kir suite green). **[✅ B4-vis-2 DEFERRED ATTRIBUTE INTERPOLATION SHADE (DAIS) LANDED 2026-07-18]** the deferred half (Schied-Dachsbacher, HPG 2015): `build_deferred_attr_shade` — one thread per PIXEL reads the visibility key, and for a covered pixel fetches the visible triangle's 3 clip positions + per-vertex attributes, reconstructs the PERSPECTIVE-CORRECT barycentrics at the pixel centre (`attr = Σ(e_i/w_i · a_i) / Σ(e_i/w_i)` — the `1/area` cancels between num and denom, so only the FINAL normalize is a divide), and writes the interpolated attribute — shading ONCE per visible pixel (the visibility-buffer payoff: no overdraw, no fat G-buffer). The `[dais]` test rasterizes a PERSPECTIVE triangle (distinct clip-w {1,2,4}) → CPU vis buffer → the GPU deferred pass; **VISIBILITY is bit-exact (B4-vis-1), the deferred SHADE matches the oracle to a TIGHT ULP tolerance** (max rel err **1.18e-7 ≈ 2 f32 ULP, IDENTICAL on Vulkan + DX12** — the two backends are bit-exact with EACH OTHER; only the correctly-rounded f64 oracle differs, because f32 division is ~2.5 ULP on Vulkan/DX12, not correctly rounded — the same honest status as the ocean spectrum's transcendentals). Proves perspective correction is active: at the screen centroid the perspective-correct value (~8) is decisively below the naive barycentric average (14). Vulkan 10 asserts · DX12 8 · 5-backend emit gate 10. **[✅ B4-vis-3 HZB TWO-PASS OCCLUSION CULL LANDED + BIT-EXACT BOTH BACKENDS 2026-07-18]** the GPU-driven culling that makes a Nanite pipeline scale: `build_hzb_downsample` (one mip level = max of the 2×2 below; the whole pyramid in ONE concatenated f32 buffer, dispatched once per level 1..n_mips-1 with a barrier between) + `build_cluster_cull` (per cluster: sample the 2×2 HZB texels covering the screen footprint at the host-selected mip, `visible = near_depth <= max_hzb_depth ? 1 : 0` — CULL iff the cluster's nearest point is behind the farthest already-drawn surface, conservative). MAX + compare are order-independent ⇒ **BIT-EXACT** vs the CPU oracle. The `[hzb]` test builds a 16×16 depth split (near wall left / far background right) → the pyramid → 3 clusters: behind-the-wall CULLED, over-open-background VISIBLE, in-FRONT-of-wall VISIBLE — GPU == oracle == analytic on Vulkan (8 asserts) + DX12 (7); a template `make_pipe` factory drives the multi-pass dispatch from both backends. 5-backend emit gate (12). mip selection is trivial host-side cull-setup (findMSB of the footprint; the GPU does the data-parallel HZB sample + occlusion decision). Regression-clean (full kir green · DX12 [visbuffer] 23 · Vulkan 27). **[✅ B4-vis-4 HW-RASTER VISIBILITY BUFFER LANDED + RENDERS BOTH BACKENDS 2026-07-18]** the hybrid Nanite split — HW raster for the big triangles the compute software rasterizer is slower on: `KBuiltin::PrimitiveId` wired into the GLSL (`gl_PrimitiveID`) + HLSL (`SV_PrimitiveID`, uint) FS emitters + a native **R32_UINT** visibility target on both devices (Vulkan: `make_target` format-parameterized + `create_visbuffer_target` + `draw_visbuffer` with a uint clear via dynamic rendering; DX12: `create_visbuffer_target` R32_UINT texture + `build_graphics_pso`/`pso_for` gain an RT-format so a R32_UINT PSO builds + `draw_visbuffer`). The `[raster][visbuffer]` test: a fullscreen quad (2 triangles) → the FS writes `gl_PrimitiveID` → the R32_UINT buffer; `read_pixel` returns the raw id, every pixel is primitive 0 or 1 (fullscreen coverage), each triangle covers a substantial half — Vulkan (10 asserts) + DX12 (7). `IRasterContext` gained `create_visbuffer_target`/`draw_visbuffer` (appended at END, vtable-stable). The FS uint output reuses the existing `vtype(out.type)` path; the deferred materialize reuses B4-vis-2 DAIS. Regression-clean (DX12 [raster] 562 · Vulkan 652 · full kir green). **✅✅ B4-vis COMPLETE — the full Nanite-class visibility-buffer core: software rasterizer (bit-exact) + DAIS deferred shade (to-ULP) + HZB occlusion cull (bit-exact) + HW-raster VBuffer (both devices).** **[remaining B4]** meshlet payload > 1 uint · compute-cull → meshlet dispatch · per-primitive VRS · the WGSL-portable `texture_2d_array` cascade · (optional) wire the full two-pass loop end-to-end. **+ B4-tess (real, not emulated): `TessControl`/`TessEval` emitters — patch levels, `TessCoord` domain eval, height-map displacement — the PORTABLE displacement path for platforms without mesh shaders (mobile/WebGPU/older HW). [✅ B4-tess LANDED + RENDERS BOTH BACKENDS 2026-07-18]** the tessellation path runs end-to-end in CKIR on BOTH devices: `KEntry` tess fields (`tess_patch_size`/`tess_inner`/`tess_outer` + `is_tess_control()`/`is_tess_eval()`) + `KBuiltin::TessPatchPosition` (the emitter-bilerp of the 4 quad corners by `gl_TessCoord`/`SV_DomainLocation`) + entry validation; **GLSL** `emit_tesc_glsl` (passthrough quad hull, `layout(vertices=N) out`, sets `gl_TessLevelInner/Outer`) + `emit_tese_glsl` (`layout(quads,equal_spacing,ccw)`, `patch_pos` bilerp + a displacement value graph → `gl_Position` + interpolants); **HLSL** `emit_tesc_hlsl` (a `SV_TessFactor`/`SV_InsideTessFactor` patch-constant fn + a `SV_OutputControlPointID` passthrough hull, `[domain("quad")][partitioning("integer")][outputtopology("triangle_ccw")]`) + `emit_tese_hlsl` (`[domain("quad")]` domain: `lerp`-bilerp `patch_pos` by `SV_DomainLocation` + the displacement → `SV_Position` LAST, the DXIL register-packing scar). DEVICE: **Vulkan** — `tessellationShader` (core) + **EDS2 `extendedDynamicState2PatchControlPoints`** enabled (gated; `tessellation()` accessor + `set_patch_control_points` PFN), `create_tess_program` (VS+TCS+TES+FS shader objects, `nextStage`-chained) + `draw_tess` (`VK_PRIMITIVE_TOPOLOGY_PATCH_LIST` + dynamic patch size 4). **DX12** — `hs_6_0`/`ds_6_0` DXC profiles + `KStage::TessControl/TessEval`→stage map + `create_program` hull/domain branches, `build_graphics_pso` extended with optional HS/DS (⇒ `PrimitiveTopologyType = PATCH`), a `Dx12RasterProgram` tess variant (`pso_for` passes HS/DS), `create_tess_program` (4-table root sig, textures ALL-visible so the DS can sample a displacement map) + `draw_tess` (`D3D12_PRIMITIVE_TOPOLOGY_4_CONTROL_POINT_PATCHLIST`). The `[tess]` test RENDERS on BOTH: a 4-corner quad patch subdivided 8×8, the domain shader reads `TessPatchPosition` + EXPANDS ×1.3 — a pixel between the base edge (clip 0.6) and the expanded edge (0.78) is red ONLY because the domain shader displaced the generated vertices (proves the tessellator ran per-vertex); vk 13 asserts · dx12 11 asserts, both full raster suites green (vk 642/58 · dx12 555/57), no feature gate on DX12 (tessellation is core FL11). **PORTABILITY:** Vulkan + DX12 render · WGSL/MSL = the mesh/vertex-pull fallback (the caller guards on `tessellation()`). `IRasterContext` gained `create_tess_program`/`draw_tess` (defaulted, appended at END — vtable-stable). remaining B4 scope: **B4-vis** Nanite visibility-buffer + software rasterizer + GPU-driven cluster cull; meshlet payload > 1 uint; compute-cull → meshlet dispatch; per-primitive VRS; the WGSL-portable bindless-LOD form. |
| ◧ | 28 | C3 | DEV | **`IRayTracingContext`** — RT pipelines · SBT · AS binding · inline `rayQuery` · **SER enable** (`VK_EXT_ray_tracing_invocation_reorder`) · **opacity-micromap build** (`VK_EXT_opacity_micromap`) · **CLUSTER acceleration structures** (`VK_NV_cluster_acceleration_structure` — RTX Mega Geometry: templates, streaming LoD, dynamic tess+displacement; cross-vendor **AMD DGF**) · **curve/sphere AS build for hair/fur** (`VK_NV_ray_tracing_linear_swept_spheres` — LSS) · motion AS. **✅ RT detour (re-sequenced ahead of B18, 2026-07-19) delivered:** `VulkanRayTracingContext` + `Dx12RayTracingContext` (DXR mirror) — BLAS/TLAS build + inline-`rayQuery` dispatch (RT-1) · `trace_rays_pipeline` RT-pipeline + 3-region SBT + **SER enable** (FA-2) · **OMM build** `build_scene_omm` (FA-1) · **cluster-AS build** `build_scene_clusters` (FA-3) · **multi-instance TLAS** `build_scene_instanced` (RT-6) · portable **`RtCapabilities`** query + graceful-degradation builders (`build_scene_alpha`/`build_scene_scalable`, P1/P4/P5). **⬜ remaining: LSS curve/sphere AS build → lands with the B18 RT strand tier.** |
| ◧ | 29 | B9 | IR | **ray-tracing shaders** — RT stages (raygen/closest-hit/any-hit/miss/intersection/callable) + AS binding + payload/attributes + **inline `rayQuery`** · **Shader Execution Reordering** (`HitObject`+`ReorderThread` — 20–100% path-trace win) · opacity-micromap-aware any-hit · cluster-AS geometry · **curve/sphere (LSS) hit attributes for hair/fur**. Runs on Vulkan (DX12 mirror). **✅ RT detour delivered (`ckir_rt.hpp` + `emit_rt_stage_{glsl,hlsl}`, both backends bit-exact-vs-oracle):** inline `rayQuery` IR (`trace_ray_closest`/`trace_ray_hit`) + full **RT-pipeline stages in CKIR** (raygen/closesthit/miss/anyhit — payload + `trace_ray_pipeline` + `ignore_hit_if` IR, P2) · **SER reorder hint** (P3) · any-hit alpha (P4) · the integrator ladder — shadows/RTAO/reflections, path-trace megakernel (RT-3), **NEE+MIS** (RT-4), **ReSTIR DI spatiotemporal** (RT-5), **ReSTIR GI** + emissive + Russian-roulette + **many-lights + power sampling** (RT-7/IB-1/2/3). **⬜ remaining: LSS curve/sphere hit attributes → with B18 RT strand tier.** |
| ⬜ | 30 | C6 | DEV | **cooperative-vector device enable** — `VK_NV_cooperative_vector` feature/property query + the coop-vector program path (the device half of B10); cross-vendor mapping (DX12 coop-vectors / CUDA tensor / Metal simdgroup-matrix). |
| ⬜ | 31 | B10 | IR | **NEURAL shading — cooperative + long vectors (THE MOAT)** — a coop-vector op class in the IR: per-invocation small-MLP eval (matmul-accumulate + activation) → `SPV_NV_cooperative_vector` (Vulkan) · HLSL coop-vectors + SM-6.9 long vectors (DX12). **Differentiable by construction** — CKIR *is* the autodiff graph (v15/v16), so a neural material/BRDF + its gradient come from ONE IR; **no shipping engine does this.** Consumers: NTC decode · neural BRDF / radiance-cache. |
| ⬜ | 32 | C5 | DEV | **GPU-driven dispatch** — the recorder grows **indirect-count** → **device-generated commands** (`VK_EXT_device_generated_commands`, "biggest addition since ray tracing") → **WORK GRAPHS** (`VK_AMDX_shader_enqueue` + mesh nodes / D3D12 Work Graphs SM 6.8) so the GPU schedules its own work. |
| ⬜ | 33 | B11 | IR | **wave/subgroup + quad ops + work-graph node shaders** — subgroup intrinsics (`subgroupAdd`/broadcast/shuffle/ballot/prefix), **fixed-tree so reductions stay deterministic** · quad ops (SM 6.6) · **work-graph NODE shaders** (`SPV_AMDX_shader_enqueue` — the shader half of C5) · device-generated-command shaders. |
| ⬜ | 34 | D1 | IR | **IR-as-crdr + reflection** — serialize the KGraph + material graph + stage/pipeline metadata as a versioned crdr resource (replaces stored GLSL/HLSL) · reflection straight from the IR (bindings, set layouts, vertex layout, push-constants, workgroup size, entry points, spec-constants). |
| ⬜ | 35 | D2 | IR | **cook — IR → per-backend bytecode** (offline) — IR → GLSL→SPIR-V / HLSL→DXIL / MSL / WGSL / PTX at build time · SPIRV-Cross/naga-class validation · content-hash cook cache · parallel cook. |
| ⬜ | 36 | D3 | IR | **variant / permutation system** — feature-toggle → static specialization matrix · variant key + DEDUP · **on-demand compilation** (only what the scene needs, UE5-style) · permutation-reduction controls · telemetry. |
| ⬜ | 37 | D4 | IR | **runtime load + pipeline cache** — load cooked bytecode · `VkPipelineCache` / D3D12 PSO-cache · spec-const binding · async warmup · **zero runtime-compile in ship**. |
| ⬜ | 38 | D5 | IR | **hot-reload** — IR/graph/text edit → recook affected variants → ATOMIC pipeline swap. |
| | | | | ── **OFFLINE RENDER MODE** — gold-standard, AAAA, film-quality. A **SWITCHABLE MODE**, not a second renderer: the SAME CKIR integrators + B9/C3 AS tier, flipped from real-time (few-spp + denoise) to **offline** (driven to convergence, no time budget, full AOVs). Also the **GROUND-TRUTH reference** that certifies every real-time approximation (SVGF/ReSTIR/NRC = "matched accuracy" only vs a converged reference). Capstone of the CKIR rendering frontier — execute after the real-time visual frontier (B18/B19). ── |
| ⬜ | 39 | OFF-1 | DEV | **offline render MODE seam + convergence orchestrator** — the flip real-time↔offline: tiled/bucketed wavefront scheduling, **adaptive sampling** to a per-pixel variance/noise threshold, thousands of spp, deterministic per-tile seeding, checkpoint/resume, no time budget. Reuses the B9/C3 AS + every CKIR integrator; one mode flag, zero shader rewrites. |
| ⬜ | 40 | OFF-2 | IR | **bidirectional path tracing (BDPT — Veach 1997)** — camera + light subpaths with full all-connections MIS; the robust base for small emitters, hard indirect, and strongly-occluded transport the unidirectional PT can't resolve. |
| ⬜ | 41 | OFF-3 | IR | **VCM / UPBP (Georgiev 2012 · Křivánek 2014)** — vertex connection + merging: BDPT ∪ progressive photon mapping under one MIS, resolving **SDS caustics** (glass, water, gems) that pure PT/BDPT never converge. |
| ⬜ | 42 | OFF-4 | IR | **practical path guiding (Müller 2017) + product/RIS guiding** — online-learned spatio-directional **SD-tree** guiding distributions; order-of-magnitude faster convergence on difficult transport, no precompute pass. |
| ⬜ | 43 | OFF-5 | IR | **Metropolis light transport (PSSMLT — Kelemen 2002 · MMLT — Hachisuka 2014)** — mutation-based sampling for extreme low-probability paths (tight visibility, caustic networks) — the last-resort robust estimator. |
| ⬜ | 44 | OFF-6 | IR | **spectral rendering (hero-wavelength — Wilkie 2014) + reflectance→spectrum upsampling (Jakob-Hanika 2019)** — dispersion, thin-film, spectral MIS; physically-accurate colour, RGB textures lifted to smooth spectra. The Huang elliptical-fibre BCSDF feeds this directly. |
| ⬜ | 45 | OFF-7 | IR | **reference volumetrics + subsurface (spectral/decomposition tracking — Kutz 2017 · random-walk SSS — Chiang 2016)** — unbiased participating media + SSS; the ground-truth **milky fur / cloud / skin** the real-time (B15 clouds, B18 fur, SSS) approximations are measured against. |
| ⬜ | 46 | OFF-8 | IR | **robust estimators + adaptive sampling (outlier/firefly rejection · Zwicker 2015 adaptive-sampling survey)** — variance-driven per-pixel spp, cascaded outlier rejection, unbiased accumulation; optional final-frame denoise (OFF by default — a true reference is un-denoised). |
| ⬜ | 47 | OFF-9 | DEV | **production film output — deep/multi-channel EXR AOVs · cryptomatte · light-path expressions (LPE)** — via our OWN HDR/EXR codec: arbitrary AOVs (albedo / normal / depth / position / motion), cryptomatte IDs, LPE-filtered passes (diffuse/spec/caustic/SSS) — the deliverable a compositor ingests. |
| → | | | | **EXIT the detour → resume Phase 3.1.6 v17 hesap-GPU compute** (then physics → rendering → UI → editor). |

**Pairings to remember** (why the order interleaves IR + DEV): raster **B3-c/d/e ↔ C1(done)/C4** · ray tracing **B9 ↔ C3**
· neural **B10 ↔ C6** · work-graphs **B11 ↔ C5**. Each IR (shader) slice needs its DEV (device) half to be *exercisable*,
so they land adjacent.

**One caveat on the done rows:** B0's dedicated multi-config DoD (`-IncludeRelease` + clang-cl + gcc for `crd-kir`) was
folded into the D-008-close full sweep (win-debug/asan/shipping/tidy all green 2026-07-11); the gcc leg stays env-blocked
(WSL lacks spirv-reflect), same as the rest of the project.

## Remaining-slice frontier detail (the contracts we execute against)

The table one-liners are the working contract; the capability→slice mapping below is the frontier bar each is measured
against, so we build the cutting-edge version by construction, not aspiration.

### IR / shader-language frontier (the B-slices)

| capability | real extension / status | slice |
|---|---|---|
| Cooperative vectors (neural inference in shaders) | `VK_NV_cooperative_vector` · HLSL coop-vectors SM 6.9 retail | **B10** |
| HLSL long vectors `vector<T,5..1024>` | SM 6.9 retail | **B10** |
| Neural texture compression (NTC) | RTXNS / Intel coop-vector demo | **B10** (consumer) |
| Shader Execution Reordering (SER) | `VK_EXT_ray_tracing_invocation_reorder` · DXR 1.2 (20–100% path-trace) | **B9** |
| Opacity micromaps | `VK_EXT_opacity_micromap` · DXR 1.2 | **B9** (shader) + **C3** (build) |
| Cluster AS (RTX Mega Geometry) · cross-vendor DGF | `VK_NV_cluster_acceleration_structure` (driver ≥ 572.16) · AMD Dense Geometry Format | **B4** (emit) + **C3** (build) |
| **Ray-traced curves & spheres (hair/fur/foliage)** | `VK_NV_ray_tracing_linear_swept_spheres` (LSS) · DXR curve primitives | **B9** (hit reads) + **C3** (AS build) |
| **Hardware tessellation + displacement (portable, non-mesh path)** | `TessControl`/`TessEval` stages · patch/domain · `TessCoord` | **B4-tess** |
| Variable rate shading | `VK_KHR_fragment_shading_rate` | **B1** / **B4** |
| Fragment interlock / ROV · conservative raster | `VK_EXT_fragment_shader_interlock` · `VK_EXT_conservative_rasterization` | **B1** |
| Sampler feedback (virtual texturing) | D3D12 `SV_FeedbackTexture*` | **B2** |
| Subgroup / wave / quad ops | GLSL `subgroup*` · HLSL `Wave*` · CUDA warp · SM 6.6 quad | **B11** |
| Work-graph node shaders | `SPV_AMDX_shader_enqueue` · D3D12 Work Graphs | **B11** (shader) + **C5** (dispatch) |

### Device / dispatch frontier (the C-slices)

| capability | real extension / status | slice |
|---|---|---|
| Shader objects (kill PSO permutation explosion) | `VK_EXT_shader_object` | **C1** ✅ |
| Graphics pipeline library (anti-hitch fallback) | `VK_EXT_graphics_pipeline_library` | **C1/C4** |
| Dynamic rendering (no `VkRenderPass`) | Vulkan 1.3 core, mandated 1.4 | **C1** ✅ |
| Bindless / descriptor buffers + heaps | descriptor indexing · `VK_EXT_descriptor_buffer`→`_heap` | **C1/C2** ✅ |
| Device-generated commands (= D3D12 ExecuteIndirect) | `VK_EXT_device_generated_commands` (Vulkan 1.3.296) | **C5** |
| Work graphs (GPU self-scheduling) + mesh nodes | `VK_AMDX_shader_enqueue` · D3D12 Work Graphs SM 6.8 | **C5** |
| SER pipeline enable · opacity-micromap build · cluster AS | `VK_EXT_ray_tracing_invocation_reorder` · `VK_EXT_opacity_micromap` · `VK_NV_cluster_acceleration_structure` | **C3** |
| Ray-traced curve/sphere AS build (hair/fur) · cross-vendor cluster geom | `VK_NV_ray_tracing_linear_swept_spheres` (LSS) · AMD DGF | **C3** |
| Cooperative vectors (neural device enable) | `VK_NV_cooperative_vector` · DX12 coop-vectors SM 6.9 | **C6** |

## B8 — the frontier lighting + shadow system (gold standard)

> **Scope (user 2026-07-12): "full lit gold standard shadows and cascaded shadow maps… everything gold standard."** B8 is
> now the frontier RASTER renderer, not a seam-validation. It **amends ADR-0102 D7** (the "defer the full pipelines + light
> culling to the post-hesap rendering phase" boundary is superseded by the locked *full-visual-frontier-before-hesap* order —
> struck in place in ADR-0102). Two layers, both gold-standard, built together:
> **(1) the CKIR LIGHTING/SHADOW/SKINNING LIBRARY** — shader-side math authored in the IR (`ckir_lighting.hpp` + shadow/skin
> helpers), each bit-exact vs its published reference on the CPU oracle and observable pixel-identical on both backends, the
> exact B5–B7 methodology; **(2) the RHI RENDERER INTEGRATION** — the frame-graph passes (`FrameGraph`/`IRenderPath`, the
> reserved `PassType::Shadow`), the `MaterialTemplate`/`variant_for_pass` cook seam fed by `lower_entry` (B7), and the
> descriptor-set frequency model (ADR-0102 D2: set 0 frame · set 1 pass/lights/shadows · set 2 material · set 3 object/skin).

**The frontier technique stack (the gold-standard bar each sub-slice is measured against).** STATUS AUDIT 2026-07-15: **every
row's portable SHADER MATH is implemented in CKIR, bit-exact on the CPU oracle + pixel-identical on Vulkan AND DX12** (the tests in
the ✅ status column pass). Where a row says "→ render phase" that is the DEVICE/RENDER-PIPELINE plumbing (render-to-texture,
structured buffers, float/2DArray texture storage, cull-compute dispatch, VSM page-table + GPU-feedback allocation) — deferred
UNIFORMLY for every technique to the post-detour render phase per the detour charter (harden the IR + shader math now; wire the
real GPU pipeline later), NOT a per-technique shortcut. **No before-B16 shader technique is missing.** (Correcting a 2026-07-15
mis-audit: VSM **is** built — `vsm_clipmap_level`+`vsm_page_coord`, `[ssshadow]` bit-exact both backends.)

| capability | gold-standard technique | reference | sub-slice | status |
|---|---|---|---|---|
| Specular BRDF | GGX/Trowbridge-Reitz `D` · height-correlated Smith `V` · Schlick `F` | Filament / Frostbite (Karis, Lagarde) | **B8-a** | ✅ math `[brdf]` |
| Energy conservation | multiscatter GGX energy compensation | Kulla-Conty; Fdez-Agüera | **B8-a** | ✅ math (+DFG-bias-floor correctness fix) |
| Diffuse | Lambert + Burley/Disney (retro-reflection) | Disney BRDF (Burley 2012) | **B8-a** | ✅ math `[brdf]` |
| Layered material | anisotropic GGX · clearcoat (IOR 1.5) · sheen `D_Charlie`/`V_Neubelt` · thin-film · transmission · subsurface | Filament; OpenPBR 1.1 (the B5 slab) | **B8-b** | ✅ math `[lobes]`/`[thinfilm]`/`[transmission]`; scene-colour sample → render phase |
| Punctual lights | directional · point (inverse-square + smooth window) · spot (cone) | Frostbite (Lagarde-de Rousiers 2014) | **B8-c** | ✅ math `[lights]`; light-array SSBO → render phase |
| **Area lights** | **Linearly Transformed Cosines** (rect/disk/sphere/tube) + LTC LUT; anisotropic-GGX LTC | Heitz 2016; Heitz-Dupuy 2022 | **B8-d** | ✅ math `[area]` (rect/tube/disk/sphere+aniso); LTC LUT texture → render phase |
| Image-based lighting | split-sum: prefiltered env (roughness mips) + DFG LUT + irradiance SH L2 | Karis 2013 (UE4 split-sum) | **B8-e** | ✅ math `[ibl]` (runtime+generation); env/DFG/SH assets → render phase |
| Shadow bias | normal-offset + slope-scaled + receiver-plane depth bias | MJP "A Sampling of Shadow Techniques" | **B8-f** | ✅ math `[shadow]` |
| Soft shadows | **PCF** (rotated Poisson/Gather) → **PCSS** (blocker search + penumbra, contact-hardening) | Fernando 2005 (PCSS); Nvidia | **B8-g** | ✅ math `[softshadow]` |
| Filterable shadows | **EVSM** · **Moment Shadow Maps** (4-moment, prefilter/MSAA) | Lauritzen (EVSM); Peters-Klein 2015 (MSM) | **B8-g** | ✅ math `[softshadow]` (resolve); float moment-map storage → render phase |
| **Cascaded shadow maps** | **stable CSM** (Valient sphere-bound + texel-snap) + **SDSM** tight fit + smooth cascade blend + per-cascade PCF/PCSS | Valient; Lauritzen 2011 (SDSM); MS "CSM" | **B8-h** | ✅ split/select/Valient-snap/blend `[csm]`; SDSM = feed the split fn the `ckir_reduce` depth-min/max range (reduction pass) + 2DArray atlas → render phase |
| Contact / fine shadows | screen-space ray-marched contact shadows | Bavoil; UE contact shadows | **B8-i** | ✅ math `[ssshadow]`; depth-buffer sample → render phase |
| Raster shadow ceiling | **Virtual Shadow Maps** (page-table/clipmap atlas, GPU-feedback allocation) | UE5 VSM | **B8-i** | ✅ addressing math `[ssshadow]` (`vsm_clipmap_level`+`vsm_page_coord`); page-table + feedback alloc → render phase |
| Skinning | linear-blend + **dual-quaternion** (no candy-wrapper) + optional compute pre-skin | Kavan 2007 (DQS) | **B8-j** | ✅ math `[skin]` (LBS+DQS); bone-palette SSBO + pre-skin dispatch → render phase |
| Lighting paths | **Forward+** (clustered froxel cull) + **Deferred** (G-buffer) HYBRID | ADR-0102 D3 (deferred opaque + forward transparent) | **B8-l** | ✅ math `[render]` (cluster/cull/G-buffer-decode); cull-compute dispatch + G-buffer RT → render phase |

**Ray-traced shadows / RTGI are NOT B8** — they are the highest-quality path but belong to **B9 + C3** (RT pipelines); B8 is
the frontier *raster* renderer. The lighting library it builds is reused by the RT path (one BRDF, two visibility sources).
**Neural** materials/BRDF are **B10**. B8's job is the complete, correct, gold-standard raster lighting + shadow system, with
the CKIR lighting library as the single shared surface→radiance evaluator both raster and (later) RT call.

## Stage model — complete from day one (B3-a′ ✅)

`ShaderStage` = the 14 SPIR-V execution models: `Compute · Vertex · TessControl · TessEval · Geometry · Fragment · Task ·
Mesh · RayGen · Intersection · AnyHit · ClosestHit · Miss · Callable`. Mesh/task is the amplification path; **geometry is
supported-but-discouraged** (single-thread bottleneck). **Tessellation (`TessControl`/`TessEval`) is a REAL path, not
emulated** (B4-tess): it is the portable displacement/terrain path for platforms without mesh shaders (mobile/WebGPU/older
HW), and its builtins (`TessCoord`, `PatchVertexCount`, `InvocationId`) are already in the stage model. A backend that cannot
yet emit a stage **refuses loudly** — it never silently falls back to compute. (Declare the whole domain; refuse the
unimplemented part loudly.) *Candidate (not scheduled): single-pass stereo / multiview (`VK_KHR_multiview` + a `ViewIndex`
builtin) — the frontier replacement for GS-based stereo; parked until VR/XR is a committed target.*

## Escape hatch (ADR-0101 §4) — preserved and boxed

Hand-written per-backend kernels stay legal (geometry-bvh-gpu's LBVH `.comp`, imgui/draw overlay GLSL, third-party HLSL):
they cook to blobs and load through `create_program(cooked)` — explicitly non-portable, never re-entering the engine as
source. The asset cooker keeps its GLSL→SPIR-V path; it just calls the *Vulkan backend's* compiler, not `crd-shader`.

## Front-ends (node editor + text DSL) — DEFERRED to the editor phase (NOT part of this detour)

> This is the old "D-007 Phase C" — renamed here so it stops colliding with the device C-slices above. It is **not** on the
> execution table; it is built at editor time (roadmap step 7). Authoring during this detour + the rendering phase is via
> the existing **C++ builders**.

Only the **round-trip design invariant is locked now** (cheap insurance that the IR is provably editable): every
KOp/builder/stage-construct maps to a typed nodedef, and graph→IR→graph is loss-less (validated by a decompile test). The
node-editor UI, the Slang-inspired text DSL, the shared semantic/LSP layer, and live-preview authoring all land in the
editor phase.

## Exit criteria

The detour exits — and the main roadmap resumes at **Phase 3.1.6 v17 hesap-GPU compute** — when the master table's rows
1–29 are ✅: the IR can express (and every validatable backend can run) ML/AI, FFT, simulation, skinning, particles,
lighting, PBR + stylized/NPR rendering, mesh shading, ray tracing, neural shading, and GPU-driven work — and the cook path
makes it shippable with zero runtime compile. Then: hesap-GPU → eylem/physics → rendering → UI → editor → node editor.
