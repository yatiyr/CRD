# 2026-06-10 — hesap v7-q CLOSE: the global/metaheuristic family (same session, part 15)

**Phase:** 3.1.6 `crd-hesap` · v7 optimisation (`crd-hesap-opt` + `crd-hesap-stats`, ADR-0090)
**Slice:** v7-q — de-slipped per the user's p/q/r decision. CLOSED.

---

## The Philox-normal prerequisite (the v12 stats cluster grows)

`crd-hesap-stats/normal.hpp` — `NormalSampler`: Box-Muller over PhiloxRng with the pair cache as part of
the sampler state (the consumption sequence — hence every sample — is a pure function of (seed, stream,
draw index)). u ∈ (0,1] via 1 − next_f64() so log() never sees zero. Gates: four-moment check on 200K
samples (mean/var/skew/kurtosis within sampling bands) + bit-identity + stream separation. Stats suite
230660/7 green.

## The family (all value-only; all bit-identical run-to-run by the Philox stream discipline)

- **`cmaes.hpp` — CMA-ES, faithful to Hansen's tutorial** (arXiv:1604.00772, the pseudocode pycma
  implements): the (μ/μ_w, λ) scheme with the standard defaults, CSA with the hσ stall guard, rank-one +
  rank-μ covariance updates, eigendecomposition via `dense::eig_sym` every generation (Hansen's lazy update
  is a CPU-cost trick — named scope), insertion-sort selection with index tie-breaks.
- **`global_search.hpp`** — differential evolution (scipy best/1/bin semantics: dithered F, binomial CR,
  clip, greedy, the f-std convergence test) · PSO (Clerc constriction) · classical simulated annealing
  (dual_annealing's Tsallis machinery named NOT shipped) · basin-hopping (scipy semantics incl.
  **AdaptiveStepsize** — added after the fixed default honestly failed the double-well gate) · multi-start.

## Gates (31 asserts / 8 cases; opt suite 3619/139; 4 configs + guards green)

CMA-ES: sphere-8 collapses to f < 1e-10; **Rosenbrock-5 from x = −2·1 to ‖x − 1‖ < 1e-4** (the rugged-valley
gate). DE: **the Rastrigin-4 global minimum through ~10⁴ local minima** (f < 1e-4, x within 1e-2 of the
origin). PSO sphere; SA + basin-hopping escape the wrong well of a tilted double-well (basin-hopping needed
step = 2.0 for the 2.5-wide barrier — exactly the scipy stepsize knob); multi-start Rastrigin-2 baseline.
Determinism: CMA-ES and DE bit-identical run-twice (the whole evolution path through Philox + eig_sym).

## Next

v7-r MIP (B&B over the v7-l bounded simplex + Gomory) → **v7-z** (CLI + the FULL gold scoreboard now incl.
pycma/scipy-global rows + system doc + ADR + 18-config sweep + cluster close).
