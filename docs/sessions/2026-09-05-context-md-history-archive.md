# 2026-09-05 — context.md history archive (CEIR-25/26/27/28-era live-state narration)

<!-- doc-role: evidence -->
> Dated evidence; counts, results and Next paragraphs are historical. Current work: [ROADMAP](../ROADMAP.md); current rules: [AGENTS](../../AGENTS.md).

> Pruned from `context.md` on 2026-09-05 at the CEIR-28 band close. The file had grown to 344 lines plus a 90 KB single-line `Current focus` blob (10 ticks of per-tick appends). This is the VERBATIM pre-prune narration: the CEIR-25→28 `Current focus` blob (line 10), the superseded CEIR-18/19 `Prior focus` stack, and the stale `Active state` / `Recently landed` / `Open questions` snapshots (which still described CEIR-1z / RAH-1a.2 as current). Authoritative per-slice detail lives in `docs/detours/D-007-ceir-tracker.md`; band records in the `docs/sessions/*-band-close.md` logs. Mirrors the 2026-08-07 archive precedent. Nothing here is live — do not edit; it is a snapshot. All filed follow-ups referenced here (19b-F1/F2, VUID-08608 bind-hygiene, the compute READS-FIRST latent defect, DX12-geometry-Y, the multi-instance cull WW race, froxel-grid config-flexibility) were verified present in the tracker before this prune.

---

## Current focus — **[2026-09-05 ▶ LIVE: CEIR-28 (autotune / config-cache / deterministic-locked-mode) ✅✅✅ BAND CLOSED · CEIR-27 (optimization-as-authored-asset) ✅ · NEXT band CEIR-29 (native graph providers) · full per-slice detail lives in docs/detours/D-007-ceir-tracker.md; the narration that follows is HISTORICAL (CEIR-25/26 era, kept for context): CEIR-25 (`ceir.autodiff`) ✅✅✅ BAND CLOSED · CEIR-26 (optimizer) BAND-OPEN · 26a (DCE) ✅✅✅ CLOSED 2026-09-04 [`dce_run` registered∧Pure∧no-uses fixpoint; the 11/5/4→9/4/3 prune + the LIVENESS-CONTRACT pin (grads are readback-by-Value, [[feedback_plan_output_by_traversal_is_not_ssa_liveness_pin_readback_before_any_pass]]) + bit-exact-vs-RAW device differential on Vk+DX12+llvmpipe] · 26b (canonicalize/fold) BAND-OPEN · 26b-1 (the `greedy_rewrite` DRIVER — the RESERVED rewrite.hpp fixpoint driver: COLLECT-PER-ROUND [not a worklist — a fn-ptr `rewrite` can't return its new op], the two pattern-author corrections [insert_before-before-RAUW; leave-dead-for-DCE], INITIAL-fixed cap→FATAL on non-monotone; `rewrite_driver.hpp` + 4 device-free gates incl. the load-bearing is_erased-skip COUNT gate) ✅ CLOSED 2026-09-04 · 26b-2a (`canonicalize_pass` + the REAL `tensor.reshape∘reshape` fold — bound pattern set not a registry; excludes the identity case [26b-3]; composes with DCE; plan-stage-neutral [reshape=alias]; device-free gate + identity-not-category T_out pin + intern-contract pin) ✅ CLOSED 2026-09-04 · 26b-2b (the Vk bit-exact-vs-RAW device differential — §137 gemm→reshape[2,32]→reshape[64]→fft→reduce, RAW → canonicalize+dce → OPT, `opt_s==raw_s` bit-exact + correct vs ref on real RTX #4747; witness proves alias-WIRING not kernel correctness [26e fusion regains 26a teeth]; alias-order gate) ✅ CLOSED 2026-09-04 · 26b-2c (the DX12 leg — bit-exact reshape-fold differential #4826) ✅ CLOSED → ✅✅ 26b-2 COMPLETE (canonicalize `tensor.reshape∘reshape` fold: device-free 2a + Vk 2b #4747 + DX12 2c #4826; llvmpipe deferred to 26z) · 26b-3 (identity-reshape elimination `reshape(x:T):T→x` — RAUW-to-operand; exclusion REMOVED so the pair fully collapses a net-identity T→U→T→x; pre-DCE fixpoint gate + plan Alias 1→0; no device leg by design) ✅ CLOSED → ✅✅✅ **26b (canonicalize/fold) COMPLETE** (26b-1 driver + 26b-2 reshape∘reshape 3-legs + 26b-3 identity; no 26b-4 — const-fold has no live consumer) · 26c (CSE — common-subexpression elimination) BAND-OPEN · 26c-1 (the CSE PASS + device-free unit gate — `cse.hpp` STANDALONE [dce_run mold, NOT the greedy driver: the per-block `seen` set is state a fn-ptr can't carry]; eligibility = leaf∧registered∧Pure∧has-results∧NOT-positively-nondeterministic [§27: a positive Nondeterministic/ExternalNondeterminism claim disqualifies; Unspecified rides device-bit-exact-by-construction — EMPTY≠UNKNOWN, gated by a synthetic `cs.nd`]; structural equality = kind+operand-Value-POINTER-equality [deep=WRONG]+result-types+order-independent attrs; NEVER add a RAUW'd dup to `seen` [3rd dup finds the survivor, not the tombstone]; same-block dominance scope; 5 gates #543-547: eliminate+rewire, three-collapse, cascade, structural negatives, eligibility negatives) ✅ CLOSED 2026-09-04 · 26c-2a (the CSE PLAN leg — two identical `gemm(A,B,C)` feeding two DISTINCT reduces [sum vs max, NOT merged]: RAW 2 Gemm+2 Reduce=4 stages → cse_run → 1 Gemm+2 Reduce=3 stages, the FIRST CSE-observable-in-the-plan drop; advisor-strengthened to TOPOLOGY — RAW two Gemms write distinct buffers [not pre-deduped], OPT both reduces read the ONE surviving gemm output + write distinct results; ⛔ FINDING: planner marks ONE Output [26a-2 g0/g1 precedent], so gate distinct reduce-output buffers not n_out; #839) ✅ CLOSED 2026-09-04 · 26c-2b (the Vk device BIT-EXACT leg — a REMOVED DISPATCH, the 26a witness at full teeth) ✅ CLOSED 2026-09-04 + ⛔⛔⛔ FOUND & FIXED a real CSE correctness bug: `opt_max==opt_sum==37`, `o_ga==o_gb==0` → CSE had MERGED the three identical `resource.declare` inputs (registered Pure) into ONE buffer → gemm computed A@A. resource.declare is an EXTERNAL SOURCE (value keyed by identity, not operands) — Pure but NOT referentially transparent (the MLIR alloc-is-not-CSE-able rule). FIX = declare `traits=[]` + `Allocate` effect (non-Pure), regen; blast radius clean (all 501 ceir green, 12b/12c/12d/DCE/mode-legality untouched); CSE regression gate + scar [[feedback_cse_must_not_merge_external_source_ops_declare_is_alloc_not_pure]]. The device leg found what 26c-2a's plan gate could not (a merged-declare corpus plans right, data corrupt). GREEN: 26c-2b Vk RTX (69) + 501 ceir + #839 + [cse] 6/6 + opgen-drift + tidy. 26c-2c (DX12 mirror — bit-exact duplicate-gemm removal #4836, 71 assertions, GREEN first try; the declare fix rode the shared lib) ✅ CLOSED → ✅✅ 26c-2 COMPLETE → ✅✅✅ **CEIR-26c (CSE) COMPLETE** (26c-1 pass + 5 unit gates; 26c-2 device legs Vk #4756 + DX12 #4836 bit-exact on a REMOVED DISPATCH; the biggest find = resource.declare-was-Pure → Allocate-effect fix, blast radius re-verified green across 501 ceir + full ceir-gpu + Vk/DX12 device suites). · 26d-0 (the CONSUMER CENSUS — §73 specialize/inline = TWO passes, census each per 26b-4) ✅ CLOSED 2026-09-04: **(a) INLINE = NO live consumer → DROPPED** (func.call = 0 in every asset AND 0 constructed across engine/ceir-gpu + all 3 gpu-test dirs; plan_tensor_pipeline walks the first func.func's ONE block, UnsupportedOp on a call; the plan.cpp:784 call graph = the general CPU-eval path, not the optimizer domain); **(b) SPECIALIZE = HAS a live consumer → 26d becomes a SHAPE-SPECIALIZATION pass** (advisor widened my too-narrow const-operand census to SHAPE: arith.const operands are grid/draw-count leaves resolved at lower time [no const-fold consumer], BUT the baked-kernel BakedKernelShapeUnsupported rejects ARE live — relu.ckir bakes local_size=32 [expand_ml.cpp:113], transpose/softmax bake fixed dims [expand_ml.cpp:166], grad relu/relu_vjp local_size=32 [grad.cpp:406] → an ml.mlp width≠32 / attention dims≠(2,3,4) is REJECTED TODAY). · 26d-1 (MECHANISM advisor gate) ✅ CLOSED 2026-09-04: **DECISION = M2 = COOK-TIME BINDING OF THE AUTHORED ASSET** (NOT a new emitter feature — spec-const is already cook-time bake and entry.local_size is the same kind of field: load→patch→emit; CONFIRMED ckir_glsl.hpp:724 reads entry.local_size at emit). TWO knobs: (i) entry.local_size ← write-operand numel (relu/relu_vjp, body untouched, single-WG scalar oracle preserved); (ii) body spec-consts ← dims via set_spec_const (softmax Sq/Sk, the 18p mechanism). REJECTED M1 (multi-WG+guard violates asset-drives-grid + stresses scalar oracle) + M3-for-relu (synth is for dialect ops; relu is the 23c authored crown activation — deleting it regresses everything-authorable). 5 build blockers locked: (1) opt-in ASSET-DECLARED via sentinel local_size=[0,1,1] + typed cook error if unbound-0 reaches emit; (2) transpose-in-attention → check if expand_ml can emit tensor.transpose→synth_transpose (25, zero new mechanism); (3) numel>device maxComputeWorkGroupSize[0] → NEW typed reject LocalSizeExceedsLimit at resolver, REMOVE the M·hidden==32 pre-check (never level-down); (4) verify eval_cpu_kernel lane count reads entry.local_size (else 2nd patch site); (5) patch site = the VizDispatch resolver — READ FIRST. HONEST: this is cook-time kernel-shape specialization at the CEIR→CKIR boundary, NOT a CEIR IR pass (shape is already in operand types); lives in ceir-gpu. · 26d-2a (26d-2 ORIENTATION — advisor blockers 4+5 resolved, seam/helper/call-sites pinned; no code) ✅ CLOSED 2026-09-04: blocker 4 — eval_cpu_kernel takes local_size as an EXPLICIT param (not entry.local_size) ⇒ sentinel invisible to eval readers, device-gate oracle is a direct float-MLP ⇒ no 2nd patch site; blocker 5 — the seam is BETWEEN ckir_read and emit in each caller's resolve_stage VizDispatch branch ⇒ a SHARED ceir-gpu helper bind_authored_kernel_shape(ctx, stage, ve, max_local_size)→{None,LocalSizeUnbound,LocalSizeExceedsLimit} patches ve.local_size[0]=trailing-write-numel when sentinel(0), capped at device maxComputeWorkGroupSize[0]. Call-sites (EMIT breaks, eval safe): 2 device resolvers (vulkan:219 + dx12:211) + test_ckir_viz.cpp:189 (CHECK eval-literal-32-vs-ke.local_size in 26d-2b). · 26d-2b (the ATOMIC relu cook-bind mechanism) ✅ CLOSED 2026-09-04: relu.ckir local_size [32,1,1]→[0,1,1] (sentinel + header note) + `bind_authored_local_size(u32&,u64 numel,u32 cap)→{None,LocalSizeUnbound,LocalSizeExceedsLimit}` + kMaxAuthoredLocalSize=1024 (pure policy, in tensor_pipeline.hpp — NO new file, no reconfigure) + BOTH device resolvers wired (between ckir_read and emit; trailing-write operand = 3+nbind-n_out) + test_ckir_viz.cpp binds 32 before its emit smoke (eval passes len explicitly). BUILD GREEN win-debug (ceir-gpu lib + vulkan + dx12 + kir tests). 32-WIDTH REGRESSION GREEN on REAL hw (ctest -R relu, 19/19): 23c-c/d crown + 24b-4 mlp + 25c-1b/25c-2 relu_vjp/backward on RTX+DX12 — cook-bind yields local_size=32 from numel = old baked value, gates re-green at oracle-TOLERANCE (not bit-exact; 26d-2c is the non-32 correctness proof). Advisor-hardened this slice: unbound-sentinel-at-emit → emit returns false (UnresolvedKernel), on BOTH compute emitters (glsl+hlsl); advisor-verified relu/relu_vjp access shapes (out last, wop=3+nbind-n_out correct). · 26d-2c (the PROVING gate on Vulkan — the reject FLIPS to a run) ✅ CLOSED 2026-09-04: REMOVED expand_ml.cpp:113-122 M·hidden==32 pre-check (rank-2 kept; attention 166-171 stays till 26d-3) + SUPERSEDED-IN-PLACE the 24z MLP-non-32 reject test (now CHECK None+expanded==1) + POSITIVE DEVICE GATE #4750 (non-32 ml.mlp widths 16[h1=64]+24[h1=96] run device-resident on real Vulkan == CPU float MLP oracle, relu cook-bound; NO "raw" for a rejected program) + REPLACEMENT TEETH: device-free pure-policy unit #818 (bind_authored_local_size None/Unbound/ExceedsLimit/boundary/non-sentinel) + a device oversize negative in #4750 (h1=1200>1024 ⇒ UnresolvedKernel ⇒ CHECK_FALSE). GREEN: #818 + #4750 real Vk + regression (expand 17/17, 24b 10/10, relu 20/20 Vk+DX12) + 4 files tidy-clean. · 26d-2d (the DX12 MIRROR — closes 26d-2) ✅ CLOSED 2026-09-04 → ✅✅ **26d-2 (relu cook-time shape-specialization) COMPLETE**: the non-32 ml.mlp runs device-resident on D3D12 too (#4833, real RTX, widths 16+24 == oracle + oversize→UnresolvedKernel discriminating negative), GREEN FIRST TRY (mechanism+teeth rode the shared lib from 26d-2b/2c — both-backends mandate discharged by a device-gate mirror). 26d-2 = 2a(orientation)+2b(mechanism)+2c(Vk proving+pre-check retirement+teeth)+2d(DX12 mirror); the 24z BakedKernelShapeUnsupported MLP reject RETIRED + PROVEN as a run on BOTH backends; the locked-checklist scar DISCHARGED for the MLP path (relu_vjp/grad = 26d-4). · 26d-3a (ATTENTION transpose census + transpose→synth refactor — zero new mechanism) ✅ CLOSED 2026-09-04: expand_attention emits Kᵀ via tensor.transpose (mk_transpose2d = tensor::build_transpose perm="1,0" → StageKind::Transpose → synth_transpose, proven 25b-4a) instead of the baked @transpose dispatch + removed the dead @transpose entry from both resolvers; pre-check KEPT (softmax still bakes Sq/Sk). GREEN: attention 13/13 (24b-4 Vk #4778 + DX12 #4831 regress — SDPA oracle proves synth Kᵀ correct at 2,3,4) + expand 17/17 (Sk=4 reject intact) + tidy. transpose.ckir RETIRED at 26d-3c (asset + reading gate DELETED; synth_transpose is the path). · 26d-3b-census + ADVISOR DECISION ✅ 2026-09-04: softmax does NOT transfer from relu (Sk is a HARDCODED UNROLL not a Const; local_size=Sq one-lane-per-row not Sq·Sk; no shared array). DECISION = RE-AUTHOR softmax.ckir as a LOOP kernel (stays authored): knob (ii) Sk=spec-const (set_spec_const after ckir_read) + local_size←Sq=dim0 (per-kernel extent in the resolver switch, NOT bind_authored_local_size's numel — "extent" contract) + body = 3 uniform For c in [0,Sk) passes (recompute exp pass 3, no RAW reload). SUB-SLICES: **26d-3b-1** (reads+advisor, NO edit — 3 blockers: how ckir For carries a scalar [spmv_csr.ckir], spec-const-as-For-bound in GLSL+HLSL+eval, TOML spec-const encoding) → 26d-3b-2 (builder→ckir_write→eval-verify (2,3)+(3,5)→commit→update 24b-2 eval gate→reading gate→DELETE builder) → 26d-3b-3 (both resolvers set_spec_const Sk + local_size←dim0 → 24b-4 attention regression Vk+DX12 oracle-tol) → 26d-3c (remove 168-171 pre-check + generic attention gate Sq=3,Sk=5,D=4 Vk+DX12 == SDPA oracle + supersede Sk=4 test + retire transpose.ckir). · 26d-3b-1 (the 3 blocking reads + design reconcile) ✅ 2026-09-04: kernel-tier For carries via BUFFER RMW not register (KOp::LoopAcc is graph-tier only, absent from ckir_kernel_eval); spec-const-as-For-bound works GLSL+HLSL+eval; TOML spec-const = axes=2147483648+iidx+cval. ⛔ RESOLVED (2 greps): NO register-carry stmt exists, but SHARED is in the kernel eval ⇒ USE groupshared for m/denom (NO scratch buffer, NO ABI change). LOCKED softmax builder: one-lane-per-row local_size=Sq(cook-bound dim0), 2 SharedDecl (m_sh,d_sh) fixed-cap, Sk=spec-const, 3 uniform For c in [0,Sk) passes (max→sum→div, recompute exp pass 3, -FLT_MAX init cval=0xC7EFFFFFE0000000, no ForBreakIf). · 26d-3b-2 (loop-based softmax BUILDER + eval-verify — the hard part) ✅ 2026-09-04: build_softmax in test_ckir_asset.cpp (3 buffers + 2 shared_decl m_sh/d_sh cap=1024 + spec_constant Sk + LocalInvocationIndex row + 3 uniform For passes max/sum/div, exp recomputed, -FLT_MAX init) + [.emit-softmax] generator. EVAL-VERIFY GREEN #4096 device-free: eval_cpu_kernel == CPU scaled rowwise softmax at (2,3) AND (3,5) within 1e-6 — groupshared carry + spec-const For-bound + 3-pass all work. tidy-clean. · 26d-3b-2b/3 (COMMIT loop softmax + RESOLVERS + device find) ✅ 2026-09-04: generated softmax.ckir (loop version: Materialize+2 SharedDecl+spec-const Sk+3 For passes) + 24b-2 reading gate evals committed asset at (2,3)+(3,5) + both resolvers softmax special-cased (local_size←dim0(probs)=Sq + set_spec_const(0,dim1=Sk)). ⛔⛔ DEVICE FIND: first device run FAILED — emitted GLSL declared base=r·Sk inside pass-1's For loop, out-of-scope for passes 2/3 (glslang reject → UnresolvedKernel); FIX = stmt_materialize(base) at body scope (temp-scope scar) + resolver must set_spec_const explicitly (device ignores the scalar-eval default). GREEN: eval-verify (2,3)+(3,5) + 24b-2 + kir asset 78/78 (softmax round-trip) + 24b-4 attention+mlp Vk+DX12 + attention 13/13 + tidy. Builder KEPT as regen source (uncommitted-delete scar overrides mold's delete; delete at user-commit). 26d-3c (ATTENTION capstone) ✅ 2026-09-04: removed the expand_ml attention pre-check (proven live FIRST — the generic gate failed 4==0 before removal) + GENERIC attention gate Sq=3,Sk=5,D=4 GREEN on real Vk+DX12 (12 assns each) == CPU SDPA oracle + superseded the Sk=4 reject→None+expanded==1 + RETIRED transpose.ckir (asset + its 24b-1 reading gate + stale comments; synth_transpose is the live path — 24b-4 attention PASSES with the asset deleted, Vk 122/6 & DX12 71/4) + removed the dead dim_count helper + BakedKernelShapeUnsupported is now a ZOMBIE (0 return sites repo-wide; enum retired at 26d-4 after a switch audit). ✅✅ **26d-3 (attention path) COMPLETE** (3a transpose→synth + 3b softmax→spec-const loop + 3c capstone). 26d-4a (orientation + advisor plan-lock, NO code) ✅ 2026-09-04: relu_vjp.ckir is an EXACT relu mirror (elementwise, local_size=32 → the 26d-2b sentinel transfers directly); grad.cpp:405-417 is a SEPARATE baked guard (GradError::MlpBakedShapeUnsupported, M·hidden==32); TWO zombie enums to retire (MlExpandError::BakedKernelShapeUnsupported already zombie + GradError::MlpBakedShapeUnsupported after 4b); ⛔ the n_out trap: the .ckir ENTRY n_out=0 ≠ the PlanStage st.n_out (=1) the resolver keys on. Sub-split LOCKED 4b→4c→4d: **4b** = relu_vjp.ckir sentinel + prose header + REMOVE grad.cpp:405-417 + supersede test_grad.cpp:494-514 (→None+backward-vocab-emitted) + a non-32 (h1=64) vjp DEVICE gate Vk+DX12 == vjp oracle (teeth-first; verify binds[] write-last + keep the 25c-2 32-width regression); **4c** = retire BOTH zombie enums (grep `default:` on both, READ hesap-autodiff/nn_reverse.hpp = 3rd consumer, FULL device-free WSL gcc sweep) — closes §73 census 3→0; **4d** = typed softmax cap (sentinel-ize softmax.ckir + route the resolver softmax branch via bind_authored_local_size, cap=kMaxAuthoredLocalSize=groupshared 1024). ⛔ NOT "26d COMPLETE" until 4c (a zombie enum ≠ a retired reject). 26d-4b (mechanism + Vulkan proving gate) ✅ 2026-09-04: relu_vjp.ckir sentinel + prose header; REMOVED grad.cpp's M·hidden==32 guard (+ dead dim_count); superseded test_grad reject (→None+backward-vocab-emitted); non-32 vjp Vulkan gate (h1=64) GREEN on real RTX == hesap ref (teeth-first: reject fired at ordinal 10 before removal). ⛔ COUPLED emit-site fix (the sentinel's blast radius, BOTH backends): 3 non-resolver emit sites (Vk+DX12 load_emit_ckir + the 25c-0 reading gate) tripped the emit-guard → added an optional local_size_x cook-bind param + assert the sentinel. GREEN: Vk 25c*/26d-4b 422/3 + DX12 25c* 221/2 + device-free 25c-1 74/2 + kir [asset] 563/30 + 6 files tidy. Scar written (sentinel=grep asset-path, not symbol). 26d-4c (DX12 non-32 vjp gate MIRROR) ✅ 2026-09-04: GREEN FIRST TRY on real D3D12 (h1=64 vjp_mlp backward == hesap ref, 423/3 incl 25c 32-regression; mechanism rode the shared lib, zero new code) + fixed the DX12 25c-2 stale "baked local_size" comment. The non-32 vjp is now proven BOTH backends. 26d-4d (retire zombie enums) ✅ 2026-09-04: DEAD-marked + KEPT BOTH enum values per the project-wide append-only convention (grep proved `⛔ append at END` is repo-wide for TYPED-reject enums; the advisor's "remove" was corrected by grad.hpp:30 + the LossNotScalar precedent — removing would be the only deleted TYPED-reject value in the repo) + added the marker to MlExpandError (sibling consistency) + recipe struck in place; §73 census 3 SITES → 0 (values KEPT, return sites gone); blast-radius build clean, 96/4 green, final grep = only the grad.cpp name-case is live. The plan was ADVISOR-CORRECTED: verify each enumerator is LAST [ordinal-shift/cooked-blob scar], remove value + GradError's grad.cpp:160 name-case [MlExpandError has no name switch] + delete the 26d-3c "retire me" comment, blast-radius build = crd-ceir-gpu lib+3 test targets [NO WSL sweep, MSVC errors on stale case as hard], update the recipe docs/recipes/…autodiff.md:21,:121 in place; skip the default-audit + nn_reverse fear). 26d-4e (typed softmax cap) ✅ 2026-09-04: softmax.ckir sentinel-ized + both resolvers route Sq through bind_authored_local_size (typed LocalSizeExceedsLimit if Sq>1024==groupshared cap) + tensor_pipeline.hpp write_numel→extent (per-kernel: relu=numel, softmax=Sq rows). ⛔ CAUGHT+FIXED a 26d-4b builder-divergence: build_relu_vjp still emitted local_size=32 while the asset was hand-edited to sentinel (a [.emit-relu-vjp] regen would revert it) → aligned to 0 + corrected the false "Authored DIRECT" header; relu.ckir confirmed hand-authored (no builder). GREEN: [ml][gpu] Vk 122/6 + DX12 71/4 (attention Sq=2+Sq=3 cook-bind softmax, no regression) + [asset] 563/30 + 24b 27/1 + [tensor-pipeline] 322/24 + tidy. Sentinel-scar extended (builder facet). ✅✅✅ **26d (SPECIALIZE) BAND COMPLETE** (0 census + 1 mechanism + 2 relu MLP + 3 attention + 4 relu_vjp/enums/softmax-cap); §73 done, all 3 baked-shape rejects retired + device-proven Vk+DX12. 26e-0 (basic-fusion CENSUS) + 26e-1 (mechanism gate) DONE: the 26e candidate = gemm→relu ELEMENTWISE-ACTIVATION EPILOGUE fusion (synth_gemm emits only plain g.contract; the MLP interior = linalg.gemm→dispatch(@relu) = 2 stages + intermediate z; live consumer ml.mlp fwd + vjp recompute, both backends; already-fused = QuantGemm producer-fold/Elementwise/attr-selection, NOT this). ADVISOR-LOCKED 26e-1: COOK-TIME stage-merge NOT a CEIR pass (@relu is a dispatch{kernel-attr}→authored .ckir; a rewrite would mint a fused op CEIR has no vocab for + carry a scheduling decision in the IR; QuantGemm precedent — detect at plan, 2 ops STAY in module, one stage emitted; differential = fused-plan vs unfused-plan device output BIT-EXACT). Census = 2 producers (mk_dispatch("relu") @ expand_ml.cpp:144 fwd + grad.cpp:414 vjp; no 3rd). Fold predicate = 3 TYPED conds (gemm result num_uses==1 & use is dispatch; kernel=="relu" & dispatch bind[0]==gemm result; gemm PLAIN — the fusion-select scar, check at FOLD not just synth or a non-plain gemm folds→UnresolvedKernel). Synth = EXTEND synth_gemm w/ optional epilogue enum None|Relu (SAME K-loop ⇒ bit-exact by construction, NOT a sibling synth). relu.ckir SURVIVES (standalone @relu non-fusable consumers). 26e-2a DONE (the fusion MECHANISM, device-free): synth_gemm gained a GemmEpilogue{None,Relu} param — Relu appends Max(contract, const0) (same K-loop ⇒ bit-exact); DISCOVERY = emit_contract_glsl is a FIXED matmul template (not a graph walker), so BOTH emit_contract_glsl+hlsl now UNWRAP a Max(Contract,0) root → store max(acc,0.0) (cval==0 checked exactly, a Max(Contract,c≠0) rejected). Gate test_ckir_synth "ceir 26e-2a" 8/8 121 assns: eval_cpu(Relu)==relu(A·B) w/ NEGATIVE outputs, eval_cpu(None)==A·B regression, GLSL+HLSL Relu contain max(acc & None don't, cval≠0 root→emit false. Additive-only ⇒ no contract-emit regression. 26e-2b DONE (plan wiring, device-free green): StageKind::GemmRelu + fusable_gemm_into_relu (4 conds cheapest-first: op-name/gemm_is_plain/⛔⛔!fusable_dequant [quant MLP is BOTH QuantGemm+GemmRelu, f32-weight only]/result-single-use→dispatch{kernel==relu,SymbolRef} num_operands==5 ∧ operand(3)==result). Fold: skip fusable gemm at gemm-site (z never allocated) BEFORE QuantGemm-detect; emit GemmRelu{op=gemm,bind={A,B,h}} at relu-dispatch-site AFTER trailing-write check, re-mark h ExternalIn→Intermediate. Gate test_tensor_pipeline "ceir 26e-2b" 5 blocks: (a) fp32 2-layer MLP→N-1 {GemmRelu,Gemm} + NAMED-BUFFER asserts (buf_of(z1)==-1 gone, h1.role==Intermediate, bind[2]==h1); (b) multi-use→not fused; (c) non-plain α=2→SynthRejected; (d) standalone relu→VizDispatch; (e) ⛔⛆ quant-MLP-NOT-hijacked FULL PLAN {QuantGemm,VizDispatch,QuantGemm} no GemmRelu. GREEN [tensor-pipeline] 25/353 + full device-free 96/1356 + tidy-clean. ⛔⛔⛆ DEVICE DEBT (pending 26e-3, NOT device-free-broken): the shared-lib fold makes fp32 ml.mlp DEVICE tests (24b-4:1431, 26d-2c:1529, 24c-3 run-A) + DX12 mirrors plan a GemmRelu the resolver if/else doesn't handle → UnresolvedKernel on-device — the advisor-blessed split (26e-3 adds the resolver case). 26e-3a DONE (Vk resolver + PlanOptions + fp32-MLP device tests re-greened): PlanOptions{fuse_gemm_relu=true} threaded into both fold sites; Vulkan resolver LIFTED Gemm→if(Gemm||GemmRelu) synth_gemm(Relu?None)→emit_contract; 26d-2c PINNED fuse=false (keeps the 26d relu-VizDispatch cook-bind + oversize cap teeth — those guard standalone relu/softmax/relu_vjp; the fused path is 26e-3b). GREEN real RTX: 24b-4 Vk #4752 (FUSED fp32 MLP == CPU float oracle — resolver GemmRelu + emit_contract relu-unwrap device-correct), 26d-2c Vk #4753 (unfused cook-bind+oversize), 24c-3 Vk #4755 (crown: fused CKIR-expand == coopvec-native) + device-free [tensor-pipeline] 353/25 unchanged + tidy-clean. ⛔ 24b-4 ==oracle is DEVICE-vs-ORACLE tol NOT yet bit-exact raw-vs-opt (=26e-3b). ⛔ DX12 STALE (#4837/#4838 ran pre-26e exe, not evidence — only vulkan-tests rebuilt); 26e-4 rebuilds+DX12 resolver+re-green. 26e-3b DONE → ✅✅ 26e-3 (Vk device leg) COMPLETE. The gemm→relu fusion is BIT-EXACT vs the unfused program on Vulkan (#4754 real RTX): SAME fp32-MLP (x[4,8]·W1[8,16]→relu→·W2[16,2]) planned twice — fuse=false (3 stages Gemm+VizDispatch+Gemm) vs fuse=true (2 stages GemmRelu+Gemm) — run both, every output element out_f[i]==out_u[i] BIT-EXACT (not tol; same emit_contract K-loop + f32 store/load identity + FMax). The FIRST dispatched-stage 26e witness at full teeth (26b alias-only, 26c dup-gemm; 26e CHANGES which kernel runs). Advisor-hardened: reused 26d-2c seeds, REQUIRE neg_count>0 (relu flips ≥1, non-vacuous), first-mismatch CAPTURE, barrier note. tidy-clean. 26e-4 DONE → ✅✅✅ CEIR-26e (basic-fusion → generated CKIR) COMPLETE on BOTH backends. DX12 mirror: observed the stale-exe RED first (26d-2d 1==0 on rebuild — confirmed the fold was masked), then LIFTED the whole DX12 resolver Gemm→Gemm||GemmRelu (synth_gemm(Relu)→emit_contract_hlsl), PINNED 26d-2d fuse=false (has the VizDispatch loop), added the DX12 26e-4 bit-exact differential (mirror of 26e-3b). GREEN real RTX: 26e-4 #4840 (fused==unfused BIT-EXACT on DX12), 24b-4 DX12 #4838 (fused MLP==oracle via the resolver case), 26d-2d #4839 (unfused cook-bind) + §137 26b-2/26c-2 both backends (no regression) + tidy-clean. gemm→relu epilogue fusion = cook-time stage-merge at the CEIR→CKIR boundary, device-resident + BIT-EXACT vs unfused on Vk+DX12 (the first fusion synthesizing a NEW kernel replacing an authored dispatch). 26e sub-slices: 0 census / 1 mechanism / 2a synth+emit / 2b plan / 3a Vk resolver+PlanOptions / 3b Vk bit-exact / 4 DX12. 26f-0 CENSUS DONE (3 axes, no build): (1) SCHEDULE = ABSORBED (name-forward to CEIR-28) — real fan-out exists (vjp_gemm backward dA∥dB + Bᵀ∥Aᵀ independent) but scheduling is a LATENCY opt (changes timing not values) ⇒ NO bit-exact witness (the 26 differential passes vacuously); perf-driven scheduling is CEIR-28's cost model, not 26f. (2) MEMORY = the ONE live 26f consumer → an INTERVAL-COLORING buffer-aliasing pass: PlanBuffer has NO lifetime (every Intermediate its own buffer); ATTENTION (both backends) has kt[D,Sk]/scores[Sq,Sk]/probs[Sq,Sk] DISJOINT lifetimes (kt∩probs=∅, max-live=2) → 3 Intermediates need 2 physical buffers, 30% byte win at Sq=3/Sk=5/D=4 (200→140B); this IS the 12d plan_block_memory interval-coloring the tensor_pipeline.hpp header PRE-DEFERRED as "a ledgered slice". Differential HAS TEETH (unlike schedule): aliasing changes addresses not kernels, a bit-exact PASS proves the lifetime analysis sound (a wrong alias clobbers→wrong output). (3) WAR = documented gap + negative gate — zero `rw` bindings in the corpora (executor barriers every output). 26f-1 MECHANISM LOCKED (advisor, same call): (1) NOT interval-coloring — linear chain → SINGLE-PASS free-list of (index,bytes); a buffer frees when its LAST reader ran, next Intermediate born-after takes it if it fits (smallest-fit else largest-grow). (2) last-use = max over ALL uses (num_uses discipline; backward dc has 2 consumers dA,dB). (3) WIDEN alias_of (NO new field): tenant = role Intermediate + alias_of=LANDLORD (different value sharing bytes); view = role Alias (same value); runner skips alloc when alias_of>=0 REGARDLESS of role (1-line, both backends); landlord bytes grows to max(landlord,tenants); alias_of→landlord never another tenant (alias_of<i realized). (4) EXCLUDED from sharing: ExternalIn/Output/value==nullptr + ⛔ func.return operands PINNED (the named-readback trap CONFIRMED: 25c-2 reads dW2 as Intermediate via is_named_out→GpuToCpu; a tenant dW2 would be alloc-skipped→readback fails; plan pins func.return'd values, subsumes Output). (5) PlanOptions{share_intermediate_storage=true} (NOT alias_intermediates — collides with Alias role) for raw-vs-opt bit-exact differential + fewer-physical-buffers assertion. Differential TEETH: bit-exact proves lifetime analysis sound (wrong alias→clobber). ✅✅✅ CEIR-26f (memory/schedule) COMPLETE both backends — 26f-4 DONE (the DX12 MIRROR: run_quant_module_n line 309 widened role==Alias→alias_of>=0 [+n_allocated], all 3 advisor pre-read greps favorable ⇒ 1-line widen [RAII dev[40] no double-destroy, upload keys role not fill, rb readback keyed on output Value]; GREEN real RTX — 26f-3b DX12 #4842 BIT-EXACT share differential + exact delta, 26d-3c DX12 #4841 n_alloc identity, 24b-4 attn #4840 + 25c-2 #4849 aliasing-live == oracle/hesap) [26f-3c: func.return-pinning POSITIVE CONTROL device-free 99/1399 — WITH return dw2.alias_of<0, WITHOUT it dw2.alias_of==bz ⇒ PINNING discriminates not lifetimes] [26f-3b: FORMAL Vk differential #4757 — SAME attention module share=false vs true, BIT-EXACT outputs + tenants==1/0 + EXACT delta; ⛔ HONEST: proves runner alias-resolution + no wrong tenancy [wrong-tenancy teeth], NOT WAR-ordering (attention has no reader of Kt after probs's write ⇒ no WAR hazard; barrier-spec per 26f-2, real WAR test = tenant-READ@s+2, a 26z optional)] [26f-3a: Vk aliasing LIVE — line 321 widened role==Alias→alias_of>=0, attention probs SHARES Kt + == SDPA oracle #4752/#4753, IDENTITY gated n_alloc==nb−tenants ∧ tenants==1; ⛆ 25c-2 dW2 readback GREEN w/ aliasing live #4762 [HONEST: pinning + the real plan's lifetimes TOGETHER, NOT pinning alone — the discriminating pinning negative is 26f-2b(c) synthetic / a 26f-3b real-corpus one-liner]; MLP inert; §137 bespoke runners stay role==Alias (inert via landlord grow-over-alloc, not "no tenants"); teardown/upload predicates VERIFIED safe not assumed; +n_allocated out-param] [base 26f-2/2b: n_out==0 guard + attention gate]: assign_shared_storage single-pass free-list (produce/last_read[MAX over all uses] per buffer; tenant b takes root L iff free_at[L]<produce[b] STRICT & fits [smallest-fit else largest-grow]; b.alias_of=L, L.bytes=max, free_at[L]=last_read[b] chaining) + PlanOptions{share_intermediate_storage=true} + alias_of doc widened (view[Alias]/tenant[Intermediate]) + func.return-PINNED (plan walks func.return, excludes operands — the 25c-2 dW2 trap) + Output/ExternalIn/nullptr excluded. Gate test_tensor_pipeline "ceir 26f-2" 3 blocks: (a) 4-dispatch chain v2 tenants v0 + GROW 16→64 + 3→2 physical; (b) last_read=MAX (z read s1&s3, c born s2 does NOT tenant z); (c) func.return-pinned dw2 keeps own buffer. + 26f-2b GATE "ceir 26f-2b" 2 blocks: (d) NEG n_out==0 guard (all-read "r,r" s1 last-reads held, later born s2 does NOT tenant held — the reader emits no barrier ⇒ never lends; never_free=0x7fffffff pins last_read); (e) POS attention via expand_ml_ops — EXACTLY 1 tenant = softmax write probs(24B) tenants Kt(48B) (the NAMED consumer fires the pass, not only a synthetic chain). GREEN [tensor-pipeline] 27/387 + full 98/1390 (+1 case/+15 assns = EXACTLY 26f-2b, ZERO regression: the guard is a no-op on every n_out>0 stage) + tidy. ⛔ WAR = a proven FACT (advisor), NOT a risk: the per-stage ShaderWrite→ShaderRead barrier has a STAGE-WIDE exec dep (srcStage=COMPUTE_SHADER orders reads too) ⇒ free_at[L]<produce[b] is ordered BY CONSTRUCTION; 26f-3 CONFIRMS, doesn't discover. The n_out==0 no-barrier HOLE is now CLOSED at the plan. ⛔ runner skip is INERT until 26f-3 (runner still skips only role==Alias → tenants allocated separately → device tests unchanged). 26z ⏳ BAND-CLOSE — ✅ (1)+(2) llvmpipe re-runs DONE: WSL warm, targeted incremental rebuild + ctest -R '26b-2|26c-2' under llvmpipe (LP_NUM_THREADS=4) → 26b-2 #4573 + 26c-2 #4574 BOTH BIT-EXACT on the current tree, no defect surfaced. ✅ 26z(3) DONE — the anon-import CSE bug FIXED (advisor: option (a) gold-standard): gave resource.import an Allocate effect (traits=[] + effects=[Allocate] in the toml, REGENERATED via ceir_opgen.py, drift ctest #703 green) ⇒ non-Pure ⇒ no import CSE'd; test_cse "ceir 26c/26z" rewritten (2 declares + 2 same-named [i2 flipped erased→KEPT] + 2 ANONYMOUS imports KEPT + cse_run==false; the file's first cs.* Pure test is the CSE-works positive control); blast radius swept — full device-free ceir 502/3930 green (dce/12c-hazard/§27 unaffected). ✅✅✅ CEIR-26 (optimizer phase 1) COMPLETE (26a DCE + 26b fold + 26c CSE + 26d specialize + 26e fusion + 26f mem-aliasing + 26z close). 26z(4) stale-exe scar WRITTEN. ✅ 26z(5) MEMORY.md compaction DONE LOSSLESSLY (advisor corrected my framing — the hook means SPLIT into topic files, not trim): moved the Scars section (77 of 242 links) VERBATIM into 4 theme-coherent topic files (scars_ckir_emitter_eval / scars_gpu_device_dx12 / scars_render_frame_graph / scars_ceir_ir.md, type=reference) + 4 pointer lines left in MEMORY.md → 21116B→15272B (UNDER the 17.1KB target), 246 links total (242 orig +4 pointers), ZERO curated lesson dropped (verified: 77 unique targets, no dup). 26z(6) CSE scar-file tail refreshed to the 26z(3) fix. WAR-device-test SKIPPED (no live corpus). ▶ **CEIR-27 OPENED 2026-09-05** (ceir.transform/ceir.rewrite — make optimization STRATEGY an authorable asset; §71/§72/§146). 27-0 census DONE (advisor-verified): PlanOptions{fuse_gemm_relu, share_intermediate_storage} EXISTS + is the 27 seed (tensor_pipeline.hpp:146, "fusion IS a schedule decision" comment); consumer = plan_tensor_pipeline is TEST-ONLY, flags read at 3 in-seam sites, NO leak ⇒ 27a = a transform-text-asset→PlanOptions loader (no signature change); canonicalize=2 patterns w/ NO plugin boundary (§72 introduces it); TWO proof lines (§146 schedule + §72 plugin-rule); schedule=a ceir.transform TEXT asset (parse.hpp, NO TOML sidecar); DEFER per-op targeting (no live consumer) + e-graph (§72 "long-term optional"). MAP: 27a ceir.transform dialect+loader → 27b §146 two-schedule BIT-EXACT differential both backends → 27c §72 rewrite-rule asset (identity_reshape, plugin-boundary proof) → 27d 2nd rule (reshape_of_reshape) → 27z close. ✅ 27a DONE 2026-09-05: transform dialect (transform.fuse/share_storage directives, NOT Pure=category-error not DCE, advisor-corrected) + plan_options_from_transform loader (asset→PlanOptions, absent=base default, no signature change) + find_transform_misuse DuplicateDirective guard (house pattern) + device-free gate #827 (both knobs, text round-trip, partial, dup-reject) + opgen-drift #706 + smoke, all tidy-clean. ✅ 27b DONE 2026-09-05: the §146 two-schedule differential — two committed .ceir schedules (schedule_fuse/schedule_nofuse) optimize ONE MLP program, plan-shape differs (fuse=2 stages GemmRelu, no-fuse=3), BIT-EXACT device output on BOTH backends (Vk #4763, DX12 #4852) + device-free asset canonicality #828 (anti-drift + roundtrip + walk); share program-global-inert on MLP (tenants==0 both, asserted); hand-authored .ceir byte-exact first try. ✅ 27c DONE 2026-09-05: the §72 DECLARATIVE REWRITE-RULE asset — rewrite dialect (`rewrite.rule`, 5 required attrs, CLOSED constraint/action vocabs, NOT Pure=metadata-not-value) + RewriteRule model + `greedy_rewrite_rules` DATA-driven interpreter (mirrors greedy_rewrite, monotone-cap→FATAL never hangs, the has_uses re-match guard IN THE DRIVER not a rule attr) + `find_rewrite_misuse` (house pattern) + committed `rule_identity_reshape.ceir`. PLUGIN-BOUNDARY proof (device-free #543): the AUTHORED rule reproduces canonicalize's identity_reshape fold via `greedy_rewrite_rules` WITHOUT being in kCanonPatterns, print-equal to `canonicalize_run` + the DISCRIMINATING idempotence assertion (a 2nd driver call FALSE proves the guard is load-bearing, not absence-of-FATAL). C++-pattern retirement DEFERRED past 27d (the differential IS the proof). +CRD_REPO_DIR on crd-ceir-tests (first asset-reading device-free ceir test). ✅ 27d DONE 2026-09-05: the SECOND authored rule reshape_of_reshape (`reshape(reshape(x))→reshape(x)`, a BUILD-a-new-op rule) — VOCAB +3 on rewrite.rule (constraint `operand_defined_by_root` + action `build_op_from_inner_operand` via the generic ctx.create_operation + OPTIONAL `inner_operand` attr, absent⇒0; `build_rule` stays 5 params per the resource.import optional-`name` precedent, so the asset OMITS inner_operand + stays anti-drift-equal). ⛔ 27c(5)(ii) SUPERSEDED — the RAUW re-match has_uses guard is UNIVERSAL (canonicalize.hpp:51 carries it, not action-specific), HOISTED before all constraints. +`ActionNeedsConstraint` misuse (a path-WALKING action may only pair with the constraint that VALIDATES the path — else a vocab-valid asset null-derefs in rule_apply). GATE #544: anti-drift + reshape_of_reshape-ALONE == canonicalize (print-equal + FIRE-COUNT 3 reshapes = fired once) + COMPOSE (both rules collapse a net-identity chain to %x == canonicalize) + ⛔ ORDER-PRIORITY PROVEN (outer-identity chain [8,8]→[64]→[64]: r-o-r-first BUILDS the fold==canon, identity-first NO fold≠canon ⇒ block order IS pattern priority, a real §72 semantic) + cross-pairing→ActionNeedsConstraint. 27c #543 regression-clean, opgen-drift #711, tidy-clean. 27z DONE 2026-09-05 → ✅✅✅ **CEIR-27 CLOSED**: BAND-CLOSE complete — 27b llvmpipe #4565 BIT-EXACT (lavapipe/CPU-Vulkan, the two authored schedules optimize the MLP bit-identically there too; WSL /mnt/d tree RECONFIGURED for the new globbed transform/rewrite gen files, gen glob is NOT CONFIGURE_DEPENDS) + e-graph §72 deferral (recorded at 27-0) + TWO scars (read-the-C++-MATCH-not-the-rewrite [feedback_data_driven_driver_read_the_match_not_just_the_rewrite]; ⛔-claim-in-source needs a discriminating gate, folded into gate-reverify). CEIR-27 = optimization STRATEGY is an AUTHORABLE asset (a transform SCHEDULE + a rewrite PEEPHOLE, both `.ceir` assets a loader/driver applies; schedule BIT-EXACT Vk+DX12+llvmpipe, rewrite reproduces canonicalize's COMPLETE tensor.reshape set WITHOUT editing kCanonPatterns — the §72 plugin boundary). §72 core (rewrite_rules.hpp, 3 enums widened) green on MSVC AND gcc -Werror (27c/27d/opgen-drift Linux). ⛔ 27e named-forward (own census): where canonicalize_run gets its rules at PASS TIME; C++ kCanonPatterns retirement waits on it. ▶ NOW: **CEIR-28** (autotune/PGO/cost model — the SELECTION policy over 27's mechanism) OPEN · 28-0 census DONE 2026-09-05: the v17 autotuner (engine/kir `ckir_autotune`/`ckir_tile`/`ckir_tuning_db.inc` + kir-cuda `autotune_cuda`) LIVES = the REFERENCE floor — REUSE its pipeline pattern (enumerate→cost-rank→measure→oracle-gate→checked-in-DB→replay) NOT its CUDA-only measurer/KIR-tile knobs. Slice-0 tunes EXACTLY the 4-config 27 `PlanOptions{fuse_gemm_relu, share_intermediate_storage}` space (oracle-gate FREE from 27b's BIT-EXACT Vk+DX12+llvmpipe proof; cost model VACUOUS at 4 configs = MEASURE ALL, roofline §82 named-forward). The target-specific config cache is a `ceir.tune` .ceir ASSET (device/env/program-hash/shape-class→schedule_ref; v17 row shape `{op,DEVICE,ENV,M,N,K,sched}` confirmed). Deterministic locked mode (§80) = loader HIT uses the row / MISS falls back to default + does NOT measure (§81 deterministic fallback); `locked=true`⇒typed reject. PGO §81 runtime feedback = named-forward (trigger: a program with a runtime hot loop). ⭐ timing path VERIFIED (Vk/DX12 compute-context timestamp/QueryPool/QueryHeap). MAP: 28a (`ceir.tune` dialect + `find_tune_misuse` + `plan_options_from_tune_cache` loader) → 28b (portable Vk measurer, reuse 27b harness, emit winner tune.entry) → 28c (deterministic locked mode) → 28d (DX12 mirror) → 28z (close: llvmpipe cache row proves device-keying; deferrals; scars). ✅ 28a DONE 2026-09-05: the `ceir.tune` dialect (`tune.entry` = one cache ROW, KEY device/env/program_hash/shape → schedule fuse/share = PlanOptions inlined; NOT Pure; 6 required attrs) + header-only `tune.hpp` (`find_tune_misuse` MODULE-WIDE DuplicateKey + `load_tune_entries`, both share `collect_tune_entries` region-recursive) + `plan_options_from_tune_cache` FULL-KEY loader → `TuneCacheLookup{hit,opts}` (HIT=row schedule, MISS=base unchanged, NEVER measures). Gate #834: load+identity, hit, miss(base preserved), ⭐ full-key discrimination (env AND program_hash matched — IDENTITY not category), same-block + MODULE-WIDE (two-block) DuplicateKey. ⛔ module-wide fix = the 27z ⛔-claim-in-source-needs-a-gate lesson applied same slice. Deferrals: env bundles driver/compiler-version (trigger: a driver update flips the winner); committed cache `.ceir`+anti-drift → 28b (measurer emits it, print-equal-to-emit oracle); program_hash has NO producer → 28b defines it = canonical-print hash. ✅ 28b-1 DONE 2026-09-05: `tune::program_hash(ctx, module, alloc)` = fnv1a-64 of the CANONICAL PRINT (hash the POST-expansion payload the planner consumes, PRE-plan; print-equal⇒hash-equal). ⛔ 28a(3) SUPERSEDED — program_hash widened u32→u64 (read_entry truncated an i64-stored 64-bit hash). Gate: print-equal⇒hash-equal + type-change⇒different + ⭐ pre/post-expand_ml_ops DISCRIMINATOR. GREEN [tune] 32 assns + full crd-ceir-gpu-tests 103/103. ⛔ bulk `ctest -R ceir` 549+858 "failures" DIAGNOSED = ctest timeouts under a concurrent clang-tidy run (host-instability doctrine; all pass in isolation + full-exe default-order 103/103); crd-ceir-tests rebuilt per the stale-exe scar. 28b-2a DONE 2026-09-05: the portable Vk MEASURER `measure_tune_entry` (Vk-test-level fn, 28d reuses) — enumerate 4 `PlanOptions` configs, N=3 warmup+K=7 timed MEDIAN of `compute.last_gpu_ms` (whole-cmdbuf 2 timestamps :541/:549, barriers cancel), FREE oracle-gate each BIT-EXACT to default (⛔ 27b ran only 2/4), ⭐ plan-sig WINNER-COLLAPSE (tt≡tf plan-identical ⇒ a noise gap must NOT flip the emitted row → 28b-2b anti-drift) + the DISCRIMINATING IDENTITY gate sig[0]==sig[1]&&sig[2]==sig[3]&&sig[0]!=sig[2] (share INERT, fuse LIVE) replacing a can't-fail n_configs==4, REQUIRE(median>0)=the value used. Emit→replay through the real loader: hit+winner opts, wrong-hash MISS, replayed schedule BIT-EXACT to default; env platform-derived. MEASURED RTX 4070 Ti SUPER: medians [tt,tf,ft,ff]=[0.008096,0.008128,0.009184,0.00912] ⇒ fuse ~12% win (share=noise), winner collapsed {fuse=T,share=T}, program_hash=13485518615284092459 (u64)/-4961225458425459157 (i64). ⭐ LAVAPIPE VERIFY (advisor gate): rebuilt+ran on WSL linux-gcc-debug lavapipe (vk:llvmpipe LLVM 20.1.2) — REQUIRE(median>0) PASSES (llvmpipe HAS timestamp queries ⇒ no CI red), SAME program_hash (fnv1a of print is platform-independent), 69 assns GREEN + gcc build clean. GREEN RTX #4774 (69 assns) + lavapipe (69) + tidy. 28b-2b DONE 2026-09-05: COMMITTED device-keyed cache asset assets/ceir/tune_cache_vk_rtx4070ti_super.ceir (= 28b-2a's EXACT -s emit, bootstrap-via-print, NO hand edits). Anti-drift print(parse(file))==print(fresh re-measure) [both through print ⇒ robust to whitespace, fails ONLY on a winner FLIP = the DESIGNED re-tune signal], DEVICE-GATED: RTX→anti-drift + replay BIT-EXACT + loader HIT; lavapipe→loader MISSES. ⭐ IDENTITY block (advisor, device-independent, gate=identity-not-category): load_tune_entries n_rows==1 + device==committed + program_hash==ph AND ==13485518615284092459ULL (the i64 -4961225458425459157 the file prints reads back as this u64 — the 28b-1 u64-widen now GATED on a REAL parsed asset) + shape ⇒ the lavapipe MISS is present-but-misses-on-KEY (device+env; device-alone=28a(d)), not absent/corrupt. Extracted build_mlp_payload SHARED by 2a+2b (the hash-lock). GREEN RTX #4774+#4775 + WSL lavapipe 84 assns/2 cases gcc-clean + tidy. ⛔ UNCOMMITTED-BATCH now includes the .ceir asset (no source but a test — commit or anti-drift breaks). ✅✅✅ CEIR-28b COMPLETE (program_hash + portable measurer + committed device-keyed cache; v17 "tune offline, replay forever" made portable). 28c DONE 2026-09-05: DETERMINISTIC LOCKED MODE — plan_tensor_pipeline_cached(ctx,m,alloc,cache_mod,device,env,shape,TunePolicy,base={}) selects the cache's schedule keyed (device,env,program_hash(m),shape). ⛔ program_hash computed INTERNALLY from m (post-expansion, same producer 28b emits → key can't drift). HIT→row's {fuse,share}; MISS→policy: ⭐ TunePolicy{Fallback (→base, no measure), Locked (→typed reject)} enum NOT a bare bool (advisor; Require name-forward to 28z, append-END slot). New PlanReject::TuneCacheLockedMiss (append-END, reject_op=nullptr — miss is a KEY property not a module one; plan_reject_name case added). ⛔ widen-enum audit: only plan_reject_name switches (case added), no count/roundtrip consumer; MSVC + gcc -Werror clean (switch-gap scar). Device-free gate #839: ml.mlp EXPANDED (fuse=T→2 stages GemmRelu; fuse=F→3 stages VizDispatch) — (a) HIT row's fuse=false beats a CONTRADICTING base fuse=true [IDENTITY: VizDispatch present, no GemmRelu] + a 2nd row at hash+1 opposite-schedule TRAP proves the key IS program_hash(m); (b) MISS+Fallback→base; (c) MISS+Locked→TuneCacheLockedMiss+reject_op null+0 stages. GREEN MSVC #839 + all-3-exes relink (stale-exe) + WSL gcc 12 assns -Werror clean + tidy (3 files). 28d DONE 2026-09-05: the §80 measurer MIRRORED onto DX12 (migrated=both-backends). measure_tune_entry mirror-inline (advisor reversal — NOT a template; the two run_quant_module are file-local to 2 TUs). Device key create_dx12_gpu_context()→adapter_name() "dx12:"+adapter (⛔ advisor reversal: NOT a not-WARP assert — false-fails on WARP-only CI; non-empty check, strict same-adapter deferred; on WARP the key is CORRECT = D3D12's llvmpipe, 28z covers). MEASURED RTX 4070 Ti SUPER #4873 (78 assns): device "dx12:NVIDIA GeForce RTX 4070 Ti SUPER", program_hash=13485518615284092459 IDENTICAL to Vk+lavapipe (portable, 3rd backend), medians [tt,tf,ft,ff]=[0.017344,0.017344,0.018432,0.018432], winner {fuse=T,share=T} (fuse wins DX12 too ~6%). ⭐ CROSS-BACKEND PLANNER DETERMINISM: plan_sig BIT-IDENTICAL to Vk (fuse 0x83e33779c4351c48, no-fuse 0x4e7803adf4d43172) ⇒ plan_tensor_pipeline has no backend branch ⇒ ONE cache shape for all backends. ⭐ MEDIANS FINDING: DX12 quantizes (tt==tf,ft==ff exact); fuse/no-fuse gap ~1.09µs (~6%) = 1-2 timer quanta = the SAME ~1µs absolute delta as Vk's ~12% (2× smaller base) ⇒ fusion saves ~1µs on BOTH; DX12's ~2× base is the readback copy(GpuOnly→GpuToCpu) INSIDE the begin→submit bracket (~9µs floor = DX12 base − Vk base). NEVER compare DX12↔Vk medians raw. GREEN MSVC #4873 + tidy (no lavapipe — D3D12 Windows-only). 28z items accrued: committed DX12 row (trivial, fuse won both) + Require policy + n_warmup drift (DX12=5 vs Vk=3) + PGO/roofline/env deferrals + readback-floor subtraction. 28z-1 DONE 2026-09-05 (BAND-CLOSE 1/3, subdivided per grant): n_warmup UNIFY Vk 3→5 (match DX12; one number both backends; PROVEN safe — winner-collapse makes the emitted row warmup-INDEPENDENT, reran 28b-2a #4775 + 28b-2b #4776 at 5, both pass, committed asset UNCHANGED). TunePolicy::Require DEFERRED (advisor reversal of the 28c "the one 28z needs": the only Require semantic with a consumer — "HIT but planner IGNORED opts → reject" via a tune_row_applied plan bit — has NO caller; no-speculative rule wins; name-forward with trigger, enum keeps append-END slot). GREEN 28b-2a+28b-2b n_warmup=5 + tidy (test_ceir_pipeline_vulkan.cpp + tensor_pipeline.hpp). 28z-2 DONE 2026-09-05: COMMITTED 3-row cross-backend cache assets/ceir/tune_cache.ceir (vk-RTX + dx12-RTX + vk-lavapipe, all fuse=true,share=true; REPLACES the single-row asset — delete+create, don't git-add the stale name) + PER-ROW FIELD anti-drift via the loader (multi-row breaks module-print-equality: 6-field compare device/env/hash/shape/fuse/share, a winner FLIP→fail=re-commit signal), ONE anti-drift test PER backend TU (Vk 28b-2b updated in place + DX12 28z-2 NEW, both read the same 3-row file), ROWS-ADDRESSABLE table (all 3 hit their key) + wrong-ENV miss (RTX under linux) + find_tune_misuse→None cross-backend + else-branch UNEXERCISED (no 4th device, not faked). GREEN Win Vk #4776 + DX12 #4874 + WSL lavapipe 92 assns gcc-clean + tidy (2 TUs). 28z-3 DONE 2026-09-05 → ✅✅✅ CEIR-28 BAND CLOSED (all subslices green Vk+DX12+lavapipe, MSVC+gcc): (1) bench docs/bench/2026-09-05-ceir28-autotune-medians.md [fusion wins both backends ~1µs, ~9µs DX12 readback floor, cross-backend planner determinism, share inert]; (2) cross-config win 7/7 + WSL gcc device-free 44 assns + Vk-lavapipe 169 assns (DX12 self-skips linux); (3) deferral ledger [PGO/roofline/env-split/Require/WARP/readback-floor/else-branch — each w/ a trigger]; (4) 2 scars [[feedback_autotuner_winner_collapse_to_plan_equivalence_class]] + [[feedback_committed_asset_antidrift_through_the_printer_not_bytes]]; (5) this LIVE header refreshed to CEIR-28-CLOSED. NEXT = **CEIR-29** (native graph providers — one graph across two provider classes: D3D MLIR Programs / VK data-graph / NPU research; §70 §102 §69; OPEN with an advisor census). ⚠ context.md STILL bloated (line 10 >74KB narrating CEIR-25→28) — a DEEP structural prune of the historical CEIR-25/26/27 narration REMAINS (the header + tail are now current; the middle narration is marked historical).]** — **CEIR-25 (last shipped):** reverse-mode autodiff as a COMPILER TRANSFORM — `build_gradient` differentiates `linalg.gemm`/`tensor.reduce(sum)`/`ml.mlp` BEFORE expansion, emitting the backward pass in the existing op vocab (VJP rules by op-kind; the composite `ml.mlp` differentiates before expansion, recompute = full checkpointing). Every CEIR-25 gate GREEN on every backend, **NO losses**; the whole MLP backward (dW1+dW2) runs device-resident on **Vulkan + DX12 + llvmpipe** == dialect-independent hesap `nn_reverse` (FD-cross-validated). Band-close suite (25z-5) re-measured fresh, zero drift: device-free MSVC+gcc 1192/88, Vk real+llvmpipe 7/7, DX12 7/7, asset 1/1. Board `docs/bench/2026-09-04-ceir25-autodiff-scoreboard.md`; recipe `docs/recipes/2026-09-04-ceir-reverse-mode-autodiff.md`; close `docs/sessions/2026-09-04-ceir-25z-band-close.md`; **tracker `docs/detours/D-007-ceir-tracker.md` CEIR-25 row ✅ (AUTHORITATIVE)**. ⚠ context.md prune to session logs STILL due (bloated across bands). — ⚠ this `## Current focus` section is a stacked history (bloated across bands — a prune to session logs is due); the AUTHORITATIVE live state is the two markers near the END of this line — `[CEIR-25 (ceir.autodiff) BAND-OPENED]` + `[CEIR-24 BAND CLOSED]` — and `docs/detours/D-007-ceir-tracker.md` (the master band table). ⚠⚠ all of CEIR-17→24 is UNCOMMITTED (git at "working on CEIR-22"); the deleted-bootstrap `.ckir` assets are sole-source in the working tree — COMMIT before extending. Older band history (CEIR-19 ← here down, then CEIR-20/21/22/23/24) is retained below for reference. — **[2026-08-16 ✅✅✅ CEIR-19 (RT / ceir.rt) BAND CLOSED]** — every RT algorithm + rendering DECISION ships as an authored `.ckir`/`.crdv` asset; `scene_renderer.cpp` gained ZERO render-technique C++ across 19a–19z. Contents: **19a** the `ceir.rt` dialect DECLARED (6 ops; blas/tlas/sbt Externs); **19b** the AUTHORED hybrid RT-shadow renderer (raster→compute-worldpos→raytrace.pipeline→composite) on Win Vk+DX12 + lavapipe; **19c** the `execute_rt_lowered` caller-hook BRIDGE + the §134 wavefront PATH TRACER (rt_witness + wavefront_{compact,trace,shade}.ckir) on 3 RT devices, ZERO bridge changes; **19z** the close — F1 (VUID-08608 = upstream VVL FALSE-POSITIVE → version-scoped whitelist), F2 (composite × shadow_mask makes the RT shadow VISIBLE via a new `draw_textured_storage` verb, root-caused to a plan-cook binding drop), the bridge-vs-runtime reads-first drift unified. ⭐ BAND-CLOSE MATRIX GREEN on 5 legs (linux-gcc-debug/asan + lavapipe + win-debug/asan), ZERO failures — row-per-config + row-per-claim + audit (zero render-technique builders, c3ac005-verified) + an 8-item deferral ledger in `docs/sessions/2026-08-16-ceir-19z-band-close.md` (the AUTHORITATIVE close record). ⛔ SURPRISE: the initial Windows red was a HARNESS run-env gap (CRD_ASSETS_DIR + shaderc + ASan-DLL PATH), NOT a defect — [[reference_windows_test_run_env_assets_shaderc_asan_dll]]. Key memories: [[feedback_authored_asset_correct_but_binding_dropped_needs_plan_cook_operand]] (F2 binding-drop), [[feedback_one_read_fullscreen_binds_single_texture_not_bindless]] (the verb). **19z close** ✅: 19z-1 (F1, commit ⑩) · 19z-2 (bridge drift, commit ⑪) · 19z-3 (F2, commit ⑫) · 19z-4 (this band close — matrix + audit + tables + ledger, commit ⑬). Sub-slice + ledger detail → the session log + the tracker CEIR-19z row (AUTHORITATIVE). **✅✅✅ CEIR-20 (`ceir.work` / device-generated work) BAND CLOSED 2026-08-17** — one authored program, THREE device lowerings (20b fallback + 20c-1 D3D12 Work Graphs + 20c-2 VK_EXT_DGC); §135 EXCEEDED (two modern lowerings). Matrix green: win-debug 20/20 + linux-gcc-debug gcc-Werror (DGC RAN on lavapipe = 2nd-vendor proof) + win-asan memcpy gates clean; zero-builder audit clean; 7-item ledger homed (⛔ work.compact device-consumer RE-HOMED off 20c). AUTHORITATIVE close: `docs/sessions/2026-08-17-ceir-20z-band-close.md` + tracker CEIR-20 row ✅. commit ⑱. **[historical build narrative for 20a–20c follows; tracker + session log are authoritative]** (`ceir.work` dynamic/device-generated work; opened + expanded 20a–20z, advisor band-open 2026-08-16). **20a ✅ 2026-08-16** — the `ceir.work` dialect DECLARED (the 19a mold): work.ceirop.toml (4 ops queue_alloc/produce/consume/compact + 2 opaque Externs work.queue/work.record) → opgen → work.hpp/cpp (find_work_misuse; consume/compact are INDIRECT — no host grid, the device-count distinction) + test_work.cpp; async reconciliation in the header (device work-gen ≠ async.launch); GATE GREEN 4 configs ([work] 53/5) + opgen no-drift + tidy; work.record declared-forward-unused; commit ⑭. NEXT 20b (the FALLBACK lowering + the AUTHORED wavefront-as-ceir.work consumer — re-express the §134 wavefront host while-loop as a ceir.work program on the queue-buffer+indirect fallback, REUSE wavefront_*.ckir, RETIRE the 19c C++ host loop). **⭐ 20b DESIGN-LOCKED 2026-08-17 (advisor ×2, in-progress) — Design Z (frame-executor):** the wavefront orchestration LIVES as an authored `assets/frame/wavefront_work.frame.toml` (branch 1 — `to_ceir_frame`→ceir.frame Module is the authored→CEIR path; queues=`resource.declare` counter/indirect_args, stages=`frame.pass` naming NEW `work.produce`/`consume`/`compact` ExecutorTypeIds — the `compute.dispatch` precedent), materialized by a NEW `build_work_ceir` (the `build_fullscreen_ceir` mold) → `lower_region` → LoweredCommand → NEW `execute_work_lowered`+`WorkHooks{resolve_queue,dispatch,trace_dispatch_indirect}` (the `execute_rt_lowered`/`RtHooks` precedent). Mapping: trace=produce (grid, TLAS), shade=consume (INDIRECT/device-count, TLAS), both compacts=compact — all 4 ops device-exercised. NEEDS a `LoweredKind::DispatchIndirect` widen (5-consumer audit) + the indirect trace-hook twin on both backends. 6-STEP BUILD ORDER + full mapping/bindings/ledger = tracker CEIR-20b row (AUTHORITATIVE). **STEP 1 ✅ 2026-08-17** (frame schema + `to_ceir_frame` work-pass mapping — 3 kExecWork* ids + kKindTable rows + pass_is_work + builtin_executor_name; 2 `[ceir20b]` round-trip tests + full [framecook] 3070/99 green, zero regressions). **STEP 2 ✅ 2026-08-17** (`build_work_ceir` — NEW engine/ceir-gpu/work_build.{hpp,cpp}: WorkBuildDesc → emit queue_alloc + produce/consume/compact → find_work_misuse verify; the build_fullscreen_ceir mold, generic machinery. 2 `[ceir20b]` tests + full ceir-gpu 640/43 green, zero regressions). **STEP 3 ✅ 2026-08-17** (`LoweredKind::DispatchIndirect` widen + 5-consumer typed-reject audit + lower_region maps work ops: produce→Dispatch, consume/compact→DispatchIndirect, queue_alloc→nothing; build_work_ceir lowers to out_plan. GREEN win-debug + linux-gcc-debug, [ceir20b] 12/3 + full ceir-gpu 647/44 + gcc `-Werror=switch` CLEAN). **STEP 4 PART 1 ✅ 2026-08-17** (host-only: `WorkHooks{kernel_bytes,dispatch,dispatch_indirect}` + `validate_work_lowered` + `execute_work_lowered` in execute.{hpp,cpp} — the execute_rt_lowered mirror; produce→DIRECT hook [const grid], consume/compact→INDIRECT hook [DEVICE reads the queue count via vkCmdDispatchIndirect, host never sizes]. ⛔ TWO self-caught divergences (advisor-reconciled): (1) host-read sizing → device-indirect; (2) RtHostBinding HOST-SPAN bindings → DEVICE-RESIDENT named buffers [WorkBufferHandle=u64 + WorkResolvedBinding; the queue is a DESCRIPTOR the producer writes; build_work_ceir exposes a WorkAssetResources out-param for the caller's table]. GREEN win-debug + linux-gcc-debug, [ceir20b] 31/5 + ceir-gpu 666/46 + gcc `-Werror=switch` clean). PART 2 (device wiring) IN PROGRESS: ✅ the 2 authored .ckir smoke kernels (assets/ckir/work_smoke_{produce,consume}.ckir — produce writes queue[0]=N device count; consume `atomicAdd(out[0],1)` per invocation → out[0]==N proves the indirect dispatch is DEVICE-count-sized; the queue is STORAGE|INDIRECT, its (count,1,1) header = the dispatch_indirect args). ✅ the DEVICE SYNC MODEL (advisor ×3): SINGLE-submit + REPLAYED barriers (the FFT precedent — the device-resident model made barriers NON-inert unlike 19c's host-span mirror); `WorkBarrierFn` hook + execute_work_lowered replays inter-stage hazards + the C5 `(queue,ShaderWrite→IndirectRead)` before each indirect dispatch; hooks RECORD into a caller-owned recorder. GREEN host-only both configs, [ceir20b] 33/5 + ceir-gpu 668/46. ✅ **VULKAN DEVICE SMOKE GREEN — Win Vulkan + lavapipe** (test_work_smoke_vulkan.cpp: the authored .ckir compile+validate; execute_work_lowered on a REAL device; out[0]==5 = consume ran N=DEVICE-count invocations [indirect grid device-sized, never host]; 20 assertions each). ✅✅ **STEP 4 DONE — execute_work_lowered PROVEN ON-DEVICE, BOTH BACKENDS** (Win Vk + lavapipe + DX12; test_work_smoke_{vulkan,dx12}.cpp — the ceir.work fallback executor runs an AUTHORED device-generated-work program: produce writes the device count, INDIRECT consume device-count-sized, out[0]==5 on all 3 devices; the DX12 IndirectRead→INDIRECT_ARGUMENT + ExecuteIndirect signature already existed). **⭐ STEP 5 DESIGN-LOCKED (advisor, 2026-08-17) — SIMPLIFIED (scope cut):** (0) device-resident RT CUT from 20b — the RT stages KEEP the 19c host-span trace_dispatch (device-indirect proven on COMPUTE step 4; RT device-resident LEDGERED). ⛔ honest scoreboard: NEVER "the wavefront is device-indirect" — RT stages host-span. (i) THE COOK = `extract_work_desc(FrameGraphDesc&, WorkBuildDesc&)` in frame-cook: CounterBuffer resource=a queue (else binding), 0/≥2 counters=named cook error. (ii) NO new RT verb — the wavefront hooks wrap the 19c trace_dispatch (submit+wait inside) + compute for compacts. (iii) PER-STAGE submits (RT+compute=different contexts; WorkBarrierFn=nullptr, coherence via host round-trip). NO kernel edits (19c layouts reuse verbatim). ✅ LOWERING FIX DONE (work.compact→Dispatch serial; only work.consume→DispatchIndirect; [ceir20b] 33/5 + ceir-gpu 668/46 both configs + Vulkan device re-green). ⭐⭐ REFINED MAPPING (advisor impedance reconcile — 19c per-buffer layout ≠ 20a queue-with-count): trace=`compute.dispatch` (plain grid, no queue); compact#1=PRODUCE (%queue=`count` CounterBuffer WRITTEN, binds hit_flags r/compacted w); shade=CONSUME (%queue=`count` READ, binds compacted/rays/hit_t r + decisions w); compact#2=PRODUCE (next_count). work.compact UNEXERCISED by the wavefront (lower/execute-tested host-side; device consumer=20c parallel-compaction, LEDGERED — no fake relabel). Counter rule: produce=1 counter WRITTEN, consume=1 READ. Operand-order vs SSBO-slot dissolves at the HOOK (fixed per-kernel binding table keyed by kernel symbol; extract_work_desc guarantees identity not order). MIXED frame (1 compute + 3 work passes; extract skips non-work). Scoreboard: "wavefront exercises produce+consume on-device; work.compact host-side, device=20c ledgered". ✅ EXTRACTOR DONE (extract_work_desc in frame-cook/frame_work.{hpp,cpp}: CounterBuffer=queue [deduped], else binding; WorkQueueNotOne cook-error; 2 `[ceir20b]` frame-cook tests + full 3083/101 both configs + gcc clean). ✅ ASSET DONE (assets/frame/wavefront_work.frame.toml AUTHORED — MIXED: trace=compute, compact1/2=work.produce, shade=work.consume; count/next_count=counter queues; rays/cont_flags=external inputs; REUSE wavefront_*.ckir. + the @OUTPUT RELAXATION for headless frames [no raster → no @output]). GATE GREEN both configs: a `[ceir20b]` test PARSES the shipped asset (headless) → extract_work_desc → 3 work stages → build_work_ceir find_work_misuse-clean (the mandate-#1 proof at the COOK layer). NEXT tick = the STEP-6 DEVICE GATE (reuse the 19c wavefront harness [test_vulkan_rt.cpp [ceir19c]], swap the C++ while-loop for parse→extract_work_desc→build_work_ceir→execute_work_lowered w/ 19c-style hooks [per-kernel SSBO binding table keyed by kernel symbol; RT trace_dispatch for trace/compact/shade — real TLAS trace/shade, dead compact; consume host-reads count; barrier=nullptr per-stage submit]; the gate provisions the TLAS + host arrays + the oracle; decision-hash == 19c triple32 oracle + [0,1,0,1] pin, Win Vk + lavapipe + DX12; ⛔ advisor at the gate before declaring 20b done). NEXT tick = step 4 PART 2 (wire Vulkan + DX12 WorkHooks lambdas over the RT contexts + an INDIRECT trace-dispatch verb + a minimal on-device work smoke on Win Vk + lavapipe + DX12). ⛔ L1 (rt.trace/rt.sbt_build lowering) RULED OUT of CEIR-20 (advisor) — a future RT-execution band. ⛔ The 19-band decisions (verb / reads-first / whitelist / plan-cook-operand) are SETTLED. **✅✅✅ CEIR-20b DONE 2026-08-17 (advisor at the gate, "real and stands") — the AUTHORED wavefront_work.frame.toml DRIVES the device wavefront, the §134 C++ `while(count>0)` host loop RETIRED (mandate #1 at the execution layer).** STEP 6 = a `[ceir20b]` device gate in test_vulkan_rt.cpp + its DX12 twin in test_dx12_rt.cpp (19c HAS a DX12 RT wavefront gate → both-backends mandate, not Vulkan-only): parse the shipped asset → extract_work_desc → build_work_ceir → the plan; drive trace via the harness, then execute_work_lowered over the 3 work stages (WorkHooks wrap the 19c host-span trace_dispatch; hooks reorder descriptor→SSBO by a fixed per-kernel table); decision-hash == 19c triple32 oracle + [0,1,0,1] + hit_count==4 + shade_gx==hit_count. GREEN Win Vk + Win DX12 (53 assertions each) + both TUs tidy-20 clean. ⛔⛔ SELF-CAUGHT at the gate: `op_name` is DIALECT-QUALIFIED (`"work.consume"` not `"consume"`) — the mismatch dispatched the compact kernel for shade, and the zero-init readback made a PLAUSIBLE partial pass (scar written). ⛔⛔⛔ HONEST SCOREBOARD: (a) the consume grid is HOST-READ in this fallback — NEVER "device-indirect" (device-indirect was proven on the COMPUTE surface, step 4; RT stages host-span, ledgered); (b) the asset drives the WORK-STAGE SEQUENCE — the trace is run by the HARNESS at the asset-declared position (a live-frame executor that runs compute+work uniformly does not exist yet, ledgered). Local close: Win Vk + DX12 gates + tidy GREEN; linux-gcc-debug WSL sweep DONE — gcc `-Werror` compiled the gate TU + the [ceir20b] gate ran/skipped green on lavapipe. ⛔ the sweep CAUGHT a real regression MSVC hid: 3 prior-tick 20b TEST_CASE names had non-ASCII (`→`/`—`) → FIXED (`->`/`--`), check re-green, [ceir20b] 14/14 win-debug re-green. The other 5 sweep reds are PRE-EXISTING/env (verified): no-malloc-allocator is red on HEAD (44 refs in committed test_plan.cpp — repo-wide debt, CI's job); no-std-transcendental = 0 real hits/scene-render (20b touches none); 3 dsp-spectral SEGFAULTs = the llvmpipe multi-thread flake. commit ⑮. **NEXT: band-review PAUSE for the human, then CEIR-20c (ONE modern device lowering — DGC or D3D12 Work Graphs).** **[2026-08-17 MALLOC SWEEP — user directive "fix them all"]** 718 `MallocAllocator` refs → `GrowableTlsfAllocator` (the endorsed unbounded, lazy-commit, default-ctor drop-in) via a verified script mirroring `check_no_malloc_allocator.sh`'s exclusions (engine/memory + the 3 allocator-test files + marked lines); host_provider's 3 per-iteration interpreter scratches hand-tuned to a 1 MiB chunk. `crd-no-malloc-allocator` GREEN; full win-debug build clean; [ceir20b] 14/14; full win-debug ctest 6466/6468. ⛔ opgen-drift was MINE (the sweep edited GENERATED smoke tests tests/ceir/generated/*_gen_smoke.cpp) → FIXED at the SOURCE (tools/ceir_opgen/ceir_opgen.py template: MallocAllocator→GrowableTlsfAllocator + growable include; regenerated; `--check` PASS). The other ctest red = `crd-no-std-transcendental-check` (scene_renderer.cpp:6980 `std::pow`) = PRE-EXISTING (unmodified by me, fails on HEAD; a 1-line crd::math::pow fix, NOT touched — scene_renderer mandate + out of scope; flagged for the user). ⭐ LINUX (gcc -Werror + lavapipe) CONFIRMS: malloc + opgen-drift + non-ascii + [ceir20b] all GREEN; only the pre-existing transcendental + the llvmpipe dsp-SEGFAULT flake fail (identical to the pre-work baseline). Changeset = 2 commits (CEIR-20b feature + the repo-wide malloc-sweep cleanup). ⛔⛔⛔ SCAR: the repo is HAND-FORMATTED — running clang-format -i reflows ~2000 lines/pristine file (no CI format gate); I did it by mistake, caught + fully recovered (revert-to-HEAD + re-sweep + re-splice additions → additions-only diffs). See [[feedback_project_is_hand_formatted_never_run_clang_format]]. **[20c APPROVED 2026-08-17 — user OK'd the recommendation]** ceir.work IS a work-graph IR → gold standard = **D3D12 Work Graphs (SM 6.8, 20c-1)** + **VK_EXT_device_generated_commands (ratified cross-vendor, 20c-2)** + keep the 20b compute-indirect fallback = one program, three lowerings. Also took the scene_renderer.cpp:6980 `std::pow`→`crd::math::pow` fix (transcendental check GREEN, compiles) — a determinism fix, NOT staged in A/B (its own tiny commit C or fold into B). **⭐ 20c-1 DESIGN-LOCK (advisor, 2026-08-17):** FEASIBILITY CONFIRMED — RTX 4070 Ti SUPER + recent NVIDIA driver run Work Graphs 1.0 on REAL HW (no WARP caveat); WinGet DXC 1.9.2602.17 compiles `lib_6_8` broadcasting nodes; the backend's default dxcompiler.dll (Vulkan SDK, cs_6_5/lib_6_3) is NOT WG-capable. BUILD ORDER: **(1)** fetch Agility SDK (`Microsoft.Direct3D.D3D12`, verify it gates `D3D12_FEATURE_D3D12_OPTIONS21` WorkGraphsTier) + a WG-capable DXC redist; `D3D12SDKVersion`/`D3D12SDKPath` exports live in the EXE + `D3D12Core.dll`→`<exe>/D3D12/`; ISOLATE in a NEW test target (don't silently switch every dx12 test's runtime); `supports_work_graphs()` cap query off OPTIONS21. **(2)** SEMANTIC MAP = the **grid-launch-record** mapping (NOT full record-payload flow): the producer node emits a broadcasting-launch record carrying the dispatch grid (from the count it computed); the consumer's `SV_DispatchGrid` comes from the record; DATA stays in the explicit named buffers, SAME .ckir kernels. ⛔ SCOREBOARD: "20c-1 = GPU-SCHEDULED launch via grid-launch records (removes 20b's host submit boundary — GPU self-schedules the consumer); record-PAYLOAD flow = `work.record`'s future slice, ledgered." **(3)** mandate #1: the node ABI wrapper (record structs, `[NodeLaunch]`, `SV_DispatchGrid`) is COOK-EMITTED boilerplate around the ckir-emitted kernel body — a node-emission mode beside `emit_compute_kernel_hlsl`, NEVER a hand-written .hlsl. **(4)** GATE = the COMPUTE smoke (work_smoke_produce/consume.ckir → nodes → `DispatchGraph` → out[0]==count on the 4070 Ti), NOT the wavefront (its RT stages are host-span, can't interleave in one DispatchGraph — the 20b RT-device-resident ledger holds). **(5)** EXECUTOR consumes the WorkBuildDesc/ceir.work MODULE (graph = a STRUCTURE built once, `DispatchGraph` = one command), NOT the flattened LoweredCommand plan; `WorkGraphHooks` caller surface (ceir-gpu names no backend). Mold: declare→author→device-gate→close, advisor at the gate. commit ⑯. **⭐⭐ 20c-1 DEVICE GATE GREEN on the 4070 Ti (2026-08-17):** the AUTHORED work_smoke_{produce,consume}.ckir run as a real D3D12 WORK GRAPH — `emit_work_graph_node_hlsl` + `emit_work_graph_library_hlsl` (cook: .ckir→node HLSL, union-deduped decls, mandate #1) → `compile_work_graph_library_to_dxil` (lib_6_8, the factored compile_dxil_core) → `Dx12WorkGraphContext::dispatch_graph` (state object + WORK_GRAPH subobject + ID3D12WorkGraphProperties backing store + `DispatchGraph`, system d3d12.h/OS runtime, NO Agility SDK) → out[0]==count==5. ⭐ the PRODUCER writes the (count,1,1) header + emits a grid-launch record; the GPU SELF-SCHEDULES the CONSUMER sized by it — removes 20b's host submit boundary. Isolated test target (crd-gpu-context-dx12-work-graph-tests) copies the WG-capable Windows-SDK dxc next to its exe (advisor isolation). Structural kir tests + the device gate GREEN. ⛔ SCOREBOARD: GPU-SCHEDULED launch via grid-launch records; record-PAYLOAD flow = work.record (ledgered). ✅ 20c-1c DONE — `build_work_graph_plan` (ceir-gpu, header-only) derives the produce→consume topology from the authored WorkBuildDesc (the SAME desc the 20b fallback consumes — one program, two lowerings); the device gate now drives THROUGH the plan (module-driven, not hardcoded), out[0]==count on the 4070 Ti. compile_dxil_core refactor blast-radius clean (19c DX12 green). **✅✅ CEIR-20c-1 (D3D12 Work Graphs) FULLY DONE + gold-standard: cook (.ckir→node HLSL, dedup library) → module-driven topology → state object + DispatchGraph on-device. commit ⑯.** NEXT = 20c-2 (VK_EXT_device_generated_commands, cross-vendor) — the SAME ceir.work desc's consume → a device-generated indirect dispatch, reusing the 20b .ckir compute kernels; on-device gate on the 4070 Ti = one program, THREE lowerings (20b fallback + 20c-1 Work Graphs + 20c-2 DGC). **✅✅ CEIR-20c-2 (VK_EXT_device_generated_commands, CROSS-VENDOR) DONE 2026-08-17 (advisor at the gate) — the AUTHORED ceir.work smoke runs as a real DEVICE-GENERATED COMMAND STREAM on the 4070 Ti.** THIRD lowering of ONE ceir.work program: **(a)** device enablement — `m_dgc_ext` + `device_generated_commands_ext()` on VulkanGpuContext (mirrors the NV `m_dgc` wiring): detect VK_EXT_device_generated_commands + VK_KHR_maintenance5 (the preprocess buffer's 64-bit usage flag), chain the EXT feature + maintenance5 + BDA when advertised, query DGC-EXT PROPERTIES + require COMPUTE in supportedIndirectCommandsShaderStages, add both to devexts. **(b)** `VulkanDgcContext` rig (engine/gpu-context-vulkan, header+cpp pimpl, the VulkanRayTracingContext mold): `dispatch_generated(produce_spirv, consume_spirv, bind maps, token/count indices, max_seq, buffers)` — a lone DISPATCH-token indirect commands layout, `indirectExecutionSet=VK_NULL_HANDLE` + the consume pipeline via `VkGeneratedCommandsPipelineInfoEXT` in pNext (the spec-MINIMAL path — the INDIRECT_BINDABLE flag is execution-set-ONLY, VU-confirmed from validusage.json, so a PLAIN pipeline + no metadata machinery), preprocess buffer via `VkBufferUsageFlags2` (maintenance5), LEGACY `COMMAND_PREPROCESS` barrier (sync2 is off on the headless device), `vkCmdExecuteGeneratedCommandsEXT`. **(c)** AUTHORED `assets/ckir/work_smoke_produce_dgc.ckir` (mandate #1): token@0 = five (1,1,1) VkDispatchIndirectCommand payloads (15 u32=1), count@1[0]=5; REUSE `work_smoke_consume.ckir` VERBATIM (count buffer doubles as its queue@0 → out[1]==5). **(d)** the device gate (tests/ceir-gpu-vulkan/test_vulkan_dgc.cpp): load→emit GLSL→SPIR-V→`build_work_graph_plan` (MODULE-DRIVEN — the plan's kernel symbols SELECT which SPIR-V plays produce/consume, not hardcoded)→`dispatch_generated` → **out[0]==5 (FIVE device-generated dispatch commands each ran one invocation), out[1]==5 (count read), count_rb[0]==5, token_rb[0]==1 on the 4070 Ti.** ⛔⛔⛔ HONEST SCOREBOARD (three lowerings of ONE ceir.work program): **20b = host-recorded dispatch + DEVICE-supplied grid** (vkCmdDispatchIndirect) · **20c-1 = GPU-SCHEDULED node launch** (D3D12 Work Graphs grid-launch record) · **20c-2 = DEVICE-GENERATED command STREAM** (the produce writes the sequence COUNT + every dispatch PAYLOAD; the host records ONLY ExecuteGeneratedCommands — the thing a single dispatch-indirect literally cannot express: N distinct commands). ⛔ SELF-CAUGHT: the first produce_dgc.ckir used ids v1/i0..i14 → `ckir_read` REJECTED (noderef resolves refs by POSITIONAL `n<pool-index>`, the id string is IGNORED) → renamed n0..n18, [[feedback_ckir_node_refs_are_positional_n_index_id_ignored]]. ⛔ ADVISOR AT THE GATE caught + I FIXED a source-vs-scoreboard mismatch: the "module-driven" claim was DECORATIVE (plan computed then dropped) — now the plan's kernel symbols select the SPIR-V (the 20c-1 is_prod resolution mirrored). Blast radius GREEN: RT-1 inline rayQuery Vulkan + 20b ceir.work Vulkan executor (shared device-creation/BDA/feature chain) both PASS; per-slice clang-tidy-20 CLEAN (5 files); **linux-gcc-debug WSL sweep — gcc `-Werror` compiled the WHOLE tree incl. the DGC rig + gate TU (exit 0), the mandatory cross-platform leg**. commit ⑰. **✅✅✅ ONE ceir.work program, THREE device lowerings (20b fallback + 20c-1 D3D12 Work Graphs + 20c-2 VK_EXT_DGC) — CEIR-20c COMPLETE.** **✅✅✅ CEIR-20z DONE 2026-08-17 — the CEIR-20 BAND is CLOSED (band-scope × 3 legs re-run green; zero-builder audit; 7-item ledger homed with work.compact re-homed off 20c; the AUTHORITATIVE record is `docs/sessions/2026-08-17-ceir-20z-band-close.md`; commit ⑱ docs-only). ⭐ NEW: the VK_EXT_DGC lowering RAN on Mesa lavapipe (2nd-vendor device proof beyond the 4070 Ti). **✅ CEIR-21 (`ceir.shape`/`ceir.tensor` high-level tensor IR) BAND-OPEN→CLOSED 2026-08-18 — [historical build narrative for 21a–21z follows; the ✅✅✅ CEIR-21 close pointer above + tracker CEIR-21 row + session log are AUTHORITATIVE]** — advisor design-locked + §21/§22/§35/§51/§70 + hesap-tensor/dtypes + hesap-dense/layout READ; the value-op dialects over the 3d tensor TYPES; crd-ceir-only, declare-only (Pure + typed NoSemantics, ⛔ NO kernel_ref/lowering — §70 native-graph-whole). SUB-SLICES 21a `ceir.shape`(§35) / 21b `ceir.tensor`(§51 structural-six: elementwise/broadcast/reshape/transpose/reduce/matmul) / 21c §22 optional layout / 21z composing gate (chained tensor workflow, 12z mold). ⛔ BOUNDARY STRIKES: sparse-encoding + quantized/fp8 → CEIR-23 (3d 'sparse encoding is CEIR-21' struck in place); FFT/solve→22. Detail = tracker CEIR-21 row (AUTHORITATIVE). **✅ 21a DONE 2026-08-18** (`ceir.shape` declared: 6 PURE ops + `find_shape_misuse` [14 kinds: operand/result KIND + RESULT-IDENTITY + the 3d predicates WRAPPED w/ tri-state Unknown→ACCEPT]; ×4-config gate GREEN re-run after the advisor-at-gate fixes [4th config + result-identity + Unknown-branch test + gen-smoke]; commit ⑲). **✅ 21b DONE 2026-08-18** (`ceir.tensor` structural-six: elementwise/broadcast/reshape/transpose/reduce/matmul + shape-aware `find_tensor_misuse` [16 kinds] + `Context::shapes_broadcast_result`; ×4-config gate GREEN re-run after the advisor-at-gate fixes [tensor.broadcast negatives+Unknown; a matmul BATCH-dim FALSE-GREEN → new BatchMismatch kind; a min-arity fold guard]; commit ⑳). **✅ 21c DONE 2026-08-18** (`ceir.layout` §22: ONE Pure op `layout.constrain(%tensor){kind,params}` — advisor-locked as a CONSTRAINT OP, NOT a tensor-type member [§23 tensor<S,E,L> = a Dxxx named-forward]; `find_layout_misuse` 8 kinds [Tensor-kind, passthrough identity, kind vocab, kind-gated strides/block/swizzle, arity vs rank, min-arity fold]; ×4-config gate GREEN re-run after the advisor fixes [arity-0 param-skip leak → min-arity guard; rank≠2 tests; §22 citation]; commit ㉑). **✅✅✅ CEIR-21 (`ceir.shape`/`ceir.tensor`/`ceir.layout` high-level tensor IR) BAND CLOSED 2026-08-18** — THREE value-op dialects over the 3d tensor TYPES, DECLARE-only (Pure + typed NoSemantics, ⛔ NO kernel_ref — §70 keeps the graph WHOLE for a native backend); crd-ceir gained ZERO gpu-context edges, no render-technique builder. **21z COMPOSING GATE** (`tests/ceir/test_band21_gate.cpp`, the 12z mold): ONE C++ module threads all three dialects — shape.make/rank + a chained rank-1 row tensor → broadcast UP → layout.constrain{blocked} → elementwise → transpose → matmul → reshape → reduce-to-RANK-0 (§21 `!shape<>`, the 21b-deferred case) — PROVING the band property: `find_shape_misuse` + `find_tensor_misuse` + `find_layout_misuse` + `find_structure_error` ALL None on the ONE module (the verifiers COMPOSE) + ⭐ the FIRST serializer crossing for these ops (text round-trip print→parse→re-walk + print==reprint byte-exact AND binary serialize→deserialize→re-walk all-None). BAND-CLOSE re-run ×4 configs GREEN (win-debug/asan 9 hand + 51 gen-smoke; linux-gcc-debug/asan 60 REBUILT 74/74) + opgen-drift/validator + `crd-ceir-invariants` + gcc `-Werror=switch`. ⛔ reduce/matmul DETERMINISM (order-sensitive float accumulation) documented in the TOML → opgen-regen drift-clean → the bit-exact contract is CEIR-22's oracle (ledger L9); §23 layout-in-type = a Dxxx declared divergence; 10-item deferral ledger each homed. AUTHORITATIVE: `docs/sessions/2026-08-18-ceir-21z-band-close.md` + tracker CEIR-21 row ✅. ⛔ advisor-at-the-gate at EVERY sub-slice caught real defects (21a result-identity gap; 21b matmul BATCH false-green; 21c arity-0 leak) — all fixed + re-verified, never inherited. commit ㉒. **▶ CEIR-22 (CRD-Hesap / numerical GPU integration) BAND-OPEN 2026-08-18 (advisor design-locked)** — GEMM→FFT→reduction→viz-prep as ONE oracle-gated CEIR asset, no CPU round-trip (§52/§59/§93/§137). ⛔ SCOREBOARD CORRECTION: there is NO `hesap-gpu` module — the GPU substrate is **CKIR** (`crd-kir`: GEMM=`KOp::Contract`+`ckir_tile`, FFT=`build_fft*`, reduce=`build_reduce*`); the bit-exact oracle is **`eval_cpu`** (discharges CEIR-21 L9); hesap-dense/hesap-fft CPU = an INDEPENDENT cross-oracle. SCOPE = declare + native-provider EXECUTION + oracle (the first band a CEIR tensor graph RUNS on-device). Mandate #1: CKIR `build_*` = the §52 native-provider MECHANISM; the provider is a GENERIC per-op mapping + the asset drives the stage ORDER. Sub-slices 22a (declare) / 22b (the CEIR→CKIR native provider in ceir-gpu) / 22c (the §137 proof asset — text FILE, no CPU round-trip, oracle w/ declared ULP bound, memory-plan + profiling) / 22z (close). **✅ 22a DONE 2026-08-18** — `ceir.linalg.gemm` (BLAS3 identity, DISTINCT from 21b matmul) + `ceir.tensor.fft` (c2c SPLIT re/im, the FIRST fixed 2-result op — round-trips BOTH serializers byte-clean → unblocks 22c's text asset) DECLARED + `find_linalg_misuse` (9 kinds) + the fft branch of `find_tensor_misuse`; gated ×4 configs + gcc `-Werror=switch` + tidy-20 (6 files) + opgen-drift (advisor caught + I fixed 3: ceir.21 inherited-green re-run, trans_b zero-coverage, the multi-result serializer round-trip). ⛔ 22b CONTRACT: the provider rejects non-F32 with a test. commit ㉓. **✅ 22b DONE 2026-08-18** — the CEIR→CKIR NATIVE PROVIDER (`ckir_synth.hpp/cpp`): each declare-only op → a CKIR construction (synth_gemm/reduce = GRAPH tier via `KirBackend::run` vs `eval_cpu`; synth_fft = KERNEL tier via `dispatch_kernel_1wg` vs `eval_cpu_kernel`); `crd-kir` = a PUBLIC header-only edge (ceir-gpu backend-free — its first crd-kir consumer). 12 typed-rejects (bit-exact envelopes; F32-only — the 22a contract discharged). **6 device gates** (gemm/reduce/fft × Vk/DX12) bit-exact/declared-tol vs the tier oracle AND an INDEPENDENT ref (triple-loop/4×serial/naive-DFT — the 20c-2 guard). MATRIX ×3: win 4070 Ti Vk+DX12 (10/10) + lavapipe Vk (7/7) + gcc-switch + tidy(9) + invariants + inherited-greens re-run. ⛔ real bug the device gate caught: the fft radix-dispatch HANG (build_fft1d_batched→radix8 vs radix-2 contract → 1500s → build_fft1d_radix2 + a local_size pin; [[feedback_ckir_fft_batched_radix_dispatch_breaks_fixed_twiddle_contract]]). advisor-at-gate fixed 3 (reduce-4fn, fft-fwd+inverse, inherited re-run). commit ㉔. 22c-1 DONE (plan_tensor_pipeline — the PURE device-free memory plan; ㉕) → 22c-2 DONE (execute_tensor_pipeline + ⭐ the sec-137 PROOF gate GREEN on the 4070 Ti win-debug+win-asan: an authored gemm→reshape→fft→reduce module PLANNED + EXECUTED as ONE device-resident submit [graph-tier gemm/reduce + kernel-tier fft into ONE recorder, reshape a zero-copy alias] vs a composed triple-loop→naive-DFT→serial-sum ref within a derived tol; ㉖). ✅ **22c-3 COMPLETE** (3a plan-VizDispatch+Output-rule-fix / 3b two viz .ckir + eval oracle / 3c the authored `tensor_pipeline.ceir` + parse-load gate / 3d the parse-loaded 6-stage Vulkan §137 proof + per-stage oracle + determinism / 3e DX12 leg / 3f lavapipe leg) — the §137 charter PROVEN: ONE authored `.ceir` asset (gemm→fft→mag(CKIR)→reduce(max)→normalize(CKIR)) planned + executed DEVICE-RESIDENT as one submit, CROSS-VENDOR (4070 Ti + DX12 + Mesa lavapipe), win-debug + win-asan, vs independent per-stage f64 refs. ✅✅ **CEIR-22 BAND CLOSED (2026-08-18)** — the §137 numerical-workflow-through-CEIR proof stands (§137 GEMM→FFT→reduction→viz as ONE parse-loaded `.ceir` asset, device-resident cross-vendor [NVIDIA Vulkan + D3D12 + Mesa lavapipe], memory-plan + PROFILING + per-stage numerical oracle + determinism). 22c-3g profiling gate + 22z fresh sweep (win-debug 29 / win-asan 27 / win-release 26 [+ a latent dialect.cpp /WX catch] / lavapipe 25) + zero-builder audit + deferral ledger. Close doc: docs/sessions/2026-08-18-ceir-22z-band-close.md. commits ㉖+㉗. **CEIR-23 BAND-OPEN LOCKED** (advisor 2026-08-18) — `ceir.quant`+`ceir.sparse` (§53/§54); decomposition 23a quant-declare → 23b quant-provider+Q8-gates → 23c the quantized-MLP CROWN (cross-backend) → 23d sparse-declare → 23e sparse-SpMV gate → 23z. Locked: NO new TypeKind (quant=quantize/dequantize ops, scale/zp=operands, storage rides result); sparse=explicit-triple `spmv(row_ptr,col_idx,values,x)` CSR-only; hesap=oracle, kernels=.ckir; dequant→gemm fusion IS 23's job. ⛔ BAND#=tracker-auth (23=sparse/quant, NOT the old-roadmap optimizer). ✅✅✅ **CEIR-23 (`ceir.quant` + `ceir.sparse`, §53/§54) BAND CLOSED 2026-08-18** — quantization + sparse linear algebra as first-class CEIR (declared dialects + verifiers + authored assets + cross-vendor device proofs). **23a** `ceir.quant` declared (quantize/dequantize; scale/zp operands; storage rides result; no new TypeKind) + `find_quant_misuse`. **23b** the unfused Q8 dequant `.ckir` (u32-pack + float sign-extend) + the **fused `dequantize→gemm` `StageKind::QuantGemm` collapse** (a PLAN 2→1 collapse; the ONE shared `fusable_dequant_into_gemm_weight` predicate gates single-use ∧ weight-slot ∧ symmetric ∧ rank-0 ∧ α=1,β=0,no-trans + a typed reject) + the 155-node `quant_gemm_q8.ckir` + Vk/DX12 gates (fused == unfused == float oracle). **23c** the quantized-MLP CROWN: `assets/ceir/quant_mlp.ceir` parse-loaded (QuantGemm→relu→QuantGemm) == a float MLP oracle bit-exact on Vk+DX12+lavapipe; `relu.ckir`; a standalone `quantize_q8_0` rounding cross-check. **23d** `ceir.sparse` declared (`sparse.spmv`, CSR-only, explicit-triple operands, no new TypeKind) + `find_sparse_misuse` (7 kinds) + the For-loop runtime-bound micro-gate. **23e** the CSR SpMV `assets/ckir/spmv_csr.ckir` — the FIRST CKIR asset with **data-dependent control flow** (runtime `For` + per-thread `ForBreakIf` + `col_idx` indirection) — proven on Vk+DX12+lavapipe vs a CPU CSR SpMV ref. **23z** the band23 composing gate (quant+sparse verify + TEXT AND BINARY round-trip byte-clean) + the fresh 5-leg sweep (win-debug/release/asan + WSL-gcc + lavapipe) ALL GREEN incl. the divergent-loop SpMV on llvmpipe; zero-builder audit clean (SpMV bootstrap DELETED; 6 assets each with a reading gate). AUTHORITATIVE: `docs/sessions/2026-08-18-ceir-23z-band-close.md` + tracker CEIR-23 row ✅. Scars written: [[feedback_ckir_eval_for_bound_is_uniform_use_max_plus_forbreakif]] (the lockstep For finding) + [[feedback_fusion_and_specialized_kernel_selection_must_check_full_semantic_attrs_not_just_structure]]. **NEXT = CEIR-24 (`ceir.ml` + provider partitioning → MLP + attention via CKIR AND a native-provider strategy; advisor at band-open). Drive to CEIR-35.** **[2026-08-18 ▶ CEIR-24 IN PROGRESS]** — **24a DONE**: `ceir.ml` DECLARED — `ml.mlp` (x + VARIADIC per-layer weights + closed-vocab `activation={relu}`) + `ml.attention` (single-head SDPA `out=softmax(Q·Kᵀ/√D)·V`); `find_ml_misuse` (13 kinds: Tensor+Float+equal element, rank-2, the mlp WIDTH CHAIN, activation vocab, the attention Q·Kᵀ·V shape relations); 2 hand gates + 3 gen smoke + opgen-drift green; full ceir suite 775/775, tidy clean. **24b ACTIVE** (advisor-locked): the CKIR EXPANSION provider (ceir-gpu, per-op) — `ml.mlp`→FLOAT gemm/relu chain (quant path name-forward, proven 23c), `ml.attention`→`transpose.ckir(K)`→plain `gemm(Q,Kt)`→`softmax.ckir`→`gemm(·,V)`. ⛔ gemm stays PLAIN (trans_b AND α≠1 both typed-rejected) → the 1/√D scale rides as a CALLER-UPLOADED 1-elem f32 buffer input to `softmax.ckir` (the `quant_mlp.ceir` dequant-scale precedent; spec-const baking name-forward — VizDispatch carries no spec-const). KOp::Exp confirmed wired eval+both emitters. Sub-slices 24b-0 (crux read) ✅ → 24b-1 transpose.ckir ✅ → 24b-2 softmax.ckir ✅ → 24b-3 expand_ml_ops pass ✅ → 24b-4 Vk+DX12 gates ✅. **[2026-08-18 ✅ 24b-4 CROWN]** the expanded ml.attention (transpose→gemm→softmax→gemm) AND ml.mlp (gemm/relu) run DEVICE-RESIDENT on Vulkan AND DX12 (4 gates green), matching the CPU SDPA / float-MLP oracles within derived tol — the §138 ML proof headline cross-backend, ZERO new StageKinds (the existing execute_tensor_pipeline). 24b-5 ✅ authored assets/ceir/attention.ceir (single-head SDPA) + parse-load gate (anti-drift vs the C++ builder + roundtrip + expand + plan) — the faithful "authorable asset" form (the 23c-b quant_mlp.ceir mold). **[2026-08-18 ✅✅ CEIR-24b COMPLETE]** the CKIR EXPANSION provider is proven end-to-end: transpose.ckir + softmax.ckir authored + eval-verified; expand_ml_ops rewrites ml.mlp/ml.attention into the 22/23 vocab (device-free 3 gates); the expanded ops run device-resident on Vulkan AND DX12 (4 gates); the authored .ceir parse-loads + expands + plans. **[2026-08-18 ✅✅✅ CEIR-24c COMPLETE — the §136 heterogeneous-execution crown]** the §69 partitioner (partition_ml: advertise/assign, device-availability as a pure INPUT caps flag, inspectable; coopvec_can_claim_mlp = a FULL-semantic-attr CLAIM check) routes each ml op to a native provider OR the CKIR fallback (24c-1, 3 device-free gates). The coopvec CLAIM conversion (ml.mlp→CoopVecMlpConfig: TRANSPOSE W[in,out]→[out,in] + f32→f16 + zero bias) proven vs the f32 oracle with ASYMMETRIC weights (24c-2a), and the claimed op dispatches NATIVE on the 4070 Ti's VK_NV_cooperative_vector tensor units == the fp16 ref (24c-2b, NVIDIA-gated honest skip). ⭐ THE §136 CROWN (24c-3): ONE ml.mlp region, TWO §69 partition strategies (CKIR-expand f32 on Vulkan vs coopvec-native fp16), the SAME numbers within fp16 tol (both legs pre-rounded through fp16) on real hardware. **[2026-08-18 ✅✅✅ CEIR-24 BAND CLOSED]** — advisor close-lock caught a BLOCKING silent-miscompile (expand emitted the baked relu.ckir local_size=32 for ANY intermediate → uninitialized GPU tail; now a TYPED `BakedKernelShapeUnsupported` reject + negative gates). The band-24 composing gate (ml.mlp variadic + ml.attention, TEXT+BINARY roundtrip byte-clean, partition) green. Sweep GREEN 5 legs: win-debug + win-release 14/14 (/WX) + win-asan 29/29 (no UAF/leak) + WSL-gcc 14/14 (-Werror, first gcc exposure for the 4 new .cpp) + lavapipe 18/18 (transpose/softmax on software Vulkan; coopvec honest SKIP). Zero-builder audit clean (ml kernel bootstraps deleted; expand/partition/coopvec = provider mechanisms). Close record: `docs/sessions/2026-08-18-ceir-24z-band-close.md`; tracker CEIR-24 → BAND ✅. Scar: [[feedback_locked_checklist_item_needs_a_gate_or_it_silently_doesnt_land]]. REMAINING housekeeping: MEMORY.md compaction (<17.1KB) + the 24z memory entries. **[2026-08-18 ▶ CEIR-25 (`ceir.autodiff`) BAND-OPENED — advisor design-lock done; NO code yet; NEXT = 25-0 reads]** — differentiation as a COMPILER TRANSFORM over the CEIR IR (the §56/§57 boundary the ml ops were kept Pure/mode-free for). LOCKED architecture (advisor): `build_gradient(ctx, m, loss, wrt[])` in ceir-gpu differentiates the HIGH-LEVEL vocab (linalg.gemm/tensor.reduce/ml.mlp) BEFORE expansion into ONE module (backward ops reference forward SSA values → planner materializes intermediates as buffers, "store" free, checkpointing=a later recompute transform); backward ops in the EXISTING vocab ⇒ zero new StageKinds; ⛔ `MissingVjp` TYPED reject day-one (unregistered op ≠ zero gradient — the registered-default-empty scar in AD form, negative-gated SAME slice); promote baked transpose.ckir → cook-time `synth_transpose` (gemm VJP dA=dC·Bᵀ needs per-shape transposes — retires the 24z ledger row); oracles = hesap-autodiff `nn_reverse.hpp` (analytic) + `gradient_check.hpp` (central-diff). §139 crown = differentiable COMPUTE; differentiable RENDER = ledger. Sub-slices 25-0 (reads+synth_transpose decision) → 25a (VJP core+registry+MissingVjp) → 25b (synth_transpose + backward-MLP Vk+DX12 vs analytic+numerical) → 25c (custom composite VJP two-strategies + checkpointing) → 25z close. **⚠⚠ COMMIT THE CEIR-24 WORK FIRST** — the transpose/softmax .ckir BOOTSTRAPS ARE DELETED, so the WORKING TREE IS THE SOLE SOURCE of softmax.ckir/attention.ceir (transpose.ckir retired 26d-3c) + the 4 new engine .cpp (ml/expand_ml/partition_ml/coopvec_mlp) + all gates; uncommitted-delete = unrecoverable. Proposed commit msg in `docs/sessions/2026-08-18-ceir-24z-band-close.md`. **HOUSEKEEPING:** MEMORY.md compacted LOSS-FREE (all 231 scar pointers preserved, verified by diff) to 19.7KB; <17.1KB is ARITHMETICALLY INFEASIBLE without dropping ~8 scars (filenames alone=13.4KB) — a USER structural decision (rename slugs / archive render-era scars to a sub-index / raise target); task #116 has detail. ⚠ context.md >300-line cap (bloated across bands) — a prune to the archive is due.
## Prior focus — **[2026-08-16 ✅ CEIR-19b DONE — the AUTHORED HYBRID RT-SHADOW renderer works END-TO-END on THREE devices: Win Vulkan + Win DX12 (DXR) + Linux lavapipe].** GREEN: Win scene-render 1967/90 + frame-cook 2973/96; Linux GCC/lavapipe scene-render 1548/66; tidy clean. ⚠ rides commit ⑦ (rt_worldpos.ckir + rt_shadow.frame.toml + frame_runtime.cpp + the gate; scene_renderer.cpp shares w/ 18a-3). ⛔ SCOPE: the composite is STILL STEP-1 PASSTHROUGH — the RT shadow term is proven via mask READBACK (STAGE 2), NOT yet VISIBLE in the image; the multiply is FILED **CEIR-19b-F2**. Detail → tracker (CEIR-19b ✅). **NEXT (loop) — CEIR-19c:** the ceir.rt→gpu execution bridge / §134 wavefront path tracer (the 19a re-home). ⬇ THE DEBUG THAT CLOSED 19b (three never-run defects, advisor-guided):
The ABSOLUTE gate (`CEIR-19b GATE` ×2 backends: receiver-only raster + TLAS-only OFFSET occluder + baked light(0,8,0); STAGE 1 = worldpos re-projection round-trip (MIRROR-PROOF) + .w validity; STAGE 2 = per-pixel CPU ray-quad vs the RT mask, disagree==0 + class floors). FINDINGS:
- ✅ the never-run COMPUTE prepass single-dispatches fine (draws.count=0 → the params `groups_x` path, NOT the cull-groups-dispatch scar).
- ⛔⛔ **LATENT DEFECT (filed):** the runtime PLAN binds a compute pass's storage slots **READS-FIRST** (rt_constants@binding0, worldpos@binding1) — the OPPOSITE of `frame_template_bridge.cpp` map_compute's writes-first code + comments. light_cull never exposed it (ONE read+write buffer). WORKAROUND: authored `rt_worldpos.ckir` to rt_constants@iidx0 / worldpos@iidx1 (matches the runtime). ⛔ FIX ONE deliberately later: the PLAN → writes-first, OR the bridge+docs → reads-first; note the contract in the .ckir header.
- ✅ DEFECT 2 (FIXED — the depth transient): the `raster.geometry` forward pass was falling to the color's COMPANION depth (frame_graph.cpp:514 REN-40-G3) instead of the DECLARED `scene_depth` D32Float transient → scene_depth was NEVER written (cleared 0) → the compute sampled 0 → worldpos = far-plane garbage. ROOT CAUSE (frame_runtime.cpp:1085): a secondary depth-format write routed to `rec.depth_target` ONLY under `pass_flag(kMrt)`; a plain forward pass declaring a depth transient (for a compute to sample) had it DROPPED. FIXED: gate on `!first_write` (a depth write FOLLOWING a colour primary → depth_target), preserving raster.depth_only's primary. Blast radius clean (scene-render 1967/90 + frame-cook 2973/96, every depth pass).
- ✅ DEFECT 3 (FILED CEIR-19b-F1): lavapipe now RUNS RT (no caps-SKIP) → the composite (shader-object) pass records AFTER the monolithic RT bind → its vkCmdSet* volley trips VUID-vkCmdTraceRaysKHR-None-08608 (graphics-only state, does NOT affect the trace; Win-SDK VVL didn't check it). ⛔ FIX in the encoder's bind-point hygiene (re-set dynamic state / re-bind shader objects on the first graphics pass AFTER an RT bind, both backends). The gate asserts a BOUNDED VUID-string exception (error_count==n_08608, n_08608<=8) so a 2nd class can't hide — REMOVE the exception when F1 lands. ⛔ LAVAPIPE = a live 3rd RT device now → upgrade the 19c/19z Linux ritual.
✅ ALL temp stderr diagnostics STRIPPED (git-verified net-zero on the diagnostic-only engine files) + the VUID-08608 fprintf superseded by the scoped exception.

## Prior focus — **[2026-08-16 ✅ CEIR-18a-3 DONE — the frame-graph pass `technique` DRIVES the forward FS cook (F2 defect CLOSED)].** The forward FS is now cooked from the INSTALLED frame's forward-pass `technique`, TWO-TIER by capability: main frame `requires=["shadows"]` ⇒ csm ← main.forward.technique, fwd ← fallback.forward.technique (forward_basic → standard_forward); main frame flat ⇒ fwd ← main.forward.technique. ⛔ NOT `csm=fwd` unconditionally — that cooked the DEFAULT renderer's FLAT variant as forward_csm (samples an unbound atlas ⇒ black/inverted; the module suite REN-2/REN-3.2-b/REN-38 caught it). Field-less pass ⇒ the C++ setter (byte-identical, no-regression); a GRAPH-named unresolvable technique FAILS (never a silent default). ⛔ `material_pass` is an ENUM (read with pass_u32 vs FrameMaterialPass, NOT pass_str — the first cut read empty + dropped every pass to the setter; the 18a-3 gate caught it). set_frame_graph_toml AND the RAF-11 frame_commit reload now RE-COOK the FS + ATOMIC ROLLBACK on a bad-technique failure (stash prev frame/override, restore + re-cook — never a half-installed frame). forward_csm.frame.toml + forward_plus.frame.toml F2 "aspirational/does nothing" comments STRUCK in place (the field is load-bearing now). REN-37.2 gate reconciled (installs graphs that NAME the technique, zero setters; + (e) asset-outranks-setter inverse pin, (d-cold) first-cook bad-name FAILS, (d-hot) atomic-rollback pin); CEIR-18a-2 Stage 1 + Stage 2a point-light gates reconciled to graph-drives-technique. VERIFIED cross-platform: **Win Vulkan+DX12 (1934 assns / 89 cases, tidy-clean) + Linux GCC/lavapipe (1514 / 65).** ⚠ 18a-3 rides commit ⑥ (SHARES scene_renderer.cpp with 18a-2/18b/18z/19b — surgical hunks). Detail → tracker (CEIR-18a-3 ✅). **NEXT (loop resumes) — CEIR-19b:** the hybrid RT-shadow renderer — all shaders authored (rt_worldpos/rt_composite `.ckir` + 3 scene_rt_shadow_* `.crdv`, all AUTHORED assets, no builders) + the scene_renderer wiring done + compiles; REMAINING = the ABSOLUTE device gate (Vk+DX12, PIN 64×64, caps-SKIP: deterministic receiver+occluder+light(0,8,0), worldpos-vs-CPU-analytic → shadow-mask occluded≈0/unoccluded≈1, val_err==0) + fix every never-run-hybrid defect. Detail → tracker (CEIR-19b 🔄).

## Prior focus — **[2026-08-16 ✅ CEIR-19a DONE + gated 4 configs] — the `ceir.rt` DIALECT is DECLARED: RT orchestration (blas/instance/tlas/sbt build → trace / ray_query) has a typed CEIR identity.** 6 ops from `rt.ceirop.toml`→opgen + hand-written `rt.hpp/cpp` (the scene mold): 3 DISTINCT opaque Extern type-classes (rt.blas/tlas/sbt — load-bearing so `trace` takes %tlas+%sbt, `ray_query` %tlas ONLY) + `find_rt_misuse` type-chain walk (I6-clean). ⛔ ALL 6 ops = native{provider=host, ExternalNondeterminism} + effects[GPUCommand, MemoryReadWrite]; ray_query carries kernel_ref. **NATIVE-vs-device-execute resolved (advisor): all-6-native** — `trace` has no kernel_ref (shaders ride the SBT) so it can ONLY be a host intrinsic; `native`+`kernel_ref` is orthogonal (kernel_ref = cook-time dep + §107 pin, not an execution-tier marker); grep confirmed nothing assumes `kernel_ref⇒not-native`. **VERIFIER GAP-FILL (advisor-caught BLOCKER):** the TOML docs promised enum names + access-arity/operand-type checks the walk didn't implement (thinner than its own `find_dispatch_misuse` mold) → corrected every doc enum name + re-attributed structural claims to the generated verify_*, and IMPLEMENTED the dispatch-shape checks for trace/ray_query (DimNotIndex/AccessTokenInvalid/AccessArityMismatch/BindingNotResource — the mold mirror); AS-build operand-resource-kind DEFERRED to 19b. GATE (80 assertions/5 cases, ALL GREEN): win-debug (full crd-ceir-tests 3429/441 + [rt] + opgen-drift/validator) + win-asan + linux-gcc-debug (`-Werror=switch` clean) + linux-gcc-asan (parse_access ASan-clean) + tidy. crd-ceir NEVER links gpu-context (I3/I4) — DECLARE-only, ZERO renderer changes. ⛔ **NEXT = CEIR-19b BUILD (DESIGN-LOCKED 2026-08-16, advisor at entry + a reconcile round).** The 4 verify-first checks found: RT pass kinds exist (`raytrace.pipeline` F6-proven both backends + `raytrace.dispatch` inline never-run); SVGF is a kernel not a pass (denoise out of the assert path); **the RT frame pass is ALREADY authored-CEIR** (frame.pass op + `ensure_rt_kernel`=cook_stage_named loader over disk `.crdv` + a registered executor — "RT is imperative" was FALSE) ⇒ **Position B: 19b assembles via the existing `raytrace.pipeline` frame.pass; the ceir.rt→gpu BRIDGE is RE-HOMED to 19c** (its real consumer = the §134 wavefront PT). Depth-handoff SOLVED with ZERO executor engine-work: the `compute.dispatch` arm binds a sampled texture (RT arms don't) ⇒ a TWO-pass split (compute reconstructs world-pos from depth → buffer; RT reads buffer+TLAS → shadow mask). AUTHORED HYBRID: raster lit → compute shadow_prepass → raytrace.pipeline rt_shadow → fullscreen composite. BUILD IN PROGRESS: ✅ the hybrid `assets/frame/rt_shadow.frame.toml` + ✅ SHADER UNIT 1 (cook_rt GENERALIZED for shadow rays — `[rt]` to_light/origin_binding/light_xyz/miss_value/hit_value, vertex-cook GREEN 445/25, F6 primary byte-identical + the 3 `scene_rt_shadow_{raygen,miss,chit}.crdv`). ✅ SHADER UNIT 2a (feasibility + raygen guard + TOML): compute texture-sampling CONFIRMED feasible (both emitters have the SampleLod/Texture arms; the worldpos kernel is the FIRST compute-stage caller); the advisor caught + I FIXED 2 defects (1D-launch pin + static params vs the one-ray scar; a sky-pixel NaN-origin validity guard in the raygen — 445 tests still green, F6 byte-identical); rt_shadow.frame.toml gets rt_constants (inverse-VP + ndc_y_sign) + depth_as_float + 1D params. ✅ SHADER UNIT 2b (the worldpos kernel): build_rt_worldpos in ckir_rt_worldpos.hpp (samples the raster depth b8/sampler b9, reconstructs world-pos via inverse-VP, writes worldpos_buf + a .w validity flag) — the FIRST compute-stage texture sampler; ALL wiring verified before authoring (binding layout kMaxKernelBuffers=8, executor routes via dispatch_kernel_sampled/the HZB precedent, all emitter arms both backends); GATE [ceir19b] 13 assertions — builds + byte-exact roundtrip + EMITS GLSL **AND** HLSL + committed rt_worldpos.ckir loads. Tidy clean. ⛔⛔ MATRIX-LAYOUT BUG CAUGHT+FIXED (before any device run): Mat4f is COLUMN-major, so the kernel's inverse-VP read is column-major (row-major would be invᵀ·ndc = garbage) — verified against compute_froxel_aabbs, re-emitted, [ceir19b] re-green. WIRING PLAN GROUNDED in the TAA-constants/froxel install site (~5682): rt_constants = memcpy inverse(view_proj) column-major + W/H + ndc_y_sign(ndc_y_points_down()?1:-1) + upload_storage; worldpos_buf/shadow_mask_buf = SIZED external buffers via the resolver+debug arms; shadow .crdv via cook_stage_named, rt_worldpos/composite via resolve_program_text; gate PINNED 64×64 (static frame.toml params). ✅ rt_composite passthrough (build_rt_composite: fullscreen stage_in-UV → tex_sample scene_hdr[1-read binding1] → @output; STEP-1 passthrough unblocks the frame, the shadow-multiply STEP-2 is a follow-up; [ceir19b] 22 assertions, rt_composite.ckir committed). ⭐ ALL 19b CKIR/shader authoring COMPLETE (worldpos + composite + 3 shadow .crdv). ✅ SCENE_RENDERER WIRING DONE (crd-scene-render COMPILES): the 4 ensure_* loaders + 5 program registrations + sized buf_worldpos/buf_shadow_mask + rt_constants (per-frame column-major inverse-VP upload, the TAA mold) + resolver/debug arms. REMAINING: (5) the ABSOLUTE device gate in test_scene_render_gpu.cpp (Vk+DX12, PIN 64×64, caps-SKIP: DETERMINISTIC receiver+occluder+light(0,8,0), TLAS from same tris; STAGE 1 worldpos-vs-CPU-analytic FIRST → STAGE 2 shadow-mask occluded≈0/unoccluded≈1 → [STAGE 3 real-composite follow-up]; val_err==0) + FIX every never-run-hybrid defect; then 4-config gate + tidy. Detail → tracker (CEIR-19b 🔄). ⚠ 19a files (crd-ceir only — a clean SEPARATE commit ⑤ from the CEIR-18 batch) UNCOMMITTED.

**PRIOR — [2026-08-16 ✅✅✅ CEIR-18 BAND CLOSED] — the RENDERER PROOF SUITE is complete: every renderer (Forward+/Clustered/Deferred/Visibility/GPU-driven/impostor) is an AUTHORED CEIR asset; the shipped path has ZERO hand-built KGraph.** CEIR-18z (band close, advisor at entry+close) FOUND + converted the LAST hand-built KGraph via a full ensure_* audit (26 classified, 25 loaders/cooker-seams + 1 builder = `ensure_visbuffer_fs`, the visbuffer id→grey FS) → authored `visbuffer_fs.ckir` (emit in a [.emitckir] regen, loader in ensure_visbuffer_fs); added the MISSING frame-cook SECTIONs (forward_plus/plus_gpu/clustered_3d_gpu had NEVER been through the A/B + plan-build gate — the latent-drop class 17z catches; now green); ran the BAND-PROOF MATRIX (the 17z full-suite discipline) GREEN — 4 configs + llvmpipe + sandbox smoke: win-debug gpu-context 6152/263+2392/144, win-asan ASan-CLEAN (scene-render full 1903/89 closing the stale-since-17e gap; gpu-context 6152/263+2392/144 closing the no-clustered3d-ASan disclosure), 2×Linux (kir [ceir18z] 7/1 + frame-cook 1803/9 + scene-render 330/13 + gpu-context 874/3; linux-gcc-asan closing the gcc-templatized-header gap), tidy clean, sandbox "PASS 5251 instances" (not overlay-only). ⛔ SHIPPED-DEFAULT CARRIED EXPLICITLY: `forward_csm_gpu` stays default; `forward_plus_gpu`/`forward_clustered_3d_gpu` are proven alternatives (flipping the default = a future PERF-gated slice, not this authored-asset band). Detail → tracker (CEIR-18 + CEIR-18z rows). **PRIOR (this batch) — CEIR-18b — the CLUSTERED 3D-FROXEL renderer works END-TO-END as an authored asset.** The z-slice ships as DATA: the renderer publishes an exponential z-boundary TABLE (header words 115-119) from `compute_froxel_slices` (unproject near/far, GLOBAL clip.w=1/h.w, exp boundaries, LINEAR interp in clip.w per slice); the 3D FS bins each fragment by `clip.w` (=view_proj row 3·world_pos) against that table via a branchless Step-sum → the froxel-AABB cuts + the FS binning agree BY CONSTRUCTION (the boundary-TABLE-not-dual-formula anti-drift discipline — 2 memories written). The device producer is a 2nd parameterization of the ONE cull builder (`scene_light_cull_3d.ckir`, 64 clusters=4×4×4=the wg) through the same ensure/register/frame shape; a `device_light_cull_3d` flag drives dispatch + the per-slice fill; a 3rd technique `forward_authored_clustered_3d` + crdl (grid[4,4,4], slice_bounds_off=115) + frame sibling. The 2D tiled path stays BYTE-IDENTICAL (cook-time grid[2]==1 branch). ⭐⭐ GATED 4 configs + a 3rd device: win-debug ([ceir18b] 70 + full scene-render 1872/87), win-asan (140/7 ASan-clean), linux-gcc-debug/llvmpipe (91/4), linux-gcc-asan (91/4 ASan-clean); gpu-context [clustered3d] VK 589 + DX12 588 + **llvmpipe 874/3** (the advisor-caught blocker: first gcc compile of the templatized kit); the NON-CIRCULAR CPU drift test (matrix-row-3 clip.w → slice → AABB-contains) + the two-depth device IMAGE gate (near/far quad, probe 2 px same tile) PASSED FIRST device run both backends; tidy clean (shared header gate-invisible). Detail → tracker (CEIR-18b row). **PRIOR (also ✅ this batch) — CEIR-18a-2 — the DEVICE Forward+ light-cull works END-TO-END (Stages 1→2b-iv).** The Forward+ clustered renderer is FUNCTIONALLY COMPLETE: a `light_cull` COMPUTE pass (`engine://scene/light_cull`, the re-authored `build_cluster_light_cull` → `scene_light_cull.ckir`) runs on the device each frame, reads the froxel-AABB table + light-input the renderer provisions, and WRITES the per-tile cluster list into the group buffer; the clustered forward FS consumes it. Sub-stages: **2b-i** FS slots are POINT-array indices (`light_base+(first+slot)*stride`, guard `slot<count`, NaN-safe select-zero); **2b-ii** the light-cull re-authored (palette_snapshot header-indirection form, component-wise scalar, `LocalInvocationIndex`, `stmt_materialize` for the compaction cursor — the if-block shared-temp scar's 3rd recurrence) — bit-exact device==oracle==analytic both backends + llvmpipe; **2b-iii** provisioning (compute_froxel_aabbs via `inverse(view_proj)`, finite-far, per-backend `flip_y`) + the `forward_plus_gpu.frame.toml` sibling + the ensure_light_cull_kernel + the device-owns precedence; **2b-iv** the device IMAGE gate. ⛔⛔ TWO first-device defects fixed: (1) the pass didn't dispatch — `dispatch_groups` derives from `d.cull_groups` (0 w/o a GPU cull) → added a `device_light_cull` flag setting `dispatch_groups=1`; (2) the NDC±Y mirror — the CPU froxel (raw view_proj) vs the FS's FragCoord tiling needs the per-backend `!ndc_y_points_down()` sign (memories written). **GREEN: [ceir18a] 103/6 Win Vk+DX12 + llvmpipe 53/3; full scene-render 1833/86; light-cook 151/12; kir [asset] 369/15; all changed files tidy-clean.** Detail → tracker (CEIR-18a row). ⛔ FOLLOW-UPS (filed): the DX12-geometry-Y question (does DX12 forward render world-up at screen-bottom?); the multi-instance-group cull WW race; the froxel-grid config-flexibility. ⛔ **NEXT = CEIR-19 (advisor at band-open; do NOT pre-design in this close).** ⚠ ALL CEIR-18 band work (18a-2 + 18b + 18z + impostor/18p) UNCOMMITTED — proposals ①②③④ above (user commits, NO AI trailer; fresh session `git status` first; `ckir_light_cull.hpp` / `build_cluster_light_cull` / `build_visbuffer_fs` + their `[.emitckir]` regen tests stay in-tree till committed, then delete + retire the regens).

**⚠ PROPOSED COMMITS (user commits, NO AI trailer; 18p + 18a-2 SHARE `scene_renderer.cpp/.hpp` + `test_scene_render_gpu.cpp` — either `git add -p` to split the hunks (impostor edits are in `ensure_impostor_program`; 18a-2 edits in `set_point_lights`/`set_cluster_light_list`/`body_scene_authored`/the layout+upload+header sites) or fold into ONE `feat(scene-render): CEIR-18p impostor + 18a-2 Forward+`):**
- **① `feat(scene-render): CEIR-18p author+delete the impostor program (4 .ckir assets)`** — `ensure_impostor_program` → thin `resolve_program_text` loader; `impostor_{vs,fs}_{plain,dither}.ckir`; builders+emit DELETED; the cull-kernel dither shared-temp fix (`vertex_asset.cpp` `stmt_materialize`) + LOUD `create_program` log (`vulkan_context.cpp`). Files: scene_renderer.cpp/.hpp, vertex_asset.cpp, vulkan_context.cpp, impostor_atlas.hpp, assets/ckir/impostor_*.ckir, test_scene_render_gpu.cpp, test_ckir_asset.cpp.
- **② `feat(scene-render): CEIR-18a-2 Forward+ device light-cull (Stages 1→2b)`** — Stage 1 `set_point_lights` consumed by the live forward pass; Stage 2a `set_cluster_light_list` + `forward_authored_clustered` (scene_forward_clustered.crdl) + viewport-dims header words (E5 frag_xy fix); Stage 2b the DEVICE light-cull: the re-authored `build_cluster_light_cull` (kir `ckir_light_cull.hpp`, KEPT IN-TREE as the regen source) → `scene_light_cull.ckir` (regenerated, 4-light header-indirection form); `ensure_light_cull_kernel` + `engine://scene/light_cull` registration; `compute_froxel_aabbs` + per-frame froxel/light-input provisioning; the `forward_plus_gpu.frame.toml` sibling + the `device_light_cull` dispatch/precedence; FS point-index contract + guard. Files: scene_renderer.cpp/.hpp, lighting_asset.cpp/.hpp, engine/kir/include/crd/kir/ckir_light_cull.hpp, assets/ckir/scene_light_cull.ckir, assets/lighting/scene_forward_clustered.crdl, assets/frame/{forward_plus,forward_plus_gpu,forward_csm}.frame.toml, tests: test_scene_render_gpu.cpp, test_lighting_asset.cpp, test_ckir_asset.cpp, gpu-shared/ckir_light_cull_test.hpp, gpu-context-vulkan/test_vulkan_context.cpp.
- **③ `feat(scene-render): CEIR-18b clustered 3D-froxel renderer as an authored asset`** — the z-slice ships as DATA: an exponential z-boundary TABLE (header words 115-119) + a 2nd parameterization of the ONE cull builder (`scene_light_cull_3d.ckir`, 64 clusters) + the 3D crdl/frame/technique. `compute_froxel_slices` (unproject near/far, GLOBAL w=1/h.w, exp boundaries, LINEAR clip.w-interp per slice); FS z-block = boundary Step-sum on clip.w with a cook-time `grid[2]==1` else-branch keeping the 2D cook BYTE-IDENTICAL; `device_light_cull_3d` flag (scan distinguishes light_cull vs _3d by exact equality) + `ensure_light_cull_3d_kernel`/register + dispatch/precedence/upload-guards + the header boundary publish; grew section consts 128→512/96→384 (2D=prefix); the templatized gpu-shared kit + `ClusterCullScene3D`; the DRIFT-catching CPU test + the two-depth device IMAGE gate (both backends). ⛔ SHARES most files with ② (scene_renderer.cpp/.hpp, lighting_asset.cpp/.hpp, the test files) — either `git add -p` by slice or fold ②+③. NEW files: assets/lighting/scene_forward_clustered_3d.crdl, assets/frame/forward_clustered_3d_gpu.frame.toml, assets/ckir/scene_light_cull_3d.ckir. Also touched: tests/kir/test_ckir_asset.cpp, tests/gpu-context-dx12/test_dx12_compute.cpp. ⛔ `ckir_light_cull.hpp` + `build_cluster_light_cull` STAY (the [.emitckir] regen source for BOTH .ckir assets — delete only AFTER this batch commits).
- **④ `refactor(scene-render): CEIR-18z band close — author the last hand-built FS (visbuffer_fs) + frame-cook coverage`** — the band-close ensure_* AUDIT (26 classified) found the LAST hand-built KGraph, `ensure_visbuffer_fs` (the visbuffer id→graded-grey FS), and CONVERTED it: `assets/ckir/visbuffer_fs.ckir` (emit in a `[.emitckir]` regen `build_visbuffer_fs`, IN-TREE till commit), `ensure_visbuffer_fs`→thin resolve_program_text loader; added forward_plus/forward_plus_gpu/forward_clustered_3d_gpu SECTIONs to the frame-cook A/B fidelity + plan-build gates (they'd never been through frame-cook). Files: engine/scene-render/src/scene_renderer.cpp (the visbuffer loader — SHARES the file with ②③), assets/ckir/visbuffer_fs.ckir (NEW), tests/kir/test_ckir_asset.cpp (regen + `[ceir18z]` load gate), tests/frame-cook/test_frame_template_bridge.cpp (the 3 SECTIONs). ⛔ POST-COMMIT DELETION (do AFTER this batch commits): delete `build_cluster_light_cull` + `engine/kir/include/crd/kir/ckir_light_cull.hpp` + `build_visbuffer_fs` + **(CEIR-19b, user-flagged 2026-08-16 — mission purpose: the .ckir is the SOLE source, no C++ builder) `build_rt_worldpos` + `engine/kir/include/crd/kir/ckir_rt_worldpos.hpp` + `build_rt_composite`**, AND retire the 3 `[.emitckir]` regen tests that call them (won't compile otherwise) — then the committed `.ckir` assets + the load/distinctness/roundtrip gates are the source of truth, git history the escape hatch.
- **⑥ `feat(scene-render): CEIR-18a-3 the frame-graph pass technique drives the forward FS cook`** — init_programs resolves the forward FS TWO-TIER from the INSTALLED frame (csm←main.forward.technique when it `requires=["shadows"]`, fwd←fallback.forward.technique; else fwd←main; field-less ⇒ setter; graph-named-unresolvable FAILS). ⛔ `material_pass` read via pass_u32 vs FrameMaterialPass (ENUM, not pass_str). set_frame_graph_toml + the RAF-11 frame_commit reload RE-COOK the FS with ATOMIC ROLLBACK. F2 "aspirational / setter-selects" claims STRUCK in place across forward_csm/forward_plus/forward_plus_gpu `.frame.toml` + the scene_renderer.hpp setter docs + the .cpp member comment (the setters are now the field-less FALLBACK). REN-37.2 gate reconciled (graphs name the technique, zero setters; +asset-outranks-setter inverse pin + bad-name cold/hot rollback pins, BOTH fwd+csm arms); CEIR-18a-2 Stage 1/2a point-light gates reconciled. Verified Win Vk+DX12 (1938/89) + Linux GCC/lavapipe (1514/65), tidy-clean. Files: engine/scene-render/src/scene_renderer.cpp (SHARES the file with ②③④ + 19b — surgical hunks: the init_programs `graph_tech`/two-tier block, the set_frame_graph_toml rollback, the Impl::frame_commit re-cook+rollback, the set_soft_shadows tier-swap comment, the ~909 member comment), engine/scene-render/include/crd/scenerender/scene_renderer.hpp (setter docs), tests/scene-render/test_scene_render_gpu.cpp (REN-37.2 sample/make_graph + e/d-cold-fwd+csm/d-hot; point_light_gate_body + point_cluster_gate_body), assets/frame/{forward_csm,forward_plus,forward_plus_gpu}.frame.toml.
- **⑦ `feat(scene-render): CEIR-19b hybrid RT-shadow renderer as an authored asset (3-device gate)`** — the AUTHORED hybrid RT-shadow renderer (rt_shadow.frame.toml) works END-TO-END on Win Vulkan + Win DX12 (DXR) + Linux lavapipe. THREE never-run defects fixed: (1) **frame_runtime.cpp** — a SECONDARY depth-format write routes to the depth attachment on `!first_write` (was gated to `pass_flag(kMrt)`), so a `raster.geometry` forward pass's DECLARED scene_depth transient is bound (via image_with_depth) + sampleable by a later compute pass — its OWN blast radius (every depth pass; scene-render 1967/90 + frame-cook 2973/96 green); (2) **rt_worldpos.ckir** authored rt_constants@iidx0 / worldpos@iidx1 to MATCH the runtime's reads-first compute-storage-slot order (the binding contract is in the .ckir header; the bridge-vs-runtime slot-order + depth-split drift is a FILED latent defect the 18z A/B gate misses); (3) the CEIR-19b GATE (×2 backends) — its VUID-08608 handling is RESOLVED in ⑩ (CEIR-19z-1): 08608 is an UPSTREAM VVL FALSE POSITIVE (not an encoder bug), so the gate version-scopes a `whitelist` + asserts `error_count()==0`. ⛔ The working tree holds ONLY that final form (the bounded exception never existed on disk at commit time) — fold ⑦+⑩'s `test_scene_render_gpu.cpp` CEIR-19b hunks, or commit ⑦ then ⑩ on the same file. rt_shadow.frame.toml gains an explicit forward `technique="standard_forward"` (18a-3 self-describing). Files: engine/frame-cook/src/frame_runtime.cpp, assets/ckir/rt_worldpos.ckir, assets/frame/rt_shadow.frame.toml, tests/scene-render/test_scene_render_gpu.cpp (the CEIR-19b GATE Vk+DX12 — SHARES the file with ⑥). ⛔ scene_renderer.cpp's 19b wiring (ensure_rt_* loaders + rt buffers + resolver arms) is ALREADY in-tree from a prior turn (folds with the 18a-3 hunks in ⑥, or its own).
- **⑧ `feat(ceir): CEIR-19c stage-1 — the ceir.rt→gpu execution bridge (3-device gate)`** — the ceir.rt→gpu EXECUTION BRIDGE (`execute_rt_lowered`) works end-to-end on Win Vulkan (RTX) + Win DX12 (DXR) + Linux lavapipe (144/1 each). NEW: `LoweredKind` +RayQuery/+AccelBuild + `lower_region` RT arms + the widen-closed-enum audit (lower.hpp/.cpp, execute.{hpp,cpp}, render_materialize.cpp); `execute_rt_lowered`/`validate_rt_lowered` + the `RtHooks` caller-surface (ceir-gpu names NO backend, the KernelResolveFn precedent) + `dispatch_inline_ray_query`; the AUTHORED `assets/ckir/rt_witness.ckir` (mandate #1, NO builder — TraceRayHit inline-rayQuery); a FORMAT FIX in `ckir_asset.hpp` (the global ext/sbegin operand pools → `[[ext]]`/`[[sbegin]]` SECTIONS — the FIRST asset with a non-empty serial_ext pool exposed the latent bare-key collision; scar written); the per-op dialect-doc strike (rt.trace/rt.sbt_build DEFERRED-to-19z) + opgen REGEN (rt_ops.cpp + rt.ops.{json,md}); the 3-device gate (test_vulkan_rt.cpp + test_dx12_rt.cpp + the 2 CMake link additions) + the lowering/executor/deferral tests (test_lower.cpp) + the serializer-proof + asset-load tests (test_ckir_asset.cpp). Files: engine/ceir-gpu/{include/crd/ceir/gpu/{lower,execute}.hpp, src/{lower,execute,render_materialize}.cpp}, engine/kir/include/crd/kir/ckir_asset.hpp, engine/ceir/ops/rt.ceirop.toml + engine/ceir/generated/crd/ceir/gen/{rt_ops.cpp,rt.ops.json,rt.ops.md}, assets/ckir/rt_witness.ckir, tests/ceir-gpu/test_lower.cpp, tests/kir/test_ckir_asset.cpp, tests/gpu-context-vulkan/{test_vulkan_rt.cpp,CMakeLists.txt}, tests/gpu-context-dx12/{test_dx12_rt.cpp,CMakeLists.txt}. ⛔ NO overlap with ①-⑦ (a clean crd-ceir/crd-ceir-gpu + kir + RT-gate batch). ⛔ SHARES test files with ⑨ (test_vulkan_rt.cpp / test_dx12_rt.cpp / test_ckir_asset.cpp — stage-1 gates + stage-2 gates both live there) — `git add -p` by stage OR fold ⑧+⑨ into one CEIR-19c commit.
- **⑨ `feat(ceir): CEIR-19c stage-2 — the §134 wavefront path tracer as authored .ckir (3-device)`** — the WAVEFRONT (raygen→trace→compact→shade, a host `while(count>0)` loop through execute_rt_lowered) works on Win Vulkan (RTX) + Win DX12 (DXR) + Linux lavapipe. NEW ASSETS: `assets/ckir/wavefront_{compact,trace,shade}.ckir` (authored, NO builder — serializer-exercise→canonical→committed→decoupled-gate). NEW TESTS: the 4 [ceir19c] device gates (compact/trace/shade isolated + the wavefront harness) in tests/gpu-context-{vulkan,dx12}/test_{vulkan,dx12}_rt.cpp (RtGateState/RtGateStateDx gain `last_gx` + the `rt_tri32`/`rt_fold` host-hash helpers); the 3 kernel serializer-proof + decoupled load gates in tests/kir/test_ckir_asset.cpp. The advisor's DOUBLE-COMPACT continuation (2nd compact on host-zeroed cont_flags → next_count=0) + the no-races-in-proof-assets ruling + the count-driven grid (rebuild shade prog per-iter, harness accommodation). Files: assets/ckir/wavefront_{compact,trace,shade}.ckir, tests/kir/test_ckir_asset.cpp, tests/gpu-context-vulkan/test_vulkan_rt.cpp, tests/gpu-context-dx12/test_dx12_rt.cpp. ⛔ TEST + ASSET only (the bridge shipped in ⑧); SHARES the 3 test files with ⑧ (see ⑧'s note). Multi-bounce / frame-runtime routing = a FUTURE band (CEIR-19z+).
- **⑩ `fix(scene-render): CEIR-19z-1 — CEIR-19b-F1 was an upstream VVL false positive, not an encoder bug`** — the VUID-vkCmdTraceRaysKHR-None-08608 that CEIR-19b filed as F1 is an UPSTREAM VVL FALSE POSITIVE. Command-stream markers PROVED the bind→trace window is clean (BIND_RT adjacent to the single TRACE); the SAME frame fires one 08608 under VVL 1.3.275 and is CLEAN under VVL 1.4.313 — a bind-point-scoping bug in VVL's since-bound dynamic-state ledger. TWO hypotheses falsified first (shader-object null-bind; bind-elision cache). RESOLUTION (Option C, advisor): a NEW `gpu::validation_layer_spec_version()` accessor (queries the loaded VK_LAYER_KHRONOS_validation specVersion; 0 if absent) + the CEIR-19b Vulkan gate version-scopes `capture.whitelist(0x29056f6a)` (numeric MessageID = a hash of the FULL VUID string → silences ONLY the TraceRays variant; draw/dispatch 08608 stay live) BEFORE the render, then `REQUIRE(spec!=0)` false-green guard AFTER the render (past the shader-backend SKIP, so Win still skips clean) → unconditional `CHECK(error_count()==0)`. The old bounded VUID-string exception DELETED. ENCODER NET-ZERO (F1-DBG markers added+reverted in vulkan_raster_context.cpp — no diff). PROOF: BOTH lavapipe arms pass (stock 1.3.275 whitelist-arm + 1.4.313 strict-arm, each prints its spec — `fetch_vvl.sh`+`run_both_arms.sh`); Win Vk+DX12 gates skip clean (no false RED); tidy-clean (LLVM-20). Files: engine/gpu-context-vulkan/include/crd/gpu/vulkan_validation_capture.hpp, engine/gpu-context-vulkan/src/vulkan_validation_capture.cpp, tests/scene-render/test_scene_render_gpu.cpp. ⛔ SHARES test_scene_render_gpu.cpp with ⑥+⑦ (the CEIR-19b gate) — fold with ⑦ or `git add -p`. Scar [[feedback_ceir19b_hybrid_rt_frame_never_run_scars]] #3 rewritten (reverses the "real spec violation" + "never match the numeric MessageID" claims).
- **⑪ `fix(frame-cook): CEIR-19z-2 — unify the bridge's compute/RT storage-slot order to the runtime's reads-first contract`** — scar #1's FILED bridge-vs-runtime DRIFT: `frame_runtime.cpp` (the shipping SceneRenderer path) binds a compute/RT kernel's storage slots READS-FIRST (its explicit CONTRACT ~L1200), but `frame_template_bridge.cpp` (map_compute + map_rt_common) bound them WRITES-FIRST — invisible while every bridged pass had ≤1 distinct storage buffer (light_cull). rt_worldpos (rt_constants READ + worldpos WRITE) is the first distinct-buffer pass; the bridge would swap its input/output. FIX: canonical = the runtime (documented, 3-device-proven, every .ckir authors to it) → flipped BOTH bridge maps to reads-first, preserving the reads-loop's args/sampled/untracked/accel routing (only loop POSITION moved). NO iidx flip (rt_worldpos.ckir already matched canonical) — rewrote its stale header comment. GATE: the 18z A/B gate is bridge-vs-bridge (round-trip) so structurally blind to this → added rt_shadow to the CEIR-15e A/B section + a dedicated `[ceir19z]` test parsing the REAL rt_shadow.frame.toml asserting the ABSOLUTE slot each buffer binds (reads-first + accel + depth-transient→depth). Safety: build_frame_graph_template has ZERO device callers. PROOF: frame-cook 3060/97 (Win) green; lavapipe 19b both arms green (runtime rt_worldpos undisturbed); tidy-clean. Files: engine/frame-cook/src/frame_template_bridge.cpp, assets/ckir/rt_worldpos.ckir (header comment only), tests/frame-cook/test_frame_template_bridge.cpp. ⛔ SHARES `assets/ckir/rt_worldpos.ckir` with ⑦ (⑦ CREATES it; ⑪ edits ONLY its header comment) AND `tests/frame-cook/test_frame_template_bridge.cpp` with ④ (④ adds 3 SECTIONs; ⑪ adds the rt_shadow SECTION + the `[ceir19z]` test) — `git add -p` by slice or fold ⑪'s hunks with ⑦/④. frame_template_bridge.cpp is ⑪-only. Scar #1 updated (FILED→RESOLVED). A live bridge-EXECUTION vs runtime pixel gate is deferred to 19z-4's ledger (no device caller today).
- **⑫ `feat(scene-render): CEIR-19z-3 — CEIR-19b-F2 the RT shadow term is VISIBLE (composite × shadow_mask) + a new draw_textured_storage verb`** — the composite now MULTIPLIES the flat-lit scene_hdr × the ray-traced shadow mask → shadowed pixels DARKEN (the RT shadow is finally visible in the image). AUTHORED (mandate #1): rt_composite.ckir edited to StorageLoad shadow_mask_buf[py*TexSize(scene_hdr).x + px] (FragCoord builtin 11 → Cast; TexSize.x for W) → IntBitsToFloat → Splat×Mul the sampled colour; the taa_resolve StorageLoad precedent, NO C++ builder. ⛔ THE ROOT WAS NOT THE ASSET: a 1-read fullscreen pass's per-pixel storage buffer was NEVER bound — `build_fullscreen_ceir` (render_fullscreen_build.cpp) cooked the constants StorageBuffer operand ONLY in its BINDLESS branch, so the PLAIN 1-read plan had no storage operand → render_materialize produced no StorageBuffer binding → the encoder fell through to `draw_textured` (no storage) and StorageLoad read set0/binding0 UNBOUND → BLACK everywhere. FIX (advisor Option B, the RHI-verb-per-binding-shape precedent): (1) the PLAIN cook branch now emits the constants StorageBuffer@0; (2) a NEW `draw_textured_storage` verb on BOTH raster contexts (texture@1 + sampler@2 + storage@0, the write_scene_textured/record_scene_textured mold); (3) the `command_lowering.hpp` dispatch branch (`plain_sampled_texture && first_storage → draw_textured_storage`). PROOF: lavapipe BOTH arms 34/34 (STAGE 1+2 + the new STAGE 3 per-pixel darkening + error_count==0 — the binding canary the advisor flagged is now clean); Win Vk+DX12 build clean (DX12 twin compiles, gates skip clean); kir [ceir19b] rt_composite roundtrip (g.size≥18); tidy-clean. Files: assets/ckir/rt_composite.ckir (EDITS the committed passthrough c3ac005 — F2 multiply), engine/ceir-gpu/src/render_fullscreen_build.cpp, engine/gpu-context/include/crd/gpu/detail/command_lowering.hpp, engine/gpu-context-vulkan/src/vulkan_raster_context.cpp, engine/gpu-context-dx12/src/dx12_raster_context.cpp, engine/frame-cook/src/frame_runtime.cpp (comment freshen only), tests/scene-render/test_scene_render_gpu.cpp (STAGE 3 — SHARES with ⑥⑦⑩), tests/kir/test_ckir_asset.cpp (rt_composite gate harden — SHARES with the CKIR-asset batch ①/⑧/⑨). ⛔ scene_renderer.cpp UNTOUCHED (the mandate held: the algorithm is in the asset; the C++ is a device binding verb, not a render technique). Scar [[feedback_one_read_fullscreen_binds_single_texture_not_bindless]] updated in place (the 1-read+storage shape now has a verb; mechanism corrected: CEIR-plan-driven, not frame_runtime's read-count branch).
- **⑬ `docs(ceir): CEIR-19z-4 — the CEIR-19 (RT / ceir.rt) band close`** — DOCS ONLY (no code; the 19z code shipped in ⑩⑪⑫). The band-close record: NEW `docs/sessions/2026-08-16-ceir-19z-band-close.md` (the full-suite MATRIX green on 5 legs — linux-gcc-debug/asan + lavapipe + win-debug/asan, zero failures + the actual per-config/per-suite counts; the ensure_*/builder AUDIT = zero render-technique KGraph builders, c3ac005-verified; the ROW-PER-CLAIM table; the 8-item DEFERRAL LEDGER, each homed); `docs/detours/D-007-ceir-tracker.md` (CEIR-19z row → ✅ + the CEIR-19 BAND ✅ + the ledger homes L1→CEIR-20 row / L2-L7→19z row / L8→19c row); `context.md` (current-focus → the compact CEIR-19 ✅ pointer + this ⑬). Also a harness-env memory [[reference_windows_test_run_env_assets_shaderc_asan_dll]] (the Windows-matrix run-env trap that cost a debug cycle — CRD_ASSETS_DIR + shaderc + ASan-DLL PATH). Files: docs/sessions/2026-08-16-ceir-19z-band-close.md, docs/detours/D-007-ceir-tracker.md, context.md. ⛔ NO overlap with ①-⑫ (docs only). ⛔ The scratchpad matrix scripts (linux_matrix.sh / windows_matrix2.ps1 / windows_asan_run.ps1) are NOT committed — the session-log harness note is their durable residue.
- **⑭ `feat(ceir): CEIR-20a declare the ceir.work dialect (device-generated work)`** — the `ceir.work` dialect (sec-43, the 19a mold): 4 native-intrinsic ops (queue_alloc / produce / consume / compact) over 2 opaque Extern type-classes (work.queue / work.record) — the semantic DEVICE-work-generation vocabulary a 20b/20c bridge lowers to indirect/DGC/Work-Graphs. produce = a grid dispatch that appends records to a %queue; consume = an INDIRECT dispatch over the queue's DEVICE count (the distinction from compute.dispatch — no host grid); compact = stream-compaction src→dst. The dialect header DOCUMENTS the async reconciliation (device work-gen ≠ ceir.async host structured-concurrency — different tier, never shared ops). find_work_misuse (the find_rt_misuse mold — the QueueTypeMismatch type-chain + capacity/record_stride range + the dispatch-shape mirror). DECLARE-only, crd-ceir NEVER links gpu-context (I3/I4), ZERO renderer changes. work.record declared-forward-unused (a future produce_one/consume_one). GATE GREEN 4 configs (linux-gcc-debug/asan + win-debug/asan, [work] 53 assertions/5 cases each) + opgen no-drift + tidy. Files: engine/ceir/ops/work.ceirop.toml (NEW), engine/ceir/generated/crd/ceir/gen/work_ops.{hpp,cpp} + work.ops.{json,md} (NEW, generated), engine/ceir/include/crd/ceir/work.hpp (NEW), engine/ceir/src/work.cpp (NEW), tests/ceir/test_work.cpp (NEW), tests/ceir/CMakeLists.txt (the test_work entry). ⛔ SHARES tests/ceir/CMakeLists.txt with ⑤ (CEIR-19a's test_rt entry) — `git add -p` the test_work line or fold. Otherwise NO overlap with ①-⑬ (a clean crd-ceir batch). ⛔ CEIR-20 work = commits ⑭+ (uncommitted, user commits).
- **⑰ `feat(ceir): CEIR-20c-2 the VK_EXT_device_generated_commands lowering (cross-vendor) — one ceir.work program, THREE device lowerings`** — the CROSS-VENDOR third lowering of a ceir.work program: the GPU AUTHORS the command STREAM (sequence count + per-sequence dispatch payloads), `vkCmdExecuteGeneratedCommandsEXT` GENERATES N separate DISPATCH commands. **(a)** device enablement — `m_dgc_ext`/`device_generated_commands_ext()` on VulkanGpuContext (mirrors the NV `m_dgc` wiring): VK_EXT_device_generated_commands + VK_KHR_maintenance5 + BDA, DGC-EXT PROPERTIES require COMPUTE in supportedIndirectCommandsShaderStages. **(b)** `VulkanDgcContext` rig (the RT-context pimpl mold): lone DISPATCH-token indirect commands layout, `indirectExecutionSet=VK_NULL_HANDLE` + consume pipeline via `VkGeneratedCommandsPipelineInfoEXT` in pNext (spec-MINIMAL — INDIRECT_BINDABLE is execution-set-ONLY per validusage.json ⇒ plain pipeline, no metadata machinery), `VkBufferUsageFlags2` preprocess buffer (maintenance5), legacy COMMAND_PREPROCESS barrier (sync2 off headless). **(c)** AUTHORED `assets/ckir/work_smoke_produce_dgc.ckir` (mandate #1): five (1,1,1) VkDispatchIndirectCommand payloads + count=5; REUSE `work_smoke_consume.ckir` verbatim. **(d)** the device gate MODULE-DRIVEN via `build_work_graph_plan` (the plan's kernel symbols select produce/consume SPIR-V) → out[0]==5 (FIVE device-generated commands) + out[1]==5 + count_rb[0]==5 + token_rb[0]==1 on the 4070 Ti. ⛔ SCOREBOARD: 20b = host-recorded dispatch + device grid · 20c-1 = GPU-scheduled node launch · 20c-2 = device-GENERATED command stream. ⛔ scars: [[feedback_ckir_node_refs_are_positional_n_index_id_ignored]] (refs are positional n<pool-index>, id ignored); advisor at the gate caught a decorative "module-driven" claim (plan computed-then-dropped → now the plan selects the SPIR-V). Blast radius (RT-1 inline rayQuery Vulkan + 20b ceir.work Vulkan executor) + per-slice clang-tidy-20 (5 files) + linux-gcc-debug gcc `-Werror` (whole tree, exit 0) GREEN. Files: engine/gpu-context-vulkan/include/crd/gpu/vulkan_context.hpp, engine/gpu-context-vulkan/src/vulkan_context.cpp, engine/gpu-context-vulkan/include/crd/gpu/vulkan_dgc_context.hpp (NEW), engine/gpu-context-vulkan/src/vulkan_dgc_context.cpp (NEW), assets/ckir/work_smoke_produce_dgc.ckir (NEW), tests/ceir-gpu-vulkan/test_vulkan_dgc.cpp (NEW), tests/ceir-gpu-vulkan/CMakeLists.txt, context.md, docs/detours/D-007-ceir-tracker.md. ⛔ NO overlap with ⑭-⑯ (a clean crd-gpu-context-vulkan + ceir-gpu-vulkan-tests batch; the device-enablement edits are additive + gated). ⛔ CEIR-20 work = commits ⑭+ (uncommitted, user commits).
- **⑱ `docs(ceir): CEIR-20z — the CEIR-20 (ceir.work / device-generated work) band close`** — DOCS + one CLOSE-HYGIENE test (the 20a–20c device code shipped in ⑭–⑰). ⭐ the advisor-at-the-gate caught that the `work_smoke_*.ckir` format was validated ONLY inside the cap-skipping DEVICE gates (ungated on a device-free machine) → NEW device-free decoupled load gate `tests/kir/test_ckir_asset.cpp::"CEIR-20b/20c: the authored work_smoke_*.ckir parse + round-trip + emit GLSL (device-free)"` (#3961: the 3 assets parse + round-trip byte-exact + emit GLSL, no device) — the 19z asset-inventory discipline; GREEN win-debug + linux-gcc-debug (gcc `-Werror` rebuild), tidy-20 clean. The band-close record: NEW `docs/sessions/2026-08-17-ceir-20z-band-close.md` (the AUTHORITATIVE close: the band-scope × 3-leg matrix RE-RUN green — win-debug 20/20 band + 2/2 NV blast-radius; linux-gcc-debug gcc-`Werror` whole-tree compile-clean + CEIR-20 band green [⭐ the VK_EXT_DGC gate RAN the full 14-assertion path on Mesa lavapipe = a 2nd-vendor device proof; sole 3 reds = the pre-existing `dsp spectral` llvmpipe flake, unrelated]; win-asan the 2 manual-readback-memcpy device gates [DGC #4582 + WG #4527] clean, zero AddressSanitizer errors; the honest three-lowering scoreboard; the zero-builder audit [cooker vs algorithm-builder classified]; the 7-item deferral ledger with ⛔ `work.compact` DEVICE-consumer RE-HOMED off "20c" [20c never discharged it]); `docs/detours/D-007-ceir-tracker.md` (CEIR-20 row 🔄→✅ + BAND ✅ + the close record + ledger homes); `context.md` (current-focus → the CEIR-20 ✅ pointer + NEXT=CEIR-21). Files: docs/sessions/2026-08-17-ceir-20z-band-close.md, docs/detours/D-007-ceir-tracker.md, context.md, tests/kir/test_ckir_asset.cpp (the NEW #3961 device-free work_smoke load gate). ⛔ SHARES tests/kir/test_ckir_asset.cpp with the CKIR-asset batch (①/⑧/⑨ add gates there) — `git add -p` the work_smoke TEST_CASE or fold. ⛔ CEIR-20 band = commits ⑭–⑱ (uncommitted, user commits).
- **⑲ `feat(ceir): CEIR-21a declare the ceir.shape dialect (high-level shape reasoning)`** — the `ceir.shape` dialect (sec-35, the 20a/12a mold): 6 PURE value-ops (make/rank/extent/broadcast/reshape/assert) over the CEIR-3d Dim/Shape TYPES (already declared) — typed NoSemantics, ⛔ NO kernel_ref (sec-70: a native graph backend ingests the tensor+shape graph WHOLE). `find_shape_misuse` (14 `ShapeMisuseKind`: operand/result KIND + RESULT-IDENTITY [make members==dims / extent==member[axis] / reshape==target / assert==lhs — the 12a `underlying==operand` precedent] + the 3d `shapes_broadcast`/`shapes_reshape` predicates WRAPPED [Incompatible→a POINTING misuse w/ the 3z position, Unknown→ACCEPT, Compatible→accept] + extent axis-bounds + assert relation closed-vocab). DECLARE-only, crd-ceir NEVER links gpu-context, ZERO renderer changes. broadcast result-exactness name-forwarded to 21b. GATE ×4 configs (win-debug/asan + linux-gcc-debug/asan: [shape] 3/3 + gen-smoke 3/3 each) + opgen drift/validator + gcc `-Werror=switch` + tidy-20 + `crd-ceir-invariants`, all GREEN (RE-RUN after the advisor-at-gate fixes: 4th config + result-identity + Unknown-branch test + gen-smoke). Files: engine/ceir/ops/shape.ceirop.toml (NEW), engine/ceir/generated/crd/ceir/gen/shape_ops.{hpp,cpp} + shape.ops.{json,md} + tests/ceir/generated/test_shape_gen_smoke.cpp (NEW, generated), engine/ceir/include/crd/ceir/shape.hpp (NEW), engine/ceir/src/shape.cpp (NEW), tests/ceir/test_shape.cpp (NEW), tests/ceir/CMakeLists.txt (the test_shape entry). ⛔ SHARES tests/ceir/CMakeLists.txt with ⑤/⑭ (rt/work test entries) — `git add -p` the test_shape line or fold. ⛔ CEIR-21 work = commit ⑲+ (uncommitted, user commits).
- **⑳ `feat(ceir): CEIR-21b declare the ceir.tensor dialect (structural-six high-level tensor IR)`** — the `ceir.tensor` dialect (sec-51, the 20a/12a/21a mold): the STRUCTURAL SIX PURE ops (elementwise/broadcast/reshape/transpose/reduce/matmul) over the CEIR-3d Tensor TYPE — typed NoSemantics, ⛔ NO kernel_ref (sec-70: a native graph backend ingests the tensor graph WHOLE). NEW `Context::shapes_broadcast_result` (the numpy-broadcast RESULT-shape producer the 3d row deferred — "rejected a broadcast-result constructor, no producer until 18"; consumer = tensor.elementwise/broadcast; NON-const, interns). Shape-aware `find_tensor_misuse` (16 `TensorMisuseKind`: Tensor-kind + element-consistency [members[0]] + the 3d `shapes_broadcast`/`shapes_reshape` predicates WRAPPED [Incompatible→3z pointing, Unknown→ACCEPT, Compatible→result-EXACTNESS] + transpose PERMUTATION + reduce AXIS-bounds + matmul CONTRACTION + BATCH + result-shape identity; check order contractual). DECLARE-only, crd-ceir NEVER links gpu-context, ZERO renderer changes. GATE ×4 configs (win-debug/asan + linux-gcc-debug/asan: [tensor] 3/3 + gen-smoke 3/3 each) + opgen drift/validator + gcc `-Werror=switch` (16-case) + tidy-20 + `crd-ceir-invariants`, all GREEN (RE-RUN after the advisor-at-gate fixes: tensor.broadcast negatives + Unknown-accept; the matmul BATCH-dim FALSE-GREEN → new `BatchMismatch` kind + explicit batch check; the per-op min-arity FOLD guard). Files: engine/ceir/ops/tensor.ceirop.toml (NEW), engine/ceir/generated/crd/ceir/gen/tensor_ops.{hpp,cpp} + tensor.ops.{json,md} + tests/ceir/generated/test_tensor_gen_smoke.cpp (NEW, generated), engine/ceir/include/crd/ceir/tensor.hpp (NEW), engine/ceir/src/tensor.cpp (NEW), engine/ceir/include/crd/ceir/context.hpp + engine/ceir/src/context.cpp (the `shapes_broadcast_result` addition), tests/ceir/test_tensor.cpp (NEW), tests/ceir/CMakeLists.txt (the test_tensor entry). ⛔ SHARES context.{hpp,cpp} with the CEIR core + tests/ceir/CMakeLists.txt with ⑤/⑭/⑲ — `git add -p` the `shapes_broadcast_result` + test_tensor hunks. ⛔ CEIR-21 work = commits ⑲–⑳ (uncommitted, user commits).
- **㉑ `feat(ceir): CEIR-21c declare the ceir.layout dialect (§22 optional data-layout constraint)`** — the `ceir.layout` dialect (sec-22): ONE Pure op `layout.constrain(%tensor) {kind, strides?, block?, swizzle?} -> %tensor` — an OPTIONAL layout CONSTRAINT annotating a tensor (a provider reads it; §22 "explicit constraints where interop/algorithms require"). ⛔ MECHANISM (advisor-locked): a CONSTRAINT OP, NOT a tensor-type member — the 21b Tensor type stays [element, shape] UNTOUCHED (an optional 3rd Layout member would make tensor<f32,4x3> ≠ tensor<f32,4x3,row_major> as TypeIds, reopening every 21b identity check + rippling into type_is_canonical / the binary decoder / the grammar / the fuzz corpus). §23 `tensor<S,E,L>` (layout-in-type) = a **Dxxx declared divergence** named-forward to the first type-identity consumer. `find_layout_misuse` (8 `LayoutMisuseKind`: Tensor-kind, PASSTHROUGH type identity [result==input], `kind` closed vocab {row_major,col_major,strided,blocked,aos,soa,swizzle — tiled folded into blocked per §22}, KIND-GATED strides/block/swizzle [ParamKindMismatch, the 12a nonsense-by-construction], strides/block arity vs the tensor RANK, block extents ≥1; + a min-arity FOLD guard). DECLARE-only, crd-ceir NEVER links gpu-context, ZERO renderer changes. GATE ×4 configs (win-debug/asan + linux-gcc-debug/asan: [layout] 2/2 + gen-smoke 3/3 each) + opgen drift/validator + gcc `-Werror=switch` (8-case) + tidy-20 + `crd-ceir-invariants`, all GREEN (RE-RUN after the advisor-at-gate fixes: the arity-0-constrain param-skip ROBUSTNESS LEAK → min-arity guard; the rank≠2 test gap → rank-3 pass+arity-fail; the §22 blocked/tiled citation). Files: engine/ceir/ops/layout.ceirop.toml (NEW), engine/ceir/generated/crd/ceir/gen/layout_ops.{hpp,cpp} + layout.ops.{json,md} + tests/ceir/generated/test_layout_gen_smoke.cpp (NEW, generated), engine/ceir/include/crd/ceir/layout.hpp (NEW), engine/ceir/src/layout.cpp (NEW), tests/ceir/test_layout.cpp (NEW), tests/ceir/CMakeLists.txt (the test_layout entry). ⛔ SHARES tests/ceir/CMakeLists.txt with ⑤/⑭/⑲/⑳ — `git add -p` the test_layout line. ⛔ CEIR-21 work = commits ⑲–㉑ (uncommitted, user commits).
- **㉒ `feat(ceir): CEIR-21z composing band gate + the CEIR-21 (tensor IR) band close`** — the COMPOSING GATE (the 12z mold) + the band close. NEW `tests/ceir/test_band21_gate.cpp` (#613 `[band21]`): ONE C++-built module threads all THREE dialects — `shape.make`/`shape.rank` alongside a chained rank-1 row tensor → `tensor.broadcast` UP → `layout.constrain{blocked}` → `elementwise{mul}` → `transpose{1,0}` → `matmul` → `reshape` → `reduce{axis=0,sum}`-to-RANK-0 (§21 `!shape<>`, the 21b-deferred case — `drop_axis_shape`→`type_shape({})` interns consistently) — PROVING (1) `find_shape_misuse` + `find_tensor_misuse` + `find_layout_misuse` + `find_structure_error` ALL None on the ONE module (the verifiers COMPOSE), (2) ⭐ the FIRST serializer crossing for shape/tensor/layout ops: TEXT round-trip (print→parse→re-walk all-None + print==reprint byte-exact) AND BINARY round-trip (serialize→deserialize→re-walk all-None). ⛔ reduce/matmul DETERMINISM (order-sensitive float accumulation) DOCUMENTED in `tensor.ceirop.toml` reduce/matmul docs → opgen-regen → drift-clean → the bit-exact contract is CEIR-22's oracle (ledger L9). BAND-CLOSE re-run ×4 configs GREEN (win-debug/asan 9 hand + 51 gen-smoke; linux-gcc-debug/asan 60 REBUILT 74/74 — a genuine compile+run) + opgen-drift (#665)/validator (#666) + `crd-ceir-invariants` (#1305) + gcc `-Werror=switch` + LLVM-20 tidy (1 file: test_band21_gate.cpp). Files: tests/ceir/test_band21_gate.cpp (NEW), tests/ceir/CMakeLists.txt (the test_band21_gate entry), engine/ceir/ops/tensor.ceirop.toml (the reduce/matmul determinism docs) + engine/ceir/generated/crd/ceir/gen/tensor.ops.{json,md} (regenerated), docs/sessions/2026-08-18-ceir-21z-band-close.md (NEW — the AUTHORITATIVE close: sub-slices + MATRIX + ROW-PER-CLAIM + builder audit + 10-item DEFERRAL LEDGER + verdict), docs/detours/D-007-ceir-tracker.md (CEIR-21 row 🔄→✅ + BAND ✅ + the 21z close record + ledger homes), context.md (current-focus → the CEIR-21 ✅ pointer + NEXT=CEIR-22 + this ㉒). ⛔ SHARES tests/ceir/CMakeLists.txt with ⑤/⑭/⑲/⑳/㉑ — `git add -p` the test_band21_gate line. ⛔ SHARES engine/ceir/ops/tensor.ceirop.toml + engine/ceir/generated/crd/ceir/gen/tensor.ops.{json,md} with ⑳ (those files are NEW-uncommitted from ⑳; the reduce/matmul determinism doc edit physically RIDES ⑳'s add — it can't be `git add -p`'d as a separate hunk of an unadded file, so it folds into ⑳'s content or ships as an amend when ⑳ is committed). ⛔ CEIR-21 band = commits ⑲–㉒ (uncommitted, user commits).
- **㉓ `feat(ceir): CEIR-22a declare ceir.linalg gemm + ceir.tensor.fft`** — the CEIR-22 (CRD-Hesap / numerical) band's first slice: DECLARE `ceir.linalg.gemm` (the BLAS3 identity D=α·op(A)·op(B)+β·C {alpha,beta,trans_a,trans_b}, DISTINCT from 21b tensor.matmul — a 3-operand accumulating/scaled/transposed form; matmul==gemm α=1,β=0,no-trans) + `ceir.tensor.fft` (a c2c FFT, complex = SPLIT re/im [2 real f32 tensors in → 2 out] MIRRORING the CKIR c2c split-buffer ABI; NO complex element TypeKind → Dxxx name-forward). NEW `linalg.ceirop.toml` → opgen → `linalg_ops.*` + hand `linalg.hpp`/`linalg.cpp` `find_linalg_misuse` (9 `LinalgMisuseKind`: Tensor-kind + element + A/B rank≥2 + the trans-applied CONTRACTION + BATCH + result [batch..,M,N] identity + accumulator C.shape==D.shape); `tensor.ceirop.toml` gains `fft` → the fft branch of `find_tensor_misuse` (3 NEW TensorMisuseKind [FftInput/Result-ShapeMismatch, FftDirectionInvalid] + reused AxisInvalid/element/kind). ⭐ fft is the FIRST fixed 2-result op — a `[fft][linalg]` ROUND-TRIP test PROVES it crosses BOTH serializers byte-clean (the printer emits the single create_operation result type, the parser reads N result SSA ids + that one type) → UNBLOCKS 22c's text-asset path. ⛔ the im_out-specific verifier lines are DEFENSIVE-redundant under the single-result-type model (re_out≡im_out by construction; no independent malformed test constructible). GATE ×4 configs GREEN (win-debug/asan + linux-gcc-debug/asan: `[linalg]` 3 + `[tensor][fft]` 3 = 6 hand + gen-smoke [linalg 3 + tensor 3] + `ceir.21` 9 RE-RUN not inherited) + opgen drift/validator + gcc `-Werror=switch` + tidy-20 (6 files) + `crd-ceir-invariants` (RE-RUN after the advisor-at-gate fixes: ceir.21 inherited-green re-run; trans_b zero-coverage → 2 tests; the multi-result serializer round-trip). ⛔ 22b CONTRACT (advisor ruling): the native provider REJECTS non-F32 elements (declare stays element-agnostic like 21b; the CKIR kernels are F32) — the F32 restriction rides 22b WITH A TEST, not a bare forward. Files: engine/ceir/ops/linalg.ceirop.toml (NEW), engine/ceir/ops/tensor.ceirop.toml (the fft op + summary), engine/ceir/generated/crd/ceir/gen/{linalg_ops.{hpp,cpp},linalg.ops.{json,md},tensor_ops.{hpp,cpp},tensor.ops.{json,md}} + tests/ceir/generated/test_linalg_gen_smoke.cpp (NEW/regenerated), engine/ceir/include/crd/ceir/linalg.hpp (NEW), engine/ceir/src/linalg.cpp (NEW), engine/ceir/include/crd/ceir/tensor.hpp + engine/ceir/src/tensor.cpp (the fft enum + branch), tests/ceir/test_linalg.cpp (NEW), tests/ceir/test_tensor_fft.cpp (NEW), tests/ceir/CMakeLists.txt (the 2 test entries). ⛔ SHARES engine/ceir/ops/tensor.ceirop.toml + tensor.ops.{json,md} + tensor_ops.* + tensor.hpp/cpp with ⑳/㉒ (the fft additions ride the same NEW-uncommitted tensor files — `git add -p` the fft hunks or fold). ⛔ SHARES tests/ceir/CMakeLists.txt with ⑤/⑭/⑲/⑳/㉑/㉒. ⛔ CEIR-22 band = commit ㉓+ (uncommitted, user commits).
- **㉔ `feat(ceir): CEIR-22b the CEIR→CKIR native provider (synth_gemm/reduce/fft) + device gates`** — the CEIR→CKIR SYNTHESIS provider (`engine/ceir-gpu/{include/crd/ceir/gpu/ckir_synth.hpp,src/ckir_synth.cpp}`): a declare-only ceir.linalg.gemm / ceir.tensor.reduce / ceir.tensor.fft op → a CKIR construction. ⛔ TWO TIERS: synth_gemm+synth_reduce = GRAPH tier (`KirBackend::run` vs `eval_cpu`); synth_fft = KERNEL tier (`build_fft1d_radix2` → `dispatch_kernel_1wg` vs `eval_cpu_kernel`). SYNTHESIS-ONLY: `crd-kir` = a NEW PUBLIC header-only edge into ceir-gpu (its FIRST consumer — resolves the ADR-0109 §4.2 dead edge; ceir-gpu stays backend-free, the device gates link the backends in tests). **12** `SynthReject` kinds (TYPED-REJECT outside the bit-exact envelope, never a silent wrong-result subset: gemm plain-contract-only [α=1∧β=0∧!trans], reduce {sum,prod,max,min}, fft rank-1 pow2 fwd/inv; F32-only — the 22a element-agnostic declare's F32 restriction discharged here with a test). **6 device gates** (gemm/reduce/fft × Vulkan/DX12): bit-exact (gemm/reduce) or declared 2e-3·maxmag tol (fft) vs the tier oracle AND an INDEPENDENT reference (triple-loop gemm / 4× serial-reduction / naive-DFT — ⛔ the 20c-2 guard: eval_cpu runs the SAME synthesized graph). MATRIX ×3 legs: win-debug 4070 Ti Vk+DX12 (ceir.22b 10/10) + lavapipe Vk (7/7 extended) + gcc `-Werror=switch` (12-case switch) + tidy-20 (9 files) + `crd-ceir-invariants` + the touched targets' INHERITED greens re-run (ceir.13 42 + ceir.14 41 + work_smoke 2 + dgc 3). ⛔ REAL BUG the device gate caught: the fft radix-dispatch HANG (`build_fft1d_batched` picks radix by log2(n): n=8→radix8, vs the radix-2 twiddle/local_size contract → `eval_cpu_kernel` mis-simulated + hung 1500s → fixed to `build_fft1d_radix2` + a device-free `CHECK(local_size==n/2)` pin; [[feedback_ckir_fft_batched_radix_dispatch_breaks_fixed_twiddle_contract]]). ⛔ advisor-at-gate fixed 3 blockers: reduce ran sum-only (max→ReduceMin swap passed) → all 4 fns vs distinct refs; fft inverse had ZERO exec coverage → fwd+inverse legs (inverse = unscaled +i DFT); inherited-greens full re-run. Files: engine/ceir-gpu/include/crd/ceir/gpu/ckir_synth.hpp (NEW), engine/ceir-gpu/src/ckir_synth.cpp (NEW), engine/ceir-gpu/CMakeLists.txt (the crd-kir PUBLIC edge + the consumed-edge comment), tests/ceir-gpu/test_ckir_synth.cpp (NEW) + tests/ceir-gpu/CMakeLists.txt, tests/ceir-gpu-vulkan/test_ceir_{gemm,reduce,fft}_vulkan.cpp (NEW) + CMakeLists, tests/ceir-gpu-dx12/test_ceir_{gemm,reduce,fft}_dx12.cpp (NEW) + CMakeLists. ⛔ SHARES tests/ceir-gpu-vulkan/CMakeLists.txt + tests/ceir-gpu-dx12/CMakeLists.txt with ⑮/⑰ (the 20b/20c device-test batches) — `git add -p` the 22b lines. ⛔ CEIR-22 band = commits ㉓–㉔ (uncommitted, user commits).
- **⑤ `feat(ceir): CEIR-19a declare the ceir.rt dialect (RT orchestration)`** — a CLEAN SEPARATE batch (crd-ceir only, no overlap with ①-④; the CEIR-19 band opens). The 6-op `rt` dialect: `engine/ceir/ops/rt.ceirop.toml` → opgen → `engine/ceir/generated/crd/ceir/gen/rt_ops.{hpp,cpp}` + `rt.ops.{json,md}`; hand-written `engine/ceir/include/crd/ceir/rt.hpp` + `engine/ceir/src/rt.cpp` (the scene.hpp/cpp mold — register_dialect + the blas/tlas/sbt opaque Extern type-classes + `find_rt_misuse` with the dispatch-shape checks); `tests/ceir/test_rt.cpp` + the `tests/ceir/CMakeLists.txt` entry; `tests/ceir/generated/test_rt_gen_smoke.cpp` (auto-globbed). ALL 6 ops native{provider=host}+GPUCommand; ray_query kernel_ref. DECLARE-only, ZERO renderer changes (crd-ceir never links gpu-context). Gate: 2 Win + 2 Linux + tidy + opgen-drift, all green (80 assertions).

**Parallel — CEIR-18p ✅ COMPLETE 2026-08-15 (cross-cutting: author+DELETE every hand-built `ensure_*` program/kernel in scene_renderer):** ALL builders converted to disk-read `.ckir` assets loaded via `resolve_program_text` (app-first `app://ckir/<name>` ?? `engine://ckir/<name>`) and DELETED. ⭐⭐ **THE IMPOSTOR (the LAST builder) is done end-to-end** — its FS config (grid/tile/mips) demoted to 18 D12 spec-consts with a 16-way PINNED select chain, 4 assets `impostor_{vs,fs}_{plain,dither}.ckir` emitted, `ensure_impostor_program` is a thin `resolve_program_text` loader, and `build_impostor_vs/fs` + the emit are DELETED (−21 KB). Renders PIXEL-EXACT 133/169/36 FROM DISK on all 3 platforms (Win Vulkan+DX12 full 1728/80; Linux llvmpipe 18/1); kir round-trip 361/14 pins the 18 spec-consts / VS-zero (the structure the pixel gate can't see). ⭐⭐ STEP 0.5 also SURFACED + FIXED a **pre-existing shipped-path defect never device-tested**: `forward_csm_gpu`+`scene_default.crdlod` (7 slots, dither) → the cull-compact kernel's DITHER DUAL-WRITE shared `in_band_u`/`alpha_fine` across two sibling `stmt_if` blocks → `create_program`-null → 0 compute passes SILENTLY (the if-block shared-temp scar [[feedback_ckir_if_block_shared_temp_scope_materialize]]). Fix = `g.stmt_materialize(primary_val/sec_vis/sec_val)` in `vertex_asset.cpp` (backend-agnostic → GLSL+HLSL) + a LOUD `create_program` shaderc-error log (`vulkan_context.cpp` `g_log_vkctx`) + defect (b) hardened (the gate now pins `gc.fill_record_ok==1`, the record-success signal that a cook-fail/step-down returning normal-looking draws would otherwise hide). Detail → tracker (impostor entry). ⚠ UNCOMMITTED (4 assets + vertex_asset.cpp + vulkan_context.cpp + scene_renderer.cpp/.hpp + impostor_atlas.hpp + test_scene_render_gpu.cpp + test_ckir_asset.cpp). NEXT = resume the CEIR-18 band order (18a-2 Forward+ consumption, task 26).

**Prior — CEIR-18d (✅ DONE-BY-VERIFICATION 2026-08-15):** the visibility renderer already ships as `scene_visbuffer.frame.toml` (16z: `scene.raster` geometry=procedural + R32Uint clear, `clear_id=7`) + renders through the full SceneRenderer with a device id read-back in `REN-38-F6 GATE` (Vk + DX12). 18d = re-verify green + close an advisor-caught FALSIFIABILITY hole (`a!=b` had an A==A escape — a primitive that fails to draw reads the `clear_id=7` background yet could pass → added `CHECK(a!=7); CHECK(b!=7)` to both twins). Rides commit ③. Detail → tracker (CEIR-18d ✅).

**Prior — CEIR-18c (✅ DONE + FULLY GATED 2026-08-15):** `engine://frame/deferred` is an AUTHORED asset (G-buffer `raster.mrt`/material_pass=GBuffer → `render::deferred_shade` fullscreen) rendering a REAL scene through a REAL G-buffer on BOTH backends — a PROMOTION (pack_gbuffer + deferred_shade + MRT frame graph all shipped). ⭐⭐ The DO-FIRST strategy (build over REAL indexed scene geometry to lock the 18z proof-harness) drove a compile-clean-but-NEVER-RAN path; disciplined isolation (Vulkan validation oracle + `from_ceir_frame` numbers, never pixel-guessing) surfaced + FIXED **6 DEFECTS**: 4 in the never-run indexed-MRT-scene path [program_gbuffer twin never wired live; emit_scene_list_mrt drew indexed as non-indexed (vertex_count=0); NO indexed-MRT draw verb → added `draw_storage_indexed_mrt` (Vk+DX12+command_lowering, pushes first_draw_index + explicit depth); attachment/depth wiring] + 2 PRE-EXISTING holes the THOROUGH gate caught [§128 DX12 MissingCeirPlan latent since CEIR-17's Vulkan-only gate; 4 CEIR-16d em-dash TEST_CASE names]. GATE (ALL GREEN): 2 Win + win-tidy + 2 Linux (ASan-clean); deferred Vk+DX12 twins 5/5 stable, val_err=0, ABSOLUTE lit verdict (135,105,89) NVIDIA vs llvmpipe ±1. Detail → `docs/detours/D-007-ceir-tracker.md` (CEIR-18c ✅).

**Prior — CEIR-17 (✅ 4-config gated):** ⭐⭐⭐ CEIR-17 SCENE-BRIDGE BAND COMPLETE (17a-e + 17z).** Scene/query/resource resolver semantics DECLARED (a new `scene.resolve_*` intrinsic dialect + 5 Extern type-classes) and rigid/skinned/indirect/GPU-cull PROVEN as CEIR — a PROMOTION, not a rewrite (the resolution/cull/skin were already authored CEIR; the band gave them a declared identity + proved them; ADR-0106 #3 held: ECS extract stays HOST, CEIR sees handles/ids only; RenderResolvers EXTENDED not reinvented). ⭐⭐ The 17z band-close (deletion-gate RE-RUN + a HEAD baseline — GATE-reverifies discipline) SURFACED + FIXED **3 latent CEIR-replay drops the per-slice gates all missed**: (a) **§128 null-plan** — a migrated executor with no plan rendered nothing silently → LOUD `FrameExecError::MissingCeirPlan` at the install site + `execute_frame_graph` stack-builds the plan (also caught REN-38-B1 silently rendering 0); (b) **WBOIT composite-blend** — `build_fullscreen_ceir` set the composite attachment's `load` but DROPPED its `blend` (Opaque composite) → emit the blend attr when `!= Opaque`; (c) **VRS shading_rate/conservative** — dropped post-migration → payload-forward like `force_load` (RenderResolvers + `record_ceir_render` + `materialize_draw_packet`). Deleted stale `scene_gpu_cull.frame.toml` (+3 refs). GATE: **gpu-context-vulkan 260/0 win-debug + win-asan (ASan-clean); 259/1skip linux-gcc-debug + linux-gcc-asan** (VRS caps-skips on llvmpipe = correct SKIP-guard); blast radius (frame-cook 2751/96 + render-graph 309/15 + scene-render 1587/74) + tidy green all 4 configs; band-proof matrix (rigid/skinned/indirect × V/DX + sandbox `--gpu-skin --gpu-cull-verify` smoke 5031 instances). Detail → `docs/design/ceir-17-scene-bridge.md` STATUS + `docs/detours/D-007-ceir-tracker.md` (CEIR-17 ✅). ⛔ **NEXT = CEIR-18** (renderer proof suite: Forward+/Clustered/Deferred/Visibility/GPU-driven as CEIR assets, no new pass algorithm; reuse technique/material stack + B8 lighting; base exists: cull=17d, visibility=16z, B8-deferred green; advisor at band-open, expand sub-slices in the tracker). ⚠ CEIR-16 (c44adbf) + most of CEIR-17 (**8c019fc**: the §128 null-plan fix + REN-38 test plan-builds + `scene_gpu_cull.frame.toml` deletion + 17a-e) are **COMMITTED**; only **7 files remain uncommitted** — the VRS+WBOIT code (`render_materialize.hpp`/`.cpp`, `frame_graph.cpp`, `render_fullscreen_build.cpp`) + these 3 band-close docs (user commits; a fresh session `git status` first).

**Prior (this band):** the CEIR-16 sub-slice journey (16a design → 16b render family → 16c item-view → 16d scene.raster live path incl. the flag dance + mrt≥2 + the-deletion-is-the-proof → 16z visbuffer dissolution) is recorded slice-by-slice in `docs/design/ceir-16-executor-migration.md` STATUS blocks. Every render pass now flows through authored frame graphs → cooked `.crdr` → CEIR. Prior CEIR bands (1–15, the CEIR substrate) + the archived band history are below.

> **CEIR-2 ✅ BAND CLOSED (op-definition generator, §8).** Ops DEFINED in `engine/ceir/ops/*.ceirop.toml` →
> `tools/ceir_opgen/ceir_opgen.py` (stdlib) emits committed C++ (self-registers via the CEIR-1d registry) +
> `.ops.{json,md}` + a gated smoke test — **an op is a TOML edit + regen, ZERO central-enum/switch edits** (proven
> end-to-end by adding the full-surface `test` dialect at 2z). Detail → `docs/sessions/2026-08-08-ceir-2-opgen.md`.
>
> **CEIR-3a ✅ (interned structural types, §16) 2026-08-08.** `TypeId` is now a real interned STRUCTURAL type (scalars +
> the §16 aggregate family): `type.hpp` (`TypeKind`/`FloatKind`/`Type`; **`kind` a FIELD** for I6), `Context::type_*` +
> `type_of` (**asserts, no silent fallback**), a canonical `!`-sigil grammar (`!vec<4x!f32>`, `!struct<P,x:!i32,y:!f32>`,
> `!option<!result<…>>`, …) printer+parser byte-exact, and binary **v2** with a **child-first `TYPE` chunk** (content-pure;
> per-result/arg refs so no future v3 bump). Every `TypeId{n}` magic literal migrated to real factories (a stray one
> aliased to incidental intern state — caught only by the dirty-context purity test). 65/65 × 4 configs + tidy +
> invariants. Detail → `docs/sessions/2026-08-08-ceir-3-types.md`.
> **CEIR-3b ✅ (generics, §16/§98) 2026-08-08.** Appended `TypeParam`/`Trait`/`Callable` kinds (reuse the v2 record —
> **no version bump**); `!param<T,!trait<Ord>>` / `!trait<..>` / `!fn<(P)->(R)>` grammar; a live trait-conformance
> registry (`register_conformance`/`satisfies`, transitive over supertraits) + a memoized, diagnostic-bearing
> `substitute` (unbound params stay generic; a constraint violation reports the (param,trait)). Added `type_is_well_formed`
> — the decoder now rejects structurally-invalid records (a latent 3a gap). 70/70 × 4 configs + tidy + GCC
> `-Werror=switch`.
> **CEIR-3c ✅ (resource + view types, §23) 2026-08-08.** Nine appended kinds (Buffer/Image/Sampler/ResourceTable/
> AccelStruct/VideoFrame/AudioBuffer/ExternalResource/View — **no version bump**, reuse the v2 record). **Interp B**:
> a view TYPE carries the underlying resource + a range-dimension presence MASK (`byte/element/mip/layer/aspect`), the
> range VALUES are runtime (tensor/sparse → 3d; §24 domains = value semantics). Grammar `!buffer<plain,!f32>` /
> `!image<d2,fmt>` / `!view<RES,mip,layer>`; tri-split `view_combination_valid` (parser fails / decoder rejects /
> factory asserts). Fixed a corrupt-input abort (parser fell into the asserting factory on a prior error). 73/73 × 4
> configs + tidy + `-Werror=switch`.
> **CEIR-3d ✅ (shapes + tensors, §21/§35) 2026-08-08.** Four appended kinds (Dim/Shape/Tensor/SparseTensor — **no
> version bump**). Type-level foundation only (the `ceir.shape`/`ceir.tensor` op dialects + layout §22 are CEIR-18;
> tensor/sparse returned from the 3c boundary). `!dim<4|dyn|N>` / `!shape<..>` / `!tensor<e,s>` grammar; tri-state
> `shapes_broadcast` (right-aligned incompatible pos) / `shapes_reshape` (overflow→Unknown). Added `type_is_canonical`
> (decoder rejects non-canonical records — a name on an Int etc. — latent since 3a) + `dyn` reservation tri-split. A
> 3b×3d seam test (substitute a param through a tensor shape). 77/77 × 4 configs + tidy + `-Werror=switch` (31 kinds).
> **CEIR-3e ✅ (physical quantities, §17/§18) 2026-08-08.** One appended kind (`Quantity`, no version bump): tags a
> numeric underlying with an 8-base SI dimension (ADR-0078) **bit-packed into count+cols** (crd::units::Dim is
> compile-time-only → CEIR mirrors it at runtime; a static_assert pins the base order). Grammar `!qty<!f32,L1T-2>` /
> dimensionless `!qty<!f32,1>`; `quantity_dimensions_equal`→first_differing_base (the 3z Length+Time diag), `quantity_dim_mul/div`
> (i8 overflow→failure). ⭐ The **`units.erase` op is the FIRST non-reference dialect through the CEIR-2 generator** (a
> `.ceirop.toml` + regen, zero central edits; drift now 3 dialects × 5 files, smoke auto-generated). 83/83 × 4 configs +
> tidy + `-Werror=switch`.
> **CEIR-3f ✅ (ownership/lifetime qualifiers + escape predicate, §19) 2026-08-08.** One appended kind (`Qualified`, no
> version bump): a WRAPPER — `members[0]`=type, `count`=`OwnershipKind` (9 modes imm/mut/borrow/own/shared/weak/state/ext/
> transient) — chosen over nine kinds (a value has exactly one mode; the wrapper composes over resources too). Grammar
> `!qual<borrow,!buffer<plain,!f32>>`; tri-split `qualified_composition_valid` (rejects qual-of-qual/dim/shape/trait). The
> escape rule splits 3f/3z: 3f ships `value_escapes_region(Value*,Region*)` (def-use walk vs directional region
> containment), 3z composes it over `Qualified<BorrowedView>`. ⛔ Two advisor-caught fixes: **`create_operation` never
> wired `Region::m_parent`** (latent since 1a; the first upward walk reported false-positive escapes) and **`substitute`
> bypassed the tri-split** (retroactively closes qty-of-qty/qual-of-qual via a new `SubstResult::failed_compose`).
> Escape contract = direct-use, type-directed. 87/87 (773 assertions) × 4 configs + tidy + `-Werror=switch` (33 kinds).
> **CEIR-3z ✅ BAND-3 GATE (§16/§17/§19/§21) 2026-08-08.** The four band-3 error checks fire as **discriminating pointing
> diagnostics**: Length+Time (`quantity_dimensions_equal`→first_differing_base, base 0 AND base 2), rank-mismatched
> broadcast (`shapes_broadcast`→position 1 + control), borrowed-view escape (a NEW public `find_borrowed_escape(Module&)`
> →`BorrowEscape{value,escaping_use}` module walk over `!qual<borrow,_>` values — both value kinds; owned-escaper +
> inside-borrow correctly ignored), generic-constraint (`substitute`→failed_param/failed_trait, exact (T,Ord) + accept
> control). Pointing upgrade: `value_escapes_region`(bool) gained `first_escaping_use`→`Operation*` (a bool can't point).
> ⭐ **Honest boundary:** pointing predicates + the one escape walk; the op-level verifier wiring of dim/broadcast/
> constraint onto typed operands composes at **CEIR-4** (no producer op yet — 3e "predicate-now" precedent). `find_borrowed_escape`
> is first-offender, structural (not dominance — CEIR-5b), direct-use+type-directed. Dedicated `test_band3_gate.cpp`
> (`[gate3]`, 4 cases). 91/91 (791 assertions) × 4 configs + tidy + `-Werror=switch`. **→ BAND 3 CLOSED.**
> **CEIR-4a ✅ BAND 4 OPENS (§26 effect vocabulary) 2026-08-09.** `effect.hpp`: `EffectFamily` (all 27 §26 families, u8,
> append-at-end, static_assert-pinned to the generator) + `EffectRecord` POD `{family, target(None/Operand/Result),
> index, range_mask}` (range_mask reuses 3c ViewRange; kind-level — per-instance is 4d, callee-derived EffectsFn is
> CEIR-5). ⭐ Attach point **B1**: effects on `OpInfo` via `register_op(...,effects={})` (arena-COPIED), queried by
> `Context::op_effects` — not a 1d interface, not reflection-only. ⛔ **EMPTY≠UNKNOWN**: an empty span is "provably
> effect-free" only when `op_info!=nullptr`; unregistered = maximally effectful. ⛔ **func.call landmine** (a registered
> op defaulting to effect-free reads as *provably* none) → declares a conservative ExternalCall barrier. Pure⇒zero
> effects at both live arms. 2a schema: bare-family string OR `{family, operand|result, range}` table, generator
> validates vocab+index+range; `OpSchema.effects` StringView[]→EffectRecord[]; `.ops.json` string[]→object[] (schema_v1,
> scaffold field). NOT serialized (no version bump). 98/98 ctest (820 assertions) × 4 configs + tidy + opgen(36 py).
> **CEIR-4b ✅ (§27 determinism + §28 numerics) 2026-08-09.** `semantics.hpp`: `DeterminismClass` (5 §27 tiers +
> `Unspecified=0` default, static_assert-pinned; `External`→`ExternalNondeterminism` reconciled) on `OpInfo` via
> `register_op`, `op_determinism`; `CompilerMode` (Normal/Fast/Deterministic/Certified, session state NOT serialized) +
> `determinism_satisfies_mode` (6×4 matrix). `NumericalSemantics` = all 12 §28 knobs (0=Inherit), per-INSTANCE, ONE
> pack/unpack into a reserved `numerics` int attr (unpack validates bounds); `numerics_satisfies_mode` honors the fmad
> scar (Fast admits FMA/fast-math). `find_mode_violation(Module&)` = first op violating the active mode's §27 class OR §28
> numerics OR a corrupt `numerics` attr (violates every mode). Cook-time native≥op consistency; `OpSchema.native_determinism`
> typed. ⛔ EMPTY≠UNKNOWN (Unspecified fails strict modes). Boundaries: pass-wiring→CEIR-6, replay/alternatives→CEIR-5+,
> kind-vs-instance not cross-checked (CEIR-6). arith int ops = BitExact. 106/106 ctest × 4 configs + tidy + opgen(40 py).
> **CEIR-4c ✅ (§15 eval domains + §32 realtime + domain-legality verifier) 2026-08-09.** ⭐ `register_op` consolidated
> into an `OpSpec{traits,verify,effects,determinism,domain}` descriptor (designated initializers; migrated every site).
> `EvalDomain` (10 §15 domains + Unspecified, static_assert-pinned) per-op-KIND on `OpInfo` via the spec + 2a `domain`
> field + `op_domain`; `OpSchema.domain` StringView→EvalDomain. `RealtimeClass` (7 §32 + Unspecified) — a REGION property,
> NOT a schema field. A region's (domain+realtime) rides ONE packed `region_exec` int attr on the region-owning op (module
> CONTENT — survives round-trip; symmetric with 4b's session-only mode). `find_domain_violation(Module&)→DomainViolation`
> — innermost-tag-wins; seeded §32 rule (FileIO/NetworkIO illegal in AudioRealTime/DeviceTime/HostAudioTime); ⛔ an
> UNREGISTERED op in an audio region is flagged (maximally effectful), a registered effect-free op is legal (empty≠unknown).
> The 4c walk does NOT consume kind-domain (→CEIR-6). arith=EitherHostOrDevice, test=HostFrameTime. 115/115 ctest × 4
> configs + tidy + opgen(43 py).
> **CEIR-4d ✅ (effect-derived ordering hazards §26/§116) 2026-08-09.** A SCHEMA-QUIET slice (hazards DERIVED from 4a
> effects — no TOML/generator/regen/py). `hazard.hpp`: `HazardKind{None,War,Raw,Waw}` + `ResourceClass` (14) +
> `effect_access(EffectFamily)` (TOTAL switch, NO default → -Werror=switch guards a 28th). ⛔ `RandomRead` WRITES (a PRNG
> draw advances the stream — else RNG draws reorder, breaking replay); `TimeRead` inert-read; Alloc/Dealloc/Residency in
> Memory (use-after-free visible); IO one rw class; ExternalCall+Synchronization = Universe barrier. `Context::ops_hazard
> (before,after)` (WAW>RAW>WAR over effect-pairs sharing a resource `(class, Value|null)` + overlapping range with ≥1
> write; distinct Values non-aliasing; reports everything; unknown=Universe rw, Pure=None) + `collect_block_hazards` (O(n²)
> all-pairs reference, one block, list order). `Operation::result` loosened to const. 122/122 ctest × 4 configs + tidy.
> **CEIR-4z ✅ BAND-4 GATE 2026-08-09 → BAND 4 CLOSED (4a–4z).** A TEST-ONLY gate (like 3z) composing find_mode_violation +
> find_domain_violation + ops_hazard/collect_block_hazards over ONE curated module — the band's exit criterion "the
> compiler distinguishes reorderable vs ordered ops correctly" met verbatim. Centerpiece: the **WAR-needs-lifetime scar**
> (read(R)-then-write(R) = WAR from effects+SSA identity, no decl-order; different buffer = None). Exact 4-edge
> collect_block_hazards matrix + reverse sweep; ⛔ every gate op BitExact (the Unspecified-default mode-axis trap);
> orthogonality by FULL edge-list identity (numerics→Certified, Unspecified-op→determinism, side audio-RT+FileIO→domain —
> hazards identical each time); hazards survive serialize/deserialize (Value identity through the parser fixup). `test_band4_gate.cpp`
> (`[gate4]`, 5 cases). 127/127 ctest × 4 configs + tidy. → BAND 4 CLOSED.
> **CEIR-5a ✅ (structured control-flow region ops + the constant-cond if fold) 2026-08-09.** Band 5 opens (a GEAR
> CHANGE). The generator gained a region-SIGNATURE schema + THREE variadic axes (operands/regions/results; arg COUNT is
> the verifier contract, MIN-ARITY builders, full arity via create_operation). `ceir.core` = `scope`/`if` (value-producing,
> variadic results), `for`/`foreach` (typed region arg), `while` (cond+body, structural — cond-yields-1-bool→5b),
> `switch`/`match` (variadic case regions; patterns→CHIR), `yield` (variadic-operand Terminator) — all BitExact + ZERO
> effects. `Context::fold_constant_if` splices the taken branch (THEN and ELSE, both tested) + RAUWs the results with the
> yield's operands + erases; BAILS on non-const/multi-block/no-yield, a `region_exec`-tagged if OR any tagged op INSIDE
> the taken block (⛔ a rewrite must audit the attrs it MOVES), and a yield/result COUNT MISMATCH. Design: HOMOGENEOUS
> result types (placeholders until CEIR-6; the fold replaces them — no create_operation overload), MIN-ARITY builders,
> SKELETON-VERIFIES, loop-carried values→5d. ⚠ This turn disproved 2 checkpoint blocker-guesses (builder-count-param,
> create_operation-overload) — mark checkpoint blockers unverified unless the code was read. 138/138 ctest × 4 + tidy +
> opgen(49 py). NEXT = **CEIR-5b** (SSACFG verifier: dominance/terminators/block-args — the general yield contract + the
> 3f back-link earn their keep, §13/§115) → 5c (calls + EffectsFn) → 5d (state/delay) → 5z (executor gate).

> **CEIR-2 generator (reusable for every future band):** `tools/ceir_opgen/ceir_opgen.py` reads
> `engine/ceir/ops/<dialect>.ceirop.toml` → validates (structure+vocab of every §8 + ADR-0110 field) → emits from ONE
> model: `engine/ceir/generated/crd/ceir/gen/<d>_ops.{hpp,cpp}` + `<d>.ops.{json,md}` + `tests/ceir/generated/test_<d>_gen_smoke.cpp`
> (3 TEST_CASEs, globbed via CONFIGURE_DEPENDS). `--check` drift ctest guards all 5 files/dialect; `test_opgen.py` = 26
> validator/emitter unit tests. **Adding a CEIR op = a TOML edit + regen, zero central-enum/switch edits.**

**⛔ THE LIVE TRACKER IS `docs/detours/D-007-ceir-tracker.md`** (CEIR bands 0–32 + the RAH parallel track). CEIR — the
Cerid Execution IR — is the new master architectural spine (user-directed 2026-08-07): every reusable algorithm becomes
a versioned, inspectable, hot-reloadable **program asset**; native C++ only for genuinely new host/hardware capability.
**THE LAW:** `docs/research/2026-08-07-ceir-universal-programming-master-roadmap.md` (§0–§185). Mantra: *ALGORITHMS ARE
PROGRAM ASSETS · CAPABILITIES ARE NATIVE PRIMITIVES · COMPILERS CHOOSE LOWERINGS · BACKENDS EXECUTE.* The old post-RAF
4-track table in `D-007-gpu-program-system.md` is **re-hung under the CEIR bands** and preserved as history/contracts
(A/RPL→CEIR-15 · C/MLR→CEIR-21 · hesap-GPU→CEIR-19 · frame+executor→CEIR-12/13 · B/I2D→CEIR-28 · D/D7E→CEIR-30).

**CEIR-0 DESIGN PHASE COMPLETE + ACCEPTED (2026-08-07/08):** 0a inventory (headline: RAF already did the atomic-vs-
composite split → CEIR is a promotion, not a rewrite) · **ADR-0108** (owned language stack; C++ no longer the *only*
authorable program — supersedes ADR-0081 §9) · **ADR-0109** (CEIR/CHIR/CKIR one-way layer contract + `crd-ceir`
host-only module + `crd-ceir-host`/`crd-ceir-gpu` dependency-inversion bridges + I3/I4/I5; **binding for CEIR-1**) ·
**ADR-0110** (native-intrinsic schema + plugin levels) · 0e CHIR-0 note · 0g two-axis maturity model · 0h deletion
ledger · 0z §184 report + sizing (CEIR-1…13 ≈ 34–55 KLOC, ~4–8 mo). CEIR-0f (D-007 restructure) executing.

**HOW WE WORK (user-directed):** **strict band order** CEIR-0→32; each band closes at its gate before the next. **RAH
runs in PARALLEL** (the binding/attachment vocabulary CEIR-9/11 lower onto). ⛔ Everything else PAUSED (§176: only bug
fixes, CKIR fixes, tests, docs, RAH, CEIR). ⛔⛔ Foundational/critical-path work done DIRECTLY, never delegated. ⛔⛔
Implementation forks require `isolation:"worktree"` + a tight mandate ([[feedback_implementation_forks_need_worktree_isolation]]).
**User controls commits (no AI trailer).**

## Active state

- **CEIR (spine) — CEIR-1a ✅ CLOSED (4-config per-slice sweep PASS, 2026-08-08).** `crd-ceir` module +
  `Context/Module/Operation/Value/Block/Region` + intrusive in-arena def-use + `crd::memory::GrowableLinearAllocator`
  (moved to crd-memory) + `crd-ceir-invariants` I3/I5 gates — all green across debug/asan/shipping-LTCG/tidy. The full
  sweep peeled **7 pre-existing cross-band blockers** (RAF/REN/CKIR bands never passed shipping-LTCG/asan-complete/tidy);
  all fixed gold-standard (2 real engine bugs: DX12+Vulkan RT pipeline-cache keyed by pointer/handle → content-hash).
  Log: `docs/sessions/2026-08-08-ceir-1a-and-preexisting-fixes.md`.
- **CEIR-1b ✅ CLOSED (2026-08-08).** `SymbolTable` (per-Module, arena-backed HashMap; duplicate-reject) + `Visibility`
  + the `ceir.func` dialect (`func.func`/`func.call`/`func.return`, cross-module resolution by name) — all on the
  generic Context factories (open-world). `tests/ceir` 12/12. **Gated across all 4 configs on crd-ceir** (a complete
  gate — crd-ceir has zero downstream consumers, grep-proven; full-tree sweep re-earns its keep at the band close).
- **CEIR-1c ✅ CLOSED (2026-08-08).** Interned typed attribute VALUES (`AttrValue`/`AttrId`, dedup) + a per-op
  AttrDict (`op->attr(name)` / `Context::set_attr`) + the source map (`register_file`→`file_id`, `file_path`) so
  every op's `SourceLoc` provenance is real (§111, no retrofit). Dissolved the 1b interim: `func.call`'s callee is
  now a `SymbolRef` attribute. `tests/ceir` 18/18. Gated all 4 configs (scoped-complete).
- **CEIR-1d ✅ CLOSED (2026-08-08).** Open-world **dialect registry** + op **traits/interfaces** + **verifier**
  dispatch — analyses query traits/interfaces, the core NEVER switches on op.kind (new **I6** grep-gate proves it,
  bites on `switch(op.kind())`). Unknown-dialect ops survive opaquely; the `func` dialect self-registers.
  `tests/ceir` 22/22. Gated all 4 configs (scoped-complete).
- **CEIR-1e ✅ CLOSED (2026-08-08).** Deterministic textual **printer** (IR→canonical MLIR-flavored text; pre-order SSA
  numbering + name-sorted attrs → byte-identical; floats keep a `.`/`e` marker; unknown-dialect opaque; **no layout**,
  §10) + recursive-descent **parser** (`parse→ParseResult`; use-before-def fixup pass, strings unescaped-before-intern,
  balanced-brace region count skipping string literals, malformed input rejected w/ byte offset). **`print(parse(x))==x`
  byte-exact.** MLIR-faithful symbol identity (advisor): func name/visibility now ride ON the op as `sym_name`/
  `sym_visibility` attrs (SymbolTable = an INDEX over `sym_name`), so identity round-trips through the generic attr
  machinery and the parser rebuilds the module table. `tests/ceir` **31/31**. Gated across the 5-config contract.
- **CEIR-1f ✅ CLOSED (2026-08-08).** **Binary serial form** (`binary.hpp`/`binary.cpp`: `serialize`/`deserialize`) —
  CRDR-shaped (ADR-0038): magic `'CEIR'` + version + FourCC/length chunks a reader **skips by length** when unknown
  (`STRP`/`SRCM`/`ATTR`/`BODY`). ⛔⛔ field-by-field LE (self-contained `put_u*` + `.ok` `Cursor`; can't link crd-kir).
  **⭐ Content-pure:** pools built from the module WALK, BODY holds pool INDICES not Context ids → the blob is a pure
  function of module content (dirty-context byte-equality proven). Carries `Region::kind` (closes the 1e divergence, via
  new `Context::set_region_kind`); `SourceLoc` survives by PATH; symbol identity via the shared `detail::register_symbol`
  (extracted from the parser). `serialize∘deserialize∘serialize` byte-exact; agrees with the text form. Malformed input
  rejected w/ byte offset (bad magic/version/truncation/trailing-junk/oob index). `tests/ceir` **37/37** (`build_rich`
  now a shared `rich_graph.hpp`). Gated across the 5-config contract; invariants I3/I5/I6 green both OSes.
- **CEIR-1g ✅ CLOSED (2026-08-08).** **`ModuleBuilder` fluent API** (`builder.hpp`/`builder.cpp`: `ModuleBuilder` +
  `OpBuilder` proxy + `InsertionGuard`). ⛔⛔ NO privileged bypass — every op routes through `Context::create_operation`
  + shared `detail::register_symbol`, so a builder module is **byte-identical to the hand-built one** (proven).
  `verify(&failing)` dispatches the REAL per-kind `Context::verify` (rejection test proves it, no stub); `build()`
  returns nullptr on a duplicate `sym_name` (op erased, no silent overwrite). `tests/ceir` **41/41**. Gated across the
  5-config contract.
- **CEIR-1h ✅ CLOSED (2026-08-08).** **The permanent harness, seeded** (§119/§167): round-trip fuzz (random valid
  modules via `ModuleBuilder`, fixed xorshift64 seeds — text+binary byte-exact), a `stable_hash` (FNV-1a over the 1f
  content-pure blob — NEW surface, deterministic + content-derived), and a malformed corpus + single-byte-corruption
  SWEEP (no crash; ASan is the proof). ⛔⛔ **The fuzz caught 2 real OOM crashes on day one** in code that had passed 4
  slices of gates — a huge textual def-id and a corrupt binary count; BOTH loaders hardened (text bound by input
  length; binary counts bounded by chunk length / `kMaxDecodeCount`). `tests/ceir` **46/46**. Gated across the 5-config
  contract.
- **CEIR-1z ✅ CLOSED (2026-08-08) — BAND-1 GATE.** A typed hello-world (func + const + call + return) round-trips
  **text ⇄ binary ⇄ builder byte-identically** and its callee symbol resolves after all three forms
  (`tests/ceir/test_hello.cpp`). `tests/ceir` **49/49**. Gated across the 5-config contract. ⭐⭐ **BAND 1 CLOSED
  (1a..1z).** Already committed this session: 1a core (`5f81ce8`) + the 7 pre-existing fixes & 1a docs (`6e6f183 "CEIR-1a
  finished"`). ⛔ NOW: (A) the USER commits+pushes the remaining **CEIR-1b..1z** batch (~38 files: `engine/ceir/**` +
  `tests/ceir/**` + `scripts/check_ceir_invariants.{ps1,sh}` + docs) — ONE commit
  `feat(ceir): band 1 core IR substrate (CEIR-1b..1z)` with the per-slice breakdown in the body (slices overlap in
  files → not per-slice stageable). (B) then make **GitHub CI GREEN** (whole-repo net; fix reds gold-standard, user
  commits fix batches). (C) MEMORY.md compaction to <17.1KB during the wait. Only after CI green → **CEIR-2**.
- **RAH (parallel track) — front = RAH-1a.2.** ✅ RAH-1a.1 (visbuffer fold) DONE + gated (REN-38-F6, 97 asserts, both
  backends). **NEXT = RAH-1a.2 (DELETE, user-chosen):** retire `IGBufferTarget`+`draw_gbuffer`+`create_gbuffer_target`
  (both backends) + `RenderingDesc.gbuffer`; migrate ~8 test sites to the `color`-span MRT path; needs a
  plain-vertex-MRT-color-span path + regular-target readback first. Then RAH-1a-close → RAH-2 (unblocks CEIR-11/B).
- **PAUSED (parked, not dropped):** B/I2D+SPR (ADR-0107 review pending) · C/CGP selector + HGP/MLR · D/MED codecs
  (animated GIF→TIFF→JPEG; real-GIF external-oracle corpus owed) · main roadmap (hesap v18, eylem v1c+). Resume paths:
  the CEIR tracker's "Paused" table.

## Recently landed

- **2026-08-08** — **CEIR-1a CLOSED** (4-config per-slice sweep PASS). The global close peeled 7 pre-existing
  cross-band blockers (RAF/REN/CKIR left them: shipping-LTCG/asan-complete/tidy had never run to completion) — all
  fixed gold-standard, incl. two real engine bugs (DX12+Vulkan RT pipeline caches keyed by pointer/handle →
  fnv1a_64 content hash; DX12 anyhit flake 200/200 after), the RAF-10 catch_discover_tests ENVIRONMENT split, the
  AS-4 ASan timing guard, the C4743 stale-obj wipe, and 37 clang-tidy errors across 12 files. Uncommitted (19 files;
  user commits — proposed message in the log). Log: `docs/sessions/2026-08-08-ceir-1a-and-preexisting-fixes.md`.
- **2026-08-07 (later)** — repository-wide **documentation hygiene pass** (uncommitted): context.md → dashboard
  (history archived), ROADMAP/systems/debt/AGENTS/READMEs refreshed to honest state, retired-module overviews
  DELETED (user direction; git history keeps them), research outcome stamps, ADR index + link fixes. Full report:
  `docs/sessions/2026-08-07-doc-hygiene-pass.md`.
- **2026-08-07/08** — **CEIR pivot + CEIR-0 design phase COMPLETE:** CEIR becomes the master spine; the live tracker
  `docs/detours/D-007-ceir-tracker.md` created; CEIR-0a inventory + ADRs 0108/0109/0110 + the 0e/0g/0h/0z design notes
  all accepted; D-007 restructured (CEIR-0f). (uncommitted at time of writing — user commits.)
- **2026-08-07** — post-RAF 4-track kickoff: RAH-1a.1 + CGP-0/CUDA + MED-1 (`c116e98`); D-007 §POST-RAF + §UI/2D
  programmes + four-track tracker (`e3f8e5e`). Log: `docs/sessions/2026-08-07-post-raf-tracks-rah1-cuda.md`.
- **2026-08-06** — **RAF band COMPLETE** (`af3e04c`): `FramePassKind` retired, ADR-0106 closed.
  Log: `docs/sessions/2026-08-06-raf12-3-retire-framepasskind.md`.
- **2026-08-03…05** — RAF-0…12: substrate → one-submission frame graph → executors → engine-default assets →
  app-custom renderer → hot reload → legacy deletion. Logs: `docs/sessions/2026-08-0{3,4,5}-raf*.md`.

## Open questions / risks

- **Per-slice gate — RATIFIED (2026-08-08):** each slice closes on **2 Windows + 2 Linux configs + tidy**, all
  clean (win-debug + win-asan + linux-debug + linux-asan + tidy; Linux via WSL), **scoped to the changed module**
  (crd-ceir has zero downstream, grep-proven → crd-ceir-tests across those configs is complete). **No whole-repo
  suite per slice** (too slow). **GitHub CI is the whole-repo safety net and must stay GREEN.** See
  `project_ceir_autonomous_loop_grant`. ⛔ **Between CEIR-1 and CEIR-2: fix CI green** (it has real build/test reds).
  ⛔ **At CEIR-14: expand its subslices explicitly in the tracker.** GOAL = all bands 1→32 closed.
- **Pending user review:** RAH-0 audit (`docs/systems/rah-0-canonical-model-audit.md`) + ADR-0107
  (`docs/decisions/0107-ui-2d-architecture.md`). Track B code is blocked on the ADR-0107 review.
- `MEMORY.md` ≈ 19.9 KB (hard read limit 24.4 KB) — deeper cull deferred, entries must be MERGED/DROPPED not just
  hook-trimmed.
- The integrated CUDA fork worktree `.claude/worktrees/agent-af34b487c5544c8fa` can be removed.
