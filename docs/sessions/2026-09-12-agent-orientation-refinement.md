# Agent orientation refinement — 2026-09-12

<!-- doc-role: evidence -->
> Dated evidence; counts, results and Next paragraphs are historical. Current work: [ROADMAP](../ROADMAP.md); current rules: [AGENTS](../../AGENTS.md).

Purpose: one understandable, portable path for every human/agent from current work to contract, source, bug and
evidence. This is documentation/tooling work; it does not claim engine builds or renderer/UI implementation.
Live completion is recorded only in [ROADMAP](../ROADMAP.md#slice-docs).

The core rulebooks, memory index and lookup workflow were compacted. Full previous instructions, sanity ledger
and indexes are preserved in [orientation history](../archive/2026-09-12-orientation-history.md). Ninety-five long
phase/system/CEIR entry documents now point to preserved bodies. All 457 host-memory records were consolidated
into seven portable reference files with named lookup; the source SHA-256 inventory preserves provenance.

Conflicts corrected: completed CEIR loop advertised as active; old phase-local tracking; duplicated Next lists;
auto-format versus the later hand-format directive; unfiltered local sweeps versus scoped CI-backed verification;
bare test-binary success and retry-pass claims; future unfiled ADR links. SANITY's stranded concerns now have
individual ROADMAP rows. All A01…A27 audit findings link to their owning rows.

Continue via context/ROADMAP, not a historical Next list. No engine build, runtime test or benchmark was performed.

## Final verification

The documentation validator passed across **666 master rows, 983 repository documents and 7,384 local links**,
including case-sensitive path spelling. It checks row states, prerequisites, parent/child completion, evidence links,
one tracker role, the current pointer and orientation size limits. All A01…A27 findings have master-row owners.
Slice search includes legacy children such as RAH-1a.2; finding search A24 reaches FOUNDATION-AUDIT; memory search
and exact-record lookup work; `--next` points to AUD-2. Negative probes rejected a missing file and anchor while
leaving C++/TOML code syntax alone.

All **457 memory records** retain their original fenced/inline code spans, guarded by source digests. Sixty-seven
legacy naming aliases resolve. Two absent historical reference names explicitly lead to current rules without
pretending their missing source existed. The external project memory directory was updated with permission and
hash-verified: its index redirects to portable repository memory; every source record warns that old state,
schedules and grants defer to the current rules. No original reference body was deleted.

The seven orientation entry documents shrank from **134,190 to 31,726 bytes (76.4%)**. Full prior
wording/technical detail remains in linked references. `git diff --check` passed for changed documentation and the
validator; Git's line-ending normalization notice is not a content/check failure. The durable measurement and
source-preservation record is `docs/research/2026-09-12-documentation-refinement.json`.

Suggested commit: `docs: unify and compact agent orientation around one roadmap`. User commits; no agent commit/push.
