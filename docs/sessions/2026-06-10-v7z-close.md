# 2026-06-10 — hesap v7-z CLOSE: CLI + the gold-standard scoreboard + docs (same session, part 17)

**Phase:** 3.1.6 `crd-hesap` · v7 optimisation — **THE CLUSTER CLOSE.** All local parts done; the IPOPT row,
the 18-config CI sweep, and the v7 commit are the named user-side remainder.

---

## CLI `hesap.opt.*` (the agent-native surface)

`src/cli_register_opt.cpp` + `cli_anchor.hpp` (the v6 eigen pattern): **`hesap.opt.{qp,lp,mip,conic}.f64`**
— the DATA-defined families (arrays in, [status, obj, x, duals] blobs out; QP/LP method selectors; conic
(type, dim) cone pairs). The nonlinear members need callable objectives and stay API-level by design.
Tests: registration + analytic-answer invocations (the box-projection QP, the vertex LP both methods, the
proven-optimum MIP, the norm-ball SOCP) + error paths — 63 asserts / 6 cases. Suite **3774/151**; 4 configs
+ guards green.

## ⭐⭐ The gold-standard scoreboard (same machine, formula-pinned identical problems)

`scripts/opt_scoreboard.py` (peers pip-installed on WSL: scipy 1.17.1, osqp, quadprog, scs, highspy, pycma)
vs `runtime/examples/opt_scoreboard.cpp` (the Cerid side, gcc -O2 + the linux-gcc-release libs):

| Problem | Reference | Cerid | Verdict |
|---|---|---|---|
| QP n=30 m=40 | OSQP/quadprog obj **−0.786540016** | ADMM/Mehrotra/GI **−0.786540016** | exact agreement; GI 0.01 ms vs quadprog 0.03 ms; ADMM 54 it vs OSQP 25 (OSQP's scaling) |
| LP n=40 m=25 | HiGHS **−34.455913325** (1.19 ms) | simplex+IPM **−34.455913325** (0.27 ms) | exact; wall caveated (tiny instance) |
| SOCP ball | scs 2.292893219 | 2.292893221 | analytic ±1e-9 both |
| MIP knapsack-12 | highspy **−80** (2.23 ms) | B&B **−80**, 21 nodes (0.02 ms) | both PROVEN; wall caveated |
| Nelder-Mead | scipy nfev **219**, f 1.099e-18 | nfev **219**, f 1.099e-18 | ⭐ **identical trajectory** |
| Powell | scipy nfev 792 | nfev 982 | same class (Brent-bracket latitude); both at the optimum |
| trust-ncg | scipy **31/28**, f 8.628e-25 | Steihaug **31/28**, f 8.628e-25 | ⭐ **identical trajectory** |
| trust-exact | scipy **27/24**, f 0.0 | exact **27/24**, f 0.0 | ⭐ **identical trajectory** |
| trust-krylov | scipy nfev **401** (warns) | GLTR nfev **39** | a genuine WIN row |
| scipy DE | f **0.995** (misses the Rastrigin-4 global) | f **0.0** (finds it) | a genuine WIN row |
| scipy basinhopping | f 5.4e-14, 8881 evals | f 3.4e-19, 15153 evals | both solve |
| pycma sphere-8 / rosen-5 | 1391 / 2254 evals | 1690 / 2688 evals | eval-comparable ≈1.2× |

HONEST caveats (recorded): Python-side wall-clocks carry per-eval interpreter overhead on the callback rows
(NM/Powell/DE/BH/CMA — **eval counts are the honest metric there**, and we sit at parity-to-exact); the
compiled peers' tiny instances are overhead-sensitive. The exact NM/trust-ncg/trust-exact trajectory matches
are the scipy analog of the NLopt port bit-exactness.

Rows incorporated by reference from earlier slices: **liblbfgs/scipy L-BFGS-B eval-parity 0.93–1.02×**
(v7-d, fair-SSE2 peer) · **CHOLMOD structure-dependent** (v7-e-2: FEA wins, 3D-lattice loss = the ADR-0082
kernel wall) · **NLopt COBYLA/NEWUOA/BOBYQA BIT-EXACT same-source** (8868 oracle checks, 0 failures) ·
**L-BFGS-B per-routine bit-identical** to the code scipy wraps · PyTorch: the v7-i closed-form step gates
ARE torch's documented formulas (the live-trajectory comparison deferred with the ~GB torch wheel — named).

And over it all: **the determinism moat** — bit-identical trajectories across runs and {1..16} workers,
which NONE of the peers carries.

## Docs + ADR

`docs/systems/hesap-opt.md` rewritten to the full shipped method set (the family table + verification style
+ honest scope notes). ADR-0090 stands Accepted (2026-06-07) — the shipped surface matches it.

## The user-side remainder (named)

1. **The IPOPT row**: `sudo apt-get install -y coinor-libipopt-dev pkg-config` →
   `bash scripts/setup-ipopt-ref.sh` (the scoreboard script auto-detects cyipopt and reports).
2. **The v7 COMMIT** (the user commits at phase boundaries; the tree holds v7-f → v7-z, ~17 slices).
3. **The 18-config sweep on CI** post-commit (the standing local-test directive: CI owns the sweep).

## v7 — the optimization domain — is COMPLETE

a→z: substrate · derivatives · line searches · L-BFGS/L-BFGS-B · dense+sparse LM · first-order · Newton ·
trust-region · stochastic+Philox · KKT · QP·LP·conic·NLP·modeling · the DFO trio + three verified Powell
ports · global/CMA-ES · MIP · CLI + scoreboard. Suite 3774/151 (+ stats 230660/7). Five gate-caught real
bugs fixed in-session, zero deferred.
