# Cerid — Live Context

> Engine'in kısa-vadeli hafızası — "Şu an neredeyiz?" sorusuna cevap verir.
> "Master plan ne?" sorusunun cevabı `docs/ROADMAP.md` ve oradan dallanan dosyalardadır.
>
> Bu dosya bir DASHBOARD'dur, append-only changelog değildir. Her milestone session log'a yazılır
> (`docs/sessions/YYYY-MM-DD-*.md`); buradaki "Last shipped milestone" sadece o session log'a link verir.
> Session log'ların listesi de `docs/sessions/` klasörünün kendisindedir, burada değil.

---

## Current focus

**As of 2026-05-16:** Phase 3.1.7 `crd-geometry` is the active phase.
**7 of 11 sub-modules complete + v7 `-mesh-processing` 🚧 in-flight**
(53 of 49 renewed-scope slices via v7 4→9 expansion per substrate
separation; nothing cut) — `-primitives` ✅ + `-bvh` ✅ + `-convex` ✅ +
v3 hull-extension ✅ + `-mesh` ✅ + `-spatial` ✅ + `-polygon` ✅ +
`-mesh-processing` 🚧 (v7a HalfEdgeMesh substrate in flight). ADR-0076
has 9 amendments (§12 through §21) all accepted; ADR-0078 (units, §1–§5)
+ ADR-0079 (perf) accepted. Phase 3.0 ✅ + Phase 3.1.7.5 ✅ + Phase 3.1
⏸ paused at v1b. **🎯 v7 plan = 9 slices**: v7a `HalfEdgeMesh` substrate
(half-edge mutable mesh data structure; the primitive every v7 algorithm
shares) → v7b Garland-Heckbert QEM decimation → v7c Loop subdivision →
v7d Botsch-Kobbelt isotropic remeshing → v7e Liepa hole filling → v7f
manifoldness repair → v7g self-intersection removal (consumes v6e
Bentley-Ottmann) → v7h Taubin volume-preserving smoothing → v7-close.

**Chronological session detail below** (2026-05-15 units / time / perf
+ 2026-05-16 v4 mesh / v5 spatial / v6 polygon).

---

**Actual sequence shipped 2026-05-15 (in chronological order):**

1. **Sprint 0 — per-slice protocol fix** (`scripts/per-slice-check.{ps1,sh}` + `docs/protocols/per-slice-verification.md` codify the win-debug + win-asan + win-shipping + win-tidy ctest-not-binary rule).
2. **Phase 3.1.7.5 v0a `crd-units` substrate ✅ SHIPPED** — new `engine/units/` module (10 headers + 1 cpp, ~1.6 KLOC); full 6-layer conversion system (`LinearUnit` + `AffineUnit` + `NonLinearUnit` + `UnitMul/Div/Pow` + federated registration + value_in boundary accessor); ~120 named units across SI + imperial + audio + electrical + thermal; 80+ UDLs; **`crd-no-untagged-physical-numeric` CI guard live**; 8 test files / **138 cases / 464 assertions**, 4-config green. **ADR-0078 Accepted.** System doc `docs/systems/units.md`. Session log `docs/sessions/2026-05-15-units-v0a-substrate.md`.
3. **Detour D-006 `crd-time` substrate ✅ SHIPPED** — new `engine/time/` module; `Instant` + `Duration = Quantity<dim::Time, f64>` (**first major consumer of the units substrate**) + `MonotonicClock`/`WallClock`/`CycleCounter` + `Stopwatch` + `FrameClock` + `DeterministicClock` + `Deadline` + `GpuTimestampHandle`. Move-and-deletes `engine/platform/include/crd/platform/timer.hpp` per the ADR-0076 §13 pattern. 53 cases / 102 assertions. Session log `docs/sessions/2026-05-15-d006-crd-time-substrate.md`.
4. **Detour D-003 `crd-perf` profiler + `crd-perf-ui` ImGui frontend ✅ SHIPPED** — 8 slices (v0a-v0h) same session. New `engine/perf/` + `engine/perf-ui/`; `crd-rhi-vulkan` gains `VulkanProfilerBackend`; `crd-jobs` gains `JobObserver` hook; `crd-memory` `MemoryStats` gate widened. **`win-shipping-profile` preset** added (per-slice DoD 4 → 5 configs). 97 perf-* cases / 336 assertions. **ADR-0079 Accepted.** System doc `docs/systems/perf.md`. 8 session logs `docs/sessions/2026-05-15-d003-v0{a..h}-*.md`. **Renamed `crd-profiler` → `crd-perf`** (collision with `crd-profile` quality-preset module).

**Full project ctest grew 1546 → 1844 across the day (+298 cases).** Both shipped modules consume `crd-units` from day 1 — D-006's `Duration` is `Quantity<dim::Time, f64>`; D-003's GPU timestamps + counter `DURATION` macros plumb through it.

---

**Phase 3.1.7.5 v0b adoption pass A ✅ SHIPPED 2026-05-15** (4 sub-slices + close, all 5-config green):
- **v0b-1 ✅** — `Vec<Quantity>` enablement: `MathValue` concept widens `Vec2`/`Vec3`/`Vec4` to accept `Quantity<D, T>` for element-wise ops; reductions stay on strict `MathScalar`. Precision-suffix aliases (`Length32`/`Length64`/`Mass32`/...). `to_raw_vec` / `from_raw_vec` boundary helpers; `from_trs(Vec3<Quantity>...)` overload. 13 cases / 40 assertions.
- **v0b-2 ✅** — `crd-config` 13 unit-tagged TOML accessors (`get_length`/`get_mass`/`get_time`/`get_angle`/`get_velocity`/`get_force`/`get_pressure`/`get_energy`/`get_power`/`get_voltage`/`get_current`/`get_frequency`/`get_temperature`). Suffix tables map authoring strings to SI; missing-key returns fallback (not 0). f32 + f64 instantiations. 13 cases / 57 assertions.
- **v0b-3 ✅** — `scene::Transform::translation` retyped to `Vec3<Length32>`; ~30 call sites across `World::set_local`/`set_translation`, eylem integrator-to-transform sync, glTF scene cooker, eylem-viz visualizers, sandbox ImGui inspector, and tests bridged with `from_raw_vec<dim::Length>` / `.value`.
- **v0b-4 ✅** — glTF cooker `.meta` `[cook] position_scale` key (cm/mm/in exports converted to SI at cook time); SI sanity-warn at `> 1e6 m`. 12 cases / 15 assertions. Pure-string parser is testable without filesystem.
- **v0b-close ✅** — ADR-0078 §2 amendment (D15-D19), `docs/systems/units.md` updated with status + boundary stencils, session log `docs/sessions/2026-05-15-units-v0b-adoption-a.md`, full 5-config sweep green (win-debug 1882/1882, win-asan 1882/1882, win-shipping 1795/1795, win-shipping-profile 1877/1877, win-tidy build-clean).

**Phase 3.1.7.5 v0c adoption pass B ✅ SHIPPED 2026-05-15** (v0c-1 + v0c-2 + v0c-3 + v0c-close, all 5-config green; v0c-4 deferred):
- **v0c-1 ✅** — `crd-eylem RigidBody` dimensional surface (`position: Vec3<Length32>`, `linear_velocity: Vec3<Velocity32>`, `angular_velocity: Vec3<AngularVelocity32>`, `inv_mass: InverseMass32 = Quantity<DimInv<Mass>, f32>`, `inv_inertia: Vec3<InverseMomentOfInertia32>`). `PhysicsConfig` typed end-to-end (`gravity: Vec3<Acceleration32>`, `fixed_dt: Duration32`, sleep thresholds typed, contact offsets typed). `IPhysicsScene` public surface typed (`set_gravity`, `apply_force(Force)`, `apply_torque(Torque)`, `apply_impulse(Momentum, Length)`, `step(Duration32)`, `raycast(Length, Vec3f direction, Length max)`, `RaycastHit { point: Vec3<Length32>; normal: Vec3f; distance: Length32 }`). 80-byte `RigidBody` freeze pin preserved (ADR-0062 §15) — precision-tier orthogonal to dimension per ADR-0078 §1 D12. New `InverseMass` / `InverseMomentOfInertia` dim aliases + `Momentum32` / `MomentOfInertia32` / `Duration32` concrete aliases.
- **v0c-2 ✅** — Integrator typed-math end-to-end via 6 new cross-Dim `Vec<Q1> * Q2 -> Vec<DimMul<Q1,Q2>>` overloads in `crd/math/vec.hpp`. The integrator pattern `v += gravity * dt; p += v * dt` now type-checks: `Acceleration * Time -> Velocity`, `Velocity * Time -> Length` close at compile time without `.value` escapes in the hot path. Damping factor (dimensionless rate) bridges raw at compute site only. `crd::math::lerp` stays `MathScalar`-only per ADR-0078 §1 D2; interpolation bridges through `to_raw_vec`. Quaternion `q̇ = 0.5·ω·q` escapes angular_velocity via `.value` at the unit-norm quat ctor.
- **v0c-3 ✅** — `ForceFieldComponent` geometric params typed: `origin: Vec3<Length32>`, `radius_min/radius_max/noise_scale: Length32`. Formula coefficients (`magnitude`/`polarity`/`falloff_p`/`poly_coeffs`/`noise_time`/`noise_octaves`) stay raw — formula-polymorphic per `FieldFormula` + `FieldMassCoupling`, set at use-time by the v1f `EylemFieldSystem` impl.
- **v0c-4 DEFERRED** — `crd-geometry-primitives` API re-tag blocked on `DimRoot<>` (fractional-exponent dimensions for `length(Vec3<Length>) -> Length` etc., explicitly deferred per ADR-0078 §1 D2). Primitives carry SI interpretation by documentation contract (D24) until DimRoot lands in v0d.
- **v0c-close ✅** — ADR-0078 §3 amendment (D20-D25 — RigidBody/Config/Scene dimensional surface, cross-Dim Vec*Q overloads, SIMD-boundary raw-f32 pin, signed_distance.hpp stays raw, geometry-primitives surface re-tag deferred to post-DimRoot, 5-config DoD carried forward). Session log `docs/sessions/2026-05-15-units-v0c-adoption-b.md`. 5-config sweep green (win-debug 1882/1882, win-asan 1882/1882, win-shipping 1795/1795, win-shipping-profile 1877/1877, win-tidy build-clean).

**Phase 3.1.7.5 v0d adoption pass C ✅ SHIPPED 2026-05-15 — Phase 3.1.7.5 CLOSED**:
- **v0d-1 ✅** — `Vec<Quantity>` reductions widened (length/dot/cross/distance/normalized/hadamard) via DimMul-then-retag. The earlier "DimRoot needed" framing was overstated.
- **v0d-2 ✅** — `crd-geometry-primitives` API re-tag: struct widening (95 sites in primitives.hpp) + new `queries_typed.hpp` boundary-wrapper layer (8 closest_point + 5 distance + 4 distance_squared typed overloads). Algorithms stay `<MathScalar T>`; typed surface lives one layer above with strip-compute-retag at the boundary. Foundation widening in scalar.hpp (Quantity overloads for abs/min/max/clamp/is_finite/is_nan/default_epsilon + new sqrt_as helper).
- **v0d-3 ✅** — `crd-renderer` raw-Mat4f boundary contract pinned in `FrameContext`. Renderer never imports `crd/units/*`. SIMD upload bit-identical to pre-units.
- **v0d-4 ✅** — `crd-resources` byte-buffer contract pinned in `mesh_resource.hpp`. Typed boundary lives at cooker + ECS.
- **v0d-5 ✅** — Layer-6 `UnitPreferences` + `format_*` / `parse_*` for 13 Dims (Length/Mass/Time/Angle/Velocity/Force/Pressure/Energy/Power/Voltage/Current/Frequency/Temperature) + 11 discipline-preset factories (game/CAD/robotics-REP-103/aerospace/PCB/audio/3D-print/CAM/cinematic/imperial/SI-strict/scientific). Affine temperature (K↔C↔F↔Ra). 14 cases / 42 assertions.
- **v0d-6 ✅** — `crd-imgui` `unit_preferences_inspector.hpp` header-only ImGui inspector (preset picker + per-Dim combos + precision/suffix/sci-notation toggles).
- **v0d-close ✅** — ADR-0078 §4 amendment (D26-D31 — Vec reductions widened without DimRoot, geometry-primitives struct + boundary-wrapper pattern, renderer raw-Mat4f contract, resources byte-buffer contract, Layer-6 UnitPreferences, Phase 3.1.7.5 CLOSE marker). Session log `docs/sessions/2026-05-15-units-v0d-phase-close.md`. 11-config Windows full sweep PASS.

🎉 **Phase 3.1.7.5 `crd-units` CLOSED 2026-05-15** — 19 sub-slices / ~4.5 KLOC engine / ~2.3 KLOC tests / 31 locked design decisions in ADR-0078 (§1-§4). Full project ctest 1546 → **1913 win-debug** across the phase (+367 cases). Every physical/scientific quantity at every API boundary carries a compile-time dimension tag. **Mars Climate Orbiter at the engine surface is now a compile error.**

**Geometry v4a `mesh_closest_point` shipped 2026-05-16** — new `engine/geometry-mesh/` module (TriangleMeshView + TriangleMeshBvh + mesh_closest_point via Ericson cascade over BVH-indexed triangles, branch-and-bound traversal with lower-bound pruning, lowest-index tiebreak per ADR-0076 §4 pin #11) + typed-overload wrapper `mesh_queries_typed.hpp` per ADR-0078 §5. 6 cases / 145 assertions; 5-config DoD green. Full project ctest 1913 → 1919.

**Geometry v4b `mesh_raycast` shipped 2026-05-16** — Woop 2013 watertight ray-tri at BVH leaves + Williams/Ize slab traversal. 8 cases / 16 assertions; full project ctest 1919 → 1927.

**Geometry v4c `mesh_winding_number` shipped 2026-05-16** — Jacobson 2013 generalised winding number; 8 cases / 281 assertions; ctest 1927 → 1935.

**Geometry v4-validate shipped 2026-05-16** — `validate_triangle_mesh(view, alloc, opts) → MeshValidationReport`. Six defect kinds: OutOfBoundsIndex / DegenerateTriangle / ZeroAreaTriangle / NonManifoldEdge / BoundaryEdge / InconsistentOrientation. Three-pass deterministic: pass 1 = triangle-level (bounds + degenerate + area-zero via |e1×e2|² vs (2·ε)²); pass 2 = build sorted edge table with canonical (min,max) keys; pass 3 = run-classify by count (1=boundary, 2=manifold with orientation check, ≥3=non-manifold). `well_formed` excludes critical defects only; `watertight` requires well_formed + 0 boundary + triangle_count > 0. **`Area32` typed at options surface; raw `f32` inside per ADR-0078 §5 D34.** Sorted-edge classification beats hashmap for cross-compiler determinism. 10 cases / 46 assertions; 5-config DoD green (win-debug 1952/1952, win-asan 1952/1952, win-shipping 1865/1865, win-shipping-profile 1947/1947, win-tidy clean). Full project ctest 1942 → 1952. Session log `docs/sessions/2026-05-16-geometry-v4-validate.md`.

**🎉 Phase 3.1.7 v4 `-mesh` cluster CLOSED 2026-05-16** — v4a + v4b + v4c + v4d + v4-validate + v4-close. 39 cases / 503 assertions; ADR-0076 §19 amendment (9 locked decisions); `docs/systems/geometry-mesh.md`; 18-config full sweep PASS (11:49, 0 failed). Phase 3.1.7 5th of 11 sub-modules COMPLETE — `-primitives` + `-bvh` + `-convex` + v3 convex-hull extension + **`-mesh`** all closed. 37 of 49 renewed-scope slices shipped (~75%).

**Geometry v5a `KdTree` shipped 2026-05-16** — new `engine/geometry-spatial/` module (umbrella + 4 query headers + 4 .cpp files + typed wrapper). Static balanced KD-tree over point sets: `KdTree<T>` (12-byte / 16-byte `KdNode<f32>` / `<f64>` flat array — `static_assert`-pinned — + `Array<u32>` payload permutation; non-owning view) + deterministic `kd_build` + 3 queries (`kd_radius` / `kd_nearest_n` / `kd_range_aabb`). **Elite-tier choices**: widest-extent split axis (X<Y<Z tiebreak; nanoflann/PCL pattern, NOT canonical round-robin) + leaf bucket size 8 default + median pick via `crd::containers::nth_element` with lex-tuple `(coord, original_index)` comparator (no two elements compare equal → bit-identical tree topology across MSVC / GCC / clang) + pre-allocate-both-children-before-DFS-recursion (the `bvh_build` adjacency trick). k-NN uses caller-allocated max-heap via `crd::containers::push_heap` / `pop_heap` (deterministic), `MaxByDistance` orders `(d², payload)` lex so tied-distance higher-payload sits at top → §4 pin #11 tiebreak by eviction; pruning is strict `>` (tied lower-bound subtree may hold tied-distance lower-payload winner — must descend); final ascending sort via `crd::containers::sort` for reproducibility. **k-NN API takes `k` explicitly NOT via `out.capacity()`** — `Array::reserve` rounds up, capacity-as-k is unreliable (caught at first run with 1-vs-8 mismatch). Builder REJECTS non-finite (debug `CRD_ASSERT`); queries TOLERATE non-finite (every finite-vs-NaN compare false → AABB-prune kills every subtree at root). Two-layer typing per ADR-0078 §5 D34: `kd_queries_typed.hpp` strip-compute-retag wrappers (`Vec3<Length32>` / `Length32` in, distance squared `Quantity<DimMul<D,D>, T>` out). 24 cases / brute-force cross-validated on 500-1000 pt random clouds × 5 trial seeds × 5 radii / 4 k values; permutation determinism (5 shuffles → set-equal positions); large-coord stability (origin + 1e6 → set-equal payloads, NOT byte-identical topology — f32 ULP at 1e6 ≈ 0.0625 validly reorders sub-ULP separations). 5-config DoD PASS: win-debug 1977/1977 + win-asan + win-shipping + win-shipping-profile 1972/1972 + win-tidy. Full project ctest 1952 → 1977 win-debug. Session log `docs/sessions/2026-05-16-geometry-v5a-kd-tree.md`.

**Geometry v5b `LooseOctree` shipped 2026-05-16** — same `engine/geometry-spatial/` module. Ulrich 2000 dynamic AABB index with overlapping children (loose factor 2.0): 64-byte `OctreeNode<T>` (`static_assert`-pinned: AABB(24) + 8×u32 children(32) + parent(4) + depth(1) + flags(1) + pad(2)) + free-list-managed object pool with stable `OctreeObjectId` handle (DynamicBvh precedent). Per-cell `Array<u32 obj_idx>` allocated on first insert. **Locked elite-tier choices** (advisor 2026-05-16): (1) update fast-path predicate = AABB-fit only, NOT center-in-cell — Ulrich's correctness invariant; (2) raycast = t-near-first child descent + best_t pruning, emission order NOT part of API contract — only nearest result is (BVH pattern, ADR-0076 §16 pin #2); (3) overlap = Morton-order child traversal (000…111) deterministic emission; (4) octant tiebreak `>=` (lower-octant-wins on midplane); (5) lowest-payload-index tiebreak on equal raycast `t` (§4 pin #11); (6) builder REJECTS non-finite + out-of-root + oversized; queries TOLERATE non-finite (defensive `is_finite(ray.origin) && is_finite(ray.direction)` at raycast entry); (7) `leaf_object_threshold = 8` is split TRIGGER not hard cap (max-depth cells retain all assigned objects); (8) lazy root allocation. Two-layer typing per ADR-0078 §5 D34: `octree_queries_typed.hpp` strip-compute-retag wrappers (`AABB3<Length32>`, `OctreeRay3T<D, T>`, `OctreeRayHitT<D, T>`). 17 cases / **fast-path 100% test** (the elite-quality discriminator — sized-by-construction object_half=0.1 + max_depth=8 ⇒ fast-path window 0.68 > cell radius 0.39 ⇒ any cell-resident motion hits fast path) + slow-path triggers when needed + brute-force overlap cross-val × 20 query boxes + raycast nearest + equal-`t` lowest-payload tiebreak + loosening 1.5/2.0/3.0 result-count invariance + permuted-insert determinism + insert/remove cycle stability + NaN tolerance. 5-config DoD PASS: win-debug 1996/1996 + win-asan + win-shipping + win-shipping-profile 1991/1991 + win-tidy clean. Full project ctest 1979 → 1996 win-debug. Session log `docs/sessions/2026-05-16-geometry-v5b-loose-octree.md`. Deferred (not blockers): f64 raycast precompute (f32-only today), `compact()` empty-subtree collapse, f64 raycast t-near ordering.

**Geometry v5c `RTree` shipped 2026-05-16** — full Beckmann 1990 R*-tree (NOT quadratic-split simplification). New `rtree.hpp` + `rtree.cpp` in `engine/geometry-spatial/`. M=16 + m=5; two-field `RTreeEntry { aabb, payload, handle }` (32 B per entry); stable handle via indirection table `m_locations[handle] → {node, entry}` updated on every split/reinsert/condense. **Full R*-tree algorithm**: choose-subtree (overlap-enlargement at leaf-parent + area-enlargement higher) + split (min-perimeter axis × min-overlap distribution, all sorts via `crd::containers::sort` with lex-tuple comparators) + forced reinsertion (4 farthest-from-center entries reinserted in distance-ASCENDING order, one-shot per level per insert via `m_treated_levels` bitmask) + Guttman condense-tree (orphan reinsertion at original level + root-single-child collapse). **STR bulk-load first-class** (Leutenegger 1997) — `bulk_load(aabbs, payloads, out_handles)` packs L=⌈N/M⌉ leaves into S=⌈√L⌉ vertical slabs (sorted by x-midpoint), each slab into M-leaves (sorted by y-midpoint), recursive pack above; ~7× faster than sequential insert for static cooked-level data. **k-NN — Hjaltason-Samet 1999 incremental** with `crd::containers::push_heap`/`pop_heap` priority queue ordered by min-distance-from-query-to-AABB. **Lowest-payload tiebreak on equal raycast `t` + equal k-NN distance** (§4 pin #11). Builder REJECTS non-finite (debug `CRD_ASSERT`); queries TOLERATE non-finite (defensive `is_finite` short-circuit at API surface for overlap + raycast + k-NN). `validate()` whole-tree structural check. Two-layer typing per ADR-0078 §5 D34: `rtree_queries_typed.hpp` strip-compute-retag wrappers (`AABB3<Length32>`, `RTreeRay3T<D, T>`, `RTreeRayHitT<D, T>`, `RTreeNeighborT<D, T>`). 17 cases — first-overflow-fires-reinsert / second-overflow-fires-split / brute-force overlap × 20 / raycast nearest / equal-`t` tiebreak / brute-force k-NN k∈{1,5,20} / insert/remove cycle handle stability / STR brute-force xval × 10 / STR depth ≤ sequential / permutation determinism / NaN tolerance / sizing pin. 5-config DoD PASS: win-debug 2012/2012 + win-asan + win-shipping + win-shipping-profile 2007/2007 + win-tidy clean. Full project ctest 1996 → 2012 win-debug. Session log `docs/sessions/2026-05-16-geometry-v5c-rtree.md`.

**Geometry v5d `SpatialHash` shipped 2026-05-16** — Teschner 2003 fixed-grid spatial hash + elite-tier widening per user mandate (phase doc spec was conservative). Hash: `H(ix,iy,iz) = (ix·73856093) ^ (iy·19349663) ^ (iz·83492791)` mod POW2 bucket count (bit-mask). AABB stored to ALL overlapping cells. Free-list-managed object pool + stable `SpatialHashObjectId`. Per-bucket `Array<u32 obj_idx>` lazily allocated. **Elite additions**: (1) **per-query generation-counter dedup** — `mutable u64 m_query_generation` bumped once per query, `obj.last_query_gen` compared on visit, **zero allocation per query** (no seen-set); (2) **Amanatides-Woo 1987 voxel raycast** — per-axis step+tDelta+tMax + advance-smallest-tMax + best_t pruning + 2^20 step cap; lowest-payload tiebreak on equal `t` (§4 pin #11); (3) **`find_overlapping_pairs(out)`** broadphase pair query (eylem v3 XPBD soft-body target) — visit each bucket, emit `(min,max)` AABB-overlapping pairs, cross-cell duplicates removed via sort + unique; (4) **update fast-path** — same-cell-range detection no rebucketing (returns false; ~95%+ small motions). Cells visited canonical (z,y,x). Builder REJECTS non-finite + inverted; queries TOLERATE non-finite + zero-direction (defensive `is_finite` short-circuit). Two-layer typing per ADR-0078 §5 D34: `hash_queries_typed.hpp` (`spatial_hash_insert/overlap/radius/raycast` typed wrappers). 21 cases — brute-force overlap × 20 boxes + brute-force radius × 5×3 + multi-cell findable + Amanatides-Woo nearest + diagonal + negative-direction + tiebreak + miss-nullopt + corner-grazing + update fast/slow paths + insert/remove cycle + `find_overlapping_pairs` brute-force xval + negative coords + NaN tolerance + dedup-on-8000-cell-object. **v5d-fast caller-scratch overload shipped same session** (user "elite, no deferring" mandate): `SpatialHashScratch` POD + scratch-taking overloads of overlap/radius/raycast (find_overlapping_pairs already const-safe); convenience and scratch overloads share `*_traverse_` template helpers parameterised over `WasVisited`/`MarkVisited` policy lambdas (provably equivalent dedup + emission). **Concurrent-query test via `crd::jobs::parallel_for`** (`SpatialHashJobsListener` Catch2 listener inits 4-worker fiber pool, same pattern as `test_bvh_parallel.cpp`'s `BvhJobsListener`): 16 distinct queries × 25 iterations = 400 fan-out tasks across 16 jobs / 4 worker fibers, per-task fresh `TlsfAllocator` + scratch, `std::atomic<u32> mismatches == 0` under win-asan race detection. +6 scratch tests + 1 fiber-jobified concurrent test. Total v5d 28 cases. 5-config DoD PASS: win-debug 2040/2040 + win-asan + win-shipping + win-shipping-profile 2035/2035 + win-tidy clean. Full project ctest 2012 → 2040. Session log `docs/sessions/2026-05-16-geometry-v5d-spatial-hash.md`.

**Geometry v5e `UniformGrid` shipped 2026-05-16** — dense bounded-domain 3D cell array (distinct from v5d hash: no collision, O(cell_count) memory). New `uniform_grid.hpp` + `uniform_grid.cpp` in `engine/geometry-spatial/`. Construction: `(nx,ny,nz) = ceil((bounds_max-bounds_min)/cell_size)`; flat `cell_idx = (z·ny+y)·nx+x`. Out-of-bounds AABBs CLAMP to grid. Per-cell `Array<u32>` lazily allocated. 256M cell sanity cap. Update fast-path = same-cell-range detection. **Per-query generation-counter dedup + `UniformGridScratch` POD + scratch overloads SHIPPED FROM DAY 1** per "elite, no deferring" mandate; convenience and scratch overloads share `*_traverse_` template helpers parameterised over `WasVisited`/`MarkVisited` policy lambdas. **Raycast = grid-bounds-clipped Amanatides-Woo voxel traversal**: slab-test ray vs grid AABB → entry-clip; clamp `t_entry ≥ 0`; compute start cell (clamped boundary); per-axis step+tDelta+tMax; walk ALL-tied-axes-advance (corner-grazing safe) until cell leaves grid OR best_t pruning OR 2²²-cell step cap; lowest-payload tiebreak on equal `t` (§4 pin #11). `find_overlapping_pairs` dense linear cell scan + sort+unique cross-cell dedup. Builder REJECTS non-finite + inverted; queries TOLERATE non-finite + zero-direction. Two-layer typing per ADR-0078 §5 D34: `grid_queries_typed.hpp`. **Concurrent test via `crd::jobs::parallel_for`** (shared SpatialHashJobsListener from v5d): 16 queries × 25 iterations = 400 fan-out tasks / 16 jobs / 4 worker fibers, per-task fresh `TlsfAllocator` + `UniformGridScratch`, `std::atomic mismatches == 0` under win-asan race detection. 26 cases — empty + cell-counts via ceil + out-of-bounds clamping + brute-force overlap × 20 + brute-force radius × 5×3 + 7 Amanatides-Woo raycast cases (nearest + tiebreak + negative + corner-grazing + miss-no-objects + starts-outside-grid + slab-rejects) + update fast/slow + insert/remove cycle + `find_overlapping_pairs` brute-force + NaN tolerance + dedup-many-cell + scratch parity + concurrent-via-jobs. One-pass sizing fix: first DoD found 16 OOM failures on 4 MB fixture vs 100³-cell default grid; bumped fixture to 64 MB + shrunk default_cfg to 20³ + bumped corpus alloc to 32 MB. 5-config DoD PASS: win-debug 2066/2066 + win-asan + win-shipping + win-shipping-profile 2061/2061 + win-tidy clean. Full project ctest 2040 → 2066. Session log `docs/sessions/2026-05-16-geometry-v5e-uniform-grid.md`.

**Geometry v5 thread-safety validation pass shipped 2026-05-16** — cross-cutting tightening after v5e close. Locks the design principle that emerged across v5d/v5e and applies it retroactively. **Principle: scratch overloads exist iff per-query dedup state requires them; fiber-jobified concurrent test mandatory for ALL backends regardless of which flavour.** Two-camp split based on algorithmic property (one-object-one-location vs multi-location-storage): KdTree/LooseOctree/R*-tree are naturally const-safe by construction (no scratch needed — adding one would be misleading-API cargo-culting); SpatialHash/UniformGrid require scratch (multi-cell membership forces dedup state mutation). Added 3 fiber-jobified concurrent-read tests (KdTree k-NN + LooseOctree overlap + R*-tree overlap) — each runs 400 fan-out tasks via `crd::jobs::parallel_for` against the SHARED tree, per-task isolated TlsfAllocator + output Array, atomic `mismatches == 0` under win-asan race detection. Renamed `SpatialHashJobsListener` → `GeometrySpatialJobsListener` (binary-wide). Memory `feedback_spatial_substrate_thread_safety.md` saved as reusable rule for any future spatial substrate (eylem broadphase, renderer cull, scene index queries). 5-config DoD PASS: win-debug 2069/2069 + win-asan + win-shipping + win-shipping-profile 2064/2064 + win-tidy clean. Total cross-backend race coverage: 5 × 400 = 2000 fan-out tasks under ASan. Session log `docs/sessions/2026-05-16-geometry-v5-thread-safety-validation.md`.

**Geometry v5-index-bringup shipped 2026-05-16** — `SpatialBVHIndex` promoted from Phase 3.0 v1i no-op shell to real LooseOctree-backed component index in `crd-scene`. **First non-reserved spatial index** — closes the ADR-0053 reserved-shell loop for SpatialBVH. New `engine/scene/spatial_bvh_index.{hpp,cpp}` + `IAabbExtractor` interface. **ADR-0053 day-one promise PRESERVED**: two-state design (unconfigured = identical no-op behaviour to the shell; configured via `world.find_index<SpatialBVHIndex>()->configure(extractor, opts)` = real work). Existing Phase-3.0 user code with `register_component<X>(SpatialBVH{})` continues running with zero changes. **Canonical IAabbExtractor pattern** = `extract(EntityId, ComponentId, const void* data)` (NOT just `(EntityId)` — avoids storage-migration-mid-commit null hazard; data IS the freshly-installed bytes per IStorageEventSink contract). Storage hooks: on_insert/on_update via extractor + LooseOctree fast-path absorption; on_remove conservative; on_entity_destroyed definitive cleanup. **Update via UPSERT** (`world.add_component`) is the only way to refresh bounds — `get_component_mut` only bumps chunk-version, doesn't fire on_update. Storage handle = u32 entity.index() in octree payload + side `HashMap<u32, EntityId>` for O(1) query-result reconstruction. Queries naturally const-safe (LooseOctree + read-only HashMap iteration; no mutable state). 8 cases including concurrent-via-`crd::jobs::parallel_for` (400 fan-out tasks ASan-clean). `crd-scene` gains PUBLIC link `crd-geometry-spatial`. Three debugging passes en route (HashMap iterator API + World API + extractor-mid-migration + UPSERT-not-mut). Pattern locked as template for future spatial extensions (LightInfluenceIndex Phase 3.5, OcclusionIndex 3.5+, AudioOcclusionIndex 3.4). 5-config DoD PASS: win-debug 2077/2077 + win-asan + win-shipping + win-shipping-profile 2072/2072 + win-tidy clean. Full project ctest 2069 → 2077. Session log `docs/sessions/2026-05-16-geometry-v5-index-bringup.md`.

**Geometry v5-queries-extension shipped 2026-05-16** — extends the unified `crd::geometry::{raycast/overlap/radius/nearest_n/find_overlapping_pairs}` facade to dispatch over the 5 v5 backends. New header-only `engine/geometry-spatial/include/crd/geometry/spatial/queries.hpp` (~250 LOC) sits ALONGSIDE `crd-geometry-bvh`'s `crd/geometry/queries.hpp`. Both facades in `crd::geometry` namespace; ADL unifies them. Compile-time overload polymorphism per ADR-0076 §16 pin #1 + v1i-a precedent — no virtual, zero overhead. Support matrix (don't fake what doesn't fit): KdTree k-NN+radius / LooseOctree overlap+raycast / RTree overlap+raycast+nearest_n / SpatialHash overlap+radius+raycast+find_overlapping_pairs / UniformGrid same / + BVH backends from the BVH facade. Convenience overloads only — scratch is backend-specific knob that doesn't generalise. 16 parity tests + cross-backend uniformity demo (same `crd::geometry::overlap` name across 4 AABB backends returns same set on same content). 5-config DoD PASS: win-debug 2093/2093 + win-asan + win-shipping + win-shipping-profile 2088/2088 + win-tidy clean. Full project ctest 2077 → 2093 (+16). Session log `docs/sessions/2026-05-16-geometry-v5-queries-extension.md`.

**🎉 Phase 3.1.7 v5 `-spatial` cluster CLOSED 2026-05-16** — v5-close shipped: ADR-0076 §20 amendment (20 locked substrate decisions + slice ledger + cluster cross-validation + sub-module table); `docs/systems/geometry-spatial.md` system overview (when-to-use matrix, architecture diagram, API stencils, support matrix, thread-safety contract, determinism pins, performance pins, scene-integration template); ROADMAP / context / MEMORY synced; 18-config full sweep PASS. **Cluster totals: 8 slices · ~4900 LOC engine + ~4330 LOC tests · 20 locked substrate decisions · 5 spatial backends · cross-backend ASan race coverage 2000 fan-out tasks · facade dispatches over 8 backend types (5 spatial + 3 BVH).** Phase 3.1.7 6th of 11 sub-modules COMPLETE — `-primitives` + `-bvh` + `-convex` + v3 convex-hull extension + `-mesh` + **`-spatial`** all closed. 45 of 49 renewed-scope slices shipped (~92%). Session log `docs/sessions/2026-05-16-geometry-v5-close.md`.

**🎉 Phase 3.1.7 v6 `-polygon` cluster CLOSED 2026-05-16** — all 6 slices shipped (v6a substrate + v6b ear-clip-with-holes + v6c CDT + v6d Vatti polygon Boolean + v6e Bentley-Ottmann + v6-close). New `engine/geometry-polygon/` module (7th `crd-geometry-*` sub-module). **Cluster totals: 6 slices · ~4350 LOC engine + ~2750 LOC tests · 92 cases / 695 assertions · 20 locked substrate decisions in ADR-0076 §21 · `docs/systems/geometry-polygon.md` · 18-config full sweep PASS.** Phase 3.1.7 7th of 11 sub-modules COMPLETE — primitives + bvh + convex + v3 convex-hull-extension + mesh + spatial + **polygon** all closed. 51 of 49 renewed-scope slices shipped (104% — v6 expanded 4→6 slices per the v6a substrate separation; nothing cut).
- **v6a substrate ✅** — `Polygon2<T>` / `PolygonView2<T>` / `Ring2<T>` non-owning + owning types (CCW outer + CW holes winding convention, prefix-sum ring offsets, layout-pinned via static_assert) + `signed_area` / `centroid` / `aabb` / `is_ccw` / `is_simple` (O(n²) brute-force for now; O(n log n) Bentley-Ottmann waits for v6e) / `ensure_orientation` / `point_in_ring` / `point_in_polygon` (Hormann-Agathos 2001 crossing-number + Shewchuk orient2d adverse) + typed `Vec2<Length32>` strip-compute-retag wrappers (ADR-0078 §5 D34) + `polygon_substrate.cpp` static_assert pins the Vec2<Quantity<D,T>> bit-identical-to-Vec2<T> contract. ~700 LOC engine + ~600 LOC tests; 39 cases / 151 assertions. 4-config DoD PASS.
- **v6b ear-clipping with holes ✅** — `triangulate_ear_clip(Ring2)` / `triangulate_ear_clip(PolygonView2)` with classical Meisters 1975 O(n²) ear-clip + Eberly 1999 cut-and-join hole bridging (rightmost-vertex pick, lex-tuple tiebreak, +X-ray closest-edge visibility, doubled-bridge insertion). Lex-tuple deterministic ear pick. Shewchuk orient2d for every reflex / ear-validity decision. Returns triangle index list (3 per CCW triangle, indices into input polygon's flat `vertices()` array). 14 cases / 50 assertions including unit square / triangle / L-shape / 3-toothed comb / 5-point star / 16-gon convex / square-with-hole / square-with-2-holes / f64-large-coord / rotation-determinism. ~900 LOC engine + ~700 LOC tests. 4-config DoD PASS.
- **v6c CDT ✅** — `constrained_delaunay(points, constraints)` + `constrained_delaunay(PolygonView2)` (polygon-with-holes convenience). Bowyer-Watson incremental Delaunay (Shewchuk `incircle` adaptive; lex-sorted insertion order for determinism; super-triangle setup + final-strip-removal; point location via jump-walk from `hint_tri`). Constraint recovery: fast-path (`find_edge` mark-both-sides) + Domiter-Zalik 2008-style carve-and-retriangulate for arbitrary multi-flip cases including non-convex-quad chains (trace chain via apex-side orient2d, partition into upper/lower sub-polygons split by constraint, free chain triangles + final triangle, ear-clip each sub-polygon, re-link outer neighbours via canonical-edge match). Lawson 1977 post-flip Delaunay restoration over non-constrained edges. Polygon variant: even-odd centroid in/out filter strips triangles in holes. 8-state diagnostic enum (TooFewPoints, NonFiniteInput, DuplicatePoint, ConstraintOutOfBounds, ConstraintsCrossing, InternalInvariant). **Mid-implementation bug-fix lesson**: original flip's convexity check used wrong orient2d args (`orient(pc, pd, pa) > 0`, `orient(pc, pb, pd) > 0`), which silently rejected valid flips; correct test is `orient(pa, pd, pc) > 0` AND `orient(pd, pb, pc) > 0` for new triangles T'=(va,vd,vc) + U'=(vd,vb,vc) to be CCW. 14 cases / 410 assertions including basic Delaunay (3-pt / 4-pt / 32-pt random + every-tri-CCW invariant + empty-circumcircle invariant) + constraint-already-edge fast-path + interior-cut single-flip + long-constraint-crossing-multiple-edges multi-flip carve-retriangulate + polygon-with-hole in/out filter + 4 diagnostic-status assertions + f64 large-coord + insertion-order determinism. ~1400 LOC engine + ~700 LOC tests. 4-config DoD PASS.

**v6d Vatti polygon Boolean shipped 2026-05-16** — 4 ops (union/intersection/difference/xor) via planar-subdivision + winding-number face classification + DCEL half-edges with CCW-sorted outgoing-edge orient2d angle comparator + brute-force O(n×m) intersection finder (v6e Bentley-Ottmann is the natural drop-in upgrade for >10k vertices) + per-loop face emission (hole rings emitted naturally per CW orientation per v6 winding convention) + EvenOdd + NonZero fill rules + clean_output (collinear vertex removal + dedup gated by opts). Convenience overloads: polygon_union / polygon_intersect / polygon_difference / polygon_xor. **Mid-implementation lesson**: face-walking originally treated each LOOP as a distinct "face" (skipping CW loops as outer-face boundaries), which broke polygon-with-hole output — fixed by emitting every kept LOOP as an independent ring (CCW loops become outer rings, CW loops become hole rings naturally per v6 winding convention). 13 cases / 47 assertions; 4-config DoD PASS.

**v6e Bentley-Ottmann shipped 2026-05-16** — full O((n + k) log n) sweep-line: event queue (min-heap, lex-sorted by y/x/kind/seg-id; uses `crd::containers::push_heap` / `pop_heap` for bit-exact ordering) + status structure (sorted Array of active segments by X-at-sweep-Y) + 3 event kinds (Start < Intersection < End at coincident y/x — canonical 1979 ordering) + horizontal-segment brute-force secondary pass (clean separation from X-sorted status) + Shewchuk orient2d throughout (transverse + endpoint-on-interior + T-junction + vertex-on-vertex detection) + dedup on canonical pair-key (avoids re-reporting after status swaps) + output sorted by lex (point.y, point.x, seg_a, seg_b) + short-circuit `bentley_ottmann_any` for is_simple-style usage + 3-state diagnostic enum (Ok, NonFiniteInput, DegenerateSegment). 13 cases / 37 assertions; 4-config DoD PASS.

**🎉 v6-close shipped 2026-05-16** — ADR-0076 §21 amendment (20 locked substrate decisions + slice ledger + cluster cross-validation + complexity table); `docs/systems/geometry-polygon.md` (when-to-use matrix, architecture diagram, API stencils, determinism + robustness contracts, performance pins, two-layer typed architecture, integration touch-points across crd-font / crd-eda / crd-eylem navmesh / crd-resources cooker / crd-renderer decals / editor sketch tool, comprehensive references); 18-config full sweep PASS.

**🎯 NEXT — Phase 3.1.7 v7 `-mesh-processing` cluster** (Garland-Heckbert QEM + Loop subdivision + isotropic remesh + Liepa hole-fill + manifoldness repair + self-intersection removal + Taubin smoothing). 7 algorithmic slices per the renewed-scope plan. Consumes v6e Bentley-Ottmann for self-intersection scan.

**Two-layer typing per ADR-0078 §5** carries forward from v4/v5 — algorithm bodies stay `<MathScalar T>` raw f32; public surface ships `Aabb<Length32>` / `Vec3<Length32>` / `Length32` strip-compute-retag wrappers. **Per-slice DoD = 5 configs**; cluster close = full 18-config sweep.

---

**Geometry v4d `mesh_raycast_simd` shipped 2026-05-16** — AVX2 8-wide Möller-Trumbore batched ray-triangle test at BVH leaves. Same BVH traversal as v4b (Williams/Ize slab + nearer-first descent + best_t pruning); differs only at the inner loop. Per leaf: chunk triangles into groups of 8, gather → SoA-transpose into 9 × Vec8f registers, run 8-way MT (edge1/edge2/pvec/det/inv_det/tvec/u/qvec/v/t — ~30 FMAs), then scalar lane scan for masking decisions (cull_back / |det|>ε / 0≤u≤1 / 0≤v / u+v≤1 / 0≤t≤best_t). **Mid-implementation lesson**: first draft used SIMD-mask AND via `min(mask_gt, mask_lt)` — combining `_CMP_*` result registers (all-bits NaN on true) via `_mm256_min_ps` is implementation-defined for NaN inputs, silently lost the cull bit. Switched to scalar lane scan after SIMD ALU — 8 cheap branches vs the SIMD ALU work. MT vs Woop precision: v4d picks different triangles on near-edge rays (strict-sign det vs watertight). Cross-validation against v4b on a 36-ray sphere corpus accepts up to 6 edge-case divergences. 7 cases / 15 assertions; 5-config DoD green (win-debug 1942/1942, win-asan 1942/1942, win-shipping 1855/1855, win-shipping-profile 1937/1937, win-tidy clean); full project ctest 1935 → 1942. Session log `docs/sessions/2026-05-16-geometry-v4d-mesh-raycast-simd.md`. Next = v4-validate (formal mesh validation pipeline stage).

**ADR-0078 §5 amendment formalised 2026-05-16: TWO-LAYER TYPED ARCHITECTURE.** Codifies the implicit pattern that emerged across §3 D22 + §3 D23 + §4 D27/D28/D29. One-line rule: **units at the API surface, raw scalars in the inner loop.** Upper layer (typed `Quantity<D, T>` everywhere): public APIs, ECS components, configs, cookers, UI display. Lower layer (raw `f32`/`f64`): SIMD kernels, math primitive inner ops, geometry algorithm bodies, numerical kernels, GPU writes, byte buffers. Bridges (`.value` / `to_raw_vec` / `from_raw_vec` / strip-compute-retag wrappers) live ONLY at the API surface; each crossing is one line with a one-line comment naming the ADR clause. ADR-0078 §5 D32-D36; full reference table of where each module sits today in `docs/systems/units.md` § Two-layer typed architecture. PRINCIPLES.md cornerstone updated. CLAUDE.md decision #9 added. Memory `feedback_always_units.md` updated with the two-layer rule.

---

**🎯 NEXT — Resume Phase 3.1.7 v2 `-convex` (GJK + EPA + SAT + Quickhull + ConvexHullView queries + GJK-cast).** Per the Strategic Execution Plan locked 2026-05-15 (`feedback_strategic_execution_plan_2026_05_15.md`): geometry phase finishes in FULL (no consumer-driven cutting) before eylem v1c resumes. v2 is the v1-close milestone after the 7-slice `-bvh` sub-module (v1a-v1g all shipped 2026-05-13 + the v1h/v1i hardening). ~2 weeks. Then `crd-hesap-dense` v0 BEFORE eylem v1c resume per the engineering-platform pivot. In parallel: detour D-004 deterministic-replay sandbox (uses D-006 `DeterministicClock`); D-005 config/resource hot-reload polish.

Phase 3.1.7.5 deferred (~~retained~~):
- `crd-renderer` uniform-upload boundary (typed `.value` egress at GPU push-constant / SSBO write sites).
- `crd-resources` cookers (texture / material / scene / mesh resource fields typed where geometric).
- `crd-imgui` user-preferred-unit display (Layer-6 `UnitPreferences` + per-discipline preset table: game / CAD / robotics / aerospace / PCB / audio / 3D-print / CAM / cinematic / imperial / SI-strict / scientific).
- Layer-6 full format / parse / `UnitPreferences` 11-discipline-preset table.
- **`DimRoot<>` fractional-exponent dimensions** design + impl (unblocks v0c-4 follow-on + `length(Vec3<Length>)` reductions on `Vec<Quantity>`).
- v0c-4 follow-on after DimRoot lands: `crd-geometry-primitives` API surface re-tag (Sphere::radius / Box::half_extents / closest_point returns / raycast t-parameter).
- 17-config sweep close.

Then **v0d adoption C** (`crd-renderer` uniform-upload boundary + `crd-resources` cookers + `crd-imgui` user-preferred-unit display + Layer-6 full format/parse/UnitPreferences + cross-engine readers + full 17-config sweep close, ~700 LOC). In parallel: D-004 deterministic-replay sandbox; D-005 config/resource hot-reload polish.

Every v0b/c/d slice runs the new **5-config per-slice-check** (win-debug + win-asan + win-shipping + win-shipping-profile + win-tidy) per `feedback_per_slice_run_ctest.md`. The just-shipped profiler is ready to instrument the dimensional-type cost from day 1.

---

**D-003 detour state (closed 2026-05-15):**
- **v0a ✅ SHIPPED 2026-05-15** — substrate skeleton: new `engine/perf/` module; 32-byte `Sample` POD + per-thread SPSC ring + content-keyed FNV-1a interned names + `ScopedRegion` RAII + `CRD_PERF_SCOPE` macros + thread registration + fiber-migration wire format via `BeginToken` + frame_mark. ADR-0063 determinism-contract substrate-level pin proven. Session log `docs/sessions/2026-05-15-d003-v0a-perf-substrate.md`. **Module renamed `crd-profiler` → `crd-perf`** to avoid collision with existing `crd-profile` (quality presets); macros `CRD_PERF_SCOPE` / `CRD_PERF_FRAME_MARK` / etc.; namespace `crd::perf`.
- **v0b ✅ SHIPPED 2026-05-15** — typed counters substrate + per-frame snapshot ring. `counters.hpp` + `frame_record.hpp` (~310 LOC engine + ~510 LOC tests); `CounterKind` (Set/Add) × `CounterType` (I64/F64/DurationNs) = 6 macros; `FrameRecord` 2080 B POD pinned by static_assert (on-disk CPROF depends on it); 240-slot rolling history ring; multi-thread atomic with CAS-loop on f64 Add. Session log `docs/sessions/2026-05-15-d003-v0b-perf-counters.md`.
- **v0c ✅ SHIPPED 2026-05-15** — crd-jobs `JobObserver` hook + crd-perf jobs adapter + new `win-shipping-profile` preset. Every job in the engine becomes a labeled `Category::Job` Sample with zero call-site code once `install_jobs_adapter()` runs after `perf::init()`. Session log `docs/sessions/2026-05-15-d003-v0c-jobs-adapter.md`.
- **v0d ✅ SHIPPED 2026-05-15** — GPU timestamp backend. `IProfilerGpuBackend` in crd-perf + `VulkanProfilerBackend` in crd-rhi-vulkan + `CRD_PERF_GPU_SCOPE` macro + dedicated "gpu" track via new `emit_gpu_sample` external-write path. Session log `docs/sessions/2026-05-15-d003-v0d-gpu-backend.md`.
- **v0e ✅ SHIPPED 2026-05-15** — memory tracking. `MemoryStats` gate widened to cover `CRD_ENABLE_PROFILING` + allocator registry + `AllocatorRecord[32]` in FrameRecord. Session log `docs/sessions/2026-05-15-d003-v0e-memory-tracking.md`.
- **v0f ✅ SHIPPED 2026-05-15** — CPROF capture file format. Pinned POD layout (`CprofHeader` 72 B + `ThreadHeader` 56 B + `CounterMeta` 64 B + `AllocatorMeta` 64 B + NameBlob preserving `Sample.name_id` across save/load + `FrameRecord` 3616 B + `Sample` 32 B). `save_capture_to_buffer/file` + `CaptureView` read-only view mirroring live profiler API 1:1. One-way save policy. Session log `docs/sessions/2026-05-15-d003-v0f-cprof-capture.md`.
- **v0g ✅ SHIPPED 2026-05-15** — ImGui frontend. New module `engine/perf-ui/` (~1100 LOC engine + 14 cases / 56 assertions). `IProfilerSource` polymorphic seam + `LiveProfilerSource`/`CaptureViewSource` + `ProfilerPanel` with 7 sub-panels (Frame Summary / Timeline with red borders on fiber-migrated samples / Flame Graph / Counters / GPU Passes / Memory / Capture Controls). Session log `docs/sessions/2026-05-15-d003-v0g-imgui-frontend.md`.
- **v0h ✅ SHIPPED 2026-05-15** — D-003 close. (1) **Sandbox wiring** in `sandbox/src/main.cpp` — profiler bring-up (init + register `default_allocator()` + Vulkan GPU backend + install jobs adapter + push `ProfilerPanelLayer`) before `jobs::init`; per-frame `resolve_gpu_frames()` + `frame_mark()`; teardown is the strict mirror. Smoke: `crd-sandbox --smoke-test 3` ran **529 frames @ 176 fps** with all 7 panels populating live. (2) **ADR-0079** captures 15 locked design decisions across v0a-v0g + slice ledger. ADR index updated. (3) **`docs/systems/perf.md`** system overview (quick-start, module graph, API surface, on-disk format struct table, panel→data mapping, sizing knobs). Plus `scripts/full-sweep.ps1` extended to include `win-shipping-profile` (Win sweep 10 → 11 configs). First sweep surfaced a **pre-existing `crd-units` clang-cl bug** (deprecated `operator"" _name` UDL syntax in `literals.hpp` — 53 macro instances; one-line whitespace fix) plus four pre-existing v0g code-quality items flagged by stricter clang-cl warnings (unused `align_up_8`, ctor init-order, three unused private fields). All fixed inline. Session log `docs/sessions/2026-05-15-d003-v0h-close.md`.

---

**🎉 D-003 detour fully closed.** Slice-by-slice detail captured in the 8 session logs above; ADR-0079 is the authoritative architecture record; `docs/systems/perf.md` is the system overview.

**v0a-locked design pins (kept for posterity):**
1. Sample = exactly 32 bytes; on-disk CPROF (v0f) memcpys arrays verbatim.
2. Wire format = paired Sample + `(begin_thread, end_thread)`. UI default = per-OS-thread tracks; per-fiber Tracy-style view reconstructed via JobObserver fiber-yield events in v0c.
3. Gate = existing project-wide `CRD_ENABLE_PROFILING` (no new build switch).
4. **Per-slice DoD = 5-config per-slice-check** (win-debug + win-asan + win-shipping + **win-shipping-profile** + win-tidy). 5th config added 2026-05-15 (D-003 v0b post-discussion): mirrors win-shipping under LTCG + `/O2` + `/OPT:ICF` but with `CRD_ENABLE_PROFILING=ON`, so the entire substrate + every gated `CRD_PERF_*` site compiles + runs under max optimization. Catches LTCG-class bugs in profiler hot paths (atomic stores, CAS-loops, thread-local indirection). `win-shipping` (profiling OFF) still runs to verify the consumer-ship zero-overhead contract. v0a+v0b retroactively validated 2026-05-15 (1795/1795 full project ctest under win-shipping-profile). Full 17-config sweep at v0h close per `feedback_full_sweep_required.md`.

See `docs/phases/phase-3.1.7.5-units.md` for the v0b/c/d adoption plan.

---

**Strategic Execution Plan LOCKED 2026-05-15** (canonical reference: `docs/ROADMAP.md` § Strategic Execution Plan). User-approved step-back review yielded: **Pathway A (units-first) + Pathway E (engineering-platform leader) confirmed**; geometry phase ships in FULL (no consumer-driven cutting per Pathway B); C++ scripting + DLL hot-reload **stays deferred to Phase 4.0** (not pulled forward); four cross-cut detours (D-006 `crd-time` ✅ + D-003 `crd-perf` ✅ both shipped 2026-05-15 / D-004 replay sandbox queued / D-005 config/resource hot-reload polish queued) run in parallel with units adoption; eylem cold-storage mitigated via per-sub-module integration smoke against eylem v1c+ stubs; hesap-dense v0 ships BEFORE eylem v1c resume (engineering-platform pivot's first concrete artifact). Calendar target: ~3.5 months to first integrated physics demo with units + profiler + replay; ~9 months to engineering-platform pivot fully started.

**Multi-domain expansion vision captured 2026-05-14 (ADR-0077).** Strategic review of the long-term roadmap against the stated 8-domain mandate (gaming + simulation + manufacturing + CAD + CFD + math + aerospace + mechanics) surfaced 9 missing peer-module substrates + a Phase 3.5 prologue + a Phase 6 platform-expansion phase. All captured as aspirational stubs in `docs/phases/`:
- **Phase 3.1.8 `crd-brep`** — NURBS / B-rep core (the manufacturing/CAD substrate; STEP / IGES / Parasolid import; exact booleans; fillet / chamfer / sweep / loft).
- **Phase 3.1.9 `crd-cad-feature`** — parametric feature trees + 2D sketching with constraints + ISO/ASME drafting + GD&T.
- **Phase 3.1.10 `crd-cfd`** — unstructured grid + FVM/FEM + Navier-Stokes + k-ε/k-ω SST/LES turbulence + multiphase (VOF + level-set) + AMR + combustion.
- **Phase 3.1.11 `crd-estimation` + `crd-control`** — Kalman/EKF/UKF/SLAM + PID/LQR/MPC + path planning (RRT / RRT* / PRM / A*).
- **Phase 3.1.12 `crd-fea`** — engineering FEA (static + modal + buckling + fatigue; distinct from eylem v7 dynamic FEM).
- **Phase 3.1.13 `crd-cam`** — manufacturing toolpaths (3-/5-axis milling + turning + additive slicing + G-code post-processors + sheet metal + PCB).
- **Phase 3.1.14 `crd-ml-inference`** — ONNX runtime + GPU inference + differentiable bridge for denoising / SR / content gen / in-game AI / animation ML / audio ML.
- **Phase 3.1.15 `crd-procgen`** — noise (extended) + Wave Function Collapse + L-systems + terrain (erosion sim) + city/building + Substance-style procedural materials.
- **Phase 3.1.16 `crd-sciviz`** — isosurfaces + streamlines + slice planes + LIC + perceptual color maps + in-engine plots + annotation/measurement (CFD/FEA validation tool).
- **Phase 3.5 prologue (amendment)** — mesh shaders + visibility buffer + GPU-driven culling + work graphs + VRS, before the existing PBR/IBL/CSM/SSS/NPR/area-lights scope.
- **Phase 6 platform expansion** (reuses the folded-Native-physics slot) — console (PS5/Xbox/Switch) + mobile (iOS/Android) + web (WebGPU/WASM) + VR/AR (OpenXR) + HPC (MPI distributed simulation).
- **Phase 3.2 animation** (flesh-out) — skeletal LBS+DQS + state machines + motion matching + FABRIK/full-body IK + facial (FACS/ML) + crowd (RVO).
- **Phase 3.4 audio** (flesh-out) — DSP + spatial (HRTF/ambisonics) + ray-traced acoustics (via `crd-geometry-bvh`) + DAW plugin host + ML audio.
- **Phase 8 domain modules expanded** — robotics (URDF + ROS2) + aerospace (VLM + orbital prop + atmosphere) + cinematic (mocap + camera systems) + medical viz (DICOM + transfer fn + multi-modal registration) + manufacturing (CAD+CAM+CAE) + scientific computing (REPL + `.cnb` notebook).

Each is a stub with scope + dependencies + sub-modules + reading list + open questions + revisit triggers — NOT a commitment to execute. Full phase plans land when consumers surface or research dossiers ship. Total long-term horizon: 5–8 years if everything ships. The substrate-first principle means each substrate close unlocks downstream consumers immediately; the engine ships *useful* increments throughout. See ADR-0077 for the comprehensive rationale + sequencing strategy.

**Phase 3.1.7.5 `crd-units` architectural commitment locked 2026-05-14 — user mandate: "every physical and scientific stuff always having units no matter what".** Compile-time-dimensional units substrate as a peer module (deps: `crd-core` only); zero-overhead `Quantity<D, T>` wrapper + 7 SI base + Angle as 8th tagged dimension + ~50 named derived quantities (Velocity / Acceleration / Force / Pressure / Energy / Power / Torque / AngularVelocity / MomentOfInertia / Frequency / Density / Voltage / Resistance / Capacitance / Inductance / MagneticFlux / LuminousFlux / Illuminance / Luminance / HeatCapacity / ThermalConductivity / …) + ~120 user-defined literals (`25.4_mm`, `9.81_m_per_s2`, `50.0_N_m`, `12000.0_rpm`, `3.3_V`, `215.0_celsius`, …) + `Vec3<Quantity>`/`Mat<Quantity>` wrappers (layout-equal to `Vec3<T>`/`Mat<T>` — SIMD/GPU upload paths reach `.value` raw) + `.value_in<TargetUnit>()` compile-time-validated boundary accessor. **SI everywhere internally; precision tier (f32/f64) orthogonal to dimensional type.** Asset/file/UI boundaries normalize via unit-tagged keys (`length_mm = 25.4`, `mass_kg = 5.0`, `force_N = 100.0`, `voltage_V = 3.3`, `temperature_celsius = 25.0`). PRINCIPLES.md gains a new project-wide rule (added 2026-05-14: "Every physical and scientific quantity carries a unit, always. There is no opt-out — bare-float-for-physical-quantity is a code-review block and a CI-guard violation"). **Slot: between Phase 3.1.7 close and Phase 3.1 eylem v1c resume** (4.5-week window). 4 slices: v0a substrate + full 6-layer conversion system (~1.6 KLOC, 7 days) → v0b adoption A (`crd-config` unit-tagged TOML + `crd-scene Transform` dimensional + glTF cooker SI normalization, ~600 LOC) → v0c adoption B (`crd-eylem RigidBody` + integrator typed-math + 9 force-field formulas re-tagged + `crd-geometry-primitives` AABB/OBB/etc. API-surface re-tag, ~800 LOC) → v0d adoption C (`crd-renderer` uniform-upload boundary + `crd-resources` cookers + `crd-imgui` user-preferred-unit display + Layer-6 format/parse/UnitPreferences + cross-engine file-format unit-tag readers + full 17-config sweep close, ~700 LOC). ADR-0078 minted at v0a close. **6-layer conversion system** locked 2026-05-14: (1) `LinearUnit<Dim, std::ratio>` exact rational factors (bit-exact SI-prefix + standardised-imperial round-trips); (2) `AffineUnit` with distinct `Temperature`/`TemperatureDelta` types (absolute-vs-delta trap closed); (3) `NonLinearUnit` for dB family / cents / semitones / magnitude / pH (arithmetic disabled at type level); (4) `UnitMul`/`UnitDiv` compound auto-derivation via `std::ratio_multiply`/`std::ratio_divide` (one new base unit unlocks N compound units automatically); (5) federated domain registration (`crd-eylem-aero` AU/ly/parsec, `crd-eda` Mil/OhmCm/AmpHour, `crd-cam` RPM/IPM/SFM, `crd-eylem-cine` Frame_24/30/60fps, future `crd-material` centipoise/pascal-second — each in its own namespace with ADL, no central registry); (6) `format_quantity`/`parse_*`/`UnitPreferences` 11-discipline-preset table (game/CAD/robotics/aerospace/PCB/audio/3D-print/CAM/cinematic/imperial/SI-strict). Performance: Layer 1/4 = 1 FP multiply, Layer 2 = multiply+add, Layer 3 = pow/log boundary-only, `Quantity` arithmetic = identical codegen to bare-scalar (objdump-verified). Extensibility: new base unit = 1 line, new domain pack = 1 header, new conversion class = ~50 LOC. **Frame transforms NOT in `crd-units` scope** (live in `crd-math::Transform` + `crd-geometry::transform_aabb`). New `crd-no-untagged-physical-numeric` CI guard blocks bare-`f32 length`/`mass`/`force`/`velocity`/etc. fields in `engine/` (scopes around `crd-math/src/simd/` + `crd-rhi-vulkan/` where raw scalars are intentional). Phase plan: `docs/phases/phase-3.1.7.5-units.md`. ROADMAP updated: new row 3.1.7.5 + long-form section "`crd-units` architectural commitment". Every downstream substrate (sdf, hesap, brep, cad-feature, cfd, estimation+control, fea, cam, ml-inference, procgen, sciviz, eda) consumes typed-math from day 1 by virtue of `crd-units` being upstream in the dependency graph. **Mars Climate Orbiter class of bug becomes a compile error.**

**Phase 3.1.7 renewed-scope review 2026-05-14 — IN-PHASE additions documented in `docs/phases/phase-3.1.7-geometry.md` "Renewed scope coverage" section.** A comprehensive scope review against the multi-domain mandate (gaming + simulation + manufacturing + CAD + CFD + aerospace + robotics + medical viz + DAW + cinematic) was run against the existing 42-slice plan. **Most of the missing-work inventory is already covered** by the v0–v9e structure (mesh core in v4, mesh BVH in v4, polygon ops in v6, mesh processing in v7, Delaunay/Voronoi in v8, GPU LBVH in v9, V-HACD in v9c, robust predicates in v3a ✅, debug viz in v1j ✅, benchmark infrastructure in v1f/v2-close ✅). **Four genuine gaps surfaced**, captured as new in-phase slice clusters: (1) **`v4-validate`** formal mesh-validation slice inside the v4 `-mesh` cluster (duplicate-vertex / degenerate-triangle / non-manifold-edge / inverted-normal / self-intersection / open-boundary / disconnected-component, cooker + editor-import gate, runtime never re-validates); (2) **`v10a–e` new `-curves` sub-module** — Bezier (quadratic + cubic) / Hermite / Catmull-Rom (uniform + centripetal) / B-spline (degree-3, closed) / circular & elliptic arcs + 2D peers + uniform/adaptive/curvature sampling + arc-length system + closest-point (Newton-Raphson) + ray-vs-curve + Frenet + RMF (Wang 2008 double-reflection) frames + viz + sandbox showcase (cinematic camera paths + robot trajectories + debug-draw smoothed polylines as primary consumers); (3) **`v11` transform-aware query helpers** in `crd-geometry-primitives` — `TransformedShape<T>` + `transform_aabb` (8-corner) + `transform_obb` (rigid-exact, affine-conservative) + `transform_capsule3/cylinder3` + `transform_ray3_to_local` (`crd-geometry` stays local-space pure; world-space dispatch belongs to consumer-side adapters in `crd-scene-spatial`/`crd-eylem`); (4) **units / scale / precision policy ADR candidate** (mint at v10 or v11 close: runtime/physics f32 meters, CAD f64 mm, PCB f64 mm or μm, aerospace f64 meters; epsilon is per-query, not global; large-coordinate test corpus mandatory). **PCB/EDA elevated to NEW `crd-eda` future-major phase stub** (Phase 3.1.17 in ROADMAP — board outline + traces + vias + DRC + Gerber export; surfaces ambition, does not commit scope; consumers wait for `crd-cad-feature` 3.1.9 close). 4 ADR candidates noted (units/scale/precision policy + transform-aware adapter pattern + curve mathematics ownership + mesh validation pipeline stage) — minted at slice time, not eagerly this session. **Renewed total: 42 → 49 slices, ~20.0 → ~22 KLOC, ~7–9 → ~7.5–9.5 months calendar.** The renewed-scope sections (priority classification, expanded consumer-mapping table, risk register, ADR candidates, validation+benchmark expectations, architectural rules) are appended to the phase doc — the existing 42-row slice table is **untouched**, and v3d hull simplification remains the immediate next slice per user directive.

**Phase 3.1.7 v3 cluster ✅ CLOSED 2026-05-15 — full 17-config sweep PASS.** All v3 slices shipped (v3a Shewchuk adaptive predicates ✅ + v3b 2D convex hull ✅ + v3c 3D Quickhull a+b+c ✅ + v3d hull simplification ✅ + v3-close ✅). **v3-close** ships `tests/geometry-convex/test_v3_close.cpp` (9 cases / 243 assertions): tiebreak conformance under input permutations for 3D Quickhull + 2D monotone-chain (vertex SET preservation per ADR-0076 §4 pin #11); large-coord stability at 1e6 + 1e7 origin shifts (topology preserved); 3D Quickhull coplanar fallback cross-check against v3b 2D monotone-chain on dominant plane (geometric vertex set equality); v3d threshold-respect against measured ground-truth shrinkage; v3d all-locked + huge-threshold = no-removal; v3d large-coord simplification stays convex + locked preserved; v3d shuffled-input simplification stays valid (weak invariants — greedy trajectory depends on hull input order). New `tests/bench/test_bench_quickhull.cpp` (8 benchmarks: Quickhull build 100/1k/10k, monotone-chain 1k/10k, simplify 200→8 and 500→16). **Full 17-config `scripts/full-sweep.ps1` PASS**: 10 Win (debug + relwithdebinfo + release + asan + clang-cl + debug-scalar + debug-sse2 + shipping + clang-cl-shipping + tidy) + 7 Linux (gcc-debug + relwithdebinfo + release + asan + debug-scalar + debug-sse2 + shipping). Convex suite at v3-close: **207 cases / 21513 assertions**. **Two latent v3-era debts paid en route**: (1) `engine/geometry-primitives/src/predicates.cpp::two_two_sum` Shewchuk-primitive helper was unused on the live code paths and clang-cl `-Werror=unused-function` failed both clang-cl Windows configs — marked `[[maybe_unused]]` with comment that it stays as a documented expansion helper for the future Stage D `insphere` consumer; (2) 19 non-ASCII TEST_CASE names in v3b/v3c (`→` / `—` mojibaking through Windows ctest argv — the `crd-no-non-ascii-test-names` guard class) replaced with ASCII equivalents — the guard was wired correctly at v1i-c but v3b/v3c shipped past it because per-slice verification was test-binary-direct not ctest. **ADR-0076 §18.5 added** locking Shewchuk-predicates + Quickhull + hull-simplify substrate decisions per the Q1–Q4 user-approved choices. Phase 3.1.7 progress: ~32 of the renewed-scope 49 total slices shipped (v0a–v0f + v1a–v1j + v2a–v2-close + v3a–v3-close all done). Session log `docs/sessions/2026-05-15-geometry-v3-close.md`. **Next sub-module = v4 `-mesh` cluster** (TriangleMeshView + half-edge + mesh closest-point + Möller-Trumbore raycast + Jacobson 2013 winding number + v4g per-leaf SIMD + v4-validate formal mesh validation pass).

**Phase 3.1.7 v3d ✅ SHIPPED 2026-05-15 — hull simplification.** New `engine/geometry-convex/include/crd/geometry/convex/hull_simplify.hpp` + `engine/geometry-convex/src/hull_simplify.cpp` (~440 LOC engine + ~440 LOC tests). `simplify_hull(source, alloc, opts) → QuickhullResult<T>` — greedy vertex-removal with **shrinkage-distance cost metric** (per-vertex cost = max signed distance from removed vertex to any fan-triangle plane that would replace it; pivot = lowest-index ring vertex per ADR-0076 §4 pin #11). **Convexity guard** at every removal candidate: each new fan face's outward plane must have all OTHER live hull vertices below within `k_distance_epsilon`; failures tombstone the candidate, try next. **`HullSimplifyOptions::keep_vertex_indices: ConstSpan<u32>`** locks contact-critical / boundary / attachment-point vertices from collapse — multi-domain pin (eylem stable contact warm-start IDs, CAD remap, FEA attachment points, robotics gripper-finger hull contact-pad vertices). Stop when `target_vertex_count` reached OR `max_error_threshold` exceeded OR all candidates exhausted. Output vertices are a strict subset of input vertices (output ⊆ input AABB trivially); vertex order = increasing-original-input-index (matches v3c convention). Degenerate sources (empty / <4 vertex / coplanar / colinear / coincident) return identity copies. Templated `T ∈ {f32, f64}` with explicit instantiations. **16 v3d test cases / 191 assertions** all pass on win-debug + win-asan + win-shipping + win-tidy (full project ctest 1546/1546 win-debug + win-asan + win-tidy, 1541/1541 win-shipping). Full convex suite **198 cases / 21270 assertions**. **Bug caught + fixed during first test run**: initial `build_ring` walked CW around v (using `(k+1)%3` for r_start); convexity check failed because fan winding was inverted. Fix: use `(k+2)%3` (the vertex that comes BEFORE v in F0's CCW order) — walks CCW around v from outside, matches outward normal direction. **Drive-by**: fixed pre-existing non-ASCII characters (`→`, `—`) in v3b + v3c TEST_CASE names that were Windows-ctest-mojibaking (same bug class as v0f / v1i-b / v1i-c which the `crd-no-non-ascii-test-names` guard was added to catch — the guard's scope needs revisit since v3b/c shipped past it). Session log `docs/sessions/2026-05-15-geometry-v3d-hull-simplify.md`. Full 17-config sweep deferred to v3-close per user directive. **Next = v3-close** (tiebreak conformance sweep + degenerate corpus + perf bench `test_bench_hull_simplify.cpp` + full 17-config `scripts/full-sweep.ps1` + ADR-0076 §18 amendment + session log).

**Phase 3.1.7 v3c ✅ CLOSED 2026-05-14 — all 3 sub-slices (a + b + c) shipped same day.** v3c-c adds `enrich_for_gjk(QuickhullResult&)` mutator that populates `vertex_adjacency_indices/offsets` (v2g hill-climb hull-support consumer) for both f32+f64, plus `vx_soa/vy_soa/vz_soa` (v2h SoA SIMD support) for f32-only via `if constexpr (std::is_same_v<T, f32>)` — padded to multiple of 8 by vertex-0 duplication matching v2h's branch-free reducer convention. Plus flat-3D-hull coplanar reconstruction: detect dominant axis → project to 2D → run v3b 2D hull on dominant plane → build 2 face copies (front +plane_normal, back -plane_normal) with CCW determination via cross-product test. Vertices stored in their original 3D coordinates (on-plane positions preserved exactly). New `engine/geometry-primitives/include/crd/geometry/primitives/hull_adjacency.hpp` ships `compute_vertex_adjacency_from_faces` (promoted from test_hill_climb.cpp — multi-consumer helper). **8 new v3c-c test cases / 230 assertions all pass first-try** across 3 configs: tetra adjacency (each vertex has 3 neighbors), cube adjacency (≥3 + edge-symmetry verified), f32 SoA padded to 8, f64 SoA stays empty, coplanar square (4 verts + 2 opposing-normal faces), coplanar pentagon (interior excluded), coplanar hull view. Tidy clean (one nested-ternary fixed). Full convex suite: 182 cases / 21079 assertions. **Total v3c**: ~1020 LOC engine + ~660 tests, 22 cases / 401 assertions across the 3 sub-slices (~1500 LOC honest sizing came in under budget). Session log `docs/sessions/2026-05-14-geometry-v3c-c-enrich-coplanar.md`. **v3d NEXT** = hull simplification (QEM decimation + `keep_vertex_indices` locked-vertex constraint, ~400 LOC + ~300 tests, 2-3 days). After v3d → v3-close (full 17-config sweep + tiebreak conformance + perf bench).

**Phase 3.1.7 v3c-b ✅ SHIPPED 2026-05-14 — Quickhull main iteration loop (~310 LOC added).** 8-step algorithm: interior-witness centroid → pick lowest-face-index work face → pick eye (argmax distance, lowest-idx tiebreak) → visible-face DFS using v3a Stage D `orient3d` (bit-exact visibility decisions) → horizon edges → build new faces with interior-witness-verified CCW orientation → wire neighbors (6a: across horizon to outer neighbor; 6b: between new faces around the eye) → tombstone visible faces + collect orphans → redistribute orphans by lowest-face-index. Cap of 100k iterations with `CRD_ASSERT`. **14 v3c test cases / 171 assertions all pass first-try** across win-debug + win-asan + win-shipping: regular tetrahedron / unit cube (12 triangles) / regular octahedron / cube+interior-points / 50 random cube / 500 random sphere (Euler `F == 2(V-2)` verified exact) / empty / single / coincident / 5 collinear / 5 coplanar (flag set; full reconstruction is v3c-c) / determinism replay / f32 / `convex_hull_view_of` integration. Tidy clean on v3c-specific files (3 minor warnings fixed inline: unused using-decl, local constexpr `V`/`F` renamed). Full convex suite 174 cases / 20849 assertions. **Why first-try worked**: advisor's design pass before code surfaced Shewchuk "below=positive" convention, interior-witness pattern, deterministic-traversal requirement; v3a-debt's full Stage D `orient3d` provides the exact-sign primitive visibility tests need. Session log `docs/sessions/2026-05-14-geometry-v3c-b-quickhull-iteration.md`. **v3c-c NEXT** = `enrich_for_gjk` (adjacency + SoA) + flat-3D-hull coplanar reconstruction via v3b 2D hull on dominant plane.

**Phase 3.1.7 v3c-a ✅ SHIPPED 2026-05-14 — Quickhull skeleton + initial tetrahedron + degenerate fallbacks.** Honestly slicing v3c (~1500 LOC honest sizing per Q3) into 3 sub-slices for per-seam closure: **v3c-a (DONE today)** ships the public header `quickhull.hpp` with `QuickhullResult<T>` owning-arrays type + `QuickhullOptions<T>` + `convex_hull_view_of` non-owning view helper + `enrich_for_gjk` stub, plus `quickhull.cpp` with internal `QhFace` triangle-face type + `find_initial_tetrahedron` (6 axis-extremal points → 2 most-spread + furthest-from-line + furthest-from-plane with v3a full Stage D `orient3d` verification) + `build_initial_tetrahedron` (4 CCW-from-outside faces with neighbor adjacency). Degenerate cases (0/1/2 points, coincident, colinear, coplanar) routed to flag-setting + early return. Builder-reject `CRD_ASSERT(all_finite(points))`. Templated on `T ∈ {f32, f64}` with explicit instantiations. ~480 LOC engine. Builds clean on win-debug. **v3c-b NEXT** = main iteration loop (eye selection, visible-face DFS, horizon walk, face replacement, conflict-list redistribution — the bulk of Quickhull, ~800-1000 LOC). **v3c-c after** = `enrich_for_gjk` adjacency+SoA + flat-3D-hull coplanar reconstruction via v3b 2D hull. Per-seam discipline per advisor: v3c-a verified to build before starting v3c-b.

**Phase 3.1.7 v3a-debt-paydown ⚠️ PARTIAL 2026-05-14 — orient3d + incircle full Stage D shipped; insphere remains Stage-A-equivalent re-expression.** Per user directive "pay all debts we created" after v3a/v3b ship. **orient3d**: full Stage D shipped (`orient3d_exact` builds six 4-element 2x2-minors → four 24-element triangle cofactors → cascading sums to 96-element exact expansion). **incircle**: full Stage D shipped (`incircle_exact` + new `expansion_product` general N×M expansion-by-expansion multiply helper; four 96-element cofactors → 384-element final expansion). **insphere**: 5-cofactor Laplacian STRUCTURE in place but inner products are f64 not expansion arithmetic — honestly documented as a Stage-A-equivalent re-expression, full Stage D deferred to v8 Bowyer-Watson 3D Delaunay (the validation-gap risk of shipping ~2000 LOC of intricate expansion arithmetic without a consumer outweighed the "pay all debts" mandate per advisor recommendation). 4 new Stage D adversarial tests added: truly-coplanar slanted plane (exact 0), tiny perpendicular perturbation at scale 100 (resolves below f64 noise floor), 4 cocircular points on radius-1e3 circle (exact 0), tiny outside-perturbation on unit circle (resolves negative). All 3 configs green (win-debug + win-asan + win-shipping); clang-tidy clean on predicates.cpp + test_predicates.cpp (pre-existing v0c deterministic.cpp warnings tracked separately). Full geometry-primitives suite 153 cases / 65597 assertions. Bug caught: first Stage D test at scale 1e7 + 1e-10 perturbation failed because perturbation was BELOW input-storage ULP at that scale (the adaptive predicate correctly returned 0 because input WAS coplanar in f64) — dropped to scale 100 + 1e-12 perturbation which actually exercises Stage D. Updated `docs/debt.md` with the honest partial-paydown status + the v8 trigger for insphere upgrade. Session log `docs/sessions/2026-05-14-geometry-v3a-debt-paydown.md`. **Now v3c 3D Quickhull is unblocked with FULL Stage D orient3d backing it.**

**Phase 3.1.7 v3b ✅ COMPLETE 2026-05-14 — 2D convex hull (Andrew's monotone chain).** New `engine/geometry-convex/include/crd/geometry/convex/convex_hull_2d.hpp` (~250 LOC) ships `convex_hull_2d_indices(points, out_indices)` primary + `convex_hull_2d_points(points, out_points)` convenience, templated on `T ∈ {f32, f64}`. Algorithm: lex-sort with `crd::containers::stable_sort` + dedup coincident + lower hull (left-to-right, pop while not strict left turn via v3a `orient2d`) + upper hull (right-to-left, same rule) + concatenate (drop duplicate endpoints). Output is CCW. Degenerate input handled (0/1/2/3 collinear/all-coincident). 14 v3b cases / 58 assertions on win-debug + win-asan + win-shipping all green. Full convex suite 160 cases / 20678 assertions. Adversarial near-collinear (Shewchuk torture: 4 points on y=0 plus outlier at y=1e-14) places the outlier on the hull correctly — proves the adaptive predicate chain works end-to-end. Clang-tidy clean on v3a + v3b files (multi-var declarations split, `std::numbers::pi_v<f64>` replaces literal pi, local constexpr renamed to `kCamelCase` per `.clang-tidy`); pre-existing v2 tidy warnings are carry-over debt. Session log `docs/sessions/2026-05-14-geometry-v3b-convex-hull-2d.md`. Next = **v3c 3D Quickhull (Barber 1996, honest 1500-LOC sizing)** on user command.

**Phase 3.1.7 v3a ✅ COMPLETE 2026-05-14 — Shewchuk 1997 adaptive predicates shipped.** New `engine/geometry-primitives/include/crd/geometry/primitives/predicates.hpp` (Stage A inline) + `engine/geometry-primitives/src/predicates.cpp` (Stage B/C/D out-of-line). `orient2d` / `orient3d` / `incircle` / `insphere` for f64 + f32 (f32 promotes to f64 internally). Substrate-foundation slice consumed by v3b/v3c + future v6 Vatti / v8 Bowyer-Watson / v9 V-HACD / CFD AMR / FEA contact / CAD boolean. Two-Product uses `std::fma` (2 FLOPs vs ~17 for Veltkamp-Dekker; not banned by `crd-no-std-math-check`). Determinism: bit-exact across compilers / SIMD widths / OSes (IEEE 754 binary64 + `/fp:precise` + no transcendentals + no extended-precision FP registers per ADR-0063). NaN/Inf inputs return 0.0 (queries-tolerate per ADR-0076 §15). Shewchuk's `orient3d` convention pinned: "below = positive" (the published convention, opposite of `det([b-a, c-a, d-a])`). 31 test cases / 1031 assertions on win-debug + win-asan + win-shipping all green. Full geometry-primitives suite: 149 cases / 65590 assertions. Bug caught + fixed: first test draft used "above = positive" intuition; fixed to match Shewchuk's published convention. Stage D for orient3d + full Stage B+C+D for incircle/insphere left as TODO with API surface in place; v8 Bowyer-Watson 3D Delaunay is the consumer that drives the deeper adaptive paths and is months out. Session log `docs/sessions/2026-05-14-geometry-v3a-shewchuk-predicates.md`. Full 17-config sweep deferred to v3-close per user directive. Next = **v3b 2D convex hull (Andrew's monotone chain)** on user command.

**Phase 3.1.7 v3 — `-convex` hull construction — scoped 2026-05-14, ready to start on user command.** 4 slices + close per ADR-0076 §18 amendment (4 user-approved substrate-foundation decisions): **v3a Shewchuk 1997 adaptive predicates** (`engine/geometry-primitives/include/crd/geometry/primitives/predicates.hpp` — NEW substrate-foundation slice; `orient2d` / `orient3d` / `incircle` / `insphere` with adaptive expansion arithmetic; resolves ADR-0076 §164/§397 open question pre-emptively; consumed by v3b/v3c + future v6 Vatti / v8 Bowyer-Watson / v9 V-HACD / CFD AMR / FEA contact / CAD boolean) → **v3b 2D convex hull** (Andrew's monotone chain; lex-sort + exact `orient2d` from v3a; CCW output; both indices + points-array API surfaces) → **v3c 3D Quickhull** (Barber-Dobkin-Huhdanpaa 1996; QuickhullResult + `convex_hull_view_of` + `enrich_for_gjk` post-processor; horizon walk; coplanar fallback to v3b on dominant plane; honest 1500-LOC sizing per Q3 recommendation, not the optimistic original 900) → **v3d hull simplification** (QEM-style cost-driven decimation; `HullSimplifyOptions::keep_vertex_indices: ConstSpan<u32>` for locked-vertex constraint from day 1 — multi-domain payoff for CAD/FEA/robotics) → **v3-close** (tiebreak conformance + degenerate corpus + perf bench + full 17-config sweep + doc updates). ~2750 LOC engine + ~2000 LOC tests, ~13-19 days calendar (~2.5 weeks). NO `ConvexHullViewOwning<T>` type (Q2 — keep the v2 non-owning view pattern). User approved all 4 Q-recommendations 2026-05-14; ready to start on user "let's go v3a" command.

**Phase 3.1.7 `crd-geometry-convex` v2 — ✅ COMPLETE 2026-05-14.** All 11 v2 slices shipped (v2a GJK distance + 3 substrate decisions → v2b GJK overlap fast-out → v2c EPA penetration → v2d SAT box-pair → v2e ConvexHullView queries → v2f GJK shapecast → v2g hill-climb support → v2h Vec4f/Vec8f SIMD support → v2i f64 instantiation + orbital corpus → v2j Sutherland-Hodgman polygon clipping + feature enumeration [eylem v1d-manifold gate] → **v2-close** tiebreak conformance + degenerate corpus + perf bench + full 17-config sweep). New module `engine/geometry-convex/` (target `crd-geometry-convex`, ns `crd::geometry::convex`). System doc `docs/systems/geometry-convex.md` (created at v2-close — was deferred at v2a). Session log `docs/sessions/2026-05-14-geometry-v2-convex-substrate.md`. **`crd-geometry-convex-tests` 146 cases / 20624 assertions.** v2-close added `test_tiebreak_conformance.cpp` (9 cases / 103 assertions exercising every ADR-0076 §4 pin #14 rule with adversarial inputs) + `test_degenerate_corpus.cpp` (17 cases — zero-radius/zero-extent primitives, 1/2/3/4-vertex hulls, coplanar hulls, identical-pose pairs, tangent contacts, far-origin 1e6/1e7, near-zero separations). Substrate decisions LOCKED: (1) C++20 `ConvexShape<S,T>` concept + ADL `support()` overloads; (2) local-frame shapes + two transforms (~40% faster on OBB pairs); (3) `SupportPoint<T>{point, vertex_idx}` with `k_invalid_vertex` for analytic + lowest-vertex-idx tiebreak for polyhedral (Box2D-style epsilon-free index-match termination, cross-platform bit-exact); (4) Sutherland-Hodgman lerp form pinned (`v_i + t*(v_{i+1}-v_i)` for seam bit-equality across plane orderings); (5) SAT preempts GJK for OBB-OBB via facade overload (bypasses the EPA rotated-non-cube OBB pathology). Bench: `tests/bench/test_bench_gjk.cpp` covers v2a–v2h paths + new v2j `clip_against_convex_volume` 5x6. **Phase 3.1.7 next = v3 `-convex` Quickhull**, or pivot to eylem v1c (broadphase consuming `crd-geometry-bvh::DynamicBvh`) + v1d-mesh / v1d-manifold (consuming v2j + v2c) per the 2026-05-11 phase-sequencing pivot (ADR-0076 §12 — Phase 3.1.7 executes BEFORE Phase 3.1 v1c so eylem consumes geometry from day 1, no deferred-refactor).

**Detour D-002 — Concurrent containers + stress-hardening — ✅ CLOSED 2026-05-12.** Shipped (v0–v6): `tests/stress/` harness (fiber + std::thread-mirror); `docs/systems/scene-concurrency.md` (per-storage concurrency contracts); `Array` debug `freeze()`/`FrozenView` + `jobs::parallel_reduce`; `crd::containers::ConcurrentQueue<T>` (Vyukov bounded MPMC promoted out of `jobs/src/mpmc_queue.hpp`; jobs scheduler re-pointed; new `crd-jobs → crd-containers` PRIVATE edge; `mpmc_queue.hpp`/`test_mpmc.cpp` deleted); `crd::containers::AtomicArray<T>` + `CacheLinePadded<T>`; allocator stress matrix (per-allocator-contract, `test_allocators_v5_stress.cpp`); scene-storage stress matrix (`test_scene_storage_stress.cpp`). Allocators stay single-threaded-by-contract; concurrent hash map + sharded global allocator + `[.soak]` nightly lane deferred to `docs/debt.md`. Built + run green on win-debug/win-asan/win-clang-cl/linux-gcc-debug per slice; full 14-config sweep + soaks delegated to CI. Detour doc: `docs/detours/D-002-concurrent-containers-and-stress-hardening.md` (closed); session log: `docs/sessions/2026-05-12-d002-concurrent-containers-stress.md`.

**→ Phase 3.1.7 `crd-geometry` IN PROGRESS. v0a ✅ done 2026-05-12** — `crd-geometry-primitives` module created (`engine/geometry-primitives/`, target `crd-geometry-primitives`, namespace `crd::geometry::primitives`); the **`crd::math::geometry` move-and-delete** (ADR-0076 §13) is done: `Ray`/`Plane`/`Sphere`/`AABB`/`Triangle`(→`Triangle3`)/`Frustum` + ~16 helpers + their `std::formatter`s migrated to `crd/geometry/primitives/{primitives,format}.hpp`; new bare types `Line`/`Segment`/`OBB`/`Capsule` added; `crd/math/geometry.hpp` deleted; `crd-math` now ships only Vec/Mat/Quat/Transform/SIMD/`deterministic`; 9 consumers repointed (`crd-scene` query/world — new edge `crd-scene → crd-geometry-primitives` PUBLIC; `crd-math` umbrella+format; `tests/math` geometry tests moved to new `tests/geometry-primitives/test_primitives.cpp`; `tests/bench`; `tests/scene`; `runtime/examples/smoke_math.cpp`); `crd-no-std-math-check` CI guard now scopes `engine/geometry-primitives`. Green on win-debug/win-asan/win-clang-cl/linux-gcc-debug (geometry-primitives-tests 6 cases, math-tests 140, scene-tests 271, smoke_math). System doc: `docs/systems/geometry-primitives.md`. **v0b ✅ done 2026-05-13** — added the 2D peer set (`Line2`/`Segment2`/`Ray2`/`AABB2`/`OBB2`/`Circle`/`Capsule2`/`Triangle2`/`Point2`), renamed the v0a 3D types to `…3` forms (dimension-suffix naming rule, ADR-0076 §14), and shipped `closest_point.hpp` (closest_point/closest_param/distance/distance_squared for every primitive 2D+3D, plus `closest_points(Segment,Segment)`; Ericson Voronoi-region triangle + Ericson segment↔segment cores). Green on win-debug + win-shipping (geometry-primitives-tests 20 cases / 39726 assertions; scene/math/smoke unaffected). **v0c ✅ done 2026-05-13** — new `intersect.hpp` (intersection corpus, 2D + 3D): ray casts `intersect_ray_{aabb,obb,cylinder,capsule}` + 2D `intersect_ray2_*`; SAT `intersects(AABB3,Triangle3)` Akenine-Möller 13-axis, `intersects(OBB3,OBB3)` 15-axis, `intersects(OBB2,OBB2)` 4-axis, `intersects(Triangle2,Triangle2)` 6-axis, `intersects(Triangle3,Triangle3)` Möller 1997; `segments_intersect(Segment2,Segment2)`; sphere/circle/capsule↔X reductions via v0b distance; frustum↔OBB3; plane↔box. Added types `Cylinder3`/`Cylinder2` (flat-cap segment+radius). Green on win-debug + win-shipping (geometry-primitives-tests 38 cases / 40988 assertions; clang-tidy/format clean; `crd-no-std-math-check` green). **v0d ✅ done 2026-05-13** — new type `Tetrahedron` (3D-only → no suffix) + `centroid`/`signed_volume`/`volume`/`operator==`/formatter/aliases; new `barycentric.hpp` — `barycentric(Tetrahedron,p)→Vec4` (orientation-stable signed-volume ratios), `contains(Tetrahedron,p)`, `from_barycentric` (inverse, for `Triangle3`/`Triangle2`/`Tetrahedron`), `decompose_prism_to_tets(bottom,top)→StaticArray<Tetrahedron,3>` (canonical 3-tet wedge split, Marching-Tetrahedra/FEM form). Green on win-debug + win-shipping (geometry-primitives-tests 46 cases / 42182 assertions; clang-tidy/format clean). **v0e ✅ done 2026-05-13** — new `formulary.hpp` (iq SDF combinators: `smin_poly`/`smin_cubic`/`smin_exp` + `smax_*`; `op_round`/`op_onion`/`extrude_2d`; `domain_repeat`/`domain_mirror`/`domain_elongate` + `domain_twist`/`domain_bend` via `crd::math::deterministic`); new `crd::math::simd::reduce_argmax_with_lex_tiebreak` (`crd/math/simd/reduce.hpp` — scalar-deterministic argmax, score-desc/(x,y,z)-asc-lex/earliest-index, ADR-0076 §4 #10, v3 Quickhull's pin); new module `crd-geometry-shader-helpers` (skeleton — GLSL/HLSL twin of the formulary, v9e fills it in). Green on win-debug + win-shipping (geometry-primitives-tests 56 cases / 49224 assertions; crd-math-tests 143; clang-tidy/format clean; `crd-no-std-math-check` green). **v0f ✅ done 2026-05-13 — closes the v0 sub-phase** — new scalar headers `watertight_ray_tri.hpp` (Woop/Benthin/Wald 2013 watertight ray-tri `precompute_ray_tri`/`intersect_ray_triangle_watertight` + Baldwin-Weber 2016 precomputed-affine `TriAffine`/`intersect_ray_triangle_precomputed`), `robust_ray_aabb.hpp` (Williams 2005 slab + Ize 2013 conservative widening, NaN-safe), `plucker.hpp` (`PluckerLine`, `plucker_side`, branchless ray-tri); SIMD `simd_batch.hpp` + out-of-line `src/simd_batch.cpp` (`Vec8f` AoSoA bundles + 6 kernels: `ray_vs_8_aabb`/`ray_packet8_vs_aabb`/`ray_vs_8_triangle`/`aabb8_vs_aabb`/`sphere8_vs_sphere`/`segment8_vs_segment_distsq`) — single-ray (leaf-batch; the scalar precompute structs are per-ray) vs multi-ray (`RayPacket8`, 8 coherent rays) split made explicit. Drive-by sweep fixes: GCC `-Werror=shadow` in `intersects(Triangle3,Triangle3)`'s `interval` lambda (renamed `d0/d1/d2`→`e0/e1/e2`); **removed non-ASCII chars from all new `TEST_CASE` names** (em-dash etc. broke Windows ctest name-matching — `check_no_non_ascii_test_names` guard now green). **Full 14-config `scripts/full-sweep.ps1` PASS** (geometry-primitives-tests 74 cases; math-tests with `test_reduce`). **v0 sub-phase ✅ CLOSED 2026-05-13.** **`crd-geometry-bvh` sub-module — v1a ✅ done 2026-05-13:** new module `engine/geometry-bvh/` (target `crd-geometry-bvh`, ns `crd::geometry::bvh`, deps core+math+containers+geometry-primitives; added to root CMake + `crd-no-std-math-check` scope). `BvhTree` (flat 32-byte-node array + leaf-order index permutation; doesn't own the AABBs); `bvh_build(ConstSpan<AABB3<f32>>, IAllocator*, BvhBuildOptions) → BvhTree` — Wald 2007 binned-SAH binary builder (centroid-bucket per axis, sweep bin boundaries for the min-cost split, recurse; ≤max_leaf_prims⇒leaf; coincident centroids⇒deterministic median-by-index fallback; single-threaded — jobs-parallel build is v1f); determinism pinned ADR-0076 §5.2 (SAH split X→Y→Z, lower bin wins; no `std::sort`; deterministic-replay test memcmps two builds); `bvh_sah_cost` quality metric; `bvh_raycast` (ordered traversal near-child-first, v0f Williams/Ize robust slab per node+prim, exact entry-t) → `optional<BvhRayHit{t, payload}>` (`BvhRayHit = RayHit<u32>` after v1i-a, payload = prim index); `bvh_overlap` callback + `Array<u32>&`. 14 new test cases (`tests/geometry-bvh/test_bvh_{build,raycast}.cpp` — structural invariants, leaf-cap, coincident-stack, SAH quality, deterministic replay, raycast/overlap vs brute force). clang-format/tidy clean; `crd-no-std-math-check` green. Built+run green on **win-debug + win-asan + win-shipping** (+ win-tidy build). System doc `docs/systems/geometry-bvh.md`. **v1b ✅ done 2026-05-13:** `bvh_refit(BvhTree&, ConstSpan<AABB3<f32>>)` — O(n) bottom-up bounds recomputation, topology untouched (single reverse pass over the node array — children have a higher index since `bvh_build` pushes the parent first); new public header `crd/geometry/bvh/bvh_update.hpp` + impl `src/bvh_update.cpp` + internal `src/aabb_ops.hpp` (the `aabb_*` helpers extracted from `bvh_build.cpp`, shared). Query correctness unaffected by how far prims move (ancestors grow to cover them). 4 new test cases (`test_bvh_refit.cpp`). Built+run green win-debug/win-asan/win-shipping/win-tidy; `crd-geometry-bvh-tests` 18 cases. **v1c ✅ done 2026-05-13:** `DynamicBvh` — the incrementally-updatable AABB tree (new `crd/geometry/bvh/dynamic_bvh.hpp` + `src/dynamic_bvh.cpp`; a *different structure* from the static `BvhTree`): binary tree of fat AABBs with parent pointers + free list of recycled nodes; `insert`/`remove`/`update` with Box2D-style height-balanced tree rotations (`balance`); fat-AABB margin (small move ⇒ no-op); `query`/`raycast` over fat AABBs (broadphase, fixed `stack[256]`); `validate()`/`sah_cost()`/`max_depth()` diagnostics; stable `DynamicBvhNodeId` handles. Catto GDC 2019 / Box2D v3 `b2DynamicTree` — the form eylem v1c broadphase wraps. 7 new test cases (`test_dynamic_bvh.cpp`, incl. a 4000-op random insert/remove/update sequence with `validate()` checkpoints + balance/SAH bounds). Drive-by: `[[maybe_unused]]` on `validate()`'s assert-only locals (Release `/WX` C4189) + fixed an em-dash in a new TEST_CASE name (Windows ctest). Built+run green win-debug/win-asan/win-shipping/win-tidy; `crd-geometry-bvh-tests` 25 cases. **v1d ✅ done 2026-05-13:** `Bvh4Tree` — the 4-wide topology variant (new `crd/geometry/bvh/bvh4.hpp` + `src/bvh4.cpp`). `bvh4_collapse(BvhTree, IAllocator*) → Bvh4Tree` widens a built binary tree (open the largest interior child, repeat, deterministic tiebreak; ≤4 children/node; ≤ binary node count) + `bvh4_raycast` (scalar four-sequential-robust-slab "ray-vs-4-AABB") + `bvh4_overlap` — both give the same results as `bvh_raycast`/`bvh_overlap` over the source binary tree. v1g promotes BVH4 to default + adds the `Vec4f` SIMD kernel. 5 new test cases (`test_bvh4.cpp`). Built+run green win-debug/win-asan/win-shipping/win-tidy; `crd-geometry-bvh-tests` 30 cases. **v1e ✅ done 2026-05-13:** `bvh_closest_point(BvhTree, prims, query, max_dist=∞) → optional<BvhClosestPoint{point, distance_squared, payload}>` (`BvhClosestPoint = ClosestPointResult<u32>` after v1i-a, payload = prim index) — branch-and-bound stack DFS over the binary tree (per-node AABB squared distance = lower bound on every leaf below; node ≥ best pruned, re-checked on pop; nearer child popped first to tighten; squared space throughout, `max_dist²` cutoff; anon-ns `aabb_dist2` keeps the TU off `closest_point.hpp`). Closest point on the primitive *AABB* (per-triangle is `-mesh` v4; Bvh4/DynamicBvh closest-point are v1i). 5 new test cases (`test_bvh_closest.cpp`). Built+run green win-debug/win-asan/win-shipping/win-tidy; `crd-geometry-bvh-tests` 35 cases. **v1f ✅ done 2026-05-13:** `bvh_build_parallel` — jobs-parallel binned-SAH build (new `crd/geometry/bvh/bvh_build_parallel.hpp` + `src/bvh_build_parallel.cpp`; `crd-geometry-bvh` gains `crd-jobs` PRIVATE; serial kernels extracted to `src/bvh_build_internal.hpp`, `bvh_build.cpp` refactored onto them). Fans the per-node centroid-bounds + 3-axis-bin-histogram reductions over `crd::jobs::parallel_for(num_jobs, num_jobs, …)` for nodes ≥ `parallel_threshold` (default 8192); per-job partials on the tree allocator (reused per node, not the frame arena); folded in fixed job order — min/max + integer adds are exact+commutative ⇒ **byte-for-byte equal to the serial `bvh_build`** for any `num_jobs`. `num_jobs=0`⇒`jobs::num_workers()`; ≤1 or sub-threshold ⇒ serial fallback. **Fixed a latent `aabb_merge` bug** (it `aabb_include_point`-ed `b.min` and `b.max` as points → corrupted `a` to the universe box when `b` was the empty sentinel `{+∞,−∞}`; misbehaved on every empty-bin merge — masked in the serial path [valid-but-suboptimal splits], wrong in the parallel histogram fold) → fixed to a componentwise corner-min/max union; side effect: `bvh_build`'s trees are now correctly SAH-split (different node arrays, all tests still pass). Embree-comparison benchmark `tests/bench/test_bench_bvh.cpp` (`[!benchmark]`, not in ctest; `crd-bench` gains `crd-geometry-bvh`+`crd-jobs`) — Cerid's own numbers, §4.1 targets inline, no Embree dep. New `tests/geometry-bvh/test_bvh_parallel.cpp` (hosts the binary's `BvhJobsListener`; 4 cases — bit-identical-to-serial for `num_jobs∈{1,2,4,8}` w/ threshold 64, 20k-AABB above-default-threshold, degenerate inputs, serial-fast-path). Built+run green win-debug/win-asan/win-shipping/win-tidy; `crd-geometry-bvh-tests` 39 cases. **v1g ✅ done 2026-05-13 — `-bvh` sub-module COMPLETE (v1a–v1g):** new `crd/geometry/bvh/bvh4_simd.hpp` + `src/bvh4_simd.cpp` — `ray_vs_4_aabb` `Vec4f` kernel (one Tavianator min/max slab chain over the 4 children, Ize 2013 conservative `tmax` widening per lane, NaN/∞-safe; out-of-line in a `.cpp` — a real TU; but `Vec4f` is 128-bit at every SIMD level so the AVX2-`ymm`-expecting `crd-simd-emission-check` doesn't scan it — a 128-bit-aware variant is a follow-up like `simd_batch.cpp`). `bvh4_raycast` now uses it (transpose ≤4 children's bounds → SoA `Vec4f` → one kernel call → scan lanes; leaf-prim tests stay scalar) — **bit-identical** (finite/well-formed inputs) to the 4 scalar `intersect_ray_aabb_robust` calls it replaced. Option A taken: kept `bvh4_collapse` as the BVH4 path, no second 4-way builder; `BvhBuildOptions::topology` + the `BvhTopology` enum **removed** (`bvh_build` is binary, `bvh4_collapse` is the path). Docs flip: BVH4 is the recommended traversal form for static query-heavy data. New lane-by-lane kernel test in `test_bvh4.cpp`. Built+run green win-debug/win-asan/win-shipping/win-tidy; `crd-geometry-bvh-tests` 40 cases. **v1h ✅ done 2026-05-13 — `-primitives` substrate hardening (ADR-0076 §15):** new `crd/geometry/primitives/constants.hpp` (the geometry epsilon/tolerance policy named by intent — templated `constexpr T k_*()` in the `k_` snake form `bvh_build_internal.hpp` already used: `k_distance/area/parallel/degenerate_extent_epsilon` = `1e-6F`/`1e-12`, `k_sah_cost_epsilon` [`bvh_build_internal.hpp` now re-exports this — one source of truth], `k_default_fat_margin` = `0.1` [a *world unit*], `k_robust_aabb_pad_ulps` = `3` [`robust_ray_aabb.hpp`'s `ray_aabb_robust_pad<T>()` derives `γ_n` from it]) — a **value-preserving rename**, `crd-geometry-bvh-tests` stays 40/40, the existing function-signature defaults still bind `crd::math::default_epsilon<T>()` (migrating those is a later pass); new `crd/geometry/primitives/is_finite.hpp` (`is_finite(x)` for `Vec2/Vec3` + every primitive [3D + 2D peers + `ConvexHullView`] + `all_finite(ConstSpan<Primitive>)` + the **NaN/Inf contract**: queries tolerate [IEEE-comparison corpus already gives this — finiteness predicates are NOT on the query path], builders reject in debug — `CRD_ASSERT(all_finite(prims))` in `bvh_build`/`bvh_build_parallel`, `CRD_ASSERT(is_finite(aabb))` in `DynamicBvh::insert`/`update`, `CRD_ASSERT(is_finite(binary.bounds()))` in `bvh4_collapse`; release → valid-but-possibly-degenerate, still no UB; internal sentinels exempt — `aabb_empty()={+∞,−∞}` is a deliberate non-finite union identity, pinned by a test); new `crd/geometry/primitives/signed_distance.hpp` (20 Inigo Quilez analytic SDFs in C++ — `sqrt`/`abs`/`min`/`max`/`clamp`/`dot`/`length` only [`crd-no-std-math-check` clean], component helpers `vabs`/`vmax0`/`max3`/`sgn` in a `sd_detail` sub-namespace: 3D `sd_sphere`/`sd_box`/`sd_round_box`/`sd_box_frame`/`sd_plane`/`sd_capsule`/`sd_cylinder`/`sd_cone`/`sd_torus`/`sd_triangle`[unsigned]/`sd_ellipsoid`[iq bound]/`sd_octahedron`; 2D `sd_circle`/`sd_box_2d`/`sd_round_box_2d`/`sd_segment_2d`[unsigned]/`sd_triangle_2d`[signed]/`sd_equilateral_triangle_2d`/`sd_pentagon_2d`/`sd_hexagon_2d` — the C++ scalar reference `crd-geometry-shader-helpers` v9e emits GLSL/HLSL twins of + `crd-sdf` v0 reuses; `closest_point.hpp` gives unsigned distance + point on the *primitive structs* in world space, this gives the signed negative-inside value in *shape-local* space); new `ConvexHullView<T>` in `primitives.hpp` (non-owning: `ConstSpan<Vec3> vertices` + `ConstSpan<Plane> faces`[outward] + `ConstSpan<u32> face_vertex_indices` + `ConstSpan<u32> face_vertex_offsets`[prefix-sum] + `support(hull,dir)`[lowest-index tiebreak] + `contains(hull,point)` — `crd-convex` v3 *produces* one, `crd-eylem`'s `Collider::ConvexHull` references one; ray/closest-point/GJK-contains land in v2). 4 new test files (`test_constants.cpp`, `test_is_finite.cpp`, `test_signed_distance.cpp` [strong cross-checks vs `closest_point.hpp`'s `distance()` for sphere/box/circle/box_2d/segment_2d/capsule/plane/triangle + sign vs `contains` + closed-form spot values + 1-Lipschitz on random pairs, ellipsoid exempt], `test_convex_hull_view.cpp`). clang-format/tidy clean; `crd-no-std-math-check` + `crd-no-non-ascii-test-names` + `crd-simd-emission-check` + `crd-no-std-sort-check` green. Built+run green win-debug/win-asan/win-shipping/win-tidy; `crd-geometry-primitives-tests` 103 cases, `crd-geometry-bvh-tests` 40 cases. System doc `docs/systems/geometry-primitives.md` updated. **v1i ✅ split into v1i-a/b/c on 2026-05-13** (user opted to land the three deliverables as separate slices since the ~700-LOC original was bigger than typical). **v1i-a ✅ done 2026-05-13 — unified query facade + templated result types + missing `closest_point` variants.** New `crd/geometry/result_types.hpp` (`template <typename Payload> struct RayHit{ f32 t; Payload payload; }` + `struct ClosestPointResult{ Vec3 point; f32 distance_squared; Payload payload; }`; field order pinned per ADR-0076 §16 pin #2). New `crd/geometry/queries.hpp` — compile-time-overload-polymorphic `raycast` / `overlap` / `closest_point` over `{BvhTree, Bvh4Tree, DynamicBvh}` (inline forwards; zero overhead); primitive overloads (`closest_point` / `distance` / `intersects` / `signed_distance` / iq `sd_*`) flow through transitively from `closest_point.hpp` / `intersect.hpp` / `signed_distance.hpp` — no redefinition, ADL picks them up. `BvhRayHit` / `BvhClosestPoint` migrated from hand-defined structs to `using BvhRayHit = RayHit<u32>;` / `using BvhClosestPoint = ClosestPointResult<u32>;` aliases — field rename `.prim_index` → `.payload`, field order `{t, payload}` / `{point, distance_squared, payload}`; impl side (`bvh_query.cpp` / `bvh4.cpp` / `dynamic_bvh.cpp`) + 4 test files updated to the new order. **New free function `bvh4_closest_point`** — branch-and-bound DFS, insertion-sort the ≤4 children near-first (push descending, pop ascending; deterministic tiebreak); same shape as v1e `bvh_closest_point`. **New member `DynamicBvh::closest_point`** — broadphase closest leaf by *fat* AABB, reports `user_data` payload (member-fn because it accesses the private `Node` array; mirrors `query`/`raycast`). `DynamicBvh::raycast` deliberately stays callback-only (broadphase semantics). New `tests/geometry-bvh/test_queries.cpp` — 10 cases: layout pins + facade `raycast`/`overlap`/`closest_point` for {BvhTree, Bvh4Tree, DynamicBvh} each cross-checked against the direct backend call + brute force on a random corpus (300+ trials, finite/∞ cutoffs); `bvh4_closest_point` and `DynamicBvh::closest_point` get dedicated edge cases (empty / single-prim / inside-fat-AABB / `max_dist` cutoff). clang-format/tidy clean; `crd-no-std-math-check` + `crd-no-non-ascii-test-names` + `crd-no-std-sort-check` green (`crd-simd-emission-check` green under vcvars). **`crd-geometry-bvh-tests` 50 cases / 65 549 assertions** (was 40 / 13 197 after v1g). Built+run green on **win-debug + win-asan + win-shipping** (+ win-tidy build); full project ctest **1232/1232** on win-debug + win-asan, **1227/1227** on win-shipping. Session log `docs/sessions/2026-05-13-geometry-v1i-a-queries-facade.md`. **In-flight `-bvh` verification directive: win-debug + win-asan + win-shipping (+ win-tidy build) per slice; full 17-config `scripts/full-sweep.ps1` deferred to v1 cluster close (after v1j) — CI is surprisingly green at this point so the sweep amortises once at the end.** **v1i-b ✅ done 2026-05-13 — shapecast (closed-form TOI).** New `crd/geometry/bvh/bvh_shapecast.hpp` + impl with `bvh_shapecast_sphere` / `bvh_shapecast_box` + `bvh4_*` variants (traversal-with-inflated-AABBs over the v0f Williams/Ize robust slab). `queries.hpp` extended with `cast_ray` / `cast_sphere` / `cast_box` overloads against primitive shapes AND BVH backends — sphere-vs-AABB conservative (square corners; GJK-cast in v2 for exact corners), sphere-vs-sphere + sphere-vs-plane + box-vs-AABB exact, all with t=0 overlap-at-start semantics. Argument convention pinned for v2/v6: primitive `(moving, dir, tmax, target)`; BVH `(tree, prims, moving, dir, tmax)`. Capsule-vs-triangle deferred to v2/v4. New `tests/geometry-bvh/test_shapecast.cpp` (15 cases) covers degenerate-recovery + closed-form spot checks + bisection sanity (advisor catches Minkowski-sign bugs the algorithm-vs-algorithm brute force can't) + BVH brute-force agreement + tmax/empty edges. `crd-geometry-bvh-tests` 65 cases / 68 383 assertions; full project ctest 1247/1247 on win-debug + win-asan, 1242/1242 on win-shipping; win-tidy build clean. Session log `docs/sessions/2026-05-13-geometry-v1i-b-shapecast.md`. **v1i ✅ closed 2026-05-13 (a/b/c shipped).** **v1i-c debt payment ✅ done 2026-05-13:** all three deferred items paid same-day per user request — caller-owned `DynamicBvhPairScratch` for `find_overlapping_pairs` hot-path (eylem v1c-ready), `Vec4f ray_vs_4_aabb_inflated` SIMD kernel for `bvh4_shapecast_*` (mirrors v1g pattern, ~2-3× per-node throughput on AVX2), AABB-equivalence shift-invariance assertion. 82 cases / 76 581 assertions on bvh-tests; 1275/1275 win-debug+asan, 1270/1270 win-shipping. Zero new debt. **v1j ✅ shipped on 2026-05-13 (a + b + two same-day bug-fix passes)**. v1j-a — `crd-geometry-viz` companion module substrate. v1j-b — sandbox integration with 3-scene selector (Physics / Geometry showcase / Draw API showcase) + 4-mode geometry showcase + line-width slider threaded through every viz::draw call. **v1 cluster CLOSED 2026-05-13** — 14 sub-slices (v1a..v1j-b). **Full 17-config `scripts/full-sweep.ps1` ✅ RAN AND PASSED** — 17/17 configs green (Win × 9 + Linux × 7 + tidy build), 1297 tests on win-debug, 16:39 wall time, zero failures. **v1-debts-paid pass ✅ done 2026-05-13** — same-day debt payment for all 4 active v1j-b debt entries: (#4) ImGui `imgui_impl_vulkan.cpp` `vkFlushMappedMemoryRanges`/`vkMapMemory` patched to `VK_WHOLE_SIZE` via CMake CPM post-fetch (idempotent; clears VUID-01389+01390); (#3) `closest_point(Cylinder3)` axial/radial decomposition + `closest_point(Tetrahedron)` per-face barycentric clamp added to `crd-geometry-primitives` (`crd-geometry-primitives-tests` 118/118, +15 cases / +22377 assertions); (#2) `BvhViewerCache` (`prims` + `centroids` arrays + `std::unique_ptr<{BvhTree,Bvh4Tree,DynamicBvh}>`, SplitMix64+FNV fingerprint over `(N, seed, world_size, max_box_size, tree_kind)`, lazy-construct under eylem TLSF) — sandbox BVH viewer rebuilds only on slider mutation, N slider cap lifted 800→5000; (#1) `tests/sandbox/test_showcase.cpp` new target `crd-sandbox-showcase-tests` (7 cases / 46 assertions, compiles `geometry_showcase.cpp` directly, validates every mode × primitive/query/tree/SDF permutation emits non-empty into TLSF RenderBuffer + line_width threads + SandboxScene enum stability static_asserts). Verification: `full-sweep.ps1` 16/17 PASS — the one failure was a transient MSVC C1001 internal compiler error inside `link!DllGetObjHandler` during `crd-sandbox.exe` LTCG codegen on `win-shipping` pointing at `engine/config/src/config.cpp(245)` (unchanged file, plain `Config::load_from_file`); standalone retry linked clean with no source change; matching `win-clang-cl-shipping` (same flags, different compiler) was green. Slice closes per `feedback_transient_msvc_ltcg_ice_accept.md`. The ICE is filed as new tracked debt under `docs/debt.md::Transient MSVC LTCG ICE on win-shipping crd-sandbox.exe link` — recurrence promotes to actionable upstream-workaround (likely `CRD_NOINLINE` on a v1-cluster header function). Win-debug ctest 1308/1308 (was 1297). Session log `docs/sessions/2026-05-13-v1-debts-paid.md`. **Next phase = v2 (`-convex`) — scope expanded 2026-05-13 from 6 → 11 slices for elite-tier completeness.** Three substrate API decisions LOCKED 2026-05-13 (ADR-0076 §4 pin #14 added): (1) **C++20 `ConvexShape` concept + ADL `support()` free-fn overloads** — compile-time overload-polymorphic, matches §16 pin #1; (2) **Local-frame shapes + two explicit transforms** in `gjk_distance(a, xform_a, b, xform_b)` — driver works in shape A's local frame (~40% faster than world-frame for OBB pairs), support functions take dir in shape's local frame and are pure axis-aligned math (~6–10 ops each, inline-perfect); (3) **`SupportPoint{Vec3f point, u32 vertex_idx}`** with `k_invalid_vertex` sentinel for analytic shapes and lowest-vertex_idx tiebreak on coincident extrema for hulls — makes GJK + EPA cross-platform bit-exact (no shipped physics engine offers this as a contract). 11 slices: **v2a** GJK distance kernel + the 3 locked decisions + Sphere/OBB/Capsule/Hull support overloads + Ericson Voronoi-region simplex → **v2b** GJK boolean fast-out → **v2c** EPA penetration (carries witness_idx for deterministic polytope tiebreaks per pin #14) → **v2d** SAT box-pair fast path (~2–3× faster than GJK+EPA for boxes) → **v2e** `ConvexHullView` queries (ray-vs-hull / closest_point / contains) → **v2f** GJK-based convex shapecast (Catto 2013 secant-root TOI; the moving-convex shapecast v1i-b deferred to) → **v2g** hill-climbing hull support with cached vertex (PhysX/Havok pattern, 3–5× win for N>32 hulls — adds adjacency to ConvexHullView) → **v2h** Vec4f/Vec8f SIMD-batched hull support (~6× per-vertex on AVX2 for N≤32 hulls; dispatch with v2g warm-start) → **v2i** f64 GJK instantiation + aerospace orbital corpus (10⁷ m scales; falls out of templates for free) → **v2j** Sutherland-Hodgman convex-polygon clipping + face/edge feature enumeration (MIGRATED from v6 — fundamentally a convex-polygon algorithm; gates eylem v1d-manifold) → **v2-close** tiebreak conformance + degenerate corpus + perf benchmarks + full 17-config sweep. New module `engine/geometry-convex/`. ~3700 LOC engine + ~3200 tests / ~3.5–4 weeks. Performance budgets: GJK 50–200 ns/pair, hull-vs-hull warm-start <30 ns on N≤32, SAT box-pair <40 ns. **v6 amended:** loses Sutherland-Hodgman to v2j → becomes v6a–v6d (ear-clipping + CDT + Vatti + Bentley-Ottmann, ~2700 LOC). **Eylem v1d expanded:** new sub-slice **v1d-manifold** between v1d-callback-c and v1d-mesh — PhysX/Jolt-grade 4-point contact manifold generator (reference/incident face identification via v2j feature enumeration + Sutherland-Hodgman face clipping via v2j + Catto manifold-reduction to ≤4 points + edge-vs-edge degeneracy). Stacking-stability gate for v1e (10-box vertical stack at rest within 30 substeps). v1d itself slimmed from "GJK + EPA + SAT + Sutherland-Hodgman manifold reduction" to "dispatch + single-point contact" (~700 LOC, down from ~900); manifold reduction is v1d-manifold's job. **Phase 3.1.7 total now 42 slices / ~20.0 KLOC engine + ~5 KLOC editor + ~4 KLOC GLSL/HLSL / ~7–9 months calendar** (was 33 slices). The `v1` range is now 14 slices after the v1i split (v1a✅ → v1b✅ refit → v1c✅ DynamicBvh → v1d✅ Bvh4Tree → v1e✅ closest-point → v1f✅ Embree benchmark + jobs-parallel build → v1g✅ BVH4-recommended + `Vec4f` SIMD ray-vs-4-AABB → v1h✅ primitives-substrate hardening → v1i-a✅ unified query facade + templated result types + missing closest_point variants → v1i-b✅ shapecast (closed-form TOI) → v1i-c✅ broadphase pairs + validation + `crd-no-non-ascii-test-names` guard fix → **v1j `crd-geometry-viz` companion module** [debug-draw: primitives, ray hits + normals, closest points, BVH nodes by depth, frustum-cull, overlap pairs — depends `crd-draw`; `crd-geometry` itself never does]) — the **ADR-0076 §15 checklist-driven additions 2026-05-13** (also: `-convex` v2 lands `ConvexHullView` queries + GJK convex shapecast; `-spatial` v5 adds a dense `UniformGrid` + extends the query facade; `-shader-helpers` v9e emits GLSL twins of `signed_distance.hpp`; SAP stays reserved). **Architecture pins (ADR-0076 §16):** query API is compile-time overload-polymorphic not virtual; `RayHit` carries a templated payload not a fat union; NaN/Inf — queries tolerate (silently never-hit), builders reject in debug; epsilon policy in one place named by intent; viz is a companion module. Geometry sub-modules take `IAllocator*` (own allocators / best-available — transient build scratch could later use a scoped/linear allocator; refit + dynamic-tree ops + bvh4 collapse allocate from the supplied allocator). Phase 3.1 v1c is gated on Phase 3.1.7 close. The `v1` range is now 14 slices after the v1i split (v1a✅ → v1b✅ refit → v1c✅ DynamicBvh → v1d✅ Bvh4Tree → v1e✅ closest-point → v1f✅ Embree benchmark + jobs-parallel build → v1g✅ BVH4-recommended + `Vec4f` SIMD ray-vs-4-AABB → v1h✅ primitives-substrate hardening → v1i-a✅ unified query facade + templated result types + missing closest_point variants → v1i-b✅ shapecast (closed-form TOI) → v1i-c✅ broadphase pairs + validation + `crd-no-non-ascii-test-names` guard fix → **v1j `crd-geometry-viz` companion module** [debug-draw: primitives, ray hits + normals, closest points, BVH nodes by depth, frustum-cull, overlap pairs — depends `crd-draw`; `crd-geometry` itself never does]) — the **ADR-0076 §15 checklist-driven additions 2026-05-13** (also: `-convex` v2 lands `ConvexHullView` queries + GJK convex shapecast; `-spatial` v5 adds a dense `UniformGrid` + extends the query facade; `-shader-helpers` v9e emits GLSL twins of `signed_distance.hpp`; SAP stays reserved). (Earlier total snapshot: 30→33 slices, ~16.6→~18.5 KLOC, ~5–7→~6–8 months — superseded by the v2-expansion total above.) **Architecture pins (ADR-0076 §16):** query API is compile-time overload-polymorphic not virtual; `RayHit` carries a templated payload not a fat union; NaN/Inf — queries tolerate (silently never-hit), builders reject in debug; epsilon policy in one place named by intent; viz is a companion module. Geometry sub-modules take `IAllocator*` (own allocators / best-available — transient build scratch could later use a scoped/linear allocator; refit + dynamic-tree ops + bvh4 collapse allocate from the supplied allocator). Phase 3.1 v1c is gated on Phase 3.1.7 close.

**Phase 3.0 — Scene/ECS foundation. ✅ CLOSED 2026-05-10.** All 17 slices shipped (v1a → v1p). The full 8-layer slot-shaped ECS substrate is in place: SlotMap entities + Archetype/SparseSet hybrid storage + 6 built-in relations + Query DSL + 7-phase Schedule + Commands + IComponentIndex framework (+ ChangeDetect + AsyncAware live + 5 reserved shells) + Transform propagation + SceneResource cooker + Öbek system (12 sub-slices) + Preset+Profile (6 sub-slices) + async GPU upload + sandbox integration (3 sub-slices) + reserved-slot + API surface freeze. **All 13 ADRs (0049–0061) realised.** 856 unit tests across 12 build configs.

**Active phase:** **Phase 3.1 — Eylem (Cerid-native physics)** is **v1b in flight (sweep running 2026-05-11)**. **⏸ PIVOT 2026-05-11 (ADR-0076 §12 amendment):** Phase 3.1 PAUSES at v1b close. **Phase 3.1.7 `crd-geometry` (full 29-slice phase, ~5–7 months) executes BEFORE eylem v1c.** Eylem v1c onward then consumes `crd-geometry-bvh`/`-convex`/`-mesh` from day 1 — no deferred-refactor debt, no throwaway BVH/GJK/EPA/winding-number code. Eylem architecture is unchanged (ADR-0062 + ADR-0063); only the execution order changes. ~30 slices over v0–v9: v0 ✅ → v1 (v1a ✅, v1b 🚧) → **[pause for 3.1.7]** → v1c (broadphase, now consuming geometry) → v1d → ... → v2 (rigid 2D) → [3.1.5 sdf interleaves, also consumes geometry from day 1] → v3 (XPBD) → v4 → ... → v9 (differentiable + 9-config replay-hash CI). Plan: `docs/phases/phase-3.1-eylem.md` (pause-marker between v1b and v1c). Research: `docs/research/cerid-eylem.md`.

**Next-active phase: Phase 3.1.7 `crd-geometry`.** Kicks off the moment v1b sweep returns PASS. 11 sub-modules / 29 slices / ~15.8 KLOC engine + ~5 KLOC editor + ~4 KLOC cooker-emitted GLSL/HLSL. ADR-0076 §12 amendment (2026-05-11) pivoted the slot from "after Phase 3.1.6 hesap" to "before Phase 3.1 v1c"; full slice list ships, no narrow subset. See "Active phase" above for rationale; phase plan: `docs/phases/phase-3.1.7-geometry.md`.

**Phase 3.1.5 — `crd-sdf` substrate (interleaved between eylem v2 and v3).** Standalone signed-distance-field module consumed by eylem (mesh colliders + closest-point), font (MTSDF, Phase 3.3), renderer (DFAO/DFGI, Phase 3.5+), audio (acoustic occlusion, Phase 3.4), editor (CSG modelling, Phase 7). Locked architecture: ADR-0064; inherits ADR-0063 determinism contract wholesale. 8 slices over ~5–6 wk: v0 analytic primitives → v1 dense 3D grid + CRDR `'SDFR'` → v2 mesh-bake (Jacobson 2013 generalised winding number + BVH closest-point, parallel via `crd-jobs`) → v3 narrow-band sparse → v4 CSG + Quílez smooth-min → v5 GPU 3D-texture upload (reuses ADR-0061 `UploadHandle`/`Fence`) + `crd_sdf.glsl` shader helper → v6 cooker (`.sdf.toml` + `bake_sdf` mesh opt-in) → v7 Marching Cubes extraction → v8 reserved (GPU baker / VDB sparse / Dual Contouring). Plan: `docs/phases/phase-3.1.5-sdf.md`. Research: `docs/research/cerid-sdf.md`.

**Phase 3.1.7 — `crd-geometry` computational-geometry substrate (ADR-0076 ✅ Accepted 2026-05-11 + Amended 2026-05-11 §12 — sequence pivot to execute BEFORE Phase 3.1 v1c, NOT after Phase 3.1.6 hesap; supplement integrated 2026-05-11).** **NOW NEXT-ACTIVE — kicks off the moment v1b sweep returns PASS.** Standalone peer module to crd-math/crd-sdf/crd-hesap. **11 sub-modules** (10 base + 1 from supplement): `-primitives` + `-bvh` (binned-SAH + Catto 2019 refit + quad-BVH) + `-convex` (GJK+EPA+SAT+Quickhull) + `-mesh` (closest-point + Möller-Trumbore raycast + Jacobson 2013 winding number) + `-spatial` (KD-tree+octree+R-tree+spatial hash) + `-polygon` (Vatti+CDT+Sutherland-Hodgman+Bentley-Ottmann) + `-mesh-processing` (QEM+Loop subdivision+remesh+repair+Taubin) + `-delaunay` (2D+3D Bowyer-Watson+Voronoi) + `-gpu` (LBVH per Karras 2012) + `-decomposition` (V-HACD per Mamou 2014, editor-tier) + **`-shader-helpers`** (cooker-emitted GLSL/HLSL primitive library mirroring iq's analytic distance functions + smin/domain operators; ULP-conformance-tested against C++ scalar reference; consumed by crd-sdf, renderer Phase 3.5+ DFAO/DF-soft-shadow, font MTSDF, editor preview). Multi-domain consumers (12 named): eylem broadphase v1c + narrow v1d + mesh-collider v1d-mesh + convex-conditioning; crd-sdf v2 mesh-bake; renderer Phase 3.5+ frustum cull + occlusion BVH; crd-scene `SpatialBVHIndex` (ADR-0053 reserved shell — lights up at v5e); audio Phase 3.4 raycasts; crd-eylem-aero/cine; navmesh Phase 7; editor V-HACD pipeline. Inherits ADR-0063 determinism contract (algorithm-specific tiebreaks pinned: GJK simplex Ericson-not-vandenBergen; SAH-split X-then-Y-then-Z; Quickhull lex order; Vatti Clipper2 vertex-on-edge). Two-layer API mirrors crd-hesap: typed C++ Eigen-class engine layer + opt-in cooker/editor façade. **Deferred-refactor pattern OBSOLETED by §12 amendment (2026-05-11)** — eylem v1c/v1d/v1d-mesh + sdf v2 all consume `crd-geometry` from day 1, no ships-own narrow versions. (eylem v7 FEM still ships own narrow PCG → consumes `crd-hesap-iterative` later — that deferred-refactor stays intact; only the geometry one is dissolved.) **29 slices** (25 base + v0e iq-formulary substrate + v1g BVH4 SIMD ray-vs-4-AABB + v4g per-leaf SIMD Möller-Trumbore + v9e GLSL/HLSL output) / ~15.8 KLOC engine + ~5 KLOC editor + ~4 KLOC cooker-emitted GLSL/HLSL / ~5–7 months calendar. **Slot (post-§12 amendment 2026-05-11):** between Phase 3.1 v1b close and Phase 3.1 v1c. Originally slotted after Phase 3.1.6 hesap per the substrate-tier-sequencing rationale (physics → implicit geometry → numerics → explicit geometry); §12 observes that eylem's broadphase + narrow-phase are explicit-geometry consumers from day 1, so the cleaner ordering is physics-interface → explicit geometry → physics-runtime → implicit geometry → numerics (eylem v1a interface ✅, eylem v1b storage ✅, **geometry next**, eylem v1c+ runtime, sdf, hesap). **Open question (deferred):** v1+ may upgrade fixed-precision predicates to Shewchuk 1997 adaptive precision conditioned on crd-sdf v2 mesh-bake stress-test data. Research dossier: `docs/research/cerid-geometry.md` (11,523 words; base) + `docs/research/cerid-geometry-supplement.md` (6,116 words; supplement covering iq's analytic-distance-function library + computational-geometry textbook foundation (Ericson, Eberly, de Berg, O'Rourke, Edelsbrunner, Botsch, Foley, Akenine-Möller) + explicit per-sub-module SIMD strategy with Vec4f/Vec8f lane-width choices + crd::containers integration patterns + per-sub-module performance budgets in ns/ms with Embree/Box2D reference targets + the new `-shader-helpers` sub-module + SIMD-specific determinism pins). Phase plan: `docs/phases/phase-3.1.7-geometry.md` (now includes Performance budgets table + Reference reading section). ADR: `docs/decisions/0076-geometry-substrate-architecture.md` (extended 2026-05-11 with §1 11th sub-module + §4 SIMD determinism pins #7-#11 + §5 SIMD/containers substrate commitment bullet + §7 four new slices v0e/v1g/v4g/v9e + §10 shader-helper-ownership open question deferred to Phase 3.1.5 close).

**Phase 3.1.6 — `crd-hesap` numerical computing substrate (sequential successor to Phase 3.1).** MATLAB-class numerical computing — every scientific-computing primitive Cerid's multi-domain mandate needs (games + robotics + medical + cinematic + DAW + scientific tool). Locked by ADR-0065 (architecture) inheriting ADR-0063 determinism contract. 18 slices over ~6–8 months across 14 sub-modules: dense (BLAS L1/L2/L3 + LAPACK-class direct + SVD + eig), sparse (CSR/CSC/BSR/COO/ELL/HYB + spmv/spmm/spgemm), iterative (CG/PCG/BiCGSTAB/GMRES/MINRES/LSQR/IDR + Jacobi/IC/ILU/AMG preconditioners), direct (sparse LU/Cholesky/QR multifrontal + AMD/RCM/nested-dissection reorderings), eig (Lanczos/Arnoldi/IRA/LOBPCG), opt (L-BFGS/SQP/IPOPT-class NLP/OSQP-style QP/simplex+IPM LP), ode (DOPRI5/8 + BDF + Rosenbrock + Pantelides DAE), fft (Cooley-Tukey + Bluestein + DCT/DST/Hartley), dsp (FIR/IIR + biquad + polyphase resample + Welch spectral), stats (20+ distributions + tests + special functions + splittable PCG + Xoshiro256\*\*), tensor (N-dim + broadcasting + einsum), autodiff (forward dual/Jet + reverse tape over BLAS), gpu (mirrors CPU API via Vulkan compute + reuses ADR-0061 UploadHandle/Fence), repl (interactive + `.cnb` notebook + plug-in C ABI per ADR-0034). Two-layer API: Eigen-class typed C++ for engine code + opt-in MATLAB-class facade for the REPL/scripting layer. Eylem v7 (FEM) ships its own narrow internal PCG until `crd-hesap` arrives, then refactors to use it; eylem v9 differentiable refactors to use `crd-hesap-autodiff`. Underwrites the MATLAB-class scientific tool ambition. Plan: `docs/phases/phase-3.1.6-hesap.md`. Research: `docs/research/cerid-hesap.md` (industry survey: LAPACK/BLAS/BLIS, SuiteSparse, PETSc, Trilinos, ARPACK, IPOPT, OSQP, SUNDIALS, FFTW, Stan, JAX, MATLAB, NumPy/SciPy, Julia, Eigen). Sits in `crd-hesap` peer module, NOT bloating `crd-math` (which stays lean).

**Next slice (post-pivot 2026-05-11):** **Phase 3.1.7 v0a — `crd-geometry-primitives` substrate** (the moment v1b sweep returns PASS). Phase 3.1 v1c is gated on Phase 3.1.7 close. Originally: **Phase 3.1 v0 substrate fully closed 2026-05-10** (verified post-mortem same evening — see post-mortem session log): v0a SIMD wrappers + v0b AoSoA Soa container + v0c `crd::math::deterministic` (Cephes f32+f64 trig/exp/log/special functions + Vec4f/Vec8f branchless SIMD; v0c-debt-A 5 debt items paid same-day) + v0d `crd::containers` deterministic sort + heap + v0e SIMD benchmark harness. **Four CI guards live** (`crd-simd-emission-check`, `crd-no-std-math-check`, `crd-no-std-sort-check`, `crd-no-non-ascii-test-names`). One-command DoD sweep: `scripts/full-sweep.ps1`. ~3500 LOC substrate, ~970 ctest entries (972 in win-debug per Catch2 discovery), 14 build steps clean (Win × 8 + Linux × 6).

The architecture is the **8-layer slot-shaped ECS** designed for million-entity scenes, agents-as-components-with-scripts, UI on the same machinery (game and editor), with every novel ECS extension as a registered slot:

```
L8 Reflection / Editor          (Phase 7)        — API reserved
L7 Scripting & Behaviors        (Phase 4.0+)     — API reserved
L6 Replication / Networking     (Phase 4.2)      — API reserved
L5 Indexes                      (Phase 3.0+)
   ChangeDetect, AsyncAware                       — live in 3.0
   History, SpatialBVH, GpuResident               — API only in 3.0
L4 Query · System · Schedule    (Phase 3.0)     — live
L3 Relations                    (Phase 3.0)     — live (6 built-ins)
L2 Storage backends             (Phase 3.0)     — live (Archetype + SparseSet hybrid)
L1 Entity / SlotMap             (Phase 3.0)     — live
L0 Memory · Containers · Jobs   (already shipped)
```

Cerid signature: a uniform `IComponentIndex` extension framework where every novel ECS extension (history, change detect, spatial, GPU-mirror, async, replication, scripts, reflection) is a registered slot consuming the same component-lifecycle event stream. Adding the next extension is a one-day job.

Active phase doc: `docs/phases/phase-3.1-eylem.md`. Phase 3.0 closed: `docs/phases/phase-3.0-scene-ecs.md`.

## Coming up next — Phase 3.1 v1a ✅ → **v1a-material a/b/c/d ✅** → v1a-sandbox-smoke ✅ → v1a-draw-d0..d4 ✅ → v1b-a/b/c/d ✅ → v1b-e 🚧 (sandbox demo + render interpolation; sweep re-running 2026-05-11 after fixing a pre-existing dangling-`ConstSpan` bug in `VulkanDescriptorSetLayout`/`VulkanPipelineLayout` that ASan surfaced on this sweep — the layout impls stored desc-by-value but `DescriptorSetLayoutDesc::bindings` is a non-owning span; callers construct the desc with a stack-local binding, then the layout outlives the stack. Fix: layouts now deep-copy span contents into owned `Array` storage and re-point the stored desc at the owned memory. Win-asan ctest + sandbox-smoke now PASS clean.) → **⏸ PIVOT 2026-05-11 (ADR-0076 §12 amendment):** **Phase 3.1.7 `crd-geometry` executes BEFORE Phase 3.1 v1c**, full 29 slices / ~5–7 months. Eylem v1c/v1d/v1d-mesh + sdf v2 consume `crd-geometry` from day 1 — no deferred-refactor debt. → Phase 3.1 v1c (broadphase, consuming `crd-geometry-bvh`) → v1c-sensor → v1d (narrow phase, consuming `crd-geometry-convex`) → v1d-filter-{a,b,c} → v1d-callback-{a,b,c} → v1d-mesh (consuming `crd-geometry-mesh`) → v1d-hf → v1e (SI solver) + v1e-material → v1f (joints) + v1f-articulation-filter + v1f-fields-{a..i,j} → v1g (islands) + v1g-contactmodify → v1h (scene queries) → v1i (character controller) → v1j (snapshot/replay) → v1k (sandbox) + v1k-material-{cooker,bench} → v1l (close) + v1l-test-{conservation,closedform,stress} → Phase 3.1.5 (crd-sdf, interleaved between eylem v2 and v3; consumes `crd-geometry-mesh` directly) → v1c (broadphase) + **v1c-sensor** (✅) → v1d (narrow phase: GJK+EPA+ConvexHull+Plane) + **v1d-filter-a/b/c** (5-tier collision filter, ADR-0068) + **v1d-callback-a/b/c** (deferred ECS event streams, ADR-0068) → **v1d-mesh** (TriangleMesh BVH) → **v1d-hf** (Heightfield) → v1e (SI solver) + **v1e-material** (Coulomb friction integration, ADR-0069) → v1f (joints) + **v1f-articulation-filter** (Tier 5 auto-filter, ADR-0068) → **v1f-fields-a..i** (force-field substrate, ADR-0067) + **v1f-fields-j** (J2 oblateness, ADR-0067 + ADR-0073 Planned) → v1g (islands) + **v1g-contactmodify** (pure-function ContactModify hook, ADR-0068) → v1h (scene queries) → v1i (character controller) → v1j (snapshot/replay) → v1k (sandbox) + **v1k-material-cooker + v1k-material-bench** (ADR-0069 §10 + §12) → **v1l (close) + v1l-test-conservation + v1l-test-closedform + v1l-test-stress** (ADR-0075) → Phase 3.1.5 (crd-sdf substrate; ColliderShape::Sdf consumer ships here)

**Five-category collider model locked 2026-05-11 (ADR-0062 §4.5):** Sphere/Box/Capsule (analytic) + ConvexHull (GJK+EPA) + Plane (analytic) + TriangleMesh (BVH+per-tri SAT) + Heightfield (per-cell analytic) + Sdf (closest-point + gradient on baked SDF, via crd-sdf). Best physics engine ever doesn't have arbitrary "not supported" answers; each shape category is best at a different problem (SDF wastes memory for terrain; heightfield wastes precision for smooth meshes; etc.). ColliderId encoding bumped to `[gen:8 | kind:4 | per_kind_idx:20]` — 16 shape kinds reservable, 1M colliders/kind.

**Force-field substrate locked 2026-05-11 (ADR-0067):** Three-tier model — analytic primitives (Directional / Radial / Vortex / Drag / Noise / Magnetic) + Gradient (consumes crd-sdf) + GridSample (Tier 2 vector grids) + Script (Tier 3, Phase 4). Reuses 5-category collider shapes for field volumes. Content-addressed FieldId for determinism. Composition modes (Add / Replace / Multiply / Max / Min) with id-stable application order. Force-field arrows visible via crd-draw VisualizerRegistry. Öbek-serializable. 9 sub-slices v1f-fields-a..i between v1f (joints) and v1g (islands), ~1500 LOC + ~30 tests + 1 bench. Two slices (Gradient, Script) wait on dependencies (3.1.5 SDF, Phase 4 scripting); API surface (force_field.hpp) ✅ shipped today, locks at v1l. Research dossier: docs/research/cerid-eylem-fields.md.

**Body types + collision filtering + callbacks locked 2026-05-11 (ADR-0068):** 3 motion types Static/Kinematic/Dynamic (no fourth — Maya "Animated Rigid Body" + MuJoCo mocap fold into Kinematic-with-velocity-inference); sensor as per-collider flag (8/9 engines — Jolt is documented exception); specialised actors via ECS composition (CharacterController/VehicleBody/ArticulationLink/SoftBody/GpuParticle/GeometryCollection atop motion types); 5-tier filtering pipeline = bit-mask layers (64-bit) + Box2D group index + explicit excluded pairs (URDF) + ECS predicate (closed read set, API-enforced) + articulation auto-filter; deferred ECS event-stream callbacks sorted by (min,max,kind) with Persist/Stay opt-in per pair (destruction storm avoidance); ContactModify as separate v1g+ pure-function API. Industry survey 10,370 words across 14 engines covering games + robotics + aerospace + cinematic + scientific computing. Surface (collision_filter.hpp + ColliderFlags + IPhysicsScene extensions + NullPhysicsScene null-impl) ✅ shipped today, locks at v1l. 9 sub-slices v1c-sensor ✅ + v1d-filter-{a,b,c} + v1d-callback-{a,b,c} + v1f-articulation-filter + v1g-contactmodify, ~1500 LOC + ~33 tests + 2 benches. Research dossier: docs/research/cerid-eylem-collision-filtering.md.

**Substrate solidification decisions locked 2026-05-10 (ADR-0066 §19, 7 decisions):**
1. DepthMode → 6-pipeline matrix `(line,triangle) × (Test,Always,GreaterDimmed)`; XRay emits twice
2. VisualizerRegistry → typed function-pointer + category-ordered iteration
3. Current-buffer → explicit canonical `*_to(buf,...)` + thread-local convenience wrappers `line()`/etc.
4. Buffer overflow → multi-batch submit (no truncation, no auto-grow)
5. MSAA tiers → 5 levels (Off/Low/Med/High/Ultra) gated via `QualityPreset::debug_overlay_aa` (schema v1→v2) [d2-aa execution deferred 2026-05-10 until a real MSAA consumer exists; design unchanged]
6. **API surface freeze at d4 close** — public `crd::draw::*` locked forever after
7. Replay capture → API surface stub in d4, real impl deferred to Phase 7 editor

Phase 3.1 v1 grew from 12 → 18 → **~50 sub-slices** as substrate decisions locked across the 5-domain mandate. The arc:

1. `crd-draw` substrate slipped between v1a (interface) and v1b (storage) so v1c+ are visually debuggable from day 1 (ADR-0066, +6 sub-slices).
2. Five-category collider model locked (sphere/box/capsule/hull/plane + TriangleMesh + Heightfield + Sdf — ADR-0062 §4.5, +2 v1d sub-slices).
3. Force-field substrate locked (ADR-0067, +9 v1f sub-slices a..i).
4. Body types + collision filtering + callbacks locked (ADR-0068, +9 sub-slices across v1c/v1d/v1f/v1g).
5. Coverage audit + Wave 1/2/3 fills (ADR-0069 materials + ADR-0075 testing rigor accepted; ADR-0070/0071/0072/0073/0074 reserved as Planned, +17 sub-slices across v1a/v1f/v1l/v4d/v6c/v6d/v6e/v6f/v9b).

Phase 3.1 v1 is now ~9–14 weeks of substantive substrate work; the broader Phase 3.1 plan (v0–v9) carries ~50 sub-slices. Each addition was driven by an industry-survey research dossier + ADR — the multi-domain mandate (games + scientific computing + robotics + aerospace + cinematic) requires breadth no shipped engine matches; the slice growth is the cost of that bar.

Doc index: ADR-0066 (`crd-draw`); ADR-0067 (fields); ADR-0068 (bodies+filter+callbacks); ADR-0069 (materials); ADR-0075 (testing rigor); ADR-0070/0071/0072/0073/0074 reserved Planned for Wave 2/3. Research dossiers live under `docs/research/cerid-eylem-*.md`. Phase plan: `docs/phases/phase-3.1-eylem.md`.

**v1a (~600 LOC, ~15 tests):** `crd-eylem` interface module (`IPhysicsScene`, `RigidBody`, `Collider`, `Material`, `Joint`, `PhysicsConfig`). Pure interface declarations + null impl that links. ADR-0063 CI guards (`crd-no-std-math-check`, `crd-no-std-sort-check`, `crd-simd-emission-check`, `crd-no-non-ascii-test-names`) auto-fire when `engine/eylem/` directory appears.

**v1a-sandbox-smoke (~150 LOC):** `crd-sandbox --smoke-test [duration_seconds]` flag (boots normally, runs main loop N seconds, exits 0 on clean shutdown) + `scripts/full-sweep.ps1` extension to call it after each per-config ctest on the 6 Win configs. Catches Vulkan validation + resource init order + profile/preset apply-cycle bugs the build doesn't.

**v1a-draw — `crd-draw` substrate (5 sub-slices, ~7-9 days, ~2500 LOC, ~30 tests).** Peer module per ADR-0066. Two-layer API: bottom = retained `RenderBuffer` (PhysX-style SoA, packed RGBA8 color, PrimFlags u32 with 3 depth modes + 7 categories + 16-bit picking_id reserved); top = immediate-mode free functions (`draw::line / box_wire / box_solid / sphere_wire / sphere_solid / capsule_wire / capsule_solid / aabb / arrow / axis_triad / arc / cross_3d / grid / frustum / text`). Vertex-shader quad-expanded AA lines (Three.js / Mapbox technique) + sort-by-centroid translucent solids. Per-shape unit primitives baked at compile time (UV sphere wireframe + icosphere solid). RGB triad joint convention (RViz). Master scale (PhysX `eSCALE`). Lifetime decay (Unreal). Per-component visualizer plug-in registry + `crd-eylem-viz` companion module (dependency-inverted: substrate modules emit pure data, companion modules bridge to renderer). GLSL shaders cooked to SPIR-V via existing `crd-shader` pipeline (no embedded byte arrays — single-path). Helper-function `add_draw_overlay_pass(fg, color, depth, buffer)` grafts onto any IRenderPath. Profile-gated (dev = ON, shipping = early-out). Replay-friendly retained buffer serializes for Phase 7 out-of-process viewer.

**Sub-slices:** d0 module skeleton + line + box wireframe + RenderBuffer + overlay pass (~3 days) → d1 solid pipeline + per-shape primitives (~2 days) → d2 full immediate-mode API surface (~1 day) → d3 DebugVizSystem + Component + VisualizerRegistry + crd-eylem-viz (~2 days) → d4 ImGui control panel + sandbox 1k-box + ragdoll demo (~1 day).

**v1b (~400 LOC, ~10 tests):** Body / shape AoSoA-8 storage in `crd-eylem-rigid3d`. **First real consumer of v0 SIMD substrate + first eylem entity visible in the debug renderer.**

**Substrate inventory + handoff:** `docs/sessions/2026-05-10-v0-substrate-closure.md`. Full v1 plan: `docs/phases/phase-3.1-eylem.md`.

## Active detour

_none — D-001 closed 2026-05-07; main roadmap resumed at v1d, subsequently shipped through v1m on 2026-05-08._

> When a detour opens, this section names it (e.g. "D-001: investigate shader-cache corruption") and the main roadmap pauses until it closes. Detour file: `docs/detours/D-NNN-<slug>.md`. Queue rules: `docs/detours/README.md`.

## Last shipped milestone

**2026-05-17 — Phase 3.1.7 v7 `-mesh-processing` cluster ✅ CLOSED — 18-config full sweep PASS (`scripts/full-sweep.ps1`, elapsed 15:17): 11 Windows (debug + relwithdebinfo + release + asan + clang-cl + debug-scalar + debug-sse2 + shipping + shipping-profile + clang-cl-shipping + tidy) + 7 Linux (gcc-debug + gcc-relwithdebinfo + gcc-release + gcc-asan + gcc-debug-scalar + gcc-debug-sse2 + gcc-shipping).** **Phase 3.1.7 sub-module 8 of 11 COMPLETE** (`crd-geometry-mesh-processing`). 8 algorithm slices + 1 substrate slice + 1 close: v7a HalfEdgeMesh + v7b QEM (Garland-Heckbert 1997) + v7c Loop (1987) + v7d Isotropic remeshing (Botsch-Kobbelt 2004) + v7e Liepa hole filling §3+§4+§5 (2003) + v7f manifoldness repair + v7g self-intersection removal (Möller 1997 + per-tri v6c CDT) + v7h Taubin smoothing (1995) + v7-close. **Cluster totals**: ~4200 LOC engine + ~2830 LOC tests / 80 cases / 1180 assertions / **ADR-0076 §22 amendment locks all 72 design decisions D1-D72** / new `docs/systems/geometry-mesh-processing.md` system overview. **Sweep history**: 2 attempts before PASS. First wider-corpus sweep caught issues the per-slice 4-config DoD (MSVC-only) missed: gcc `-Wfloat-conversion -Werror` on `T{double_literal}` patterns in 8 hpp/cpp sites (`T{1e-10}`, `T{1e-20}`, `T{-0.53}`, `T{1.41421356}`, `T{kPi}`, `T{kTwoPi}`) + `-Wcomment -Werror` on a `loop_subdivide.hpp` ASCII-art diagram with trailing-`\` line + clang-cl `-Wunused-but-set-variable -Werror` on `orig_v` in `repair_manifoldness.cpp`. All fixed via `static_cast<T>(literal)` (avoids narrowing-warning class) + removing the ASCII art + removing unused var. Plus 2 transient compiler crashes (MSVC LTCG ICE on `<sstream>` + clang-tidy access violation) auto-cleared on retry per `feedback_transient_msvc_ltcg_ice_accept.md` + `feedback_transient_clang_tidy_crash.md` policies. **Carryover lesson**: gcc-linux is part of the wider sweep; `T{double}` narrowing is silent on MSVC per-slice DoD; **use `static_cast<T>(literal)` for non-exact-representable defaults + constexpr in `<MathScalar T>` template code from now on**. Session log `docs/sessions/2026-05-17-geometry-v7-close.md`. **🎯 Phase 3.1.7 progress: 8 of 11 sub-modules COMPLETE** (primitives ✅ + bvh ✅ + convex ✅ + v3 hull-extension ✅ + mesh ✅ + spatial ✅ + polygon ✅ + **mesh-processing ✅**). 59 of renewed-scope 49 slices shipped (120%; nothing cut). **🎯 Next sub-module = v8 `-delaunay`** (2D + 3D Bowyer-Watson + Voronoi-from-Delaunay + mandatory `insphere_exact` Stage D paydown at v8c-pre).

### Earlier milestone

**2026-05-17 — Phase 3.1.7 v7h Taubin smoothing (Taubin 1995) ✅ SHIPPED — 4-config DoD PASS first try (`scripts/per-slice-check.ps1`, elapsed 03:41): win-debug + win-asan + win-shipping + win-tidy.** Volume-preserving Laplacian smoothing — the standard noise-removal primitive that DOESN'T melt the mesh down to its centroid like pure Laplacian. **Algorithm**: 2-pass alternation per iteration: (1) Pass 1 shrink with λ > 0 (`v ← v + λ·(mean(neighbours) - v)`), (2) Pass 2 un-shrink with μ < 0 (`v ← v + μ·(mean(neighbours) - v)`). Frequency-domain LOW-PASS: high frequencies (noise) damped, low frequencies (shape) preserved. Default (λ=0.5, μ=-0.53) targets Taubin's wide-pass-band filter (k_PB ≈ 0.113). New `taubin_smooth(const HalfEdgeMesh<T>&, opts) -> HalfEdgeMesh<T>` entry — input untouched. Boundary handling: `keep_boundary_fixed = true` default clamps boundary vertices; false smooths them using ONLY boundary 1-ring neighbours (matches v7c cubic-B-spline-mask intent with Taubin λ/μ alternation; D70). Boundary flag cached ONCE at entry since Taubin doesn't change topology (D68). Jacobi-style atomic update per pass via `set_vertex_position` (D69 — order-independent determinism). Uniform-weight umbrella operator `L(v) = mean(neighbours) - v` (D71 — Taubin's original form; cotangent weights ship as v7h-cotan followon if consumer requires). `InvalidParameters` rejects λ ≤ 0 and μ ≥ 0 but allows `|μ| ≤ λ` (high-pass / experimental filters; D72). Reports `Ok` / `EmptyMesh` / `NonManifoldInput` / `InvalidParameters` + telemetry (iterations_run, vertices_smoothed, boundary_vertices_clamped, output_vertices, output_faces). Two-layer typing per ADR-0078 §5 D34: raw `<MathScalar T>` body. Explicit instantiations for `f32` + `f64`. 8 Catch2 cases — empty / λ=0-InvalidParams / μ=0-InvalidParams / n_iterations=0-clone / **Taubin-drift-strictly-less-than-pure-Laplacian-drift on subdivided cube** (hallmark comparison) / open-quad-boundary-clamped / determinism / f64. Mesh-processing suite total **80 cases / 1180 assertions** (v7a 14 + v7b 15 + v7c 9 + v7d 9 + v7e 11 + v7f 9 + v7g 5 + v7h 8). **Mid-slice test fix**: initial volume-preservation assertion was too tight on a RAW 8-vertex cube (drift = 93% — Taubin's frequency analysis assumes smooth manifold, not sharp-corner cubes); revised to subdivide first via v7c Loop (2 levels → 98 vertices, smooth low-frequency cube shape) then assert Taubin drift STRICTLY < pure-Laplacian drift (the algorithm's hallmark comparison). Pinned design decisions D67-D72 carried for ADR-0076 §22 amendment at v7-close. Session log `docs/sessions/2026-05-17-geometry-v7h-taubin-smooth.md`. **🎯 Next = v7-close** (ADR-0076 §22 amendment locking D1-D72 + `docs/systems/geometry-mesh-processing.md` + 18-config full sweep + roadmap/context/MEMORY final sync — closes Phase 3.1.7 sub-module 8).

### Earlier milestone

**2026-05-17 — Phase 3.1.7 v7g self-intersection removal ✅ SHIPPED — 4-config DoD PASS on retry (`scripts/per-slice-check.ps1`, elapsed 03:16): win-debug + win-asan + win-shipping + win-tidy.** Cut triangle-triangle 3D self-intersections + retriangulate locally so the intersection segment becomes a clean mesh edge. Downstream consumers (procedural booleans, photogrammetry cleanup, animated-deformation post-pass) all require self-intersection-free input. **Algorithm 3 phases**: (1) **Broadphase**: brute-force O(n²) pair enumeration with AABB-overlap filter + share-vertex filter (adjacent triangles "touch" at shared edges, not self-intersections; D60 — phase-doc-mentioned BVH broadphase is a v7g-followon optimization since static BvhTree doesn't expose find_overlapping_pairs). (2) **Narrowphase**: Möller 1997 triangle-triangle intersection test gated by exact-sign `crd::geometry::primitives::orient3d` (Shewchuk adaptive, FP-roundoff-immune; D61). Touch-only cases (≥ 1 orient3d sign zero + the rest same sign, or any 2 zeros) explicitly rejected — vertex/edge of T₂ lying ON T₁'s plane is not a transversal cut. (3) **Per-triangle retriangulation**: project T's vertices + segment endpoints to 2D via drop-largest-normal-axis + flip-winding-if-normal-negative (D63 — maximally orthogonal projection, CCW preservation); epsilon-dedup at `opts.dedup_epsilon` default 1e-6 for 3+ triangle-common-edge case (D64); build CDT input (T boundary edges + intersection segments as constraints); call `crd::geometry::polygon::constrained_delaunay`; centroid-inside-T sanity filter on output sub-triangles (D66 — strip super-triangle artifacts); lift back to 3D via plane equation. **Cross-triangle vertex stitching** (D62): each intersection event creates 2 endpoint vertices ONCE and shares the global indices between BOTH T_i and T_j's segment lists → independent CDT calls reference same global vertices → output 2-manifold along the cut. **Graceful CDT-failure degradation** (D65): on `ConstraintsCrossing` (or other non-Ok), keep T's original tessellation and increment `triangles_skipped_cdt_failure`. New `remove_self_intersections(const HalfEdgeMesh<T>&, opts) -> HalfEdgeMesh<T>` entry — input untouched. Reports `Ok` / `EmptyMesh` / `NoSelfIntersections` + telemetry (candidate_pairs_tested, intersection_pairs_detected, intersection_vertices_added, triangles_retriangulated, triangles_skipped_cdt_failure). Module dep EXTENDED: `crd-geometry-mesh-processing` now PUBLIC-links `crd-geometry-polygon` (for v6c CDT). Two-layer typing per ADR-0078 §5 D34: raw `<MathScalar T>` body. Explicit instantiations for `f32` + `f64`. 5 Catch2 cases — empty / closed-cube-no-intersections / two-crossing-triangles (XY-plane × YZ-plane → 1 pair, 2 endpoints, retriangulated) / determinism / f64-cube. Mesh-processing suite total **72 cases / 1087 assertions** (v7a 14 + v7b 15 + v7c 9 + v7d 9 + v7e 11 + v7f 9 + v7g 5). **Mid-slice bug fix**: cube test initially reported false-positive intersections (1 pair detected on a closed convex cube). Root cause: axis-aligned faces produce many `orient3d == 0` cases (vertex-on-perpendicular-plane), and my naïve sign comparison in `compute_interval` mishandled zeros (treated 0 as "same side as negative"). Fix: explicit touch-only rejection in the orient3d gate. **First DoD attempt** failed at win-tidy with transient clang-tidy build error (upstream LLVM bug per `feedback_transient_clang_tidy_crash.md` policy); retry PASS. Pinned design decisions D60-D66 carried for ADR-0076 §22 amendment at v7-close. Session log `docs/sessions/2026-05-17-geometry-v7g-remove-self-intersections.md`. **🎯 Next = v7h Taubin smoothing** (volume-preserving Laplacian: 2-pass λ-shrink + μ-anti-shrink with μ ≈ -1.04·λ; removes noise without Gaussian "candy melt" drift).

### Earlier milestone

**2026-05-17 — Phase 3.1.7 v7f manifoldness repair ✅ SHIPPED — 4-config DoD PASS first try (`scripts/per-slice-check.ps1`, elapsed 03:45): win-debug + win-asan + win-shipping + win-tidy.** Detect + fix the two classical non-manifold pathologies: (a) **non-manifold edges** (>2 incident faces) — Phase A pairs incident triangles by orientation (forwards: origin < dest; backwards: origin > dest); first pair keeps original endpoints; subsequent pairs share a single duplicated `min(u,v)` vertex; singletons (orientation imbalance) get individual duplicates; index-level operation via `crd::containers::HashMap<u64, Array<u32>>` keyed on packed `(min(u,v), max(u,v))` u64 (D52, D53). (b) **bowtie vertices** (>1 disjoint fan) — Phase B builds temp HalfEdgeMesh, runs v7a CW fan-walk per vertex (D55); if walk_count != slot_count → bowtie; identifies fans via union-find on triangle-share-edge-through-v adjacency BFS (D56); lowest-min-triangle-id fan keeps `v`, each other fan gets duplicated vertex (D57). **Phase A → Phase B sequence** (D58) — Phase B requires manifold edges as precondition; Phase A's vertex duplication can CASCADE into a bowtie that Phase B then detects + repairs (tests verify this explicitly with phase-A-only count vs A+B count). New `repair_manifoldness(const HalfEdgeMesh<T>&, opts) -> HalfEdgeMesh<T>` entry — input untouched. Reports `Ok` / `EmptyMesh` / `AlreadyManifold` (D59) + telemetry (`non_manifold_edges_detected`, `non_manifold_edges_repaired`, `bowtie_vertices_detected`, `bowtie_vertices_repaired`, `duplicated_vertices_added`). Two-layer typing per ADR-0078 §5 D34: raw `<MathScalar T>` body. Explicit instantiations for `f32` + `f64`. 9 Catch2 cases: empty / closed-cube-AlreadyManifold / 3-tri-non-manifold-edge (verified Phase-A-only count = 1 + full cascade count = 2) / 4-tri-non-manifold-edge-paired (same cascade pattern) / 2-disjoint-tetra-fans-bowtie / phase-A-toggle-off / phase-B-toggle-off / determinism / f64. Mesh-processing suite total **67 cases / 1065 assertions** (v7a 14 + v7b 15 + v7c 9 + v7d 9 + v7e 11 + v7f 9). Pinned design decisions D52-D59 carried for ADR-0076 §22 amendment at v7-close. Session log `docs/sessions/2026-05-17-geometry-v7f-repair-manifoldness.md`. **🎯 Next = v7g self-intersection removal** (BVH broadphase + Möller 1997 triangle-triangle exact test + per-face CDT with intersection segments as constraints; consumes v6e Bentley-Ottmann).

### Earlier milestone

**2026-05-17 — Phase 3.1.7 v7e Liepa hole filling FULL PIPELINE (Liepa 2003 §3 DP + §4 Steiner refinement + §5 Laplacian fairing) ✅ SHIPPED — 4-config DoD PASS first try TWICE (`scripts/per-slice-check.ps1`, initial v7e §3-only elapsed 03:41 + same-session v7e-refine §3+§4+§5 elapsed 03:22): win-debug + win-asan + win-shipping + win-tidy.** Scanner-data cleanup primitive — photogrammetry + 3D-scanned meshes always have gaps; this is the elite-tier algorithm that closes them with density-matched patches (no naive single-triangle-spanning-the-hole results). New `fill_holes(const HalfEdgeMesh<T>&, opts) -> HalfEdgeMesh<T>` entry — input untouched. **Algorithm**: (1) boundary loop detection (walk all alive boundary HEs in slot order, group via `.next` traversal; visited bitmap prevents double-processing; D39); (2) per-loop outside-mesh face normal precomputation (for each loop edge, twin's face normal computed once before DP; D40); (3) **Liepa O(N³) DP min-weight triangulation** with weight `ω(T) = area(T) + λ · dihedral_penalty_sum_over_T's_edges` (D41) — dihedral on each edge resolved either against the precomputed outside-mesh normal (if loop boundary edge) or against the PHANTOM-EDGE TRIANGLE of the relevant sub-DP `T_{i, O[i,m], m}` (if interior patch edge); the phantom dihedral on edge `(b_i, b_k)` is DEFERRED to when the parent merge fixes the triangle on the opposite side, or at top-level resolves against the closing-edge outside normal (D42); (4) recursive triangulation reconstruction from the DP optimal-split table; (5) **patch attachment via to_indexed → append patch indices → build_from** (D45) — single rebuild, deterministic, avoids needing per-loop atomic edits. Patch triangle vertex order `(b_i, b_m, b_k)` with `i < m < k` is CCW from OUTSIDE the surrounding mesh (per v7a's boundary HE walk direction invariant; D44) — outward normals consistent with rest of mesh, verified via signed-volume divergence-theorem check on filled cubes (positive volume). Reports `Ok` / `EmptyMesh` / `NonManifoldInput` / `NoHolesToFill` + telemetry (`holes_detected`, `holes_filled`, `holes_skipped_too_large` (loops > `max_hole_size`), `triangles_added`). Two-layer typing per ADR-0078 §5 D34: raw `<MathScalar T>` body. Explicit instantiations for `f32` + `f64`. 9 Catch2 cases — empty mesh / no-holes / 3-vertex-hole (cube-minus-1-triangle) / 4-vertex-hole (cube-minus-1-face) / 2-separate-holes (cube-minus-2-opposite-faces) / `max_hole_size` guard / determinism / f64 / signed-volume orientation. Mesh-processing suite total **55 cases / 990 assertions** (v7a 14 + v7b 15 + v7c 9 + v7d 9 + v7e 9). One MSVC `C4267 (size_t → u32)` fixed mid-pass with explicit cast on `report.triangles_added` update. **Liepa §4 Steiner-point refinement + §5 high-order fairing DEFERRED** (D46) — `fairing_iterations` option API surface reserved (no-op in v7e — without Steiner points, no patch-interior vertices exist); will ship in v7-close or follow-on. **v7e-refine same-session re-open (after user scope-check)**: shipped Liepa §4 refinement (per-loop σ from input mesh = arithmetic mean of incident edge lengths; D47) + too-coarse `α·dist > σ AND α·dist > σ_avg` test (D48; α=√2 default); centroid Steiner insertion at index level (D51 — replace 1 tri with 3, append Steiner with σ = (σa+σb+σc)/3) + Delaunay flip pass on interior edges (D49 — `∠acb + ∠adb > π` via `crd::math::deterministic::acos`; v7d D36 duplicate-edge guard reused) + convergence loop (max_refine_iterations default 10) + Liepa §5 Jacobi-style Laplacian fairing on Steiner vertices only with loop clamp (D50). 3 new tests: refine-on convergence, 4×4-grid-hole triggers Steiner, fairing-iterations-match-when-Steiner. Mesh-processing suite total **58 cases / 1009 assertions**. **Scope-check learning** filed in session log: "optional" phase-doc language + "elite" user mandate ⇒ explicit `AskUserQuestion` scope-check, not silent deferral. Pinned design decisions D39-D51 carried for ADR-0076 §22 amendment at v7-close. Session log `docs/sessions/2026-05-17-geometry-v7e-fill-holes.md`. **🎯 Next = v7f manifoldness repair** (split non-manifold edges + duplicate bowtie vertices; consumes v7a's `BuildStatus::NonManifoldEdge` partial-build flag).

### Earlier milestone

**2026-05-17 — Phase 3.1.7 v7d Isotropic remeshing (Botsch-Kobbelt 2004) ✅ SHIPPED — 4-config DoD PASS on retry (`scripts/per-slice-check.ps1`, elapsed 03:13): win-debug + win-asan + win-shipping + win-tidy.** Canonical "uniform-triangle-size mesh + valence-6-interior / valence-4-boundary" algorithm — FEA solver / GPU vertex-shader / shading-rate input gold standard. New `isotropic_remesh(const HalfEdgeMesh<T>&, opts) -> HalfEdgeMesh<T>` entry — input untouched; constructs fresh remeshed copy. **4-pass-per-iteration loop** (Botsch-Kobbelt §4): (1) split edges > `(4/3)·L` at midpoint; (2) collapse edges < `(4/5)·L` GATED by link condition (HalfEdgeMesh's internal check) + **post-collapse safety pre-check** (no resulting edge to merged-vertex neighbours may exceed `(4/3)·L`, prevents oscillation); (3) flip interior edges toward target valence (6 interior / 4 boundary), gated by total-deviation reduction AND **`vertices_connected(c, d)` duplicate-edge pre-check** (v7a's `flip_edge` doesn't gate on c-d-already-connected — would create non-manifold; D36); (4) **true tangential smoothing** (Botsch-Kobbelt §4 actual spec, not the naïve "move-to-centroid"): displacement decomposed via area-weighted vertex normal `n`, only tangential component `d_t = d - (d·n)·n` applied, then projected back to ORIGINAL input surface via f32 BVH closest-point query (precision-cast at the boundary; D37); (5) **inversion-rejection safeguard** — if move would flip any incident face's orientation, keep v fixed (Hoppe-style; D38). **Per-pass canonical-HE snapshot** discipline (Botsch-Kobbelt iteration order: new HEs created within a pass NOT re-processed in same pass — required for convergence). Jacobi-style smoothing (all positions computed against OLD, applied atomically via new `HalfEdgeMesh::set_vertex_position` setter). Multi-iteration via `n_iterations` option; `n_iterations = 0` returns clone-of-input. Module deps EXTENDED: `crd-geometry-mesh-processing` now PUBLIC-links `crd-geometry-bvh` + `crd-geometry-mesh` (for `TriangleMeshBvh` + `mesh_closest_point` used in surface projection). Reports `Ok` / `EmptyMesh` / `NonManifoldInput` / `InvalidTargetLength` + telemetry (`splits_applied`, `collapses_applied`, `flips_applied`, `vertices_smoothed`). 9 Catch2 cases — cube-refines / cube-coarsens / open-quad-boundary-fixed / determinism / surface-projection-keeps-cube-bounded / f64 precision / diagnostic statuses / `n_iterations=0` clone. Mesh-processing suite total **47 cases / 925 assertions** (v7a 14 + v7b 15 + v7c 9 + v7d 9). **2 mid-slice bugs caught + fixed by bisection** (D36 flip-creates-duplicate-edge + D37/D38 naïve-Laplacian-smoothing-degenerates-at-projection) — the manifold-test failure at iteration 3 of cube-refines test was the canary; bisected via disabling projection (passes) → disabling smoothing (passes) → root-causing two distinct issues. **First DoD attempt failed at win-tidy with transient `bugprone-reserved-identifier` clang-tidy access violation** (upstream LLVM bug, same policy class as `feedback_transient_msvc_ltcg_ice_accept.md` MSVC LTCG ICE); retry PASS. New `feedback_transient_clang_tidy_crash.md` policy filed. Pinned design decisions D26-D38 carried for ADR-0076 §22 amendment at v7-close. Session log `docs/sessions/2026-05-17-geometry-v7d-isotropic-remesh.md`. **🎯 Next = v7e Liepa 2003 hole filling** (weighted minimum-area triangulation over boundary loops via dynamic programming; optional fairing pass).

### Earlier milestone

**2026-05-17 — Phase 3.1.7 v7c Loop subdivision (Loop 1987) ✅ SHIPPED — 4-config DoD PASS (`scripts/per-slice-check.ps1`, elapsed 03:47): win-debug + win-asan + win-shipping + win-tidy.** Charles Loop's classical 1 → 4 triangle subdivision; C² limit surface almost everywhere (C¹ at extraordinary vertices). New `loop_subdivide(const HalfEdgeMesh<T>&, opts) -> HalfEdgeMesh<T>` entry — input untouched; constructs fresh subdivided output via the indexed-form pipeline (extract input → build temp HE mesh per level → emit new positions+indices → build_from on final). **Interior edge midpoint mask**: `3/8·A + 3/8·B + 1/8·C + 1/8·D` (Loop 1987 §2.2; C, D = apex of incident faces). **Boundary edge midpoint mask**: `(A + B)/2` (cubic-B-spline limit position). **Interior vertex update mask**: `(1 - n·β)·V + β·Σ neighbours` with `β = (1/n)·(5/8 - (3/8 + 1/4·cos(2π/n))²)` — uses `crd::math::deterministic::cos` for bit-identical FP across compilers/architectures per ADR-0063. **Boundary vertex update mask**: `3/4·V + 1/8·u_left + 1/8·u_right` (cubic-B-spline boundary curve refinement). **Sub-triangle layout** (4 per face, CCW preserved): corner@v₀ (v₀, m₀₁, m₂₀), corner@v₁ (v₁, m₁₂, m₀₁), corner@v₂ (v₂, m₂₀, m₁₂), central (m₀₁, m₁₂, m₂₀). **Multi-level** = repeated single-level application; `n_levels = 0` returns clone-of-input. **Boundary-neighbour detection**: for each outgoing HE at v, edge is boundary iff `he_is_boundary(ho) OR he_is_boundary(twin(ho))` — v7a's CW fan walk guarantees exactly two such hits for 2-manifold boundary vertex. Reports `Ok` / `EmptyMesh` / `NonManifoldInput` + telemetry (`levels_applied`, `output_vertices`, `output_faces`). 9 Catch2 cases — cube L=1 (V=26, F=48, E=72, χ=2 preserved) + L=2 (V=98, F=192, E=288); open quad L=1 (V=9, F=8, E=16, χ=1 disk, 1 boundary loop preserved); boundary midpoint geometric-midpoint invariant; determinism (byte-identical positions); cube-corner-inward smoothing assert; f64 precision tier; `EmptyMesh` diagnostic; `n_levels=0` no-op. Mesh-processing suite total = **38 cases / 605 assertions** (v7a 14 + v7b 15 + v7c 9). One clang-tidy warning fixed mid-pass (local `constexpr T k_two_pi` violated `.clang-tidy`'s `LocalConstexprVariableCase = CamelCase + Prefix = 'k'` — renamed `kTwoPi`; project's `.clang-tidy` is authoritative over `CLAUDE.md`'s lower_case-for-constexpr spec). Pinned design decisions D17-D25 carried for ADR-0076 §22 amendment at v7-close. Session log `docs/sessions/2026-05-17-geometry-v7c-loop-subdivision.md`. **🎯 Next = v7d Botsch-Kobbelt 2004 isotropic remeshing** (5-10 iterations of split-long + collapse-short + flip-toward-valence-6 + tangential-relaxation-with-BVH-projection).

### Earlier milestone

**2026-05-17 — Phase 3.1.7 v7b Quadric Edge Collapse Decimation (Garland-Heckbert 1997 + Garland 1998) ✅ SHIPPED — 4-config DoD PASS (`scripts/per-slice-check.ps1`, elapsed 03:24): win-debug + win-asan + win-shipping + win-tidy.** The cooker LOD ladder primitive (every shipping game generates 3-5 LODs per mesh from QEM). New `Quadric<T>` type (symmetric 4x4 stored as 10 unique floats; layout pinned; outer-product `from_plane` ctor + `evaluate(q, v)` expanded form + `optimal_position(q)` 3x3 Cramer inverse with midpoint fallback). New `qem_decimate(const HalfEdgeMesh<T>&, options) -> HalfEdgeMesh<T>` entry — input untouched; constructs fresh output via `to_indexed → build_from` on requested allocator. **Boundary preservation** (Garland 1998 §3.1: heavy `n_b = edge × n_f` wall plane on every boundary HE — silhouette can't implode). **Inversion prevention** (predict-then-collapse: substitute `v_opt`, check no face flips; rejects self-intersection). **Locked vertices** (cooker LOD anchor primitive; 1-locked constrains `v_opt` to locked endpoint; both-locked rejected; positions bit-identical). Lazy-invalidation min-heap via `crd::containers::push_heap`/`pop_heap` keyed lex `(cost, canonical_he_id)` for byte-identical determinism across compilers (per-canonical-HE generation counter, pop checks alive + generation match + non-boundary). Stop conditions: `target_face_count` OR `max_error_threshold` (at least one required, else status = `NoStopCondition`). 15 Catch2 cases / 92 assertions added (7 Quadric + 8 qem_decimate); mesh-processing suite total now 29 cases / 156 assertions. First DoD found 3 fixable issues (em-dash in test name caught by `crd-no-non-ascii-test-names` guard + clang-tidy variable-naming warnings on `K`/`Kb`/`Qe`/`Q` + multi-decl + unchecked optional in tests — all cleaned, retry PASS). Pinned design decisions D9-D16 carried for ADR-0076 §22 amendment at v7-close. Session log `docs/sessions/2026-05-17-geometry-v7b-qem-decimation.md`. **🎯 Next = v7c Loop subdivision** (Loop 1987: edge-midpoint split via `HalfEdgeMesh::split_edge` from v7a + `3/8 + 1/8` edge-vertex weights + `β(n)` vertex-update weights + cubic-B-spline boundary weights).

### Earlier milestone

**2026-05-16 — Phase 3.1.7 v7a `HalfEdgeMesh<T>` substrate ✅ SHIPPED — 4-config DoD PASS (`scripts/per-slice-check.ps1`, elapsed 03:51): win-debug + win-asan + win-shipping + win-tidy.** New `engine/geometry-mesh-processing/` module (8th `crd-geometry-*` sub-module, mutable side of geometry — v4 `-mesh` is read-only). Full mutable mesh data structure: stable-index flat arrays (vertices / half-edges / faces) + free-lists; **16-byte `HalfEdgeSlot`** pin (origin, twin, next, face — static_asserted); **materialised boundary half-edges** (`face == k_null_face`) so boundary loop traversal is the same `.next` walk as a face loop, branchless on the twin pointer; **lex-tuple twin pairing** in `build_from` (bit-identical across MSVC / GCC / clang per ADR-0063); **uniform CW fan rotation** `cur.twin.next` for `for_each_outgoing_he` (interior + boundary use the same walk); **three atomic edit operations** — `collapse_edge` (Edelsbrunner 2001 link-condition gate; rejects boundary collapse for v7a — deferred to v7f), `split_edge` (1 new vertex + 4 new HEs + 2 new faces), `flip_edge` (diagonal swap with face/HE rewiring); topology queries (`vertex_count`/`face_count`/`edge_count`/`boundary_loop_count`/`is_manifold`/`is_closed`/`euler_characteristic`); explicit instantiations for `f32` and `f64`. 14 Catch2 cases / 67 assertions. Session log `docs/sessions/2026-05-16-geometry-v7a-half-edge-mesh.md`. Pinned design decisions D1-D8 (carried for ADR-0076 §22 amendment at v7-close). **🎯 Next = v7b QEM decimation** (Garland-Heckbert 1997: quadric matrix per vertex + collapse-cost min-heap + `collapse_edge` link-condition gate already in place).

### Earlier milestone

**2026-05-16 — Phase 3.1.7 v6 `-polygon` cluster ✅ FULLY VERIFIED via `scripts/full-sweep.ps1` — 18/18 configs PASS, elapsed 13:52.** All 11 Windows configs (debug / relwithdebinfo / release / asan / clang-cl / debug-scalar / debug-sse2 / shipping / shipping-profile / clang-cl-shipping / tidy) + all 7 Linux configs (gcc-debug / relwithdebinfo / release / asan / debug-scalar / debug-sse2 / shipping) PASS build+ctest+sandbox-smoke. **v6 cluster CLOSED 2026-05-16** — 6 slices (v6a substrate + v6b ear-clipping w/ holes + v6c CDT w/ Bowyer-Watson + Domiter-Zalik carve-and-retriangulate + v6d Vatti polygon Boolean via planar-subdivision + winding-number + v6e Bentley-Ottmann O((n+k) log n) sweep + v6-close), new `engine/geometry-polygon/` module (7th `crd-geometry-*` sub-module), ~4350 LOC engine + ~2750 LOC tests, 92 cases / 695 assertions, ADR-0076 §21 amendment (20 locked substrate decisions), `docs/systems/geometry-polygon.md`. Session log `docs/sessions/2026-05-16-geometry-v6-close.md`.

### Earlier milestone

**2026-05-16 — Phase 3.1.7 v5 `-spatial` cluster CLOSED.** 8 slices (v5a KdTree + v5b LooseOctree + v5c R*-tree + v5d SpatialHash + v5e UniformGrid + thread-safety pass + scene-index bringup realizing the ADR-0053 `SpatialBVHIndex` slot + unified facade extension + v5-close), new `engine/geometry-spatial/` module, ~4900 LOC engine + ~4330 LOC tests, cross-backend ASan race coverage = 2000 fan-out tasks, unified `crd::geometry::*` facade now dispatches over 8 backend types (5 spatial + 3 BVH), ADR-0076 §20 amendment (20 locked substrate decisions), `docs/systems/geometry-spatial.md`. 18-config full sweep PASS. Session log `docs/sessions/2026-05-16-geometry-v5-close.md`.

### Earlier milestone

**2026-05-16 — Phase 3.1.7 v4 `-mesh` cluster CLOSED.** 5 slices (v4a mesh_closest_point Ericson + v4b mesh_raycast Woop watertight + v4c mesh_winding_number Jacobson 2013 + v4d mesh_raycast_simd AVX2 8-wide MT + v4-validate 6-defect-class formal validator + v4-close), new `engine/geometry-mesh/` module, ADR-0076 §19 amendment (9 locked decisions), `docs/systems/geometry-mesh.md`. 18-config full sweep PASS.

### Earlier milestone

**2026-05-15 — Phase 3.1.7.5 `crd-units` CLOSED + D-006 `crd-time` + D-003 `crd-perf` profiler + ImGui frontend all shipped same day.** 19 sub-slices of units (substrate + v0b/c/d adoption passes), 8 slices of perf (v0a-v0h), D-006 time substrate. ADR-0078 §1-§5 (31 locked decisions including §5 two-layer typed architecture pin). Full project ctest 1546 → 1913 win-debug across the day. **Mars Climate Orbiter at the engine surface is now a compile error.**

### Earlier milestone

**2026-05-13 — Phase 3.1.7 v1 `-bvh` cluster CLOSED via `scripts/full-sweep.ps1` — 17/17 configs PASS.** All 9 Windows configs + win-tidy build + all 7 Linux configs PASS. v1 cluster (BvhTree → BVH4 SIMD → unified queries + shapecast + broadphase + viz substrate + sandbox showcase). Phase 3.1.7 v1 cluster CLOSED 2026-05-13.

### Earlier milestone

**2026-05-13 — Phase 3.1.7 v1j-b: sandbox integration — 3-scene selector + 4-mode geometry showcase + line-width slider. *v1 cluster CLOSED. Two same-day bug-fix passes addressed 5 user-reported issues.*** Round 1: physics scene bleed-through (clear-buffer-after-step in non-Physics modes), SDF heatmap invisibility (no point pipeline → cross_3d_to), Vulkan validation noise (upstream ImGui imgui_impl_vulkan.cpp:815 — documented). Round 2: historic v1a-draw d0d demo block (axis_triad/box/sphere/capsule/arrow/arc) bleeding into all scenes → moved out of render_scene into its own `SandboxScene::DrawShowcase` (3 scenes total: Physics / GeometryViz / DrawShowcase, each gets a clean canvas); `line_width` slider added to GeometryShowcaseState + threaded through every viz::draw call in primitive viewer / query showcase / BVH viewer / SDF heatmap / draw showcase. New `sandbox/src/geometry_showcase.{hpp,cpp}` (~870 LOC, kept out of bloated `sandbox_layer.cpp`). `enum class SandboxScene{Physics, GeometryViz}` + `GeometryShowcaseState` POD + pure `render_geometry_showcase` / `draw_geometry_showcase_imgui` functions. Four ImGui-selectable sub-modes: **Primitive viewer** (11 primitives, ImGui sliders for params, draggable query point + closest-point overlay for the 7 with closest_point overloads), **Query showcase** (N random AABBs → BvhTree; raycast/overlap/closest-point/sphere-cast via the v1i-a/b unified facade), **BVH viewer** (N up to 800, BvhTree/Bvh4Tree/DynamicBvh toggle, depth-coloured AABBs via viz::draw_bvh, draw_overlap_pairs_with overlay for DynamicBvh, frustum-cull toggle), **SDF heatmap** (9 SDF kinds, 3D grid sampling, HSV ramp blue-inside/green-surface/red-outside). `SandboxLayer` patched: 2 new members, conditional eylem `step_fixed` skip in GeometryViz mode, new "Scene" ImGui panel, showcase emits into shared `m_draw_buffer`. Default scene = Physics — existing v1b-e boot behaviour preserved. Per-advisor: BVH viewer slider capped at 800 (per-frame rebuild + walk budget), with tooltip explaining the rebuild-caching debt. **Coverage caveat (advisor):** automated smoke covers only the Physics-default boot path; the four showcase sub-modes' code paths are NOT exercised by automation — user-side validation = manually click through every sub-mode + every primitive type + every SDF kind (documented in the session log's "Validation responsibility" section). Filed `tests/geometry-viz/test_showcase.cpp` smoke automation as debt. Built+run green **win-debug + win-asan + win-shipping** (+ win-tidy build); full project ctest **1302/1302** on win-debug + win-asan, **1297/1297** on win-shipping; sandbox `--smoke-test 3` PASSES on all three (528-530 frames @ ~176 fps, zero Vulkan validation, clean exit). **v1 cluster CLOSED 2026-05-13** — 14 sub-slices (v1a..v1j-b). **Full 17-config `scripts/full-sweep.ps1` is NOW DUE** (deferred since v1c per user; user-triggered when ready). Session log `docs/sessions/2026-05-13-geometry-v1j-b-sandbox-showcase.md`. **Next phase = v2 (`-convex`)** — GJK distance + GJK boolean + EPA penetration + SAT box-pair fast path + ConvexHullView queries + GJK-based convex shapecast (the general moving-convex-vs-static-convex TOI v1i-b deferred to). ADR-0076 §1 sub-module 3. ~3300 LOC engine + ~1000 tests / ~2 weeks.

### Earlier milestone

**2026-05-13 — Phase 3.1.7 v1j-a: `crd-geometry-viz` companion module substrate.** First of two v1j sub-slices (user split after expanding scope to include a sandbox demo with scene selector + 4 showcase modes). Substrate ships the new module `engine/geometry-viz/` (target `crd-geometry-viz`, ns `crd::geometry::viz`; companion-module pattern mirroring `crd-eylem-viz` per ADR-0066 §13 — `crd-geometry-*` substrate stays consumable from headless / cooker / DAW / scientific-computing builds, the bridge knowing about both `crd-geometry-*` and `crd-draw` lives here). Three layered headers + impls: **primitives.hpp** overloaded `draw(RenderBuffer&, Shape, ...)` for every primitive (AABB3 / OBB3 / Sphere / Capsule3 / Cylinder3 / Plane[finite patch with tangent-frame grid] / Triangle3 / Tetrahedron / Frustum[8-corner reconstruction via closed-form 3-plane intersection] / Ray3 / Segment3 / Line3 — forwards to existing `crd::draw::*_to`); **queries.hpp** `draw_ray_hit(ray + cross + optional normal arrow)` / `draw_closest_point(segment + endpoints)` / `draw_normals(per-vertex hairs)`; **bvh.hpp** `depth_color` 8-entry palette + `draw_bvh(BvhTree | Bvh4Tree | DynamicBvh, depth_limit)` recursive walks emitting depth-coloured AABBs + `draw_overlap_pairs_with(DynamicBvh, ud→pos lambda)` template + `draw_overlap_pairs` convenience + `draw_frustum_cull(Frustum, BvhTree, prims)` two-colour kept/culled. Tests: 27 cases / 46 assertions on `crd-geometry-viz-tests` (post-advisor rename `draw_bvh(DynamicBvh)` → `draw_bvh_bounds` + made `draw_overlap_pairs(DynamicBvh)` convenience an explicit no-op since the tree has no centroid table) — exact line-count pins (AABB=12, Triangle=3, Tetrahedron=6, Plane patch = `(divs+1)*2`, Frustum=12), depth-palette cycle, BVH node-count match, overlap-pair count cross-check, kept-vs-culled frustum cull verification. Built+run green **win-debug + win-asan + win-shipping** (+ win-tidy build); full project ctest **1301/1301** on win-debug + win-asan, **1296/1296** on win-shipping. clang-format/tidy clean (zero new warnings on v1j-a files). All 4 CI guards green (`crd-no-non-ascii-test-names` actually verifies now post-v1i-c fix). New module wired into root `CMakeLists.txt` after `engine/draw`+`engine/eylem-viz`; `tests/CMakeLists.txt` adds `geometry-viz/`. New system doc `docs/systems/geometry-viz.md`. Session log `docs/sessions/2026-05-13-geometry-v1j-a-viz-substrate.md`. **Debt added (carried):** `DynamicBvh::for_each_leaf(Fn)` public walker — so `draw_bvh(DynamicBvh)` can show per-leaf bounds, not just the root (editor Phase 7 will need it; not blocking today, no current consumer). **Next slice = v1j-b** — sandbox integration. ImGui dropdown selects between existing eylem physics demo and new geometry-showcase scene with 4 sub-modes: primitive viewer (sliders for every primitive type, closest-point line from a draggable query point) + query showcase (fixed 5-primitive scene, ImGui picks raycast/overlap/closest-point/shapecast, renders input + result) + BVH viewer (N random AABBs slider, depth-coloured nodes, BvhTree/Bvh4Tree/DynamicBvh toggle, insert/remove for DynamicBvh + overlap pairs, frustum-cull toggle) + SDF heatmap (sample v1h `sd_*` at a grid, tint points by distance). ~1500 LOC sandbox/UI, ~3 days. **After v1j-b**: v1 cluster closes (v1a → v1j-b) → full 17-config `scripts/full-sweep.ps1` → v2 (`-convex`: GJK + EPA + SAT + Quickhull).

### Earlier milestone

**2026-05-13 — Phase 3.1.7 v1i-c debt payment: all three debts paid immediately.** User asked for v1i-c's three debt entries paid this session rather than carried to v1j/v2. (#1) **Caller-owned scratch for `find_overlapping_pairs`** — new public types `DynamicBvhPairWork{a,b}` (promoted out of function scope) + `DynamicBvhPairScratch{Array<u32> walk; Array<DynamicBvhPairWork> cross}` (move-only, allocator-constructed, `clear()` retains capacity); new scratch-taking overload `find_overlapping_pairs(Fn&&, DynamicBvhPairScratch&)` + facade mirror; existing alloc-per-call overload now one-line forward. Test: 150-leaf tree, 5 mutation iterations with `update(random leaf)` between, scratch-path produces bit-identical sorted pair sets to alloc-per-call. Eylem v1c can now wrap the scratch overload on the broadphase hot path without per-tick alloc churn. (#2) **`Vec4f ray_vs_4_aabb_inflated` SIMD kernel** — new free fn in `bvh4_simd.hpp/cpp` mirrors v1g pattern: splat pad to Vec4f, inflate columns, forward to existing `ray_vs_4_aabb`. `bvh4_inflated_raycast` (the shapecast traversal) now transposes ≤4 child bounds into SoA Vec4f and does one kernel call per node instead of four sequential scalar slab calls; leaf-prim path stays scalar (per-leaf SIMD is `-mesh` v4g). 2000-trial lane-by-lane test (varying pad: zero / small / moderate) confirms bit-identical results to the scalar form. Per-instruction comparison suggests a ~2-3× per-node ratio for the AABB test itself; per-call speedup depends on the leaf-scalar/interior-SIMD work mix and is **unmeasured this slice** (dedicated shapecast micro-bench is future work). (#3) **AABB-equivalence shift-invariance assertion** — `BVH raycast at +1e6 origin` now compares the named hit AABB at the far origin (shifted back) to the named hit AABB at the origin within local ULP tolerance, instead of comparing payload indices; decouples the test from builder permutation. Drive-by: removed two unused `using` decls in `test_validation.cpp` flagged by clang-tidy. **`crd-geometry-bvh-tests` 82 cases / 76 581 assertions** (was 80 / 68 563 after v1i-c initial; +2 cases, +8018 assertions from the SIMD kernel's 2000 lane-by-lane trials). Built+run green **win-debug + win-asan + win-shipping** (+ win-tidy build); full project ctest **1275/1275** on win-debug + win-asan, **1270/1270** on win-shipping. clang-format/tidy clean (the pre-existing `bugprone-unchecked-optional-access` Catch2 pattern remains). **All three v1i-c debts struck from `docs/debt.md`. Zero new debt added.** Session log `docs/sessions/2026-05-13-geometry-v1i-c-debt-payment.md`. **Next slice = v1j** (`crd-geometry-viz` companion module — unchanged from prior plan; debt payment didn't reshape the surface).

### Earlier milestone

**2026-05-13 — Phase 3.1.7 v1i-c: `find_overlapping_pairs(DynamicBvh)` Catto dual-descent + degenerate-corpus / large-coordinate validation discipline + `crd-no-non-ascii-test-names` guard correctness fix (broken silently since v0e).** Closes the v1i cluster (a/b/c). New `DynamicBvhPair{a,b}` POD + member template `DynamicBvh::find_overlapping_pairs(Fn)` (Catto GDC 2019 dual-descent — outer DFS over interior nodes + cross-walk on (child1, child2) at each; `Array<CrossWork>` stacks allocated from the tree's own allocator so unbounded cross-stack growth on dense corpora is safe) + Array<DynamicBvhPair> append form + facade overload. Static-tree variants deliberately NOT shipped (no user_data on static leaves). New shared `tests/geometry-primitives/include/test_corpus.hpp` (header-only, both test binaries include): `degenerate_aabbs/triangles/spheres/rays()` generators (zero-volume / collinear / NaN / ∞ / half-infinite) + large-coordinate `shift()` helpers + `k_far_origin_modest=1e6F`/`stress=1e7F` + `ulp_tolerance_for(m)` (≈ `m/2²²`). New `test_overlapping_pairs.cpp` (8 cases: empty/single/hand-built/brute-force agreement on 5 varying-density corpora + callback==Array + deterministic + **n=10000 dense soak** per advisor — 64 MB allocator headroom, no TLSF OOM, count matches brute / + insert/remove/update tracking). New `test_validation.cpp` in both binaries (11 primitives + 7 BVH cases) exercising the NaN/Inf contract (§16 pin #3 — queries tolerate, builders reject) over all primitive + BVH queries, plus +1e6/+1e7 shift-invariance within local ULP. **`crd-no-non-ascii-test-names` guard correctness fix** — the same Windows ctest non-ASCII argv mojibake bug hit three consecutive slices (v0f em-dashes, v1i-b `²`+em-dash, v1i-c `×`). Investigation found the guard had been a silent no-op since v0e (~3 days): (a) param default `[string] $RepoRoot = (Resolve-Path "$PSScriptRoot/..").Path` — PowerShell evaluates param defaults in a scope where `$PSScriptRoot` is empty → resolved to `D:\` → `$testsDir = D:\tests` doesn't exist → early-return PASS; (b) regex `[^ -]` — `-` between space and class-close `]` parses as range op (`[^\x20-\x2D]`) matching every letter/digit. Fixed both: `$RepoRoot` initialized in script body after `$PSScriptRoot` is in scope; regex replaced with per-char codepoint loop (`[int]$ch -gt 0x7F`) that can't be misparsed; inline postmortem comments name the bug + dates + slices. Verified by seeding `tests/_corruption_test/test_x.cpp` with `×` (FAILed correctly) then removing (PASSed on real tree). Linux .sh was correct. **`crd-geometry-bvh-tests` 80 cases / 68 563 assertions** (was 65 / 68 383 after v1i-b); **`crd-geometry-primitives-tests` 114 cases / 64 467 assertions** (was 103 after v1h). Built+run green **win-debug + win-asan + win-shipping** (+ win-tidy build); full project ctest **1273/1273** on win-debug + win-asan, **1268/1268** on win-shipping. clang-format/tidy clean. Full 17-config sweep still deferred to v1 cluster close (after v1j). Session log `docs/sessions/2026-05-13-geometry-v1i-c-broadphase-pairs-and-validation.md`. **Next slice = v1j** (`crd-geometry-viz` companion module — debug-draw bridge to `crd-draw`; `crd-geometry` itself never depends on `crd-draw`; functions `draw_aabb` / `draw_bvh(depth-coloured)` / `draw_ray_hit` / **`draw_overlap_pairs(DynamicBvh)`** = direct visualisation of v1i-c). Then v1 cluster closes → **full 17-config `scripts/full-sweep.ps1`** → v2 (`-convex`: GJK + EPA + SAT + Quickhull, including GJK-cast).

### Earlier milestone

**2026-05-13 — Phase 3.1.7 v1i-b: shapecast (closed-form TOI) — `bvh_shapecast_sphere` / `bvh_shapecast_box` + `bvh4_*` variants + `cast_ray` / `cast_sphere` / `cast_box` facade overloads.** Second sub-slice of the v1i split. New `crd/geometry/bvh/bvh_shapecast.hpp` + impl — sphere-cast and box-cast over `BvhTree` and `Bvh4Tree`, both reducing to "ray-cast against AABB inflated by moving shape's pad" via the v0f Williams/Ize robust slab. Sphere-vs-AABB conservative (square corners — broadphase precondition every shipped engine accepts; exact-corners answer is GJK-cast in `-convex` v2); box-vs-AABB exact (Minkowski sum of two AABBs is an AABB). `Vec4f` ray-vs-4-inflated-AABB SIMD kernel for `bvh4_shapecast_*` deferred (~40 LOC follow-up; mirror of v1g pattern). `queries.hpp` facade extended with `cast_ray(Ray3, {AABB3/OBB3/Sphere/Plane}, tmax)` (thin `optional`-wraps over existing `intersect_ray_*`), `cast_sphere(Sphere moving, dir, tmax, {AABB3/Sphere/Plane})` (exact for sphere and plane via overlap-at-start early return + intersect_ray_sphere; conservative for AABB), `cast_box(AABB3 moving, dir, tmax, AABB3)` (exact), plus BVH overloads `cast_sphere(BvhTree/Bvh4Tree, prims, moving, dir, tmax)` and `cast_box(...)`. **Argument convention pinned (propagates to v2 GJK-cast and v6 CCD):** primitive overloads = `(moving, dir, tmax, target)` (matches PhysX/Jolt); BVH overloads = `(tree, prims, moving, dir, tmax)` (matches v1a `raycast` shape). Capsule-vs-triangle deferred to v2/v4 — explicitly trimmed from the v1i-b phase row (was an example in the original v1i description). New `tests/geometry-bvh/test_shapecast.cpp` — 15 cases: degenerate-recovery (`cast_sphere(r=0)` ≡ `cast_ray`; `cast_box(half=0)` ≡ `cast_ray`); overlap-at-start gives `t=0` across all four primitive pairs; exact-numeric closed-form spot checks (sphere×AABB t=4, sphere×sphere t=8, sphere×plane t=4.5, box×AABB t=8); **bisection cross-check** (advisor recommendation — `distance²(target, swept_center(TOI)) ≤ r²` at TOI for 20 random trials, `> r²·0.95` one ε before; catches Minkowski-sign bugs the algorithm-vs-algorithm brute force can't); BVH shapecast matches brute force (200 prims × 300 random sphere + 300 random box sweeps, BvhTree and Bvh4Tree byte-for-byte agree); tmax cutoff; empty-tree → nullopt. Drive-by em-dash fix in one TEST_CASE name (Windows ctest non-ASCII matching). **`crd-geometry-bvh-tests` 65 cases / 68 383 assertions** (was 50 / 66 091 after v1i-a). Built+run green **win-debug + win-asan + win-shipping** (+ win-tidy build); full project ctest **1247/1247** on win-debug + win-asan, **1242/1242** on win-shipping. clang-format/tidy clean on new files. Full 17-config sweep still deferred to v1 cluster close (after v1j). Session log `docs/sessions/2026-05-13-geometry-v1i-b-shapecast.md`. **Next slice = v1i-c** (`find_overlapping_pairs(DynamicBvh)` Catto-style dual-descent + reusable degenerate-corpus / large-coordinate validation helpers). Then v1j (`crd-geometry-viz`).

### Earlier milestone

**2026-05-13 — Phase 3.1.7 v1i-a: unified query facade + templated `RayHit<P>` / `ClosestPointResult<P>` + missing `closest_point` for `Bvh4Tree` and `DynamicBvh`.** First sub-slice of the v1i split (v1i sized ~700 LOC; user opted for a/b/c since bigger than typical). New `crd/geometry/result_types.hpp` parks the templated result structs (substrate; field order pinned `RayHit{t, payload}` / `ClosestPointResult{point, distance_squared, payload}` per ADR-0076 §16 pin #2). New `crd/geometry/queries.hpp` — compile-time-overload-polymorphic `raycast` / `overlap` / `closest_point` over `{BvhTree, Bvh4Tree, DynamicBvh}`, inline forwards (zero overhead); primitive overloads flow through transitively from the existing `closest_point.hpp` / `intersect.hpp` / `signed_distance.hpp` in `crd::geometry::primitives` — picked up via ADL, no redefinition. Backend aliases land in `bvh_query.hpp`: `using BvhRayHit = RayHit<u32>;` / `using BvhClosestPoint = ClosestPointResult<u32>;`. Field rename `.prim_index` → `.payload` + aggregate-init field-order swap across 3 impl files + 4 test files (10-site migration). **New `bvh4_closest_point`** (free function, branch-and-bound with ≤4-child insertion-sort to near-first; same shape as v1e `bvh_closest_point`). **New `DynamicBvh::closest_point`** (member, broadphase closest-leaf by *fat* AABB, reports `user_data` payload; member because of private `Node` array access; mirrors `query`/`raycast` member style). `DynamicBvh::raycast` deliberately stays callback-only (broadphase). New `test_queries.cpp` — 10 cases / 13 142 assertions: layout pins (`static_assert` BvhRayHit ≡ RayHit<u32>), facade-equivalence (300+ random rays/queries per backend cross-checked against direct call), brute-force agreement for the two new closest-point variants, edge cases (empty / single-prim / inside-fat-AABB / `max_dist` cutoff). **`crd-geometry-bvh-tests` 50 cases / 65 549 assertions** (was 40 / 13 197 after v1g). Built+run green **win-debug + win-asan + win-shipping** (+ win-tidy build); full project ctest **1232/1232** on win-debug + win-asan, **1227/1227** on win-shipping. clang-format/tidy clean; all 4 CI guards green. **Full 17-config `scripts/full-sweep.ps1` still deferred to v1 cluster close (after v1j) — CI is surprisingly green so the sweep amortises once at the end.** Session log: `docs/sessions/2026-05-13-geometry-v1i-a-queries-facade.md`. **Next slice = v1i-b** (shapecast: closed-form TOI for `cast_ray`/`cast_sphere`/`cast_box` vs primitives + BVH backends; general convex-cast is v2). Then v1i-c (`find_overlapping_pairs(DynamicBvh)` + validation corpora).

### Earlier milestone

**2026-05-11 — Phase pivot: Phase 3.1.7 `crd-geometry` executes BEFORE Phase 3.1 v1c (ADR-0076 §12 amendment).** Full 29-slice geometry phase ships first, then eylem v1c onward consumes `crd-geometry` from day 1 with no deferred-refactor debt. Sequence: v1b ✅ → Phase 3.1.7 → eylem v1c+ → sdf (interleaved between eylem v2/v3, also consumes geometry from day 1) → hesap. Calendar cost: ~5–7 months before eylem v1c can resume; the explicit tradeoff is "no shortcuts, single-path" per `feedback_quality_bar.md`. Doc cascade: ADR-0076 §12 amendment block + Status update; phase-3.1-eylem.md status updated + pause-marker inserted between v1b and v1c; phase-3.1.7-geometry.md status flipped to "next-active"; phase-3.1.5-sdf.md notes the ADR-0064 §4 deferred-refactor is OBSOLETED; ROADMAP.md updated. Memory: `project_phase_sequencing_pivot.md` saved.

**2026-05-11 — Phase 3.1 v1b-e in-flight: sandbox eylem demo + render interpolation (Glenn Fiedler "Fix Your Timestep") + pre-existing dangling-ConstSpan fix in VulkanDescriptorSetLayout/VulkanPipelineLayout.** The latter was surfaced by win-asan during the sweep: `VulkanDescriptorSetLayout` stored `DescriptorSetLayoutDesc` by value, but `bindings` inside it is a non-owning `ConstSpan`; the canonical caller pattern (`ForwardRenderPath::create` at forward_render_path.cpp:32-35) builds the desc with a span pointing to a stack-local binding, then lets the local fall out of scope. Any later `desc().bindings` read freed stack memory — latent bug, only triggered now because v1b-e's stack-layout changes (BodyChunk 608B → 832B + extra system registration) shifted the stack such that ASan's red zones caught the read. Fix: both `VulkanDescriptorSetLayout` and `VulkanPipelineLayout` now deep-copy the span contents into owned `crd::containers::Array` storage and re-point the stored desc at the owned memory. Win-asan ctest 1059/1059 + sandbox-smoke 528 frames / 175.9 fps clean. Sweep re-launched. Sandbox now spawns 3 rigid bodies (sphere/box/capsule) at +y=5 with `Transform + RigidBodyComponent + ColliderComponent + DebugVizComponent{AxisTriad+Wireframe+ShowVelocity}` on a 16 MB dedicated `TlsfAllocator{..,"eylem-tlsf"}`; per-frame `World::step_fixed(dt, 1/64, max=8)`. **Interpolation substrate added (not in original v1b-e scope; user flagged jerky variable-rate render of fixed-step physics — addressed in-slice):** `BodyChunkT<Lane>` grew from 19 → 26 columns (608 → 832 B per AVX2 chunk) with prev_pos + prev_rot mirror columns; `BodyPool::snapshot_state_to_prev()` is one SIMD column assignment per chunk; `BodyPool::write_curr_only` (integrator-write, preserves prev) vs `BodyPool::write` (teleport-write, mirrors prev=curr); `BodyPool::read_prev → PrevState{pos, rot}`. `EylemSystem::run` calls `snapshot_state_to_prev()` ONCE at start of each substep then `write_curr_only` instead of `write`. New `World::fixed_step_alpha(fixed_dt)` + `fixed_step_accumulator()` accessors. New `RigidBodyInterpolationSystem` in `crd-eylem-rigid3d` (phase=PreRender, fixed_step()=false, registered BEFORE TransformPropagation in PreRender) — per body: skip null/stale id and sync_to_transform=0; lerp pos linearly, nlerp rot with dot<0 short-arc fix; write via `World::set_translation` / `set_rotation_quat`. **7 new tests / +41 assertions:** schedule pins (PreRender + variable); alpha=0 → prev; alpha=0.5 → midpoint; nlerp short-arc with non-degenerate antipodal pair (prev=identity, curr=negated small Y-rot; would land near 180° without fix); sync_to_transform=0 honoured; null/stale body_id no-op; fixed_step_alpha clamps on irregular accumulator; **multi-substep** (frame_dt=2.5·fixed_dt, max_substeps=4 → 2 substeps run + alpha=0.5; verifies prev=pose_after_substep_1, curr=pose_after_substep_2, lerp=2·g·dt²). crd-eylem-rigid3d-tests: 287 assertions / 32 cases. Tests use named `TlsfAllocator{4 MB, "interp-test"}` per fixture. **Side effects:** installed WinDbg/cdb via `winget install Microsoft.WinDbg` + saved feedback memory `feedback_use_crash_dumps_first.md` mandating cdb-first crash-dump analysis (path: `C:\Program Files\WindowsApps\Microsoft.WinDbg_1.2603.20001.0_x64__8wekyb3d8bbwe\amd64\cdb.exe`). **Pending:** user eye-test of demo smoothness + full 14-config sweep (the C4789 LTCG risk noted in CLAUDE.md known issues applies — BodyChunk grew from 608B → 832B → must clean-rebuild win-release / win-relwithdebinfo). Flagged for v1c+: double-write to Transform per frame (EylemSystem writes curr, InterpolationSystem overwrites with lerped) — functionally correct, marginally wasteful.

**2026-05-11 — Phase 3.1 v1b-d shipped: `crd-eylem-viz` companion module.** Peer module bridging eylem to crd-draw's VisualizerRegistry per ADR-0066 §13 dependency-inverted plug-in. `register_eylem_visualizers(VisualizerRegistry&, BodyPool&, ColliderPool&)`. RigidBodyComponent → velocity arrow on ShowVelocity flag (speed-threshold-gated, max-arrow-length capped); ColliderComponent → wireframe matching shape (sphere_wire / box_wire / capsule_wire); ConvexHull/Plane/TriangleMesh/Heightfield/Sdf no-op until v1d / v1d-mesh / v1d-hf / Phase 3.1.5 ship narrow-phase impls. Pre-req: extended `VisualizerContext` with `const World*` (defaulted nullptr — non-breaking) so cross-component lookup works. Pool refs captured at registration as file-scope statics — v1b-d-tier solution; one pair/process. 5 new TEST_CASEs / 14 assertions; **tests use `crd::memory::TlsfAllocator{16 MB}` per fixture** (production-realistic + catches leaks at destruction; saved as `feedback_named_allocators_in_tests.md` for project-wide convention going forward — never `default_allocator()` in tests). win-debug 1051/1051 + win-shipping 1048/1048 PASS. Full 14-config sweep deferred to v1b-e cluster close.

### Earlier milestone

**2026-05-11 — Phase 3.1 v1b-c shipped: RigidBodyComponent + ColliderComponent + EylemSystem.** First eylem entity integrating motion in the ECS world. PODs in `crd-eylem` (RBC = 8B BodyId+sync_to_transform+pad; CC = 4B ColliderId), registered SparseSet per ADR-0050. `EylemSystem` in `crd-eylem-rigid3d`: phase=Physics, fixed_step()=true, run() integrates `v += g·dt; p += v·dt` + small-angle quaternion update for rotation, syncs back to Transform via World setters. Static bodies (inv_mass=0) skipped. 5 new tests / 21 assertions: registration round-trip, phase + fixed_step pins, integration vs discrete-Euler closed form (60 substeps under -9.81 m/s² gravity), step_fixed substep-count exactness (10/64-second frame at 1/64 fixed_dt = exactly 10 substeps), static body inv_mass=0 doesn't integrate. **Tested: win-debug 1046/1046 + win-shipping 1043/1043 PASS.** Full 14-config sweep deferred to v1b cluster close (after v1b-d eylem-viz + v1b-e sandbox demo). Bodies fall through the world — that's the honest v1b-c framing (broadphase lands v1c, SI solver v1e). Session log: `docs/sessions/2026-05-11-v1b-c-eylem-system.md`.

### Earlier milestone

**2026-05-11 — clang-cl thin-LTO debt PAID + shipping config hardening (max opts + tests + sandbox + CI parity).** All three production-compiler shipping configs (MSVC + clang-cl + GCC-Linux) now build full test suite + sandbox AND run them in local sweep + GitHub CI, with **full LTO ON across the entire shipping config**. New `win-clang-cl-shipping` preset added. Optimisation flags expanded: MSVC adds `/Gy /Zc:inline` on top of `/O2 /Gw /Zi /OPT:REF /OPT:ICF`; GCC/Clang adds `-ffunction-sections -fdata-sections -funroll-loops -Wl,--gc-sections` on top of `-O3 -g --build-id`. Determinism contract (ADR-0063) + security checks preserved. clang-cl thin-LTO miscompile in `crdr.cpp` isolated via single-file `-fno-lto` (mirrors the existing `/Od` isolation on `worker_pool.cpp + fiber_init.cpp` in `crd-jobs`). All three shipping configs PASS 1038/1038 tests + sandbox-smoke. Win sweep 9/9 PASS. Session logs: `docs/sessions/2026-05-11-shipping-config-hardening.md` + `docs/sessions/2026-05-11-clang-cl-thin-lto-debt-payment.md`.

### Earlier milestone

**2026-05-11 — Phase 3.1 v1a-material substrate CLOSED (cluster a → b → c → d).** ADR-0069 fully realised. ~860 LOC + 17 new test cases / 119 new assertions across `crd-eylem-tests` (now 236 assertions / 31 cases). `Material` struct + `MaterialId` + 6 friction models + 3 restitution models + `MaterialPool` on scene + per-collider `Collider::material` handle + canonical 2-arg `add_collider` (3-arg convenience as non-virtual NVI) + `derive_mass_properties` free function (with `IPhysicsScene::derive_body_mass` wrapper) producing mass + COM + inertia diagonal for sphere/box/capsule. ColliderId-stable summation order; rotate-then-parallel-axis-shift inertia accumulation. Drive-by fix: `body_pool.cpp` + `collider_pool.cpp` `put_lane` Vec8f/Vec4f overload set collapsed into a single template<ColT> (clang-cl + scalar Lane=4 fallback both clean under -Werror,-Wunused-function). **Full 14-config sweep PASS** (Win × 8 via `scripts/full-sweep.ps1`; Linux × 6 verified directly via wsl + per-config ctest because the PS sweep wsl-stderr handler tripped on a CMake deprecation warning — same brittleness pattern noted in v0e post-mortem; pure cosmetic). Session log: `docs/sessions/2026-05-11-v1a-material-d.md` (cluster recap inline; sister logs for a/b/c).

### Earlier milestone

**2026-05-10 — Phase 3.1 v0e: SIMD benchmark harness — CLOSES Phase 3.1 v0 substrate.** New `tests/bench/test_bench_simd.cpp` ships AoSoA-8-vs-scalar micro-benchmarks for the four hot operations eylem v1+ will rely on per physics tick: Vec3f dot, Vec3f cross, Mat4f multiply, Quatf compose. Catch2's BENCHMARK macro reports timing; goal-line numbers + regression-investigation threshold (~30%) inline in the source. **Win-release measured speedups (AVX2 desktop):** Vec3f dot **5.9×**, Vec3f cross **5.6×**, Mat4f multiply **12.7×**, Quatf compose **0.65× regression** (expected — per-instance Quatf SIMD has Vec4f store-to-stack overhead that doesn't pay off without batching; AoSoA-8 quaternion layout reserved for eylem v4 articulation joint composition). Side-fix: scoped MSVC C4714 (`__forceinline` not inlined) suppression to `crd-bench` only — pre-existing LTCG noise from `crd-log::detail::should_log` that surfaces when bench TU mix changes inlining decisions. **Phase 3.1 v0 inventory now complete:** v0a SIMD wrappers (Vec4f/Vec8f/Mat4f/Quatf/Vec4i/Vec8i + bitwise/convert helpers) + v0b AoSoA Soa container + v0c+debt-A `crd::math::deterministic` (Cephes f32+f64 trig/exp/log + erf/erfc/gamma/lgamma/beta + Vec4f/Vec8f branchless SIMD overloads of sin/cos/exp/log) + v0d `crd::containers` deterministic sort + heap + v0e benchmark harness. Three CI guards live: `crd-simd-emission-check` (disasm-verifies SIMD level matches `CRD_SIMD_LEVEL_RESOLVED`), `crd-no-std-math-check` (bans `std::sin` etc. in eylem/hesap), `crd-no-std-sort-check` (bans `std::sort` etc.). ~3500 LOC across substrate + ~200 test cases / ~5800 assertions across `crd-math-tests` + `crd-containers-tests` + `crd-bench`. 12-config sweep clean throughout. Session log: `docs/sessions/2026-05-10-v0e-bench-harness.md`.

### Earlier milestone

**2026-05-10 — Phase 3.1 v0d: deterministic sort + heap substrate.** New `engine/containers/include/crd/containers/sort.hpp` (~280 LOC) ships `crd::containers::sort` / `stable_sort` / `nth_element` / `push_heap` / `pop_heap` / `make_heap` / `sort_heap` with deterministic-by-construction guarantees per ADR-0063 §3. Algorithm choices: merge sort for `sort` + `stable_sort` (naturally stable + deterministic, no pivot-dependent ordering — both functions bit-exact equivalent), median-of-three quickselect for `nth_element`, Floyd's bottom-up heapify + standard sift-up/sift-down for heap ops. New `crd-no-std-sort-check` CI guard greps `engine/eylem/**` + `engine/hesap/**` for banned `std::sort` etc. (opt-out marker `// crd-lint-allow-std-sort`). 14 cases / 1068 assertions. 12-config sweep all green. Session log: `docs/sessions/2026-05-10-v0d-sort-substrate.md`.

### Earlier milestone

**2026-05-10 — Phase 3.1 v0c: `crd::math::deterministic` Cephes-style transcendentals.** New `engine/math/include/crd/math/deterministic.hpp` + `engine/math/src/deterministic.cpp` (~600 LOC) ships bit-exact-cross-platform replacements for `std::sin`/`std::cos`/`std::tan`/`std::asin`/`std::acos`/`std::atan`/`std::atan2`/`std::exp`/`std::exp2`/`std::log`/`std::log2`/`std::log10`/`std::pow` plus IEEE-correct rounding wrappers (`floor`/`ceil`/`trunc`/`round`/`abs`/`copysign`/`fmod`). Coefficients are Cephes-derived (Stephen Moshier, public domain) + per-function citation in source. **Why this matters:** ADR-0063 §2 bans `std::sin` etc. in `engine/eylem/**` + `engine/hesap/**` because the C runtime's libm differs across MSVC CRT vs glibc vs Apple vecLib vs Bionic — same input returns different f32 bits, breaking the eylem v9b 9-config replay-hash CI. The substrate's polynomial evaluations are pure adds + multiplies + bit-tricks, deterministic by construction under the v0a `-ffp-contract=off` + `/fp:precise` contract. **CI guard added:** `crd-no-std-math-check` CTest greps `engine/eylem/**` + `engine/hesap/**` for banned `std::sin`/`std::cos`/`std::exp`/etc. and fails CI on any hit. Today (eylem doesn't exist yet) it's a no-op; lights up the moment eylem v1a creates the directory. Opt-out marker `// crd-lint-allow-std-math` for justified exceptions. **Two-tier tests:** accuracy (ulp distance vs `std::*` within 4-16 ulps depending on function) + golden (cross-check identities like `sin(-x) == -sin(x)` bit-for-bit, `exp(0) == 1` exactly, `log(1) == +0.0` exactly). Test helper `ulp_diff` works in signed-magnitude space so `+tiny vs -tiny` is reported as small distance not 2.1B. **Two real bugs fixed during implementation:** (1) atan polynomial used `-` operators between already-signed coefficients, double-negating p1/p3 → atan was ~5% wrong → cascaded to atan2/asin/acos; fixed by switching to all-`+` operators per Cephes convention; (2) sin/cos used `with_sign` (overwrite) where Cephes uses XOR-combine semantics → sign(-3π/4) returned wrong sign; added `apply_sign` (XOR) helper, kept `with_sign` for true copysign use. **Five deferrals captured in `docs/debt.md` BEFORE coding** (f64 overloads → `crd-hesap` v0a; Vec4f/Vec8f SIMD-batched → eylem v1+ when demand surfaces; hyperbolic sinh/cosh/tanh → reserved; special functions erf/gamma/bessel → `crd-hesap-stats` v13; expm1/log1p → reserved). 23 new test cases / 59 assertions; math suite now **114 cases / 2803 assertions** (was 91/2744 after v0b). Session log: `docs/sessions/2026-05-10-v0c-deterministic.md`.

### Earlier milestone

**2026-05-10 — Phase 3.1 v0b: AoSoA storage substrate (`crd::math::simd::Soa`).** New `engine/math/include/crd/math/simd/soa.hpp` (~240 LOC) ships `Soa<TChunk, Lane>` typed AoSoA container backed by `crd::containers::Array<TChunk>`. `Lane` defaults to v0a's `k_native_lane_width` (8 on AVX2, 4 elsewhere); user defines `TChunk` explicitly as a struct of `Vec8f`/`Vec4f` columns (no template reflection magic). Public surface: `resize`/`reserve`/`clear` (chunk-aligned grow), `chunk()`/`chunks()` access, `chunk_of`/`lane_of`/`make_index` constexpr index decomposition, `last_chunk_active_lanes()` (1..Lane for partial tail). Iteration helpers `soa_for_each_chunk` (SIMD path; lambda gets `(chunk&, active_lane_count)` so tail is maskable) + `soa_for_each_lane` (slow path for ad-hoc lookup). Cross-chunk lane movers `gather8`/`scatter8` over `Vec8f TChunk::*` member pointers + `gather4`/`scatter4` for `Vec4f` columns. **Software gather/scatter** (extract-and-pack), not hardware `_mm256_i32gather_ps` — preserves the v0a determinism contract; hardware-gather fast path reserved for v0e benchmark-driven optimisation. Static asserts close the supported `Lane` set to {4, 8}; AVX-512 width 16 reserved. New `tests/math/test_soa.cpp` (~290 LOC, **19 cases / 345 assertions**) covers static layout pins (Body8 = 64 B / `alignof >= 32`, Body4 = 32 B / `alignof >= 16`), sizing math (`resize(0..24)` cross-checked against `chunk_count` + `last_chunk_active_lanes`), index decomposition round-trip on `[0, 64)`, iteration coverage + correctness for full + partial-tail chunks + empty Soa, gather/scatter round-trip with intact untouched lanes, and an end-to-end `pos += vel * dt` SIMD step over 20 entities (3 chunks: 8+8+4) producing bit-exact results. Math suite is now **91 cases / 2744 assertions** (was 72/2399 after v0a). Session log: `docs/sessions/2026-05-10-v0b-soa-substrate.md`.

### Earlier milestone

**2026-05-10 — Phase 3.1 v0a: SIMD wrapper substrate (`crd::math::simd`).** New `crd::math::simd::Vec4f` (4-lane f32), `Vec8f` (8-lane f32; AVX2 native or composed Vec4f×2), `Mat4f` (4 Vec4f columns), `Quatf` (Vec4f storage) under `engine/math/include/crd/math/simd/`. Backend (SSE2/AVX2/NEON/scalar) chosen at compile time via the new `cmake/CrdSimd.cmake` module which exposes `CRD_SIMD_LEVEL` cache string + `CRD_DETERMINISTIC_FP` option, emits `/arch:AVX2`/`-mavx2`/etc. on a `crd-simd-flags` interface target propagated PUBLIC through `crd-math`. ADR-0063 contract realised: `mul_add` is `(a*b)+c` with two roundings on every backend (no hardware FMA), reductions use a fixed pairwise binary tree, hardware sqrt direct (IEEE-correct). **Three real bugs fixed during the audit:** (1) AVX2 was silently never used in any non-shipping build because `__AVX2__` isn't defined unless `/arch:AVX2` is explicitly passed — `CRD_SIMD_TARGET` macro is now the single source of truth; (2) `CRD_SHIPPING` enabled `/fp:fast` + `-ffast-math` violating ADR-0063 — both removed, determinism preserved; (3) no scalar-parity CI lane existed — new `win-debug-scalar` + `linux-gcc-debug-scalar` presets force scalar for bit-exact validation. **Permanent CI guard added:** `crd-simd-emission-check` CTest disassembles `test_simd.cpp.obj` (via `dumpbin`/`objdump`) and asserts the actual machine code matches `CRD_SIMD_LEVEL_RESOLVED` (passes if AVX2 build emits `vaddps ymm`/`vmulps ymm` etc., fails if scalar build accidentally emits ymm). `tests/math/test_simd.cpp` ships **30 new test cases / 148 new assertions** with bit-exact scalar-reference parity, plus a runtime backend-identity report. Disassembly evidence: win-debug emits 42 `ymm` references + 5 distinct 256-bit AVX FP ops; win-debug-scalar emits zero. Sandbox-always-built rule reaffirmed (saved as memory `feedback_sandbox_always_built.md`). Session log: `docs/sessions/2026-05-10-v0a-simd-substrate.md`.

### Earlier milestone

**2026-05-10 — Phase 3.0 v1p: Reserved-slot freeze (CLOSES Phase 3.0).** New `ScriptComponent` + `ScriptHandle` types under `crd/scene/script_component.hpp` (16 B / 8 B layouts pinned, ADR-0056 §2 — registers as a regular SparseSet component, not a trait). Reserved spatial DSL operators `Query<>::in_aabb(AABB)` + `Query<>::within_radius(center, radius)` shipped as passthrough no-ops behind the `SpatialBVHIndex` consumer (ADR-0053 §6) — chain-friendly today, start filtering when Phase 3.5 wires the BVH. New `tests/scene/test_phase_3_0_freeze.cpp` (5 cases): every reserved trait round-trips through `register_component`; auto-register fires every shell index in one call; `ScriptComponent` registers + spawns + reads back; spatial operators compile + chain + iterate; **API surface freeze** captured as compile-time `static_assert` pins on every public sizeof / alignof / FourCC / version + closed-enum value the cooker writes and the loader reads (Öbek info+descriptors, QualityPreset v2, CameraPreset v1, ProfileFileInfo, PredicateRecord, InheritPolicy, Replication, PredicateField, PredicateOp, ScriptHandle, ScriptComponent). Editing any pinned size/version is a deliberate schema break — the cooker's payload-size check turns it into `LoadState::Failed`. **Full 12-config sweep: 856/853 green** (Win × 7 + Linux × 5). Phase 3.0 retrospective in the session log. Session log: `docs/sessions/2026-05-10-v1p-reserved-slot-freeze.md`.

### Earlier milestone

**2026-05-09 — Phase 3.0 v1o3: sandbox integration (closes v1o).** Five layers: (1) `RenderMeshIndex` — new `IComponentIndex` that owns the GpuMesh store and evicts on `World::destroy(e)` via the L5 fan-out — the proper drop-callback hook v1o2 promised (no sandbox-side `release_owned` workaround). (2) `QualityPreset` v2 — `enable_depth_prepass` introduced by repurposing one byte of `_reserved` (layout still 144 B). (3) `ForwardRenderPath` implements `crd::preset::IPresetTarget`; `apply(QualityPreset)` skips depth-prepass DRAWS while keeping the FrameGraph pass declaration (topology invariant). (4) Three new cooker handlers (`.preset.toml`, `.profile.toml`, `.obek.toml`) registered with the asset cooker; profile cooker resolves bundle entries via `.meta` sidecars. (5) Demo content: `quality_default.preset.toml` + `camera_default.preset.toml` + `default.profile.toml` + `obek_demo.obek.toml`. The sandbox now owns a `crd::scene::World`; procedurals + imports both spawn entities (single render path); profile resolves at boot and the bundle drives `ForwardRenderPath` + a sandbox-side `SandboxCameraTarget`; ImGui exposes Quality + Camera L4 runtime overrides plus Öbek instantiate/override-translate/revert/unpack. Build infra: `wsl-build.ps1` redirects Linux build dirs to `~/cerid-build/<preset>` on native ext4 (drops 9P-bridge tax); `-Reconfigure` defaults OFF for fast incremental sweeps. AGENTS.md gained Agent Conduct rules (never silently reduce scope; hook-based contracts > explicit-call APIs; no dual demo/real paths). 2 new tests (RenderMeshIndex drop-callback + manual release). **Full 12-config sweep: 851/848 green (Win × 7 + Linux × 5).** Session log: `docs/sessions/2026-05-09-v1o3-sandbox-integration.md`.

### Earlier milestone

**2026-05-09 — Phase 3.0 v1o2: `UploadHandle` + async `GpuUploader` + `RenderUploadSystem`.** ADR-0061 Layer 2 + Layer 3. `UploadHandle` (move-only) in `crd-renderer` owning the in-flight Fence + staging buffers + recorded CommandBuffer + a pending `GpuMesh`/`GpuTexture`; `is_valid()` / `is_ready()` (non-blocking) / `wait()` / `take_mesh()` / `take_texture()`. `GpuUploader::upload_mesh_async` + `upload_texture_async` record-and-submit-with-fence (uses v1o1's `Queue::submit(cmd, fence)`) returning the handle. ECS-side `PendingMeshUpload { UploadHandle handle }` component + `RenderUploadSystem` running in `RenderExtract` phase: polls `(Renderable, PendingMeshUpload)` query, on `is_ready()` moves the GpuMesh into a per-system `HashMap<EntityId.raw, GpuMesh>`, patches the Renderable's vertex/index buffer pointers, calls `AsyncAwareIndex::mark_loaded`, and queues a `Commands::remove_component<PendingMeshUpload>`. The standard `query<...>().skip_pending<Renderable>()` filter automatically excludes pending entities until promotion. Synthetic-handle test pathway (no real Vulkan needed): 6 tests, 26 assertions. Pinned: handle is single ownership root; three states via two booleans (no enum, no Failed at this layer); `m_owned_meshes` keyed by `EntityId.raw` and grows monotonically until World destruction (sandbox-safe; v1o3 drives the per-entity cleanup contract). Side-fix: `scripts/wsl-build.ps1` now uses per-preset temp filename (was `.wsl-build-tmp.sh`, parallel streams collided). **Full 12-config sweep all green: 849/849 (846 in optimised configs).** Session log: `docs/sessions/2026-05-09-renderer-v1o2-upload-handle.md`.

### Earlier milestone

**2026-05-09 — Phase 3.0 v1o1: `crd::rhi::Fence` + non-waiting `Queue::submit(cmd, fence)`.** ADR-0061 Layer 1. New `Fence` interface in `crd-rhi` (`is_signaled` non-blocking / `wait` blocking / `reset` re-arm); `Device::create_fence()` factory; `Queue::submit(CommandBuffer&, Fence&)` non-waiting variant. Vulkan backend wraps `VkFence` via `VulkanFence` class (~50 LOC: `vkCreateFence` / `vkGetFenceStatus` / `vkWaitForFences` / `vkResetFences` / `vkDestroyFence`); `VulkanQueue::submit(cmd, fence)` calls `vkQueueSubmit(queue, &info, fence_handle)` with no `vkQueueWaitIdle`. Fake backend + downstream fakes (renderer test, smoke_rhi_api, smoke_renderer) updated to satisfy the new pure virtuals. **Three queue submit paths now coexist**: per-frame `submit(cmd, swapchain)` (existing), `submit_and_wait(cmd)` (existing one-shot), `submit(cmd, fence)` (new non-blocking). 5 tests (4 fake + 1 real Vulkan, headless-skipped). Pinned design choices: binary fence (not timeline semaphore), no transfer queue (reserved follow-up), no fence pool (plain factory; layer pool atop later if hot path appears). **Full 12-config sweep all green: 843/843 (840 in optimised configs).** Session log: `docs/sessions/2026-05-09-rhi-v1o1-fence.md`.

### Earlier milestone

**2026-05-09 — Phase 3.0 v1n FULLY DELIVERED.** Six sub-slices (v1n1 substrate / v1n2 QualityPreset / v1n3 CameraPreset / v1n4 resolver primitive / v1n5 Profile substrate / v1n6 Profile resolver + composition + context detection); 24 tests / ~280 assertions. ADRs 0059 (Preset) + 0060 (Profile) fully realised. Two new modules: `crd-preset` (10 headers + 4 sources; per-type registration grammar, IPresetTarget overload set, five-layer resolver) and `crd-profile` (9 headers + 3 sources; closed predicate schema, ProfileResolver with additive composition, context detection helpers for OS + CPU cores). Closing slice (v1n6): predicate eval over canonical signed-i64 widening (sign-preserving for integer fields), priority-stable-sort + concat for additive composition, GPU tier detection deferred to consumers (no RHI dep in leaf data module), `apply_profile_bundle` driver deferred to v1o3 sandbox bridge. **Full 12-config sweep all green: 838/838 (835 in optimised configs).** Closing session log: `docs/sessions/2026-05-09-profile-v1n6-resolver.md`.

### Earlier milestone

**2026-05-09 — Phase 3.0 v1n5: Profile substrate (ADR-0060).** New `crd-profile` module — peer of `crd-preset`, depends only on `crd-resources` (the apply-bundle driver bridging to `IPresetTarget*` lands in v1n6). Ships `ProfileContext` + 4 closed enums (`OperatingSystem` / `GpuTier` / `ProjectDomain` / `AppMode`) + closed predicate schema (`PredicateField` / `PredicateOp` / `PredicateRecord` 8 B pinned) + runtime `Profile` struct + `ProfileResource` + `ProfileLoader` (FourCC `'PROF'`) + `ProfileArtifactBuilder`. CRDR layout FINF + FRLE (rules) + FBND (bundles with rule_idx cross-link). v1n5 enforces 1 bundle per rule; resolver / additive composition / context detection land in v1n6. **Full 12-config sweep all green: 834/834 (831 in optimised configs).** Session log: `docs/sessions/2026-05-09-profile-v1n5-substrate.md`.

### Earlier milestone

**2026-05-09 — Phase 3.0 v1n4: five-layer resolver primitive.** `resolve_preset<T>(resource, runtime_override)` + `apply_preset<T>(target, resource, runtime_override)` template helpers in `preset_resolver.hpp` (header-only). Captures ADR-0059 §2's five-layer model: L0 schema default, L1+L2 fold into the cooked PDAT bytes (cooker pre-resolves extends chain at cook time, sharing the v1m3b Öbek resolver; PCHN already round-trips from v1n1), L3 reserved (Phase 4+), L4 caller-supplied full-T override (partial-field deferred to v1o3 sandbox quality slider). Stateless free-function design — no per-target caching at the resolver level; that's the target's responsibility per ADR. **Full 12-config sweep all green: Windows 7/7 + Linux 5/5 at 830/830 (827 in optimised configs).** Session log: `docs/sessions/2026-05-09-preset-v1n4-resolver.md`.

### Earlier milestone

**2026-05-09 — Phase 3.0 v1n3: `CameraPreset` second concrete type.** Schema struct (FourCC `'PRCM'`, version 1, 40 B layout pinned) with FOV / near / far / aperture / shutter / ISO / EV-comp / EV100 clamps / `LensModel` / `ExposureMode` enums. `IPresetTarget::apply(const CameraPreset&)` overload appended. **Partial-override convention pinned** in `preset_target.hpp`: derived targets that override only some apply overloads must `using IPresetTarget::apply;` to avoid C++ name-hiding rules (GCC's `-Woverloaded-virtual` enforces this; MSVC doesn't catch it — exactly what cross-platform CI is for). **Full 12-config sweep all green: Windows 7/7 (826/826 / 823 release / build-only configs clean), Linux 5/5 (826/826 / 823 release / build-only clean).** Session log: `docs/sessions/2026-05-09-preset-v1n3-camera-preset.md`.

### Earlier milestone

**2026-05-09 — Phase 3.0 v1n2: `QualityPreset` first concrete type.** Schema struct in `crd-preset` (FourCC `'PRQL'`, version 1, 144 B layout pinned via `static_assert`) with `shadow_resolution` / `msaa_samples` / `ssr_quality` / `ssao_quality` / `post_fx_count` / `post_fx[8]` (`ResourceId` slots). `IPresetTarget::apply(const QualityPreset&)` overload added to the base interface; targets opt in by overriding. **win-debug 823/823, linux-gcc-debug 823/823 (+4 cases / +38 assertions over the 819/819 v1n1 baseline).** Session log: `docs/sessions/2026-05-09-preset-v1n2-quality-preset.md`.

### Earlier milestone

**2026-05-09 — Phase 3.0 v1n1: Preset substrate.** New `crd-preset` module with `PresetResource` (PINF/PDAT/PCHN chunk model) + `PresetLoader` (per-type, runtime-configured by FourCC + schema_version + payload_size) + `PresetRegistry::register_type<T>()` (closed-by-C++-types, idempotent re-registration) + `PresetArtifactBuilder` (test-only public; cooker promotes later) + `IPresetTarget` empty base + `PresetApplyEvent`. No concrete types yet (v1n2 ships `QualityPreset`, v1n3 ships `CameraPreset`). **win-debug 819/819, linux-gcc-debug 819/819 (+5 cases, +38 assertions).** Session log: `docs/sessions/2026-05-09-preset-v1n1-substrate.md`.

### Earlier milestone

**2026-05-08 — Phase 3.0 v1m FULLY DELIVERED.** Twelve sub-slices, ~2700 LOC, 58 öbek tests across the v1m series. All five published v1m sub-slices closed: substrate (v1m1), override patches + OCHN (v1m2), full ObekCooker (v1m3a–d), InheritPolicy + DontInherit (v1m4), Inherit transparent CoW backend (v1m4b1–3), revert/unpack/enumerate + AAAA reservations (v1m5a–b). Hot-reload watcher + `obekc extract` CLI deferred to post-Phase-3.0 follow-up (task #108).

Closing session log: `docs/sessions/2026-05-08-scene-v1m5-revert-batch.md` (links the v1m1..v1m5 chain inline). Earlier in-phase milestones: `docs/sessions/2026-05-06-scene-v1a-slotmap.md` … `docs/sessions/2026-05-08-scene-v1l-cooker.md`.

## Test counts (post-v1p, 2026-05-10 — Phase 3.0 CLOSED)

| Config | Tests |
|---|---|
| win-debug | 856/856 |
| win-relwithdebinfo | 856/856 |
| win-release | 853/853 (3 debug-only `FiberState` tests gated `#if CRD_ENABLE_ASSERTS`) |
| win-asan | 856/856 |
| win-clang-cl | 856/856 |
| win-shipping | ✅ build clean |
| win-tidy | ✅ build clean |
| linux-gcc-debug | 856/856 |
| linux-gcc-relwithdebinfo | 856/856 |
| linux-gcc-release | 853/853 |
| linux-gcc-asan | 856/856 |
| linux-gcc-shipping | ✅ build clean |

12/12 configs green; 17/17 headless smokes per non-tidy/non-shipping Windows config.

## Open questions

- `crd-config` hot-reload remains 1.6b unless ImGui integration proves it should move earlier.

## Recently resolved decisions

- **2026-05-11 — `crd-geometry` supplement integrated.** Same-day extension to ADR-0076: research supplement at `docs/research/cerid-geometry-supplement.md` (6,116 words) covering Inigo Quilez's body of work (~30 analytic SDF primitives + smin operators + domain ops on iquilezles.org — directly maps to `-primitives` formulas) + Mercury hg_sdf + Hart 1996 / Wright 2015 / Frisken 2000 / Bloomenthal 1997 references; computational-geometry textbook foundation (5 NEW books beyond base: Preparata-Shamos, Edelsbrunner ×2, Goldman, Foley/van Dam, Ebert et al., Bloomenthal — anchoring each v0–v9 slice to primary textbook + paper); explicit per-sub-module SIMD strategy table (Vec4f/Vec8f lane-width choices) + performance budgets (BVH4 ~8ns/node, GJK 50–200ns/pair, Quickhull 100k pts <30ms, mesh closest-point <1µs/8-pt batch, QEM 1M→100k tris <3s, LBVH GPU 1M prims <8ms); `crd::containers` integration patterns per algorithm class (Array / ConstSpan / HashMap / sort / Soa with named per-algorithm allocator pairings); SIMD determinism pins (pairwise binary-tree reductions, lex-tiebreak argmax, integer SAH bin counts, ULP-budget shader-helper conformance); ADR-0076 extended with new sub-module `crd-geometry-shader-helpers` (cooker-emitted GLSL/HLSL primitive library mirroring C++ side, ULP-conformance-tested) + 4 new slices (v0e iq-formulary substrate + v1g BVH4 SIMD ray-vs-4-AABB + v4g per-leaf SIMD Möller-Trumbore + v9e GLSL/HLSL output side); 1 new open question (shader-helper ownership: `-geometry-shader-helpers` vs `-sdf-shader-helpers` deferred to Phase 3.1.5 close); totals bumped 25→29 slices, 14→15.8 KLOC engine + 4 KLOC cooker-emitted GLSL/HLSL, 4-6→5-7 months calendar.

- **2026-05-11 — `crd-geometry` substrate architecture (ADR-0076 ✅ Accepted) — Phase 3.1.7.** Standalone peer module to crd-math/crd-sdf/crd-hesap. 10 sub-modules over 25 slices / ~14 KLOC engine + ~5 KLOC editor / ~4-6 months. Multi-domain consumer matrix locked: eylem broadphase v1c + narrow v1d + mesh v1d-mesh + convex-conditioning; sdf v2 mesh-bake; renderer Phase 3.5+ frustum cull + occlusion BVH; scene SpatialBVHIndex (ADR-0053 reserved shell); audio Phase 3.4 raycasts; eylem-aero/cine; navmesh Phase 7; editor V-HACD pipeline. Determinism contract inherited from ADR-0063 with algorithm-specific tiebreaks pinned in §4 (GJK simplex Ericson-not-vandenBergen; SAH-split X-then-Y-then-Z; Quickhull lex order; Vatti Clipper2 vertex-on-edge convention). Two-layer API (typed C++ Eigen-class for engine + opt-in cooker/editor façade). Deferred-refactor pattern locked: eylem v1c+v1d + sdf v2 ship own narrow versions FIRST then refactor when 3.1.7 lands — same precedent as eylem v7 + hesap-iterative. Slot decision: 3.1.7 between hesap and Phase 3.2 animation (substrate-tier sequencing: physics → implicit geometry → numerics → explicit geometry). Industry survey across 30+ libraries (Bullet/PhysX/Jolt/Box2D v3/Havok/Unity DOTS/Unreal/Godot/CGAL/Geogram/libigl/GTE/Open3D/OpenMesh/MeshLab/PMP/Embree/nanoRT/Boost.Geometry/S2/FCL/HPP-FCL/OMPL/Clipper2/V-HACD/HACD/CoACD/TetGen/fTetWild/MeshFix/qhull). Research dossier: `docs/research/cerid-geometry.md` (11,523 words). ADR: `docs/decisions/0076-geometry-substrate-architecture.md`. Phase plan: `docs/phases/phase-3.1.7-geometry.md`.

- **2026-05-11 — clang-cl thin-LTO debt PAID.** Same-day follow-up to the shipping-config-hardening session. `win-clang-cl-shipping` re-enabled to `CMAKE_INTERPROCEDURAL_OPTIMIZATION=ON` (full thin-LTO across the entire shipping config). Bisected the miscompile by source file across all 5 files in `crd-resources` — sole trigger is `crdr.cpp`. Targeted fix: `set_source_files_properties(src/crdr.cpp PROPERTIES COMPILE_OPTIONS "-fno-lto")` in `engine/resources/CMakeLists.txt`, mirroring the existing `worker_pool.cpp + fiber_init.cpp` `/Od` isolation pattern in `engine/jobs/CMakeLists.txt`. Single-file compiler-bug isolation, not a global LTO disable. CRD_NOINLINE on crdr_read alone insufficient (verified). Bug is in clang-cl LLVM's LTO IR transforms over crdr.cpp's `std::memcpy + Array<u8>::resize/push_back + ZSTD_decompress` constellation; none individually problematic on MSVC LTCG or GCC LTO. All three shipping configs verified 1038/1038 PASS with full LTO ON. Debt entry removed from `docs/debt.md`. Session log: `docs/sessions/2026-05-11-clang-cl-thin-lto-debt-payment.md`.

- **2026-05-11 — Shipping config hardening (max opts + tests + sandbox + CI parity).** All three production-compiler shipping configs (MSVC `win-shipping`, clang-cl `win-clang-cl-shipping` NEW, GCC `linux-gcc-shipping`) now build the full test suite + sandbox AND run them in local sweep + GitHub CI. MSVC adds `/Gy /Zc:inline` on top of `/O2 /Gw /Zi /OPT:REF /OPT:ICF`. GCC/Clang adds `-ffunction-sections -fdata-sections -funroll-loops -Wl,--gc-sections` on top of `-O3 -g --build-id`. Determinism contract (`/fp:precise` + `-ffp-contract=off` + `-mfpmath=sse`, no `/fp:fast`/`-Ofast`) preserved per ADR-0063. Stack-buffer security (`/GS`, `-fstack-protector`) kept ON. `/Ob3` evaluated and rejected (no measurable speedup, extra LTO debug surface). Drive-by fixes: `CRD_NOINLINE` added to `run_load_job` + `run_stream_load_job` (same family as the documented MSVC LTCG `evict_block_locked` fix); `[[maybe_unused]]` on a Query loop var that's only used in CRD_ASSERT (compiles away in shipping). Verified locally on all 3 configs: 1038/1038 tests + sandbox-smoke PASS each. CI: added Test step to existing `windows-shipping` + `linux-gcc-shipping` jobs, added new `windows-clang-cl-shipping` job. Sweep: shipping moved out of `$buildOnly`; lane count bumped Win × 8 → Win × 9. Session log: `docs/sessions/2026-05-11-shipping-config-hardening.md`.

- **2026-05-11 — Phase 3.1 v1a-material substrate ✅ CLOSED (cluster a/b/c/d).** ADR-0069 §1-§11 fully realised in 4 sub-slices in one day: 64-byte `Material` struct + 6 friction models + 3 restitution models + `MaterialId` strong type + `GeometricMean` combine mode + `MaterialPool` on scene + `IPhysicsScene::create_material/update_material/material/has_material` + per-collider `Collider::material` MaterialId field + canonical 2-arg `add_collider(body, collider)` + non-virtual NVI 3-arg convenience overload + `derive_mass_properties(colliders, accessor) → DerivedMassProperties{mass, com_local, inertia_diagonal}` free function + `IPhysicsScene::derive_body_mass(BodyId)` virtual. Volume + inertia for sphere/box/capsule analytic; convex/plane/mesh/heightfield/sdf carry zero-volume in v1a (cooker pre-computes at v1d-mesh / v1d-hf / v1d-sdf + v1k). Two-pass walk: pass 1 mass + COM, pass 2 full 3×3 symmetric inertia tensor (rotated to body frame via R I R^T, parallel-axis-shifted to body COM, summed → diagonal extracted). ColliderId-stable summation order per ADR-0063 §4 fixed-position-write protocol. Cluster total ~860 LOC + 17 cases / 119 assertions; crd-eylem-tests at 236/31, win-debug ctest 1041/1041. Drive-by fix: `body_pool.cpp` + `collider_pool.cpp` `put_lane` Vec8f/Vec4f overload set replaced with single template<ColT> (clang-cl + scalar Lane=4 fallback both clean). Full 14-config sweep PASS (Win × 8 + Linux × 6). Session logs: `docs/sessions/2026-05-11-v1a-material-{a,b,c,d}.md`.

- **2026-05-10 — `crd-draw` substrate architecture (ADR-0066).** Standalone debug-renderer substrate as a peer module of `crd-renderer`, slipped into Phase 3.1 v1 between v1a (eylem interface) and v1b (eylem AoSoA storage) so v1c+ have visual debugging from day 1. Two-layer API: retained `RenderBuffer` (PhysX-style SoA, packed RGBA8 color, PrimFlags u32 with 3 depth modes + 7 categories + 16-bit picking_id reserved) + immediate-mode free functions. Vertex-shader quad-expanded AA lines (Three.js / Mapbox technique) — pixel-perfect at any line angle / thickness / camera distance. Sort-by-centroid translucent solids; WBOIT reserved for SDF cell viz Phase 3.1.5+. Three depth modes per primitive (Test / Always / XRay). Master scale (PhysX `eSCALE`). RGB triad joint convention (RViz / Maya). UV sphere wireframe + icosphere solid (recognisable structure vs vertex regularity). Per-component visualizer plug-in registry + companion-module pattern (`crd-eylem-viz` etc.) keeps substrate modules free of rendering deps via dependency-inverted plug-in. GLSL shaders cooked to SPIR-V via existing `crd-shader` pipeline — single-path, no embedded byte arrays. Helper-function `add_draw_overlay_pass(fg, color, depth, buffer)` grafts onto any IRenderPath. Profile-gated (dev = ON, shipping = early-out, zero CPU overhead). Replay-friendly retained buffer serializes naturally per ADR-0063. **Consumed by 7+ planned modules:** eylem v1c+ (bodies + AABBs + contacts + joints + ragdolls), `crd-sdf` (Phase 3.1.5 — narrow-band cells + isosurface + CSG), `crd-audio` (Phase 3.4 — acoustic ray probes), navmesh (Phase 7), editor (Phase 7 — selection + manipulators + picking), renderer (Phase 3.5+ — light bounds + frustum + clusters + BVH), sandbox (already shipped — per-entity inspection). Research dossier: `docs/research/cerid-draw.md` (industry survey). Phase plan rows: `docs/phases/phase-3.1-eylem.md` v1a-draw d0–d4.

- **2026-05-10 — `crd-hesap` numerical computing substrate (ADR-0065).** MATLAB-class numerical substrate as a peer module to `crd-math` (which stays lean). Sequential successor to Phase 3.1 — ships post-eylem-v9 as Phase 3.1.6 (~6–8 months, 18 slices, 14 sub-modules). Covers dense + sparse + iterative + direct + eig + opt + ODE + FFT + DSP + stats + tensor + autodiff + GPU + REPL. Two-layer API: Eigen-class typed C++ (engine code) + MATLAB-class facade (REPL/scripting). Inherits ADR-0063 determinism contract wholesale; reuses ADR-0061 async upload for GPU sub-module; reuses ADR-0034 C ABI pattern for plug-in surface. Underwrites MATLAB-class scientific tool ambition. Eylem v7 (FEM) ships narrow internal PCG meanwhile; refactors to use `crd-hesap-iterative` later. Decision explicitly chosen as separate module (NOT bloating `crd-math`) because: bloating `crd-math` would slow every dependent module's compile time, violate module isolation, conflate two different testing tiers, and undersell the MATLAB-tool ambition.

- **2026-05-10 — `crd-sdf` substrate architecture (ADR-0064).** Standalone signed-distance-field module consumed by eylem (mesh colliders + closest-point), font (MTSDF), renderer (DFAO/DFGI in 3.5+), audio (acoustic occlusion in 3.4), editor (CSG modelling in 7). Module split: analytic + dense + narrow-band + CSG-tree backends behind one `ISdf` interface. Mesh→SDF baker uses Jacobson 2013 generalised winding number for robust signs on non-watertight meshes. Inherits ADR-0063 determinism contract; reuses ADR-0061 async upload via new `upload_sdf_async`. Slots as Phase 3.1.5 between eylem v2 (rigid 2D done) and eylem v3 (XPBD soft uses SDF colliders).

- **2026-05-10 — Eylem physics architecture (ADR-0062 + ADR-0063).** Cerid builds physics native from day 1; PhysX wrap step abandoned. Module split: `crd-eylem` substrate + rigid3d/rigid2d/soft/articulation/vehicles/ccd/fem/gpu/diff sub-modules. Determinism baked in via FP contract + Cerid-internal trig/sort/hash + commutative cross-thread merges + id-stable iteration (ADR-0063). v1 ships SI rigid solver with snapshot/replay; v3 ships XPBD soft/cloth/rope. ADR-0018 superseded; Phase 6 (native physics) folded into Phase 3.1.

- **Coverage audit completed 2026-05-11 (8,604 words):** Identified 7 real gaps blocking the multi-domain mandate. Fill plan: ADR-0069 / 0075 ship now (Wave 1, P0); ADR-0071 / 0073 ship at v6 cluster (Wave 2, P1, blocks robotics + aerospace claims); ADR-0070 / 0072 / 0074 + tuning cookbook ship post-v1 (Wave 3, P2). ~6 additional months on top of existing Phase 3.1 plan. Without these fills Phase 3.1 closes a games-engine-equivalent substrate; with them Cerid genuinely matches MuJoCo + Drake + PhysX + Bullet + Houdini Vellum + Maya nDynamics + GMAT/Orekit domain-by-domain. Audit dossier: docs/research/cerid-eylem-coverage-audit.md.

- **Eylem materials substrate locked 2026-05-11 (ADR-0069 ✅ accepted):** 64-byte cache-line `Material` struct (one cache line; locked at v1a freeze). 6-value `FrictionModel` (Coulomb / Stribeck / LuGre / Karnopp / Anisotropic / FrictionTriple — last reserved for v5 vehicles' MuJoCo-style sliding/torsional/rolling triple); 3-value `RestitutionModel` (Constant / Newton / HuntCrossley); 5 combine modes (Average / Min / Max / Multiply / **GeometricMean** — new; Box2D v3 / Jolt / Unity DOTS / AGX consensus default for friction); per-collider material via `MaterialId` handle (PhysX / Jolt pattern; content-addressed FNV-1a-64 like FieldId); LuGre per-contact bristle state folded into ADR-0068 §8 contact warm-start cache (16B lazy alloc); Tustin discretisation with cooker-precomputed division-free constants; AGX-style per-pair `ContactMaterial` REJECTED for v1; 8 shipped default materials (Default/Rubber/Steel/Ice/Wood/Concrete/Water/Flesh); cooker artifact `'EMAT'` + `MaterialLibrary` `'EMLB'`. Industry survey 9,650 words across 16 engines + tools (PhysX, Bullet, Jolt, Box2D v3, Unity classic + DOTS, Unreal Chaos, Godot, MuJoCo, Drake, IsaacSim, Project Chrono, AGX, Maya, Houdini Vellum, Vortex Studio, Blender). 7 v1a-critical-path sub-slices (v1a-material-a/b/c/d + v1e-material + v1k-material-cooker + v1k-material-bench) + 8 deferred sub-slices (v5 vehicles / v7 FEM / v8d MPM / post-v1 destruction) — same blocked-sub-slice discipline as ADR-0067 (Gradient/Script). Research dossier: docs/research/cerid-eylem-materials.md.

- **Eylem testing rigor locked 2026-05-11 (ADR-0075):** Six test categories — conservation-law CI (energy / momentum / angular momentum / constraint violation), closed-form regression (pendulum / projectile / SHO / Kepler / SHM / drag terminal / rolling), long-duration drift (60 min, snapshot-hash rate-bounded), cross-engine comparison (Box2D + Bullet test-only deps; "within 2× best-of-three" elite-tier bar), property-based (random scenes via Catch2 generators), stress (1k stack / 30-link humanoid / 100k particles). Slots split v1l + v9b. ~1450 LOC + ~30 tests + 5 benches + nightly CI. P0 — must close before v1l (without it the scientific-computing domain claim is unsubstantiated).

- **Reserved-slot ADRs (Wave 2/3, mint at slice time):** ADR-0070 solver catalog (Planned, v6d Nonsmooth Newton + selection guidance doc); ADR-0071 robotics importers + motor catalogue (Planned, v6c URDF/SDF/MJCF + v6c-actuators); ADR-0072 sensor substrate (Planned, v6e IMU/LIDAR/proximity/threshold/diagnostic sensors); ADR-0073 aerospace substrate + new crd-eylem-aero module (Planned, v6f variable-mass / aero forces / US Std Atm 1976 / propulsion / multi-body separation + v1f-fields-j J2 reservation); ADR-0074 cinematic-bridge + new crd-eylem-cine module (Planned, v4d animation curves driving kinematic + pre-roll + per-shot overrides + slow-motion substep). Each ships when its dossier completes; phase plan rows + ADR README slots are reserved now (Planned status).

- **2026-05-09 — Async GPU upload contract (ADR-0061).** Three-layer contract: `crd-rhi` `Fence` primitive, `crd-renderer` `UploadHandle` + `PendingMeshUpload` + `RenderUploadSystem`, `crd-scene` unchanged. Closes the open question carried in the phase doc since v1o was first sketched. Implementation lands as v1o1 + v1o2; sandbox migration is v1o3.

## Pointers (lazy-load reference)

Agents: don't read everything. Use these breadcrumbs.

- **Hub:** `docs/ROADMAP.md` (small navigation page; safe to read fully)
- **Principles:** `docs/PRINCIPLES.md` (read every session, short)
- **Active phase only:** `docs/phases/phase-3.0-scene-ecs.md`
- **Other phases:** `docs/phases/phase-<X>.md` (read ONLY when relevant)
- **Specific decision:** `docs/decisions/<NNNN>-<slug>.md` (find via `docs/decisions/README.md` tag index)
- **Last session detail:** the single file linked in "Last shipped milestone" above, not the whole `docs/sessions/` folder
- **Module overview:** `docs/systems/<module>.md` (when working on that module)
- **Module deep-dive:** `docs/<module>/<MODULE>_FILE.md` (only when doing surgery)
- **Open debt:** `docs/debt.md`
- **Detour queue + rules:** `docs/detours/README.md`

When in doubt, ASK before reading large files.

## Session log (rolling, last 5)

- **2026-05-13** — Phase 3.1.7 v1h (`crd-geometry-primitives` hardening, ADR-0076 §15: `constants.hpp` geometry epsilon/tolerance policy named by intent [value-preserving — `bvh_build_internal.hpp`'s `k_sah_cost_epsilon` + `robust_ray_aabb.hpp`'s `ray_aabb_robust_pad` retrofitted]; `is_finite.hpp` `is_finite`/`all_finite` + the NaN/Inf contract [queries tolerate, builders reject in debug — asserts in `bvh_build`/`bvh_build_parallel`/`DynamicBvh::insert`/`update`/`bvh4_collapse`]; `signed_distance.hpp` 20 iq analytic SDFs in C++ [3D 12 + 2D 8, sqrt/abs/min/max only]; `ConvexHullView<T>` + `support`/`contains`; 4 new test files; `crd-geometry-primitives-tests` 103 cases; green win-debug/win-asan/win-shipping/win-tidy + guards). `docs/sessions/2026-05-13-geometry-v1h-primitives-hardening.md`.
- **2026-05-13** — Phase 3.1.7 v1g (`crd-geometry-bvh`: `Vec4f` ray-vs-4-AABB kernel `ray_vs_4_aabb` in `bvh4_simd.hpp`/`.cpp`; `bvh4_raycast` switched to it [bit-identical to the 4 scalar slab tests]; BVH4 = recommended traversal form; `BvhTopology`/`topology` removed; lane-by-lane kernel test). **`-bvh` sub-module complete (v1a–v1g).** `docs/sessions/2026-05-13-bvh-v1g.md`.
- **2026-05-13** — Phase 3.1.7 v1f (`crd-geometry-bvh`: `bvh_build_parallel` — jobs-parallel binned-SAH build, bit-identical to serial; serial kernels extracted to `bvh_build_internal.hpp`; **fixed a latent `aabb_merge` empty-box bug** that corrupted merges of empty bins; Embree-comparison `[!benchmark]`; `BvhJobsListener`; 4 cases). `docs/sessions/2026-05-13-bvh-v1f.md`.
- **2026-05-13** — Phase 3.1.7 v1e (`crd-geometry-bvh`: `bvh_closest_point` — branch-and-bound closest-point query over the binary tree, `max_dist` cutoff, squared-space; 5 cases) + ADR-0076 §15 roadmap amendment (3 new geometry slices v1h/v1i/v1j from the user's domain checklist). `docs/sessions/2026-05-13-bvh-v1e.md`.
- **2026-05-13** — Phase 3.1.7 v1d (`crd-geometry-bvh`: `Bvh4Tree` — 4-wide topology variant; `bvh4_collapse` widens a built binary tree into ≤4-child nodes + `bvh4_raycast`/`bvh4_overlap` scalar; same results as the binary tree; 5 cases). `docs/sessions/2026-05-13-bvh-v1d.md`.
- **2026-05-13** — Phase 3.1.7 v1c (`crd-geometry-bvh`: `DynamicBvh` — incrementally-updatable AABB tree, fat AABBs + parent ptrs + free list, insert/remove/update with Box2D-style height-balanced rotations, fat-margin no-op, query/raycast broadphase; 7 cases incl. a 4000-op random sequence). `docs/sessions/2026-05-13-bvh-v1c.md`.
- **2026-05-13** — Phase 3.1.7 v1b (`crd-geometry-bvh`: `bvh_refit` — O(n) bottom-up bounds recomputation, topology untouched; new `bvh_update.hpp` + internal `aabb_ops.hpp`; 4 cases). `docs/sessions/2026-05-13-bvh-v1b.md`.
- **2026-05-13** — Phase 3.1.7 v1a (`crd-geometry-bvh` sub-module: `BvhTree` + binned-SAH binary builder + ordered robust raycast + AABB-overlap query; functional API form; ADR-0076 §5.2 SAH-split-tiebreak pinned; deterministic-replay test; 14 cases; new module + `crd-no-std-math-check` scope; verified win-debug/win-asan/win-shipping/win-tidy per the in-flight `-bvh` directive). `docs/sessions/2026-05-13-bvh-v1a.md`.
- **2026-05-13** — Phase 3.1.7 v0b→v0f (`crd-geometry-primitives` v0 sub-phase, CLOSED): 2D peer set + dimension-suffix naming rule + closest-point catalogue (v0b); intersection corpus + `Cylinder3`/`Cylinder2` (v0c); `Tetrahedron` + barycentric/prism-decomposition (v0d); iq formulary + `reduce_argmax_with_lex_tiebreak` + `crd-geometry-shader-helpers` skeleton (v0e); cutting-edge/branchless/SIMD ray corpus — Woop watertight ray-tri, Baldwin-Weber affine, Williams/Ize robust ray-AABB, Plücker, `Vec8f` batch kernels (v0f). Sweep-driven fixes: GCC `-Werror=shadow`, non-ASCII `TEST_CASE` names breaking Windows ctest, std-math-comment false-positives. Full 17-config sweep PASS. `docs/sessions/2026-05-13-geometry-v0b-v0f-v0-close.md`.
- **2026-05-12** — Phase 3.1.7 v0a (`crd-geometry-primitives` module + v0 type catalogue + the ADR-0076 §13 `crd::math::geometry` move-and-delete; `crd-math` now lean; new edge `crd-scene → crd-geometry-primitives`). `docs/sessions/2026-05-12-v0a-geometry-primitives.md`.
- **2026-05-10** — Phase 3.0 v1p (reserved-slot + API surface freeze; CLOSES Phase 3.0). `docs/sessions/2026-05-10-v1p-reserved-slot-freeze.md`.
- **2026-05-09** — Phase 3.0 v1o3 (sandbox integration; RenderMeshIndex drop-callback hook + ForwardRenderPath as IPresetTarget + 3 cookers + demo content; closes v1o). `docs/sessions/2026-05-09-v1o3-sandbox-integration.md`.
- **2026-05-09** — Phase 3.0 v1o2 (UploadHandle + async GpuUploader + RenderUploadSystem; ADR-0061 Layer 2 + Layer 3). `docs/sessions/2026-05-09-renderer-v1o2-upload-handle.md`.
- **2026-05-09** — Phase 3.0 v1o1 (`crd::rhi::Fence` + non-waiting `Queue::submit(cmd, fence)`; ADR-0061 Layer 1). `docs/sessions/2026-05-09-rhi-v1o1-fence.md`.
- **2026-05-09** — Phase 3.0 v1n6 (Profile resolver + additive composition + context detection; closes v1n). `docs/sessions/2026-05-09-profile-v1n6-resolver.md`.
- **2026-05-10** — Phase 3.1 v0a (SIMD wrappers + CrdSimd.cmake + scalar-parity preset + AVX2-emission CTest). `docs/sessions/2026-05-10-v0a-simd-substrate.md`.
- **2026-05-10** — Phase 3.1 v0b (Soa AoSoA container + iteration + gather/scatter). `docs/sessions/2026-05-10-v0b-soa-substrate.md`.
- **2026-05-10** — Phase 3.1 v0c (`crd::math::deterministic` Cephes-style transcendentals + `crd-no-std-math-check` CI guard). `docs/sessions/2026-05-10-v0c-deterministic.md`.
- **2026-05-10** — Phase 3.1 v0c-debt-A paydown (5 debt items closed same-day: f64 overloads + special functions + hyperbolic + expm1/log1p + Vec4f/Vec8f branchless SIMD batching with Vec4i/Vec8i/convert.hpp infrastructure). `docs/sessions/2026-05-10-v0c-debt-A-paydown.md`.
- **2026-05-10** — Phase 3.1 v0d (deterministic sort + heap substrate + `crd-no-std-sort-check` CI guard). `docs/sessions/2026-05-10-v0d-sort-substrate.md`.
- **2026-05-10** — Phase 3.1 v0e (SIMD benchmark harness; CLOSES Phase 3.1 v0 substrate). `docs/sessions/2026-05-10-v0e-bench-harness.md`.
- **2026-05-10** — Phase 3.1 v0 substrate closure dossier (comprehensive retrospective + handoff for v1a; substrate inventory, architectural decisions, every bug surfaced + fixed, perf numbers, pitfalls, extension recipes — read this BEFORE starting v1a). `docs/sessions/2026-05-10-v0-substrate-closure.md`.
- **2026-05-10** — Phase 3.1 v0 post-mortem: real Definition-of-Done sweep surfaced LNK1257 (C4714 LTCG drift) + Quatf #151 ctest mojibake (UTF-8 `°` vs Windows ACP argv) that the bench-only sweeps missed. Both fixed; new CI guard `crd-no-non-ascii-test-names` added; global `/wd4714` on `crd-warnings`; new `scripts/full-sweep.ps1` for one-command DoD sweep. v0 verified clean across all 14 build steps. `docs/sessions/2026-05-10-v0-postmortem-c4714-and-utf8-argv.md`.
- **2026-05-10** — `crd-draw` substrate architecture locked (ADR-0066) + research dossier written. Slipped into Phase 3.1 v1 between v1a and v1b as `v1a-sandbox-smoke` + `v1a-draw` (5 sub-slices d0-d4, ~7-9 days, ~2500 LOC, ~30 tests). Industry-survey research agent + Cerid-audit research agent both delivered; synthesis into `docs/research/cerid-draw.md` + `docs/decisions/0066-draw-substrate-architecture.md`. v1 grew from 12 to 18 slices.
- **2026-05-10** — Phase 3.1 v1a: `crd-eylem` interface module shipped — `IPhysicsScene`, `RigidBody`, `Collider` (Sphere/Box/Capsule/ConvexHull/Plane variant), `Material`, `Joint`, `PhysicsConfig`, strong-typed `BodyId`/`ColliderId`/`JointId`, null impl that links + round-trips add/has/remove. 15 tests / 80 assertions covering API freeze (sizeof + alignof + enum value pins) + behaviour smokes. Win × 8 sweep all green (988/988); Linux × 6 source compiles clean. 4 CI guards auto-fire on `engine/eylem/**`.
- **2026-05-10** — Phase 3.1 v0f: `Mat4<f32> operator*` SIMD-routed via new `mat_simd_f32.hpp`. Non-template overload wins for exact `Mat4<f32>` match; other `Mat4<T>` instantiations unchanged. **12.7× measured speedup**. Bit-exact parity with scalar reference verified. Doc-comments added to `vec.hpp` + `quat.hpp` explaining why per-instance Vec3f/Quatf are intentionally NOT SIMD-ified (Bullet btVector3 cautionary tale; eylem batched workloads use `simd::Soa<TChunk>` AoSoA columns instead). Phase 3.1 v0 grew from 5 to 6 slices (v0a-v0f).
- **2026-05-10** — Eylem allocator strategy doc shipped (`docs/systems/eylem-allocators.md`). No new allocators created — Cerid already ships the full physics-allocator toolkit (linear / stack / pool / growable_pool / tlsf / malloc) on parity with PhysX / Jolt / Box2D / Havok. v1b adds `PhysicsConfig::persistent_alloc` (TLSF) + `solver_scratch` (LinearAllocator) slots; per-fiber scratch uses existing `crd::jobs::frame_alloc()` (lock-free, 1 MB/thread).
- **2026-05-10** — Phase 3.1 v1a-sandbox-smoke shipped: `crd-sandbox --smoke-test [duration_seconds]` boots normally, runs main loop N seconds (default 3.0), exits 0 on clean shutdown / 2 if 0 frames presented (swapchain failure detector). Verified on win-debug: 354 frames / 2.0s / 177 fps, clean. `scripts/full-sweep.ps1` extended with `-SkipSandboxSmoke` + `-SandboxSmokeDurationSeconds` params; runs sandbox-smoke after each Win ctest (6 configs), treats non-zero as SANDBOX-SMOKE-FAIL. Skipped on win-tidy / win-shipping (build-only) + Linux (WSL2 GPU passthrough brittle). Catches Vulkan validation + resource init order + profile/preset apply-cycle + render-path runtime bugs ctest can't. Side-fix: ASCII-only enforced in inline PS strings (em-dash mojibake bit us via the same UTF-8/ACP gotcha as the Quatf #151 ctest issue from the v0e post-mortem).

> Older entries: `docs/sessions/` (one file per session, chronologically named).
