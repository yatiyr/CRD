# Cerid — Live Context

> Short-term memory: "where are we now?" The master plan lives in `docs/ROADMAP.md`.
> This is a DASHBOARD, not an append-only changelog. Each milestone goes to a session log (`docs/sessions/YYYY-MM-DD-*.md`); "Last shipped milestone" below just summarises and points there. The session-log directory itself is the chronological index, not this file.

---

## Current focus

**As of 2026-05-27.** 🎉 **Phase 3.1.6 `crd-hesap` v4 (iterative solvers + preconditioners + AMG) — CLOSED (v4z shipped).** All Krylov solvers (v4a–v4f), all preconditioners (v4g–v4j incl. ILUPACK-class multilevel ILU), and the full AMG cluster (v4k-a..e) shipped, tested, cross-config green; v4z closed the cluster (audits + reorder default-ON + robustness fix + ADR-0065 §26 lock D(iter)-1..10). **The complete sparse-iterative substrate beats Eigen on its overlapping surface and stands apart on the families Eigen lacks (Schwarz/SPAI/Chebyshev/AMG/multilevel-ILU), with the cross-thread determinism moat held end-to-end.**

**▶ RESUME HERE (fresh context):** **Streaming-allocator sub-phase (ADR-0085, Phase 2.2) — runs BEFORE hesap v5 (user-directed 2026-05-27).** Build the open-world memory substrate: ✅ S1 vm API → ✅ S2 VirtualMemoryAllocator → ✅ S3 GrowableTlsf-on-VM (malloc-free heap) → ✅ S4 RingAllocator (epoch-gated staging) → ✅ S5 StreamingAllocator (policy layer) → ✅ S6 OffsetAllocator + Vulkan GpuAllocator (VkDeviceMemory suballocation) → ✅ S7 GPU defrag + residency (full: buffer+image defrag + auto-pressure eviction; pluggable policies) → **S8 NEXT (CLUSTER CLOSE: ADR lock, docs/systems overview, 18-config full sweep, no-leak + determinism gates)**. GPU built now per explicit eyes-open user override. **Design dossier (deep research, all 7 slices): `docs/research/cerid-streaming-allocators.md`.** Plan: `docs/phases/phase-2.2-streaming-allocators.md`; ADR-0085.
- **S1 ✅** — `crd::vm` reserve/commit/decommit/protect (relocated from crd-platform to the **new `crd-vm` leaf** at S2; ADR-0085 §1 amendment).
- **S2 🟡 (impl done 2026-05-27, CI sweep pending)** — `crd::memory::VirtualMemoryAllocator`: stable-address **bump arena** over `crd::vm` (64 GiB default reserve, 64 KiB commit blocks, `deallocate`=no-op, `mark`/`reset_to`/`reset`/`purge`/`reset_and_purge`, in-place top-`reallocate`, `try_allocate` graceful-OOM, ASan poison-the-gap). Page-source parent for S3. 14 vma + 11 vm tests + 2 smokes. **Local: win-debug + win-asan + win-tidy + win-clang-cl green, ctest 41/41 incl. no-malloc guard;** gcc/shipping/release/18-config → CI.
- **S3 🟡 (impl done 2026-05-27, CI sweep pending)** — `GrowableTlsfAllocator` re-parented onto `VirtualMemoryAllocator` = malloc-free general heap with stable addresses. **Solved the try-contract gap in-slice (NO debt):** `try_allocate` promoted to a `virtual` on `IAllocator` (vtable-appended; default→`allocate`; Malloc/TLSF/VM/Growable override) + `grow()` uses `parent->try_allocate` with a non-owning TLSF, so VM exhaustion → `nullptr` not fatal. +2 tests. **Local: win-debug (88 memory cases) + asan + tidy + clang-cl green;** gcc/shipping/release/18-config → CI.
- **S4 🟡 (impl done 2026-05-27, CI sweep pending)** — `RingAllocator`: thread-safe epoch/fence-gated FIFO staging. **N-epochs-in-flight** model — lock-free `try_claim` (atomic head CAS, multi-producer), `begin_epoch`/`retire`, wrap padding, `u64` timeline-fence token (rhi-free), VM-backed buffer, pure `std::atomic` (WASM-friendly). Lib stays jobs-free; concurrency test runs through `crd-jobs parallel_for` (per user directive — use the engine's job system, NOT std::thread). 7 tests (incl. 32k-claim disjoint race). **Local: win-debug + asan + tidy + clang-cl green;** gcc/shipping/release/18-config → CI.
- **S5 🟡 (impl done 2026-05-27, CI sweep pending)** — `StreamingAllocator` (crd-memory): composes VM resident reservation + GrowableTlsf resident store (real per-payload free) + RingAllocator staging. Per-`CategoryId` soft budgets + injected `IResidencyPolicy` (NullResidencyPolicy default); pressure protocol (evict outside the mutex → retry → graceful nullptr). Resident path mutex-guarded, staging lock-free. **ResourceManager wiring deferred to Phase 2.7's first real streaming consumer (NOT debt — consumer-known shape).** 7 tests. **Local: win-debug + asan + tidy + clang-cl green;** gcc/shipping/release/18-config → CI.
- **S6 🟡 (impl done 2026-05-27, CI sweep pending)** — **`crd::memory::OffsetAllocator`** (O(1), external-metadata offset allocator — the VRAM-capable kernel; 769K-assertion stress) + Vulkan **`GpuAllocator`** replacing v1e's one-alloc-per-resource: per-`(memoryTypeIndex, linear)` block pools suballocated via OffsetAllocator, dedicated ≥16 MiB, persistent-mapped host-visible, nonCoherentAtom-aligned, granularity-safe via linear/non-linear pool split. `create_buffer`/`create_image` cut over (swapchain untouched; blocks freed before `vkDestroyDevice`). 4 ValidationCapture GPU tests (0 errors) + full rhi-vulkan suite (20) + bootstrap smoke green. **Local: win-debug + asan + tidy + clang-cl;** gcc/shipping/release/18-config → CI.
- **S7 🟡 (impl done 2026-05-27, CI sweep pending)** — GPU defrag + residency, **full build** (user-directed). Pluggable `IDefragPolicy`/`IResidencyPolicy`/`IGpuResidencyContext` (backend-neutral, `crd/rhi/gpu_residency.hpp`, null defaults). Defrag: registry → recreate+transfer-copy+swap+generation+`on_relocated` (outside mutex), idle-gated one-submit; **buffers + images** (subresource-correct copy, layout transitions, view recreate, restore layout). Residency: device↔host relocation + **auto-pressure eviction loop** in `sub_allocate` (S5 pattern, soft device-local budget) + `make_resident`. 5 ValidationCapture GPU tests (data round-trips, 0 validation errors, budget-bounded auto-evict) + full suite (25) + smoke. **Local: win-debug + asan + tidy + clang-cl green;** gcc/shipping/release/18-config → CI.
- **▶ S8 NEXT — CLUSTER CLOSE:** lock ADR-0085 D(mem-stream)-1..7, `docs/systems/` overview, **18-config full sweep** (`scripts/full-sweep.ps1`), cluster no-leak + determinism gates. Then restore RESUME → **hesap v5 (sparse-direct factorization)**.

**THEN v5 — sparse-DIRECT factorization** (supernodal Cholesky + Gilbert-Peierls LU + multifrontal QR + LDLT + HSS reserve + complex + CLI), then eylem v1c.

**Also done 2026-05-27 (before this sub-phase):** project-wide no-malloc sweep COMPLETE — every MallocAllocator usage purged from consumer code (22 hesap benches + 11 smokes + 27 tests + 6 engine consumers) → crd Array/String + TlsfAllocator/GrowableTlsfAllocator + crd types + crd-native file I/O; new `crd::memory::GrowableTlsfAllocator` (unbounded pooled, +5 tests); `crd-no-malloc-allocator` ctest guard (ps1+sh). Static-destruction-order gotcha fixed via never-destroyed placement-new pools. See `project_no_malloc_sweep_before_v5`.

**Closed phases:** v0 (dense BLAS + direct solvers), v1 (sparse storage + spmv/spgemm/SDDMM + BSR/ELL/DIA + MM I/O), v2 (fill-reducing reorderings + symbolic factorisation), v3 (SVD + symmetric/Hermitian + non-symmetric eig + least-squares — all beating Eigen + LAPACK).

**v4 status:**
- **v4a–v4h CLOSED** — Krylov family (CG/PCG/FGMRES/BiCGSTAB/MINRES/SYMMLQ/LSQR/LSMR/QMR/IDR(s)) + recycling (GCR/GCROT/RMINRES) + block-Krylov + inner-Krylov-as-preconditioner + IC(0)/ILU(0)/ILU(p)/ILUT.
- **v4i CLOSED (2026-05-26)** — SPAI + FSPAI (+ AMD-reorder adapter) · Chebyshev polynomial · Additive/Restricted Schwarz. The matrix-free / naturally-parallel / domain-decomposition families Eigen lacks.
- **v4j — multilevel ILU (ILUPACK-class): v4j-1a MC64 ✅ · v4j-1b scaffold ✅ · v4j-2a Crout core ✅ · v4j-2b recursion ✅ · v4j-2c complex+adjoint+CLI ✅ · v4j-3 CHARACTERIZED perf-wall (not grinding).** `InverseBasedIlu<T>` = bi-index Crout LDU + CMSW inverse-norm estimator + κ-bounded accept/defer + S-version Schur factored RECURSIVELY (recursive `InverseBasedIlu` leaf → exact `DenseLuLeaf` dense-LU base at n≤64) + eq.(20) apply. 4 tests/514 asserts on MSVC+gcc-Werror+clang-cl+guards (incl. genuine >2-level semi-coarsening + determinism). Pins D(mlilu)-1..6 locked. **ILUPACK V2.4 local-only apples-to-apples oracle** (`CRD_BUILD_HESAP_VS_ILUPACK`, WSL, gitignored `external/`; `scripts/setup-ilupack-ref.sh`) + `bench_hesap_mlilu_vs_ilupack` (byte-identical in-process matrix, matched true residual). **Honest bench (cd2d, condest=5):** recursion engages + solves but stays 2 levels / mesh-DEPENDENT iters 118→387 vs ILUPACK 3-5 lvl / flat 7→10. **κ×droptol sweep proved the crush is NOT one-knob** (fill not the lever; depth alone not the lever) — v4j-3 deepening resolved as a characterized perf-wall (consumer-pulled only). The convection crush lives in v4k AMG, not v4j (v4k-a-2 finding).
- **v4k-a SHIPPED 2026-05-26 — Smoothed-Aggregation AMG 🎉 CRUSHES ILUPACK on diffusion.** `crd-hesap-amg` module (`SaAmg<T>` : LinearOp): strength→aggregation→smoothed prolongator→Galerkin `PᵀAP`→V-cycle (Gauss-Seidel smoother, dense-LU coarsest). **Mesh-independent** (2D Poisson AMG-CG 8→10 iters flat across n) — the property v4j's inverse-based ILU fundamentally lacked (root cause: single-pivot deferral ⇒ degenerate coarse space; proven via Bollhöfer-Mehrmann 2001 + complete-LU/diagonal-Schur experiments). **vs ILUPACK V2.4 (matched true residual, full mesh-sweep, GS ν=2 default): isotropic Poisson Cerid 8→10→10 iters (flat) / 1.5–11.6ms factor / 1.36 fill vs ILUPACK 12→18→26 (GROWING — not mesh-indep!) / 13–100ms / 3.6 — 8.7× faster factor, 4× solve, 2.6× lower fill. Anisotropic diffusion (eps=0.01 β=0) Cerid 7→7→9 vs 11→17→23, 2× factor & solve. Mild convection β=0.05 also crushes (8 vs 13 iters, 3.5× factor). The crush, on AMG's home turf.** Smoother is an enum `{GaussSeidel (default, robust), Ilu0 (opt-in)}` — Ilu0 gives the fewest diffusion iters (4–5) but DIVERGES on strong convection, so GS ships default. 5 tests/268 asserts MSVC+gcc+clang-cl+guards. Pins D(amg)-1..5.
- **v4k-a-2 SHIPPED 2026-05-26 — convection probe (honest, all measured):** **(1) MC64 is a VERIFIED no-op on cd2d** — wired `Mc64Mode::{None(default),TopLevel,EveryLevel}` into `InverseBasedIlu`; diagnostic proved `dr=0.495` uniform, identity perm (constant-coefficient grid is already I-matrix-like ⇒ B=scalar·A). Premise falsified; MC64 wiring kept as correct infra (8 tests green). **(2) W-cycle (`cycle_gamma`=2) is a per-problem lever, NOT a robust default** — ties ILUPACK iters + crushes wall 5–7× on strictly-dominant cd2d β=0.1 (V 17→W 9), but DIVERGES on conservative zero-row-sum cd2d (V=12, W=2000); V stays default, W exposed. **(3) β=0.3 strong convection is a genuine SA-AMG wall** (W byte-identical 70/108/112) → robust answer = **v4k-b AGMG K-cycle** (Krylov stabilizes the W divergence). **Cerid's AMG beats Cerid's InverseBasedIlu on convection at every (β,mesh)** ⇒ crush lives in v4k not v4j. +2 tests; MSVC+gcc+clang-cl+guards.
- **v4 AMG cluster reorganized 2026-05-26** (folded former top-level v4l/v4m into v4k; value-ordered): v4k-a ✅ SA-AMG · v4k-a-2 ✅ convection probe · v4k-b ✅ K-cycle · v4k-c ⬜ polish+CLI · v4k-d ⬜ classical Ruge-Stüben · v4k-e ⬜ bootstrap αSA. v4j tail: v4j-2c ⬜ (complex+CLI+adjoint) · v4j-3 ⬜ (genuine-multilevel, COMPLETENESS not crush).
- **v4k-b SHIPPED 2026-05-26 — K-cycle (Notay AGMG): the robust convection cycle, NOT the β=0.3 crush.** `Cycle::{V(default),W,K}`; K = 2 flexible-GCR steps per level preconditioned by the recursive cycle (serial dotc ⇒ deterministic; moat held). **K = W on cases W helps (Poisson 7 flat; cd2d β=0.1 dominant 9, ties ILUPACK) AND converges where W DIVERGES (conservative zero-row-sum cd2d V=12/W=2000/K=4) — W's benefit, no instability.** Honest: K does NOT crush β=0.3 (still 70→112=V=W); cycle isn't the lever, the COARSE SPACE is → v4k-e. V stays default, K recommended for convection, W retained. 7/7 AMG tests MSVC+gcc+clang-cl+guards.
- **v4k-c SHIPPED 2026-05-27 — AMG CLI + F-cycle + complex.** `hesap.amg.{f32,f64,c32,c64}` (AMG-as-solver, stationary V/W/F/K cycle; closes the CLI-per-op debt). `Cycle::{V,W,F,K}`. Complex AMG (dinv→T through amg/prolongator) — **unblocked by completing complex dense LU** (`lu.cpp` abs_value→RealType modulus via if constexpr, real path bit-identical; +c32/c64 factor_lu/solve_lu; closed the old v0e-a2 complex-LU follow-on). 9 AMG tests; MSVC+gcc(38/38, LU regression intact)+clang-cl+guards.
- **v4k-e-1 SHIPPED 2026-05-27 — αSA adaptive-candidate hook + β=0.3 wall locked.** `Options.adaptive_candidate` (default off): tentative T seeded from a weighted-Jacobi-relaxed near-nullspace candidate (Brezina αSA), per-aggregate normalized. **Measured (the cheap increment of the user-chosen bootstrap path): αSA does NOT move β=0.3** (70→112 ≈ SA; Poisson 7 flat held). **VERIFIED not a no-op** (candidate range 1.8) ⇒ coarse space is NOT the β=0.3 lever ⇒ full bootstrap won't help. **β=0.3 anisotropic strong-convection = measured WALL for aggregation AMG (5 levers exhausted: κ, droptol, V/W/K, smoothed/plain agg, αSA).** Honest β=0.3: Cerid AMG WINS total wall 1.3–1.9× (12× cheaper factor) but LOSES iters 112 vs 10 (ILUPACK home turf). αSA kept as infra (elasticity-type non-constant near-nullspace). 10 AMG tests; MSVC+gcc+clang-cl+guards.
- **v4k-e-2 2026-05-27 — line-GS attempted (user-directed) → REVERTED; β=0.3 iter crush = the WALL.** Algebraic line-GS (block-GS, strong-coupling chains, Thomas): Poisson clean (7=point-GS via singletons) but β=0.1/0.3 DIVERGE (bug — block-GS can't be weaker than point-GS which handles β=0.1; algebraic chains not grid-aligned enough for line-solve stability). Reverted (don't ship diverging smoother; low-odds lever). **β=0.3 iteration crush CLOSED as documented wall — 6 levers exhausted** (κ, droptol, V/W/K, smoothed/plain agg, αSA, line-GS). Cerid AMG wins β=0.3 wall-time 1.3–1.9×, loses iters 112 vs 10 (ILUPACK home turf). 10 AMG tests intact.
- **v4j-2c SHIPPED 2026-05-27 — complex + adjoint + CLI for `InverseBasedIlu` (v4j-2 completeness filled).** Complex non-Herm solve (reuses v4k-c complex dense LU); `apply_adjoint` = conj-transpose of the folded eq.(20) (block-derived: transpose tri-solves + conj + DenseLuLeaf's 2nd LU of S̃ᴴ); CLI `hesap.precond.mlilu_ib.{f32,f64,c32,c64}` + `mlilu_ib` in all 5 nonsym solver selectors. +2 tests (complex solve + **adjoint identity ⟨y,M⁻¹x⟩=⟨M⁻ᴴy,x⟩ to round-off**). 6 InverseBasedIlu tests; MSVC+gcc+clang-cl+guards. **v4j-3 resolved as a CHARACTERIZED perf-wall (not a deferred feature — multilevel-ILU ships; deepening it is a deep low-odds rework, κ sweep showed worse).**
- **v4k-d SHIPPED 2026-05-27 — classical Ruge-Stüben AMG.** Full pipeline: `rs_strength` (directed −a_ij≥θ·max-neg, θ=0.25) → `cf_splitting` (bucketed max-λ coloring + interp-correctness promotion) → `rs_interpolation` (direct interp, sign-split α/β lumping). Wired as `Options.coarsening={SmoothedAggregation(default),RugeStuben}` — reuses ALL cycle/smoother/Galerkin machinery (one build() branch, zero dup). **RS-AMG-PCG mesh-independent on Poisson: 6 iters @ n=2500 AND n=10000** (beats SA's 8–10 on isotropic). CLI `coarsening` arg (sa|rs). +2 tests; 12 AMG tests MSVC+gcc+clang-cl+guards.
- **v4j-3 REOPENED 2026-05-27 (user decision — CFD is a top-priority consumer): close the β=0.3 convection crush.** DIAGNOSIS VERIFIED (advisor-discriminated, measured): NOT the estimator (child Schur genuinely well-conditioned, max_estL=1.31 ≤ κ=1.5 → correctly accepts; ILUPACK uses the same simple estimator) and NOT per-level scaling (MC64 EveryLevel = byte-identical no-op). **5 LEVERS RULED OUT by measurement:** per-level MC64 scaling (byte-identical), estimator (child Schur maxE=1.27 ≤ κ → correctly accepts), **MILU diagonal compensation (~zero effect 581→581/90→90 — but SHIPPED as a correct default-off option `m_milu` α∈[0,1])**, fill/droptol (extra fill goes to the easy LB/UB block; deferred count κ-fixed; iters 88→90), per-level κ (maxE 1.27 ≪ any sane κ). **LEVER CONFIRMED = REORDERING (T-version RULED OUT).** T-version dropped (droptol=1e-4 proved the easy Schur isn't a dropping artifact; S/T converge as drop→0). HILUCSI/ILUPACK mechanism (ar5iv 1911.10139): REORDER each level (RCM/AMD on A+Aᵀ) before factoring; deferral is one-row like ours, the reordering is the difference. **Discriminating experiment (`run_cerid_amd`: AMD-reorder cd2d top-level → InverseBasedIlu): iters HALVED (88→44 / 138→59 / 165→71) AND hierarchy DEEPENED to 3 levels (natural stuck at 2); Schur 12× harder.** Six wrong levers ruled out (estimator/per-level-scaling/MILU/fill/per-level-κ/T-version); MILU shipped default-off; run_cerid_amd kept as proof.
- **v4j-3(b) FAST AMD — ✅ DONE 2026-05-27.** O(n²)→O(n): amd_order + camd_order selected pivots by scanning the whole min-degree bucket for lowest-index (camd also rescanned d=0..n) → O(n²) on grids. FIX: O(1) bucket-HEAD selection (amd) + first-cur-class-in-head-order (camd), still deterministic. **amd_order 144→3.4ms @22.5k (42×, near-linear); fill PRESERVED (SuiteSparse 0.96–1.04× Eigen-AMD, GATE-OK, beats Eigen on bcsstk24); 931 win-debug tests + gcc + clang-cl.** Speeds up ALL AMD consumers. **Unblocks the wall-time win: cd2d β=0.3 Cerid-AMD now 2.1–2.7× FASTER than ILUPACK total wall** (5.1/29/80.8 vs 13.9/65.6/173ms) — still loses iters 45-75 vs 7-10 (that's part (a)).
- **v4j-3(a) PER-LEVEL REORDER — ✅ DONE 2026-05-27.** `reorder` ctor option on InverseBasedIlu: AMD-reorders B=PAPᵀ at every level (recursion auto-applies); apply/apply_adjoint unwrap Pᵀ…P (symmetric perm); build_reorder_transformed mirrors MC64 infra. **β=0.3: iters HALVED (88/138/165→45/64/76, 3 levels), total wall 2.0-2.8× FASTER than ILUPACK (5.1/31/85 vs 14/67/174ms).** +2 tests (solves+deepens+adjoint-identity; complex adjoint-identity to round-off); 8/8 MSVC+gcc+clang-cl. reorder+tight-κ cures the old collapse (κ=1.5→6 levels); κ=5 sweet spot.
- **v4j-3 NET (CFD home turf, cd2d β=0.3): Cerid WINS total wall-time 2.0-2.8× at every mesh** (fast factor + cheap AMD + low fill); still loses iteration count (45-76 vs 7-10, halved from natural — diminishing-returns gap). reorder + MILU both default-off options.
- **v4z SHIPPED 2026-05-27 — v4 CLOSE.** (1) **Break-even quantified:** cd2d β=0.3 `mlilu_ib`+reorder WINS single-shot wall 2.1–2.2×/mesh; ILUPACK overtakes only after **k\*≈3.6–5.9 re-solves** (bench prints split + break-even). (2) **`reorder` default → ON for nonsym `InverseBasedIlu`** — no-regression measured (sherman3 1066→557 it/1.47×, cd2d-150 269→68/1.75×, fill flat), matches ILUPACK; `mlilu_ib` CLI gains `reorder` Bool; 4 natural-order tests pinned `reorder=false`. (3) **Robustness:** singular dense Schur leaf shifts-diagonal-and-refactors (gemat11 wrong-tool → EXIT 0, was an assert) — preconditioners degrade, never abort (+1 test). (4) **Complex audit:** every variant has a complex residual test (+2: block_pcg<C>, point-Jacobi<C>). (5) **CLI audit:** every op × 4 types registered. (6) **ADR-0065 §26** locks D(iter)-1..10. Touched-module gate green (hesap-iterative 144 cases, hesap CLI 39 cases); 18-config sweep → CI.

**The determinism moat — bit-exact {iterations, residual, solution} across {1,2,4,8,16} threads — holds across the entire v4 family; no frontier library (Eigen/PETSc/AMGCL/Trilinos) offers it.**

> **Navigation:** authoritative per-slice ledger + honest vs-reference numbers → `docs/phases/phase-3.1.6-hesap.md`. This session (2026-05-26): `docs/sessions/2026-05-26-hesap-v4i-1-spai-fspai.md`, `…-v4i-2-chebyshev.md`, `…-v4i-3-schwarz.md`, `…-v4j-1a-mc64.md`, `…-v4j-1b-multilevel-scaffold.md`. **The long paragraph below is the appended per-slice narrative (v2/v3/v4-era), kept for depth — read the bullets above for current state.** **v3d-1c-3 (AED, 2026-05-23) — HARD-GATE MET:** `schur_aed` (LAPACK `dlaqr0`-class driver: `aed_deflate`/`dlaqr2` window deflation + double-shift `dshift_sweep` using undeflated AED eigenvalues as shifts + NMIN=200 crossover to pure-`dlahqr`/`real_schur`) beats Eigen `RealSchur` 1.17–1.74× AND LAPACK `dhseqr` 1.26–1.97× at every N (100/200/400); at n=400 AED engages (184 sweeps), beats pure-dlahqr 1.53×; recon ~1e-13; 4-config DoD green. Built on v3d-1a Hessenberg (SIMD row-wise unblocked `dgehd2` — beats blocked small-K), v3d-1b `real_schur` (dlahqr + dlanv2), v3d-1c-1 `reorder_schur` (dtrexc/dlaexc/dlasy2), v3d-1c-2 `aed_deflate` (dlaqr2). **v3d-1c-4 (multishift train, 2026-05-23) CLOSED:** the full `dlaqr5` small-bulge multishift sweep (general nbmps, KACC22=1 accumulate-into-U + BLAS-3 gemm slab updates) is now the live AED sweep in `schur_aed` (one train pass over all undeflated shifts vs the old ns/2 single-bulge sweeps). The win over Eigen RealSchur WIDENS with N — 1.85× (n=400) → 3.20× (n=800) → 3.99× (n=1200), in 3/4/5 train passes (the train batches shifts per pass; old double-shift took 184 sweeps at n=400); NMIN kept at 200 (measurement: AED+train loses below 200). Validated by implicit-Q (ns=2/4/6) + full `[nonsym]` 242 795 assertions; 4-config DoD green. **v3d-2a ✅ 2026-05-24** (eigenvectors of T: faithful `dlaln2` 1×1/2×2 complex solve + `dtrevc_right` back-substitution; gates `(ca·A−w·D)X=scale·B` <1e-11 + `T·v=λv` <1e-9 incl. complex pairs; 4-config DoD). **v3d-2b ✅ 2026-05-24 — public real `eig()` BEATS Eigen + LAPACK at every N.** `EigNonsym<T>` (complex values+vectors, Schur order) + `eig(Matrix<T>)` full pipeline (balance→hessenberg+formQ→`schur_aed`→`dtrevc`→3-stage back-transform `V=D⁻¹P·Q·Z·V_schur` with faithful `gebak_right` = dgebak SIDE=R/JOB=B) + CLI `eig.nonsym.{f32,f64}`. **D(non-sym)-4** (norm ‖·‖₂=1 + largest-mag-component phase-real-positive = dgeev/Eigen convention; supersedes plan "max-abs=1"), **D(non-sym)-5** (Schur eigenpair order). Residual ~1e-13 (≪1e-9), eigenvalues match Eigen ~1e-13; dgebak perm+scale branches gated by corner-isolated + `A=D·B·D⁻¹` fixtures. **Bench (full eig values+vectors, f64): beats Eigen `EigenSolver` 1.09×/1.08×/1.65× (n=100/200/400), crushes LAPACK `dgeev` 2.03×/3.04×/1.57×.** Perf lever: stage-timing showed `form_hessenberg_q` (dorghr) was scalar column-strided = 3.35ms@200 (the only thing losing to Eigen) → rewrote the reflector apply ROW-WISE (contiguous `simd_axpy`, same accumulation order) → 0.58ms (5.8×), flipping small-N loss→win (the v3d-1a SIMD-row-wise lever). Full `[nonsym]` 247 624 assertions / 26 cases + 4 new `eig:` cases; 4-config DoD green (debug/shipping-LTCG/asan/tidy). **v3d-2c STARTED — 2c-1 ✅ 2026-05-24 (complex Hessenberg + unitary Q, beats Eigen + LAPACK).** `zgehd2`+`zunghr` on the two-real-array (`ar`/`ai`) SIMD path; unified `hessenberg<T>`/`form_hessenberg_q<T>` dispatch real/complex via `if constexpr`. `make_householder_complex` (zlarfg) promoted to shared `detail/householder.hpp` (deduped eig_herm). New fused complex SIMD `detail/dot_simd_complex.hpp` (`simd_cdot_nc`/`simd_caxpy`/`simd_caxpy_conjx`, 8-wide for FMA-port ILP — 4-wide regressed, measure-don't-guess). Gate MET: `A=Q·H·Qᴴ` recon <1e-12 (c64 to n=256), Q unitary; bench BEATS Eigen `HessenbergDecomposition<MatrixXcd>` 1.05×/1.21× (n=64/128 measured regime) + crushes `zgehrd` 2.2–11.9×. (Eigen's complex Hessenberg reference access-violates at n≥256 — complex path only, real-double fine; root cause not pinpointed, treated as ref fragility, NOT Cerid; Cerid recon-clean to 512/ASan-clean to 256; refs capped at 128 in the bench.) 4-config DoD green (353 406 assertions / 308 cases). **v3d-2c-2 ✅ 2026-05-24 (complex balance + single-shift Schur, beats Eigen + LAPACK):** `balance<T>` unified real+complex (`scale` now `Array<RealType<T>>`); `complex_schur` (`zlahqr`) single Wilkinson-shift QR → upper-triangular T (no 2×2/no dlanv2) + unitary Z + eigenvalues, complex-Givens bulge-chase, D(non-sym)-6 exceptional schedule; new complex `sqrt` (complex.hpp) + `complex_givens` (zlartg, householder.hpp, reused by 2c-3). Gate MET: `H=Z·T·Zᴴ` recon <1e-8 (~1e-13), Z unitary, T upper-triangular, c32/c64. Bench BEATS Eigen `ComplexSchur` 1.16×/1.10× (n=64/128) + crushes `zhseqr` 6.58×/1.45× (Eigen ComplexSchur also AVs at n≥256, confirmed; refs capped at 128). 4-config DoD green (353 435 assertions / 311 cases). **v3d-2c-2b STARTED — 2b-1 ✅ + 2b-2 ✅ 2026-05-24.** **2b-1 (complex Schur reorder `ztrexc`):** `reorder_complex_schur<T>` moves a diagonal eigenvalue `ifst→ilst` by adjacent 1×1 swaps; each swap = faithful `zlartg(t(p,p+1), t(p+1,p+1)−t(p,p))` complex Givens applied as a unitary similarity `G·T·Gᴴ` over the full block region (left `[p,n-1]`, right `[0,p+1]`) + `(p+1,p):=0` + `Z·Gᴴ`. No 2×2 ⇒ no dlasy2/dlaexc ⇒ ~95 LOC. Backward move uses a `here` cursor (`usize` underflow at `ilst==0` is a real bug class — advisor flag; tested). Gate MET: reordered form is a valid Schur of the SAME H `<1e-9` + eigenvalue lands at target (c64; c32 `<1e-3`). **2b-2 (complex AED deflation window `zlaqr2`):** `complex_aed_deflate<T>` — trailing-window `complex_schur` + 1×1 deflation test `|s|·|V(1,j)|≤max(smlnum,ulp·cabs1(T(j,j)))` + `reorder_complex_schur` up-move + diagonal eigenvalue restore (no dlanv2) + spike reflect (`make_householder_complex`) + re-Hessenbergize (`hessenberg<Complex>`) + global `H:=Vᴴ·H·V` via complex `gemm` slabs. **3 zlaqr2-faithful complex divergences the real path can't show:** LEFT spike apply `conj(tau)` / RIGHT `tau` (new conjugating `apply_hc_*`), left slab `Vᴴ·H` (new `slab_left_h` ConjTranspose; `slab_right` `C·V` reused), coupling restore `H(kwtop,kwtop−1)=s·conj(V(1,1))`. Gate MET: general window keeps `z·H·zᴴ==H0` whole-matrix `<1e-8` AND spectrum invariant `<1e-7` (recon+spectrum split per advisor — catches coupling/sign conj errors); decoupled window deflates fully (`nd==nw`,`ns==0`); `nd+ns==nw`; c32 `<1e-3`. ~330 LOC. Both 4-config DoD green (full `[nonsym]` 247 715 assertions / 37 cases; `[aed]` 43 806 ASan-clean; win-tidy fixed 5 pre-existing violations in the file at 2b-1). **2b-3 ✅ 2026-05-24 (complex AED driver `zlaqr0` + multishift sweep `zlaqr5`) — THE SCALE CRUSH.** `complex_dlaqr5_sweep` (faithful zlaqr5: NBMPS bulges via `complex_dlaqr1`+`make_householder_complex` 3-vectors, KACC22=1 accumulate-into-U + BLAS-3 `gemm` far-updates `slab_left_h`=Uᴴ/`slab_right`=U; conj rule fetched VERBATIM from zlaqr5.f — RIGHT `tau·conj(v)` plain gather / LEFT `conj(tau)·v` conj gather, similarity Hᴴ·A·H) + `complex_schur_small_block` crossover + `complex_schur_aed` driver (split→deflate→nibble→sweep→NMIN=150 crossover). Gate MET: spectrum+recon vs single-shift `complex_schur` ~1e-13 (n=40/160/260); **BEATS single-shift 1.10× (n=256) / 1.12× (400) / 2.01–2.14× (512) — win WIDENS with N, sweeps collapse to 3/4/4** (refs AV at n≥256 so single-shift is the baseline). NMIN=150 measured (crossover ~200; n=128 loses 0.90×, n≥256 wins) — NOT real-path 200. D(non-sym)-7 (window size) + D(non-sym)-8 (shift order) pinned. **THE BUG (model isolation trail): smooth sin/cos unit tests passed ~1e-13 but random matrices gave recon ~1 → isolated via implicit-Q (sweep exact ~1e-15) + crossover-only (clean) + per-step recon → the `complex_aed_deflate` spike path on PARTIAL deflation; root cause = zlaqr2.f conjugates the spike row (`conj(V(1,:))`), my port copied plain (advisor flagged this exact conj in 2b-2 review). 1-line fix. Lesson: test eigensolvers on RANDOM matrices, smooth spectra skip the spike path.** ~700 LOC, 6 cases (incl. implicit-Q + random repros as permanent guards). 4-config DoD green (full `[nonsym]` 247 757 assertions / 44 cases). **🎉 v3d-2c-2b (complex AED) CLOSED.** **2c-3 ✅ 2026-05-24 (complex ztrevc + public complex `eig` + CLI) — closes v3d-2c.** `ztrevc_right<T>` (complex right eigenvectors of upper-tri Schur T: per-column back-solve, `smin` near-defective floor + inline `cnorm`/`bignum` overflow scaling, no zlatrs; all-scalar no dlaln2; ported verbatim from ztrevc.f; no normalization — deferred to eig). `eig<T>` → dispatcher (`eig_real_impl` renamed + new `eig_complex_impl` via `if constexpr`): balance→hess+formQ→`complex_schur_aed`→ztrevc→`V=D⁻¹P·Q·Z·V_schur` (2 gemms + complex `gebak_right` generalized to `<V,S>`)→normalize once D(non-sym)-4 (‖·‖₂=1, largest-**modulus** comp real-positive). CLI `eig.nonsym.{c32,c64}`. Gate MET: residual `‖A·vₖ−λₖ·vₖ‖₁/‖vₖ‖₁` ~1e-13 (random c64 n=20/60 + non-triangular near-defective dup-eigenvalue smin test + c32), ‖v‖₂=1, phase asserted. **Bench BEATS Eigen `ComplexEigenSolver` 1.13×/1.32× (n=64/128) + crushes `zgeev` 4.79×/2.80×; refs AV at n≥256 confirmed empirically (capped n≤128).** Test-bug caught (phase pivot used cabs1 vs eig's modulus — fixed test). 4-config DoD green (full `[nonsym]` 248 181 assertions / 47 cases). **🎉 v3d-2c (complex non-sym eig) CLOSED — complex Hessenberg + balance + single-shift Schur + AED (zlaqr0/zlaqr5) + ztrevc + public complex `eig` + CLI, all beating Eigen+LAPACK.** Open follow-on `v3d-eig-fully-reducible-input` (triangular/fully-reducible input → balance empty-block `ihi` underflow → hessenberg assert; pre-existing, affects real eig too; narrow edge, deferred). **🎉🎉 v3 CLOSED (v3e close, 2026-05-25).** ADR-0065 **§25** locks v3d (D(non-sym)-1..8 + faithful-port divergences + NMIN-measured + vs-reference rollup); §17–§25 cover the full v3 family (v3a sym/herm + v3b SVD + v3c least-squares + v3d non-sym). **Constant verification** (vs zlahqr.f): `dat1=0.75`/`itmax` exact; **upgraded `complex_schur` exceptional schedule from classic 2-kick (its==10/20) to modern `KEXSH=10` continuous kicks** for consistency with `real_schur`'s dlahqr `kdefl` path + robustness (behavior-neutral — converging spectra never reach its≥10; `[nonsym]` 248 181 assertions unchanged). **CLI audit CLEAN** (every dense-eig op incl. rsvd/rsyev has a command). **18-config full sweep PASS** (11 Windows incl clang-cl/scalar/sse2/LTCG + 7 gcc-linux, 0 failed) after the v3e cross-config cleanup (18 non-ASCII test names + 9 clang/gcc `-Werror` issues — all latent in the MSVC-only per-slice path; see `feedback_per_slice_binary_direct_misses_ctest_and_crossconfig`). **🎯 v4-corpus ✅ SHIPPED 2026-05-25 (matrices as cooked resources; cooked binary loads 6–7× faster than re-parsing `.mtx`; ADR-0084; `docs/systems/hesap-resources.md`; 10 cases/108 assertions; local DoD green debug-ctest+tidy+clang-cl+asan+shipping, gcc/sweep→CI). v4a-1 ✅ SHIPPED 2026-05-25 — Krylov framework (`KrylovWorkspace`/`IterativeResult<R>`/`stopping`) + CG + PCG + JacobiPreconditioner, two modules (`crd-hesap-iterative` + `crd-hesap-preconditioners`), 12 CLI, `smoke_hesap_solve_cli` proves the thesis (resource-loaded matrix → CG converges); 7 cases/82 assertions, local DoD green (debug-ctest+tidy+clang-cl+asan+shipping). v4a-2 ✅ SHIPPED 2026-05-25 — block-Jacobi (self-contained complex-capable Gauss-Jordan inverse) + SSOR (sequential sweeps) + `ParallelSparseLinearOp` (size-adaptive serial/parallel SELL) + 8 CLI; **determinism moat GATED** (CG over parallel spmv == serial, bit-exact); **CRUSHES Eigen CG+DiagonalPreconditioner on real SuiteSparse SPD: bcsstk13 1.81× / bcsstk24 1.49× / bcsstk25 1.86×** (size-adaptive op was the lever — serial-SELL sub-cache + parallel large wins everywhere). 10 cases/390 assertions; local DoD green (debug-ctest+tidy+clang-cl+asan+shipping; fixed release-only C4189). **🎉 v4a CLOSED.** v4b ✅ SHIPPED 2026-05-25 — **FGMRES(m)** (`gmres.hpp`: flexible-from-the-start `fgmres` + plain `gmres`, `GmresWorkspace`, self-contained real+complex Givens, MGS, restart, back-sub) + 4 CLI (`fgmres` with precond/restart selector) + determinism moat GATED. **Crushes Eigen on nonsym SuiteSparse: gemat11 FGMRES 1.84× vs Eigen BiCGSTAB + 2.47× vs Eigen GMRES (all GMRES-class stagnate → throughput win); sherman3 = restarted-GMRES(30) stagnation (→ v4c BiCGSTAB, not a kernel defect).** Tests incl. restart-invariance + VARYING-M flexibility + complex nonsym. 17 cases/755 assertions; 5-config local DoD green. Side-fixes: complex `operator/` C4723 Release pragma + Jacobi graceful zero-diagonal fallback. v4c-1 ✅ SHIPPED 2026-05-25 — **BiCGSTAB** (`bicgstab.hpp`, van der Vorst, short-recurrence nonsym; dotc throughout, smlnum breakdown thresholds + CG hardened to match, lucky-breakdown; 4 CLI + precond selector; determinism moat GATED). **APPLES-TO-APPLES crush of Eigen BiCGSTAB: gemat11 1.86× + sherman3 1.66× (Cerid 454 it conv vs Eigen 579) — solves the sherman3-class nonsym GMRES(m) couldn't reach.** 21 cases/1014 assertions; 5-config DoD green. v4c-2a ✅ SHIPPED 2026-05-25 — **MINRES** (`minres.hpp`, Paige-Saunders: symmetric Lanczos + incremental Givens + short w-recurrence, O(1) storage; Hermitian contract = real tridiagonal via `krylov_real`; 4 CLI; determinism moat GATED). Tests: SPD + **symmetric INDEFINITE where CG diverges** + Hermitian-indefinite c64 + determinism. **Crushes Eigen MINRES (apples-to-apples, unpreconditioned, real SPD): bcsstk13 1.97× / bcsstk24 1.57× / bcsstk25 2.06×.** 25 cases/1354 assertions; 5-config DoD green. v4c-2a-precond ✅ SHIPPED 2026-05-25 — **preconditioned MINRES** (`minres` unified with optional SPD/HPD `m_inv` via M-inner-product Lanczos; no-precond path bit-identical; CLI moved to preconditioners module with precond selector). **FIXES the ill-conditioned gap: bcsstk13 now CONVERGES (Cerid 1380 it / Eigen 1378), 1.85× faster** (was capping); crushes Eigen Jacobi-MINRES 1.47–1.85× on real SPD. 26 cases/1356 assertions; 5-config DoD green. v4c-2b ✅ SHIPPED 2026-05-25 — **SYMMLQ** (`symmlq.hpp`, Paige-Saunders LQ; recurrence transcribed verbatim from Krylov.jl, validated by final ‖Ax−b‖ since residual non-monotone; 4 CLI; determinism moat GATED; Eigen ships no SYMMLQ = breadth win). Tests SPD + indefinite + Hermitian-indefinite c64 + determinism. Consistency fix: `gmres_mag`→shared `krylov_mag`. SYMMLQ is **preconditioned + unpreconditioned** (optional SPD/HPD M via M-inner-product Lanczos, no-precond path bit-identical; CLI moved to preconditioners module with precond selector; NO follow-ons). **🎉 v4c CLOSED** (BiCGSTAB + MINRES un/precond + SYMMLQ un/precond). Full iterative suite 31 cases/1699 assertions; 5-config DoD green. **v4d IN PROGRESS** — **v4d-1 ✅ CLOSED 2026-05-25** (core + preconditioned). Core: **LSQR + LSMR** (`lsqr.hpp`/`lsmr.hpp`, Golub-Kahan bidiag, recurrences verbatim from Krylov.jl, validated by ‖Aᴴ(Ax−b)‖≈0) + new **`ParallelSpmvLeastSquaresOp`** in crd-hesap-sparse (rectangular, parallel SELL for A·x AND Aᴴ·y, size-adaptive, determinism-gated — reused by v4d-2 QMR). Eigen ships neither LSQR nor LSMR (its LeastSquaresCG = the normal-equations CG LSQR avoids) → breadth + determinism, no fake crush. **v4d-1-precond ✅:** LSQR/LSMR unified with an optional N-space column preconditioner via the M-inner-product Golub-Kahan bidiagonalization (no-precond path bit-identical, `nv` aliases `v`; plain convenience overloads kept) + new **`LeastSquaresColumnJacobi<T>`** (M=diag(AᴴA)⁻¹, one CSR pass, parallel-trivial + bit-deterministic); selector deliberately `{none, jacobi}` only (square Jacobi/block-Jacobi/SSOR need a square operator a least-squares A lacks — not a defer). **LSQR/LSMR CLI moved iterative→preconditioners** with `precond none|jacobi` (consistency with minres/symmlq; command names unchanged). Tests: column-Jacobi LSQR+LSMR converge on a 1e12-cond column-scaled problem where plain stalls; determinism moat holds under preconditioning. Iterative suite 41 cases/3469 assertions; win-debug ctest+guards + clang-cl `-Werror` green, gcc/full-sweep→CI. **v4d-2a ✅ SHIPPED 2026-05-25 — QMR** (`qmr.hpp`, Freund-Nachtigal two-sided bi-Lanczos + `sym_givens`, transcribed VERBATIM from Krylov.jl; uses Aᴴ+dotc; RIGHT preconditioner N=m_inv keeps the quasi-residual in true-residual norm + needs the precond adjoint; post-loop untransform; shared `detail::GivensRot` hoisted to cg.hpp, deduped gmres). **NO-DEFER completion: block-Jacobi + SSOR gained apply_adjoint** so QMR's CLI carries the full none|jacobi|block_jacobi|ssor selector. 4 CLI; Eigen ships no QMR → breadth+determinism (no fabricated crush); determinism moat GATED. Tests +8 (incl. seeded-random nonsym, complex, all-precond convergence, direct adjoint-identity c64, determinism). Iterative suite 49 cases/3805 assertions; win-debug ctest+guards + clang-cl green. **v4d-2b ✅ SHIPPED 2026-05-25 — IDR(s) (Sonneveld-van Gijzen); 🎉 v4d CLOSED.** `idrs.hpp` (short-recurrence A-only with s-dim shadow space, transcribed VERBATIM from IterativeSolvers.jl; deterministic seeded + MGS-orthonormalized shadow P in the workspace ctor; left precond; van Gijzen ρ-safeguard omega; M[k,k] breakdown guard; standalone triangular-solve unit-tested). 4 CLI (+`s`+precond selector); determinism moat GATED (two same-seed workspaces, serial-vs-parallel bit-exact). **APPLES-TO-APPLES vs Eigen IDRS (gated bench, s=4, true residual both sides): Cerid 7–35× faster AND far more robust — Eigen's residual explodes 1e+6…1e+152 on every real matrix while Cerid stays bounded; sherman3+Jacobi Cerid CONVERGES (596 it, r=7.5e-9) where Eigen DIVERGES (r=4.3e+18). gemat11/bcsstk both STALL (IDR(s) inappropriate; honest, no fake crush).** Iterative suite 55 cases/4141 assertions; win-debug ctest+guards + clang-cl + gated bench green. **🎉 v4d CLOSED — LSQR/LSMR (un/precond) + QMR + IDR(s).** **v4e STARTED (split v4e-1/2/3, advisor). v4e-1 ✅ SHIPPED 2026-05-25 — GCR(m)** (`gcr.hpp`, restarted Generalized Conjugate Residual, Eisenstat-Elman-Schultz; orthonormal C=A·U search basis + minimal-residual correction = the recycle substrate GCRO-DR deflates; optional right precond; 4 CLI; determinism moat GATED — baseline verified bit-exact before harmonic-Ritz dense LA enters at v4e-2). Tests +5 (nonsym, monotone-residual, Jacobi-precond, complex, determinism); 60 cases/4479 assertions; win-debug ctest+guards + clang-cl green. GCR≈GMRES → no Eigen peer (recycling crush lands v4e-2/3; Eigen has NO recycling). **v4e-2 reference SWITCHED to GCROT(m,k) (scipy `gcrotmk`, verified) from GCRO-DR/harmonic-Ritz (advisor reconcile — verbatim-reference discipline). v4e-2a ✅ SHIPPED 2026-05-26 — GCROT(m,k) core ('oldest' truncation):** `gcrot.hpp` (recycle C/U pairs, inner FGMRES kept ⊥ C with B=CᴴAV, combined direction ux=Zy−U(By)/cx=V·H̄·y, GCR step, 'oldest' truncate+append; right precond; 4 CLI; determinism moat GATED). **iters-saved gate PASSES: GCROT(8,8) < GMRES(8) on a small-eigenvalue-cluster matrix.** Eigen has NO recycling → breadth + iters-saved demo (honest). 65 cases/4795 assertions; win-debug ctest+guards + clang-cl green. **v4e-2b ✅ SHIPPED 2026-05-26 — GCROT SVD-optimal 'smallest' truncation; 🎉 v4e-2 CLOSED.** Default truncation now keeps the k−1 dominant left singular vectors of D=((Rsq)ᵀ)⁻¹Bᵀ via the Hermitian Gram `eig_sym`(real)/`eig_herm`(complex) of DDᴴ (dense `svd` is real-only; the Gram-eig route is complex-capable + reuses our deterministic eig). `GcrotTruncate{Oldest,Smallest}` knob; standalone `gcrot_build_d` unit-tested; 'smallest' ≤ 'oldest' iters gated. 67 cases/4803 assertions; win-debug ctest+guards + clang-cl green. **Gated wall-time bench `bench_hesap_gcrot_vs_reference` (vs Cerid GMRES(m) baseline + Eigen GMRES(m) ref): GCROT CONVERGES sherman3 in 595 it/24ms (r=8.5e-10) where restarted GMRES(30) STALLS at 8000 it (r=0.76) — 13.45× iters / 8.7× time WIN; cluster 2.58× iters/1.44× time. Single-solve overhead (0.22–0.42× time) where neither converges — inherent O(k²n)/cycle SVD-optimal-truncation cost, amortized cross-solve.** **v4e-3a ✅ SHIPPED 2026-05-26 — cross-solve recycling.** New `RecycleSpace<T>` (opaque+persistent; rebuilds C=A·U on entry ⇒ safe across DIFFERENT operators A_i) + `gcrot_recycled`. **Bench sequence (6 shifted solves): fresh GMRES(8) 17319 it/91ms vs GCROT recycled 247 it/4.6ms = 70× fewer iters, 19.6× faster — the de Sturler payoff.** Tests: trap (fill on A₁, reuse on A₂ → correct) + iters-saved sequence gate. **NO-DEBT inline fixes: SSOR true non-Hermitian adjoint (M_SSOR(A)ᴴ=M_SSOR(Aᴴ), sweep on stored conj-transpose); GCROT eig-truncation allocation eliminated via a reset-per-cycle LinearAllocator arena + FallbackArena (25–40% faster truncation-heavy, determinism preserved).** 69 cases/4821 assertions; win-debug ctest+guards + clang-cl green. **v4e-3b ✅ SHIPPED 2026-05-26 — RMINRES (recycling MINRES); 🎉 v4e CLOSED.** `rminres.hpp` = deflated-MINRES built on the shared recycling core (`gcrot_core` parameterized on `SymLanczos`: 3-term symmetric Lanczos vs Arnoldi MGS — only the inner orthogonalization differs, reusing the verbatim MINRES Lanczos + GCROT recycle machinery; 3-term preserved under the C-projection for symmetric A; real symmetric tridiagonal even for complex Hermitian). `rminres`/`rminres_recycled` + 4 CLI (no precond — recycle = deflation). Tests: symmetric-indefinite + complex Hermitian + cross-operator trap + cross-solve iters-saved sequence + determinism. NO-DEBT inline fix: zero `hbar` per cycle (latent `cx=V·H̄·y` staleness for gcrot+rminres). 74 cases/5149 assertions; win-debug ctest+guards + clang-cl green. **🎉 v4e CLOSED — GCR + GCROT(m,k) + cross-solve recycling + RMINRES, the full recycling-Krylov family.** **v4f STARTED (split v4f-1/2/3, advisor). v4f-1 ✅ SHIPPED 2026-05-26 — inner-Krylov-as-preconditioner:** `KrylovPreconditioner<T,InnerApply>` (templated callable, zero-overhead) wraps an inner solve as a LinearOp (z≈A⁻¹r) for FGMRES inner-outer compositions; `make_krylov_preconditioner` helper. Tests +4 (inner-GMRES accelerates outer FGMRES, inner-CG on SPD, complex, determinism). **Bench: cluster nested converges 10 outer-it vs flat GMRES 300 — 30× fewer outer-iters, 1.6× faster, 3.5× vs Eigen; hard SuiteSparse both stall (honest — nesting helps only when inner approximates A⁻¹).** No CLI (composition primitive). 78 cases/5464 assertions; win-debug ctest+guards + clang-cl green. **v4f-2 ✅ SHIPPED 2026-05-26 — block-CG + block-PCG (the complete block-CG family):** new `BlockLinearOp<T>` + `ParallelSpmmLinearOp` (one fused size-adaptive `spmm` for all s RHS, bit-exact); `block_cg`/`block_pcg` = classic O'Leary Galerkin block-CG, **breakdown-free via per-step packed-MGS search-block orthonormalization** (transposed to column-contiguous for bit-exact SIMD blas1 — robust on cond~1e10 where CholeskyQR2 overflows; no-QR stalls + strided-MGS ran 4.8× slow); allocation-free **row-streaming** block GEMMs (the K=s tall-skinny win, flipped banded 0.92→1.18×); rank-deficiency → zero-column + regularized (M+εI) Cholesky. Block-PCG folds M in where M=I recovers block-CG; **complete preconditioner family** = `JacobiBlockPreconditioner` (native one-pass) + `BlockPreconditionerAdapter` (wraps ANY `LinearOp<T>`). 8 CLI (`block_cg`/`block_pcg` × 4). **Gated 3-way bench (all Jacobi-precond): Cerid per-column PCG CRUSHES Eigen EVERYWHERE 1.23–2.43× (parallel SELL spmv, the cache-resident-regime owner); block-PCG additionally beats Eigen on the expensive banded operator (1.12× @s=4, 3–16× fewer A-passes) + converges robustly on ill-conditioned bcsstk.** Honest floor (per crush-mandate-bounded-by-importance): on cache-resident A block-PCG's O(n·s²) dense flops have no DRAM-pass to trade for ⇒ per-column PCG (v4a) owns that regime; block-PCG's wall-win scales with operator expense (A>L2 / matrix-free). +7 tests (incl. ill-conditioned-Laplacian QR guard + rank-deficiency + native≡adapter bit-exact + determinism). Iterative suite **85 cases/7892 assertions**; win-debug ctest+guards green (clang-cl stale-PCH + win-tidy LLVM-skew on pre-existing files → CI owns the pinned cross-config gate, per `feedback_local_test_only_ci_owns_sweep`). **v4f-3 ✅ SHIPPED 2026-05-26 — block-GMRES + block-BiCGSTAB (nonsym multi-RHS); 🎉 v4f CLOSED.** `block_gmres.hpp` (block Arnoldi + block-Hessenberg least-squares via **banded SCALAR Givens** — H_{j+1,j} upper-tri ⇒ s-wide band, reusing verbatim `gmres_givens`/`gmres_rot_apply`; deflation guard on the back-solve) + `block_bicgstab.hpp` (El Guennouni 2003: s×s α/β via GENERAL `block_lu_solve` not SPD, scalar ω Frobenius, β reduces to scalar at s=1; **divergence guard** worst_rel>1e10⇒Breakdown). Foundational `block_qr` generalizes v4f-2's orthonormalizer to return R (reorth ADDS to R[i,j]; W=Q·R test) + new `block_lu_solve`. 16 CLI. +7 tests (block_qr recon, nonsym GMRES + shared-Krylov ratio + complex + determinism; BiCGSTAB nonsym + peer + complex + determinism). **Gated 3-way bench: Cerid per-column GMRES/BiCGSTAB CRUSHES Eigen 2.27–2.87× on expensive operators (parallel SELL spmv); block adds A-pass reduction 4–24× + breadth (Eigen has no block algorithm).** Honest floor (= block-CG): per-column owns cache-resident/sparse wall-time; block wins matrix-free/expensive-apply. Iterative suite **91 cases/12317 assertions**; win-debug ctest+guards + engine win-tidy clean. **🎉 v4f CLOSED — nested-precond + block-CG/PCG + block-GMRES + block-BiCGSTAB, the complete block-Krylov family.** **v4g ✅ SHIPPED 2026-05-26 — IC(0) + ILU(0) + ILUT incomplete-factorization preconditioners.** Three LinearOp preconditioners (crd-hesap-preconditioners): **IC(0)** (diagonal-scaled + Manteuffel shift), **ILU(0)** (IKJ level-0 + diagonal insertion + pivot floor), **ILUT** (SPARSKIT dual-threshold verbatim, row-scaled for droptol scale-invariance). 24 CLI. **Bench: IC(0)-PCG CRUSHES Eigen IncompleteCholesky 2.59–2.78× wall (1.36–1.75× per-iteration) at MATCHED TRUE residual (Cerid reaches a TIGHTER residual in FEWER iters; corrected 2026-05-26 — the original recurrence-residual numbers were a bench artifact, [[feedback_iterative_bench_matched_true_residual]]) on real SuiteSparse SPD; ILUT small-nonsym (sherman3) is triangular-solve-bound — see the v4i-1 AMD-reorder finding: Cerid's STRUCTURE-PRESERVING default ILUT CRUSHES Eigen 2.28× on large structured cd2d-200 (Eigen's mandatory AMD destroys the banded structure Cerid's parallel tri-solve exploits), while on small cache-resident sherman3 Eigen's forced-AMD ILUT edges Cerid ~1.4× (the new opt-in `ReorderedPreconditioner` narrows it from 2× to 1.4×)** ⇒ Cerid is MORE complete (default + opt-in AMD); Eigen forces AMD always. +17 tests; iterative suite 105 cases / 17328 assertions; win-debug ctest+guards + engine win-tidy clean. Robustness fixes (no debt): IC diagonal-scaling-before-shift, ILU(0) diagonal-insertion + pivot-floor, ILUT row-scaling + in-place U-compaction. **v4g-tri-solve-parallel ✅ SHIPPED 2026-05-26 — level-scheduled parallel triangular solve + large-nonsym ILUT crush.** `triangular_solve.hpp` (crd-hesap-sparse): `TriSchedule` (topological dependency levels) + `tri_solve_lower/upper_levelsched` (off-diag CSR + inv_diag; parallel-within-level, bit-exact vs sequential); size-adaptive (parallel iff max_level_width≥256 AND n≥8192 — sherman3's 689 narrow levels ran 2× slower parallel ⇒ correctly serial). Integrated into ILUT. **Bench `cd2d-200` (n=40000): Cerid ILUT CRUSHES Eigen IncompleteLUT 2.28× (3 vs 11 iters) at MATCHED TRUE residual (corrected 2026-05-26; Cerid's structure-preserving default ILUT beats Eigen's forced-AMD ILUT on this large banded matrix) via ILUT quality + parallel SELL spmv** (chain-y ILUT factor keeps tri-solve serial; parallel path verified bit-exact at n=90000 wide-wavefront). +1 test; 106 cases / 107330 assertions; win-debug ctest+guards + engine win-tidy clean. **v4h ✅ SHIPPED 2026-05-26 — ILU(p) level-of-fill; 🎉 ILU family complete.** `IlupPreconditioner<T>(a,alloc,p)` (Saad Alg 10.5): fused symbolic+numeric IKJ, each entry carries a fill level (A=0; fill via k = min path lev(i,k)+lev(k,j)+1); NEW fill created iff level≤p, EXISTING pattern entries always updated (the level gates creation not updates — the bug fixed: ILU(0) had skipped all updates). Reuses the v4g level-sched tri-solve + L/U dynamic CSR + adjoint. 4 CLI + ilup in all 9 selectors. +4 tests (recovery exact, monotone fill+convergence in p, complex, determinism). **Bench (vs Eigen IncompleteLUT, matched fill): cd2d-100 ILU(0)→(1)→(2) cuts 48→29→25 iters; Cerid ILU(2) CRUSHES Eigen 2.58× (5.40 vs 13.92ms at equal fill, matched TRUE residual; corrected 2026-05-26 from 3.2×); sherman3 ILU(2) converges where Eigen ILUT diverges.** 110 cases / 108024 assertions; win-debug ctest+guards + engine win-tidy clean. **🎉 ILU family complete: IC(0) + ILU(0) + ILU(p) + ILUT, all crushing/matching Eigen.** **v4i STARTED (split i-1/i-2/i-3, advisor). v4i-1 ✅ SHIPPED 2026-05-26 — SPAI + FSPAI (sparse approximate inverse).** Classical **`SpaiPreconditioner<T>`** (right-SPAI `M≈A⁻¹`, `min‖AM−I‖_F` column-decoupled; static `pattern(A)` + **adaptive Grote-Huckle** augmentation; small dense LS via complex-capable `block_qr` + R-floor; general A → 6 nonsym selectors) + **`FspaiPreconditioner<T>`** (factored **`M=L·Lᴴ`, SPD-by-construction** — the SPD variant classical SPAI cannot give; Kolotilina-Yeremin/Huckle local HPD solve via `block_lu_solve`; static + Huckle-adaptive; SPD/HPD A → 3 SPD selectors PCG/MINRES/SYMMLQ). **Setup embarrassingly PARALLEL** (per-column `parallel_for`, capped per-worker scratch + diagonal fallback for dense columns; factor **bit-identical across thread counts** = determinism moat at setup); **apply = matrix-free spmv, NO triangular solve** (SPAI 1 spmv / FSPAI 2, delegated to `ParallelSpmvLeastSquaresOp`, size-adaptive parallel-SELL, GPU-mappable). 16 CLI (`{spai,fspai}.{f32,f64,c32,c64}` + selectors w/ `spai_pattern`/`spai_eps`/`spai_fill`). **Honest crush (gated bench, MATCHED TRUE residual — both sides on `‖b−Ax‖/‖b‖`, Cerid driven to rel_tol 1e-12 since the Krylov recurrence residual drifts): FSPAI-PCG CRUSHES Eigen IncompleteCholesky-CG on SPD — bcsstk24 9.77×/bcsstk25 3.70×/lap2d-160 2.10× WALL, and 1.9–3.1× PER-ITERATION (the durable structural no-tri-solve + parallel-apply win); adaptive cuts iters hard (bcsstk24 801→298, bcsstk25 5497→978); parallel setup 1.6–1.9× vs sequential IChol.** classical SPAI: parallel setup 1.6–2.3× vs Eigen ILUT + **converges to true 1e-10 (adaptive) on gemat11/sherman3 where Eigen IncompleteLUT-GMRES diverges** (robustness/breadth; Eigen ships no SPAI). Static SPAI(0) brittle on ill-cond nonsym (stalls gemat11) — adaptive is the production choice. **v4i-1 also shipped `ReorderedPreconditioner<T,Inner>`** (AMD fill-reducing reorder adapter reusing v2 `amd_order`; `M_inner≈(PAPᵀ)⁻¹` ⇒ apply=Pᵀ·M_inner⁻¹·P; composition primitive, no CLI per the KrylovPreconditioner precedent) — built after discovering (reading Eigen's source) that **Eigen's IncompleteLUT/IncompleteCholesky AMD-reorder internally**, which is the real reason Eigen edged Cerid on small-irregular sherman3 (the adapter narrows 2×→1.4×). Reordering is REGIME-DEPENDENT ([[feedback_nd_fill_regime_dependent_not_correctness]]): Cerid's structure-preserving DEFAULT CRUSHES Eigen 2.28× on large structured cd2d-200 (Eigen's forced AMD scrambles the parallel-tri-solve structure) ⇒ opt-in, not default = Cerid is MORE complete than Eigen here. +12 tests (SPAI/FSPAI + 4 reordered; nonsym/SPD convergence, adaptive, complex/Hermitian, determinism, AMD-solves-original-system); iterative suite **122 cases / 110074 assertions**; win-debug ctest+guards (fixed a pre-existing non-ASCII `Q·R` test name) + engine win-tidy + clang-cl clean (gcc/sweep→CI). **v4i-2 ✅ SHIPPED 2026-05-26 — Chebyshev polynomial preconditioner.** `ChebyshevPreconditioner<T>` (`chebyshev.hpp`): `M⁻¹≈p_deg(A)` via the `deg`-term Chebyshev three-term recurrence = `deg-1` MATRIX-FREE spmv + axpy, **NO factorization, NO triangular solve** (GPU-mappable, perfectly parallel, the AMG smoother for v4k). λmax via deterministic power iteration (**non-uniform seed** — a uniform vector collapses on Laplacian-like nullspaces — fixed iters, Rayleigh quotient); `lo=λmax·lo_ratio` (default 1/30, caller-overridable). SPD/HPD → PCG/MINRES/SYMMLQ selectors; `apply_adjoint==apply`. Determinism moat held (power-iter + parallel-SELL-spmv apply bit-exact across threads). 8 CLI (`chebyshev.{f32,f64,c32,c64}` + selector entries w/ `degree`/`cheb_lo_ratio`). **Honest bench: on well-conditioned lap2d-160 Cerid Chebyshev(8)-PCG CRUSHES Eigen IncompleteCholesky-CG 2.0× (35.7 vs 71.2 ms) at a tighter residual, matrix-free; Eigen ships NO polynomial preconditioner (breadth). On κ≈1e12 bcsstk13 it stalls — Chebyshev standalone needs a good spectral bracket (it is fundamentally the matrix-free parallel SMOOTHER, not an ill-conditioned standalone preconditioner; honest).** +4 tests (converge+beat-CG, degree-monotone strength, complex HPD, determinism); iterative suite **126 cases / 110866 assertions**; win-debug ctest+guards + engine win-tidy + clang-cl clean (gcc/sweep→CI). **v4i-3 ✅ SHIPPED 2026-05-26 — Additive + Restricted Schwarz; 🎉 v4i CLOSED.** `SchwarzPreconditioner<T>` (`schwarz.hpp`): overlapping domain decomposition `M⁻¹=Σ R̃ᵢᵀ Aᵢᵢ⁻¹ Rᵢ`; base partition (contiguous or ND via v2 `nd_order`) + `overlap` BFS layers; factor-once dense LU per subdomain (`detail/dense_lu.hpp`) + adjoint blocks; cap `|Ωᵢ|≤1024` + base-block fallback; sorted-Ωᵢ deterministic. **`Additive` (AS)** = symmetric for symmetric A WITH EXACT local solves ⇒ SPD/PCG (serial summed apply); **`Restricted` (RAS, default)** = non-overlap prolongation, nonsym/BiCGSTAB, **contention-free PARALLEL apply bit-exact across threads** (moat). 8 CLI (AS→SPD selectors, RAS→6 nonsym selectors). **Honest bench vs the CORRECT peer block-Jacobi (Eigen ships NO domain decomposition at all — benchmarking Schwarz vs Eigen IChol was an apples-to-oranges category error, corrected): RAS-Schwarz(overlap=1) BEATS block-Jacobi on nonsym cd2d-200 (117 it/766 ms vs 264 it/854 ms = 1.11× wall + 2.3× fewer iters) — overlap is the DD value-add; on SPD lap2d overlap cuts iters 374→120 but bigger dense blocks raise per-iter (block-Jacobi wins wall — honest knob).** One-level (no coarse space ⇒ 1/H²; the two-level coarse correction is AMG's job at v4k); the distributed-memory + AMG-component building block. +5 tests (131 cases / 111662 assertions); win-debug ctest+guards + engine win-tidy + clang-cl clean. **🎉 v4i CLOSED — SPAI + FSPAI (+ AMD-reorder adapter) + Chebyshev + Additive/Restricted Schwarz: the matrix-free / naturally-parallel / domain-decomposition preconditioner families Eigen lacks. v4j STARTED (multilevel ILU, ILUPACK-class — research-grade; re-split to 5 sub-slices, advisor: 1a MC64 / 1b multilevel scaffold / 2 inverse-based pivoting / 3 quality+head-to-head). **v4j peer = ILUPACK (the same-algorithm Bollhöfer-Saad gold standard, built on Linux/WSL where reference comparisons already run) + AMGCL as a secondary AMG-frontier data point — NOT Eigen (Eigen has no multilevel ILU; AMGCL is AMG, a different method).** **v4j-1a ✅ SHIPPED 2026-05-26 — MC64 max-weight matching + scaling (`crd-hesap-ordering/mc64.hpp` + `mc64.cpp`).** Duff-Koster: max-product matching → min-sum LAP on `c_ij=log(rowmax_i/|a_ij|)` solved by sparse shortest-augmenting-path (binary heap, lexicographic `(dist,col)` ⇒ deterministic) + greedy init; dual potentials `u,v` → scaling `D_r[i]=exp(u_i)/rowmax_i`, `D_c[j]=exp(v_j)` giving the I-matrix form (|matched diag|=1, |off-diag|≤1). Heavy LAP core in `.cpp` (f64 magnitudes, compiled once) + templated wrapper (f32/f64/c32/c64 via |a_ij|, overloaded — no hesap-dense dep). CLI `hesap.ordering.mc64` (colperm+D_r+D_c blob). The robustness front-end ILUPACK/MUMPS/SuperLU use; reusable by the ILU family + the v4j-1b multilevel scaffold. +4 tests (cyclic matching, I-matrix property on a badly-scaled matrix [validates the duals], determinism, complex); ordering suite 40 cases / 7760 assertions; win-debug + clang-cl + win-tidy clean. **NEXT: v4j-1b (multilevel scaffold) → v4j-2 (inverse-based pivoting) → v4j-3 (head-to-head vs ILUPACK).** **v4j-1b ✅ SHIPPED 2026-05-26 — MultilevelIlu scaffold (`multilevel_ilu.hpp`).** `MultilevelIlu<T>` = MC64 (v4j-1a) match+scale → build `B=D_r·A·D_c·Pᶜ` (matched entries on diagonal, I-matrix-scaled) → ILUT(B); `apply M⁻¹r = D_c·Pᶜ·(ILUT(B)⁻¹·(D_r·r))`, adjoint likewise. **Single-level placeholder** = the recursion entry point v4j-2's inverse-based pivot test (‖L⁻¹‖,‖U⁻¹‖≤κ → defer rows to a coarser level) populates. A working robust preconditioner now: MC64's two-sided scaling+matching fixes the conditioning of hard/off-diagonal-dominant matrices plain ILUT chokes on. 8 CLI (`mlilu.{f32,f64,c32,c64}` + `mlilu` in the 6 nonsym selectors). +4 tests (solves an off-diagonal-dominant system [validates the MC64 transform], **beats plain ILUT on it**, complex, determinism); iterative suite **135 cases / 112068 assertions**; win-debug ctest+guards + engine win-tidy + clang-cl clean. **NEXT: v4j-2 (inverse-based pivoting — the real multilevel recursion) → v4j-3 (vs ILUPACK).** Then v4k–m (AMG).** (then v4j multilevel-ILU + v4k–m AMG per the v4 ledger.) **NEXT: v4e-3** (cross-solve `RecycleSpace<T>` recycling — the de Sturler iters-saved-across-a-sequence payoff — + RMINRES recycling-MINRES for symmetric/indefinite). The v4 cluster opens with **v4-corpus** (user directive): SuiteSparse matrices become **first-class cooked engine resources** — new `crd-hesap-resources` bridge module (`'HMTX'` FourCC + CRDR cooked CSR + cooker `.mtx`→binary + runtime `ILoader` + `hesap.matrix.{fetch,cook,load,info}` CLI; **ADR-0084**) so the framework + every vs-reference bench loads the corpus through `ResourceManager` (eviction/hot-reload/replay for free). Then **v4a** = Krylov framework (`KrylovWorkspace<T>` + `IterativeResult` + deterministic `blas1`-routed KBN `kdot`/`knrm2` = the determinism moat) + CG/PCG + Jacobi/block-Jacobi/SSOR, creating **two** modules (`crd-hesap-iterative` + `crd-hesap-preconditioners`). Concrete v4a skeleton + advisor-vetted design pins live in the phase doc. **Full v4 plan + 18-subslice ledger in `docs/phases/phase-3.1.6-hesap.md` § "v4 — Iterative solvers + preconditioners + AMG — DETAILED PLAN" (and the `↳ v4-corpus, v4a..v4z` rows in the slice table).** COMPLETE family, NEVER-DEFER (`feedback_hesap_substrate_never_defer_features` — block-Krylov + nested + AMG all IN v4; only sparse-DIRECT is v5). 4 modules (`crd-hesap-resources`/`-iterative`/`-preconditioners`/`-amg`), solvers all over the matrix-free `LinearOp`. 3 pillars per slice: **determinism** (bit-exact across {1,2,4,8,16} threads — the moat, gated) + **correctness** (SuiteSparse matrices loaded as resources) + **performance** (beat Eigen + AMGCL/PETSc/hypre). **Advisor-vet each v4 slice design before implementing (project rule).** KEY: refs (Eigen ComplexSchur + zhseqr) AV at n≥256, so gate AED vs OUR OWN single-shift `complex_schur` baseline (beat it at scale + match accuracy), not external refs. Big simplification: no 2×2 blocks → no dlanv2/dlasy2/dlaexc. Reuses complex_schur/complex_givens/complex sqrt/zgehd2/gemm. Then 2c-3 (`ztrevc` + back-transform + complex `eig` + CLI), v3d close + v3e (§17 lock: verify D(non-sym)-6 zlahqr constants vs zlahqr.f). **(Pre-flight commit of the v3c+v3d body DONE this session.)** v3c (least-squares family: col-piv QR `dgeqp3` + COD + `lstsq`/`pinv`/`nnls`/`tls`) shipped; v3a (symmetric + Hermitian eig) **v3a-1** (real symmetric: blocked dsytrd + QL/QR + Cuppen D&C) **beats Eigen 1.2–1.9× / LAPACK 1.9–4.0×** at N=64–1024. **v3a-2.5** (complex Hermitian `eig_herm`): **SIMD complex reduction** — the working Hermitian is carried as two REAL arrays `ar`/`ai` (lower tri) so the zhetd2 matvec + rank-2 update are contiguous-row real-SIMD (`simd_dot`/`simd_axpy`), no scalar complex; reflectors land in `ar`/`ai` and the back-transform reads them directly (no complex materialization). **Beats Eigen N≥128 (1.11× → 2.22× at N=1024), ties N=64 (1.00× — floor, Eigen's hand-inlined small `zhetrd`); beats LAPACK zheev everywhere (2.5–4.4×); accuracy ~1e-14.** CLI `hesap.dense.eig.herm.{c32,c64}`; full hesap-dense suite **206 cases / 98,716 assertions**; 5-config DoD PASS (-IncludeRelease; win-tidy naming fix re-verified). **(v3a-3 MRRR + v3b SVD now shipped; v3d-2 eigenvectors is next — see the v3 status at the top of this paragraph.)** (Full v1 sparse + v2 reorderings history below.) **v2 reorderings CLOSED ✅ (v2a–v2e)** in `crd-hesap-ordering` — nested dissection + CAMD beats Eigen-AMD on 2/3 FEM matrices. Module `crd-hesap-sparse`. Pattern/values/analysis **trinity** + `SparseMatrix<T,Format>` (Format NTTP, move-only); `SparseId`; `SparseFormat`; `'HSPM'` pin; determinism spec (`docs/systems/hesap-sparse.md`, D1). **COO `TripletBuilder<T>`** → CSR `compress()` + CSC `compress_csc()` (counting-sort outer bucket + per-outer `crd::containers::stable_sort` + dedup-merge summed in insertion order = deterministic, bit-reproducible). **Uncompressed Eigen-4-array mode** (`make_uncompressed`/`coeff_ref` insert/grow/`make_compressed`); **canonical slack-invariant `topology_hash`** (compressed == uncompressed; golden `0x9978E97C37B7D174`). **Structural queries** (`structural_stats`/`inner_indices`). **19 CLI commands** (`from_triplets`/`to_csr`/`build`/`to_csc` ×4 + `nnz`/`density`/`structural_query` type-agnostic). `smoke_hesap_sparse`. **Assembly bench vs `Eigen::setFromTriplets`** (win-release): **WIN at every size — 1.03× @ N=50k, 1.44× @ N=200k, 1.76× @ N=1M** (optimized `assemble`: direct scatter + in-place sort/dedup-compact + new `crd::containers::Array::resize_uninitialized`; beat Eigen across the board after an initial 0.74× small-N loss). Raw `T`. **29 cases / 212 assertions PASS.** 🎯 **NOW: v1b spmv (subdivided v1b-1/2/3). v1b-1 CLOSED ✅.** **v1b-1**: `spmv(α,A,Trans,x,β,y)` (CSR scalar, deterministic L-to-R two-rounded; β=0 NaN-safe; None/Transpose/ConjTranspose; compressed-CSR assert) + `SparseLinearOp<T>` (first sparse `LinearOp` consumer; apply/transpose/adjoint bit-identical to kernel) + 8 CLI (`spmv`/`spmv_transpose` ×4) + cross-platform `y` golden. **D(sparse)-3 pinned** (per-row reduction two-rounded `mul_add` ≠ `simd::fma`; SELL pad=0; compressed-only). ST bench: CSR scalar **0.09–0.17×** of Eigen (latency-bound serial accumulator — the gap the SELL primary must close). **38 cases / 254 assertions PASS; 4-config DoD PASS.** **v1b-2(+3) CLOSED ✅ — SELL-C-σ spmv beats Eigen.** SELL storage (C=8 f32/4 f64) + σ row-length sort (global; identity fast-path for uniform/banded) + CSR→SELL + SELL spmv (f64 slice-pair AVX2: `_mm256_i32gather_pd` + prefetch + two-rounded `mul+add`, bit-exact vs CSR baseline) + `spmv_sell_parallel` (nnz-balanced over `crd::jobs`, bit-exact vs serial at any worker count) + `spmv_sell.{T}` CLI. **v1b-3 (parallel) merged into v1b-2** (MT deferral rejected — user directive: implement, don't defer). **Honest regime-dependent shootout vs Eigen** (best-of-15+warmup, f64): **ST WIN 1.21–1.27× at N=2M (DRAM-bound — the regime that matters for large sparse)**; cache-resident N=1M mixed (Eigen's leaner scalar wins uniform-16 0.92×); **MT parity-or-better** (banded 1.13–1.35×, uniform-16 1.05×; bandwidth-wall tie elsewhere). Gate (user call): dominate DRAM-bound ST + Eigen-MT parity (replaced ≥60% STREAM-triad proxy). **46 cases / 30,034 assertions; 5-config DoD (-IncludeRelease) PASS** (win-release full ctest 2868/2868 — note: had to force `CRD_BUILD_HESAP_VS_REFERENCE=OFF` in the win-release cache where a stale bench flag had leaked OpenBLAS into the DoD build + hit a transient MSVC ICE; my code builds clean under LTCG). **v1b CLOSED.** 🎯 **v1c CLOSED ✅ — element-wise + structural + conversions.** Conversions (`to_csc`/`from_csc`/`to_coo`/`transpose` — one `organize_by_inner` kernel, `transpose∘transpose==A` byte-exact) + element-wise (`add`/`subtract` topology-hash fast-path + symbolic-union merge, `scale`, `hadamard`; **D(sparse)-5** = `a OP b` left-first single-rounded) + structural (`extract_diagonal`/`scale_rows`/`triu`/`tril`/`submatrix`). All `[[nodiscard]]` factories, 4 type variants, deterministic + bit-exact. Conversion hub=CSR; BSR/ELL/DIA + runtime `convert(from,to)` dispatch land at v1f. **36 CLI commands added.** **61 cases / 99,258 assertions; 4-config DoD PASS on all 3 sub-subslices** (v1c-1/2/3). 🎯 **v1d CLOSED ✅ — spgemm BEATS Eigen on real SuiteSparse.** Gustavson dense-SPA: serial fused single-pass (O(flops)) + two-phase parallel (flop-balanced A-row partition over `crd::jobs`, per-worker SPA via `worker_index`, bit-exact vs serial any worker count). C=A·B + `spgemm_ata` (A·Aᵀ); f32/f64/c32/c64 (complex + A·Aᵀ folded in — defer nothing). **Verified on REAL SuiteSparse** (user: max rigor — pulled a Matrix-Market reader + gated `file(DOWNLOAD)` fetch forward; engine MM I/O+writer+CLI stay v1g): **C=A·A vs Eigen-ST median par/Eigen = 2.32×, 5/6 WIN** (bcsstk13 2.86× / bcsstk24 2.81× / bcsstk25 2.63× / gemat11 2.02× / sherman3 1.19×; tiny west2021 0.92× small-input edge, user-accepted). `.pruned()` fairness trap caught. 16 CLI commands. **70 cases / 146,183 assertions; 4+5-config DoD PASS.** 🎯 **v1e CLOSED ✅ — spmm (sparse×dense) + SDDMM.** `spmm(α,A,B,β,C)` (B/C dense row-major, `ldb`/`ldc` for block-Krylov slicing; accumulate into C[i,:], init-once NaN-safe, auto-vec axpy over RHS) serial + nnz-balanced row-parallel (disjoint C rows = bit-exact). SDDMM `C=α·sample(X·Yᵀ, mask)` (only masked dots; output carries mask pattern; row-parallel bit-exact; explicit-SIMD multi-accumulator dot, two-rounded, cross-width bit-exact). f32/f64/c32/c64 (generic kernels instantiate directly). **SDDMM bench `bench_hesap_sddmm_vs_reference`: vs dense-then-mask 26–434×; vs same-flops Eigen per-entry dot — compute-bound matrices WIN (gemat11/sherman3 r=32 1.31–1.49×), high-nnz bcsstk24 0.62–0.76× = random-Y-gather cache wall (parallel scales only 1.4×, single-rounded fma moves it 0% → not a compute/determinism tax; user-accepted at the wall).** **spmm vs Eigen on v1d SuiteSparse, r∈{1,4,32,128}: crushes Eigen-ST 5–12× everywhere; vs Eigen-MT wins the heavy-RHS regime (r=32 1.32–1.33×, r=128 1.79–1.94×), ties small-RHS (gemat11 r=4 0.96×) at the irreducible B-gather bandwidth wall** (= the spmv regime; per-job binary-search overhead removed after it regressed small-r; user-accepted "chased to the wall"). **8 CLI commands** (`spmm`/`sddmm` × {f32,f64,c32,c64}). **82 cases / 283,311 assertions; 5-config DoD (-IncludeRelease) PASS.** **v1e CLOSED.** 🎯 **v1f CLOSED ✅ — block + structured formats BSR + ELL + DIA.** **BSR** `BsrMatrix<T>` (CSR-of-blocks, dense b×b) + small-block GEMV (compile-time b∈{1,2,3,4,6} dispatch + runtime fallback, b-way ILP accumulators, two-rounded β=0 NaN-safe; serial + block-row-balanced parallel bit-exact; **dedicated kernel NOT the v0d microkernel — D(sparse)-6**) + CSR↔BSR (zero-pad partial blocks, D(sparse)-7). **ELL** (slot-major global-pad, interop/base — SELL stays the irregular perf path) + **DIA** (diagonal-major, contiguous-x stream), each storage + spmv (serial+parallel) + CSR↔X. f32/f64/c32/c64; **36 CLI commands** (to_/from_/spmv × {bsr,ell,dia} × 4). **Gates CRUSHED on native patterns** (Eigen has no BSR → vs Eigen scalar-CSR on same matrix): **BSR par 3.45–6.72× Eigen / 3.20–6.46× our CSR (even serial 1.8–2.0× Eigen); ELL 4.9–5.2×; DIA 4.8–5.9×.** **87 cases / 672,343 assertions; 5-config DoD (-IncludeRelease).** 🎯 **v1g CLOSED ✅ — Matrix-Market I/O + spgemm cap-lift + CLI audit + v1-close.** **v1g-1:** engine-side `.mtx` reader/writer (in-memory, no platform dep; coordinate × real/complex/integer/pattern × general/symmetric/skew/hermitian; array rejected) + 8 CLI (`mtx_read`/`mtx_write` ×4). **v1g-2:** spgemm adversarial stress (wins all in-cap 2.5–5.5× vs Eigen-ST → no BRMerge/MAGNUS refinement needed) + **hash-accumulator path lifts the 4M-col dense-SPA ceiling** to arbitrary cols (bit-exact with dense; per-job hashes pre-sized — allocating inside `parallel_for` from non-thread-safe Tlsf was a real heap-corruption bug, fixed) + **fixed `crd::containers::sort` hidden malloc** (`sort`→in-place introsort zero-alloc; `stable_sort`→caller `IAllocator*`/scratch). **v1g-3:** CLI-completeness audit (grep-diff → +19 commands: from_csc/scale_rows/submatrix/to_sell/spmv_adjoint/inner_indices) so **every op has a CLI**; **ADR-0065 §15** locks D(sparse)-1..9; killed two recurring upstream-toolchain ICEs on `crd-sandbox-showcase-tests` (imgui-vendor IPO-off → win-shipping LTCG ICE; PCH-off → gcc-13 asan ICE — both validated standalone on their failing config). **🎉 Phase 3.1.6 v1 (sparse) CLOSED — full module: COO/CSR/CSC/SELL/BSR/ELL/DIA + spmv/spmm/spgemm/SDDMM + conversions + element-wise + structural + MM I/O, ~146 CLI commands, beats Eigen across the board.** 🎯 **v2 STARTED — fill-reducing reorderings + symbolic factorization → NEW module `crd-hesap-ordering` (hesap continues to completion before eylem resumes).** **v2a CLOSED ✅:** `AdjacencyGraph` (symmetrised A∪Aᵀ) + `Permutation`/`apply_symmetric` + **RCM** (George-Liu pseudo-peripheral) + symbolic fill metric (`elimination_tree`/`column_counts`/`nnz_l` — CSparse port in signed i32) + bandwidth/profile + 7 type-agnostic CLI. **Port bit-exact-validated vs Eigen `SimplicialLLT` nnz(L)** (bcsstk13/24/25 exact MATCH); RCM bandwidth 1250→422 / 3333→251; v2b AMD target recorded. D(ord)-1..4 determinism pinned (`docs/systems/hesap-ordering.md`). **5 cases / 128 assertions; 4-config DoD PASS.** **v2b CLOSED ✅ — AMD beats/ties Eigen-AMD fill.** Quotient-graph approximate-minimum-degree built up a 4-rung ladder (v2b-1 machinery → rung 1 packed workspace → rung 2 exact min-degree → rung 3a Amestoy approx degree [~11× ordering speedup] → rung 3b supervariables + aggressive absorption → rung 3c **faithful `cs_amd` port: mass elimination was the gate-closer**). **Fill nnz(L) 0.989×/1.002×/1.044× Eigen-AMD on bcsstk13/24/25 — beats on bcsstk13, ties on bcsstk24, within ≤1.05× gate on bcsstk25; ordering 3–15 ms.** Mass elim + supervariables + aggressive absorption + dense-node-last (deg>max(16,10√n)) all faithful to cs_amd; degree formula confirmed algebraically identical. cs_amd POSTORDERS its assembly tree (step-by-step elim-order compare vs Eigen is invalid — postorder is fill-invariant; gap was real elimination-quality). bcsstk25 residual = un-isolated tie-break/iteration-order divergence (user-confirmed not a defer: AMD is a heuristic, fill is downstream-perf-only, never correctness; tracked follow-on `v2b-amd-cs_amd-tiebreak-isolate`). +D(ord)-5/-6 pinned. **+2 CLI (`amd`/`amd_nnz_l` → 9 ordering commands); 7 cases / 360 assertions; 4-config DoD PASS.** **v2c CLOSED ✅ — full symbolic factorisation; L-pattern bit-exact vs Eigen; symbolic analysis beats Eigen at scale.** `postorder` (cs_post, public) + `SymbolicFactor` (etree + post + colcount + full **L pattern CSC** via faithful `cs_ereach` row-subtree port + **fundamental supernodes** Liu-Ng-Peyton/CHOLMOD `super_symbolic` test) + `symbolic_factorize` driver (one adjacency build, shared scratch). **Rigorous PATTERN gate MET bit-exact** — column-by-column L row-index diff vs `Eigen::SimplicialLLT<Lower,NaturalOrdering>` factor = MATCH on bcsstk13/24/25 (the actual structure, not just nnz). **Symbolic analysis beats Eigen `analyzePattern` 1.77× (n=3562) / 1.49× (n=15439)** — scales better (our 2.4× vs Eigen 4.4× over the 7.7× n-range); small-N bcsstk13 0.80× = `build_adjacency` alloc/sort constant-factor (filed `v2c-small-n-analyze-constant-factor`, NOT algorithmic — pattern gate passed). Eigen has no full-Li symbolic twin (defers Li to `factorize`, `SimplicialCholesky_impl.h:143`). Pure integer, no new D(ord) pin. **+4 CLI (`postorder`/`symbolic_nnz_l`/`supernode_count`/`supernodes` → 13 ordering commands); +10 cases → 17 cases / 2412 assertions; 4-config DoD PASS.** **v2d CLOSED ✅ — multilevel-ND scaffold, near-perfect balance.** `WeightedGraph` (CSR + adjwgt + vwgt) + heavy-edge-matching coarsening (`coarsen_match` + `contract`, sorted, weight-conserving) + `coarsen` level stack (stop n≤100 / n_coarse≥0.9n / ≤30 levels) + `bisect_coarsest` (**re-seeding** BFS region-grow = disconnected-graph contract, +D(ord)-7) + `project_down` + `edge_cut` + public `nd_bipartition`. **+1 CLI (`nd_bipartition` → 14 ordering commands), shipped now (not deferred — user directive).** **Scaffold balance is PERFECT/near-perfect** (50/50, 72/72, 112/113, 90/90; disconnected 54→27/27; multilevel 225→coarsest 57/3 levels; tridiagonal cut=1 optimal). No FM yet (v2e). Conservation + well-formedness + matching-validity + determinism all verified. **+10 cases → 25 cases / 5664 assertions; 4-config DoD PASS.** **v2e CLOSED ✅ — nested-dissection ordering + CAMD; BEATS Eigen-AMD on 2/3 FEM matrices.** **FM** (`fm_refine`: gain buckets, best-prefix rollback, balance 1.03, both-sides-non-empty) reaches optimal grid cuts (40×40→20) + 51–65% cut reduction on bcsstk*. **König min-vertex-separator** (bipartite max-matching) + **node-FM**. Recursive **`nd_order`** = separator-tree postorder `cmember` → **`camd_order`** (constraint-aware copy of the cs_amd port — CHOLMOD-style: per-class min-degree on the FULL graph, dense-off, supervar/mass-elim cmember-gated; this is the **interface-fill fix** — plain per-subdomain AMD leaks fill into live separators). +AMD-hybrid leaf threshold (100). **Gate MET on FEM subset: nd fill beats Eigen-AMD on bcsstk13 (0.983×) + bcsstk24 (0.999×)** (and our AMD 0.994×/0.997×); **bcsstk25** (large 3D multi-DOF) loses 1.158× — opposite the textbook asymptote; root-caused to the multi-DOF supervariable-vs-cmember interaction → tracked follow-on **`v2e-weighted-compression`** (graph compression needs vertex-weight propagation; unweighted compression regressed, reverted). **CAMD validated** (CAMD-uniform == AMD exactly); valid-perm + determinism + disconnected + n=0/1/2 all green. Lesson: ND fill is **regime-dependent** (1D path: AMD is provably optimal, ND legitimately worse; ND wins on 2D/3D FEM) and is a **downstream-perf knob, never correctness** (any valid permutation → identical solve). **+5 CLI (`nd_bipartition`/`nd_order`/`nd_nnz_l`/`supernode*` → 16 ordering commands); 36 cases / 7275 assertions; 4-config DoD PASS.** **🎉 Phase 3.1.6 v2 (reorderings) CLOSED — RCM + AMD + symbolic factorisation + nested dissection + CAMD, beats Eigen on AMD-fill (2/3 FEM) and symbolic analysis.** ADR-0065 §16 locks D(ord)-1..7; 18-config sweep on CI. **NEXT = v3** (SVD: Golub-Reinsch + randomized; dense eig: MRRR + QR-shift; least squares) … through the full hesap roadmap (v3–v18). **(18-config sweep: prior run found only the 2 sandbox-showcase upstream ICEs, fixed + verified standalone; CI validates residual cross-config on push.)** **(18-config sweep: first run found only the 2 sandbox-showcase upstream ICEs, both fixed + verified standalone on win-shipping & linux-gcc-asan; re-sweep skipped per `feedback_targeted_fix_skip_resweep` — CI validates residual cross-config on push.)**

**Phase 3.1.6 `crd-hesap` v0 (dense foundation) — CLOSED** (18-config full sweep delegated to CI for the canonical `RESULT: PASS`). All sub-slices v0a–f shipped; **ADR-0065 §14 decision lock Accepted**. v0f shipped slimmed: a seeded `RandomMatrix` test factory + 5 property tests + a `crd_add_hesap_vs_ref_bench()` CMake helper; the speculative `crd-hesap-bench` sub-module + committed LAPACK/SuiteSparse/FFTW fixtures were deferred to the FFT/sparse slices that need them. **hesap-dense suite: 176 cases / 66,703 assertions PASS.**

**v0-close caught + fixed 6 latent v0d cross-config bugs** (the first full sweep since `simd::fma()` landed; the 5-config per-slice DoD only builds MSVC-AVX2): gcc/clang need `-mfma` (`_mm256_fmadd`); `Vec4f` needed an `fma()` for the scalar/SSE2 `Vec8f` fallback; clang `-Werror` on an unused lambda capture (`syrk_microkernel.hpp`) + unused fn (`cli_register.cpp`); scalar/SSE2 unused prefetch locals in `blas2.cpp`; and `complex.hpp::arg()` used `std::atan2` (ADR-0063 violation) → now `crd::math::deterministic::atan2` via a new `crd-hesap → crd-math` PUBLIC edge. `-mfma` is determinism-safe: `mul_add` stays two-rounded under `-ffp-contract=off`; only explicit `simd::fma()` emits hardware FMA. Lesson saved in memory ([[feedback_full_sweep_catches_cross_config_simd]]).

**What v0 delivered (v0a–f):** substrate (`Complex<T>` + ~30-type matrix catalog + `LinearOp<T>` + typed handles + `'HDV0'` pin + CLI protocol scaffolding) → BLAS L1 (KBN pairwise + 8-acc SIMD) → BLAS L2 (single-pass symv + tiled gemv) → BLAS L3 (Goto/BLIS task-DAG gemm, **10/10 WINS over Eigen-MT**) → dense direct solvers (LU/Cholesky/LDLT/QR + LinearOp + Hager κ₁ + Wilkinson IR) → property/bench infrastructure. Reference-class solver shootout (i9-14900K AVX2, factor+solve): **LU WINS N≥128; Cholesky/QR/LDLT WIN or tie across most N**; small-N dense-factor residual root-caused to row-major-vs-Eigen-column-major layout fit (**ADR-0083 Accepted**, not a kernel-quality gap).

**§14 lock (ADR-0065):** formalises the v0 implementation decisions L50–L55 + the v0e set (v0e-D1…D8) + records every deviation from the §13 elite plan (bench sub-module deferred, `bench_common.hpp` deferred to third bench, microkernel AVX-512/NEON/SVE2 hardware-gated, mixed-precision IR filed `v0e-f2`). See ADR-0065 §14.

**Post-v0-close CI hardening (2026-05-20):** CI's MSVC 14.44 (vs local 14.50) + clang-cl surfaced more cross-config issues after the push, all fixed: (1) **C4127** "conditional expression is constant" globally suppressed via `/wd4127` on `crd-warnings` (matches the existing `/wd4714` precedent — a version-skewed, low-value warning with no clean per-site fix for mixed compile-time/runtime template conditions like `curve.closed`); the one genuine constexpr pin check (`smoke_hesap_substrate` FourCC) became a `static_assert`. (2) **clang-cl determinism hole**: clang-cl's `/fp:precise` left within-statement FMA contraction ON (unlike MSVC `cl`), violating ADR-0063 and breaking the LU exact-singular `==0` detector (`4-(2/3)*6` was ~2.2e-16 instead of 0) → forced `/clang:-ffp-contract=off`; the two singular tests now pass + were hardened with a structural-zero-column matrix (the rank-deficient case kept as a determinism canary). Lessons: [[feedback_msvc_c4127_ci_local_version_skew]], [[feedback_full_sweep_catches_cross_config_simd]].

🎯 **NEXT = v1 sparse — full 7-sub-slice plan in `docs/phases/phase-3.1.6-hesap.md`** (§ "v1 — Sparse storage + kernels — DETAILED PLAN"). **Core-tier scope** (2026-05-20 review, Tier-3 dropped): COO builder + CSR/CSC + BSR/ELL/**SELL-C-σ**/DIA; spmv (SELL primary — Eigen has no SELL) + spmv-T + spmm + spgemm (parallel hash, beats Eigen's `AmbiVector`) + element-wise + conversion graph + `SparseLinearOp` + Matrix-Market I/O + complex + **CLI command per op, registered per-slice**. **Dropped:** CSR5/Merge-CSR → v17 GPU; JDS/SkyLine → filed follow-on. **First slice = v1a** (`SparsePattern`/`SparseValues`/`AnalysisHandle` trinity + COO `TripletBuilder` → CSR/CSC + `'HSPM'` pin + determinism spec). Anti-rabbit-hole rules baked in: numeric perf stop-criteria per slice, cross-config from day 1, spgemm split (v1d core / v1g elite). Then **Phase 3.1 eylem v1c+** resumes (consumes geometry + hesap-dense from day 1).

**Filed v0 follow-ons** (all hardware-/consumer-gated, NOT blocking v0):
- `v0d-microkernel-{avx512,neon,sve2,blocks-empirical-sweep}`, `v0d-asm-microkernel` (ADR-0082 gate), `v0d-small-gemm-fastpath`
- `v0e-a2` (LU complex), `v0e-b-hpd` (Hermitian Cholesky), `v0e-c-blocked`, `v0e-d-blocked`, `v0e-d-colpiv`, `v0e-e2`, `v0e-f2` (mixed-precision HPL-AI IR), `v0e-g-eigen-mt-config`
- `vs-ref-blas2-followups`, `crd-hesap-bench` sub-module + reference-fixture replay (FFT/sparse-gated), `bench_common.hpp` (third-bench-gated)

**Earlier 2026-05-20 — Phase 3.1.6 `crd-hesap` reference-class L1 + L2 + L3 shootout closed 2026-05-20.** Head-to-head benchmarks vs **Eigen 3.4 + OpenBLAS 0.3.27** at every level of the BLAS hierarchy. Single-thread P-core results except GEMM which is multi-threaded 16 P-threads. Final ratios on i9-14900K AVX2:

- **BLAS L3 GEMM (multi-threaded, 16 P-threads, best-of-3): 10/10 WINS over Eigen-MT.** f32: N=256 **368×**, N=512 **1.21×**, N=1024 **1.69×**, N=2048 **1.70×**, N=4096 **1.30×**. f64: N=256 **1.17×**, N=512 **1.19×**, N=1024 **1.39×**, N=2048 **1.12×**, N=4096 **1.01×**.
- **BLAS L1 (single-thread, f64): mixed. axpy 4/5 WINS (1.15-1.70×) at L1/L2-resident sizes, loses at N=262144 (memory-bandwidth-bound, both ~9 GFLOPS). dot 0.71-1.07×. nrm2 0.64-1.48× (WINS at N=1024-4096, loses 0.64-0.82× at L3-overflow). vs OpenBLAS: WINS everywhere 1.6-14.7×.**
- **BLAS L2 (single-thread, f64): gemv 3/7 WINS at N=1024+ (1.02-1.25×), 0.89-0.98× at small N. symv 0/7 wins, all 0.72-0.99× (N=4096 is virtually tied at 0.99×). trsv 1/7 WIN at N=128 (1.04×), 0.75-0.96× elsewhere. vs OpenBLAS-MSVC: WINS everywhere 1.4-5.4× across all L2 ops.**

The journey for the hardest cases — **symv N=2048: 0.01× → 0.85× (85× absolute improvement); symv N=4096: 0.02× → 0.99× (50× improvement); gemv N=4096: 0.85× → 1.25× WIN; trsv N=4096: 0.37× → 0.75×.**

The five core perf changes that landed this slice: (1) `crd::math::simd::fma(a,b,c)` single-rounded IEEE 754 FMA on Vec8f + Vec4d (ADR-0063 still respects two-rounded `mul_add` for eylem); (2) hesap GEMM microkernel + `simd_dot_*` / `simd_sumsq_*` helpers use `fma()` for ~2× FMA-port throughput; (3) Mc auto-tune + small_gemm_parallel direct unpacked fast-path; (4) classic BLAS single-pass symv (each lower-half element touched once → updates both y[i] and y[k] — halves memory bandwidth); (5) 8-row tiled gemv + 16-wide-unroll symv with 4 dot accumulators + gated `_mm_prefetch(_T1)` on next row of A (only when `n > 512`).

**Reference-class deps gated via `CRD_BUILD_HESAP_VS_REFERENCE=ON`** — Eigen + OpenBLAS fetched into `build/_deps/`, never vendored. OpenBLAS-on-MSVC limitation documented (upstream `FORCE_GENERIC` hardcoded; real asm-tuned OpenBLAS requires Linux/MinGW/vcpkg).

**Allocator propagation through hesap**: `gemm/gemm_parallel/gemm_parallel_auto/small_gemm_parallel/symv` now take/propagate `IAllocator*` scratch from the input Matrix/Symmetric's allocator — no more `default_allocator()` (MallocAllocator) inside library code.

**Continuous-benchmarking policy** (`docs/PRINCIPLES_reference_class_benchmarking.md`): every performance-critical kernel Cerid ships from here on runs a head-to-head shootout vs Eigen + OpenBLAS + the appropriate reference. Future kernels under this policy: v0e dense direct solvers, v1 sparse, FFT, eigensolvers, jobs/sort primitives.

**Filed for later — explicit deferred work** (all hardware-gated or asm-class):
- `v0d-asm-microkernel` — hand-tuned asm per microarchitecture for the last 5-25% gap on L2 ops. Deferred indefinitely per ADR-0082's three-condition revisit gate (not triggered — intrinsics+FMA already at 70-100% single-core peak; gap to MKL/BLIS-asm is <30%).
- `v0d-microkernel-avx512` / `-neon` / `-sve2` — wider SIMD paths. Hardware-gated.
- `v0d-microkernel-blocks-empirical-sweep` — per-box Mc/Kc/Nc tuning. Low priority.
- `vs-ref-blas2-followups` — close gemv 0.89× small-N + symv mid-N + trsv 0.75-0.83× large-N. Requires either asm or AVX-512.

**Test state**: 118 hesap-dense cases / 4731 assertions PASS + 7 hesap-sched cases / 82 assertions PASS = **4813 assertions GREEN** across the full hesap surface. 5-config DoD (debug + asan + shipping + release + tidy) all green.

Session log: `docs/sessions/2026-05-19-hesap-vs-reference-shootout.md` (now the canonical record of the full L1/L2/L3 work).

🎯 **NEXT options** (in priority order):
1. **v0e dense direct solvers** — LU + Cholesky (SPD + HPD) + QR + LDLT + iterative refinement + LinearOp factor view + condition estimation. First real consumer of `gemm_parallel`. ~1200 LOC + ~120 tests + ~7 d. Per reference-class policy ships with shootout vs Eigen LU/QR/LLT + LAPACK.
2. **v0d follow-on work** when hardware or consumer demand arrives (AVX-512 / NEON / SVE2 / asm microkernel / L2 mid-N gap closure).

**Earlier 2026-05-19 — Phase 3.1.6 `crd-hesap` v0d-parallelism shipped 2026-05-19** — BLIS-style outer-loop GEMM parallelism via new `gemm_parallel<T, L>(num_workers, …)` API. Algorithm: scale C by beta upfront → outer (jc, pc) sequential → pack Bc once → `parallel_for` distributes ic row-slab tiles across workers → each worker packs its own Ac panel + invokes the microkernel into its disjoint slab of C. **Bit-exact across thread counts** (`std::memcmp` on output) — verified across worker counts {1, 2, 4, 8, 16} at sizes {64, 256, 1024} for f32 + f64, plus transposed + non-multiple shapes + repeated-run determinism. **124 cases / 4754 assertions PASS** (+8 cases / +25 assertions). Scaling on dev box (AVX2, ~5.58 GHz, win-release, N=1024): **f32 = 4.71× at 32 workers (508 GFLOPS); f64 = 4.20× at 16 workers (227 GFLOPS)**. Three debugging traps documented in session log: (a) `worker_index() % num_workers` aliases packed-A buffers (size by total threads not chunk count), (b) lambda 13-capture exceeds 41-byte SBO (pack into pointer-captured struct), (c) parallel_for's per-thread 1 MB frame arena exhausts across many calls (bench resets between iters). 5-config DoD with `-IncludeRelease` running at session close.

🎯 **NEXT options** (in priority order):
1. **v0e dense direct solvers** — LU + Cholesky (SPD + HPD) + QR + LDLT + iterative refinement + LinearOp factor view + condition estimation; first consumer of `gemm_parallel`; ~1200 LOC + ~120 tests + ~7 d.
2. **v0d-microkernel-tune** — AVX-512 + NEON + SVE2 microkernels + prefetch + block tuning + ≥70% AVX-512 peak benchmark per HW-floor mandate; multi-session.
3. **v0d-parallelism-auto-dispatch** — heuristic to pick `num_workers` from `min(m / Mc, num_workers())` with a serial floor; future API ergonomics.

### Earlier same day

**Phase 3.1.6 `crd-hesap` v0d-perf-f64-avx2 shipped 2026-05-19** — Vec4d AVX2 SIMD substrate + f64 microkernel. f64 at N=1024 = 51.92 GFLOPS = 57.3% peak (was 17%); 3.3× uplift. Per-user-mandate: no shortcuts — Vec4d substrate added to `crd-math::simd` (alongside Vec4f / Vec8f) as a proper wrapper (alignas(32) struct + `__m256d` AVX2 path + scalar fallback + deterministic pairwise reductions, no FMA). Microkernel processes 8×8 tile as 2×(8×4) halves (10 of 16 YMM live). 116 + 8 cases / 4729 + 24 assertions PASS.

**Phase 3.1.6 `crd-hesap` v0d-perf chunk 1 shipped 2026-05-19** — Goto/BLIS 5-loop layered GEMM with packing + driver-invokes-microkernel path swapped in over v0d-foundation's scalar-inner-product. **ADR-0082 Accepted** (intrinsics-via-Vec8f/Vec16f microkernel strategy; ASM deferred behind locked `CRD_HESAP_MICROKERNEL_BACKEND` switch + three-condition revisit gate). Honest benchmark on dev box (AVX2, 5.6 GHz): **f32 at 59% single-core peak / f64 at 17%** at N=1024 — ~12× over v0d-foundation scalar baseline; intrinsics-tier reference class (Eigen / Faer / Highway peers per ADR-0082 §rationale).

### Earlier same day

**Phase 3.1.6 `crd-hesap` v0d FOUNDATION shipped 2026-05-19** — fourth v0 slice of the day. BLAS L3 surface (gemm / syrk / herk / syr2k / her2k / trmm / trsm) + Goto/BLIS layered GEMM + scalar + AVX2 microkernels + mixed-precision dispatch + new `crd-hesap-sched` module (TaskGraph over crd::jobs) + 4 working CLI commands. **108 + 4 cases / 4704 + 61 assertions PASS** locally; smoke pass at 128×128 gemm + 32×4 trmm/trsm round-trip + CLI dispatch bit-equal. **5-config DoD with `-IncludeRelease` running for LTCG microkernel coverage at session close.** **Follow-ons filed (multi-session work)**: `v0d-microkernel-tune` (AVX-512 + NEON + SVE2 microkernels + ≥70% AVX-512 peak benchmark per HW-floor mandate), `v0d-formal-dag` (PLASMA/PaRSEC dep-tracking task DAG), `v0d-cli-extend` (full BLAS L3 CLI surface beyond gemm+trsm). v0d FOUNDATION ships in 1 day matching v0a-c elite-1-day pattern; perf-tuning sessions remain.

🎯 **NEXT = v0d perf-tuning follow-ons (multi-session)** then **v0e dense direct solvers** — LU + Cholesky (SPD + HPD) + QR + LDLT + iterative refinement + mixed-precision variants + LinearOp<T> factor view + condition estimation + CLI registration. ~1200 LOC + ~120 tests + ~7 d.

### Earlier same day

**2026-05-19 — Phase 3.1.6 `crd-hesap` v0c BLAS L2 shipped 2026-05-19** — third slice of the day. **17 BLAS L2 ops** (gemv / gbmv / ger / geru / gerc / symv / hemv / syr / her / syr2 / her2 / sbmv / hbmv / trmv / trsv / tbmv / tbsv) across f32/f64/Complex32/Complex64 (type variance per BLAS convention). **6 matrix-type bodies populated** from v0a catalog shells: Matrix<T, Layout>, MatrixView<T, Layout> (with const-converting ctor + cview() helper), Symmetric<T>, Hermitian<T>, Triangular<T, Side, Diag>, Banded<T> (LAPACK column-major band storage). 8 working CLI commands (gemv/symv/trsv core surface; additional commands filed for v0c-cli-extend follow-on). **94 cases / 521 assertions PASS locally** (60 v0a+v0b + 34 v0c: 11 matrix-types + 12 real BLAS L2 + 6 complex BLAS L2 + 5 banded). 4-config DoD running at session close.

🎯 **NEXT = `crd-hesap` v0d** — BLAS L3 (gemm/syrk/herk/syr2k/her2k/trmm/trsm) via task-DAG `crd-hesap-sched` (new sub-module) + tile-based gemm + AVX2/AVX-512/NEON/SVE2/scalar microkernels + mixed-precision dispatch helper. ~1200 LOC + ~100 tests + ~7 d. After v0a-f sub-cluster closes (~5wk total): v1 sparse → ... → eylem v1c+ resume.

### Earlier same day

**Phase 3.1.6 `crd-hesap` v0b BLAS L1 shipped 2026-05-19** — first ALGORITHM slice of the elite-and-big hesap cluster (after v0a substrate same day). 9 BLAS L1 ops × 4 type variants (f32 / f64 / Complex32 / Complex64) routed through Kahan-Babuška-Neumaier compensated pairwise summation. 28 working CLI commands via static-init `CRD_HESAP_CLI_REGISTER_MODULE` + anchor-symbol pattern + new `ArgValue` typed-param substrate extending v0a CLI. End-to-end smoke proves agent-native plumbing: smoke binary calls `hesap.dense.blas1.dot.f64` via registry → bit-equal result. Total v0a+v0b: 99 cases / 469 assertions PASS locally. 4-config per-slice DoD running at session close.

**🎯 NEXT = `crd-hesap` v0c** — BLAS L2 across 4 type variants (gemv / gbmv / hemv / hbmv / symv / sbmv / ger / geru / gerc / syr / her / syr2 / her2 / trmv / trsv / tbmv / tbsv). Adds `Matrix<T, Layout>` body (v0a shipped shell), strided views, banded + triangular dispatch, per-op CLI registration. ~1000 LOC + ~80 tests + ~5 d. After v0a-f sub-cluster closes (~5wk total): v1 sparse → v2 reorderings → ... → v17 GPU mirror → v18 notebook. **Phase 3.1 eylem v1c+** resumes after Phase 3.1.6 closes.

**Future workstream (high priority, filed in `docs/debt.md`):** direct-manipulation UX cluster (transform gizmos / curve CP gizmos / navmesh editor / Blender-class mesh vertex+edge select). Until that cluster opens, every geometry showcase scene uses ImGui `DragFloat3` for control-point editing.

**ADR posture.** ADR-0076 has 16 amendments (§12-§28) all accepted. ADR-0078 (units §1-§5), ADR-0079 (perf), ADR-0080 (`crd-rhi-compute`) all accepted.

---

## Coming up next (Phase 3.1 eylem — ⏸ PAUSED at v1b)

Phase 3.1 eylem is paused at v1b close per the ADR-0076 §12 sequencing pivot (2026-05-11): `crd-geometry` ships FULL before eylem v1c resumes, so eylem v1c+ consumes geometry from day 1 (no deferred-refactor debt). Resume order after Phase 3.1.7 close + `crd-hesap-dense` v0: v1c broadphase → v1c-sensor → v1d narrowphase + v1d-filter + v1d-callback + v1d-mesh + v1d-hf → v1e SI solver + v1e-material → v1f joints + v1f-articulation-filter + v1f-fields → v1g islands + v1g-contactmodify → v1h scene queries → v1i character controller → v1j snapshot/replay → v1k sandbox → v1l close. Full slice plan in `docs/phases/phase-3.1-eylem.md`.

---

## Active detour

_none — D-001 closed 2026-05-07; D-002 (concurrent containers) closed 2026-05-12; D-003 (crd-perf) closed 2026-05-15; D-006 (crd-time) closed 2026-05-15._

> When a detour opens, this section names it (e.g. "D-001: investigate shader-cache corruption") and the main roadmap pauses until it closes. Detour file: `docs/detours/D-NNN-<slug>.md`. Queue rules: `docs/detours/README.md`.

---

## Last shipped milestone

**2026-05-27 (latest) — 🎉 v4 (iterative solvers + preconditioners + AMG) CLOSED via v4z.** Session log `docs/sessions/2026-05-27-hesap-v4z-close.md`. Quantified the cd2d β=0.3 factor-vs-solve trade (Cerid `mlilu_ib`+reorder wins single-shot wall 2.1–2.2×/mesh; ILUPACK overtakes only after k\*≈3.6–5.9 re-solves), flipped `reorder` default-ON for nonsym `InverseBasedIlu` (no-regression: sherman3 iters 1066→557, cd2d-150 269→68; `mlilu_ib` CLI gains a `reorder` knob), fixed the singular-dense-leaf assert into a graceful diagonal-shift-and-refactor (gemat11 wrong-tool → EXIT 0), closed the complex-completeness audit (+block_pcg<C> + point-Jacobi<C> tests) + CLI audit, and locked ADR-0065 §26 (D(iter)-1..10: serial-reduction determinism moat, size-adaptive operator, breakdown guards, packed-MGS block-orthonormalization, graceful-degrade, reorder default-ON, O(1) AMD bucket-head, the β=0.3 quantified-not-slogan result). Touched-module gate green; 18-config sweep → CI. **NEXT = v5 sparse-direct factorization.** Earlier history below.

**2026-05-23 — v3d STARTED — v3d-1a Hessenberg reduction shipped.** Non-symmetric eigensolver kicked off (the AED hard-gate slice). **Plan advisor-vetted:** subdivision **v3d-1a** (balance + Hessenberg reduction) → **v3d-1b** (Francis double-shift QR → real Schur, `dlanv2` 2×2 standardization) → **v3d-1c** (Aggressive Early Deflation — HARD-GATE: 3-way bench dlahqr/AED/LAPACK dhseqr + sweep-count crossover at N≈75) → **v3d-2** (eigenvectors `dtrevc` + 3-stage back-transform + complex Schur). Traps pinned (reflector-offset for blocked `apply_q_block` reuse; `dlanv2` fragility); D(non-sym)-1..4 drafted (Wilkinson+exceptional-shift schedule, AED window formula, descending-|λ| shift order ascending-index tie-break, eigenvector phase pin). **v3d-1a CLOSED — BEATS Eigen + LAPACK.** `eig_nonsym.{hpp,cpp}` — `balance` (dgebal: faithful eigenvalue-isolating permutation + radix-2 diagonal scaling; gated isolation + trace-invariance + block-norm reduction) + `hessenberg` (dgehd2) + `form_hessenberg_q` (dorghr). Real f32/f64. **Gate MET: A=Q·H·Qᵀ recon <1e-7 at n=6/32/64/160/320/512, Q orthogonal, H upper-Hessenberg, balance isolation+trace; 8 cases / 196 290 assertions; 4-config DoD (debug/shipping/tidy/asan); full suite 287/302 054.** **The winning kernel is a SIMD ROW-WISE unblocked reduction**: the two-sided updates (`vᵀ·A` accumulate + rank-1 + per-row dot/axpy) stream along CONTIGUOUS rows in row-major, sidestepping the column-reduction layout penalty AND the small-K gemm overhead. I implemented + validated the blocked `dlahr2`+gemm port (faithful, recon-correct to n=512) but **DROPPED it** — it ran 0.2× Eigen (small-K nb=32 gemm + scalar/strided panel + jobs-frame-arena exhaustion on the parallel trailing across ~15 panels); the SIMD-unblocked is ~5× faster. **Bench (i9-14900K AVX2, f64): vs Eigen HessenbergDecomposition 1.16–1.28×; vs LAPACK dgehrd 2.07–13.86× (OpenBLAS-generic-on-MSVC).** Lesson: for a column-oriented reduction in a row-major engine, SIMD row-wise unblocked beats blocked-small-K-gemm at these N. **v3d-1c STARTED — v3d-1c-1 ✅ (Schur eigenvalue reordering — the AED prerequisite).** `eig_nonsym.cpp` + `reorder_schur` (public): faithful LAPACK `dlasy2` (2×2/4×4 Sylvester solve, complete pivoting) + `dlaexc` (adjacent 1×1/2×2 diagonal-block swap: Givens for 1×1/1×1, Householder + Sylvester + `dlanv2` re-standardize + provisional-swap reject for the 2×2 cases) + `dtrexc` (reorder driver, both directions, 2×2-split handling) — ported 1-based internally, converted at the public boundary. **Gate MET: reorder a random real Schur form (4 ifst/ilst pairs, up+down) → `Z'·T'·Z'ᵀ` = SAME matrix <1e-9, Z' orthogonal, T' quasi-triangular; a real eigenvalue moves ifst→ilst exactly; 2 cases / 244 assertions; 4-config DoD (debug/shipping/tidy/asan); full suite 293/304 762.** **v3d-1c-2 ✅ — AED deflation window (`dlaqr2`).** `aed_deflate`: window Schur via the shipped `real_schur` + spike test `|S·V(1,j)| ≤ ulp·|T(j,j)|` + `reorder_schur` to move undeflatable eigenvalues up + spike reflection + re-Hessenbergize (`hessenberg`+form-Q) + global H/Z similarity slab-updates via gemm; returns {ns shifts, nd deflated}. Faithful `dlaqr2` (bubble-sort accuracy step skipped — optional). **Gate MET: decoupled trailing window deflates fully (nd==nw, ns==0); general window preserves similarity `H0=Z·H·Zᵀ` <1e-8 AND spectrum eig(h)==eig(h0) <1e-7; nd+ns==nw; 2 cases / 252 assertions; 4-config DoD; full suite 295/304 770.** **NEXT = v3d-1c-3 — the HARD-GATE close: `dlaqr5` (small-bulge multishift QR sweep, packed shift pairs, reuses `dlanv2`/reflectors) + `dlaqr0` (driver: window-size schedule + `aed_deflate` + `dlaqr5` + nibble crossover, dispatch to `real_schur` below NMIN≈75) wired into `real_schur` as the large-N path. 3-way bench (pure-dlahqr / AED / LAPACK dhseqr) + Eigen RealSchur; AED's QR-sweep count drops vs pure double-shift = the proof.** Earlier same-day:

**v3d-1b ✅ — Francis double-shift QR → real Schur.** `real_schur` (LAPACK `dlahqr` faithful: implicit double-shift bulge-chase + Ahues-Tisseur small-subdiagonal deflation + exceptional-shift schedule + `dlanv2` 2×2 standardization → quasi-triangular T + orthogonal Schur vectors Z + eigenvalues wr/wi). Real f32/f64. **Gate MET: H=Z·T·Zᵀ recon <1e-8 (n=8/32/64), Z orthogonal, T quasi-upper-triangular, 2×2 blocks → complex-conjugate pairs (±i exact), trace invariant, AND the full pipeline A=(Q·Zs)·T·(Q·Zs)ᵀ via hessenberg+real_schur recon <1e-8; 4 cases / 2464 assertions; 4-config DoD (debug/shipping/tidy/asan).** D(non-sym)-1 (fixed Wilkinson + exceptional-shift schedule, capped iters). **NEXT = v3d-1c AED — the HARD-GATE** (Aggressive Early Deflation `dlaqr0/2/3` + small-bulge multishift `dlaqr5`; 3-way bench dlahqr/AED/LAPACK dhseqr + QR-sweep-count crossover at N≈75 = the proof AED works). Then v3d-2 (eigenvectors `dtrevc` + 3-stage back-transform + complex Schur). Fresh focused session — the hardest numerical code in the module.** Earlier same-day v3c:

**2026-05-23 — 🎉 v3c (least-squares family) CLOSED — v3c-2 NNLS + TLS shipped.** **NNLS** (`nnls`, real f32/f64) = Lawson-Hanson 1974 active-set with an **incremental thin QR** of the passive columns (Björck §5.8: re-orthogonalised 2-pass MGS add + Givens re-triangularisation downdate — no full refactor per active-set change); D(lstsq)-1 entering variable = largest gradient, ascending-index tie-break, strict `>`. **TLS** (`tls`, 4 type variants via the shipped complex SVD) = augmented `[A|b]` SVD, `X = −V12·V22⁻¹` from the last-d right singular vectors, type-generic Gauss-Jordan V22 inverse, `exists` flag (V22-singular ⇒ no unique TLS), multivariate d≥1. +6 CLI (`nnls`×{f32,f64}, `tls`×{f32,f64,c32,c64}) → 14 v3c commands; every op has a command. **Gated:** NNLS by KKT optimality + exact recovery of a full-rank non-negative ground truth + the 2×2 textbook clamp; TLS by exact recovery on consistent systems (smallest σ=0 ⇒ null vector `[x;−1]`) + multivariate + complex. ADR-0065 §24 (D(lstsq)-1..3). 4-config DoD (debug/shipping/tidy/asan) PASS; full hesap-dense **279 cases / 105 764 assertions**. Filed `v3c-2-nnls-vs-eigen-bench` (head-to-head; solution unique for full-rank ⇒ correctness gates pin it). **🎉 v3c COMPLETE: v3c-1 (lstsq + pinv, col-piv QR + COD + SVD, blocked-apply crush — pinv 0.11×→1.60× Eigen, crushes LAPACK) + v3c-2 (NNLS + TLS). NEXT = v3d (non-symmetric eigensolver: balance + Hessenberg + Francis double-shift Schur + AED + eigenvectors).** Earlier same-day v3c-1:

**2026-05-23 — v3c-1 (least-squares: lstsq + pinv) CLOSED.** New surface in `crd-hesap-dense`: **v3c-1a** column-pivoting Householder QR (`QRColPiv`/`factor_qr_colpiv` — `dgeqp3`/`dlaqp2` faithful: Businger-Golub pivot on transposed SIMD scratch + LAPACK partial-norm downdate-with-recompute, reveals numerical rank) + **complete orthogonal decomposition** (`COD`/`factor_cod`/`solve_cod` — `dtzrzf`/`dlatrz` RZ reflectors reduce the rank×n trapezoid to a rank×rank `T11`; `solve_cod` = LAPACK dgelsy min-norm). **v3c-1b** `lstsq` (methods Auto=COD/QR/SVD; multi-RHS; lazy residual; rcond=max(m,n)·eps; returns {X, rank, residual}) + `pinv` (Auto=COD/SVD; Moore-Penrose) + complex via the shipped complex SVD + 8 CLI. **Blocked-reflector-apply attack (user-directed elite path):** BLAS-3 `dlarfb` (`detail/apply_q_block.hpp` — all 4 side×transpose modes, gemm-driven, bit-matches the scalar applies) + blocked recursive `trtri` → **pinv 0.11× → 1.60× Eigen** (the scalar O(r³) T⁻¹ back-sub was the large-r bottleneck). **Bench (i9-14900K AVX2, f64, win-vs-ref): CRUSHES LAPACK everywhere** (lstsq QR 1.70–8.69× / COD 1.88–12.19× / SVD 1.07–4.01× vs dgels/dgelsy/dgelsd); **vs Eigen — pinv 1.12–1.60× (↑ with n), SVD-path 1.08–2.40× (n≥128 beats BDCSVD), COD-default 0.84–1.07× (parity, beats at n=512), QR-tall 0.69–0.93× (the accepted ADR-0083 row-major small-n layout-fit; filed `v3c-1-qr-tall-blocked`).** D(lstsq)-1 pins (NNLS/col-piv tie-break = ascending index; rank count strict `σ_i > rcond·σ_max`). 4-config DoD (debug/shipping/tidy/asan) PASS for the touched module (CI owns the 18-config sweep); full hesap-dense **271 cases / 105 709 assertions**. **NEXT = v3c-2 (NNLS Lawson-Hanson + Givens; TLS via SVD; CLI audit; ADR-0065 §24; close v3c).** Filed: `v3c-1-qr-tall-blocked`, `v3c-1-blocked-rz-apply`. Earlier same-day v3b:

**2026-05-23 — 🎉 v3b (SVD) CLOSED — v3b-1c complex SVD shipped.** Complex SVD (`zgesvd`-class) via `detail/svd_complex.hpp` (`make_complex_householder` zlarfg + `bidiagonalize_complex` → REAL (d,e) + complex unitary Q,P [D(svd)-16: skip 2nd ZLACGV] + `form_q/p_complex`) + complex driver in `svd.cpp` (`if constexpr` dispatch; **reduces to the REAL (d,e) → reuses the shipped D&C/dbdsqr crush verbatim** → complex back-transform U=Q U_b, V=P V_b; phase-pinned D(svd)-18; m<n via A^H). svd/svdvals c32/c64 + CLI (interleaved [re,im]). **Gated: Householder unitary <1e-12; A=Q B P^H recon <1e-11; full A=U S V^H recon + U/V unitary + S descending <1e-9 (m≥n / m<n / D&C path).** DoD debug/tidy/asan/shipping green. ADR-0065 §23 (D(svd)-16/17/18). HONEST: complex bidiag UNBLOCKED — at-scale complex speed-crush needs blocked complex zgebrd (filed `v3b-1c-blocked-complex-bidiag`); correctness + reuse of the real crush done. **🎉 v3b SVD COMPLETE: v3b-1 (real bidiag+dbdsqr+blocked dorgbr) + v3b-2 (D&C — beats Eigen BDCSVD 1.6–3.2× + LAPACK dgesdd 1.4–4.6×) + v3b-3 (randomized) + v3b-1c (complex). NEXT = v3c (least-squares, consumes the SVD).** Filed perf follow-ons: v3b-1c-blocked-complex-bidiag, v3b-1c-svdvals-dqds-direct, v3b-2-parallel-merges, v3b-3-nystrom-cholesky. Earlier same-day v3b-3:

**2026-05-23 — v3b-3 randomized SVD + symmetric eig CLOSED.** `rsvd` (Halko-Martinsson-Tropp: Gaussian sketch + QR range-finder + power iters → small dense `svd`) + `rsyev` (randomized symmetric eig, Rayleigh-Ritz `QᵀAQ`+`eig_sym`; D(svd)-15 — chose Rayleigh-Ritz over Nyström-`C⁻ᵀ`: more general + reuses eig_sym). **Built ENTIRELY on shipped gemm/QR/svd/eig_sym — zero new kernels.** Gated: exact rank-r SVD recon `<1e-8` + orthonormal U/V; rank-r PSD eig residual `<1e-7`. No head-to-head bench (Eigen/LAPACK have no randomized path — gate is accuracy + structural O(mn·ℓ) speed). CLI `rsvd`/`rsyev`.{f32,f64} (per-op rule). ADR-0065 §22. DoD debug/asan/shipping/tidy green. Filed `v3b-3-nystrom-cholesky`. **v3b SVD now substantively complete — v3b-1 ✅ + v3b-2 (D&C crush) ✅ + v3b-3 ✅; remaining v3b-1c (complex SVD).** Earlier same-day v3b-2:

**2026-05-23 — v3b-2 Gu-Eisenstat D&C SVD SHIPPED — full SVD CRUSHES Eigen + LAPACK at scale.** Replaced the O(n³) serial `dbdsqr` rotation sweep with a faithful Gu-Eisenstat divide-and-conquer bidiagonal SVD on parallel BLAS-3. New `detail/svd_secular.hpp` (`dlasd5` 2×2 + `dlasd4` ψ/φ secular root + `dlaed6` 3-pole cubic) + `detail/svd_dc.hpp` (`dlasd2` deflation + `dlasd3` secular-solve/vector-assembly + `dlasd1` merge + `dlasdq` base + `dlasdt` tree + `dlasd0` recursion). Wired into `svd()` at n≥64 crossover (smlsiz=25); back-transform `U=Q·U_b`/`VT=VT_b·Pᵀ` + the dlasd3 merge assembly on `gemm_parallel_auto`. **HEAD-TO-HEAD (i9 AVX2, f64, full SVD values+vectors): beats EVERY reference at ALL N=128–1024 — vs Eigen `BDCSVD` (fair gate) 1.59–3.21×, vs LAPACK `dgesdd` 1.37–4.55×, vs `dgesvd` 4.78–10.48×, vs `JacobiSVD` 11–28×; recon ~1e-14. @512: full SVD 690ms→52.9ms this session (13×); C/BDCSVD flipped 0.17→1.76.** Layout bridge pinned (column-major D&C ↔ tested row-major dbdsqr via the `dlasdq` adapter; D(svd)-11). ADR-0065 §21 (D(svd)-10..14). Validated by reconstruction gates: `dlasd0` full D&C `‖B−UΣVᵀ‖<1e-9` (n=6–50 multi-level) + dlasdq base + dlasd4 secular residual; **suite [dc]+[svd] 35 cases green**; clang-tidy clean. Filed `v3b-2-parallel-merges` (widen the lead). **REMAINING to formally close v3b-2.3: 5-config DoD + commit; complex SVD = v3b-1c.** Earlier same-day v3b-1b-perf:

**2026-05-23 — v3b-1b-perf (blocked dorgbr) CLOSED — the SVD VECTOR path now beats LAPACK `dgesvd`.** Profiling the full-SVD vector path at N=512 split it into form_q+form_pt **49% (335 ms, serial scalar ~1.5 GFLOPS)** + `dbdsqr` **49%**. The elite fix for the forming-half: **blocked `dorgbr` (BLAS-3 compact-WY)**, not a parallel scalar loop — new `detail/orgbr.hpp` (`orgbr_q` builds U=Q, `orgbr_p` builds VT=Pᵀ; both reduce to ONE columnwise block-WY apply via the transpose-trick — right reflectors G(g) as columns C(g), P=C(0)…C(n−2), VT=Mᵀ) + shared `detail/block_reflector.hpp` (`build_block_t_from_vtv`). Scalar paths kept for n≤2·nb (oracle). **form_q+form_pt 335 → 12 ms (28×); full SVD 690 → 378 ms; C/`dgesvd` 0.81 → 1.45 (BEATS LAPACK's dbdsqr-class routine), C/BDCSVD 0.17→0.24, C/`dgesdd` 0.15→0.27.** Two real bugs found+fixed: **D(svd)-7** forming Q applies (I−V·T·Vᵀ) with **T not Tᵀ** (qr.cpp uses Tᵀ to apply Q_panelᵀ for A→R — opposite direction; Tᵀ gave exactly Qᵀ); **D(svd)-8** `build_block_t_from_vtv` now zeros the strict-lower triangle (reused/partial-stride buffer fed garbage to the T·W GEMM). **PARALLEL `dbdsqr` SKIPPED** (advisor+user): dorgbr alone won the leaf; per-`dlasr` parallel hits frame-arena exhaustion (~1000–2000 sweeps × 2 `dlasr`) + at-best-TIE BDC/`dgesdd` (parallelizing O(n³) memory-bound sweep can't beat O(n²) D&C). The BDC/`dgesdd` crush = **v3b-2** (Gu-Eisenstat `dbdsdc`); `dbdsqr` stays small-N fallback. ADR-0065 §20 (D(svd)-7/8/9). 303 SVD assertions; blocked dorgbr validated vs scalar oracle (multi-panel, m=n + m>n). Filed `v3b-1b-perf-followon-qr-block_reflector-consolidate` + `v3b-2-svd-via-mrrr` (deferred fork). **NEXT = v3b-1c (complex SVD) then v3b-2 (D&C crush).** Earlier same-day v3b-1a-perf:

**2026-05-23 — v3b-1a-perf (blocked bidiagonalization) SHIPPED — the SVD reduction now CRUSHES LAPACK + Eigen at all N.** `bidiagonalize` is blocked `dgebrd` (`dlabrd_upper` panels + ONE BLAS-3 rank-2k trailing update via `gemm_parallel_auto` + unblocked tail; nb=32). Three measure-driven increments: faithful scalar `dlabrd` → row-outer **contiguous-`yacc`** Y matvec (kills the strided-column cache thrash that blew N=1024 up to 1331ms/0.19×) → **SIMD panel** via `simd_dot`/`simd_axpy` (promoted to canonical `detail/dot_simd.hpp`). **`svdvals` vs `dgesvd`: 3.6×@128 · 2.1×@256 · 1.8×@512 · 1.3–1.5×@1024** (headline @512 **156→26ms ≈6×**; @1024 flipped **0.94×→1.31× winning**); vs `dgesdd` comparable; vs Eigen `BDCSVD` 2.0–6.5×; val err ~1e-13. Reduction is 97% of svdvals cost @1024; the win = SIMD panel + parallel trailing GEMM beating LAPACK's serial-SIMD panel. **HONEST remaining gap: full SVD (values+vectors) still loses to D&C (`BDCSVD`/`dgesdd`, C/dgesdd 0.15@512) — that is the serial `dbdsqr` O(n³) VECTOR sweep, NOT the reduction (svdvals proves it wins); closing it = v3b-1b-perf (parallel dbdsqr) + v3b-2 (Gu-Eisenstat D&C `dbdsdc` vs SVD-via-MRRR), the next leaves.** ADR-0065 §19 (D(svd)-6); +2 svd cases (blocked-path 65/96/100/128/150 + blocked svd/svdvals incl. m<n); suite **245 cases / 99,843 assertions**; 5-config DoD running. Filed `v3b-1a-perf-followon-parallel-panel` + `…-dot_simd-consolidate-eig_sym`. **NEXT = v3b-2** (the full-SVD-at-scale crush) or v3b-1b-perf. Below is the earlier v3a/v3b-1a/1b history.

**2026-05-23 — Phase 3.1.6 `crd-hesap` v3a (MRRR) CLOSED + v3a COMPLETE + v3b (SVD) OPENED ✅** — session log `docs/sessions/2026-05-23-hesap-v3a3-mrrr-close-and-v3b-svd-open.md`. **v3a-3 MRRR** shipped end-to-end (eigenvalues: full dqds engine `dlasq2/3/4/5/6` + Sturm + parallel multisection; eigenvectors: `dlar1v`/`dlaneg`/`dlarrb`/`dlarrf`/`dlarrv` cluster-robust + parallel; `eig_sym_mrrr`; CLI `eig.sym.mrrr`; ADR-0065 §17; 5-config DoD PASS), completing **v3a** (QL/QR + D&C + MRRR + complex Hermitian). **Beats BOTH Eigen AND LAPACK**: full eig 1.4–1.95× Eigen / 2.1–3.7× LAPACK; MRRR tridiagonal vectors crush Eigen 5–64× + LAPACK `dstemr` 1.7–2.7×, match/beat `dstedc`; glued-Wilkinson W₂₁⁺ `‖VᵀV−I‖<1e-8`. Honest findings saved: SIMD-across-eigenvectors is memory-bound (reverted, at CPU limit → GPU later); always-bench-both-Eigen-and-LAPACK rule locked. **v3b (SVD) opened** — design locked (`dlasq2` reuse for singular values, 4-column bench, SVD-via-MRRR fork flagged, leaf split); **v3b-1a `bidiagonalize` ✅** (Golub-Kahan, `A=Q B Pᵀ`<1e-12). Suite **236 cases / 99,557 assertions**. **NEXT = v3b-1b: port `dbdsqr` (Demmel-Kahan QR) + `svd` driver + 4-column bench (reference read done; fragile ~400-line kernel — port with the reconstruction/orthogonality/vs-LAPACK-values test as the safety gate), then v3b-1b-perf (parallel split-block dbdsqr = the crush).** The **Current focus** block above carries the full v0/v1/v2/v3a rollup + the v3b status.

🎯 **v3a-3 (MRRR — `dstemr`) IN PROGRESS.** The single hardest routine in v3, subdivided into 4 leaves (design + ADR-0065 §17 determinism-pin draft D(dense-eig)-9..12 + bench protocol + LOC re-estimate ~1,600–2,200 written into `docs/phases/phase-3.1.6-hesap.md` § "v3a-3 locked design", 2026-05-23): **v3a-3.1** eigenvalues-only · **v3a-3.2** single-RRR vectors (`dlar1v`) · **v3a-3.3** ⭐ `dlarrv` cluster handling + GS-fallback (the hard-gate: clustered orthogonality) · **v3a-3.4** dispatch + CLI + bench vs LAPACK `dstegr`. **v3a-3.1 SUBSTRATE SHIPPED 2026-05-23** — `detail/sturm_count.hpp` (faithful `dlarrk`/`dlarrc`/`dlarra`/`dlarre`-Gershgorin+pivmin ports: tridiagonal Sturm `negcount` + two-pivot interval count + `compute_pivmin` + `gershgorin_bounds` + `tridiag_split` + `bisect_eigenvalue` + the `tridiag_eigenvalues` split-and-bisect driver), pure-scalar zero-alloc f32/f64. **Gate MET vs the Toeplitz closed form AND the `eig_sym` (D&C/QL) oracle on random + reducible tridiagonals (worst rel err ~1e-9); determinism `memcmp`-bit-identical (the pivmin guard makes `negcount` a strict step function). +9 cases/+33 assertions → suite 215/98,749 green (win-debug).** **v3a-3.1 dqds-a SHIPPED 2026-05-23** — `detail/dqds.hpp`: `dlasq6` (dqd) + `dlasq5` (dqds, IEEE-only) inner sweeps ported line-for-line via a 1-based `Z1` ping-pong wrapper (D(dense-eig)-MRRR-Z1base + dqds-ieee-only), `build_qd_ldlt` (strict-shift LDLᵀ qd-array, pivot guard), and an unshifted dqd driver. **Gate MET: dlasq6 one-step == independent Rutishauser transform (proves the `4*N0+PP-3` Z-layout); dlasq5(τ=0)==dlasq6; unshifted driver matches Toeplitz + `eig_sym` (rel err <1e-9). +5 cases/+33 assertions; suite 220/98,782 green (win-debug).** **v3a-3.1 dqds-b + eigvals_sym + BENCHMARKS SHIPPED 2026-05-23** — full `dlasq2/3/4` shift+deflation+split driver ported faithfully (the complete dqds engine `dlasq2/3/4/5/6`); `dqds_eigenvalues` (single block) + `tridiag_eigenvalues_dqds` (split + dqds + bisection fall-through); public `eigvals_sym` (dense → dsytrd → dqds) + CLI `hesap.dense.eigvals.sym.{f32,f64}`. **Gates: general n=40 vs eig_sym <1e-10; GRADED spectrum 1e-8..1e11 to <1e-10 RELATIVE (the dqds payoff); Toeplitz n=64 <1e-12; reducible; f32; determinism. 226→227 cases / 98,837 assertions (win-debug).** **HEAD-TO-HEAD BENCHMARK (`bench_hesap_eig_vs_reference`, build/win-vs-ref, i9 f64) — HONEST read: ① FULL eigendecomp (values+vectors, the headline op) CRUSHES — eig_sym 1.18–1.95× Eigen / 2.12–3.61× LAPACK dsyev; eig_herm 2.17× Eigen / 2.58× LAPACK zheev @ N=1024. ② dqds VALUES-ONLY ties LAPACK's own MRRR dstemr (0.92–0.98×), loses to dsterf (0.53–0.65×), ~0.9× Eigen — EXPECTED: dqds is a sequential recurrence (faithful port ties LAPACK's identical dqds; dsterf is the values-only specialist). accuracy ~1e-14.** **MRRR's perf crush is VECTORS O(n²), not values — lands at v3a-3.3 ([[project_mrrr_perf_win_is_vectors_not_values]]).** **VALUES-ONLY CRUSH via PARALLEL MULTISECTION SHIPPED 2026-05-23** — `detail::multisection_chunk` (shared-Sturm: one `negcount(mid)` splits an interval for ALL eigenvalues in it = kills naive bisection's ~16× redundancy) + `solve_tridiag_multisection` (parallel over disjoint eigenvalue-index chunks across `crd::jobs`; serial fallback when `num_workers()==0`), wired into `eigvals_sym` at N≥512. **Measured (build/win-vs-ref, i9): par/dsterf 0.95/1.04/1.06/1.05 at N=1024/2048/4096/8192 → BEATS `dsterf` at N≥2048; par/dstemr 1.56–1.70× → crushes LAPACK MRRR everywhere; ~1e-14. Below ~1024 serial `dsterf` wins (parallel overhead crossover).** The lever LAPACK's serial solvers can't answer = 16 cores. Suite **228 cases / 99,537 assertions** (win-debug). **Remaining for .1 close: per-slice DoD.** **v3a-3.2 VECTOR KERNEL STARTED 2026-05-23** — `detail/mrrr_vectors.hpp::dlar1v` (faithful IEEE port, 1-based `Z1`): twisted-factorization eigenvector — stationary (dstqds) + progressive (dqds) differential transforms meet at the twist index `r = argmin|s+p|`, eigenvector solved outward from `r` (no Gram-Schmidt = the O(n²) win), with NaN-fallback loops + `resid`/`rqcorr`/`isuppz`. **Gate MET: on an RRR built from a Toeplitz tridiagonal, dlar1v at a closed-form eigenvalue gives `‖T z − λ z‖ < 1e-12`.** **v3a-3.2 SINGLE-RRR DRIVER SHIPPED 2026-05-23** — `detail/mrrr_vectors.hpp::mrrr_single_rrr_vectors`: build root RRR (`T−σI = L D Lᵀ`, σ strict lower bound = PD/robust) + per-eigenvalue `dlar1v` + Rayleigh-quotient refinement loop (MAXITR=10, RQTOL=2eps, best-residual tracking, bracketed rqcorr — faithful dlarrv structure) + pinned sign. **Gate MET on a well-separated tridiagonal (n=24): `‖VᵀV−I‖ < 1e-10` (orthonormal BY CONSTRUCTION — no Gram-Schmidt = the O(n²) MRRR win) + `‖T v − λ v‖ < 1e-10`.** Suite **230 cases / 99,541 assertions**. **v3a-3.3 ⭐ HARD-GATE SHIPPED 2026-05-23 (A1–A4, the part that destroys eigensolver attempts) — `detail/mrrr_vectors.hpp::mrrr_compute_vectors`:** A1 `dlaneg` (twisted Sturm count on LDLᵀ, gate vs tridiag Sturm) + A2 `dlarrb_refine` (refine eigenvalues within an RRR, perturbed→<1e-10) + A3 `dlarrf` (child RRR: shift to cluster end, element-growth ≤ MAXGROWTH1·spdiam, KTRYMAX back-off, refined-RRR fallback) + A4 recursive `dlarrv` cluster loop (segment by relative gap MINRGP=1e-3; singleton→`dlar1v`+RQ; cluster→child RRR + refine + recurse depth≤8; **Gram-Schmidt fallback** for residual near-multiplicity). **HARD-GATE MET: glued-Wilkinson W₂₁⁺ (tightest eigenvalue clusters) → `‖VᵀV−I‖ < 1e-8` + `‖Tv−λv‖ < 1e-9`** — orthonormal BY CONSTRUCTION via child-RRR shifts (no Gram-Schmidt in the common case = the O(n²) win). Each leaf tested in isolation then assembled. Suite **233 cases / 99,549 assertions** (win-debug). **v3a-3.4 SHIPPED 2026-05-23 — full MRRR eigendecomposition + the VECTOR-path crush.** `eig_sym_mrrr` (dense → dsytrd → dqds eigenvalues → `mrrr_compute_vectors` → back-transform V=Q·Z; gate MET: matches `eig_sym` values <1e-10, orthonormal <1e-9, residual <1e-9). **PARALLEL MRRR vectors** — `mrrr_compute_vectors` parallelized over independent top-level eigenvalue segments across `crd::jobs` (per-worker Tlsf arena; serial fallback when `num_workers()<=1`). **HONEST benchmark finding + crush: serial MRRR LOST (0.21× `dstedc`, 0.53× `dstemr` @ N=2048 — even LAPACK's `dstemr` is 2.5× slower than its `dstedc` for ALL vectors; MRRR's O(n²) has a huge scalar constant vs D&C's tiny BLAS-3 O(n³)). Parallelism is the lever (independent eigenvectors × cores LAPACK lacks). **MILKED (fine-grained work-stealing tasks + per-worker pre-allocated buffers + external-buffer Tlsf arena ∝ n + adaptive granularity + no-re-run): FINAL tridiagonal values+vectors vs BOTH refs (Eigen `computeFromTridiagonal` + LAPACK `dstedc`/`dstemr`), N=256–2048 — C/Eigen 5.0/9.8/21.6/62.7× (CRUSH), C/dstedc 1.48/1.50/0.99/1.19× (BEAT/tie LAPACK's BEST D&C), C/dstemr 1.73/2.00/2.47/2.93× (CRUSH LAPACK MRRR), orth ~1e-13.** We beat BOTH Eigen AND LAPACK on the vector path. (Rule locked: [[feedback_always_bench_both_eigen_and_lapack]] — every bench gets an Eigen col AND a LAPACK col.)** **SIMD-across-eigenvectors (`dlar1v_x4`) tried + reverted — memory-bound (4-batch's 16n scratch blows L2), nets ~0; CPU is at the hardware limit, next lever = GPU (future). 🎉 v3a-3 (MRRR) CLOSED 2026-05-23: CLI `hesap.dense.eig.sym.mrrr.{f32,f64}` + ADR-0065 §17 lock (D(dense-eig)-9..12 + Z1base/dqds-ieee-only/dlarrb-per-eigenvalue divergences) + 5-config per-slice DoD PASS (debug+asan+shipping+release+tidy). Suite 234 cases / 99,552 assertions.** 🎉 **v3a (symmetric + Hermitian eigensolver) COMPLETE — QL/QR + D&C + MRRR, beats Eigen + LAPACK across the board. v3b (SVD) OPENED 2026-05-23 — design locked** (reuse map: `dlasq2` IS bidiagonal singular values + `make_householder` for `dgebrd`; 4-column bench Eigen `JacobiSVD`+`BDCSVD` AND LAPACK `dgesvd`+`dgesdd`; SVD-via-MRRR fork flagged for v3b-2; leaf split — `docs/phases/phase-3.1.6-hesap.md` § "v3b locked design"). **v3b-1a ✅ — `svd.{hpp,cpp}::bidiagonalize` (Golub-Kahan `dgebd2`, m≥n upper bidiagonal): gate MET `A=Q B Pᵀ`<1e-12 + Q/P orthonormal<1e-12. Suite 236/99,557.** NEXT: v3b-1a-perf (blocked `dlabrd` BLAS-3) → v3b-1b (`dbdsqr` + `svd` driver + 4-col bench + CLI) → v3b-1c (complex) → v3b-2 (D&C vs SVD-via-MRRR, pick by perf) → v3b-3 (randomized). Crush BOTH Eigen AND LAPACK ([[feedback_always_bench_both_eigen_and_lapack]]).** ([[project_mrrr_perf_win_is_vectors_not_values]].) Suite **234 cases / 99,552 assertions** (win-debug). **Remaining to formally close v3a-3: CLI for `eig_sym_mrrr` + per-slice DoD + ADR-0065 §17 determinism-pin lock.** No calendar pressure (`feedback_hesap_clean_structure_over_calendar`). After v3a-3 closes v3a → v3b (SVD).

### Earlier milestones (2026-05-19, v0 kickoff day — historical depth; full v0→v3a status is in Current focus)

**2026-05-19 — Phase 3.1.6 `crd-hesap` v0d FOUNDATION ✅.** Fourth v0 slice of the day. Engine surface: **all 7 BLAS L3 ops** (gemm / syrk / herk / syr2k / her2k / trmm / trsm) across applicable type variants. **Goto/BLIS 3-loop tiled GEMM** with cache-block defaults Mc=120, Kc=256, Nc=4080 (BLIS defaults). **Microkernels**: scalar 8×8 fully-unrolled fallback (works for any T) + AVX2 8×8 via `crd-math::simd::Vec8f` (8 register accumulators for C rows, broadcast A elements, single Vec8f load of B[p,:]). Microkernel selection at compile time per type — `gemm_microkernel<f32>` → AVX2 path when `CRD_SIMD_HAS_AVX2`, scalar fallback otherwise; complex + f64 use scalar. **`gemm_mixed<TIn, TAcc, Layout>`** for HPL-AI iterative refinement (D5 from ADR-0065 §13): f32 input + f64 accumulator path. **`crd-hesap-sched` NEW MODULE** with `TaskGraph` (independent task batch + parallel-for execute over `crd::jobs`) + `parallel_tiles_for` (2D tile-grid convenience). v0d-foundation form: no formal dep-tracking (filed as `v0d-formal-dag` follow-on); independent tile dispatch only. **4 working CLI commands**: gemm.f32/f64 + trsm.lower.f32/f64. **Verification**: crd-hesap-dense-tests **108 cases / 4704 assertions PASS** (12 new BLAS L3 cases: gemm at N=64 vs naive, beta scaling, ConjTranspose, syrk/syr2k/herk textbook, trmm Lower, trsm Lower, trmm+trsm round-trip, complex trmm+trsm round-trip, mixed-precision); crd-hesap-sched-tests **4 cases / 61 assertions PASS** (TileId, TaskGraph execute, parallel_tiles_for visits-each-tile-once, clear). **Smoke** `smoke_hesap_blas3`: 128×128 gemm vs naive RMSE OK; 32×4 trmm+trsm round-trip OK; 4 BLAS L3 commands registered; CLI dispatch of `hesap.dense.blas3.gemm.f64` bit-equal to engine over 16384 elements. **5-config DoD with `-IncludeRelease`** for LTCG microkernel coverage running at session close. **Follow-ons filed** (multi-session): `v0d-microkernel-tune` (AVX-512 + NEON + SVE2 + ≥70% AVX-512 peak benchmark per `feedback_reference_implementations_are_the_floor`), `v0d-formal-dag` (PLASMA/PaRSEC dep-tracking task DAG), `v0d-cli-extend` (full BLAS L3 CLI surface). 12 decisions D33-D44 queued for ADR-0065 §14 lock at v0-close. Session log `docs/sessions/2026-05-19-hesap-v0d-blas3.md`.

🎯 **NEXT = v0d perf-tuning follow-ons (multi-session)** + **v0e dense direct solvers** — LU + Cholesky + QR + LDLT + iterative refinement + mixed-precision + LinearOp<T> factor view + condition estimation. ~1200 LOC + ~120 tests + ~7 d.

### Earlier same day

**2026-05-19 — Phase 3.1.6 `crd-hesap` v0c BLAS L2 ✅.** Third v0 slice of the day. Engine surface: **17 BLAS L2 ops** across applicable type variants — gemv / gbmv / ger / geru / gerc / symv / hemv / syr / her / syr2 / her2 / sbmv / hbmv / trmv / trsv / tbmv / tbsv (real-only / complex-only per BLAS convention; total ~50 explicit-instantiations). **6 matrix-type bodies populated** from v0a catalog: `Matrix<T, Layout>` (RowMajor + ColMajor NTTP per D21; `ld()` / `cview()` / `set_identity()`), `MatrixView<T, Layout>` (non-owning + sub-view preserves stride + non-const→const converting ctor), `Symmetric<T>` (dense storage; lower-triangle canonical; symmetric mirror on read), `Hermitian<T>` (`at_lower(i,j)` mutable + `at_value(i,j)` returns conj on upper half), `Triangular<T, Side, Diag>` (UnitDiag returns `T{1}`), `Banded<T>` (LAPACK col-major band). Reductions route through pairwise KBN tree from v0b. **8 working CLI commands** (gemv.f32/f64, symv.f32/f64, trsv.lower/upper.f32/f64); 38 additional schemas filed as v0c-cli-extend follow-on. **94 cases / 521 assertions PASS** locally. Smoke `smoke_hesap_blas2`: gemv vs naive RMSE OK on 50×30, trmv+trsv round-trip OK, CLI dispatch bit-equal to engine. **2 new engineering lessons**: D21 layout as NTTP; D27 non-template per-precision CLI impls (MSVC parser quirk in templated function bodies dispatching through `Vector<T>` when `vector.hpp` not transitively included). 12 decisions D21-D32 queued for ADR-0065 §14 lock at v0-close. Session log `docs/sessions/2026-05-19-hesap-v0c-blas2.md`.

🎯 **NEXT = `crd-hesap` v0d BLAS L3** — gemm/syrk/herk/syr2k/her2k/trmm/trsm via task-DAG `crd-hesap-sched` (new sub-module) + tile gemm + AVX2/AVX-512/NEON/SVE2/scalar microkernels + mixed-precision. ~1200 LOC + ~100 tests + ~7 d.

### Earlier same day

**2026-05-19 — Phase 3.1.6 `crd-hesap` v0b BLAS L1 ✅.** Second slice of the day; first ALGORITHM slice of the elite hesap cluster. Surface: 9 BLAS L1 ops (`axpy / dot / dotu / dotc / nrm2 / scal / copy / swap / asum / iamax`) × 4 type variants (f32 / f64 / Complex32 / Complex64). **Kahan-Babuška-Neumaier compensated pairwise summation** is the deterministic reduction primitive (D10, kPairwiseLeafBlock=8 = Vec8f lane count D11) — every sum / norm / abs-sum / dot routes through it; bit-exact across SIMD widths is the contract. **Real `dot<T>` real-only**; complex split into BLAS-convention `dotu` (unconjugated) + `dotc` (Hermitian inner product) per D13. **`real_type<T>` trait + `is_complex_v<T>` predicate** so `nrm2<Complex<U>> -> U` (returns the magnitude as real). **`iamax` first-index tie-break** per D16 + LAPACK convention. **Vector<T>** move-only owning value type (D15) with `IAllocator*` ctor, `initializer_list` / `ConstSpan` ctors, `clone(alloc)` for explicit deep copies. **`ArgValue`** typed-param substrate (NEW): discriminated-union value-type with 11 kinds (Empty / Bool / I64 / U64 / F64 / Complex64 / String / F64Array / I64Array / MatrixId / VectorId). `CommandArgs` carries `HashMap<String, ArgValue>` + typed setters/getters. **28 working CLI commands** (7 real ops × 2 precisions + 7 complex ops × 2 precisions) registered via static-init `CRD_HESAP_CLI_REGISTER_MODULE` per ADR-0081 §7. **Anchor-symbol pattern** (NEW): `register_blas1_cli_anchor()` exported function that test/smoke references via an anonymous-namespace `AnchorPull` struct — fixes MSVC dropping static-only .obj from static libraries (D19). **CLI inline-flattened-F64Array shape** for v0b (D14); future `VectorRegistry` consumer bumps to v2.0 keeping v1.0 Deprecated. **`CommandRegistry` ctor made public** so tests build local instances rather than stomping the global-registered static-init entries (D18). **C1060 fix in pairwise sum** (D20): produced-fn version parameterised over `(start, end)` indices, not capturing sub-lambdas — every sub-lambda has a distinct closure type, infinite template instantiation. Memory entry queued sibling to v0a's `feedback_macro_lambda_decltype_double_eval`. **Tests**: 39 cases / 151 assertions in `crd-hesap-tests` (v0a 30 + v0b 9 ArgValue) + **60 cases / 318 assertions in `crd-hesap-dense-tests`** (v0a 8 catalog + v0b 52: 10 Vector + 21 real BLAS L1 + 11 complex BLAS L1 + 10 CLI roundtrip). **Smoke**: `smoke_hesap_blas1` runs the full BLAS L1 surface on a 1000-element sin/cos vector, dispatches `hesap.dense.blas1.dot.f64` via registry, reports 28-command catalog. v0b closed same day as v0a (vs phase-doc ~3d estimate). 11 decisions D10-D20 queued for ADR-0065 §14 lock at v0-close. Session log `docs/sessions/2026-05-19-hesap-v0b-blas1.md`.

🎯 **NEXT = `crd-hesap` v0c BLAS L2** — gemv / gbmv / hemv / hbmv / symv / sbmv / ger / geru / gerc / syr / her / syr2 / her2 / trmv / trsv / tbmv / tbsv. Adds Matrix<T,Layout> body, strided views, banded + triangular dispatch, per-op CLI. ~1000 LOC + ~80 tests + ~5 d.

### Earlier same day

**2026-05-19 — Phase 3.1.6 `crd-hesap` v0a substrate scaffolding ✅.** Kicks off the elite-and-big hesap cluster locked the same day. Two new modules: `crd-hesap` (substrate shared by every later sub-module) + `crd-hesap-dense` (matrix-type catalog shells). Substrate ships: **`Complex<T>`** Cerid-native value type with Smith 1962 robust division + `std::hypot` abs + `std::atan2` arg (f32 + f64 specs, `sizeof(Complex64) == 16`, trivially copyable); **`LinearOp<T>`** matrix-free interface with vtable shape LOCKED (3 pure: `apply` / `n_rows` / `n_cols` + 2 virtual-with-default: `apply_transpose` / `apply_adjoint` returning false — PETSc / Trilinos pattern, matrix-free preconditioners don't fake transpose, Krylov solvers query `has_transpose()` at runtime); **`MatrixId` / `VectorId`** opaque 64-bit POD handles (`[generation:32 | index:32]` matching `scene::EntityId`); **`'HDV0'` (0x30564448)** CRDR FourCC pin for v0f load/save; **15-type dense-matrix catalog shells** (Matrix / MatrixView / Diagonal / Identity / Permutation / Triangular / Symmetric / Hermitian / Banded / BlockDiagonal / BlockTridiagonal / Toeplitz / Hankel / Circulant / Vandermonde — bodies arrive v0b-v0e). **CLI protocol plumbing per ADR-0081 §10** (every slice ships CLI from v0): typed `CommandSchema` (15 ParamKinds incl. Complex32/64 + MatrixId/VectorId/EntityId), `CommandResult` typed variant (8 alternatives, `std::variant` is allowed value type), `CommandRegistry` Meyers singleton, `CRD_HESAP_CLI_REGISTER_MODULE` static-init macro (uses `auto + make_module_registrar<Init>` helper because naive `decltype(lambda)` in template-argument position evaluates the lambda twice on MSVC), 250-LOC minimal JSON writer (RFC 8259 string escaping, fixed-depth-32 stack), MCP-tool-descriptor emit (canonical MCP-spec fields + Cerid extensions side by side per ADR-0081 §6.4). **38 cases / 151 assertions** PASS locally + `smoke_hesap_substrate` registers `hesap.smoke.echo` via static-init, looks it up, dispatches, dumps valid MCP JSON. **Sparse-catalog DEFERRED to v1** (cleaner separation than scattering shells across v0a + v1). 4-config per-slice DoD (win-debug + win-asan + win-shipping + win-tidy) in flight at session-close authoring. v0a closed in 1 day vs the phase-doc ~3d estimate. 8 decisions D1-D8 queued for ADR-0065 §14 lock at v0-close. Session log `docs/sessions/2026-05-19-hesap-v0a-substrate.md`.

🎯 **NEXT = `crd-hesap` v0b BLAS L1** — axpy / dot{u,c} / nrm2 / scal / copy / swap / asum / iamax for f32 / f64 / Complex32 / Complex64; Kahan-pairwise reduction; SIMD/scalar bit-exact parity; per-op CLI registration. ~700 LOC + ~80 tests + ~3 d. First `.cpp` in `engine/hesap-dense/` flips it from INTERFACE to STATIC.

### Earlier same day

**2026-05-19 — 🎉 Phase 3.1.7 CLOSE + Strategic Pivot session.** Weight-class-up session: Phase 3.1.7 fully closed (v10d verified + v10e + v10-close + v11 — 4 substantial slices in one day) **AND** a load-bearing strategic pivot locked. **3 user mandates** crystallized: (1) **Agent-native engine** — CLI/RPC is source of truth, GUI emits commands, MCP exact compatibility, AI agents are peer users; (2) **C++ hot-reload as the ONLY scripting language** — no Lua/Python/GDScript, Cerid scripts ARE C++ files in hot-reload DLLs; (3) **`crd-hesap` goes elite-and-big** — full v0-v17 substrate (~57 KLOC over ~10-12 months) ships before eylem v1c resume, matrix-type catalog ~30 types + complex from v0 + LinearOp + task-DAG + mixed-precision IR + 10 SOTA threads + CLI protocol plumbing per slice. New ADR-0081 (Proposed); ADR-0065 §13 amendment locks D1-D17; PRINCIPLES.md gains 3 cornerstones; ROADMAP.md §Revision 2026-05-19 supersedes 2026-05-15 calendar. **NEXT SESSION = `crd-hesap` v0a kicks off** ("bite every slice one by one like a rat" — user 2026-05-19). Comprehensive close session log `docs/sessions/2026-05-19-phase-3.1.7-close-and-strategic-pivot.md`.

🎯 **NEXT = `crd-hesap` v0a substrate** — module skeleton + Complex<T> + matrix-type catalog headers + LinearOp<T> + MatrixId/VectorId handles + CLI protocol scaffolding (CommandRegistry + CommandSchema + structured-output types). ~3 days. See `docs/phases/phase-3.1.6-hesap.md` §"Slice structure" v0a row.

### Earlier same day

**2026-05-19 — 🎉 Phase 3.1.7 FULLY CLOSED ✅** (v11 transform-aware + ADR-0076 §28 ✅ Accepted, 17 design decisions D217-D233, 18-config full sweep PASS). The last Phase 3.1.7 slice: transform-aware shape helpers in `crd-geometry-primitives`. Surface widened from the phase-doc's 7 enumerated entries to **FULL primitive catalog (~21 entries)** per the user's 2026-05-19 elite-completeness mandate (`feedback_elite_only_no_shortcuts`): 14 3D + 7 2D + `TransformedShape<Shape>` composition wrapper with trait-based `shape_scalar<Shape>::type` deduction (D218; 21 specialisations). Internals: `MatrixAttributes` cached once (D228), identity-matrix bit-exact fast path (D229), `transform_frustum` batched inverse-transpose reused across 6 planes (D232). Edge-case discipline: `transform_plane` `CRD_ASSERT`s on singular matrices (D225 per `feedback_quality_bar`); `transform_ray3_to_local` `CRD_ASSERT`s on rigid+uniform-scale precondition (D224; `LocalRayWithScale` general-affine variant filed). 5 advisor-pinned discriminator tests reliably catch the common transform-helper bug classes: identity bit-exact + 45°-rotation diagonal grows by sqrt(2) + plane normal stays perpendicular (inverse-transpose correctness) + ray-to-local round-trip + negative-determinant volume preservation. `transform_typed.hpp` mirrors entire surface via strip-compute-retag (D233). **Files**: ~600 LOC engine + ~430 LOC typed + ~370 LOC tests (26 cases / 95 assertions). **🎉 12 of 11 Phase 3.1.7 sub-modules complete — substrate FULLY done.** Session log `docs/sessions/2026-05-19-geometry-v11-transform-aware.md`.

🎯 **NEXT = `crd-hesap-dense` v0** (Phase 3.1.6 prologue, dense numerical substrate) → Phase 3.1 eylem v1c+ resume per Strategic Execution Plan locked 2026-05-15.

### Earlier same day

**2026-05-19 — Phase 3.1.7 v10 `-curves` cluster CLOSED ✅** (v10-close: ADR-0076 §27 ✅ Accepted, 35 design decisions D182-D216 locked, 18-config full sweep PASS). 6 slices in one day across the new `crd-geometry-curves` module (12th `crd-geometry-*` sibling): v10a substrate (curve types + evaluator + validators) + v10b sampling (uniform / adaptive / by-curvature / to_polyline) + v10c arc-length system + v10d queries (aabb_of / closest_point / distance / intersect_ray) + v10e frames-viz-sandbox (Wang 2008 RMF + closure twist + viz adapters + `SandboxScene::CurvesShowcase = 3`) + v10-close typed boundary (`queries_typed.hpp` covering the WHOLE v10 surface with 18 `*_typed` entry points per ADR-0078 §5 D34). Cluster totals: **~2 480 LOC engine + ~2 310 LOC tests + ~480 LOC system doc**; module binary at close: **95 cases / 3 010 assertions PASS**. Key discriminators (catch regressions reliably): D186 `evaluate IS the algorithm` (every downstream consumer routes through one evaluator entry point); D211 Wang reflection-sign discriminator (planar-arc RMF normals coplanar / binormals = plane normal up to sign); D214 strip-then-call (typed == strip(typed)→raw bit-exactly). Substrate-side new entry point: `detail_typed::strip(curve, alloc)` unified strip pattern with constexpr-zero-cost for value kinds and short-lived raw-copy allocation for owning kinds (D214). New system doc `docs/systems/geometry-curves.md`. **🎉 Phase 3.1.7 sub-module count 12 of 11 ✅** — substrate fully done. Filed follow-ons (consumer-pull, none blocking): `v10e-arclength-rmf`, `v10a-cr-analytic-2nd-derivative` + `v10a-bspline-analytic-2nd-derivative`, `v10a-cr-analytic-derivative` + `v10a-bspline-analytic-derivative`, `MultiCubicBezier3`, `v10a-2d-{hermite,catmull,bspline}`, `v10b-2d-sampling`, `v10c-analytic-arc-length`, `v10d-analytic-aabb`, `v10d-multi-hit-ray`, `v10d-polyline-fast-path`, `v10a-bspline-arbitrary-degree`. **Filed in `docs/debt.md` (user-flagged future workstream):** direct-manipulation UX cluster (gizmos / curve CP / navmesh / Blender-class mesh editing). Session log `docs/sessions/2026-05-19-geometry-v10-close.md`.

🎯 **NEXT = v11 transform-aware geometry queries** — new `crd/geometry/primitives/transform.hpp` (~2 days). After v11 → Phase 3.1.7 fully closes → `crd-hesap-dense` v0 → Phase 3.1 eylem v1c resumes.

### Earlier same day

**2026-05-19 — Phase 3.1.7 v9 cluster CLOSED ✅** (v9-close: ADR-0076 §24 + §25 + §26 all locked, 50 design decisions D123-D181 across the cluster, 18-config full sweep PASS). 16 algorithm + close slices across the v9 cluster: v9a `-gpu` LBVH (10 slices, D132-D165 incl. fat-node elite rewrite matching KittenGpuLBVH at 1.45 ms / 1M GPU-inputs path) + v9b GPU BVH refit (sub-1ms / 1M) + v9c `-decomposition` V-HACD (3 slices, D123-D131) + v9e `-shader-helpers` (4 slices + close, D166-D181). Cluster totals: **~9 790 LOC engine + ~6 440 LOC tests + 106 test cases / ~830 K assertions**. New modules `crd-geometry-bvh-gpu` + `crd-geometry-decomposition` + `crd-geometry-shader-helpers` shipped; `crd::shader::compile_hlsl` substrate-side addition shipped. v9d letter reserved (REPL bindings — reclaims when `crd-hesap-repl` lands in Phase 3.1.6+). Filed follow-ons (consumer-pull, none blocking): v9a-c-gpu-reorder + v9a-c-60bit + v9a-b2-large/v9a-c-large + v9a-b2-atomics + v9e-d-toml + v9e-d-cmake + v9e-d-crdr-pack + v9e-glsl-versions + v9e-d3d12-native + GPU-side IR interp + GPU GJK per-pair compute (needs eylem v1c) + async-compute Lloyd CVT for v8e. **🎉 Phase 3.1.7 `crd-geometry` substrate at sub-module count 11 of 11 — full substrate done.** Session log `docs/sessions/2026-05-19-geometry-v9-close.md`.

🎯 **NEXT = v10 `-curves`** — Bezier/B-spline/Hermite curves + 2D peers + sampling + arc-length + closest-point + Frenet/RMF frames + viz. 5 slices · ~2 weeks.

### Earlier same day

**2026-05-19 — Phase 3.1.7 v9e `-shader-helpers` cluster CLOSED ✅** (v9e-close: ADR-0076 §26 ✅ Accepted, 16 design decisions D166-D181 locked, new system doc `docs/systems/geometry-shader-helpers.md`). 4 algorithm slices + close: v9e-a (formula-IR flat 3-array storage + IrBuilder + validate + evaluate<T> + 21 golden manifests) + v9e-b (GLSL backend + SSA emission + Cephes-poly port + ULP-conformance GPU dispatch via shaderc → SPIR-V → Vulkan, 21 × 32³ samples = 688 128 per-pixel pass) + v9e-c (HLSL backend mirror + same-day `v9e-c-dxc-spirv-dispatch` follow-on: new `crd::shader::compile_hlsl` dxc dynamic-load + full GPU verification 21 × 32³) + v9e-d (cooker library: `cook_helpers_prelude` + `cook_ir` write GLSL/HLSL to disk via `crd::platform::fs`, 5 cases / 190 assertions). **Mixed ULP+abs tolerance** (1 ULP OR 1e-6 absolute) replaces the §26-planned "1 ULP" pin — catastrophic cancellation near zero crossings makes pure-ULP impossible. **Library-first cooker** (D177) replaces the planned CMake-target — library composes into build-time / editor-time / test fixtures.

### Earlier same day

**2026-05-19 — Phase 3.1.7 v9e-d shader-helpers cooker ✅**. New `crd::geometry::shader_helpers::Cooker` API: `cook_helpers_prelude(output_dir, alloc) → CookResult` writes the shared GLSL/HLSL prelude as `<output_dir>/sdf_helpers.glsl` + `sdf_helpers.hlsl` (idempotent, byte-identical to in-memory `glsl_helpers_prelude()` / `hlsl_helpers_prelude()`); `cook_ir(ir, name, output_dir, alloc) → CookResult` writes per-IR named SDF functions as `<output_dir>/<name>.glsl` (`float <name>(vec3 p)`) + `<name>.hlsl` (`float <name>(float3 p)`). `CookResult{ok, error_message, emitted_paths}` reports outcome + failure path. Uses `crd::platform::fs::write_file_text` + `create_directories`. Pre-validates IR via `validate(ir)`. Closes the `designer authors → cooker emits → renderer loads` loop for the shader-helpers module. **Test corpus (5 cases / 190 assertions)**: prelude byte-identity to in-memory emitter + 21-manifest cross-check (21 goldens × 2 languages × {file-exists + byte-identity + signature-shape}) + empty-name error + invalid-IR error + idempotent re-cook byte-identity. Full module binary: **21 cases / 910 assertions PASS**. Filed follow-ons (consumer-pull): `v9e-d-toml` (TOML manifest parser), `v9e-d-cmake` (`crd_cook_sdf_manifest()` build-time helper), `v9e-d-crdr-pack` (CRDR asset bundle pack). Session log `docs/sessions/2026-05-19-geometry-v9e-d-cooker.md`.

🎯 **NEXT = v9e-close** — ADR-0076 §26 amendment + system doc `docs/systems/geometry-shader-helpers.md`.

### Earlier

**2026-05-18 — Phase 3.1.7 v9a-c-followon "Track A elite rewrite" (fat-node 64 B + dual-output paths) ✅**. The v9a-close cluster had pinned 53.7 ms / 1M end-to-end as honest baseline; advisor + user push surfaced that this was 30× slower than KittenGpuLBVH (~1.5 ms / 1M on RTX 3090) and the published target was reachable. Elite rewrite swaps the compact 32 B `BvhNode` + canonical-reorder layout for the **KittenGpuLBVH fat-node 64 B `LbvhFatNode`** layout — both children's bounds inline in the parent's struct (one cache line per internal node), siblings NOT consecutive. Carry-register upsweep replaces the per-level barrier walk: each leaf-thread holds its bounds in a register, walks up via `parent_idx`, writes into `parent.bounds[isRight]`, atomicAdd on `done[parent]` for sibling-rendezvous, unions and continues. **Two output paths**: (a) `dispatch_build_lbvh` → CPU `LbvhTree` (with 64 MB readback); (b) `dispatch_build_lbvh_gpu_resident` → handle referencing pipeline-owned GPU buffers (no readback, intended for eylem broadphase / GPU traversal / occlusion culling). **Perf at 1M / RTX 4070 Ti SUPER (win-shipping, median-of-5) — full v9a-c stack + v9b refit:** CPU-output **~20.0 ms** (28% gain); GPU-resident **~6.7 ms** (76% gain); **GPU-inputs `dispatch_build_lbvh_from_gpu`: 1.45 ms** — **MATCHES KittenGpuLBVH's published 1.5 ms on RTX 3090 despite our card having ~40% LESS memory bandwidth**. **🎯 v9b GPU REFIT: 0.98-1.09 ms** — sub-1ms per-frame refit cost (rebuilds bounds only, topology preserved); ideal for eylem dynamic-body broadphase at 60 FPS (<6% frame budget for 1M bodies). 16 test cases / 402K assertions / 5-config DoD PASS. v9a-c-persistent-threads kernel kept compiled as documented future-option (bandwidth-floor proven; same scheduling pattern as flat dispatch on random scenes). Per-phase breakdown surfaced via temp profiling: ~3 ms CPU prep + ~5 ms GPU compute + 12 ms readback at CPU-output, ~3 ms + ~5 ms = 7.6 ms at GPU-resident. **5-config DoD PASS in 40 s**. Test corpus expands by 1 case: 12 → **13 cases / 398 279 assertions**. D156 / D158 / D161 / D163 superseded; **new D165 locks fat-node + dual-output rationale**. Obsolete shaders (`lbvh_build.comp`, `lbvh_aabb_upsweep.comp`, `lbvh_finalize.comp`) deleted; new `lbvh_fat_build.comp` + `lbvh_fat_upsweep.comp` ship. Session log `docs/sessions/2026-05-18-geometry-v9a-c-followon-fat-node-elite-rewrite.md`.

🎯 **NEXT = v10 `-curves` cluster** — Bezier/B-spline/Hermite curves + surfaces, 5 slices, ~2 weeks. Consumer: animation paths, motion design, future cinematic cameras.

### Earlier same day

**2026-05-18 — Phase 3.1.7 v9a `-gpu` LBVH CLUSTER CLOSED ✅** (v9a-close: ADR-0076 §25 ✅ Accepted, 33 design decisions D132-D164 locked, 18-config full sweep PASS). Cluster totals across 10 algorithmic slices + close: ~5 600 LOC engine + ~3 800 LOC tests + 71 cases / ~827 K assertions across the new `crd-geometry-bvh-gpu` sibling module. **Phase 3.1.7 sub-module 11 of 11 ✅ — `crd-geometry` substrate COMPLETE.** Performance characterization honestly pinned: test-harness end-to-end 53.7 ms / 1M median-of-5 on dev box (full CPU↔GPU round-trip + Phase C reorder); subsequent v9a-c-followon elite rewrite (same day) drops this to 20.9 ms CPU-output / 7.6 ms GPU-resident on RTX 4070 Ti SUPER. The phase-doc "atomic vs 2-pass barriers" decision-fork explicitly dissolved in D159: Karras 2012 §2.4 atomic-counter parent-walk is BOTH faster (1 dispatch vs log₂N) AND bit-deterministic (commutative AABB union). 3 lessons added to `docs/lessons/`: Lesson 02 (scalar vs SIMD at MB scale), Lesson 04 (parallel stable merge template), Lesson 09 (GPU memory ordering — GLSL `coherent` + `memoryBarrierBuffer`). Filed follow-ons for consumer-pull: v9b GPU BVH refit + v9a-c-60bit + v9a-b2-large/v9a-c-large + v9a-b2-atomics. Session log `docs/sessions/2026-05-18-geometry-v9a-close.md`.

### Earlier same day

**2026-05-18 — Phase 3.1.7 v9a-c GPU LBVH tree-build + AABB upsweep (elite combined slice, absorbs originally-separate v9a-d) ✅**. Combined per `feedback_quality_bar` — shipping topology-with-sentinel-AABBs would be a half-built deliverable. Single complete queryable LBVH end-to-end. New `build_lbvh_cpu<KeyT>(sorted_pairs, leaf_aabbs, alloc) -> BvhTree` (CPU reference, the algorithm definition D157) + `LbvhGpuPipeline::dispatch_build_lbvh` (GPU mechanical translation). **3 internal phases**: (A) Karras 2012 §2.2 binary-tree-from-sorted-codes; (B) **Karras 2012 §2.4 atomic-counter parent-walk upsweep** — the original "atomic vs 2-pass barriers" decision-fork dissolves (D159 — Karras §2.4 is BOTH faster (1 dispatch vs log₂N) AND bit-deterministic because AABB union is commutative+associative); (C) DFS canonical reorder so siblings are consecutive (matches `bvh_build`'s `left_first+1=right` invariant, D156). **Pinned D156-D164** for ADR-0076 §25 amendment. **CRITICAL `coherent` + `memoryBarrierBuffer()` discipline** in the GLSL upsweep — GLSL `atomicAdd` provides acquire-release on the atomic ONLY; non-atomic cross-invocation bounds writes need `coherent` qualifier or they're invisible. **The bug that surfaced at N=10000 (0.5+ unit bound diffs) only manifests at scale where tree depth ≥14 forces cross-workgroup sync** — Lesson 09 captures the rule. **Test corpus (12 cases / 379 021 assertions)**: calibration + 4 advisor-flagged degenerate cases (N=1 / N=2 / all-equal / adjacent-equal-interspersed) + 8-corner + N=10000 CPU oracle + GPU calibration + GPU N=10000 byte-identical-topology + 1-ULP-bounds + end-to-end CPU↔GPU pipeline (4096 AABBs) + `gpu_determinism_check` 3 rounds + `ValidationCapture` silent + `CRD_PERF_BUDGET_LE` 200ms NDEBUG / 60s debug for 1M items. **5-config DoD PASS in 39 s**. Full geometry-bvh-gpu binary: **71 cases / 827 611 assertions** (was 59/448 590 → +12 cases / +379 021 assertions). New Lesson 09 captures the GLSL coherent rule. Session log `docs/sessions/2026-05-18-geometry-v9a-c-lbvh-tree-and-upsweep.md`.

🎯 **NEXT = v9a-close** — cluster-close docs + ADR-0076 §25 amendment locking D132-D164 + first-light sandbox demo + 18-config full sweep + 1M-primitives <8ms perf budget pin.

### Earlier same day

**2026-05-18 — Phase 3.1.7 v9a-b1-parallel 3-phase parallel CPU radix sort ✅** (9th slice of the v9a cluster, same day as v9a-a base + 4 follow-ons + v9a-b1 + v9a-b2 + v9a-b1-simd). New opt-in entry point `sort_morton_pairs_parallel<KeyT>(span, alloc, num_jobs, threshold)` runs 3-phase deterministic parallel radix via `crd::jobs::parallel_for`: Phase 1 per-chunk local histograms, Phase 2 serial per-(chunk, bucket) scatter offset table (the stability invariant), Phase 3 disjoint-range parallel scatter. **Output byte-identical to serial `sort_morton_pairs<KeyT>` for any input + any num_jobs ≥ 1** — verified at num_jobs ∈ {1, 2, 4, 8, 16} across the 400 025-assertion test corpus including a worker-spanning all-equal-keys stability discriminator + cross-chunk equal-keys discriminator. **Measured speedup: 1.86× on 1M u32 win-shipping median-of-5** (4.77 ms serial+prefetch → 2.56 ms parallel-8w). Budget headroom now **7.8× over the 20ms NDEBUG budget** (was 4× at v9a-b1-simd close). Public `sort_morton_pairs<KeyT>` stays the serial scalar+prefetch entry point — parallelism is opt-in, no implicit jobs dependency. Pinned **D152-D155** (parallel_for chunk-index dispatch pattern, per-(chunk, bucket) offset-table stability invariant, threshold = 65536, 1.86× is the correct bandwidth-bound number) for ADR-0076 §25 amendment at v9a-close. Substrate value: jobs-bandwidth-stress validation + parallel-stable-merge template for eylem v1c broadphase + parallel BVH refit + cooker LBVH bake. **5-config DoD PASS in 46 s** via `scripts/per-slice-check.ps1 -IncludeRelease -Parallel`. Full geometry-bvh-gpu binary post-slice: **59 cases / 448 590 assertions** (was 51/48 565 → +8 cases / +400 025 assertions). 1 ms target NOT pursued — that's a GPU number, v9a-b2 already owns it. Session log `docs/sessions/2026-05-18-geometry-v9a-b1-parallel-close.md`.

### Recent slice history (one line per slice — full detail in linked session log)

- **2026-05-19 — Phase 3.1.7 v9 cluster CLOSED ✅** — ADR-0076 §24 + §25 + §26 all locked; 50 design decisions D123-D181; 18-config full sweep PASS; 16 algorithm + close slices · ~9 790 LOC engine + ~6 440 LOC tests · 106 cases / ~830 K assertions; new modules `crd-geometry-bvh-gpu` + `crd-geometry-decomposition` + `crd-geometry-shader-helpers`; `crd::shader::compile_hlsl` substrate-side addition. **Phase 3.1.7 sub-module count 11 of 11 — full crd-geometry substrate done.** `docs/sessions/2026-05-19-geometry-v9-close.md`.
- **2026-05-19 — v9e `-shader-helpers` cluster CLOSED ✅** — ADR-0076 §26 Accepted; D166-D181 locked (formula-IR storage + SSA emission + mixed ULP+abs tolerance + Cephes-poly port + library-first cooker + cooked-file consumer wire-up + `compile_hlsl` substrate addition); new `docs/systems/geometry-shader-helpers.md`. Module 11 of 11 (final crd-geometry sibling).
- **2026-05-19 — v9e-d shader-helpers cooker ✅** — `cook_helpers_prelude` + `cook_ir` write GLSL + HLSL to disk via `crd::platform::fs::write_file_text`; 5 cases / 190 assertions; module total 21 / 910. Filed `v9e-d-toml` + `v9e-d-cmake` + `v9e-d-crdr-pack` follow-ons. `docs/sessions/2026-05-19-geometry-v9e-d-cooker.md`.
- **2026-05-19 — v9e-c HLSL emitter + dxc → SPIR-V GPU dispatch ✅** — mirror of GLSL emitter (syntax swap only); structural-parity test proves HLSL ≡ GLSL ≡ C++ by induction; same-day `v9e-c-dxc-spirv-dispatch` follow-on adds runtime `compile_hlsl` (dxc dynamic-load) + full GPU verification (21 manifests × 32³ = 688 128 per-pixel comparisons within ~1 ULP / ~1e-6 abs). `docs/sessions/2026-05-19-geometry-v9e-c-hlsl-emitter-and-dxc-spirv-dispatch.md`.
- **2026-05-18 — v9e-b GLSL emitter + ULP-conformance GPU dispatch ✅** — SSA-style GLSL emission; 21 golden manifests × 32³ grid via `compile_glsl` (shaderc) → SPIR-V → Vulkan; deterministic Cephes-poly port for sin/cos/exp2 to close GPU-vs-CPU divergence on transcendentals; `crd_sign_bit` mask fix (0/0x80000000), `smin_poly kk = k*4.0` + `smin_cubic kk = k*6.0` C++-parity fixes. `docs/sessions/2026-05-18-geometry-v9e-b-glsl-emitter.md`.
- **2026-05-18 — v9e-a formula-IR ✅** — flat 3-array tree (`Array<IrNode>` nodes + `Array<f32>` params + `Array<u32>` children + root index); `IrBuilder` fluent API; `validate(ir) -> IrValidationResult`; 21 golden manifests covering every primitive + every smin/op/domain operator; `evaluate<T>(ir, p)` C++ ground-truth evaluator drives ULP-conformance against the GLSL/HLSL backends. `docs/sessions/2026-05-18-geometry-v9e-a-formula-ir.md`.
- **2026-05-18 — v9b GPU BVH refit ✅** — sub-1ms / 1M per-frame refit cost via `dispatch_refit_lbvh` reusing fat-node upsweep kernel; 0.98-1.09 ms on RTX 4070 Ti SUPER. `docs/sessions/2026-05-18-geometry-v9b-gpu-refit.md`.
- **2026-05-18 — v9a-c-followon Track A elite rewrite (fat-node 64 B + dual-output paths) ✅** — KittenGpuLBVH-parity at 1.45 ms / 1M GPU-inputs path despite ~40% less memory bandwidth. `docs/sessions/2026-05-18-geometry-v9a-c-followon-fat-node-elite-rewrite.md`.
- **2026-05-18 — v9a `-gpu` LBVH cluster CLOSED ✅** — ADR-0076 §25; D132-D164 locked; 18-config sweep PASS; sub-module 11 of 11 ✅; substrate COMPLETE. `docs/sessions/2026-05-18-geometry-v9a-close.md`.
- **2026-05-18 — v9a-b1-parallel 3-phase parallel CPU radix ✅** — opt-in `sort_morton_pairs_parallel<KeyT>` via crd-jobs; per-(chunk, bucket) offset-table stability invariant (D153); 1.86× speedup (4.77 → 2.56 ms / 1M u32 win-shipping); D152-D155 pinned. `docs/sessions/2026-05-18-geometry-v9a-b1-parallel-close.md`.
- **2026-05-18 — v9a-b1-simd scalar+prefetch ✅** — Investigated three SOTA radix techniques (AVX2 sub-histograms, Wassenberg SWWC, multi-pass histogram fusion) — all slower at 1M / 8MB working set. Winning technique = `_mm_prefetch` 8 iters ahead in scatter loop; +7% throughput (5.32 → 4.99 ms median-of-5). D145/D150/D151 pinned; debt entry cleared. `docs/sessions/2026-05-18-geometry-v9a-b1-simd-close.md`.
- **2026-05-18 — v9a-b2 GPU LSD radix sort ✅** — `MortonRadixGpuPipeline` + 4 compute shaders + 25-dispatch sync pipeline; prefix-sum scatter (D146) ⇒ byte-identical to CPU oracle (NOT throughput-tier); D146-D149 pinned; 9 cases / 8 257 assertions. `docs/sessions/2026-05-18-geometry-v9a-b2-gpu-radix-sort.md`.
- **2026-05-18 — v9a-b1 CPU stable LSD radix sort ✅** — new `morton_sort.hpp` + `morton_sort.cpp` + tests; `MortonPair<KeyT>` AoS pair + `sort_morton_pairs<KeyT>` templated for `KeyT ∈ {u32, u64}` from day 1; 8-bit-digit LSD radix, even pass count, monotonic-index stability; 20 cases / 40 134 assertions; D141-D144 pinned. `docs/sessions/2026-05-18-geometry-v9a-b1-morton-sort.md`.
- **2026-05-18 — v9a-a base + 4 follow-ons (v9a-a-typed + v9a-60bit-cpu + v9a-a-async-compute + v9a-60bit-gpu) ✅** — new module `crd-geometry-bvh-gpu`; 30-bit + 60-bit Morton CPU + GPU; typed Length<T> wrappers; async-compute dispatch path; shaderInt64 capability gate. 5 slices · ~1170 LOC engine + ~880 tests · 26 cases / 175 assertions · D132-D140 pinned. `docs/sessions/2026-05-18-geometry-v9a-a-morton.md` + `…-followons.md`.
- **2026-05-18 — v9c `-decomposition` cluster CLOSED** — 3 slices (v9c-a voxelize + v9c-b decompose + v9c-close) · ADR-0076 §24 amendment locks D123-D131 · 18-config full sweep PASS · eylem v1c stub integration smoke shipped · sub-module 10 of 11 ✅. `docs/sessions/2026-05-18-geometry-v9c-close.md`.
- **2026-05-18 — v9-prereq-test-harness** — `ValidationCapture` + `ulp_compare`/`bit_compare` + `measure_ms`/`CRD_PERF_BUDGET_LE` + `gpu_determinism_check` + `-IncludeRelease` 5-config DoD flag. Discipline locked for all v9 GPU slices. `docs/sessions/2026-05-18-v9-prereq-test-harness.md`.
- **2026-05-17/18 — Phase 3.1.7.6 `crd-rhi-compute` substrate CLOSED** — 6 slices in one day (v0a-v0e + v0-close); ADR-0080 Accepted (D1-D12 + revisions); 18-config full sweep 18/18 PASS in 53:06. Surfaced vtable-stability discipline + zstd LTCG ICE fix + Linux spirv-reflect API mismatch. `docs/sessions/2026-05-17-rhi-compute-v0-close.md`.
- **2026-05-17 — v8 `-delaunay` cluster CLOSED** — 11 algorithm slices + paydown + close; ADR-0076 §23 locks D73-D122; 18-config sweep PASS; sub-module 9 of 11 ✅. `docs/sessions/2026-05-17-geometry-v8-close.md`.
- **2026-05-17 — v7 `-mesh-processing` cluster CLOSED** — 8 algorithm slices + close; ADR-0076 §22 locks D1-D72; sub-module 8 of 11 ✅. `docs/sessions/2026-05-17-geometry-v7-close.md`.
- **2026-05-16 — v6 `-polygon` cluster CLOSED** — 6 slices; ADR-0076 §21; sub-module 7 of 11 ✅. `docs/sessions/2026-05-16-geometry-v6-close.md`.
- **2026-05-16 — v5 `-spatial` cluster CLOSED** — 8 slices (KdTree + LooseOctree + R*-tree + SpatialHash + UniformGrid + scene-index bringup + queries-extension); ADR-0076 §20; sub-module 6 of 11 ✅. `docs/sessions/2026-05-16-geometry-v5-close.md`.
- **2026-05-16 — v4 `-mesh` cluster CLOSED** — closest-point + Möller-Trumbore raycast + Jacobson winding number + SIMD + validate; sub-module 5 of 11 ✅.
- **2026-05-15 — Phase 3.1.7.5 `crd-units` CLOSED** — 19 sub-slices · ADR-0078 §1-§5 (two-layer typed architecture) · 31 locked decisions · `crd-no-untagged-physical-numeric` CI guard live. Mars Climate Orbiter is now a compile error.
- **2026-05-15 — D-006 `crd-time` + D-003 `crd-perf` shipped** — first units consumer + frame profiler with ImGui frontend. ADR-0079.
- **2026-05-14 — Phase 3.1.7 v2 `-convex` + v3 (Shewchuk predicates + 3D Quickhull + hull-extension) CLOSED**.
- **2026-05-13 — Phase 3.1.7 v1 `-bvh` cluster CLOSED** — v1a-v1g shipped (BvhTree + binned-SAH + parallel build + DynamicBvh + Bvh4 + Vec4f SIMD + closest-point) + v1h primitives hardening + v1i unified query facade + v1j viz companion.

For complete chronology, see `docs/sessions/`. For ADR index, see `docs/decisions/README.md`. For active phase plan, see `docs/phases/phase-3.1.7-geometry.md`.

> Older milestones intentionally truncated — they live in session logs.
