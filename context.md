# Cerid — Live Context

> Short-term memory: "where are we now?" The master plan lives in `docs/ROADMAP.md`; the doc map in `docs/README.md`.
> This is a **DASHBOARD, not a changelog.** Each milestone's detail lives in its session log (`docs/sessions/YYYY-MM-DD-*.md`); this file summarises the *current* state and points there. Keep it lean (≤ 300 lines) — prune stale snapshots, don't stack them. (History pruned 2026-08-07 → `docs/sessions/2026-08-07-context-md-history-archive.md`.)

---

## Current focus — ⭐ THE 2026-08-09 UNIVERSALITY RE-BASELINE (user-directed quest; the autonomous loop is LIVE, driving CEIR-8→35, 60s floor). The universality quest audited bands 1–7b (232 tests × 4 configs) against the ONE-substrate standard (engine/MATLAB/DAW/DCC/CAD/EDA/ML/agents on one programmable substrate) — verdict in `docs/research/2026-08-09-ceir-universality-review.md` (the standing U-§127 report): ops are genuinely open-world, but types/attrs/effect-families/effect-locations/region-kinds are CLOSED vocabularies, semantic identity is pointer-based, and analysis-pass/rewrite, incremental-evaluation, transaction, and capability models are MISSING — several are recorded IOUs (ADR-0109 §6 stable identity @1c; attr.hpp CEIR-2/3 attr kinds; the 7a capability flag). The tracker is FULL-RENUMBERED (user verdict): bands 0–7 frozen (band-0 stale-⬜ header fixed — 0a–0z were all ✅; band 7 PAUSED at 7b); NEW CEIR-8 FOUNDATION CLOSURE (8a open-world types · 8b open-world attrs · 8c effect u64+locations · 8d STABLE SEMANTIC IDENTITY · 8e trait/interface+region reservation · 8f capabilities+domain/safety+time domains · 8g AnalysisManager/PassManager/rewrite/DiagnosticEngine skeleton · 8h INCREMENTAL unification (⛔ the tree already holds ≥2 dependency graphs — absorb, never a third) · 8i TRANSACTIONS · 8z the U-§121 20-item gate) + NEW CEIR-9 UNIVERSALITY VALIDATION (9a notebook · 9b DAW · 9c DCC · 9d CAD+transaction · 9e PCB/EDA+external provider · 9f ECS · 9g agent transaction · 9h the MANDATORY external-plugin proof · 9z the 17-domain matrix + U-§126 answer); 7c/7d/7z → CEIR-10; old 8–13 → 11–16; old 14–32 → 17–35 (mapping table in the tracker banner). Cadence: foundation ADRs advisor-approved fully autonomous — EVERYTHING GOLD STANDARD AND UNIVERSAL; loop LIVE, 60s floor. **CEIR-8a ✅ CLOSED 2026-08-09 (ADR-0111): the open-world TYPE model — `TypeKind::Extern` is the one-time enum edit; dialect-defined type-classes (`TypeClassId` + `register_type_class`) carry params in the existing slots, inheriting interning/equality/hash/serialization; string-keyed serialization + a GENERIC text form (no pretty hooks — the U-§56 round-trip); the verify triple (factory asserts / decoder+parser reject / unregistered preserves); version range-check; conditional interface-hash class emission (zero churn). The U-§56 headline proven text AND binary; `testcad.toposhape`+`testeda.net` landed with zero enum edits. 242/242 × 4 configs (all-3 targets fresh against the new Type layout), fuzz-swept ASan-clean.** **CEIR-8b ✅ CLOSED 2026-08-09 (ADR-0112): the open-world ATTRIBUTE model — TWO wrappers (`TypedConst`/`Extern`) over ONE aggregate vocabulary (`Array`/`Dict`), the 8a surface applied to attributes; the `attr.hpp` CEIR-2/3 IOU (arrays/dicts/typed constants) PAID. `AttrClassId` + `register_attr_class`; a child-first ATTR pool; Dict keys SORTED+UNIQUE; the wrapper-composition rule (no wrapper-on-wrapper); attrs feed `stable_hash` not `interface_hash`; the version range-check made SYMMETRIC across text+binary (retrofitted into 8a's `parse_extern`); a parser depth guard. The U-§56 headline proven both forms; `testunit.dimension` + nested aggregates landed with zero enum edits. 255/255 × 4 configs (all-3 targets fresh against the new AttrValue layout), fuzz-swept ASan-clean, no version bump.** **CEIR-8c ✅ CLOSED 2026-08-09 (ADR-0113): effect FAMILY mask widened u32→u64 (+8 U-§19 families — Document/Constraint/UI Read/Write, TransactionBoundary, AgentAction — ordinals 27..34; families stay CLOSED, documented-why: the O(1) bitmask union + the total-switch append guard) + the OPEN effect-LOCATION model (a `LocationClassId` registry + `EffectTarget::Extern` door; EMPTY≠UNKNOWN: an unregistered Extern location is ResourceClass::Universe). BOTH family consumers audited (`effect_access` hazards + `effect_legal_in_region` audio-RT legality — the second is a denylist, not switch-guarded, so classified by hand: Agent/Txn forbidden, domain read/writes legal). Effects are registration metadata (not serialized) → the location model has NO per-instance serialization surface (N/A-by-design, identity named-forward to 8d); the ONE serialized surface is the §107 interface-hash projection (u32→u64 mask) → a NAMED universal recook, kCeirCookSchema 1→2 (rejects stale blobs cleanly), NO kBinaryVersion bump. 263/263 × 4 configs (all-3 targets fresh against the new EffectRecord layout), GCC -Werror=switch, opgen lockstep 27→35.** **CEIR-8d ✅ CLOSED 2026-08-09 (ADR-0114): content-independent STABLE SEMANTIC IDENTITY (the ADR-0109 §6 IOU). A `StableId` (u64) rides `Operation`; ONE id space (func/state-slot/visual-node = op id). Module-scoped pre-order one-time assignment (NOT a Context counter — blob purity) + a monotone per-module WATERMARK so an erase()d op's id is never reused (in-memory + across load). Serialized as an additive forward-skippable STID chunk (NO kBinaryVersion bump); content hash skips STID → id-INDEPENDENT → ZERO content-hash churn (cache hits survive). Text stays the id-free content projection (documented-why). The §20 state schema re-keyed by stable id (sorted, id VALUE hashed) → reorder-invariant (the 7a/8c false-incompatible FIXED) yet delete/re-add-sensitive. Interface-hash recook (kCeirCookSchema 2→3). 275/275 × 4 configs (all-3 targets fresh against the new Operation/Module layout), fuzz-swept ASan, advisor-caught erase-id-reuse blocker fixed.** **CEIR-8e ✅ CLOSED 2026-08-09 (ADR-0115): trait/interface split + region-kind reservation. OpTrait stays CLOSED (documented-why) + a factory guard (register_op rejects a minted bit; kKnownTraitsMask; OP_TRAITS count-pinned). The OpInterface registry PROMOTED to the TYPED open-world surface (interface.hpp): plugin BEHAVIOR lives in typed function-table interfaces (never a new trait bit); InterfaceId → u64 FNV so T::kId is compile-time (per-query string scan gone); register/get_op_interface<T> id+cast-safe; register_interface asserts op-first (loud, not silent-drop). Catalog: ONE live proof (crd.iface.cost) + 7 reserved names mapped to existing homes (extend, never fork). RegionKind stays {Graph,SsaCfg}; reserved semantics interface/attr-gated via policy (region_exec precedent, no enum widening). The band's FIRST ZERO-MOTION slice: no kBinaryVersion/kCeirCookSchema bump, no recook. 279/279 × 4 configs (all-3 targets fresh against the new InterfaceId/OpInterface layout), advisor-caught silent-register_interface-drop fixed.** **CEIR-8f ✅ CLOSED 2026-08-09 (ADR-0116): capabilities + domain/safety split + typed time domains (3 gaps). (1) U-§57 CAPABILITY model OWNS the §107 ownerless field: a `CapabilityId` (FNV of a name, intern-only); the op-kind required set on OpInfo (opgen-emitted from [op.native], empty-name-rejected); the module-wide sorted-unique program set (unregistered⇒external.process, EMPTY≠UNKNOWN) joins the interface hash (caps: section, count unconditional) → kCeirCookSchema 3→4; program_capabilities/capabilities_satisfied queries. (2) U-§23 safety split: EvalDomain stays WHERE; safety axes (alloc/block/IO+realtime_safe) a total-switch effect_safety PROJECTION folded by op_safety; the two RT oracles (effect_legal_in_region vs realtime_safe) reconciled as a documented subset. (3) U-§17 time domains ARE 8a type-classes under a `time` dialect (NO TimeDomainId); wall≠sim as distinct TypeIds; plugin game.turn zero central edits; mixing-enforcement named-forward (no operand-type checker yet). 291/291 × 4 configs (new OpInfo/OpSpec layout), GCC -Werror=switch (35-arm effect_safety), advisor-caught (time-hook numeric-check, empty-cap-name, func-body-reach, byte-exact round-trip). stable_hash+kBinaryVersion unchanged.** **CEIR-8g ✅ CLOSED 2026-08-09 (ADR-0117): the compiler-infrastructure SKELETON — four thin FRAMEWORKS ONLY (no optimization passes/lowering — CEIR-26/27). Context stays lean (separate classes, zero new members). ONE shared fnv1a_ct (id.hpp) behind all make_*_id. AnalysisManager (cached + never-stale preserved-set invalidation; unchanged⇒preserve-all; NO inter-analysis dep tracking — named-forward to 8h). PassManager (sequences + drives invalidation + STOPS on a Fatal). RewritePattern/ConversionTarget (caller-driven try_apply — the walking driver reserved for 26; unlisted⇒Illegal). DiagnosticEngine (codes/severity/SourceLoc/notes/fixits, one surface, text COPIED — ASan-proven). ZERO-MOTION (nothing serializes). 303/303 × 4 configs, advisor-caught (Fatal-short-circuit wired, fresh-data-after-invalidation test).** **CEIR-8h ✅ CLOSED 2026-08-09 (ADR-0118): the incremental-evaluation ENGINE-FIRST UNIFICATION. ⛔ The LAYERING WALL (crd-ceir asset-free + render-asset-core doesn't link crd-ceir → independent siblings) forced the home to the LOW shared module both link → `crd::containers::IncrementalDag`. A u64-keyed DAG with deterministic topo (deps-first, min-id tie-break) + affected_by ABSORBED byte-identical from DependencyGraph, PLUS per-node content/interface revisions driving the §107 rule generalized (recompute_after_change: content-or-interface⇒self, interface⇒dependents, content-only⇒hot-swap). ⭐ THE ABSORB IS REAL: render-asset-core::DependencyGraph is now a byte-stable WRAPPER over the engine (the RAF/scene-render/ceir-cook suites are the regression net — a canonical model with zero consumers is how third graphs are born). AnalysisManager + CookDb named-forward (different paradigm / semantic-only, no code motion). ZERO-MOTION recook (CDEP records byte-stable). Gated across containers+render-asset+scene-render+ceir-cook(relinked)+ceir on 4 configs; advisor-caught the `interface` COM-macro rename, the ceir-cook relink, a reverse-insertion determinism test, interface-change-implies-self-recompute.** **CEIR-8i ✅ CLOSED 2026-08-09 (ADR-0119): the TRANSACTION model — atomic authored-mutation with commit/rollback.** Eager mutation + an inverse-op UNDO JOURNAL (reverse-order replay — the correctness proof; COW-snapshot rejected as it fights the arena's stable-pointer identity, deferred-apply rejected as the body couldn't see its own edits). The transaction RECORDS, Context APPLIES — six thin transaction-only Context primitives on Context's EXISTING friendships → ZERO ir.hpp edit, no second mutation implementation. REUSES 8d (reference a pre-existing op by StableId, survives the edit) + 8h (touched/removed id-sets feed IncrementalDag; the tx says WHAT changed, the consumer computes revisions) + 8g (rejected edit/failed commit → Diagnostic). ⛔ id-under-rollback: begin() settles pre-existing ids up front (= the canonical serialized form) + records the watermark; rollback() restores it → byte-identity UNCONDITIONAL, the 8d delete/re-add discriminator holds across a committed tx. Graceful-reject+POISON on every mutator (agent-facing); commit VERIFIES (per-op + symbol re-sync) before the point of no return. Advisor pre-close caught the region-bearing-erase subtree-boundary hole (nested IN/OUT SSA edges leaking across the tombstone) — the erase mutator walks the subtree, rejects both directions, accepts+restores a CLOSED subtree whole. ZERO FORMAT MOTION (a tx serializes nothing; a committed tx is an ordinary content-hash cache miss, not a schema recook) — kBinaryVersion=2/kCeirCookSchema=4 untouched, no new fuzz corpus. Scope: authored edits only (compiler rewrites adopt journaling at CEIR-26). 315/315 × 4 configs (all 3 targets fresh vs the new context.hpp surface), the money config proving the un-erase re-thread + attr-snapshot Context-arena lifetime; tidy 6 files incl. the new transaction.hpp. **CEIR-8z ✅ CLOSED 2026-08-09 (no ADR — a gate COMPOSES ADR-0111…0119) → ⭐⭐ BAND 8 CLOSED (8a..8z).** The BAND-8 GATE: the U-§121 foundation DoD answered ITEM-BY-ITEM from evidence (the §B matrix, 23 rows each → closing slice + specific test; 20/23 CLOSED, the 3 remainders Provenance/unknown-plugin-enforcement/agent-query are consumer-band-deferred to 26/9h/9g, NOT foundation gaps). Advisor-caught: the §B matrix contradicted itself — 3 stale rows fixed (Time domains, unknown-plugin policy, and Provenance's UNVERIFIED "policy at 8g" → corrected to named-forward-26 after checking ADR-0117 states none). `test_band8_gate.cpp` (5 [gate8]) is the COMPOSING proof: ONE program (Extern type+attr + cap op + explicit axes + stable ids) round-trips text⇔binary byte-identically, PRESERVES through an unregistered host + re-serializes byte-exact (U-§56 × STID × caps × extern in one blob), UNIFIES on re-register with a cross-Context-pure interface hash, and takes a TRANSACTION editing the preserved plugin content (commit + byte-identical rollback — the U-§52 agent-edits-plugin story), plus a composed-blob fuzz sweep. Recook migrations (kCeirCookSchema 1→2→3→4, kBinaryVersion=2) executed+clean; fuzz covers every serialized open-world record (locations/effects/caps are registration metadata → N/A-by-design); NO new invariant earns its keep (I6 + -Werror=switch + the ADR discipline suffice — documented-why). 320/320 × 4 configs. **THE VERDICT: the foundation is universal by open-world semantics, not a second architecture — a new domain adds dialects/types/attrs/locations/interfaces/caps/providers with ZERO core edits, reuses identity/serialization/incremental/transaction, reports through one diagnostic surface.** **CEIR-9a ✅ CLOSED 2026-08-09 (no ADR — a proof composes; no new serialized record → no fuzz): U1 Notebook/incremental — the FIRST universality-VALIDATION proof.** A notebook/reactive-recompute workload runs on the foundation with ZERO new machinery: REAL CEIR arith cells (const/addi/muli), deps = SSA operands, 8d ids = cell identity; each cell's 8h content revision = a FORMULA hash (reorder/id-independent), interface revision = a computed-VALUE hash → the §107 rule IS the notebook early-cutoff (a formula edit that preserves the value hot-swaps its dependents). Edits ride 8i transactions (touched-set seeds the eval; a no-op edit's unchanged content hash ⇒ zero recomputes — the content-addressed memo, first real 8i→8h seam consumer). ⛔ U-§97 verdict: NO second engine, DEMONSTRATED (single-level cutoff IS the engine; the chained driver is consumer-side because the engine holds no results by the 8h division; the direct-dependents map mirrors the engine's edges) — the generic machinery is ALL IncrementalDag, the consumer ~100 lines of glue. `test_notebook.cpp` (7 [ceir][notebook], exact counts + chained cutoff via A*0 + the no-op memo). 327/327 × 4 configs, advisor-approved close. **CEIR-9b ✅ CLOSED 2026-08-09 (no ADR — a proof composes; no new serialized record → no fuzz): U2 DAW/timeline.** A DAW/audio graph runs on the foundation with ZERO new machinery — an INLINE-registered mock `audio` dialect (zero central edits) composing 8f time domains (`time.audio_sample<T>` ≠ wall + a plugin beat clock), 5d state/feedback (the plugin delay's StateEdge trait → the cycles-only-through-state verifier accepts it, rejects a combinational loop), 4c region legality (FileIO-in-audio-RT flagged), 8e latency interface. ⛔ U-§97 verdict: NO second engine — (1) feedback keys on the TRAIT not a kind name (plugin delay == core.delay); (2) offline = an in-place region-TAG flip on ONE module object (no second graph, U-§38); (3) latency is a typed interface with EMPTY≠UNKNOWN. Multi-rate assessed: distinct rates = distinct time domains, resample = an ordinary op, enforcement rides the named-forward operand checker — no new time model. `test_daw.cpp` (5 [ceir][daw]); latency proven per op-kind (a graph-walking accumulator is trivial glue, not exercised). 332/332 × 4 configs, advisor-approved close. **CEIR-9c ✅ CLOSED 2026-08-09 (no ADR — a proof composes; no new serialized record → no fuzz): U3 DCC modifier graph.** A Blender-class modifier stack runs on the foundation with ZERO new machinery — the 8h `affected_by` IS the depsgraph. ⭐ The differentiator from 9a: TWO DRIVERS, ONE ENGINE, NO CODE MOTION — 9a PUSHED forward from a seed (Salsa); 9c PULLS (`affected_by(M)` = the invalidation set UPFRONT = Blender's TAG pass, then ONE topo walk early-outing unchanged inputs). The engine changed by ZERO lines (a 9z synthesis input). Mock `dcc` dialect (inline, zero central edits); a DAG with a JOIN over two SEPARATE-source branches; edits ride 8i transactions. U-§97 verdict: NO second depsgraph — (1) the TAG pass IS the engine (`affected_by` asserted exact, sibling branch excluded — precise upstream isolation); (2) the eval walk is consumer policy (early-out needs re-eval, engine holds no results); (3) `deps[]` mirrors the engine's edges. §107 early-out proven by a topology-preserving deform edit (output the named cut). Facets assessed (a facet = an extra output node, not a gap); modifier reorder named-forward. `test_dcc.cpp` (5 [ceir][dcc]). 337/337 × 4 configs, advisor-approved close. **CEIR-9d ✅ CLOSED 2026-08-09 (no ADR — a proof composes; no new serialized record → no fuzz): U4 CAD parametric — the 8i TRANSACTION is the headline.** A parametric feature graph runs on the foundation with ZERO new machinery. Shared incremental helpers HOISTED to tests/ceir/incremental_helpers.hpp at this third consumer (9a/9c refactored, proven neutral at 337/337 before any CAD — the 9c forward-note discharged). U-§97 verdict: NO second transaction-manager/depsgraph/constraint-solver — (1) multi-op atomicity IS 8i unmodified (a multi-id touched() seeds the eval from the UNION of affected subtrees → **the 9a multi-cell-per-transaction named-forward is CLOSED**; + a no-op-inside-multi-op recomputes only the real subtree); (2) a CAD constraint is TWO existing surfaces — 8c ConstraintRead = classification, the sketch VerifyFn (the tx's commit-verify) = enforcement; a violating commit REJECTED (diag verify_failed → rollback byte-identical) / a satisfying one succeeds; (3) a constraint solver is an ordinary fixpoint node (graph stays one-way). ⭐ The CAD argument for transactions: the satisfying edit (4,4→6,6) transits a VIOLATING intermediate (6,4) and commits anyway because verify is ATOMIC at commit — a constrained model can't be edited dimension-by-dimension without inconsistent states, so enforcement MUST be transactional. `test_cad.cpp` (5 [ceir][cad]). 342/342 × 4 configs, advisor-approved close. **CEIR-9e ✅ CLOSED 2026-08-09 (no ADR — a proof composes; no new serialized record → no fuzz): U5 PCB/EDA + external provider.** A board pipeline (board_document→drc→route→gerber_export) runs on the foundation with ZERO new machinery; external tools are ordinary capability-bearing ops (8f caps + 4a effects + 4b determinism + content-addressed `tool` attr = the typed contract; a GPU-only host provably can't run it). U-§97 verdict: NO second provider/capability system. ⛔ The op-LOCAL commit-verify boundary NAMED: 8i commit-verify runs on TOUCHED ops only, so a rule fires iff its carrier is edited (the DRC = the board's verifier); a whole-board DRC sweep needs a find_*_violation walk / a pass (named-forward to 9g + CEIR-26) — a boundary, not a gap. ⭐ U-§34 DEMONSTRATED by a program-level walk: the EDA pipeline uses NO GPUCommand effect / DeviceTime domain / gpu.compute cap — the vocabulary was domain-neutral before this domain arrived. `test_eda.cpp` (4 [ceir][eda]). 346/346 × 4 configs, advisor-approved close. ⭐ FIVE domains in (9a–9e), U-§97 = NO every time, zero foundation edits, zero engine-line changes (a 9z tally input). **CEIR-9f ✅ CLOSED 2026-08-09 (no ADR — a proof composes; no new serialized record → no fuzz): U6 Game/ECS effects.** ECS parallelism runs on the foundation with ZERO new machinery — the 4d hazard analysis over 4a EcsRead/EcsWrite effects IS the parallelism oracle. U-§97 verdict: NO second scheduler — (1) ops_hazard/collect_block_hazards UNMODIFIED (disjoint components→parallel, shared+write→ordered exact RAW/WAR/WAW; the full 6-edge set from effects alone; the scheduler consumes edges at 12d, named-forward); (2) per-component identity = the SSA Value (Operand target — canonical 4d); the 8c EcsComponent KIND exercised only as the conservative whole-class fallback (safe-but-pessimal, EMPTY≠UNKNOWN for location identity); (3) a structural mutation (whole-store write, null resource) is the barrier by the existing nullptr rule. ⛔ Corrected a STALE forward-claim: the 4d comment said the whole-class fallback was "named-forward to 8d", but 8d shipped per-OP not per-LOCATION identity (the 8z scar) — fixed in place (comment-only crd-ceir edit, re-gated). `test_ecs.cpp` (4 [ceir][ecs]). 350/350 × 4 configs (all 3 targets rebuilt), advisor-approved. ⭐ SIX domains in (9a–9f), U-§97 = NO every time — zero engine-line changes; the ONLY foundation change any proof forced is correcting one stale comment. **CEIR-9g ✅ CLOSED 2026-08-09 (no ADR — a proof composes; no new serialized record → no fuzz): U7 Agent transaction — the CULMINATION of band 9.** The FULL agent loop runs on the foundation with ZERO new machinery: a mock agent DISCOVERS ops via the machine-readable OpSchema registry (no no-std JSON reader exists → the in-process `<dialect>_op_schemas()` tables ARE the registry `.ops.json` serializes), ⛔ ZERO hard-coded op knowledge (selects by property, interns via schema.dialect/name; op_name(kind)==schema.qualified closes the loop) → 8i tx author/modify → verify at TWO granularities (op-local commit-verify BACKSTOP + a module-wide find_structure_error SWEEP mid-tx) → 8g diagnostics read by CODE → commit/rollback byte-identical. U-§97 verdict: NO second authoring/verify/diagnostic system. ⭐ The op-local/module-wide composition DEMONSTRATED (closing the 9e named-forward): the sweep catches a same-block forward reference (→ FeedbackWithoutState, exact kind pinned) NO op-local verify sees; the agent emits agent.sweep.structure_defect + rolls back. The three-legged contract layered honestly: schema arity+attrs + chosen type + STRUCTURAL verify demonstrated; SEMANTIC type validity a bound named-forward (verifiers are structural — "Semantic verification lands at CEIR-3/4"; type-constraint language = CHIR). `test_agent.cpp` (3 [ceir][agent]). 353/353 × 4 configs, advisor-approved. ⭐ SEVEN domains in (9a–9g), U-§97 = NO every time — zero engine-line changes; the ONLY foundation change any proof forced is one stale-comment correction (9f). **CEIR-9h ✅ CLOSED 2026-08-09 (no ADR — a proof composes; a foreign-plugin blob serializes → fuzz-swept): U8 the MANDATORY external-plugin proof — the ACID TEST of the open-world thesis.** A whole `plugin` dialect authored in a test-side namespace (`plugin_ext`, public headers only, ZERO engine source edits) registers its FULL surface (9 pieces: type+attr+location+interface+ops+verifier+rewrite+lowering+provider) through PUBLIC open-world APIs. U-§97/U-§116 verdict: ZERO core edits, NO private wall — (1) the acid test passed + the claim is MECHANICAL (⛔ a STANDING GATE in crd-ceir-invariants: U-§116 TypeKind/AttrKind end at Extern via a new script pin + EffectFamily via its compile-time static_assert — two mechanisms, one gate, both platforms); (2) the 8g rewrite carried its FIRST real pattern (A(B(x))→C(x) via try_apply — a real IR mutation, worklist driver still reserved); (3) two honest asymmetries named (a plugin HAND-AUTHORS its OpSchema table where a first-party dialect gets it generated — named-forward; execution is a contract stub — dispatch → 24/29). Foreign U-§56 round-trip/preserve/unify + fuzz ASan-clean; discoverable by the 9g agent path (U-§117). `test_plugin.cpp` (5 [ceir][plugin]). 358/358 × 4 configs; ZERO engine source edited (only tests/ + invariants scripts). ⭐ EIGHT domains in (9a–9h), U-§97 = NO every time — zero engine-line changes; the only foundation changes any proof forced are one stale-comment correction (9f) + one standing gate ADDED (the U-§116 pin). **CEIR-9z ✅ CLOSED 2026-08-10 → ⭐⭐ BAND 9 CLOSED (9a..9z) — UNIVERSALITY VALIDATION COMPLETE.** The BAND-9 GATE, DOC-SYNTHESIS only (no ADR, no gate test, no recook — DECISIONS not omissions: unlike the 3z/4z/5z/8z composition gates whose guarantees lived in separate tests that never met, band-9's eight slices are each ALREADY a composition, so the 358-suite × 4 configs IS the gate's executable half). The U-§122 seventeen-domain matrix filled from evidence (`docs/research/…-universality-review.md` §I): **7 PROVEN** (9a–9g) + **2 ◧ foundation-validated** (Build pipeline, Reactive UI) + **4 ◑ analogous-proven** (MATLAB, CAM, Physics/CAE, Media) + **4 ○ future** (Renderer-15, Compute-13, ML-24, Distributed-30) = 17; a 9th proof (9h foreign plugin) validates the open-world DOORS not a matrix row → **8 proof slices**. ⛔ RE-VERIFIED every mid-band §I row against code (the 8z discipline) — caught+fixed two stale "Agent introspection" rows citing 9g-as-forward after 9g shipped. The U-§97 tally recorded in one place: **NO ×8, zero engine-line changes** (only deltas: one 9f stale-comment fix + one standing gate ADDED, the U-§116 pin). The U-§126 final answer, BOTH halves affirmative (foundation + empirical), bounded honestly to PROOF scale: CEIR is a universal substrate — validated (not merely designed) across 7 mock-domains + 1 foreign plugin, each a COMPOSITION of shipped slices; production-scale validation accrues as consumer bands 10→35 build the real domains. Advisor-approved close (caught+fixed the legend/U-§126 count errors: 17 = 7+2+4+4). Gate (doc-only ⇒ no rebuild, binaries current from 9h): **358/358 × 4 configs** (win-debug/win-asan/linux-gcc-debug/linux-gcc-asan) + opgen drift/validator + crd-ceir-invariants (incl. U-§116 pin) + GCC -Werror=switch; no tidy (docs-only), no fuzz (no new record). **NEXT = CEIR-10a** (Asset lifecycle — was 7c: hot reload detect→verify→cook→compile-affected→validate-set→atomic install→generation bump→deferred destroy, last-good on failure, no mixed-generation call graph; state migration decided by 8d stable-id state schema + 7b interface-hash discriminators; the paused band-7 contract RESUMED on the closed foundation). Bands 1–6 ✅; 7a/7b ✅; **8a–8z ✅ BAND 8 CLOSED**; **9a–9z ✅ BAND 9 CLOSED** (all uncommitted); CEIR-10 ◀ IN PROGRESS (10a). **CEIR-10a stage 1 DONE (2026-08-10, design `docs/design/ceir-10a-hot-reload-and-state-migration.md`):** the crd-ceir CORE migration surfaces — `collect_state_schema` + `contract_hash` (the §107 projection MINUS the state schema = the Reject-vs-Migrate decision split) refactored out of `interface_hash` **byte-identically** (no recook — the §107 matrix is the net) + Interpreter `snapshot_state_by_id`/`restore_state_by_id` (the cell-value move by 8d stable id; absent/depth-mismatch → skip → init-fill) + 3 stale in-place comment fixes. `test_reload_migration.cpp` (6 `[ceir][reload]`). **364/364 × 4 configs** + tidy(5) + `-Werror=switch` + opgen + invariants (U-§116 holds). NO recook/fuzz. **10a stage 2 API-LOCKED** (2026-08-10, 3rd advisor consult, design note §8): `ReloadSet` in `ceir-cook/hot_reload.{hpp,cpp}` — heap-`Generation{Context*,RuntimeProgram}` units (push-back-UAF scar), the 8h dag keyed `set_revision(content_hash, contract_hash)` IS the validate-set (⭐ advisor correction: dependent-safety keys on **contract_hash** not interface_hash — a state-schema-only edit must not reject dependents), a 3-way `ReloadDecision`, Reject-always on contract change (EMPTY≠UNKNOWN, host is an unseen caller), reject destroys the candidate, dep graph rebuilt wholesale, dup-symbol+AssetId-0 reject. **10a stage 2 DONE + gated (2026-08-10):** `ReloadSet` in `ceir-cook/hot_reload.{hpp,cpp}` — heap-`Generation` units (placement-new Context, reject-destroys-candidate, one-deep zombie drain, ASan-clean); the 8h dag `set_revision(content, contract)` IS the validate-set; 3-way `ReloadDecision` (HotSwap installs, rest keep last-good); `Registrar` callback; dup-symbol + AssetId-0 reject. `test_hot_reload.cpp` (6 `[ceir][reload]`). **370/370 × 4 configs** + tidy(3) + `-Werror=switch` + opgen + invariants (U-§116 holds, bridge-only). NO recook/fuzz. **10a stage 3 DONE + gated (2026-08-10, advisor-confirmed fork B — CALLER-DRIVEN migration):** the ReloadSet does NOT own interpreters (live §20 cells are populated by execution). A `MigrationFn` registered per-asset, its PRESENCE flips NeedsMigration → install; a free `migrate_state(old_in, new_in, new_module, fn)` = snapshot→fn→restore (nullptr fn = verbatim HotSwap). +3 `[ceir][reload]` tests (the DoD: value 7 survives a depth-1→depth-2 reshape via a mock runtime; refusal→init-fill; HotSwap-verbatim). **373/373 × 4 configs** + tidy(3) + `-Werror=switch` + opgen + invariants (U-§116 holds, bridge-only). NO recook/fuzz. **10a stage 4 DONE + gated (2026-08-10):** the SOURCE-in detect seam — `add_source`/`reload_source` (transient cook Context via `cook_program_text` → delegate to add/reload; §121 text≡builder); a cook failure = distinct `CookFailed`/`cook_error` that installs+destroys nothing → **last-good keeps running** (bad-edit path, finally tested); the ⛔ RAF-11 reentrant guard (`m_reloading` + RAII, a mutation from a registrar/fn = loud typed reject, proven via a smuggling registrar). Closes the twice-deferred 7a routing (host-subset form); mtime signal = production named-forward (CEIR owns the swap, not ResourceManager — two-swap-authority). +4 `[ceir][reload]` tests. **377/377 × 4 configs** + tidy(3) + `-Werror=switch` + opgen + invariants (U-§116 holds, bridge-only). NO recook/fuzz. ⭐ **CEIR-10a ✅ CLOSED 2026-08-10 (ADR-0120)** — the full hot-reload lifecycle on a `ReloadSet` (crd-ceir-cook bridge), shipped as 4 gated increments (364→370→373→377): (1) crd-ceir CORE surfaces (`collect_state_schema` + `contract_hash` byte-identical split + Interpreter snapshot/restore); (2) the supervisor — heap `Generation` units, the 8h dag `set_revision(content, contract_hash)` IS the validate-set, 3-way `ReloadDecision`, Reject-always on contract change; (3) CALLER-DRIVEN migration (fork B — the ReloadSet doesn't own interpreters; `MigrationFn` presence gates, `migrate_state` helper); (4) source-in seam + `CookFailed`→last-good + the RAF-11 reentrant guard. Named-forwards: mtime signal (production), CookedHeader.id validation, leaf-allow (11), KernelRef record (10b/13). Advisor-reviewed close (fixed 2 ADR misstatements). **19 `ceir 10a` cases; 377/377 × 4 configs; NO recook/fuzz.** CEIR-10 ◧ (10a ✅). **CEIR-10b IMPLEMENTATION DONE + gated (2026-08-10):** a `PlanCache` in `ceir-cook/plan_cache.{hpp,cpp}` (bridge-only) — a STORE + VALIDATE-ON-HIT client of live truth (advisor fork, NOT a compile pipeline): the caller `put()`s an opaque artifact; `get()` returns hit/miss/stale-deps but only after validating each recorded dep's current `interface_hash` via a resolver fn-ptr (the cache never trusts itself). No compile callback (compile→plan interface stays deferred to 21/26); deps key on `interface_hash` (conservative); no second dag (client of the ReloadSet's 8h dag via the resolver, 0⇒stale); in-memory only (persistence→11/13, refines the 10a KernelRef forward); bridge-local `PlanDep` (≡ ADR-0109 §85, not a core mint). Key={content_hash, target=FNV(provider name), compiler_version}; hit/miss counters. `test_plan_cache.cpp` (3) + `test_hot_reload.cpp` +1 (the 10z rehearsal: a callee interface change stales exactly `affected(B)∪{B}`). **381/381 × 4 configs, ASan-clean; NO recook/fuzz.** **CEIR-10b ✅ CLOSED 2026-08-10 (ADR-0121)** — advisor-reviewed (ADR clean; caught + reconciled 4 design-note doc-vs-reality drifts). The 10a "KernelRef → 10b PERSISTED plan cache" forward REFINED (struck in place): the in-memory `PlanDep` landed at 10b, the serialized form → 11/13. No `plan_status_name` (deliberate — programmatic result, not a diagnostic surface). **CEIR-10z ✅ CLOSED 2026-08-10 → ⭐⭐ BAND 10 CLOSED (10a..10z).** The BAND-10 GATE — the §172 matrix, a COMPOSING test (`test_band10_gate.cpp`, 1 `[ceir][gate10]`): ONE program graph (A imports B; C independent) walked through the 3 edit classes against a resolver backed by the LIVE ReloadSet — (1) body-edit B → HotSwap live + A's plan stays HIT (§107 at the plan layer); (2) signature-edit B → ContractChange reject + last-good + A still HIT; (3) dep-edit (cold-reload B) → A StaleDeps, B self-miss = recompile set `affected(B)∪{B}`, C untouched. ⛔ EXACT hit/miss counter deltas (3/0→6/1→8/1→9/3). Advisor pre-close caught 2 (unasserted ∪{B} + weak counters → exact), fixed + 4-config re-run. **382/382 × 4 configs; NO recook/fuzz/ADR** (a gate composes). CEIR-10 ✅ BAND 10 CLOSED (10a ADR-0120 · 10b ADR-0121 · 10z gate). **BAND 11 OPEN (Reference executor + compiled host plan, §83/§84 two-tier). CEIR-11a DESIGN LOCKED (2026-08-10, advisor consult, design note `docs/design/ceir-11a-reference-executor.md`):** grow the 5z reference `Interpreter` to the full host subset (the §118 oracle). Audit — executor covers arith(4)/core(10)/func(3)/async-seq(6); 11a adds the six §38 residual ops (spawn/continuation/main-thread/worker/fiber-wait/task-group — DON'T EXIST, need TOML+opgen+semantics), jobs-backed launch/await ON-POOL (today sequential), §112 step hooks (fn-ptr seam). ⭐ Value-model risk CLOSED (arith integer-only → i64 covers the subset). Sequential reference for everything in crd-ceir CORE, jobs-backed in crd-ceir-host (last-install-wins rebind); ops-first mechanical (no format motion/fuzz/recook) but the six ops' SIGNATURES are a design sub-fork (§38 is a vocabulary list) → advisor at stage 2. Stages: (1) audit+note ✅ · (2) core pass [six ops + parallel_for/map_reduce seq-ref + §112 hooks + 2 stale-comment fixes; 3-target rebuild returns] · (3) jobs-backed ON-POOL async · (4) DoD gate. **CEIR-11a stage 2a DONE + gated (2026-08-10):** the six §38 residual ops (advisor-designed signatures) — `task.spawn`/`main_thread`/`worker` (TokenProducer, share `eval_launch`) · `continuation` (Consumer+Producer, NEW `eval_continuation`) · `fiber_wait` (mirrors await) · `group` (no token traits, shares scope). Layering: async=STRUCTURE, task=PLACEMENT (shared-EvalFn = state/delay/history precedent). Blocking pre-TOML crd-jobs verify PASSED (all lowerings exist). New `install_task_semantics`. ⭐ Open-world payoff ASSERTED: the 6a token verifier + generated smoke cover all six with ZERO verifier edits; §32 audio flip fires on a task op. `test_task.cpp` (4 `[ceir][task]`) + 3 auto-grown smoke. **386/386 × 4 configs, ASan-clean; NO format motion/fuzz/recook** (name-interned). **CEIR-11a stage 2b-i DONE + gated (2026-08-10):** §112 step hooks (the debugger SEAM, hooks only — pre/post `StepHook` fn-ptr + `set_step_hooks` on the Interpreter; pre before dispatch, post only on success; null-default zero-cost; not proto-copied) + 3 stale-comment fixes (exec.hpp ×2, task.toml header ×1). `test_task.cpp` +1 (exact pre/post counts 2/2 + null-default). **387/387 × 4 configs, ASan-clean; NO format motion.** **CEIR-11a stage 2b-ii-A DONE + gated (2026-08-10):** the parallel-purity pre-flight MOVED from the crd-ceir-host bridge to crd-ceir core (public `exec::preflight_parallel`/`check_parallel_region`; the 9d hoist-at-2nd-consumer — provider's parallel run + reference's sequential run share ONE legality analysis, agree by construction); provider `execute()` delegates; string compares → interned OpIds. Zero behavior change (existing host tests exercise it via delegation). **387/387 × 4 configs, ASan-clean; crd-ceir STILL jobs-free (I4)**; NO format motion, no new tests. **CEIR-11a stage 2b-ii-B DONE + gated (2026-08-10) — the "full host subset" reference is COMPLETE:** the SEQUENTIAL reference for parallel_for/map_reduce (the §118 oracle) via the 6z fold pattern (fresh sub-interpreter, per-index invoke_region — the advisor landmine avoided) + an Interpreter-owned `map_output` store + `invoke()` captures `const Module*` + `module()`/`fuel()` accessors; provider proto gains `install_task_semantics`. ⛔ NO execution sharing (only the pre-flight analysis is shared — bit-exact-blind scar). ⭐ The first oracle-vs-provider AGREEMENT (11b harness in miniature): a non-associative map_reduce core-sequential vs HostProvider across {1..16} byte-identical + map_output span-equal; stateful→ParallelBodyStateful; outer-capture→UndefinedValue in both. `test_host_provider.cpp` +3. **390/390 × 4 configs, ASan-clean; crd-ceir STILL jobs-free**; NO format motion/fuzz/recook. **CEIR-11a stage 3 DESIGN LOCKED (2026-08-10, advisor consult → design note §8; implement next tick):** jobs-backed launch/await ON-POOL via CONDITIONAL POOLING — ⛔ the crux: async.launch bodies run IN-FRAME in the reference (captures + state legally work, unlike parallel_for), so "require pure" would be a capability regression; a bridge-local pool-eligibility classifier (StateEdge-free + calls-resolved + no outer captures) → pool (fresh sub, counter = happens-before) or sequential-fallback; byte-parity by construction both branches. Pooled handle > u32-max truncates+aliases → ALL FIVE consumers route through one `resolve_token(i64)`. execute() drains the pooled table at exit. spawn/worker → pooled; main_thread → pin_thread=0; group/scope → no override. ⭐ `pooled_count()` witness (==N pooled/==0 fallback). Verifies done (intern_op read-only on hit; wait pumps thread 0). Tests T1–T6. **CEIR-11a stage 3 DONE + gated (2026-08-10) — jobs-backed launch/await ON-POOL (conditional pooling):** a bridge-local pool-eligibility classifier (StateEdge-free + calls-resolved + no outer captures) → pool (heap-owned `PooledToken{scratch,result,counter}`; worker fills result before the counter decrements) or sequential in-frame fallback; byte-parity by construction both branches. Disjoint handle space (`kPoolBase=1<<40`) so pooled tokens never truncate/alias sequential; ALL FIVE consumers (await/join/race/cancel/fiber_wait) route through `resolve_yields`; spawn/worker → pooled, main_thread → pin_thread=0, group/scope → no override; `execute()` drains at exit. ⭐ `pooled_count()` cumulative witness. `test_host_provider.cpp` +6 (T1 parity+witness / T2 capture-fallback / T3 stateful-fallback / T4 main_thread pump / T5 join·race·cancel / T6 leak-drain). **396/396 × 4 configs, ASan-clean (both money configs); crd-ceir core STILL jobs-free** (region_state_free exported, no jobs type). ⭐ **CEIR-11a ✅ CLOSED 2026-08-10 (ADR-0122)** — the reference executor grown to the FULL host subset (the §118 oracle for 11b), 4 gated stages (382→386→387→390→396→397): the six §38 ops (layering: async=STRUCTURE/task=PLACEMENT, shared EvalFns) · §112 step hooks · the pre-flight moved to core · the parallel_for/map_reduce sequential reference (6z fold pattern, first oracle-vs-provider byte-agreement) · jobs-backed launch/await ON-POOL (conditional pooling — never reject what the reference ran; disjoint handle space; drain; cumulative witness) · the composing DoD gate (arith+core+func+async+task in one session, exact result 21 + exact §112 count 29 + state latching + cross-dialect token hygiene). Advisor-reviewed close (mandated the composing gate). crd-ceir core STILL jobs-free. **397/397 × 4 configs; NO recook/fuzz/version-bump.** CEIR-11 ◧ (11a ✅). **CEIR-11b DESIGN LOCKED (2026-08-10, advisor consult → design note `docs/design/ceir-11b-compiled-execution-plan.md`, ADR-0123 substrate):** plan compiler + `CompiledExecutionPlan` + differential harness. ⭐ Representation (advisor-corrected): STRUCTURED CONTROL FLOW COMPILED TO DENSE INSTRUCTION ARRAYS — Module is compile-time input only; plan owns a tree of dense `Instr{thunk, operand/result-slot spans, immediate, child-seq indices}`; the hot loop NEVER touches an `Operation*` (a structural Region walk = authoring-graph traversal, still a §153 violation; flat jump-streams rejected). Slots kill the env map; pre-resolved thunks kill the dispatch map. ⛔ 3 hidden §153 violations (VERIFIED): per-eval attr reads (const/cmpi-string/state-depth) → compile-time immediates; state-cell map → dense indices + latch lists; per-call resolve → compiled-function index + arena frames. Compiled thunks INDEPENDENT (never delegate to reference — bit-exact-blind). CompileError for compile-time-caught defects. Home = crd-ceir core `plan.{hpp,cpp}`; don't name the §69 interface (21/26). PlanCache: in-memory object (not 10b's byte-artifact; serializable blueprint named-forward). Corpus via a shared builder header (rich_graph.hpp); "7z" is pre-renumber → §121 cooked-vs-builder at the plan layer. Stages: (1) note ✅ · (2) straight-line arith ✅ · (3) control flow + state · (4) calls + async/task · (5) full-corpus differential + ADR-0123 + close. **CEIR-11b stage 2 DONE + gated (2026-08-10):** `plan.{hpp,cpp}` (crd-ceir core) — `compile(ctx,module,entry)` → dense `CompiledPlan` for straight-line arith (const/addi/muli/cmpi + return); compile-time slot allocator + §153 immediates (cmpi predicate STRING → dense `CmpPred` enum); `run()` = flat i64 slots + dense switch dispatch (no `Operation*`/map/string in the loop); INDEPENDENT thunks; typed `CompileError`. ⭐ The DIFFERENTIAL harness skeleton (`test_plan.cpp`, 3 `[ceir][plan]`): reference vs compiled `pin_values` byte-compare. **400/400 × 4 configs, ASan-clean; crd-ceir jobs-free; NO recook/fuzz.** **CEIR-11b stage 3a DONE + gated (2026-08-10) — CONTROL FLOW** (split from stage 3; state → 3b): the plan is now a tree of dense `Seq{instrs, yield_slots}`; control-flow `Instr`s record CHILD SEQ indices; the recursive `compile_seq`/`run_seq` handle if(forward yield)/for(induction slot + BadForStep)/while(CondArity)/switch(SelectorOutOfRange), recursing into child seqs BY INDEX (no `Operation*`). New `RunError`/`RunResult` — the differential compares the ERROR too; fuel budget as hang-guard. `test_plan.cpp` +3. **403/403 × 4 configs, ASan-clean; NO recook/fuzz.** **CEIR-11b stage 3b DONE + gated (2026-08-10) — §20 STATE:** `core.state`/`delay`/`history` → `Op::State`, a depth-N ring keyed by a dense CELL index (Instr imm), replacing the reference `HashMap<Operation*,Cell>`; per-run storage = `Array<Ring>` (pre-sized from compile-time `cell_depths`); cell_read reproduced INDEPENDENTLY (first eval init-fills from op0, result=ring[pos]); the latch is a pre-baked `Latch{cell,next_slot}` list per `Seq` (block-end `ring[pos]=slots[next]; pos=(pos+1)%depth`, read-all-then-latch — the §153 win, no runtime trait scan). ⭐ The FEEDBACK-EDGE fix (found by the for+state test): a cell's `next` names a value defined LATER in the block → a state op pushes ONLY op0 (init), the `next` slot is DEFERRED and resolved into the latch after the block compiles. ⭐ `RunResult.cells` = cell inspection (a for+state accumulator's value lives in the cell, not the return) byte-compared to the reference `cell_value`. `test_plan.cpp` +4 (state(7,0)→7; for(0,3,1)+state accumulator latches 0+1+2=3 in BOTH; ⭐ a depth-2 delay-line witness reads 0,0,0,1→ring[pos]==2, discriminating depth-1's 3; delay(3)+history(4)=7 routing/multi-cell — advisor-mandated depth≥2 + delay/history coverage). **407/407 × 4 configs, ASan-clean; opgen+invariants OK; crd-ceir jobs-free; NO recook/fuzz.** **CEIR-11b stage 4a DONE + gated (2026-08-10) — func.call (frame windows):** the plan is now a table of `CompiledFn{entry_seq, num_slots, param_slots}` with FRAME-RELATIVE slots; `compile` is a WORK-LIST over the call graph (@entry=fn0; each `func.call` resolves its callee ONCE → a compiled-fn INDEX in the Instr imm, index assigned BEFORE the body so recursion resolves); `run_seq` threads a `frame_base` (every slot = `stack[base+slot]` on a growing slot STACK; only `Op::Call` pushes a WINDOW — grow by callee num_slots, bind args, run, min-copy yields→results before popping, amortized arena; ⛔ never hold a slot ref across the callee run — ASan-clean). ⭐ Cells stay GLOBAL per-op (a callee's cell shared across invocations; dense index already matches). Error tiers: unresolved-callee/call-arity → COMPILE reject (`UnresolvedCall`/`CallArity`), reference fails at runtime (§4); result copy count matches the reference but the residue differs (reference unset→UndefinedValue-if-read, compiled zero — a defined/undefined gap → stage-5 watch (c), NOT an arity error since the reference runs such a program when the extra is never read). `test_plan.cpp` +5 (basic call 42; nested add2→inc→inc 42; shallow recursion sum(5)=15; ⭐ state-cell-in-callee-called-twice witness 0+1=1 & cells[0]=2 — per-frame would give 0; unresolved+arity compile-rejects). **412/412 × 4 configs, ASan-clean (frame-stack UAF money configs); opgen+invariants OK; crd-ceir jobs-free/asset-free; NO recook/fuzz.** **CEIR-11b stage 4b DONE + gated (2026-08-10) — async/task (COMPLETES stage 4):** a run-global TOKEN STORE (`Array<Array<i64>>` in RS, grows per launch — ⛔ NOT per-frame; `store_token`=push+handle, mirroring the reference `store_yields`) + six INDEPENDENT thunks, all in the CURRENT frame (only func.call pushes): `Op::Launch` (async.launch/task.spawn/main_thread/worker — run body NOW, store yields, result=handle), `Op::Await` (async.await/task.fiber_wait — bad handle→`RunError::BadToken`; min-copy stored yields), `Op::Continuation` (COPY antecedent yields BEFORE running the body — the store reallocs under a span; bind as body args via new `Seq::arg_slots`; run; store→new handle; arity mismatch→`ContinuationArity`), `Op::Join` (concat all tokens→one store), `Op::Race` (winner index 0, ⛔ validates NO handle), `Op::Cancel` (no-op, ⛔ validates NO handle); async.scope/task.group→`Op::Scope`. ⭐ NAMED FORWARD → STAGE 4c: task.parallel_for/map_reduce (isolated sub-interpreter + captures-pinned-UndefinedValue) compile-reject as UnsupportedOp for now — the advisor corrected the rationale: NOT a provider punt but a small 4c (compile the body with a FRESH slot map so a capture fails slot_of at COMPILE — the §4 pattern; non-capturing bodies run in a 4a frame window + per-index run + independent index-order fold; also a locked stage-5 corpus entry, must be built not dropped). `test_plan.cpp` +9 (launch→await 42; ⭐ token launched in CALLEE awaited in CALLER=7, the run-global-store witness — per-frame would BadToken; continuation chain 15; join concat 7; race→0; cancel no-op 8; task.spawn→fiber_wait routing 9; ⭐ BadToken agreement=EXACT ExecError::BadToken; ⭐ ContinuationArity agreement=1-yield→2-arg body→BadArity). Token guard u32-truncates the handle EXACTLY like the reference (3 sites). Differential helper now installs async+task semantics (SEPARATE installers). **421/421 × 4 configs.** **CEIR-11b stage 4c DONE + gated (2026-08-10) — data-parallel (COMPLETES STAGE 4):** `task.parallel_for`/`map_reduce`, the 6z-fold reference, compiled. The map/combine body compiles as an ISOLATED mini-function (`compile_fn_body`, FRESH slot map → an outer capture fails `slot_of` at COMPILE = `CapturedValue`, the §4 pattern; runs per-index in a 4a frame window; ⛔ never holds a funcs/seqs ref across the inner compile_seq). ⭐ SHARED pre-flight (advisor: REUSE, not reimplement): at compile, `exec::check_parallel_region` (core since 11a — legality analysis is the shared layer, execution independent) validates the map (1 arg, yields 1, state-free) + combine (2 args, yields 1); ExecError→CompileError (`ParallelArity`/`ParallelYield`/`ParallelStateful`/`UnresolvedCall`); sharing it makes accept/reject agreement true by construction. Thunks (independent): `Op::ParallelFor` maps→`map_output[imm]` (statement); `Op::MapReduce` maps then folds the combine fn in INDEX order→reduced SSA result; count=verbatim reference expr; step<=0→BadForStep; ⛔ fuel snapshot/restored around body loops (reference gives the body a separate budget); child_pool holds compiled-FUNCTION indices for dp ops. ⭐ FULL body isolation (advisor pre-close): the reference sub isolates env+cells+m_yield_store — a RAII `TokenScope` gives each parallel body phase a FRESH per-phase token store (swap `rs.tokens` out, discard the body's after; the fold gets its own — two subs); without it a body `await(parent-handle)` would WRONGLY resolve (reference BadToken — the dangerous accept direction) + body-launched handles would leak the parent's numbering. `RunResult.map_outputs` (dense map index)=§118 parity vs reference `map_output` (default-empty agrees, no watch item; watch (d): a nested dp op's map_output is sub-local/discarded in the reference → stage-5 skips nested dp ops). `test_plan.cpp` +8 (⭐ parallel_for map inspection iv²=[0,1,4,9]; ⭐ NON-ASSOCIATIVE map_reduce acc*2+elem=11, the index-order witness; capture reject=CapturedValue; stateful reject=reference-runtime-ParallelBodyStateful+compiled-compile-ParallelStateful paired; BadForStep runtime agreement; empty range; ⭐ fresh-store await witness=parent-handle-0+body-await(0)→BadToken both; ⭐ fresh-store numbering witness=body launches from 0→map [0,1] both not [1,2]). Unsupported-op reject test migrated to `core.foreach`. **429/429 × 4 configs, ASan-clean (map frame-window realloc + TokenScope move/discard); opgen+invariants OK; crd-ceir jobs-free/asset-free; NO recook/fuzz.** ⭐ **STAGE 4 COMPLETE — full op-class coverage (arith, control flow, state, calls, async/task, data-parallel).** **CEIR-11b stage 5 ◧ IN PROGRESS (corpus + differential + §121 twin + a real bug fix, gated 436/436; REMAINING = ADR-0123 + row flip):** the SHARED corpus builder (`tests/ceir/corpus.hpp`, the rich_graph precedent — 5 programs: 5z/6z/async/six-task-ops/composing, each returning module + state/dp handles in COMPILE ORDER); the corpus differential (each through BOTH engines — values+cells+map_outputs, per program) + the §121 plan-layer twin (a cooked→loaded module compiles to byte-identical results — compile both twins, compare the dense arrays, NO handles). ⛔ `build_5z` REUSED EXACTLY (test_band5_gate refactored to consume it — build-once). ⭐⭐ **THE CORPUS DID ITS JOB — a real stage-3a bug caught by 5z + FIXED:** compile_seq captured a control-flow op's `children_off` BEFORE compiling its region children, so a for-body containing a `match` (nested cf) aliased the match's arm0 → `Op::For` ran arm0 (`acc(iv)`) every iteration → 15 not 18. Every simpler test missed it (no nested cf). Fix: compile children FIRST into a local buffer, THEN reserve a contiguous child_pool block + set `children_off`. Pinned by a minimal nested-cf regression (for containing an if → 22) + 5z. **436/436 × 4 configs, ASan-clean; opgen+invariants OK; tidy clean; crd-ceir jobs-free/asset-free; NO recook/fuzz.** **CEIR-11b ✅ CLOSED 2026-08-10 (ADR-0123)** — the COMPILED tier of §84, differential-verified byte-for-byte against the §118 oracle across the corpus. ADR-0123 ACCEPTED (representation + the 3 killed §153 violations + independence [one shared analysis layer = the preflight] + the §4 error-tier table [the differential contract] + watch (a)–(d)); ADR-0121 §2.1 "opaque bytes" struck-in-place (the plan is an in-memory object). Advisor pre-close review (fixed six→seven stages + the split const/predicate table row). Consolidated "compiled-tier mirror scars" memory landed. **CEIR-11c ◧ IN PROGRESS — the CORE SEAM DONE + gated (2026-08-10):** ⛔ crd-ceir core CANNOT link crd-perf (I5 + jobs-free; crd-perf PUBLIC-links jobs), so 11c adds only the SEAM — a perf-linking BRIDGE consumer owns the crd-perf calls (the §112 StepHook / crd-perf jobs_adapter pattern). `plan.{hpp,cpp}`: `RunHooks{pre,post,user}` null-default fn-ptrs; `run()` gains a defaulted hooks param; `run_seq` fires pre(op)/post(op) around the dense switch, passing the dense `Op` id (u8, NOT an Operation* — §153); zero-cost when unset (one predicted branch); OBSERVATION-ONLY (reads only the op id → cannot perturb the differential); post fires only on SUCCESS → UNBALANCED (pre>post) on error. `CompileResult.stats` (PlanStats: instrs/seqs/funcs/cells/maps). Volume-tension ruling (advisor): region-per-instr is a LYING profiler (4096-ring overflows at fuel 1<<24 — no-silent-caps); the bridge does per-op-class counts + self-time (parent-pause) + 1 region/run + 1/compile + stats-as-counters. `test_plan.cpp` +2 (seam pre==post==35 on composing + byte-identical with/without hooks; erroring run pre>post). **CEIR-11c ✅ CLOSED 2026-08-10 (no ADR — integration slice):** PART 2 = the BRIDGE ADAPTER `engine/ceir-host/plan_perf.{hpp,cpp}` (crd-perf+crd-time added PRIVATE) — `PlanProfile` (per-Op counts + parent-pause self-time + a `depth_overflow` witness), `profiled_run` (`ceir.plan.run` region + the parent-pause hooks), `profiled_compile` (`ceir.plan.compile` region + the stat counters); self-time via `MonotonicClock` (fills even perf-OFF). ⭐ NOT a region-per-op (would overflow the 4096-ring at fuel 1<<24 — a lying profiler); aggregate per-Op (ring-safe). `test_plan_perf.cpp` +3 (straight-line exact counts + perf-ON counter readback; a LOOP for(0,1000){muli} → nesting + granularity-proof total_self>0; an erroring await→BadToken run → adapter survives unbalanced pre>post). Self-time STRUCTURAL only (flake scar); a static_assert guards the per-Op arrays. ⛔ crd-ceir CORE still jobs-free/asset-free (crd-perf/jobs is BRIDGE-only). Real consumer → CEIR-21/26. Advisor pre-close review (added loop+error tests + static_assert). **441/441 × 4 configs; tidy/opgen/invariants OK; NO recook/fuzz.** **CEIR-11z ✅ CLOSED 2026-08-10 (no ADR — a §153 proof slice) → CEIR BAND 11 COMPLETE.** The hot-loop AUDIT (`test_plan.cpp` +3, a `CountingAllocator` decorator): (1) allocation-free arith loop (M=10 vs M=1000 → alloc count independent of trip count + ≤4 → zero heap per iteration); (2) cells + call frames AMORTIZED-arena (`for{call inc + state cell}` → count(10)==count(1000): ring init-fill + first frame growth one-time, steady-state zero — the ADR-0123 §2.2 claim; dp token arrays amortized-arena BY DESIGN, out of strict-zero scope); (3) no-string-table-touch (a plan runs correctly AFTER the Context is destroyed — `run()` is Context-free, the plan is dense). All hooks-NULL (shipping path); the differential corpus already green in-gate. **444/444 × 4 configs.** ⭐⭐ **CEIR BAND 11 (Reference executor + compiled host plan) CLOSED — the §84 two-tier model complete (11a oracle/ADR-0122 · 11b compiled tier/ADR-0123 · 11c crd-perf · 11z §153 audit); the FIRST fully-closed band of the re-baselined spine.** **CEIR-12 OPENED — Resource/memory subsystem** (`ceir.resource` §36; sub-rows 12a decl/view/import-export · 12b lifetime/domain/residency · 12c alias/lifetime analysis · 12d planner · 12z gate). **CEIR-12a ◧ IN PROGRESS — the DIALECT DONE + gated (2026-08-10):** ⭐ the substrate already exists (CEIR-3c resource/View TypeKinds + `type_*` factories + `view_combination_valid` + the ViewRange mask) — REUSE it, resource-ness rides the TYPE not a new trait. `resource.ceirop.toml` (4 ops): `declare` (graph-owned; name+alias_group symbol attrs; Pure), `view` (view/range/subresource in ONE op — mask in `type_view`, offsets/sizes a variadic tail; Pure), `import` (externally-owned; provider-bound = §150 named-forward; distinct OP from declare — planner plans declare's NEVER import's; Pure), `export` (boundary; conservative `MemoryReadWrite{operand=0}` for the 12c hazard walk). Semantics = typed NoSemantics named-forward (async precedent). opgen regenerated the 5 artifacts (no central edit). Smoke 3/3 × win-debug/win-asan/linux-gcc + opgen drift/validator; **full ceir 447/447**. **CEIR-12a ✅ CLOSED 2026-08-10 (no ADR — op-vocabulary slice):** PART 2 = `Context::find_resource_misuse` (`context.cpp`; the type-system enforcement — identifies ops by `op_name` string [const-safe, I6-clean], reads the CEIR-3c resource TYPE) — 8 kinds: view (result-is-View · operand-VIEWABLE[Buffer/Image only — ⛔ advisor caught view-of-view legal-by-accident, §36 views are flat] · underlying==operand · mask-valid · arity=1+2·popcount) · export operand-resource · declare/import result-resource; check order CONTRACTUAL. `test_resource.cpp` +2 (well-formed module + ⭐ BINARY+TEXT round-trip [View/ExternalResource kinds in op results survive serializers] + 8 malformed constructions rejected incl. view-of-view). Advisor pre-close (tightened view operand to viewable + contractual-order comment). **449/449 × 4 configs; tidy/opgen/invariants OK; NO recook/fuzz.** Named-fwd: SEMANTICS→12b+; PROVIDER binding→13+/§150; alias/lifetime→12c. **CEIR-12b ✅ CLOSED 2026-08-10 (no ADR — attr-vocabulary slice):** the §20/§24/§25 planning-INTENT attrs on `ceir.resource`. `resource.declare` gains `lifetime` {transient\|persistent\|history} (`history<T>` binds here) + `history_length` (ring depth, ≥1, valid only under lifetime=history) + `memory_domain` (§24 INTENT — providers lower, ⛔ NEVER Vulkan/D3D flags; §24's transient/persistent ride the `lifetime` axis) + `residency` (§25) as CLOSED vocabularies, + `streaming_priority`/`budget_class` OPEN tags (12d consumes); `resource.export` gains `direction` {read\|readwrite} ADVISORY (closes 12a's stale export forward). Verifier = a SEPARATE walk `Context::find_resource_intent_misuse` + `ResourceIntentMisuseKind` (8 kinds; wrong VALUE≡wrong KIND fold = state-depth precedent; `IntentAttrOnImport` since imports are never planned) — kept distinct so 12a's `find_resource_misuse` order + negatives stay untouched. `test_resource.cpp` +2 (all-attrs + BINARY+TEXT round-trip with VALUE read-back on BOTH twins [string/int/symbol]; 2 positives + 9 negatives). Advisor fork+pre-close (minted export direction + §25 open tags now; text-twin read-back). **451/451 × 4 configs; tidy/opgen/invariants OK; NO recook/fuzz/version-bump.** **CEIR-12c ✅ CLOSED 2026-08-10 (no ADR — analysis slice, mirrors CEIR-4d):** the resource ALIAS/LIFETIME analysis. `Context::compute_block_lifetimes` computes per graph-owned resource (declare result; import excluded) a live range over the block's op order — `last` follows `resource.view` chains + NESTED-region uses (attributed to the containing "pass" op) + ambient Memory/Universe effects (the "over 4d effects" rule, unregistered ops count) + pins to block-END when exported; ONE recursive walker does uses+export+ambient at every nesting (⛔ wrapping never weakens — pre-close caught nested export/ambient invisible). `resources_interfere` (interval overlap) + `resources_may_alias` (both explicit Transient ∧ neither exported ∧ same TypeKind ∧ same NON-ZERO size_class bucket ∧ disjoint — the 12d planner colors it). Mints the OPEN `size_class` int attr on declare (frame-graph slot bucket; joined kIntentAttrNames→IntentAttrOnImport). `test_lifetime.cpp` +4 (WAR-lifetime + positive control; class gate; slot-size/kind/exported/ambient/unregistered/unspecified-size; nested-region/export/ambient + view-chain + import-excluded). Doc-truth: the 4d hazard "views don't exist yet" clause STRUCK (write(%buf) vs read(view(%buf)) is now a real ops_hazard false-negative; hazard-side consulting the view→root map = named-forward to 12d+). **455/455 × 4 configs; tidy/opgen/invariants OK; NO recook/fuzz/version-bump.** **CEIR-12d ✅ CLOSED 2026-08-10 (ADR-0124 — a plan-producing tier w/ a downstream contract, the 11b/0123 precedent):** the memory PLANNER `Context::plan_block_memory(block, PlanProfile, MemoryPlan&)` colors 12c's live ranges into physical slots (poolable-eligible in (first,decl-idx) order → first-fit a same-(size_class,kind)-bucket non-dedicated slot whose end<first, else fresh shareable; else dedicated — the frame-graph greedy aliaser ported). Inspectable `MemoryPlan` (§162): slots (kind·size_class·dedicated·span·history_length [ring depth, default 1]) + assignments (SlotReason EXPLAINS the negatives: Pooled·NewPoolSlot·Dedicated{Lifetime,Exported,Unsized,Profile}·prior) + transient_logical/physical (physical<logical iff pooling — the REN-1 proof). §78 PlanProfile: Memory(alias) vs Latency(dedicate) real; Balanced/Deterministic ride Memory. Consistency invariant (co-slotted ⇒ resources_may_alias) by construction. `test_planner.cpp` +5. **460/460 × 4 configs; tidy/opgen/invariants OK.** **CEIR-12z ✅ CLOSED 2026-08-10 (no ADR — a gate composes) → CEIR-12 ✅ CLOSED (the 2nd fully-closed band of the re-baselined spine):** `test_band12_gate.cpp` — ONE frame-graph module composes all 4 of 12a's ops (declare/view/import/export) + the intent attrs + mixed lifetimes IN SEQUENCE: type+intent verifiers None; the WAR resource extends to its last use THROUGH a view chain (pos 19, not decl order); the planner pools 3 disjoint same-bucket transients → logical 5, physical 3 (⭐ REN-1 proof, EXACT counts) while %war/%big/history(depth 2)/persistent/exported each keep own slots; the import gets NO assignment (planner never plans imports); Latency cross-check (physical==logical==5); co-slotted consistency invariant; a serialize→deserialize twin re-plans to the SAME shape per declaration-index. **461/461 × 4 configs; tidy/opgen/invariants OK.** **CEIR-12 pipeline COMPLETE: dialect (12a) → intent attrs (12b) → live-range analysis (12c) → interval-coloring planner (12d, ADR-0124) → composing gate (12z).** **CEIR-13a ✅ CLOSED 2026-08-10 (no ADR — op-vocabulary + verifier slice):** the `ceir.compute` dialect (CORE-only; the `crd-ceir-gpu` bridge lowers at 13d). `compute.dispatch` (grid = 3 dynamic Index operands + variadic bindings) + `compute.dispatch_indirect` (args buffer + bindings), RESULTLESS; kernel by `kernel` SYMBOL (KernelRef resolution = 13c); per-binding `access` STRING {r|w|rw}; binding KIND from type (Buffer→storage/Image→texture/View→underlying), SLOT positional — no kind attr. ⛔ effects = CONSERVATIVE BASELINE GPUCommand + ambient MemoryReadWrite (indirect + MemoryRead{args}) so a dispatch hazards the export of its output (the WAR-scar IR edition — advisor Q4); 13d NARROWS to per-binding. `Context::find_dispatch_misuse` (6 kinds; ⛔ wrong-KIND/absent access FOLDS into token-invalid per 12b, robust to a raw deserialized module). `test_compute.cpp` +3 (well-formed + round-trip + 6 negatives + the hazard/12c pins). **467/467 × 4 configs; tidy/opgen/invariants OK.** **CEIR-13b ✅ CLOSED 2026-08-10 (no ADR — op-vocabulary + verifier slice):** the `ceir.transfer` dialect (core-only) — `copy` (dst,src) unified over buffer/image/buffer↔image + subresource-via-views · `upload`/`readback` (+open source/dest, resultless) · `clear` (value=buffer fill word; typed image→13d) · `mip_gen` — all with STATIC per-kind 4a effects (Write{dst}/Read{src}/…). So a transfer hazards PRECISELY: `upload(%b)`→`readback(%b)` is exactly RAW (the upload→first-read barrier), and — unlike a dispatch — does NOT extend an unrelated resource's 12c range. `Context::find_transfer_misuse` (6 kinds; advisor Q4 closed 3 legal-by-accident shapes: transferable=Buffer\|Image\|View-of-those, copy src≠dst [distinct views of one root PASS], mip-gen is-image, clear value fold + on-image). `test_transfer.cpp` +3 (well-formed+round-trip; 5 negatives; RAW + precise-no-bleed + cross-dialect pins). KNOWN HOLE (12c strike): transfer-through-view escapes ops_hazard→13d. **473/473 × 4 configs; tidy/opgen/invariants OK.** **CEIR-13c ✅ CLOSED 2026-08-10 (no ADR — cook-surface extension; two-tick split, both parts gated 475/475 × 4).** CKIR-by-identity, Reading B (the dispatch PINS an expected §107 hash; cook compares pin==resolved — NO cross-layer hash recompute). **PART 1 shipped:** the opgen `[op.kernel_ref]` table {symbol, interface} (validates+emits into OpSpec/OpInfo; test_opgen 53→59); `compute.dispatch`/`dispatch_indirect` gain an OPTIONAL `kernel_interface` int (the hash pin, u64 as i64 bits; additive, no version bump); `DependencyRecord.ckir_refs` ({name, interface_hash, pinned}) extracted by `collect_dependencies` SCHEMA-DRIVEN from `op_info.kernel_ref_symbol` (⛔ I6), deduped+sorted; TWO named-forwards STRUCK (§64 ckir_refs LANDED + program_asset.hpp:28 KernelRef-not-in-interface-hash, hot-swap reasoning). `test_compute.cpp` +1 (pin/unpinned/dedup/sorted/I6 + high-bit round-trip). **PART 2 (cook bridge) DONE:** CDEP schema v4→v5 (the FIRST cooked-format change of the detour — appends ckir_refs {name·u64·u8 pinned}; stale v4 blobs clean-reject at read_cooked_header; content hash + kBinaryVersion unchanged) + `cook_program(…, KernelResolveFn resolve = nullptr, void* user = nullptr)` (fn-ptr+user; nullptr=deferred, CDEP persists) + `scan_kernel_contracts` re-walk (CookResult.op → the dispatch): existence ALWAYS checked (KernelUnresolved incl. unreadable identity), hash IFF pinned (KernelInterfaceMismatch). Identity became load-bearing → `find_dispatch_misuse` gained a 7th kind `KernelNotSymbol`. `test_program_cook.cpp` +1 (mock resolver: pin-match/mismatch/unresolved/unpinned/no-resolver→CDEP round-trips 2 refs). Advisor pre-close caught 2 blockers (the kernel-identity fold at BOTH the cook walk + find_dispatch_misuse; stale TOML forwards) + 3 pins; a regression I introduced (absent-interface reads of_int(0)→spurious mismatch) was caught by the unpinned test + fixed. **475/475 × 4; opgen 59; tidy/invariants OK.** **CEIR-13d ◧ PARTS 1a (scaffold + ADR-0125) + 1b (dispatch lowering + barriers) + 2 (transfer + ambient-narrowing) DONE + gated 2026-08-10 (480/480 × 4); part 3 NEXT (current slice, not closed).** The lowering pass — advisor decomposition: `lower_region(ctx, block) → Array<LoweredCommand>` is a PURE VALUE-LIST fn (compile ≠ run, §158 — NEVER drives IComputeContext, testable with no GPU); handles stay UNRESOLVED (the 13c KernelRef + Value*s) → bind to a gpu-context DispatchDesc at EXECUTE (13z). **PART 1a shipped:** ADR-0125 (bridge birth + value-list contract [plan-producing-tier shape] + conservative hazard barriers + dep-narrowing); the NEW `crd-ceir-gpu` bridge (`engine/ceir-gpu`, links crd-ceir + crd-gpu-context ONLY — render-graph/kir edges added when consumed, §4.2 amendment-by-narrowing); `crd/ceir/gpu/lower.hpp` (`LoweredCommand`{Dispatch·Barrier} + `lower_region` sig) + stub; `tests/ceir-gpu` smoke; ⛔ gate6b.sh target list updated (it hardcodes targets — else the Linux gate silently skips crd-ceir-gpu-tests). ⭐ invariants confirm crd-ceir CORE stays host-only (I5) despite the bridge. **PART 1b DONE:** `lower_region` emits a Dispatch per compute.dispatch/_indirect (grid resolved ALL-OR-NOTHING from arith.const operands via `defining_op()`, else `dynamic_grid`; Indirect never resolves a grid; zero group as-is — ZeroDraw is validate_dispatch's EXECUTE rejection) + ONE Barrier before each carrying the strongest incoming `ops_hazard` (reverse scan → NEAREST source, deterministic). ⛔ LoweredCommand is CEIR-NATIVE + validate-at-EXECUTE (primary-source divergence from the advisor's checklist — validate_dispatch needs a non-null kernel the lowering lacks; surfaced + confirmed; ADR-0125 §2.3 already had it right). `test_lower.cpp` +2. **PART 2 DONE:** `ceir.transfer` lowered (LoweredKind::Transfer + our own LoweredTransferKind = the 5 shipped 13b ops; Clear fill word resolved) + the 13a ambient NARROWED in the bridge — a `gather(op)→precise accesses` (dispatch bindings+`access`; every other op's static effects via op_effects/effect_access) + `precise_hazard` (mirrors accesses_conflict). Upload→first-read barrier now DISCRIMINATING (present iff the dispatch binds the uploaded buffer). ⛔ TWO hazard notions BY DESIGN (core ops_hazard stays conservative — don't unify); coupling reason = FALSE-GREEN avoidance not test-churn. 4 pins tested. `test_lower.cpp` rewritten (part-1b binding-less asserts FLIPPED to no-barrier). **CEIR-13d ✅ CLOSED 2026-08-10 (ADR-0125) — part 3 (12c VIEW→HAZARD retrofit) DONE + gated:** the 12c view hole CLOSED across CORE + bridge in ONE move — new `Context::resource_root` (follows `resource.view` chains to the root via `defining_op`, one hop per 12a, loop-guarded; IDENTITY only, not byte-range) normalizes every captured resource in `op_access_at` (core) AND `gather` (bridge — dispatch bindings + indirect args + static effects), so `write(%buf)` vs `read(view(%buf))` now HAZARDS in both the core hazard walk and the bridge barriers. The context.hpp `ops_hazard`/`accesses_conflict` SUPERSEDED clauses + ADR-0124 §3 + ADR-0125 §2.5/§3 + the tracker 12c-row named-forwards STRUCK-in-place LANDED. `test_hazard.cpp` +1 (view→root RAW/WAR + different-buffer-view→None [precise, not blanket] + two-views-of-one-buffer→hazard); the part-2 bridge view-hole pin FLIPPED (no-barrier → Barrier(Raw)) + retitled. ⛔ a view laundered through a region-yield/call-result STILL escapes (a deeper inter-op alias hole, named-forward). Advisor pre-close CONFIRMED (design + the source=scoreboard sweep). **481/481 × 4 configs** + tidy(5) + `-Werror=switch` + opgen + invariants (crd-ceir CORE stays host-only/jobs-free/asset-free — a pure hazard-walk refinement, no new deps). NO recook/fuzz. ⭐ **CEIR-13d ✅ — all 4 checkpoints (1a scaffold+ADR-0125 · 1b dispatch lowering+barriers · 2 transfer+ambient-narrowing · 3 view→hazard retrofit+FLIP): the `crd-ceir-gpu` lowering bridge — CEIR region → an INSPECTABLE command list with 4d-derived barriers, compile≠run, validate-at-execute.** CEIR-13 ◧ (13a✅ 13b✅ 13c✅ 13d✅). **CEIR-13z ◧ DESIGN-LOCKED 2026-08-10 (advisor-consulted; design note `docs/design/ceir-13z-execution-proof.md`) — the §129 EXECUTION PROOF + BAND-13 close.** add·reduce·scan·FFT as CEIR program ASSETS (text AND builder), dispatched Vulkan AND DX12, bit-exact vs the existing CPU oracles (`ckir_{reduce,scan,fft}.hpp`), hot-reload live-swap, reference validates host-visible; the 13d lowering is the seam it executes through. **Forks (advisor):** A → standalone `execute_lowered(...)` in crd-ceir-gpu (NOT IExecutionProvider — provider-wrap = 21/26 fwd); B+C COLLAPSE → NO crd-kir edge, a caller-supplied resolver fn-ptr → `IGpuProgram*` (test links kir; null→validate_dispatch NullProgram); D → backend-agnostic (`ckir_kernel_dispatch.hpp` abstracts both), FFT LAST (its multi-dispatch chain stresses 13d's barriers); F → the honest claim = CEIR-path output BYTE-IDENTICAL to direct-CKIR-path (all 4), math keeps convention (int `==`, FFT f32-tol vs DFT + bit-exact impulse/DC — source=scoreboard); G → the reference leg is a BRIDGE-side CPU-oracle run (core Interpreter STAYS NoSemantics for dispatch, §150 fwd + I5 intact). ⚠ the flip is ALREADY user-authorized (ADR-0108 §7) — the ONE commit at 13z-z is the ratification gate. **13z-1a DONE + gated 2026-08-10 (ADR-0126):** the execution SEAM — `execute_lowered`/`validate_lowered` (crd-ceir-gpu) bind a 13d-lowered list to the ADR-0100 IComputeContext surface (`KernelResolveFn`→`ComputePipeline*` + `ResolvedBinding` Value*→`ComputeBuffer*`, `resource_root`-normalized). ⭐ ADR-0126 pins the TWO-SURFACE reality (ADR-0100 IComputeContext = the compute-KERNEL surface [CKIR proof kernels + oracles] vs RAF-2 command_model DispatchDesc = the frame-graph surface, NOT dead code); the ADR-0125 §2.3 + lower.hpp "DispatchDesc at execute" language STRUCK-in-place, `validate_dispatch` re-homed into typed `ExecuteError` (Unresolved/Zero/Arity/Unmapped/Unsupported). Dispatch-only (Transfer/dynamic-grid→Unsupported); the Barrier→ComputeAccess map is 13z-3 (fft2d TransferDst scar). 4 DEVICE-FREE always-runs tests (fake resolver/buffers/recorder — the all-skip-false-green guard). **485/485 × 4 configs** + tidy(4) + `-Werror=switch` + opgen + invariants (crd-ceir CORE untouched). **13z-1b `add` on device — VULKAN ✅ DONE + gated 2026-08-10 (487/487 × 4), DX12 NEXT.** ⭐ the FIRST real on-device CEIR execution: new `tests/ceir-gpu-vulkan` (links crd-kir-vulkan) + shared `tests/gpu-shared/ceir_execute_1wg.hpp` (`build_add_kernel` KGraph c=a+b · `build_add_ceir_asset` · `dispatch_ceir_1wg` = clone of `dispatch_kernel_1wg` swapping ONLY `rec.dispatch`→`execute_lowered`). The `add` CEIR asset lowers → runs on a REAL Vulkan device (Windows) AND llvmpipe (Linux), **byte-identical to direct CKIR** + `==` the CPU oracle + **validation-SILENT** (`ValidationCapture==0`); device-skip-guarded + a device-free lower-shape always-runs case. ⛔ tidy-gate fix: `scripts/tidy-files.ps1` now globs `engine/*/generated` (a header incl. a generated engine header was parsing UNGATED). **13z-1b DX12 mirror ✅ DONE → 13z-1b CLOSED both backends 2026-08-10.** `tests/ceir-gpu-dx12` (guarded `if(NOT TARGET crd-kir-dx12)`; links crd-gpu-context-dx12; `emit_compute_kernel_hlsl`→`create_pipeline_from_hlsl`) — the SAME `add` asset + backend-agnostic `dispatch_ceir_1wg` on a REAL D3D12 device, byte-identical to direct CKIR + `==` oracle. ⛔ ValidationCapture-silent stays Vulkan-only (gpu-context-dx12 has no info-queue capture — named-forward). ONE build fix: link crd-gpu-context-dx12 explicitly (its include dir isn't re-exported by crd-kir-dx12). Windows-only, NOT in gate6b. **Gate: win-debug/win-asan 489 (with DX12) · linux 487 (Vulkan/llvmpipe, DX12 guarded out) — honest per-platform counts.** ⭐ the `add` CEIR asset now runs on BOTH Vulkan + DX12 real devices through author→13d-lower→13z-execute_lowered. **13z-2 ✅ CLOSED 2026-08-10 reduce/scan (text+builder), BOTH BACKENDS (win 496 / linux 492).** reduce (`build_reduce_block`, 2 buffers r,w) + scan (`build_scan_block` write_blocksum=TRUE, 3 buffers r,w,w, `bsum[0]==total`) as CEIR assets on Vulkan+DX12+llvmpipe, byte-identical to direct CKIR + integer oracle (⛔ INTEGER-valued f32 inputs → exact under any association order, naive `==` legit — landmine 1) + validation-silent. The D-PROBE answered fork D (dangling `@kernel` symbol round-trips CEIR text). Harness → module-wrapped `build_ceir_dispatch_asset(kernel,access,nbufs)`. ⭐ the full §121 TEXT-AUTHORING proven: an embedded hand-authored text literal parses to the SAME canonical module as the builder (`print(parse)==print(builder)`) + lowers identically (device-free, 4 configs), AND a **parsed-from-text reduce asset EXECUTES on a real Vulkan GPU == oracle** (text-on-both-backends is ceremony — byte-equal canonical modules + deterministic lowering). ⛔ single-workgroup variants only (`execute_lowered` NO-OPs Barrier until 13z-3). ⛔ ASCII-only test-name scar re-hit (a `§` broke ctest Windows matching → renamed). **13z-3 ◧ FFT (barrier stress): PART 1 DONE + gated 2026-08-10 (win 497 / linux 493) — the 13d BARRIER COMPLETION.** Advisor Fork A option (a): `LoweredCommand::Barrier` gains a `resource` field; `lower_region` emits ONE barrier PER conflicting root resource (in `after`'s binding-operand order), not one-per-dispatch. ⛔ a 13d CORRECTNESS FIX: the part-2 "one barrier, nearest-strongest" DROPPED conflicts (a dispatch reading N buffers from N prior passes recorded only 1; the FFT is where the 2nd is a real data race). `precise_hazard` retired; ADR-0126 §3.3 resolved; +1 discriminating multi-resource test (2 buffers/2 writers → 2 RAW barriers). **13z-3 part 2 DONE + gated 2026-08-10 (win 499 / linux 495):** `execute_lowered` REPLAYS each Barrier as `rec.barrier(root_buffer, from, to)` via `hazard_access` (RAW→ShaderWrite→ShaderRead · WAW→WW · WAR→RW; nullptr resource → all bound buffers). ⛔ WAR wrinkle VERIFIED both backends: Vulkan issues a real COMPUTE→COMPUTE execution dep, DX12 IGNORES `from` and issues a `to`=ShaderWrite→UNORDERED_ACCESS UAV barrier (direction-agnostic) — no conservatism. FakeRec extended to RECORD barrier calls; +2 device-free tests (write-then-read → 1 RAW barrier on the shared root w/ right accesses; 2-buffers-from-2-writers → 2 per-resource barriers). **13z-3 ✅ CLOSED 2026-08-10 FFT (barrier stress), BOTH BACKENDS (win 501 / linux 496).** ⭐ the on-device barrier stress — the 6-dispatch 2D FFT (`build_fft2d_c2c`, 14 buffers, 4 kernels) as a MULTI-DISPATCH CEIR asset runs BIT-EXACT vs the CPU oracle (`run_fft2d_cpu`) on real Vulkan (0.99s) + llvmpipe + real DX12 (0.54s) + validation-silent (Vulkan). New harness: `build_ceir_multi_asset` + `resolve_multi` (dispatch op→pipeline BY IDENTITY, no registry) + `dispatch_ceir_multi` (clone of `dispatch_fft2d`, execute_lowered replacing the dispatch+barrier loop). ⭐ pass 3 gets TWO per-resource barriers (RAW on b_tr_re from pass 1 AND b_tr_im from pass 2) — the part-1 completion LOAD-BEARING; execute_lowered replays them. All 3 parts (per-resource lowering + execute replay + FFT) validated on a real multi-dispatch transform both backends. **13z-4 ✅ reference leg + hot-reload — BOTH LEGS DONE + gated 2026-08-10 (leg-1: win 503 / linux 498, device-free; leg-2: win 510 / linux 505, device hot-swap + CUDA-on-Linux).** Leg 1 both device-free (4 configs). 1a: `execute_lowered_cpu` (test-side, links kir — I5 wall holds, "bridge-side"=test-side NOT crd-ceir-gpu) runs the LOWERED FFT list on the CPU via `eval_cpu_kernel`, BYTE-EQUAL to the plan-driven `run_fft2d_cpu` (proves dispatch sequence + binding mapping + grid `cmd.groups_x`). 1b: the CORE Interpreter REFUSES a `compute.dispatch` with a TYPED `NoSemantics` error (exec.cpp:238; the interp eagerly evals the resultless dispatch) — I5 device-blind, §150 fwd, NO fake skip. **CEIR-13z-4 leg 2 (hot-reload) ✅ DONE + gated 2026-08-10** (win-debug/asan full ceir 510/510 · linux-gcc-debug/asan 505/505) via the 10a `add_source`/`reload_source` TEXT seam (⛔ option B — a test changing its own fn-ptr — was TAUTOLOGICAL, rejected): (1) PROBE — a func-less top-level `compute.dispatch` over `resource.declare` COOKS + LOADS into a fresh generation Context + re-lowers to 1 dispatch (the untested cooked-blob round-trip of compute+resource; a Registrar installs arith/func/resource/compute into the transient cook Context so the verifiers aren't vacuous); (2) HOTSWAP (device-free) — `reload_source` @radd→@rmax moves `content_hash` but HOLDS `interface_hash` (func-less ⇒ empty exported-func projection ⇒ symbol→stable_hash NOT interface_hash — CONFIRMED, decision==HotSwap+installed) + the NoChange seal (identical text = no-op → cook determinism); (3) DEVICE — a SYMBOL-KEYED resolver (reads the dispatch's `kernel` symbol) so the RELOADED TEXT ALONE picks add-reduce(2016)→max-reduce(63) on a real Vulkan device — tautology killed (⛔ rebind `sctx.ctx=gen->ctx` + re-collect binds from the RELOADED module; ASan-clean; harness refactor `dispatch_ceir_1wg_resolved` core + `dispatch_ceir_1wg` delegate, all callers unchanged). ⭐ CUDA as a 2nd platform (user-directed): the CUDA CEIR tests (add/reduce/scan/FFT) now BUILD + RUN on the real Linux **RTX 4070 Ti SUPER** (WSL passthrough, linux-gcc-debug 5/5, added to gate6b). ⛔ scar: the PRE_TEST `_NOT_BUILT` sentinel fires in BOTH discovery modes (**built-vs-configured is the axis**, not PRE_TEST vs POST_BUILD) — it correctly flagged unbuilt CUDA on a CUDA-capable box; Linux-ASan CUDA skips (WSL `libcuda` cuInit fails under ASan) via `ASAN_OPTIONS=detect_leaks=0`, `if(UNIX)`-guarded (a `leak:libcuda` suppression was insufficient — `<unknown module>` JIT frames). **NEXT = 13z-z: the BAND-13 GATE + ⚠ the ADR-0108 CORNERSTONE FLIP (already user-authorized).** Checkpoints: 13z-1a ✅ · 13z-1b ✅ · 13z-2 ✅ · 13z-3 FFT ✅ · 13z-4: leg 1 reference-executor ✅, leg 2 hot-reload ✅ (+ CUDA on real Linux RTX 4070 Ti) · 13z-z band gate + cornerstone flip NEXT. All crd-ceir core (3-target × 4-config)

> **CEIR-2 ✅ BAND CLOSED (op-definition generator, §8).** Ops DEFINED in `engine/ceir/ops/*.ceirop.toml` →
> `tools/ceir_opgen/ceir_opgen.py` (stdlib) emits committed C++ (self-registers via the CEIR-1d registry) +
> `.ops.{json,md}` + a gated smoke test — **an op is a TOML edit + regen, ZERO central-enum/switch edits** (proven
> end-to-end by adding the full-surface `test` dialect at 2z). Detail → `docs/sessions/2026-08-08-ceir-2-opgen.md`.
>
> **CEIR-3a ✅ (interned structural types, §16) 2026-08-08.** `TypeId` is now a real interned STRUCTURAL type (scalars +
> the §16 aggregate family): `type.hpp` (`TypeKind`/`FloatKind`/`Type`; **`kind` a FIELD** for I6), `Context::type_*` +
> `type_of` (**asserts, no silent fallback**), a canonical `!`-sigil grammar (`!vec<4x!f32>`, `!struct<P,x:!i32,y:!f32>`,
> `!option<!result<…>>`, …) printer+parser byte-exact, and binary **v2** with a **child-first `TYPE` chunk** (content-pure;
> per-result/arg refs so no future v3 bump). Every `TypeId{n}` magic literal migrated to real factories (a stray one
> aliased to incidental intern state — caught only by the dirty-context purity test). 65/65 × 4 configs + tidy +
> invariants. Detail → `docs/sessions/2026-08-08-ceir-3-types.md`.
> **CEIR-3b ✅ (generics, §16/§98) 2026-08-08.** Appended `TypeParam`/`Trait`/`Callable` kinds (reuse the v2 record —
> **no version bump**); `!param<T,!trait<Ord>>` / `!trait<..>` / `!fn<(P)->(R)>` grammar; a live trait-conformance
> registry (`register_conformance`/`satisfies`, transitive over supertraits) + a memoized, diagnostic-bearing
> `substitute` (unbound params stay generic; a constraint violation reports the (param,trait)). Added `type_is_well_formed`
> — the decoder now rejects structurally-invalid records (a latent 3a gap). 70/70 × 4 configs + tidy + GCC
> `-Werror=switch`.
> **CEIR-3c ✅ (resource + view types, §23) 2026-08-08.** Nine appended kinds (Buffer/Image/Sampler/ResourceTable/
> AccelStruct/VideoFrame/AudioBuffer/ExternalResource/View — **no version bump**, reuse the v2 record). **Interp B**:
> a view TYPE carries the underlying resource + a range-dimension presence MASK (`byte/element/mip/layer/aspect`), the
> range VALUES are runtime (tensor/sparse → 3d; §24 domains = value semantics). Grammar `!buffer<plain,!f32>` /
> `!image<d2,fmt>` / `!view<RES,mip,layer>`; tri-split `view_combination_valid` (parser fails / decoder rejects /
> factory asserts). Fixed a corrupt-input abort (parser fell into the asserting factory on a prior error). 73/73 × 4
> configs + tidy + `-Werror=switch`.
> **CEIR-3d ✅ (shapes + tensors, §21/§35) 2026-08-08.** Four appended kinds (Dim/Shape/Tensor/SparseTensor — **no
> version bump**). Type-level foundation only (the `ceir.shape`/`ceir.tensor` op dialects + layout §22 are CEIR-18;
> tensor/sparse returned from the 3c boundary). `!dim<4|dyn|N>` / `!shape<..>` / `!tensor<e,s>` grammar; tri-state
> `shapes_broadcast` (right-aligned incompatible pos) / `shapes_reshape` (overflow→Unknown). Added `type_is_canonical`
> (decoder rejects non-canonical records — a name on an Int etc. — latent since 3a) + `dyn` reservation tri-split. A
> 3b×3d seam test (substitute a param through a tensor shape). 77/77 × 4 configs + tidy + `-Werror=switch` (31 kinds).
> **CEIR-3e ✅ (physical quantities, §17/§18) 2026-08-08.** One appended kind (`Quantity`, no version bump): tags a
> numeric underlying with an 8-base SI dimension (ADR-0078) **bit-packed into count+cols** (crd::units::Dim is
> compile-time-only → CEIR mirrors it at runtime; a static_assert pins the base order). Grammar `!qty<!f32,L1T-2>` /
> dimensionless `!qty<!f32,1>`; `quantity_dimensions_equal`→first_differing_base (the 3z Length+Time diag), `quantity_dim_mul/div`
> (i8 overflow→failure). ⭐ The **`units.erase` op is the FIRST non-reference dialect through the CEIR-2 generator** (a
> `.ceirop.toml` + regen, zero central edits; drift now 3 dialects × 5 files, smoke auto-generated). 83/83 × 4 configs +
> tidy + `-Werror=switch`.
> **CEIR-3f ✅ (ownership/lifetime qualifiers + escape predicate, §19) 2026-08-08.** One appended kind (`Qualified`, no
> version bump): a WRAPPER — `members[0]`=type, `count`=`OwnershipKind` (9 modes imm/mut/borrow/own/shared/weak/state/ext/
> transient) — chosen over nine kinds (a value has exactly one mode; the wrapper composes over resources too). Grammar
> `!qual<borrow,!buffer<plain,!f32>>`; tri-split `qualified_composition_valid` (rejects qual-of-qual/dim/shape/trait). The
> escape rule splits 3f/3z: 3f ships `value_escapes_region(Value*,Region*)` (def-use walk vs directional region
> containment), 3z composes it over `Qualified<BorrowedView>`. ⛔ Two advisor-caught fixes: **`create_operation` never
> wired `Region::m_parent`** (latent since 1a; the first upward walk reported false-positive escapes) and **`substitute`
> bypassed the tri-split** (retroactively closes qty-of-qty/qual-of-qual via a new `SubstResult::failed_compose`).
> Escape contract = direct-use, type-directed. 87/87 (773 assertions) × 4 configs + tidy + `-Werror=switch` (33 kinds).
> **CEIR-3z ✅ BAND-3 GATE (§16/§17/§19/§21) 2026-08-08.** The four band-3 error checks fire as **discriminating pointing
> diagnostics**: Length+Time (`quantity_dimensions_equal`→first_differing_base, base 0 AND base 2), rank-mismatched
> broadcast (`shapes_broadcast`→position 1 + control), borrowed-view escape (a NEW public `find_borrowed_escape(Module&)`
> →`BorrowEscape{value,escaping_use}` module walk over `!qual<borrow,_>` values — both value kinds; owned-escaper +
> inside-borrow correctly ignored), generic-constraint (`substitute`→failed_param/failed_trait, exact (T,Ord) + accept
> control). Pointing upgrade: `value_escapes_region`(bool) gained `first_escaping_use`→`Operation*` (a bool can't point).
> ⭐ **Honest boundary:** pointing predicates + the one escape walk; the op-level verifier wiring of dim/broadcast/
> constraint onto typed operands composes at **CEIR-4** (no producer op yet — 3e "predicate-now" precedent). `find_borrowed_escape`
> is first-offender, structural (not dominance — CEIR-5b), direct-use+type-directed. Dedicated `test_band3_gate.cpp`
> (`[gate3]`, 4 cases). 91/91 (791 assertions) × 4 configs + tidy + `-Werror=switch`. **→ BAND 3 CLOSED.**
> **CEIR-4a ✅ BAND 4 OPENS (§26 effect vocabulary) 2026-08-09.** `effect.hpp`: `EffectFamily` (all 27 §26 families, u8,
> append-at-end, static_assert-pinned to the generator) + `EffectRecord` POD `{family, target(None/Operand/Result),
> index, range_mask}` (range_mask reuses 3c ViewRange; kind-level — per-instance is 4d, callee-derived EffectsFn is
> CEIR-5). ⭐ Attach point **B1**: effects on `OpInfo` via `register_op(...,effects={})` (arena-COPIED), queried by
> `Context::op_effects` — not a 1d interface, not reflection-only. ⛔ **EMPTY≠UNKNOWN**: an empty span is "provably
> effect-free" only when `op_info!=nullptr`; unregistered = maximally effectful. ⛔ **func.call landmine** (a registered
> op defaulting to effect-free reads as *provably* none) → declares a conservative ExternalCall barrier. Pure⇒zero
> effects at both live arms. 2a schema: bare-family string OR `{family, operand|result, range}` table, generator
> validates vocab+index+range; `OpSchema.effects` StringView[]→EffectRecord[]; `.ops.json` string[]→object[] (schema_v1,
> scaffold field). NOT serialized (no version bump). 98/98 ctest (820 assertions) × 4 configs + tidy + opgen(36 py).
> **CEIR-4b ✅ (§27 determinism + §28 numerics) 2026-08-09.** `semantics.hpp`: `DeterminismClass` (5 §27 tiers +
> `Unspecified=0` default, static_assert-pinned; `External`→`ExternalNondeterminism` reconciled) on `OpInfo` via
> `register_op`, `op_determinism`; `CompilerMode` (Normal/Fast/Deterministic/Certified, session state NOT serialized) +
> `determinism_satisfies_mode` (6×4 matrix). `NumericalSemantics` = all 12 §28 knobs (0=Inherit), per-INSTANCE, ONE
> pack/unpack into a reserved `numerics` int attr (unpack validates bounds); `numerics_satisfies_mode` honors the fmad
> scar (Fast admits FMA/fast-math). `find_mode_violation(Module&)` = first op violating the active mode's §27 class OR §28
> numerics OR a corrupt `numerics` attr (violates every mode). Cook-time native≥op consistency; `OpSchema.native_determinism`
> typed. ⛔ EMPTY≠UNKNOWN (Unspecified fails strict modes). Boundaries: pass-wiring→CEIR-6, replay/alternatives→CEIR-5+,
> kind-vs-instance not cross-checked (CEIR-6). arith int ops = BitExact. 106/106 ctest × 4 configs + tidy + opgen(40 py).
> **CEIR-4c ✅ (§15 eval domains + §32 realtime + domain-legality verifier) 2026-08-09.** ⭐ `register_op` consolidated
> into an `OpSpec{traits,verify,effects,determinism,domain}` descriptor (designated initializers; migrated every site).
> `EvalDomain` (10 §15 domains + Unspecified, static_assert-pinned) per-op-KIND on `OpInfo` via the spec + 2a `domain`
> field + `op_domain`; `OpSchema.domain` StringView→EvalDomain. `RealtimeClass` (7 §32 + Unspecified) — a REGION property,
> NOT a schema field. A region's (domain+realtime) rides ONE packed `region_exec` int attr on the region-owning op (module
> CONTENT — survives round-trip; symmetric with 4b's session-only mode). `find_domain_violation(Module&)→DomainViolation`
> — innermost-tag-wins; seeded §32 rule (FileIO/NetworkIO illegal in AudioRealTime/DeviceTime/HostAudioTime); ⛔ an
> UNREGISTERED op in an audio region is flagged (maximally effectful), a registered effect-free op is legal (empty≠unknown).
> The 4c walk does NOT consume kind-domain (→CEIR-6). arith=EitherHostOrDevice, test=HostFrameTime. 115/115 ctest × 4
> configs + tidy + opgen(43 py).
> **CEIR-4d ✅ (effect-derived ordering hazards §26/§116) 2026-08-09.** A SCHEMA-QUIET slice (hazards DERIVED from 4a
> effects — no TOML/generator/regen/py). `hazard.hpp`: `HazardKind{None,War,Raw,Waw}` + `ResourceClass` (14) +
> `effect_access(EffectFamily)` (TOTAL switch, NO default → -Werror=switch guards a 28th). ⛔ `RandomRead` WRITES (a PRNG
> draw advances the stream — else RNG draws reorder, breaking replay); `TimeRead` inert-read; Alloc/Dealloc/Residency in
> Memory (use-after-free visible); IO one rw class; ExternalCall+Synchronization = Universe barrier. `Context::ops_hazard
> (before,after)` (WAW>RAW>WAR over effect-pairs sharing a resource `(class, Value|null)` + overlapping range with ≥1
> write; distinct Values non-aliasing; reports everything; unknown=Universe rw, Pure=None) + `collect_block_hazards` (O(n²)
> all-pairs reference, one block, list order). `Operation::result` loosened to const. 122/122 ctest × 4 configs + tidy.
> **CEIR-4z ✅ BAND-4 GATE 2026-08-09 → BAND 4 CLOSED (4a–4z).** A TEST-ONLY gate (like 3z) composing find_mode_violation +
> find_domain_violation + ops_hazard/collect_block_hazards over ONE curated module — the band's exit criterion "the
> compiler distinguishes reorderable vs ordered ops correctly" met verbatim. Centerpiece: the **WAR-needs-lifetime scar**
> (read(R)-then-write(R) = WAR from effects+SSA identity, no decl-order; different buffer = None). Exact 4-edge
> collect_block_hazards matrix + reverse sweep; ⛔ every gate op BitExact (the Unspecified-default mode-axis trap);
> orthogonality by FULL edge-list identity (numerics→Certified, Unspecified-op→determinism, side audio-RT+FileIO→domain —
> hazards identical each time); hazards survive serialize/deserialize (Value identity through the parser fixup). `test_band4_gate.cpp`
> (`[gate4]`, 5 cases). 127/127 ctest × 4 configs + tidy. → BAND 4 CLOSED.
> **CEIR-5a ✅ (structured control-flow region ops + the constant-cond if fold) 2026-08-09.** Band 5 opens (a GEAR
> CHANGE). The generator gained a region-SIGNATURE schema + THREE variadic axes (operands/regions/results; arg COUNT is
> the verifier contract, MIN-ARITY builders, full arity via create_operation). `ceir.core` = `scope`/`if` (value-producing,
> variadic results), `for`/`foreach` (typed region arg), `while` (cond+body, structural — cond-yields-1-bool→5b),
> `switch`/`match` (variadic case regions; patterns→CHIR), `yield` (variadic-operand Terminator) — all BitExact + ZERO
> effects. `Context::fold_constant_if` splices the taken branch (THEN and ELSE, both tested) + RAUWs the results with the
> yield's operands + erases; BAILS on non-const/multi-block/no-yield, a `region_exec`-tagged if OR any tagged op INSIDE
> the taken block (⛔ a rewrite must audit the attrs it MOVES), and a yield/result COUNT MISMATCH. Design: HOMOGENEOUS
> result types (placeholders until CEIR-6; the fold replaces them — no create_operation overload), MIN-ARITY builders,
> SKELETON-VERIFIES, loop-carried values→5d. ⚠ This turn disproved 2 checkpoint blocker-guesses (builder-count-param,
> create_operation-overload) — mark checkpoint blockers unverified unless the code was read. 138/138 ctest × 4 + tidy +
> opgen(49 py). NEXT = **CEIR-5b** (SSACFG verifier: dominance/terminators/block-args — the general yield contract + the
> 3f back-link earn their keep, §13/§115) → 5c (calls + EffectsFn) → 5d (state/delay) → 5z (executor gate).

> **CEIR-2 generator (reusable for every future band):** `tools/ceir_opgen/ceir_opgen.py` reads
> `engine/ceir/ops/<dialect>.ceirop.toml` → validates (structure+vocab of every §8 + ADR-0110 field) → emits from ONE
> model: `engine/ceir/generated/crd/ceir/gen/<d>_ops.{hpp,cpp}` + `<d>.ops.{json,md}` + `tests/ceir/generated/test_<d>_gen_smoke.cpp`
> (3 TEST_CASEs, globbed via CONFIGURE_DEPENDS). `--check` drift ctest guards all 5 files/dialect; `test_opgen.py` = 26
> validator/emitter unit tests. **Adding a CEIR op = a TOML edit + regen, zero central-enum/switch edits.**

**⛔ THE LIVE TRACKER IS `docs/detours/D-007-ceir-tracker.md`** (CEIR bands 0–32 + the RAH parallel track). CEIR — the
Cerid Execution IR — is the new master architectural spine (user-directed 2026-08-07): every reusable algorithm becomes
a versioned, inspectable, hot-reloadable **program asset**; native C++ only for genuinely new host/hardware capability.
**THE LAW:** `docs/research/2026-08-07-ceir-universal-programming-master-roadmap.md` (§0–§185). Mantra: *ALGORITHMS ARE
PROGRAM ASSETS · CAPABILITIES ARE NATIVE PRIMITIVES · COMPILERS CHOOSE LOWERINGS · BACKENDS EXECUTE.* The old post-RAF
4-track table in `D-007-gpu-program-system.md` is **re-hung under the CEIR bands** and preserved as history/contracts
(A/RPL→CEIR-15 · C/MLR→CEIR-21 · hesap-GPU→CEIR-19 · frame+executor→CEIR-12/13 · B/I2D→CEIR-28 · D/D7E→CEIR-30).

**CEIR-0 DESIGN PHASE COMPLETE + ACCEPTED (2026-08-07/08):** 0a inventory (headline: RAF already did the atomic-vs-
composite split → CEIR is a promotion, not a rewrite) · **ADR-0108** (owned language stack; C++ no longer the *only*
authorable program — supersedes ADR-0081 §9) · **ADR-0109** (CEIR/CHIR/CKIR one-way layer contract + `crd-ceir`
host-only module + `crd-ceir-host`/`crd-ceir-gpu` dependency-inversion bridges + I3/I4/I5; **binding for CEIR-1**) ·
**ADR-0110** (native-intrinsic schema + plugin levels) · 0e CHIR-0 note · 0g two-axis maturity model · 0h deletion
ledger · 0z §184 report + sizing (CEIR-1…13 ≈ 34–55 KLOC, ~4–8 mo). CEIR-0f (D-007 restructure) executing.

**HOW WE WORK (user-directed):** **strict band order** CEIR-0→32; each band closes at its gate before the next. **RAH
runs in PARALLEL** (the binding/attachment vocabulary CEIR-9/11 lower onto). ⛔ Everything else PAUSED (§176: only bug
fixes, CKIR fixes, tests, docs, RAH, CEIR). ⛔⛔ Foundational/critical-path work done DIRECTLY, never delegated. ⛔⛔
Implementation forks require `isolation:"worktree"` + a tight mandate ([[feedback_implementation_forks_need_worktree_isolation]]).
**User controls commits (no AI trailer).**

## Active state

- **CEIR (spine) — CEIR-1a ✅ CLOSED (4-config per-slice sweep PASS, 2026-08-08).** `crd-ceir` module +
  `Context/Module/Operation/Value/Block/Region` + intrusive in-arena def-use + `crd::memory::GrowableLinearAllocator`
  (moved to crd-memory) + `crd-ceir-invariants` I3/I5 gates — all green across debug/asan/shipping-LTCG/tidy. The full
  sweep peeled **7 pre-existing cross-band blockers** (RAF/REN/CKIR bands never passed shipping-LTCG/asan-complete/tidy);
  all fixed gold-standard (2 real engine bugs: DX12+Vulkan RT pipeline-cache keyed by pointer/handle → content-hash).
  Log: `docs/sessions/2026-08-08-ceir-1a-and-preexisting-fixes.md`.
- **CEIR-1b ✅ CLOSED (2026-08-08).** `SymbolTable` (per-Module, arena-backed HashMap; duplicate-reject) + `Visibility`
  + the `ceir.func` dialect (`func.func`/`func.call`/`func.return`, cross-module resolution by name) — all on the
  generic Context factories (open-world). `tests/ceir` 12/12. **Gated across all 4 configs on crd-ceir** (a complete
  gate — crd-ceir has zero downstream consumers, grep-proven; full-tree sweep re-earns its keep at the band close).
- **CEIR-1c ✅ CLOSED (2026-08-08).** Interned typed attribute VALUES (`AttrValue`/`AttrId`, dedup) + a per-op
  AttrDict (`op->attr(name)` / `Context::set_attr`) + the source map (`register_file`→`file_id`, `file_path`) so
  every op's `SourceLoc` provenance is real (§111, no retrofit). Dissolved the 1b interim: `func.call`'s callee is
  now a `SymbolRef` attribute. `tests/ceir` 18/18. Gated all 4 configs (scoped-complete).
- **CEIR-1d ✅ CLOSED (2026-08-08).** Open-world **dialect registry** + op **traits/interfaces** + **verifier**
  dispatch — analyses query traits/interfaces, the core NEVER switches on op.kind (new **I6** grep-gate proves it,
  bites on `switch(op.kind())`). Unknown-dialect ops survive opaquely; the `func` dialect self-registers.
  `tests/ceir` 22/22. Gated all 4 configs (scoped-complete).
- **CEIR-1e ✅ CLOSED (2026-08-08).** Deterministic textual **printer** (IR→canonical MLIR-flavored text; pre-order SSA
  numbering + name-sorted attrs → byte-identical; floats keep a `.`/`e` marker; unknown-dialect opaque; **no layout**,
  §10) + recursive-descent **parser** (`parse→ParseResult`; use-before-def fixup pass, strings unescaped-before-intern,
  balanced-brace region count skipping string literals, malformed input rejected w/ byte offset). **`print(parse(x))==x`
  byte-exact.** MLIR-faithful symbol identity (advisor): func name/visibility now ride ON the op as `sym_name`/
  `sym_visibility` attrs (SymbolTable = an INDEX over `sym_name`), so identity round-trips through the generic attr
  machinery and the parser rebuilds the module table. `tests/ceir` **31/31**. Gated across the 5-config contract.
- **CEIR-1f ✅ CLOSED (2026-08-08).** **Binary serial form** (`binary.hpp`/`binary.cpp`: `serialize`/`deserialize`) —
  CRDR-shaped (ADR-0038): magic `'CEIR'` + version + FourCC/length chunks a reader **skips by length** when unknown
  (`STRP`/`SRCM`/`ATTR`/`BODY`). ⛔⛔ field-by-field LE (self-contained `put_u*` + `.ok` `Cursor`; can't link crd-kir).
  **⭐ Content-pure:** pools built from the module WALK, BODY holds pool INDICES not Context ids → the blob is a pure
  function of module content (dirty-context byte-equality proven). Carries `Region::kind` (closes the 1e divergence, via
  new `Context::set_region_kind`); `SourceLoc` survives by PATH; symbol identity via the shared `detail::register_symbol`
  (extracted from the parser). `serialize∘deserialize∘serialize` byte-exact; agrees with the text form. Malformed input
  rejected w/ byte offset (bad magic/version/truncation/trailing-junk/oob index). `tests/ceir` **37/37** (`build_rich`
  now a shared `rich_graph.hpp`). Gated across the 5-config contract; invariants I3/I5/I6 green both OSes.
- **CEIR-1g ✅ CLOSED (2026-08-08).** **`ModuleBuilder` fluent API** (`builder.hpp`/`builder.cpp`: `ModuleBuilder` +
  `OpBuilder` proxy + `InsertionGuard`). ⛔⛔ NO privileged bypass — every op routes through `Context::create_operation`
  + shared `detail::register_symbol`, so a builder module is **byte-identical to the hand-built one** (proven).
  `verify(&failing)` dispatches the REAL per-kind `Context::verify` (rejection test proves it, no stub); `build()`
  returns nullptr on a duplicate `sym_name` (op erased, no silent overwrite). `tests/ceir` **41/41**. Gated across the
  5-config contract.
- **CEIR-1h ✅ CLOSED (2026-08-08).** **The permanent harness, seeded** (§119/§167): round-trip fuzz (random valid
  modules via `ModuleBuilder`, fixed xorshift64 seeds — text+binary byte-exact), a `stable_hash` (FNV-1a over the 1f
  content-pure blob — NEW surface, deterministic + content-derived), and a malformed corpus + single-byte-corruption
  SWEEP (no crash; ASan is the proof). ⛔⛔ **The fuzz caught 2 real OOM crashes on day one** in code that had passed 4
  slices of gates — a huge textual def-id and a corrupt binary count; BOTH loaders hardened (text bound by input
  length; binary counts bounded by chunk length / `kMaxDecodeCount`). `tests/ceir` **46/46**. Gated across the 5-config
  contract.
- **CEIR-1z ✅ CLOSED (2026-08-08) — BAND-1 GATE.** A typed hello-world (func + const + call + return) round-trips
  **text ⇄ binary ⇄ builder byte-identically** and its callee symbol resolves after all three forms
  (`tests/ceir/test_hello.cpp`). `tests/ceir` **49/49**. Gated across the 5-config contract. ⭐⭐ **BAND 1 CLOSED
  (1a..1z).** Already committed this session: 1a core (`5f81ce8`) + the 7 pre-existing fixes & 1a docs (`6e6f183 "CEIR-1a
  finished"`). ⛔ NOW: (A) the USER commits+pushes the remaining **CEIR-1b..1z** batch (~38 files: `engine/ceir/**` +
  `tests/ceir/**` + `scripts/check_ceir_invariants.{ps1,sh}` + docs) — ONE commit
  `feat(ceir): band 1 core IR substrate (CEIR-1b..1z)` with the per-slice breakdown in the body (slices overlap in
  files → not per-slice stageable). (B) then make **GitHub CI GREEN** (whole-repo net; fix reds gold-standard, user
  commits fix batches). (C) MEMORY.md compaction to <17.1KB during the wait. Only after CI green → **CEIR-2**.
- **RAH (parallel track) — front = RAH-1a.2.** ✅ RAH-1a.1 (visbuffer fold) DONE + gated (REN-38-F6, 97 asserts, both
  backends). **NEXT = RAH-1a.2 (DELETE, user-chosen):** retire `IGBufferTarget`+`draw_gbuffer`+`create_gbuffer_target`
  (both backends) + `RenderingDesc.gbuffer`; migrate ~8 test sites to the `color`-span MRT path; needs a
  plain-vertex-MRT-color-span path + regular-target readback first. Then RAH-1a-close → RAH-2 (unblocks CEIR-11/B).
- **PAUSED (parked, not dropped):** B/I2D+SPR (ADR-0107 review pending) · C/CGP selector + HGP/MLR · D/MED codecs
  (animated GIF→TIFF→JPEG; real-GIF external-oracle corpus owed) · main roadmap (hesap v18, eylem v1c+). Resume paths:
  the CEIR tracker's "Paused" table.

## Recently landed

- **2026-08-08** — **CEIR-1a CLOSED** (4-config per-slice sweep PASS). The global close peeled 7 pre-existing
  cross-band blockers (RAF/REN/CKIR left them: shipping-LTCG/asan-complete/tidy had never run to completion) — all
  fixed gold-standard, incl. two real engine bugs (DX12+Vulkan RT pipeline caches keyed by pointer/handle →
  fnv1a_64 content hash; DX12 anyhit flake 200/200 after), the RAF-10 catch_discover_tests ENVIRONMENT split, the
  AS-4 ASan timing guard, the C4743 stale-obj wipe, and 37 clang-tidy errors across 12 files. Uncommitted (19 files;
  user commits — proposed message in the log). Log: `docs/sessions/2026-08-08-ceir-1a-and-preexisting-fixes.md`.
- **2026-08-07 (later)** — repository-wide **documentation hygiene pass** (uncommitted): context.md → dashboard
  (history archived), ROADMAP/systems/debt/AGENTS/READMEs refreshed to honest state, retired-module overviews
  DELETED (user direction; git history keeps them), research outcome stamps, ADR index + link fixes. Full report:
  `docs/sessions/2026-08-07-doc-hygiene-pass.md`.
- **2026-08-07/08** — **CEIR pivot + CEIR-0 design phase COMPLETE:** CEIR becomes the master spine; the live tracker
  `docs/detours/D-007-ceir-tracker.md` created; CEIR-0a inventory + ADRs 0108/0109/0110 + the 0e/0g/0h/0z design notes
  all accepted; D-007 restructured (CEIR-0f). (uncommitted at time of writing — user commits.)
- **2026-08-07** — post-RAF 4-track kickoff: RAH-1a.1 + CGP-0/CUDA + MED-1 (`c116e98`); D-007 §POST-RAF + §UI/2D
  programmes + four-track tracker (`e3f8e5e`). Log: `docs/sessions/2026-08-07-post-raf-tracks-rah1-cuda.md`.
- **2026-08-06** — **RAF band COMPLETE** (`af3e04c`): `FramePassKind` retired, ADR-0106 closed.
  Log: `docs/sessions/2026-08-06-raf12-3-retire-framepasskind.md`.
- **2026-08-03…05** — RAF-0…12: substrate → one-submission frame graph → executors → engine-default assets →
  app-custom renderer → hot reload → legacy deletion. Logs: `docs/sessions/2026-08-0{3,4,5}-raf*.md`.

## Open questions / risks

- **Per-slice gate — RATIFIED (2026-08-08):** each slice closes on **2 Windows + 2 Linux configs + tidy**, all
  clean (win-debug + win-asan + linux-debug + linux-asan + tidy; Linux via WSL), **scoped to the changed module**
  (crd-ceir has zero downstream, grep-proven → crd-ceir-tests across those configs is complete). **No whole-repo
  suite per slice** (too slow). **GitHub CI is the whole-repo safety net and must stay GREEN.** See
  `project_ceir_autonomous_loop_grant`. ⛔ **Between CEIR-1 and CEIR-2: fix CI green** (it has real build/test reds).
  ⛔ **At CEIR-14: expand its subslices explicitly in the tracker.** GOAL = all bands 1→32 closed.
- **Pending user review:** RAH-0 audit (`docs/systems/rah-0-canonical-model-audit.md`) + ADR-0107
  (`docs/decisions/0107-ui-2d-architecture.md`). Track B code is blocked on the ADR-0107 review.
- `MEMORY.md` ≈ 19.9 KB (hard read limit 24.4 KB) — deeper cull deferred, entries must be MERGED/DROPPED not just
  hook-trimmed.
- The integrated CUDA fork worktree `.claude/worktrees/agent-af34b487c5544c8fa` can be removed.

## Gates that matter

Per-slice DoD: `scripts/per-slice-check.ps1` (+ `-IncludeRelease` for GPU/LTCG slices); cluster close =
`scripts/full-sweep.ps1` (18-config). **Run `ctest`, never the bare test binary** (guards are ctest-only). GPU slices:
`ValidationCapture` + both backends. Tidy per touched file via `scripts/tidy-files.ps1`, never accumulated.

## Active detour

**D-007 (merged with D-008 on 2026-07-11) — the GPU program system.** ACTIVE; grew out of hesap v17 (2026-07-07).
CKIR IR + gpu-context convergence + the full visual frontier + RAF (all ✅) → now **CEIR is the master spine** (2026-08-07;
the live tracker is `docs/detours/D-007-ceir-tracker.md`, the landed-history ledger is `D-007-gpu-program-system.md`).
Everything above is D-007 state. Queue rules: `docs/detours/README.md`.

## Recent milestones (one line each; details in session logs + `docs/bench/`)

- **2026-08-06 — RAF complete:** engine renderers are ordinary assets; one backend-neutral command model; executor
  registry; hot reload; legacy paths deleted (ADR-0106).
- **2026-07-21…08-03 — REN-36…41:** authored frame graphs/techniques/materials (`.crdm/.crdt/.crdv/.crdl/.frame.toml`),
  bindless+multi-draw (38-G1 119 fps), indexed-pull reuse, O(chunks) extract, soft shadows (PCSS/EVSM/MSM), velocity +
  TAA, Nanite-class cluster LOD start.
- **2026-07-13…16 — the GPU compute crush campaigns:** 2D FFT 1.16–1.20× cuFFT bit-exact; reduction beats CUB; radix
  sort 0.73× CUB (bit-exact, 8.4× session gain); NRC fused MLP 2.37× cuBLAS; B14 SVGF/DDGI/ReSTIR/NRC + B15
  atmosphere/clouds + B16 FFT ocean — all gold-standard CKIR. (Narrative: the context-history archive; boards:
  `docs/bench/`.)
- **2026-07-10…12 — D-007 device+IR convergence:** one `VkDevice`, I1/I2 leak gates closed, oracle rounds per-op, CUDA
  fan-out bit-exact.
- **2026-07-23 — RET band: crd-rhi/rhi-vulkan/renderer/shader DELETED** (ADR-0105); gpu-context IS the graphics layer.
- **2026-07-02 — hesap v13 close:** interpolation/quadrature/differentiation/motion — full peer-board crush (scipy/
  MATLAB/Boost/GSL/Ruckig).
- **Earlier (hesap v0→v12, geometry, units, scene/ECS):** see `docs/phases/` + the archive.

## Paused main-roadmap work

- **Phase 3.1.6 hesap:** paused mid-v17 (GPU compute) — v17's substrate is being built AS D-007; hesap-GPU is the
  detour's last stop. v14 tensors ✅ (2026-07-05) · v15 forward AD ✅ · v16 reverse AD ✅ (2026-07-07, ADR-0097).
  `docs/phases/phase-3.1.6-hesap.md`.
- **Phase 3.1 eylem:** ⏸ paused at v1b close (ADR-0076 §12 sequencing); resumes v1c+ after the detour + hesap.
  `docs/phases/phase-3.1-eylem.md`.

For the full doc map: `docs/README.md`. ADR index: `docs/decisions/README.md`. Open debt: `docs/debt.md`.
