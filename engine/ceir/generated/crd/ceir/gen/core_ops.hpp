#pragma once

// GENERATED by ceir_opgen.py from engine/ceir/ops/core.ceirop.toml - DO NOT EDIT (schema v1). Regenerate with
// `python tools/ceir_opgen/ceir_opgen.py`; the crd-ceir-opgen-drift ctest fails on drift.
//
// core - Structured control-flow region ops: scope / if / for / foreach + the yield terminator (sec 13/14).

#include <crd/ceir/attr.hpp>
#include <crd/ceir/context.hpp>
#include <crd/ceir/dialect.hpp>
#include <crd/ceir/ir.hpp>
#include <crd/ceir/op_schema.hpp>
#include <crd/containers/span.hpp>

namespace crd::ceir::core
{
// -- op-kind identities (interned lazily against `ctx`, idempotent) --
[[nodiscard]] inline OpId delay_kind(Context& ctx) { return ctx.intern_op("core", "delay"); }
[[nodiscard]] inline OpId for_kind(Context& ctx) { return ctx.intern_op("core", "for"); }
[[nodiscard]] inline OpId foreach_kind(Context& ctx) { return ctx.intern_op("core", "foreach"); }
[[nodiscard]] inline OpId history_kind(Context& ctx) { return ctx.intern_op("core", "history"); }
[[nodiscard]] inline OpId if_kind(Context& ctx) { return ctx.intern_op("core", "if"); }
[[nodiscard]] inline OpId match_kind(Context& ctx) { return ctx.intern_op("core", "match"); }
[[nodiscard]] inline OpId scope_kind(Context& ctx) { return ctx.intern_op("core", "scope"); }
[[nodiscard]] inline OpId state_kind(Context& ctx) { return ctx.intern_op("core", "state"); }
[[nodiscard]] inline OpId switch_kind(Context& ctx) { return ctx.intern_op("core", "switch"); }
[[nodiscard]] inline OpId while_kind(Context& ctx) { return ctx.intern_op("core", "while"); }
[[nodiscard]] inline OpId yield_kind(Context& ctx) { return ctx.intern_op("core", "yield"); }

// -- typed op wrappers (a thin view over an Operation*; the registered verifier enforces shape) --
// core.delay - A one-step delay register (sec 20 delay<T>): `current` = `init` on the first evaluation, thereafter the `next` from the PREVIOUS evaluation.
class DelayOp
{
public:
    explicit DelayOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* init() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* next() const noexcept { return m_op->operand(1U); }
    [[nodiscard]] Value* current() const noexcept { return m_op->result(0U); }

private:
    Operation* m_op;
};
// core.for - A counted loop: run the body for each index in [lower, upper) stepping by step.
class ForOp
{
public:
    explicit ForOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* lower() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* upper() const noexcept { return m_op->operand(1U); }
    [[nodiscard]] Value* step() const noexcept { return m_op->operand(2U); }

private:
    Operation* m_op;
};
// core.foreach - Iterate a collection / view / work-set: run the body once per element.
class ForeachOp
{
public:
    explicit ForeachOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* items() const noexcept { return m_op->operand(0U); }

private:
    Operation* m_op;
};
// core.history - A depth-N history buffer (sec 20 history<T>): a delay line keeping the previous `depth` values (default 1).
class HistoryOp
{
public:
    explicit HistoryOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* init() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* next() const noexcept { return m_op->operand(1U); }
    [[nodiscard]] Value* current() const noexcept { return m_op->result(0U); }

private:
    Operation* m_op;
};
// core.if - Run the THEN region when the condition is nonzero, else the ELSE region; both yield the (variadic) results.
class IfOp
{
public:
    explicit IfOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* cond() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* results() const noexcept { return m_op->result(0U); }

private:
    Operation* m_op;
};
// core.match - Structural pattern match: run the arm region selected by matching the scrutinee.
class MatchOp
{
public:
    explicit MatchOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* scrutinee() const noexcept { return m_op->operand(0U); }

private:
    Operation* m_op;
};
// core.scope - A structured scope: unconditionally run its region; its yield forwards to the op's (variadic) results.
class ScopeOp
{
public:
    explicit ScopeOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* results() const noexcept { return m_op->result(0U); }

private:
    Operation* m_op;
};
// core.state - An explicit persistent state cell (sec 20 state<T>): `current` = `init` on the first evaluation of the enclosing region instance, thereafter the previous `next`.
class StateOp
{
public:
    explicit StateOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* init() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* next() const noexcept { return m_op->operand(1U); }
    [[nodiscard]] Value* current() const noexcept { return m_op->result(0U); }

private:
    Operation* m_op;
};
// core.switch - Multi-way branch: run the case region selected by the integer selector.
class SwitchOp
{
public:
    explicit SwitchOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* selector() const noexcept { return m_op->operand(0U); }

private:
    Operation* m_op;
};
// core.while - A while loop: repeat the body region while the condition region yields true.
class WhileOp
{
public:
    explicit WhileOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }

private:
    Operation* m_op;
};
// core.yield - Terminate a structured region's block (the region terminator).
class YieldOp
{
public:
    explicit YieldOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* values() const noexcept { return m_op->operand(0U); }

private:
    Operation* m_op;
};

// -- builders (through the ordinary Context factories - no privileged construction). NOTE: a builder
// produces the MINIMUM arity on every variadic axis (operands / results / regions); build the full arity
// (extra variadic operands, N result values, N case regions) directly with `Context::create_operation`. --
[[nodiscard]] Operation* build_delay(Context& ctx, Value* init, Value* next, TypeId result_type = {});
[[nodiscard]] Operation* build_for(Context& ctx, Value* lower, Value* upper, Value* step, TypeId result_type = {});
[[nodiscard]] Operation* build_foreach(Context& ctx, Value* items, TypeId result_type = {});
[[nodiscard]] Operation* build_history(Context& ctx, Value* init, Value* next, TypeId result_type = {});
[[nodiscard]] Operation* build_if(Context& ctx, Value* cond, TypeId result_type = {});
[[nodiscard]] Operation* build_match(Context& ctx, Value* scrutinee, TypeId result_type = {});
[[nodiscard]] Operation* build_scope(Context& ctx, TypeId result_type = {});
[[nodiscard]] Operation* build_state(Context& ctx, Value* init, Value* next, TypeId result_type = {});
[[nodiscard]] Operation* build_switch(Context& ctx, Value* selector, TypeId result_type = {});
[[nodiscard]] Operation* build_while(Context& ctx, TypeId result_type = {});
[[nodiscard]] Operation* build_yield(Context& ctx, Value* values, TypeId result_type = {});

// -- registration: self-registers the dialect + every op (traits + verifier), NO central edit (section 7) --
Dialect* register_core_ops(Context& ctx);

// -- reflection: the op schema as queryable data - the SAME truth the wrapper/builder/verifier emit
// from (no parallel truth); the CR-D007 node editor + the committed core.ops.json read it (section 122/161) --
[[nodiscard]] containers::ConstSpan<OpSchema> core_op_schemas() noexcept;
} // namespace crd::ceir::core
