#pragma once

// GENERATED by ceir_opgen.py from engine/ceir/ops/quant.ceirop.toml - DO NOT EDIT (schema v1). Regenerate with
// `python tools/ceir_opgen/ceir_opgen.py`; the crd-ceir-opgen-drift ctest fails on drift.
//
// quant - CEIR-23a sec-54 ceir.quant: QUANTIZATION SEMANTICS as first-class IR — quantize (float -> integer-storage) + dequantize (integer-storage -> float), so a provider may fuse dequant into loads / emit a native Q8 program (sec-54 'quantization/dequantization may be fused into loads/provider programs'; important for ML + neural rendering). ⛔ NO new element TypeKind (the split-complex precedent: a quantized element kind carries the 21c identity-ripple cost) — the STORAGE type RIDES the result type (quantize's result is an INTEGER-element tensor via type_int(width,signed); the 21b target-rides-result doctrine) and the EXPRESSED type rides dequantize's float result. ⛔ scale + zero_point are OPERANDS, not attrs (ATTR_KINDS has no array kind — the transpose-perm precedent): a PER-TENSOR scale is a RANK-0 tensor, PER-AXIS is RANK-1, ONE op form covers both (the design-B rank-0 binding precedent). ⛔ EVERY op is Pure + typed NoSemantics + carries NO kernel_ref / NO lowering hook (sec-70; the ckir/native-Q8-provider mapping lands at 23b, NOT here) — declare stays ELEMENT-AGNOSTIC (Q8/INT8/4/2/FP8-specific restriction rides the 23b provider as typed SynthRejects, the 22a/22b split). ⛔ NO-FOLLOW / NAME-FORWARD (declared != implemented; a later band with a real consumer, never a silent subset): packed low-bit formats (INT4/2 sub-byte packing) / FP8 element / calibration metadata (sec-54) -> named-forward (no consumer this band, the 12a rule); block-quantized dtypes -> the sparse/provider rows. ⛔ I6 — find_quant_misuse switches on op NAME + TypeKind, never op.kind. crd-ceir NEVER links gpu-context (I3/I4); ZERO renderer changes.

#include <crd/ceir/attr.hpp>
#include <crd/ceir/context.hpp>
#include <crd/ceir/dialect.hpp>
#include <crd/ceir/ir.hpp>
#include <crd/ceir/op_schema.hpp>
#include <crd/containers/span.hpp>

namespace crd::ceir::quant
{
// -- op-kind identities (interned lazily against `ctx`, idempotent) --
[[nodiscard]] inline OpId dequantize_kind(Context& ctx) { return ctx.intern_op("quant", "dequantize"); }
[[nodiscard]] inline OpId quantize_kind(Context& ctx) { return ctx.intern_op("quant", "quantize"); }

// -- typed op wrappers (a thin view over an Operation*; the registered verifier enforces shape) --
// quant.dequantize - Integer-storage -> float dequantization: output = (input - zero_point) * scale (per-tensor rank-0 or per-axis rank-1 scale/zero_point).
class DequantizeOp
{
public:
    explicit DequantizeOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* input() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* scale() const noexcept { return m_op->operand(1U); }
    [[nodiscard]] Value* zero_point() const noexcept { return m_op->operand(2U); }
    [[nodiscard]] Value* output() const noexcept { return m_op->result(0U); }
    [[nodiscard]] AttrId axis() const noexcept { return m_op->attr("axis"); }
    [[nodiscard]] AttrId scheme() const noexcept { return m_op->attr("scheme"); }

private:
    Operation* m_op;
};
// quant.quantize - Float -> integer-storage quantization: output = round(input/scale) + zero_point (per-tensor rank-0 or per-axis rank-1 scale/zero_point).
class QuantizeOp
{
public:
    explicit QuantizeOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* input() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* scale() const noexcept { return m_op->operand(1U); }
    [[nodiscard]] Value* zero_point() const noexcept { return m_op->operand(2U); }
    [[nodiscard]] Value* output() const noexcept { return m_op->result(0U); }
    [[nodiscard]] AttrId axis() const noexcept { return m_op->attr("axis"); }
    [[nodiscard]] AttrId scheme() const noexcept { return m_op->attr("scheme"); }

private:
    Operation* m_op;
};

// -- builders (through the ordinary Context factories - no privileged construction). NOTE: a builder
// produces the MINIMUM arity on every variadic axis (operands / results / regions); build the full arity
// (extra variadic operands, N result values, N case regions) directly with `Context::create_operation`. --
[[nodiscard]] Operation* build_dequantize(Context& ctx, Value* input, Value* scale, Value* zero_point, AttrId axis, AttrId scheme, TypeId result_type = {});
[[nodiscard]] Operation* build_quantize(Context& ctx, Value* input, Value* scale, Value* zero_point, AttrId axis, AttrId scheme, TypeId result_type = {});

// -- registration: self-registers the dialect + every op (traits + verifier), NO central edit (section 7) --
Dialect* register_quant_ops(Context& ctx);

// -- reflection: the op schema as queryable data - the SAME truth the wrapper/builder/verifier emit
// from (no parallel truth); the CR-D007 node editor + the committed quant.ops.json read it (section 122/161) --
[[nodiscard]] containers::ConstSpan<OpSchema> quant_op_schemas() noexcept;
} // namespace crd::ceir::quant
