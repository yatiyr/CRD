#pragma once

// GENERATED by ceir_opgen.py from engine/ceir/ops/shape.ceirop.toml - DO NOT EDIT (schema v1). Regenerate with
// `python tools/ceir_opgen/ceir_opgen.py`; the crd-ceir-opgen-drift ctest fails on drift.
//
// shape - CEIR-21 sec-35 ceir.shape: OWN symbolic + dynamic SHAPE reasoning as authored CEIR — the value-op dialect over the CEIR-3d shape TYPES (Dim/Shape, already declared TypeKinds; §21/§35). A high-level shape vocabulary a tensor program (ceir.tensor, 21b) + a provider partitioner CITE, so shape rules live in the IR — NEVER 'encoded only as ad-hoc runtime checks inside kernels' (§21). ⛔ DECLARE-only (21a): types already exist (3d), this slice adds the OPS + the module-walk verifier Context::find_shape_misuse + tests, ZERO renderer changes, crd-ceir NEVER links gpu-context (I3/I4). ⛔ ALL PURE + typed NoSemantics (value constructors/queries over shapes; no memory/device effect) — the 12a resource.declare precedent, NOT the 20a native-intrinsic tier. ⛔ NO kernel_ref, NO lowering hook on any op (§70: a NATIVE graph backend must ingest the tensor+shape graph WHOLE — never flatten a shape query to a dispatch). ⛔ CURRENCY: the ops WRAP the 3d tri-state predicates (`shapes_broadcast`→BroadcastResult, `shapes_reshape`→ShapeCompat); the VERIFIER contract is Incompatible → a POINTING misuse (the 3z BroadcastResult.position), Unknown → ACCEPT (the principled deferral 3d named — a `shape.assert` DISCHARGES it at runtime), Compatible → accept. ⛔ I6 — find_shape_misuse switches on TypeId/TypeKind, NEVER op.kind. ⛔ NO-FOLLOW / DEFERRED (declared != implemented — a later slice with a real consumer, never a silent subset): symbolic-dim EXPRESSIONS (add/mul/floordiv over Symbolic dims — §35 'symbolic expressions'; the arith-over-Dim algebra) land when a specializing consumer needs them; specialization-from-observed-shapes (§21) is a provider/JIT concern (CEIR-24+); layout constraints (§22) are CEIR-21c; the sparse-format encoding is CEIR-23 (struck from 3d @ band-open). ⛔ `shape.assert` is a runtime GUARD — for THIS declare-only slice it carries typed NoSemantics + is modeled Pure (no effect family fits a control guard; §26 stops at Debug); the CANONICALIZER-must-not-fold-it-to-identity contract is NAMED-FORWARD to CEIR-6 canonicalization (no DCE/fold pass targets ceir.shape today, so the concern is theoretical until a consumer runs one).

#include <crd/ceir/attr.hpp>
#include <crd/ceir/context.hpp>
#include <crd/ceir/dialect.hpp>
#include <crd/ceir/ir.hpp>
#include <crd/ceir/op_schema.hpp>
#include <crd/containers/span.hpp>

namespace crd::ceir::shape
{
// -- op-kind identities (interned lazily against `ctx`, idempotent) --
[[nodiscard]] inline OpId assert_kind(Context& ctx) { return ctx.intern_op("shape", "assert"); }
[[nodiscard]] inline OpId broadcast_kind(Context& ctx) { return ctx.intern_op("shape", "broadcast"); }
[[nodiscard]] inline OpId extent_kind(Context& ctx) { return ctx.intern_op("shape", "extent"); }
[[nodiscard]] inline OpId make_kind(Context& ctx) { return ctx.intern_op("shape", "make"); }
[[nodiscard]] inline OpId rank_kind(Context& ctx) { return ctx.intern_op("shape", "rank"); }
[[nodiscard]] inline OpId reshape_kind(Context& ctx) { return ctx.intern_op("shape", "reshape"); }

// -- typed op wrappers (a thin view over an Operation*; the registered verifier enforces shape) --
// shape.assert - A runtime shape-relation check (the Unknown discharge): assert `relation` between two Shapes, pass %lhs through.
class AssertOp
{
public:
    explicit AssertOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* lhs() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* rhs() const noexcept { return m_op->operand(1U); }
    [[nodiscard]] Value* shape() const noexcept { return m_op->result(0U); }
    [[nodiscard]] AttrId relation() const noexcept { return m_op->attr("relation"); }

private:
    Operation* m_op;
};
// shape.broadcast - The numpy-broadcast RESULT shape of two Shapes (wraps the 3d shapes_broadcast predicate; Unknown defers to shape.assert).
class BroadcastOp
{
public:
    explicit BroadcastOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* lhs() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* rhs() const noexcept { return m_op->operand(1U); }
    [[nodiscard]] Value* shape() const noexcept { return m_op->result(0U); }

private:
    Operation* m_op;
};
// shape.extent - The extent Dim at `axis` of a Shape (axis bounds-checked against a statically-known rank).
class ExtentOp
{
public:
    explicit ExtentOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* shape() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* dim() const noexcept { return m_op->result(0U); }
    [[nodiscard]] AttrId axis() const noexcept { return m_op->attr("axis"); }

private:
    Operation* m_op;
};
// shape.make - Construct a Shape from ordered Dim operands (0 dims = the rank-0 !shape<> scalar-shape); a Pure value constructor.
class MakeOp
{
public:
    explicit MakeOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* dims() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* shape() const noexcept { return m_op->result(0U); }

private:
    Operation* m_op;
};
// shape.rank - The RANK (dimension count) of a Shape, as an Index.
class RankOp
{
public:
    explicit RankOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* shape() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* rank() const noexcept { return m_op->result(0U); }

private:
    Operation* m_op;
};
// shape.reshape - Validate a reshape from a source Shape to a target Shape (wraps the 3d shapes_reshape predicate); result = the validated target.
class ReshapeOp
{
public:
    explicit ReshapeOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* src() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* target() const noexcept { return m_op->operand(1U); }
    [[nodiscard]] Value* shape() const noexcept { return m_op->result(0U); }

private:
    Operation* m_op;
};

// -- builders (through the ordinary Context factories - no privileged construction). NOTE: a builder
// produces the MINIMUM arity on every variadic axis (operands / results / regions); build the full arity
// (extra variadic operands, N result values, N case regions) directly with `Context::create_operation`. --
[[nodiscard]] Operation* build_assert(Context& ctx, Value* lhs, Value* rhs, AttrId relation, TypeId result_type = {});
[[nodiscard]] Operation* build_broadcast(Context& ctx, Value* lhs, Value* rhs, TypeId result_type = {});
[[nodiscard]] Operation* build_extent(Context& ctx, Value* shape, AttrId axis, TypeId result_type = {});
[[nodiscard]] Operation* build_make(Context& ctx, Value* dims, TypeId result_type = {});
[[nodiscard]] Operation* build_rank(Context& ctx, Value* shape, TypeId result_type = {});
[[nodiscard]] Operation* build_reshape(Context& ctx, Value* src, Value* target, TypeId result_type = {});

// -- registration: self-registers the dialect + every op (traits + verifier), NO central edit (section 7) --
Dialect* register_shape_ops(Context& ctx);

// -- reflection: the op schema as queryable data - the SAME truth the wrapper/builder/verifier emit
// from (no parallel truth); the CR-D007 node editor + the committed shape.ops.json read it (section 122/161) --
[[nodiscard]] containers::ConstSpan<OpSchema> shape_op_schemas() noexcept;
} // namespace crd::ceir::shape
