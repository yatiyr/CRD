#pragma once

// GENERATED by ceir_opgen.py from engine/ceir/ops/arith.ceirop.toml - DO NOT EDIT (schema v1). Regenerate with
// `python tools/ceir_opgen/ceir_opgen.py`; the crd-ceir-opgen-drift ctest fails on drift.
//
// arith - Integer/float constants and arithmetic — the canonical value-domain dialect.

#include <crd/ceir/attr.hpp>
#include <crd/ceir/context.hpp>
#include <crd/ceir/dialect.hpp>
#include <crd/ceir/ir.hpp>
#include <crd/ceir/op_schema.hpp>
#include <crd/containers/span.hpp>

namespace crd::ceir::arith
{
// -- op-kind identities (interned lazily against `ctx`, idempotent) --
[[nodiscard]] inline OpId addi_kind(Context& ctx) { return ctx.intern_op("arith", "addi"); }
[[nodiscard]] inline OpId cmpi_kind(Context& ctx) { return ctx.intern_op("arith", "cmpi"); }
[[nodiscard]] inline OpId const_kind(Context& ctx) { return ctx.intern_op("arith", "const"); }
[[nodiscard]] inline OpId muli_kind(Context& ctx) { return ctx.intern_op("arith", "muli"); }

// -- typed op wrappers (a thin view over an Operation*; the registered verifier enforces shape) --
// arith.addi - Two's-complement integer addition.
class AddiOp
{
public:
    explicit AddiOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* lhs() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* rhs() const noexcept { return m_op->operand(1U); }
    [[nodiscard]] Value* result() const noexcept { return m_op->result(0U); }

private:
    Operation* m_op;
};
// arith.cmpi - Integer comparison producing a boolean-valued result.
class CmpiOp
{
public:
    explicit CmpiOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* lhs() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* rhs() const noexcept { return m_op->operand(1U); }
    [[nodiscard]] Value* result() const noexcept { return m_op->result(0U); }
    [[nodiscard]] AttrId predicate() const noexcept { return m_op->attr("predicate"); }

private:
    Operation* m_op;
};
// arith.const - A typed compile-time constant materialized into an SSA value.
class ConstOp
{
public:
    explicit ConstOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* result() const noexcept { return m_op->result(0U); }
    [[nodiscard]] AttrId value() const noexcept { return m_op->attr("value"); }

private:
    Operation* m_op;
};
// arith.muli - Two's-complement integer multiplication.
class MuliOp
{
public:
    explicit MuliOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* lhs() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* rhs() const noexcept { return m_op->operand(1U); }
    [[nodiscard]] Value* result() const noexcept { return m_op->result(0U); }

private:
    Operation* m_op;
};

// -- builders (through the ordinary Context factories - no privileged construction). NOTE: a builder
// produces the MINIMUM arity on every variadic axis (operands / results / regions); build the full arity
// (extra variadic operands, N result values, N case regions) directly with `Context::create_operation`. --
[[nodiscard]] Operation* build_addi(Context& ctx, Value* lhs, Value* rhs, TypeId result_type = {});
[[nodiscard]] Operation* build_cmpi(Context& ctx, Value* lhs, Value* rhs, AttrId predicate, TypeId result_type = {});
[[nodiscard]] Operation* build_const(Context& ctx, AttrId value, TypeId result_type = {});
[[nodiscard]] Operation* build_muli(Context& ctx, Value* lhs, Value* rhs, TypeId result_type = {});

// -- registration: self-registers the dialect + every op (traits + verifier), NO central edit (section 7) --
Dialect* register_arith_ops(Context& ctx);

// -- reflection: the op schema as queryable data - the SAME truth the wrapper/builder/verifier emit
// from (no parallel truth); the CR-D007 node editor + the committed arith.ops.json read it (section 122/161) --
[[nodiscard]] containers::ConstSpan<OpSchema> arith_op_schemas() noexcept;
} // namespace crd::ceir::arith
