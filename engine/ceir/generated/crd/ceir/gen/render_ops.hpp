#pragma once

// GENERATED by ceir_opgen.py from engine/ceir/ops/render.ceirop.toml - DO NOT EDIT (schema v1). Regenerate with
// `python tools/ceir_opgen/ceir_opgen.py`; the crd-ceir-opgen-drift ctest fails on drift.
//
// render - CEIR-14 sec-40/41 ceir.render: backend-neutral rendering orchestration on RAH's hardened model. A render.scope REGION op (the structured begin/end -- the draws live in its graph region; the crd-ceir-gpu BRIDGE lowers it to the canonical RenderingDesc begin/end + draw verbs the frame graph records today, CEIR-14b; crd-ceir core NEVER links gpu-context -- I3/I4). ATTACHMENTS ARE GENERAL (sec-41): render.color_attachment / render.depth_attachment DECLARE ops each take an image resource + carry per-attachment config (load/store/typed-clear/blend/read-only) and PRODUCE a value of a render-dialect ATTACHMENT TYPE (the 8a Extern type-class machinery; the ROLE -- color vs depth -- rides the TYPE, NOT a string attr, per the 12a one-source-of-truth doctrine). render.scope consumes N of them through its single variadic operand tail. ⛔ the attachment TARGETS are OPERANDS (not attrs) so the 13d per-resource barrier machinery + resource_root normalization SEE them (attribute-encoded targets are invisible to hazard derivation -- the RTT/WAR scar). G-buffer / visibility-buffer / object-ID / primitive-ID / velocity / selection / material-ID are PROGRAM-DEFINED output contracts over general typed attachments -- NOT special ops or canonical fields (sec-41, the RAH-1a.1 lesson lifted to the IR). ⛔ no material/shadow/deferred combinations in op NAMES (sec-40) -- combinations stay typed data/program composition. Effects: render.scope is a CONSERVATIVE baseline (GPUCommand + ambient MemoryReadWrite -- a scope may touch any bound attachment/resource, so it MUST hazard against exports/transfers; 14b narrows to the attachment writes + resource reads in lowering); the attachment-declare ops are EFFECT-FREE metadata (the resource.declare shape). Well-formedness rides the module-walk verifier Context::find_render_misuse (the find_dispatch_misuse house pattern, CEIR-14a): attachment operand is image/view-kinded; render.scope operands are all attachment-typed; at most one depth attachment; draw-family ops (CEIR-14b) are legal ONLY inside a render.scope region; and -- the RAH-1a.1 lesson made STATIC -- a Uint typed-clear only on a uint-format attachment. ⛔ NO-FOLLOW (named, never silently subset): the DRAW FAMILY (draw / indexed / indirect / indirect-count / mesh-dispatch / indirect-mesh / patch-tess) + dynamic state (viewport / scissor / shading-rate) -> CEIR-14b; indirect + indirect-count + mesh/task dispatch (the DrawIndex scar) -> CEIR-14c; RAH-2 resident resource-table binding semantics -> CEIR-14d; multiview / view-selection + fragment-density/foveation + the shading-rate ATTACHMENT -> CEIR-14b dynamic state; mip/layer/aspect attachment VIEWS ride the existing resource.view op (sec-41's typed views); DYNAMIC render area (a runtime viewport) -> CEIR-14b (width/height/sample_count are compile-time scope config here, the RenderingDesc-at-record model).

#include <crd/ceir/attr.hpp>
#include <crd/ceir/context.hpp>
#include <crd/ceir/dialect.hpp>
#include <crd/ceir/ir.hpp>
#include <crd/ceir/op_schema.hpp>
#include <crd/containers/span.hpp>

namespace crd::ceir::render
{
// -- op-kind identities (interned lazily against `ctx`, idempotent) --
[[nodiscard]] inline OpId color_attachment_kind(Context& ctx) { return ctx.intern_op("render", "color_attachment"); }
[[nodiscard]] inline OpId depth_attachment_kind(Context& ctx) { return ctx.intern_op("render", "depth_attachment"); }
[[nodiscard]] inline OpId draw_kind(Context& ctx) { return ctx.intern_op("render", "draw"); }
[[nodiscard]] inline OpId draw_indexed_kind(Context& ctx) { return ctx.intern_op("render", "draw_indexed"); }
[[nodiscard]] inline OpId draw_indirect_kind(Context& ctx) { return ctx.intern_op("render", "draw_indirect"); }
[[nodiscard]] inline OpId draw_indirect_count_kind(Context& ctx) { return ctx.intern_op("render", "draw_indirect_count"); }
[[nodiscard]] inline OpId mesh_dispatch_kind(Context& ctx) { return ctx.intern_op("render", "mesh_dispatch"); }
[[nodiscard]] inline OpId mesh_dispatch_indirect_kind(Context& ctx) { return ctx.intern_op("render", "mesh_dispatch_indirect"); }
[[nodiscard]] inline OpId mesh_dispatch_list_kind(Context& ctx) { return ctx.intern_op("render", "mesh_dispatch_list"); }
[[nodiscard]] inline OpId scene_draw_list_kind(Context& ctx) { return ctx.intern_op("render", "scene_draw_list"); }
[[nodiscard]] inline OpId scope_kind(Context& ctx) { return ctx.intern_op("render", "scope"); }

// -- typed op wrappers (a thin view over an Operation*; the registered verifier enforces shape) --
// render.color_attachment - Declare a color attachment over an image (load/store/typed-clear/blend); produces a color-attachment-typed value.
class ColorAttachmentOp
{
public:
    explicit ColorAttachmentOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* image() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* attachment() const noexcept { return m_op->result(0U); }
    [[nodiscard]] AttrId load() const noexcept { return m_op->attr("load"); }
    [[nodiscard]] AttrId store() const noexcept { return m_op->attr("store"); }
    [[nodiscard]] AttrId clear_kind() const noexcept { return m_op->attr("clear_kind"); }
    [[nodiscard]] AttrId clear_r() const noexcept { return m_op->attr("clear_r"); }
    [[nodiscard]] AttrId clear_g() const noexcept { return m_op->attr("clear_g"); }
    [[nodiscard]] AttrId clear_b() const noexcept { return m_op->attr("clear_b"); }
    [[nodiscard]] AttrId clear_a() const noexcept { return m_op->attr("clear_a"); }
    [[nodiscard]] AttrId clear_uint() const noexcept { return m_op->attr("clear_uint"); }
    [[nodiscard]] AttrId blend() const noexcept { return m_op->attr("blend"); }

private:
    Operation* m_op;
};
// render.depth_attachment - Declare the depth/stencil attachment over an image (load/store/clear/compare/read-only); produces a depth-attachment-typed value.
class DepthAttachmentOp
{
public:
    explicit DepthAttachmentOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* image() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* attachment() const noexcept { return m_op->result(0U); }
    [[nodiscard]] AttrId load() const noexcept { return m_op->attr("load"); }
    [[nodiscard]] AttrId store() const noexcept { return m_op->attr("store"); }
    [[nodiscard]] AttrId clear_depth() const noexcept { return m_op->attr("clear_depth"); }
    [[nodiscard]] AttrId compare() const noexcept { return m_op->attr("compare"); }
    [[nodiscard]] AttrId read_only() const noexcept { return m_op->attr("read_only"); }

private:
    Operation* m_op;
};
// render.draw - A non-indexed draw: run @program over vertex_count x instance_count with the bound resources (inside a render.scope).
class DrawOp
{
public:
    explicit DrawOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* vertex_count() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* instance_count() const noexcept { return m_op->operand(1U); }
    [[nodiscard]] Value* bindings() const noexcept { return m_op->operand(2U); }
    [[nodiscard]] AttrId program() const noexcept { return m_op->attr("program"); }
    [[nodiscard]] AttrId access() const noexcept { return m_op->attr("access"); }
    [[nodiscard]] AttrId program_interface() const noexcept { return m_op->attr("program_interface"); }
    [[nodiscard]] AttrId first_vertex() const noexcept { return m_op->attr("first_vertex"); }
    [[nodiscard]] AttrId geometry() const noexcept { return m_op->attr("geometry"); }

private:
    Operation* m_op;
};
// render.draw_indexed - An indexed draw: run @program over index_count indices (bound index buffer) x instance_count with the bound resources.
class DrawIndexedOp
{
public:
    explicit DrawIndexedOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* index_count() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* instance_count() const noexcept { return m_op->operand(1U); }
    [[nodiscard]] Value* index_buffer() const noexcept { return m_op->operand(2U); }
    [[nodiscard]] Value* bindings() const noexcept { return m_op->operand(3U); }
    [[nodiscard]] AttrId program() const noexcept { return m_op->attr("program"); }
    [[nodiscard]] AttrId access() const noexcept { return m_op->attr("access"); }
    [[nodiscard]] AttrId program_interface() const noexcept { return m_op->attr("program_interface"); }
    [[nodiscard]] AttrId first_index() const noexcept { return m_op->attr("first_index"); }

private:
    Operation* m_op;
};
// render.draw_indirect - An indirect draw: @program with draw params read from an %args buffer, up to max_draws sub-draws, inside a render.scope.
class DrawIndirectOp
{
public:
    explicit DrawIndirectOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* args() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* bindings() const noexcept { return m_op->operand(1U); }
    [[nodiscard]] AttrId program() const noexcept { return m_op->attr("program"); }
    [[nodiscard]] AttrId access() const noexcept { return m_op->attr("access"); }
    [[nodiscard]] AttrId program_interface() const noexcept { return m_op->attr("program_interface"); }
    [[nodiscard]] AttrId max_draws() const noexcept { return m_op->attr("max_draws"); }
    [[nodiscard]] AttrId args_offset() const noexcept { return m_op->attr("args_offset"); }
    [[nodiscard]] AttrId index_offset() const noexcept { return m_op->attr("index_offset"); }

private:
    Operation* m_op;
};
// render.draw_indirect_count - An indirect-count draw: @program with args + a device count buffer (the GPU decides the draw count, up to max_draws).
class DrawIndirectCountOp
{
public:
    explicit DrawIndirectCountOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* args() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* count() const noexcept { return m_op->operand(1U); }
    [[nodiscard]] Value* bindings() const noexcept { return m_op->operand(2U); }
    [[nodiscard]] AttrId program() const noexcept { return m_op->attr("program"); }
    [[nodiscard]] AttrId access() const noexcept { return m_op->attr("access"); }
    [[nodiscard]] AttrId program_interface() const noexcept { return m_op->attr("program_interface"); }
    [[nodiscard]] AttrId max_draws() const noexcept { return m_op->attr("max_draws"); }
    [[nodiscard]] AttrId args_offset() const noexcept { return m_op->attr("args_offset"); }
    [[nodiscard]] AttrId count_offset() const noexcept { return m_op->attr("count_offset"); }
    [[nodiscard]] AttrId index_offset() const noexcept { return m_op->attr("index_offset"); }

private:
    Operation* m_op;
};
// render.mesh_dispatch - A mesh-shader dispatch: run the mesh @program over a (gx,gy,gz) workgroup grid with the bound resources, inside a render.scope.
class MeshDispatchOp
{
public:
    explicit MeshDispatchOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* grid_x() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* grid_y() const noexcept { return m_op->operand(1U); }
    [[nodiscard]] Value* grid_z() const noexcept { return m_op->operand(2U); }
    [[nodiscard]] Value* bindings() const noexcept { return m_op->operand(3U); }
    [[nodiscard]] AttrId program() const noexcept { return m_op->attr("program"); }
    [[nodiscard]] AttrId access() const noexcept { return m_op->attr("access"); }
    [[nodiscard]] AttrId program_interface() const noexcept { return m_op->attr("program_interface"); }

private:
    Operation* m_op;
};
// render.mesh_dispatch_indirect - An indirect mesh dispatch: the mesh @program group counts read from an %args buffer, with the bound resources.
class MeshDispatchIndirectOp
{
public:
    explicit MeshDispatchIndirectOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* args() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* bindings() const noexcept { return m_op->operand(1U); }
    [[nodiscard]] AttrId program() const noexcept { return m_op->attr("program"); }
    [[nodiscard]] AttrId access() const noexcept { return m_op->attr("access"); }
    [[nodiscard]] AttrId program_interface() const noexcept { return m_op->attr("program_interface"); }
    [[nodiscard]] AttrId args_offset() const noexcept { return m_op->attr("args_offset"); }

private:
    Operation* m_op;
};
// render.mesh_dispatch_list - A per-draw-item amplification: ONE render.scope, N draws over the host DrawList; each item's count/storage feed a mesh dispatch (primitive=meshlet) or a patch draw (primitive=patches).
class MeshDispatchListOp
{
public:
    explicit MeshDispatchListOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] AttrId program() const noexcept { return m_op->attr("program"); }
    [[nodiscard]] AttrId primitive() const noexcept { return m_op->attr("primitive"); }
    [[nodiscard]] AttrId fallback_count() const noexcept { return m_op->attr("fallback_count"); }
    [[nodiscard]] AttrId access() const noexcept { return m_op->attr("access"); }
    [[nodiscard]] AttrId program_interface() const noexcept { return m_op->attr("program_interface"); }

private:
    Operation* m_op;
};
// render.scene_draw_list - The scene per-draw-item verb ladder: ONE render.scope, N packets over the host DrawList; each item's fields (args/index_count/texture/storage) select its verb (indirect/indexed/storage-pull/coalesced-multi).
class SceneDrawListOp
{
public:
    explicit SceneDrawListOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] AttrId program() const noexcept { return m_op->attr("program"); }
    [[nodiscard]] AttrId access() const noexcept { return m_op->attr("access"); }
    [[nodiscard]] AttrId program_interface() const noexcept { return m_op->attr("program_interface"); }
    [[nodiscard]] AttrId geometry() const noexcept { return m_op->attr("geometry"); }

private:
    Operation* m_op;
};
// render.scope - A render scope: bind the attachments + render area, run the draws in the region. Lowers to RenderingDesc begin/end + draw verbs (14b).
class ScopeOp
{
public:
    explicit ScopeOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* attachments() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] AttrId width() const noexcept { return m_op->attr("width"); }
    [[nodiscard]] AttrId height() const noexcept { return m_op->attr("height"); }
    [[nodiscard]] AttrId sample_count() const noexcept { return m_op->attr("sample_count"); }
    [[nodiscard]] AttrId extent_from_target() const noexcept { return m_op->attr("extent_from_target"); }

private:
    Operation* m_op;
};

// -- builders (through the ordinary Context factories - no privileged construction). NOTE: a builder
// produces the MINIMUM arity on every variadic axis (operands / results / regions); build the full arity
// (extra variadic operands, N result values, N case regions) directly with `Context::create_operation`. --
[[nodiscard]] Operation* build_color_attachment(Context& ctx, Value* image, TypeId result_type = {});
[[nodiscard]] Operation* build_depth_attachment(Context& ctx, Value* image, TypeId result_type = {});
[[nodiscard]] Operation* build_draw(Context& ctx, Value* vertex_count, Value* instance_count, Value* bindings, AttrId program, AttrId access, TypeId result_type = {});
[[nodiscard]] Operation* build_draw_indexed(Context& ctx, Value* index_count, Value* instance_count, Value* index_buffer, Value* bindings, AttrId program, AttrId access, TypeId result_type = {});
[[nodiscard]] Operation* build_draw_indirect(Context& ctx, Value* args, Value* bindings, AttrId program, AttrId access, TypeId result_type = {});
[[nodiscard]] Operation* build_draw_indirect_count(Context& ctx, Value* args, Value* count, Value* bindings, AttrId program, AttrId access, TypeId result_type = {});
[[nodiscard]] Operation* build_mesh_dispatch(Context& ctx, Value* grid_x, Value* grid_y, Value* grid_z, Value* bindings, AttrId program, AttrId access, TypeId result_type = {});
[[nodiscard]] Operation* build_mesh_dispatch_indirect(Context& ctx, Value* args, Value* bindings, AttrId program, AttrId access, TypeId result_type = {});
[[nodiscard]] Operation* build_mesh_dispatch_list(Context& ctx, AttrId program, AttrId primitive, AttrId access, TypeId result_type = {});
[[nodiscard]] Operation* build_scene_draw_list(Context& ctx, AttrId program, AttrId access, TypeId result_type = {});
[[nodiscard]] Operation* build_scope(Context& ctx, Value* attachments, AttrId width, AttrId height, TypeId result_type = {});

// -- registration: self-registers the dialect + every op (traits + verifier), NO central edit (section 7) --
Dialect* register_render_ops(Context& ctx);

// -- reflection: the op schema as queryable data - the SAME truth the wrapper/builder/verifier emit
// from (no parallel truth); the CR-D007 node editor + the committed render.ops.json read it (section 122/161) --
[[nodiscard]] containers::ConstSpan<OpSchema> render_op_schemas() noexcept;
} // namespace crd::ceir::render
