#pragma once

// GENERATED by ceir_opgen.py from engine/ceir/ops/test.ceirop.toml - DO NOT EDIT (schema v1). Regenerate with
// `python tools/ceir_opgen/ceir_opgen.py`; the crd-ceir-opgen-drift ctest fails on drift.
//
// test - A full-surface reference dialect exercising every CEIR-2 schema field - no runtime meaning.

#include <crd/ceir/attr.hpp>
#include <crd/ceir/context.hpp>
#include <crd/ceir/dialect.hpp>
#include <crd/ceir/ir.hpp>
#include <crd/ceir/op_schema.hpp>
#include <crd/containers/span.hpp>

namespace crd::ceir::test
{
// -- op-kind identities (interned lazily against `ctx`, idempotent) --
[[nodiscard]] inline OpId dummy_kind(Context& ctx) { return ctx.intern_op("test", "dummy"); }
[[nodiscard]] inline OpId kinds_kind(Context& ctx) { return ctx.intern_op("test", "kinds"); }

// -- typed op wrappers (a thin view over an Operation*; the registered verifier enforces shape) --
// test.dummy - Shape-coverage op: fixed + variadic operands, required + optional attrs, effects, domain, native, a region.
class DummyOp
{
public:
    explicit DummyOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* lhs() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* rest() const noexcept { return m_op->operand(1U); }
    [[nodiscard]] Value* out() const noexcept { return m_op->result(0U); }
    [[nodiscard]] AttrId axis() const noexcept { return m_op->attr("axis"); }
    [[nodiscard]] AttrId mode() const noexcept { return m_op->attr("mode"); }
    [[nodiscard]] AttrId flag() const noexcept { return m_op->attr("flag"); }

private:
    Operation* m_op;
};
// test.kinds - Attribute-kind-coverage op: a required attribute of every remaining kind (float / bool / symbol / type).
class KindsOp
{
public:
    explicit KindsOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* out() const noexcept { return m_op->result(0U); }
    [[nodiscard]] AttrId a_float() const noexcept { return m_op->attr("a_float"); }
    [[nodiscard]] AttrId a_bool() const noexcept { return m_op->attr("a_bool"); }
    [[nodiscard]] AttrId a_sym() const noexcept { return m_op->attr("a_sym"); }
    [[nodiscard]] AttrId a_type() const noexcept { return m_op->attr("a_type"); }

private:
    Operation* m_op;
};

// -- builders (through the ordinary Context factories - no privileged construction). NOTE: a builder
// produces the MINIMUM arity on every variadic axis (operands / results / regions); build the full arity
// (extra variadic operands, N result values, N case regions) directly with `Context::create_operation`. --
[[nodiscard]] Operation* build_dummy(Context& ctx, Value* lhs, Value* rest, AttrId axis, AttrId mode, TypeId result_type = {});
[[nodiscard]] Operation* build_kinds(Context& ctx, AttrId a_float, AttrId a_bool, AttrId a_sym, AttrId a_type, TypeId result_type = {});

// -- registration: self-registers the dialect + every op (traits + verifier), NO central edit (section 7) --
Dialect* register_test_ops(Context& ctx);

// -- reflection: the op schema as queryable data - the SAME truth the wrapper/builder/verifier emit
// from (no parallel truth); the CR-D007 node editor + the committed test.ops.json read it (section 122/161) --
[[nodiscard]] containers::ConstSpan<OpSchema> test_op_schemas() noexcept;
} // namespace crd::ceir::test
