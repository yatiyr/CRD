#pragma once

// GENERATED by ceir_opgen.py from engine/ceir/ops/frame.ceirop.toml - DO NOT EDIT (schema v1). Regenerate with
// `python tools/ceir_opgen/ceir_opgen.py`; the crd-ceir-opgen-drift ctest fails on drift.
//
// frame - CEIR-15 sec-39 the ceir.frame dialect: the AUTHORABLE FRAME GRAPH as CEIR (REN-36's .frame.toml asset system, promoted). A frame.graph REGION contains the frame's resource.declare/import ops (graph-owned + external resources -- the frame graph's resources ARE resource dialect values, so the CEIR-12b/c/d lifetime/alias/memory planner plans them; NO parallel frame.resource op), its frame.draw_list ECS-query declares, and its frame.pass work-unit nodes. A pass' MECHANIC is a registered ExecutorTypeId named by the `executor` SYMBOL attr (never one-op-per-executor -- that recreates the retired FramePassKind enum); its read/written resources ride ONE variadic `resources` operand tail tokened by the `access` string (opgen: only the last operand may be variadic -- the 13a compute.dispatch / 14b render.draw house pattern), so CEIR's resource_root + ops_hazard machinery DERIVES the frame's barriers + transient lifetimes (the sec-159 realization: topology/lifetime/history become a CEIR analysis pass, not a hand-rolled one). HISTORY (ping-pong / TAA prev-frame) rides resource.declare's EXISTING lifetime=history + history_length (CEIR-12b); frame.history(R) is the prev-frame READ -- a distinct op whose result is its own resource_root, so a read-of-prev vs write-of-curr does NOT derive a false intra-frame RAW (the RMW scar's frame edition, fixed STRUCTURALLY). Frame-scoped SIZING (width/height/scale/samples/mips/depth_buffer/no_alias/resizable) + buffer usage (indirect_args/structured/counter) ride as OPEN attrs on the resource.declare inside a frame.graph region (the open-attr precedent is IN-dialect: declare already carries open planner tags streaming_priority/budget_class), validated by Context::find_frame_misuse WHEN the declare sits in a frame.graph region. Executor-specific pass config (shader/kernel/technique/draw_list/clear/blend/depth/sampler/render-state/role-bits) rides as OPEN attrs on frame.pass, validated at COOK against the EXECUTOR's schema (15c), NOT the op verifier -- CEIR attrs are an open set (the opgen verifier checks only required attrs). Migration: .frame.toml + FrameGraphBuilder both END at ceir.frame via a FrameGraphDesc<->ceir.frame converter pair with a round-trip-identity gate (15a-3); the old runtime consumes ceir.frame through the backward converter (incremental 15e); ADR-0106 superseded in place @ 15f; asset IDs preserved. ADR-0127. Reference/interpreter semantics = a NAMED-FORWARD typed NoSemantics (the resource/async precedent): a frame op executes when the frame runtime + provider binding land (15d/15e).

#include <crd/ceir/attr.hpp>
#include <crd/ceir/context.hpp>
#include <crd/ceir/dialect.hpp>
#include <crd/ceir/ir.hpp>
#include <crd/ceir/op_schema.hpp>
#include <crd/containers/span.hpp>

namespace crd::ceir::frame
{
// -- op-kind identities (interned lazily against `ctx`, idempotent) --
[[nodiscard]] inline OpId draw_list_kind(Context& ctx) { return ctx.intern_op("frame", "draw_list"); }
[[nodiscard]] inline OpId graph_kind(Context& ctx) { return ctx.intern_op("frame", "graph"); }
[[nodiscard]] inline OpId history_kind(Context& ctx) { return ctx.intern_op("frame", "history"); }
[[nodiscard]] inline OpId pass_kind(Context& ctx) { return ctx.intern_op("frame", "pass"); }

// -- typed op wrappers (a thin view over an Operation*; the registered verifier enforces shape) --
// frame.draw_list - An ECS-query draw list (all/any/none component filters + cull/sort/limit); produces a draw-list value a pass consumes.
class DrawListOp
{
public:
    explicit DrawListOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* list() const noexcept { return m_op->result(0U); }
    [[nodiscard]] AttrId all() const noexcept { return m_op->attr("all"); }
    [[nodiscard]] AttrId any() const noexcept { return m_op->attr("any"); }
    [[nodiscard]] AttrId none() const noexcept { return m_op->attr("none"); }
    [[nodiscard]] AttrId name() const noexcept { return m_op->attr("name"); }
    [[nodiscard]] AttrId cull() const noexcept { return m_op->attr("cull"); }
    [[nodiscard]] AttrId sort() const noexcept { return m_op->attr("sort"); }
    [[nodiscard]] AttrId limit() const noexcept { return m_op->attr("limit"); }

private:
    Operation* m_op;
};
// frame.graph - The whole-frame REGION container: a graph region holding the frame's resource declares, draw-list declares, and passes.
class GraphOp
{
public:
    explicit GraphOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] AttrId name() const noexcept { return m_op->attr("name"); }
    [[nodiscard]] AttrId schema() const noexcept { return m_op->attr("schema"); }

private:
    Operation* m_op;
};
// frame.history - The previous-frame read of a lifetime=history resource (ping-pong / TAA prev-frame); a distinct op ⇒ its own resource_root.
class HistoryOp
{
public:
    explicit HistoryOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* resource() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* prev() const noexcept { return m_op->result(0U); }
    [[nodiscard]] AttrId frames_back() const noexcept { return m_op->attr("frames_back"); }

private:
    Operation* m_op;
};
// frame.pass - A frame work-unit node: run the `executor` MECHANIC over the `resources` (tokened by `access`), inside a frame.graph.
class PassOp
{
public:
    explicit PassOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* resources() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] AttrId executor() const noexcept { return m_op->attr("executor"); }
    [[nodiscard]] AttrId name() const noexcept { return m_op->attr("name"); }
    [[nodiscard]] AttrId access() const noexcept { return m_op->attr("access"); }
    [[nodiscard]] AttrId for_each() const noexcept { return m_op->attr("for_each"); }
    [[nodiscard]] AttrId for_each_arg() const noexcept { return m_op->attr("for_each_arg"); }
    [[nodiscard]] AttrId queue() const noexcept { return m_op->attr("queue"); }
    [[nodiscard]] AttrId shader() const noexcept { return m_op->attr("shader"); }
    [[nodiscard]] AttrId kernel() const noexcept { return m_op->attr("kernel"); }
    [[nodiscard]] AttrId technique() const noexcept { return m_op->attr("technique"); }

private:
    Operation* m_op;
};

// -- builders (through the ordinary Context factories - no privileged construction). NOTE: a builder
// produces the MINIMUM arity on every variadic axis (operands / results / regions); build the full arity
// (extra variadic operands, N result values, N case regions) directly with `Context::create_operation`. --
[[nodiscard]] Operation* build_draw_list(Context& ctx, TypeId result_type = {});
[[nodiscard]] Operation* build_graph(Context& ctx, TypeId result_type = {});
[[nodiscard]] Operation* build_history(Context& ctx, Value* resource, TypeId result_type = {});
[[nodiscard]] Operation* build_pass(Context& ctx, Value* resources, AttrId executor, AttrId access, TypeId result_type = {});

// -- registration: self-registers the dialect + every op (traits + verifier), NO central edit (section 7) --
Dialect* register_frame_ops(Context& ctx);

// -- reflection: the op schema as queryable data - the SAME truth the wrapper/builder/verifier emit
// from (no parallel truth); the CR-D007 node editor + the committed frame.ops.json read it (section 122/161) --
[[nodiscard]] containers::ConstSpan<OpSchema> frame_op_schemas() noexcept;
} // namespace crd::ceir::frame
