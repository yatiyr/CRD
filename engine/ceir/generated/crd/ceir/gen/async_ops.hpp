#pragma once

// GENERATED by ceir_opgen.py from engine/ceir/ops/async.ceirop.toml - DO NOT EDIT (schema v1). Regenerate with
// `python tools/ceir_opgen/ceir_opgen.py`; the crd-ceir-opgen-drift ctest fails on drift.
//
// async - Structured-concurrency ops (sec 37): tokens / waits / joins / race / cancellation, host-only.

#include <crd/ceir/attr.hpp>
#include <crd/ceir/context.hpp>
#include <crd/ceir/dialect.hpp>
#include <crd/ceir/ir.hpp>
#include <crd/ceir/op_schema.hpp>
#include <crd/containers/span.hpp>

namespace crd::ceir::async
{
// -- op-kind identities (interned lazily against `ctx`, idempotent) --
[[nodiscard]] inline OpId await_kind(Context& ctx) { return ctx.intern_op("async", "await"); }
[[nodiscard]] inline OpId cancel_kind(Context& ctx) { return ctx.intern_op("async", "cancel"); }
[[nodiscard]] inline OpId join_kind(Context& ctx) { return ctx.intern_op("async", "join"); }
[[nodiscard]] inline OpId launch_kind(Context& ctx) { return ctx.intern_op("async", "launch"); }
[[nodiscard]] inline OpId race_kind(Context& ctx) { return ctx.intern_op("async", "race"); }
[[nodiscard]] inline OpId scope_kind(Context& ctx) { return ctx.intern_op("async", "scope"); }

// -- typed op wrappers (a thin view over an Operation*; the registered verifier enforces shape) --
// async.await - Wait for a token; produce the async body's yielded values.
class AwaitOp
{
public:
    explicit AwaitOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* token() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* values() const noexcept { return m_op->result(0U); }

private:
    Operation* m_op;
};
// async.cancel - Cancel a pending async operation (consumes its token).
class CancelOp
{
public:
    explicit CancelOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* token() const noexcept { return m_op->operand(0U); }

private:
    Operation* m_op;
};
// async.join - All-of: consume N tokens; produce one token that completes when all of them do.
class JoinOp
{
public:
    explicit JoinOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* tokens() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* token() const noexcept { return m_op->result(0U); }

private:
    Operation* m_op;
};
// async.launch - Structured async fork: run the region asynchronously and produce a completion token.
class LaunchOp
{
public:
    explicit LaunchOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* token() const noexcept { return m_op->result(0U); }

private:
    Operation* m_op;
};
// async.race - Any-of / select: consume N tokens; produce the index of the first ready.
class RaceOp
{
public:
    explicit RaceOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* tokens() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* index() const noexcept { return m_op->result(0U); }

private:
    Operation* m_op;
};
// async.scope - A structured-concurrency scope (sec 30): a region whose dynamic extent ends only when every token launched inside it is consumed.
class ScopeOp
{
public:
    explicit ScopeOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* results() const noexcept { return m_op->result(0U); }

private:
    Operation* m_op;
};

// -- builders (through the ordinary Context factories - no privileged construction). NOTE: a builder
// produces the MINIMUM arity on every variadic axis (operands / results / regions); build the full arity
// (extra variadic operands, N result values, N case regions) directly with `Context::create_operation`. --
[[nodiscard]] Operation* build_await(Context& ctx, Value* token, TypeId result_type = {});
[[nodiscard]] Operation* build_cancel(Context& ctx, Value* token, TypeId result_type = {});
[[nodiscard]] Operation* build_join(Context& ctx, Value* tokens, TypeId result_type = {});
[[nodiscard]] Operation* build_launch(Context& ctx, TypeId result_type = {});
[[nodiscard]] Operation* build_race(Context& ctx, Value* tokens, TypeId result_type = {});
[[nodiscard]] Operation* build_scope(Context& ctx, TypeId result_type = {});

// -- registration: self-registers the dialect + every op (traits + verifier), NO central edit (section 7) --
Dialect* register_async_ops(Context& ctx);

// -- reflection: the op schema as queryable data - the SAME truth the wrapper/builder/verifier emit
// from (no parallel truth); the CR-D007 node editor + the committed async.ops.json read it (section 122/161) --
[[nodiscard]] containers::ConstSpan<OpSchema> async_op_schemas() noexcept;
} // namespace crd::ceir::async
