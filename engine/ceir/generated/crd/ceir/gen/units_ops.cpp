// GENERATED by ceir_opgen.py from engine/ceir/ops/units.ceirop.toml - DO NOT EDIT (schema v1). Regenerate with
// `python tools/ceir_opgen/ceir_opgen.py`; the crd-ceir-opgen-drift ctest fails on drift.
//
// units - Physical-quantity boundary ops (section 17): strip a dimension tag at the raw/kernel boundary.

#include <crd/ceir/gen/units_ops.hpp>

#include <crd/ceir/effect.hpp>
#include <crd/ceir/semantics.hpp>
#include <crd/containers/span.hpp>

namespace crd::ceir::units
{
namespace
{
// verifiers - structural conformance (operand/result/region counts + required attrs & kinds), wired to
// Context::verify via register_op. Semantic verification (types/effects/domain) lands at CEIR-3/4.
[[nodiscard]] bool verify_erase(const Context& /*ctx*/, const Operation& op) noexcept
{
    if (op.num_operands() != 1U) { return false; }
    if (op.num_results() != 1U) { return false; }
    if (op.num_regions() != 0U) { return false; }
    return true;
}
} // namespace

Operation* build_erase(Context& ctx, Value* tagged, TypeId result_type)
{
    Value* operands[] = {tagged};
    Operation* const op = ctx.create_operation(erase_kind(ctx), containers::ConstSpan<Value*>(operands, 1U), 1U, result_type, 0U);
    return op;
}

Dialect* register_units_ops(Context& ctx)
{
    Dialect* const d = ctx.register_dialect("units");
    d->register_op("erase", {.traits = flags_of(OpTrait::Pure), .verify = &verify_erase, .effects = containers::ConstSpan<EffectRecord>{}, .determinism = DeterminismClass::Unspecified, .domain = EvalDomain::Unspecified});
    return d;
}

namespace
{
constexpr OperandInfo kEraseOperands[] = {{"tagged", "the dimension-tagged quantity value", false}};
constexpr ResultInfo kEraseResults[] = {{"raw", "the underlying raw value, dimension tag erased"}};

constexpr OpSchema kOpSchemas[] = {
    {"erase", "units.erase", "units", 1U, "Erase the physical-quantity dimension tag from a value at the raw/kernel boundary.", "units.erase(%tagged) -> %raw. A boundary marker: dimensional errors (section 17) are caught ABOVE this op; below it values are raw SI scalars. The operand-is-a-Quantity / result-is-its-underlying check lands with typed-operand verification at CEIR-4+ (like a 3b trait conformance, it has no producer op yet); the generator's arity/attr verifier is the correct scope here.",
     containers::ConstSpan<OperandInfo>(kEraseOperands, 1U), containers::ConstSpan<ResultInfo>(kEraseResults, 1U), containers::ConstSpan<AttrInfo>{},
     flags_of(OpTrait::Pure), 0U, containers::ConstSpan<EffectRecord>{}, DeterminismClass::Unspecified, EvalDomain::Unspecified, false, "", DeterminismClass::Unspecified},
};
} // namespace

containers::ConstSpan<OpSchema> units_op_schemas() noexcept { return containers::ConstSpan<OpSchema>(kOpSchemas, 1U); }
} // namespace crd::ceir::units
