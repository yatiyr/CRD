#pragma once

// GENERATED by ceir_opgen.py from engine/ceir/ops/units.ceirop.toml - DO NOT EDIT (schema v1). Regenerate with
// `python tools/ceir_opgen/ceir_opgen.py`; the crd-ceir-opgen-drift ctest fails on drift.
//
// units - Physical-quantity boundary ops (section 17): strip a dimension tag at the raw/kernel boundary.

#include <crd/ceir/attr.hpp>
#include <crd/ceir/context.hpp>
#include <crd/ceir/dialect.hpp>
#include <crd/ceir/ir.hpp>
#include <crd/ceir/op_schema.hpp>
#include <crd/containers/span.hpp>

namespace crd::ceir::units
{
// -- op-kind identities (interned lazily against `ctx`, idempotent) --
[[nodiscard]] inline OpId erase_kind(Context& ctx) { return ctx.intern_op("units", "erase"); }

// -- typed op wrappers (a thin view over an Operation*; the registered verifier enforces shape) --
// units.erase - Erase the physical-quantity dimension tag from a value at the raw/kernel boundary.
class EraseOp
{
public:
    explicit EraseOp(Operation* op) noexcept : m_op(op) {}
    [[nodiscard]] Operation* operation() const noexcept { return m_op; }
    [[nodiscard]] Value* tagged() const noexcept { return m_op->operand(0U); }
    [[nodiscard]] Value* raw() const noexcept { return m_op->result(0U); }

private:
    Operation* m_op;
};

// -- builders (through the ordinary Context factories - no privileged construction). NOTE: a builder
// produces the MINIMUM arity on every variadic axis (operands / results / regions); build the full arity
// (extra variadic operands, N result values, N case regions) directly with `Context::create_operation`. --
[[nodiscard]] Operation* build_erase(Context& ctx, Value* tagged, TypeId result_type = {});

// -- registration: self-registers the dialect + every op (traits + verifier), NO central edit (section 7) --
Dialect* register_units_ops(Context& ctx);

// -- reflection: the op schema as queryable data - the SAME truth the wrapper/builder/verifier emit
// from (no parallel truth); the CR-D007 node editor + the committed units.ops.json read it (section 122/161) --
[[nodiscard]] containers::ConstSpan<OpSchema> units_op_schemas() noexcept;
} // namespace crd::ceir::units
