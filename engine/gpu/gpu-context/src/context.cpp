// context.cpp — translation-unit anchor for the header-only crd-gpu-context interface (IGpuContext + GpuContextManager
// are inline in the header). Keeps crd-gpu-context a STATIC library matching the engine module pattern. ADR-0099.
#include <crd/gpu/context.hpp>
