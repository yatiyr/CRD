// crd-hesap-diff — translation-unit anchor (the module is header-only templates; this gives the static lib a TU).
#include <crd/hesap/diff/diff.hpp>

namespace crd::hesap::diff
{
// intentionally empty — keeps the archive non-empty for linkers that dislike empty .a files.
} // namespace crd::hesap::diff
