// crd-hesap-eigen anchor — Phase 3.1.6 v6. Gives the STATIC library a translation unit; the eigensolvers are
// header-only templates over LinearOp<T> (like crd-hesap-iterative). The `hesap.eigen.*` CLI registration
// lands at v6-z.

#include <crd/hesap/eigen/eigen.hpp>

namespace crd::hesap::eigen
{
// (intentionally empty — substrate spec is header-only; methods + CLI land per v6-a..z)
}
